"""``beava.cli`` — CLI entry point.

Reserves the ``beava`` console-script entry-point ID. The argparse
subcommand graph is wired by a follow-up plan.
"""
from __future__ import annotations


def main() -> None:
    """Placeholder entry point — no subcommands wired yet."""
    raise SystemExit(
        "beava.cli main() is not wired yet."
    )


__all__ = ["main"]
