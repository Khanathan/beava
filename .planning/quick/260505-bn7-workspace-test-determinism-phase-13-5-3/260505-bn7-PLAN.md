---
phase: quick
plan: 260505-bn7
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/beava-server/src/server.rs
  - crates/beava-server/src/wal_config.rs
  - crates/beava-server/src/apply_shard.rs
  - crates/beava-server/src/lib.rs
  - crates/beava-server/src/main.rs
  - crates/beava-server/src/testing.rs
  - crates/beava-server/tests/wal_env_var_tunables.rs
  - crates/beava-server/tests/phase12_8_metrics_endpoint.rs
  - crates/beava-server/tests/phase12_8_unbounded_op_in_lifetime_mode.rs
  - crates/beava-server/tests/phase13_4_reset_gated_env_var.rs
  - crates/beava-server/tests/phase12_08_drain_until_empty_test.rs
  - crates/beava-server/tests/phase12_08_response_batch_test.rs
  - crates/beava-server/tests/phase12_08_bytes_pool_test.rs
  - crates/beava-server/tests/phase13_5_3_no_env_var_pokes_in_tests.rs
autonomous: true
requirements: []
tags:
  - workspace-determinism
  - test-infra
  - env-var-plumbing
  - phase-13.5.3

must_haves:
  truths:
    - "`cargo test --workspace --features testing` passes 5 consecutive runs back-to-back GREEN."
    - "`pytest python/tests/v0/` passes 5 consecutive runs back-to-back at 89/89 GREEN."
    - "No `std::env::set_var(` or `env::set_var(` calls survive in `crates/beava-server/tests/` (architectural tripwire enforces)."
    - "`TestServerBuilder` exposes builder methods for every previously env-driven per-server config (`wal_buffers`, `wal_buffer_size_mb`, `wal_tick_ms`, `io_threads`, `test_mode`, `memory_governance_enforce`)."
    - "Production `main.rs` continues to read `BEAVA_*` env vars at config-load time; the operator-facing env interface is preserved unchanged."
    - "Two `TestServer::spawn()` calls in parallel never race on WAL-dir EEXIST (`tempfile::tempdir()` provides guaranteed-unique auto-cleanup paths)."
    - "`cargo clippy --workspace --all-targets --all-features -- -D warnings` clean."
    - "`cargo fmt --all --check` clean."
  artifacts:
    - path: "crates/beava-server/src/server.rs"
      provides: "Extended `ServerV18Config` with `wal_buffers`/`wal_buffer_size_mb`/`wal_tick_ms`/`io_threads`/`memory_governance_enforce` Option fields + `ServerV18Config::from_env()` constructor; hot-path env reads in `build_runtime_state{,_with_persistence}` + `default_io_threads` removed."
      contains: "fn from_env"
    - path: "crates/beava-server/src/wal_config.rs"
      provides: "`WalConfig::resolve(overrides)` accepts explicit overrides; env-reading variant moved into `ServerV18Config::from_env`; clamp/parse logic preserved verbatim."
      contains: "fn resolve"
    - path: "crates/beava-server/src/apply_shard.rs"
      provides: "`memory_gov_enforce_enabled` env read replaced by `AppState.memory_governance_enforce: bool` set at boot."
    - path: "crates/beava-server/src/main.rs"
      provides: "Production binary now calls `ServerV18Config::from_env()` then `ServerV18::bind_with_config(...)`; production env interface unchanged."
      contains: "ServerV18Config::from_env"
    - path: "crates/beava-server/src/testing.rs"
      provides: "`TestServerBuilder` gains `.wal_buffers(n)` / `.wal_buffer_size_mb(mb)` / `.wal_tick_ms(ms)` / `.io_threads(n)` / `.test_mode(b)` / `.memory_governance_enforce(b)` builder methods; default constructor uses `tempfile::tempdir()` for WAL+snapshot dirs and stores the `TempDir` handles inside `TestServer` for auto-cleanup."
      contains: "tempfile::tempdir"
    - path: "crates/beava-server/tests/phase13_5_3_no_env_var_pokes_in_tests.rs"
      provides: "Architectural tripwire test that walks `crates/beava-server/tests/` at runtime and fails on any `std::env::set_var(` or `env::set_var(` occurrence (mirrors `phase12_6_mio_only_dataplane.rs` shape)."
      contains: "set_var("
  key_links:
    - from: "crates/beava-server/src/main.rs"
      to: "crates/beava-server/src/server.rs::ServerV18Config::from_env"
      via: "Production config-load env-var resolution at boot"
      pattern: "ServerV18Config::from_env"
    - from: "crates/beava-server/src/server.rs::build_runtime_state_with_persistence"
      to: "crates/beava-server/src/wal_config.rs::WalConfig::resolve"
      via: "WAL config now resolved from struct overrides, not env"
      pattern: "WalConfig::resolve"
    - from: "crates/beava-server/src/testing.rs::TestServerBuilder"
      to: "crates/beava-server/src/server.rs::ServerV18::bind_with_config"
      via: "Per-server config plumbed via struct field, not process env"
      pattern: "bind_with_config"
    - from: "crates/beava-server/tests/phase13_5_3_no_env_var_pokes_in_tests.rs"
      to: "crates/beava-server/tests/"
      via: "Recursive walk + grep-after-comment-strip for `set_var(`"
      pattern: "set_var\\("
---

<objective>
Phase 13.5.3 — Workspace test determinism. Ship the Path A architectural fix (full plumb-through of `BEAVA_*` env vars from process-global to per-`ServerV18Config` struct fields) so that `cargo test --workspace --features testing` is deterministic across consecutive runs and 8 known *_per_user_high_volume Python failures stop rotating.

Purpose: Pulled forward from Phase 999.1 backlog because workspace test flakiness now gates v0 OSS launch (the v0 critical path is 13.7.5 / 13.7.6 / 13.8 → ship; flaky workspace gates would land on top of that and erode confidence in the GA tag). Replicates the proven `tcp_max_frame_bytes` plumbing pattern from commit `acac4254` (Phase 13.5.2-postclose) for the remaining four env-var families.

Output: Five new struct fields on `ServerV18Config`; one new `ServerV18Config::from_env()` constructor; `main.rs` migrated to `bind_with_config`; six TestServerBuilder methods; tempfile-based WAL/snapshot dir defaults; one architectural tripwire test asserting no `set_var` survives in `crates/beava-server/tests/`. Acceptance criteria: 5/5 cargo test --workspace + 5/5 pytest v0 GREEN consecutively.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/ideas/workspace-test-determinism.md

<!-- Reference fix already shipped — replicate this pattern for the four remaining env-var families. -->
@crates/beava-server/src/server.rs
@crates/beava-server/src/wal_config.rs
@crates/beava-server/src/testing.rs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From `crates/beava-server/src/server.rs` (current shape — extend, do not replace):

```rust
#[derive(Debug, Clone)]
pub struct ServerV18Config {
    pub persistence: Persistence,
    pub test_mode: bool,
    pub tcp_max_frame_bytes: u32,
    // Phase 13.5.3: ADD the following Option fields. None == "use the
    // documented default"; Some(v) == "use v verbatim, do not consult env".
    // pub wal_buffers: Option<usize>,
    // pub wal_buffer_size_mb: Option<usize>,
    // pub wal_tick_ms: Option<u64>,
    // pub io_threads: Option<usize>,
    // pub memory_governance_enforce: Option<bool>,
}
```

Production env-var read site already established in `crates/beava-core/src/config.rs:267` for `BEAVA_TCP_MAX_FRAME_BYTES`:

```rust
if let Ok(v) = std::env::var("BEAVA_TCP_MAX_FRAME_BYTES") {
    cfg.tcp.max_frame_bytes = v.parse().map_err(|e: std::num::ParseIntError| {
        ConfigError::Validation { field: "tcp.max_frame_bytes", reason: format!(...) }
    })?;
}
```

Replicate this exact shape inside `ServerV18Config::from_env()` for the five new env vars. Clamp/range logic for WAL fields lives in `wal_config.rs` (`parse_clamp_usize`/`parse_clamp_u64`); preserve it verbatim — only the function signature changes from "read from process env" to "read from `&self` struct overrides + fall back to defaults".

From `crates/beava-server/src/wal_config.rs` (current shape):

```rust
impl WalConfig {
    pub const DEFAULT_BUFFERS: usize = 4;
    pub const DEFAULT_BUFFER_SIZE_MB: usize = 32;
    pub const DEFAULT_TICK_MS: u64 = 20;
    pub const BUFFERS_MIN: usize = 2;
    pub const BUFFERS_MAX: usize = 32;
    pub const BUFFER_SIZE_MB_MIN: usize = 4;
    pub const BUFFER_SIZE_MB_MAX: usize = 256;
    pub const TICK_MS_MIN: u64 = 1;
    pub const TICK_MS_MAX: u64 = 1000;

    /// Phase 13.5.3 RENAME from `resolve_from_env` to `resolve(overrides)`.
    /// Production main.rs reads env at config-load and passes Some(values);
    /// tests pass None for default OR Some(value) for explicit override.
    pub fn resolve(overrides: WalConfigOverrides) -> Self { ... }
}

pub struct WalConfigOverrides {
    pub buffers: Option<usize>,
    pub buffer_size_mb: Option<usize>,
    pub tick_ms: Option<u64>,
}
```

Reference architectural-test pattern (mirror this exact structure for `phase13_5_3_no_env_var_pokes_in_tests.rs`):

`crates/beava-server/tests/phase12_6_mio_only_dataplane.rs` — uses `workspace_root()` + `collect_rs_files()` + `strip_line_comments()` helpers; walks a target dir; greps stripped content for forbidden patterns; emits a violations Vec; asserts empty.

For Phase 13.5.3 the target dir is `crates/beava-server/tests/` (NOT src/), the forbidden patterns are `std::env::set_var(` and `env::set_var(`, the allowlist is just the architectural test file itself.

Test-rewrite map (each test currently uses `set_var`; rewrite to use new TestServerBuilder methods):

| Test file | Current set_var call | New builder method |
|---|---|---|
| `phase12_08_drain_until_empty_test.rs` | `BEAVA_IO_THREADS=n_workers` | `.io_threads(n_workers)` |
| `phase12_08_response_batch_test.rs` | `BEAVA_IO_THREADS=n_workers` | `.io_threads(n_workers)` |
| `phase12_08_bytes_pool_test.rs` | `BEAVA_IO_THREADS=n_workers` | `.io_threads(n_workers)` |
| `phase12_8_metrics_endpoint.rs` | `BEAVA_MEMORY_GOV_ENFORCE=0` | `.memory_governance_enforce(false)` |
| `phase12_8_unbounded_op_in_lifetime_mode.rs` | `BEAVA_MEMORY_GOV_ENFORCE=1` | `.memory_governance_enforce(true)` |
| `phase13_4_reset_gated_env_var.rs` | `BEAVA_TEST_MODE=1` / `=true` | `.test_mode(true)` |
| `wal_env_var_tunables.rs` | `BEAVA_WAL_BUFFERS/...` | **MOVE** to `wal_config.rs` `#[cfg(test)] mod tests` (it tests env-parsing — legitimate; just relocate from `tests/` to unit tests so the architectural tripwire doesn't false-positive) |

Note on `wal_env_var_tunables.rs`: this test EXERCISES `ServerV18Config::from_env()` (or `WalConfig::resolve_from_env` once relocated to a private helper). It must continue to set env vars to validate the parse/clamp logic. The right home is `crates/beava-server/src/server.rs` `#[cfg(test)] mod tests` for the `from_env()` constructor — same crate, but in the `src/` tree where the architectural tripwire does not walk. The 3 existing tests (default / overrides apply / clamp ranges) move verbatim, with `WalConfig::resolve_from_env()` swapped to `ServerV18Config::from_env()` field reads.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plumb env-var overrides through ServerV18Config + migrate production main.rs (red→green)</name>
  <files>
    crates/beava-server/src/server.rs,
    crates/beava-server/src/wal_config.rs,
    crates/beava-server/src/apply_shard.rs,
    crates/beava-server/src/lib.rs,
    crates/beava-server/src/main.rs
  </files>
  <behavior>
    - Test 1 (red): `ServerV18Config::default()` returns `wal_buffers: None, wal_buffer_size_mb: None, wal_tick_ms: None, io_threads: None, memory_governance_enforce: None`.
    - Test 2 (red): `ServerV18Config::from_env()` reads `BEAVA_WAL_BUFFERS`/`BEAVA_WAL_BUFFER_SIZE_MB`/`BEAVA_WAL_TICK_MS`/`BEAVA_IO_THREADS`/`BEAVA_TEST_MODE`/`BEAVA_MEMORY_GOV_ENFORCE` and populates the corresponding fields. Unset env → field stays `None` (or `false` for `test_mode`); set+parseable → `Some(parsed)` (or `true` for `test_mode == "1"`); out-of-range WAL values clamp to `[MIN, MAX]` per existing `wal_config.rs` constants.
    - Test 3 (red): `WalConfig::resolve(WalConfigOverrides { buffers: Some(8), buffer_size_mb: Some(64), tick_ms: Some(100) })` returns `WalConfig { buffers: 8, buffer_size_mb: 64, tick_ms: 100 }` WITHOUT consulting any env var (set `BEAVA_WAL_BUFFERS=99999` in the test process before the call to prove the override wins).
    - Test 4 (red): existing 3 tests in `wal_env_var_tunables.rs` (default / overrides apply / clamp ranges) keep passing after relocation to `ServerV18Config::from_env()` unit tests in `server.rs::#[cfg(test)] mod tests`. Existing in-test mutex pattern preserved.
    - Test 5 (red): grep gate — `grep -rE 'std::env::var\("BEAVA_(WAL_BUFFERS|WAL_BUFFER_SIZE_MB|WAL_TICK_MS|IO_THREADS|TEST_MODE|MEMORY_GOV_ENFORCE)"\)' crates/beava-server/src/{apply_shard,server,wal_config}.rs | grep -v '^.*://' | grep -c .` returns `0` (lines stripped of comments, all five env-var families absent from hot-path code). The ONLY file allowed to read these env vars is the new `from_env()` function (which lives in `server.rs` — gate uses `grep -v 'fn from_env'` filter, or stripping by surrounding function. Simplest: assert the count of `env::var("BEAVA_WAL_BUFFERS")` ANYWHERE in those three files post-strip is exactly 1, located inside `from_env`. Use `grep -c 'env::var.*"BEAVA_WAL_BUFFERS"'` against the post-comment-strip stream and assert == 1).
  </behavior>
  <action>
    Per CLAUDE.md §TDD Discipline item #1: ship as TWO atomic commits — red commit (failing tests landing) THEN green commit (impl that makes them pass).

    **RED commit** (`test(13.5.3): plumb env-var overrides through ServerV18Config — failing tests`):

    1. Edit `crates/beava-server/src/server.rs`:
       - Add `#[cfg(test)] mod env_var_plumbing_tests { ... }` at the bottom of the file with Tests 1-4 above. Test 4 imports the 3 tests verbatim from `wal_env_var_tunables.rs` (default config / env overrides apply / clamp ranges) but switches to calling `ServerV18Config::from_env()` and asserting `cfg.wal_buffers == Some(8)` etc. Use the existing `static ENV_LOCK: std::sync::Mutex<()> = std::sync::Mutex::new(());` mutex pattern from `wal_env_var_tunables.rs` (sync, not tokio — these are `#[test]`).
       - Tests 1-4 require `ServerV18Config::from_env`, the new Option fields, and `WalConfigOverrides` to compile. To make the RED commit COMPILE-FAIL (not link-fail / panic at runtime), add stub signatures:
         ```rust
         impl ServerV18Config {
             pub fn from_env() -> Self { unimplemented!("Phase 13.5.3 — see PLAN") }
         }
         ```
         and add the new fields to the struct with `Option` defaulting to None in `Default::default()`. Stub `WalConfigOverrides` struct in `wal_config.rs` with `pub fn resolve(_o: WalConfigOverrides) -> Self { unimplemented!() }`. The unit tests will then compile + panic at runtime → RED.
    2. Run `cargo test -p beava-server --features testing env_var_plumbing_tests -- --test-threads=1` to confirm the tests compile + fail with `unimplemented!()` panics. If they pass, the test isn't actually red — fix the test.
    3. Commit: `test(13.5.3): plumb env-var overrides through ServerV18Config — failing tests`.

    **GREEN commit** (`feat(13.5.3): plumb env-var overrides through ServerV18Config`):

    1. Edit `crates/beava-server/src/server.rs`:
       - Remove `unimplemented!()` stubs.
       - `ServerV18Config::from_env()` reads env vars at call-time:
         - `BEAVA_WAL_BUFFERS` / `BEAVA_WAL_BUFFER_SIZE_MB` / `BEAVA_WAL_TICK_MS` parsed via `wal_config.rs` helpers (or new wrapping helpers) — preserve clamp + WARN log behavior verbatim. None when unset, Some(clamped) when set+parseable.
         - `BEAVA_IO_THREADS` parsed as `Option<usize>`; preserve `n.max(1)` clamp from existing `default_io_threads`.
         - `BEAVA_TEST_MODE` written into existing `test_mode: bool` field — `cfg.test_mode = env::var("BEAVA_TEST_MODE").map(|v| v == "1").unwrap_or(false)`. Strict `== "1"` per Phase 13.4 D-03 USER-LOCKED (no truthy coercion).
         - `BEAVA_MEMORY_GOV_ENFORCE` written into `memory_governance_enforce: Option<bool>` — `Some(false)` when env == "0", `Some(true)` when set to anything else, `None` when unset (= "use default ON" per Phase 12.8 D-03).
       - `Default::default()` returns all `None` for the new fields + `test_mode: false` + existing tcp_max_frame_bytes default.
       - Update `bind_with_config` to NOT consult env for `BEAVA_TEST_MODE`: replace `let effective_test_mode = cfg.test_mode || std::env::var("BEAVA_TEST_MODE").map(|v| v == "1").unwrap_or(false);` with just `let effective_test_mode = cfg.test_mode;` (env reading moved to `from_env`).
       - Update `build_runtime_state` (the legacy `bind()` path used by main.rs pre-migration): switch from reading `BEAVA_TEST_MODE` env directly (line ~506-508) to accepting `effective_test_mode: bool` as a parameter.
       - Update `build_runtime_state_with_persistence` to take a new `&ServerV18Config` (or unpacked overrides) param and pass the WAL overrides to `WalConfig::resolve(...)` instead of `WalConfig::resolve_from_env()`.
       - Replace `default_io_threads()` with `resolve_io_threads(override_: Option<usize>)` — `override_.map(|n| n.max(1)).unwrap_or_else(|| { let p = std::thread::available_parallelism().map(|n| n.get()).unwrap_or(4); std::cmp::max(2, p / 4) })`. Plumb through to `WorkerConfig.io_threads` via the existing wiring (search for `default_io_threads()` call sites and pass `cfg.io_threads` through `ServerV18State`).
    2. Edit `crates/beava-server/src/wal_config.rs`:
       - Add `pub struct WalConfigOverrides { pub buffers: Option<usize>, pub buffer_size_mb: Option<usize>, pub tick_ms: Option<u64> }`.
       - Add `impl WalConfig { pub fn resolve(overrides: WalConfigOverrides) -> Self { ... } }` — clamps via existing `usize.clamp(MIN, MAX)` / `u64.clamp(MIN, MAX)` calls; falls back to `DEFAULT_*` constants when override is None. Emit existing WARN logs on out-of-range overrides (preserve operator visibility).
       - Keep `WalConfig::resolve_from_env()` as a thin wrapper that builds `WalConfigOverrides` from env vars (used by the migrated `from_env` chain — single consolidated env-read site). Or remove it entirely and inline the env-reading shim into `ServerV18Config::from_env`. PREFER inlining (one less indirection); update doc comments accordingly.
    3. Edit `crates/beava-server/src/apply_shard.rs`:
       - Delete the inline `fn memory_gov_enforce_enabled() -> bool { std::env::var("BEAVA_MEMORY_GOV_ENFORCE").ok().as_deref() != Some("0") }` (lines ~195-205).
       - Add a `memory_governance_enforce: bool` field on `AppState` (or hang it off `ApplyShard` directly — whichever is closer to the apply-time read site). Set at boot in `build_runtime_state_with_persistence`: `app_state_inner.memory_governance_enforce = cfg.memory_governance_enforce.unwrap_or(true)` (default-ON post Plan 12.8-06 flip).
       - Replace the `if memory_gov_enforce_enabled()` callsite with `if self.state.app_state.memory_governance_enforce`.
    4. Edit `crates/beava-server/src/lib.rs`: re-export `ServerV18Config` (already exported via `pub use server::*` likely; verify). If `WalConfigOverrides` needs to be public for `bind_with_config` callers, add a re-export.
    5. Edit `crates/beava-server/src/main.rs`:
       - Replace `let server = ServerV18::bind(http_addr, tcp_addr, admin_addr).await...` with `let server = ServerV18::bind_with_config(http_addr, Some(tcp_addr), admin_addr, ServerV18Config::from_env()).await...`.
       - `cfg.durability.wal_dir` etc still flow into `serve_with_dirs` as before (those are not env-tunable in this scope; `BEAVA_WAL_DIR` already plumbed via `crates/beava-core/src/config.rs::Config::from_env`).
       - The `ServerV18Config::from_env()` populates `tcp_max_frame_bytes` from `cfg.tcp.max_frame_bytes` field already — verify the from_env() reads the right Config struct (or simply reads BEAVA_TCP_MAX_FRAME_BYTES once; the existing `crates/beava-core/src/config.rs:267` read can stay since it populates `Config.tcp.max_frame_bytes` which TestServer uses; ServerV18Config::from_env reads it independently for the production-binary boot path. Two reads of the same env var at boot = harmless; fix later if it bothers).
    6. Run `cargo test -p beava-server --features testing env_var_plumbing_tests -- --test-threads=1` — all 5 tests GREEN.
    7. Run `cargo test --workspace --features testing` — must pass (every existing test that previously read these env vars still works because `wal_env_var_tunables.rs` and the `BEAVA_TEST_MODE` / `BEAVA_MEMORY_GOV_ENFORCE` setting tests are still using set_var → set_var still reaches `from_env()` AT BOOT or `bind_with_config`'s test_mode resolution. They're left unchanged in this task; Task 2 rewrites them).
    8. Run `cargo clippy --workspace --all-targets --all-features -- -D warnings` + `cargo fmt --all --check`.
    9. Commit: `feat(13.5.3): plumb env-var overrides through ServerV18Config`.

    **PRESERVE VERBATIM**: clamp ranges + WARN log structure + `BEAVA_TEST_MODE == "1"` strict check + Phase 12.8 D-03 default-ON memory governance + production env interface (operators still set `BEAVA_*` env vars; `from_env()` reads them at boot just like the legacy code did).
  </action>
  <verify>
    <automated>cargo test -p beava-server --features testing env_var_plumbing_tests -- --test-threads=1 && cargo test -p beava-server --features testing wal_env_var_tunables -- --test-threads=1 && cargo test --workspace --features testing && cargo clippy --workspace --all-targets --all-features -- -D warnings && cargo fmt --all --check && [ "$(cd crates/beava-server/src && grep -hE 'env::var\("BEAVA_(WAL_BUFFERS|WAL_BUFFER_SIZE_MB|WAL_TICK_MS|IO_THREADS|TEST_MODE|MEMORY_GOV_ENFORCE)"\)' apply_shard.rs build_runtime_state* 2>/dev/null | grep -v '^[[:space:]]*//' | wc -l)" = "0" ]</automated>
  </verify>
  <done>
    `ServerV18Config` extended with five new override fields + `from_env()` constructor; `main.rs` migrated to `bind_with_config`; hot-path env reads in `apply_shard.rs::memory_gov_enforce_enabled` + `default_io_threads` + `build_runtime_state*` `BEAVA_TEST_MODE` shim removed; `wal_config.rs::resolve(overrides)` accepts explicit overrides; production env-var interface preserved at boot. All workspace tests still passing (Task 2 will rewrite the test files that still use set_var to use the new builder methods).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TestServerBuilder methods + tempfile WAL/snapshot dirs + rewrite tests off set_var (red→green)</name>
  <files>
    crates/beava-server/src/testing.rs,
    crates/beava-server/tests/wal_env_var_tunables.rs,
    crates/beava-server/tests/phase12_8_metrics_endpoint.rs,
    crates/beava-server/tests/phase12_8_unbounded_op_in_lifetime_mode.rs,
    crates/beava-server/tests/phase13_4_reset_gated_env_var.rs,
    crates/beava-server/tests/phase12_08_drain_until_empty_test.rs,
    crates/beava-server/tests/phase12_08_response_batch_test.rs,
    crates/beava-server/tests/phase12_08_bytes_pool_test.rs
  </files>
  <behavior>
    - Test 1 (red): `TestServerBuilder::new().wal_buffers(8).wal_buffer_size_mb(64).wal_tick_ms(100).io_threads(4).test_mode(true).memory_governance_enforce(false).spawn().await` returns a TestServer that internally booted with those exact values (verifiable via debug logs: search for `wal.config.resolved buffers=8 buffer_size_mb=64 tick_ms=100`, `server.test_mode_enabled` log line for test_mode, etc) WITHOUT the test setting any env var.
    - Test 2 (red): two `TestServer::spawn()` calls back-to-back in a tokio test (one after the other, sequential) succeed without WAL-EEXIST race. Test asserts both servers' `wal_dir` paths differ. (Stronger test for parallelism: use `tokio::join!` to spawn 4 TestServers concurrently and assert all 4 succeed — exercises the `tempfile::tempdir()` race-free guarantee.)
    - Test 3 (red): `TestServer` drop releases the `TempDir` handle (i.e., the WAL dir path is removed from disk after `ts.shutdown().await + drop(ts)`). Verify by capturing `ts.builder()._wal_dir_path()` accessor (or inspect via reflection) BEFORE drop, then assert `!path.exists()` AFTER drop+sleep. (If `_wal_dir_path()` accessor too invasive, skip Test 3 — `tempfile::TempDir` auto-cleanup is well-tested upstream. Just assert no panics on drop.)
    - Test 4 (red): the 6 existing test files listed in `<files>` no longer contain `std::env::set_var(` / `env::set_var(` calls (grep gate).
    - All previously passing tests in those 6 files continue to pass after the rewrite (semantic preservation).
  </behavior>
  <action>
    Per CLAUDE.md §TDD Discipline item #1: ship as TWO atomic commits — red commit (failing tests + new builder method signatures stubbed) THEN green commit (impl + test rewrites).

    **RED commit** (`test(13.5.3): TestServerBuilder per-server config methods + tempdir — failing tests`):

    1. Edit `crates/beava-server/src/testing.rs`:
       - Add stub builder methods:
         ```rust
         impl TestServerBuilder {
             pub fn wal_buffers(mut self, n: usize) -> Self { self.wal_buffers_override = Some(n); self }
             pub fn wal_buffer_size_mb(mut self, mb: usize) -> Self { self.wal_buffer_size_mb_override = Some(mb); self }
             pub fn wal_tick_ms(mut self, ms: u64) -> Self { self.wal_tick_ms_override = Some(ms); self }
             pub fn io_threads(mut self, n: usize) -> Self { self.io_threads_override = Some(n); self }
             pub fn test_mode(mut self, b: bool) -> Self { self.test_mode_override = b; self }
             pub fn memory_governance_enforce(mut self, b: bool) -> Self { self.memory_governance_enforce_override = Some(b); self }
         }
         ```
         Add the corresponding fields on `TestServerBuilder` initialized to `None` / `false` in `Default`. Make `spawn()` IGNORE these fields for now (red — values not actually plumbed) so Test 1 fails by NOT seeing the configured values reflected at runtime.
       - Add a stub `#[cfg(test)] mod testserver_builder_phase_13_5_3_tests` with Tests 1-3 from above. Use the admin `/metrics` endpoint or a `test_mode_observable` accessor on `TestServer` to verify config values (e.g., expose `pub fn effective_test_mode(&self) -> bool { self.app_state.effective_test_mode }` since test_mode is observable via OP_RESET acceptance — call OP_RESET and assert it succeeds when test_mode(true) is set).
       - Concrete Test 1 shape: spawn with `.test_mode(true).memory_governance_enforce(false)`, then POST `/reset` with empty body and assert 200 OK + `body["reset"] == true`. Without `.test_mode(true)` plumbed, `/reset` returns 403; test fails RED.
       - Concrete Test 2 shape: 4× `tokio::spawn(async { TestServer::spawn().await })` joined with `tokio::join!`; assert all 4 return `Ok`.
    2. Run `cargo test -p beava-server --features testing testserver_builder_phase_13_5_3_tests -- --test-threads=1` — must FAIL (Test 1 fails because `/reset` returns 403; the builder method existed but spawn didn't plumb the value).
    3. Commit: `test(13.5.3): TestServerBuilder per-server config methods + tempdir — failing tests`.

    **GREEN commit** (`feat(13.5.3): plumb TestServerBuilder per-server config to ServerV18Config + tempfile dirs + rewrite tests off set_var`):

    1. Edit `crates/beava-server/src/testing.rs`:
       - Update `TestServerBuilder::spawn()` to construct a `ServerV18Config { test_mode: self.test_mode_override, memory_governance_enforce: self.memory_governance_enforce_override, wal_buffers: self.wal_buffers_override, wal_buffer_size_mb: self.wal_buffer_size_mb_override, wal_tick_ms: self.wal_tick_ms_override, io_threads: self.io_threads_override, tcp_max_frame_bytes: self.cfg.tcp.max_frame_bytes, persistence: Persistence::Disk { wal_dir: ..., snapshot_dir: ..., sync_mode: SyncMode::Periodic } }` and call `ServerV18::bind_with_config(http_addr, Some(tcp_addr_socket), admin_addr, cfg)` instead of `bind_with_state`.
         - Note: `bind_with_state` already exists; refactor `spawn` to use `bind_with_config` so the tests exercise the same code path as production main.rs. The `bind_with_state` method can stay for back-compat (callers other than TestServerBuilder may use it; check `cargo grep`).
       - Swap `TestServerBuilder::default()`'s WAL+snapshot dir construction from `temp_dir + (pid, counter)` to `tempfile::tempdir()`. Store both `TempDir` handles on `TestServerBuilder`. After `bind_with_config` consumes the path, transfer the `TempDir` handles to `TestServer` so they live until server drop. Add `_wal_tempdir: Option<tempfile::TempDir>` and `_snap_tempdir: Option<tempfile::TempDir>` private fields on `TestServer` (underscore-prefixed; held for RAII only).
       - The `.wal_dir(custom_path)` builder method (already exists, used by some tests for explicit path control) sets `self._wal_tempdir = None` (caller manages cleanup) and overrides the path. Same for `.snapshot_dir(...)`.
    2. Run `cargo test -p beava-server --features testing testserver_builder_phase_13_5_3_tests -- --test-threads=1` — Tests 1-3 GREEN.
    3. Rewrite the 6 test files to drop `set_var`:

       **`wal_env_var_tunables.rs`**: This file's tests live in `tests/` but exercise env-var parsing — they MUST move to unit tests inside `server.rs` `#[cfg(test)] mod env_var_plumbing_tests` (already done by Task 1). DELETE `crates/beava-server/tests/wal_env_var_tunables.rs` entirely. (Task 1's red commit landed the unit tests; the integration version is now redundant.)

       **`phase12_08_drain_until_empty_test.rs`** (line 33): replace `std::env::set_var("BEAVA_IO_THREADS", n_workers.to_string());` with `.io_threads(n_workers)` on the TestServerBuilder chain. The test currently uses raw `ServerV18::bind` with no config; rewrite to use `TestServerBuilder::new().io_threads(n_workers).spawn()` (or `ServerV18::bind_with_config` directly with `ServerV18Config { io_threads: Some(n_workers), ..default() }`).

       **`phase12_08_response_batch_test.rs`** (line 36): same shape as above.

       **`phase12_08_bytes_pool_test.rs`** (line 35): same shape as above.

       **`phase12_8_metrics_endpoint.rs`** (lines 53-58 `enforce_off()`/`enforce_zero()` + line 454 `test_env_var_zero_disables_enforcement`): the test asserts `BEAVA_MEMORY_GOV_ENFORCE=0` accepts a histogram-without-buckets register payload. Rewrite: spawn TestServer with `.memory_governance_enforce(false)` and assert the same behavior. The helper `enforce_zero()`/`enforce_off()` + `ENV_LOCK` mutex become DEAD CODE — delete them. Test name can stay (`test_env_var_zero_disables_enforcement`) — historical reference is fine, but rename to `test_memory_governance_enforce_false_disables_enforcement` for clarity.

       **`phase12_8_unbounded_op_in_lifetime_mode.rs`** (line 57): rewrite `enforce_on()` (sets `BEAVA_MEMORY_GOV_ENFORCE=1`) to spawn TestServer with `.memory_governance_enforce(true)`. The `ENV_LOCK` tokio mutex becomes dead code — delete it. The doc comment block at the top mentioning "per-call env var read" is stale; update.

       **`phase13_4_reset_gated_env_var.rs`** (lines 192, 246, 286): three tests use `BEAVA_TEST_MODE=1` / `=true`. The two "env var enables" tests rewrite to use `.test_mode(true)`. The third test (`env_var_value_other_than_1_does_not_enable`) tests env-VAR-string-parsing semantics — that test belongs in Task 1's `from_env()` unit tests now (move it to `server.rs::env_var_plumbing_tests`) since with the new architecture the truthy-coercion check happens AT `from_env()` boot-time, not at server runtime. The remaining tests use `.test_mode(b)` builder.
       - The `EnvGuard` struct + `serial_test::serial` attributes become dead code in this file — delete. (`#[serial_test::serial]` was a workaround for env contamination; with no env, parallelism is safe.)
    4. Run `cargo test --workspace --features testing` — all green.
    5. Run `cargo clippy --workspace --all-targets --all-features -- -D warnings` + `cargo fmt --all --check`.
    6. Run the 5-consecutive-runs gate: `for i in 1 2 3 4 5; do cargo test --workspace --features testing 2>&1 | tail -5; done` — all 5 iterations PASS.
    7. Commit: `feat(13.5.3): plumb TestServerBuilder per-server config to ServerV18Config + tempfile dirs + rewrite tests off set_var`.

    **NOTE on `serial_test::serial`**: keep `#[serial_test::serial]` only on the unit tests that REMAIN env-coupled (i.e., the `from_env()` parse tests inside `server.rs` `#[cfg(test)] mod`). Strip it from rewritten integration tests since they no longer touch process env.

    **OUT OF SCOPE**: do NOT modify `crates/beava-core/src/config.rs:267` `BEAVA_TCP_MAX_FRAME_BYTES` env read (already plumbed in `acac4254`). Do NOT touch `crates/beava-server/src/http_admin.rs` (admin sidecar isolation per `project_phase18_no_dual_runtime`).
  </action>
  <verify>
    <automated>cargo test -p beava-server --features testing testserver_builder_phase_13_5_3_tests -- --test-threads=1 && cargo test --workspace --features testing && for i in 1 2 3 4 5; do cargo test --workspace --features testing 2>&1 | grep -E '(test result|FAILED)' | tail -3 || exit 1; done && cargo clippy --workspace --all-targets --all-features -- -D warnings && cargo fmt --all --check && [ "$(grep -rEc 'std::env::set_var\(|env::set_var\(' crates/beava-server/tests/phase12_08_drain_until_empty_test.rs crates/beava-server/tests/phase12_08_response_batch_test.rs crates/beava-server/tests/phase12_08_bytes_pool_test.rs crates/beava-server/tests/phase12_8_metrics_endpoint.rs crates/beava-server/tests/phase12_8_unbounded_op_in_lifetime_mode.rs crates/beava-server/tests/phase13_4_reset_gated_env_var.rs 2>/dev/null | grep -v ':0$' | wc -l)" = "0" ] && [ ! -f crates/beava-server/tests/wal_env_var_tunables.rs ]</automated>
  </verify>
  <done>
    `TestServerBuilder` exposes 6 new builder methods (`wal_buffers` / `wal_buffer_size_mb` / `wal_tick_ms` / `io_threads` / `test_mode` / `memory_governance_enforce`); WAL+snapshot dirs default to `tempfile::tempdir()` with `TempDir` handles owned by `TestServer` for auto-cleanup; 6 test files no longer call `set_var`; `wal_env_var_tunables.rs` deleted (its tests moved to unit tests in Task 1); 5 consecutive `cargo test --workspace --features testing` runs all GREEN; clippy + fmt clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Architectural tripwire — assert no `set_var` in `crates/beava-server/tests/` (red→green)</name>
  <files>
    crates/beava-server/tests/phase13_5_3_no_env_var_pokes_in_tests.rs
  </files>
  <behavior>
    - Test 1: walk `crates/beava-server/tests/` recursively, collect all `.rs` files, strip line+doc comments, and grep for `std::env::set_var(` and `env::set_var(`. Allowlist contains only `phase13_5_3_no_env_var_pokes_in_tests.rs` itself (the test file is allowed to mention the pattern in its docstring + assertion strings — that's allowed because `strip_line_comments` removes the docstrings, but the literal `set_var(` inside `let pattern = "set_var("` source code line would survive comment-stripping and trigger a false positive on the test file itself).
    - Test 2 (sanity): assert that exactly the architectural-test file is allowlisted, and that the test correctly catches a planted violation (manual probe procedure documented in module-level docstring, mirroring `phase12_6_mio_only_dataplane.rs` Probe 1/Probe 2 doc).
  </behavior>
  <action>
    Per CLAUDE.md §TDD Discipline item #1: ship as TWO atomic commits.

    **RED commit** (`test(13.5.3): architectural tripwire — assert no env::set_var in beava-server/tests/`):

    1. Create `crates/beava-server/tests/phase13_5_3_no_env_var_pokes_in_tests.rs` mirroring the `phase12_6_mio_only_dataplane.rs` shape:
       - Module-level `//!` doc explaining: "Phase 13.5.3 architectural tripwire. The mio data-plane stack reads `BEAVA_*` env vars at config-load time (production `main.rs`) ONLY. Per-test config is plumbed via `TestServerBuilder` builder methods, not via `std::env::set_var`. Any new `set_var` call in `crates/beava-server/tests/` is a regression that re-introduces the parallel-test determinism bug killed in Phase 13.5.3 / pulled forward from Phase 999.1 backlog."
       - Verification probe doc-block mirroring Phase 12.6 Probe 1/2 — instructing future maintainers how to plant a violation, run the test, confirm RED, revert.
       - `fn workspace_root()` + `fn collect_rs_files(dir)` + `fn strip_line_comments(src)` helpers — copy-paste verbatim from `phase12_6_mio_only_dataplane.rs` (or factor to a shared `tests/_arch_helpers.rs` if Cargo permits — preferred is copy-paste for now to avoid module-graph friction; that's how `phase12_6` and `phase12_7` chose).
       - `#[test] fn no_env_set_var_in_beava_server_tests()`: walks `crates/beava-server/tests/`, strips comments, greps for `"std::env::set_var("` and `"env::set_var("`, allowlists `phase13_5_3_no_env_var_pokes_in_tests.rs` (basename match). Asserts violations.is_empty() with a panic message including the violations Vec and a pointer to the architectural commitment.
       - `#[test] fn sanity_test_file_contains_grep_pattern()`: ensures the test file itself contains the literal grep patterns it searches for (i.e., the test isn't silently a no-op). Read the test file's own source, post-strip, assert it contains `"std::env::set_var("` and `"env::set_var("` strings (in the assertion-error-message string literals).
    2. Run `cargo test -p beava-server --test phase13_5_3_no_env_var_pokes_in_tests` — this MUST be GREEN already after Task 2 stripped all set_vars. So this isn't strictly a RED commit in the behavior-driven sense; it's a confirmation-test commit per `phase12_6_mio_only_dataplane.rs` precedent.

       **HOWEVER** to satisfy CLAUDE.md §TDD Discipline item #1 (red→green per task), simulate RED via Probe-1: temporarily `git stash` Task 2's set_var removals (or just revert one file: `git checkout HEAD~2 -- crates/beava-server/tests/phase12_8_metrics_endpoint.rs`), run the test, confirm it FAILS with phase12_8_metrics_endpoint.rs in the violations list, then `git stash pop` (or re-apply Task 2's clean state). Document the probe outcome in the commit message body. This is "the test is its own RED contract" pattern documented in CLAUDE.md §TDD Discipline item #4 (smoke / acceptance tests as the architectural-invariant red form, parallel to proptest item #3) — the architectural test IS the red contract for the work-already-landed in Task 2.
       
       **Alternate clean RED-GREEN**: land Task 3 BEFORE Task 2's GREEN commit. Sequence becomes Task 1 (red→green) → Task 3 (red — the architectural test fails because Task 2 hasn't stripped set_vars yet) → Task 2 (red→green that turns Task 3 green by stripping the set_vars). Pick this if logistically simpler.

       **Recommended ordering for the executor**: Task 1 → Task 3 RED (architectural test landed, FAILS because tests still have set_vars) → Task 3 STAYS RED → Task 2 (red→green, the green commit ALSO turns Task 3's architectural test GREEN). This is the cleanest TDD red-then-green loop because the architectural test IS the red contract for Task 2. Re-order Tasks 2 and 3 accordingly: Task 3 lands BETWEEN Task 1 GREEN and Task 2 RED, as a single RED commit.
    3. Commit: `test(13.5.3): architectural tripwire — assert no env::set_var in beava-server/tests/ (RED — fails until Task 2 strips set_vars)`.

    **GREEN commit**: NO new code in this task — Task 2's GREEN commit also turns Task 3's architectural test GREEN (the moment Task 2 deletes the last set_var, this test passes). Document this in Task 2's GREEN commit body: "Also turns phase13_5_3_no_env_var_pokes_in_tests GREEN — no remaining set_var in crates/beava-server/tests/". No separate green commit needed for Task 3 — the test is GREEN-by-construction once Task 2 lands.

    **OR if executor prefers symmetric structure**: land an empty `chore(13.5.3): confirm architectural tripwire green` commit at end touching nothing (just a marker). Skip if it feels ceremonial.
  </action>
  <verify>
    <automated>cargo test -p beava-server --test phase13_5_3_no_env_var_pokes_in_tests && [ "$(grep -rEc 'std::env::set_var\(|env::set_var\(' crates/beava-server/tests/ 2>/dev/null | grep -v 'phase13_5_3_no_env_var_pokes_in_tests' | grep -v ':0$' | wc -l)" = "0" ]</automated>
  </verify>
  <done>
    `crates/beava-server/tests/phase13_5_3_no_env_var_pokes_in_tests.rs` exists with 2 tests + a probe-procedure docstring; `cargo test -p beava-server --test phase13_5_3_no_env_var_pokes_in_tests` GREEN; running test from a state with a planted set_var violation (Probe-1) produces a FAILED test with the violating file's path in the violations Vec — confirms the tripwire actually catches regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → process env | Operator-controlled `BEAVA_*` env vars at boot; trusted (operator already controls the binary's runtime environment). |
| test process A → test process B (cross-binary) | `cargo test --workspace` parallelism; `std::env::*` is process-local → set_var in binary A cannot affect binary B. |
| test process within a binary | `cargo test`'s `--test-threads=N` parallelism within a single binary; `std::env::*` is GLOBAL within a process → set_var in test1 leaks to test2 mid-run. **This is the bug class Phase 13.5.3 closes.** |
| TestServer instance A → TestServer instance B (same process) | Multiple `TestServer::spawn()` calls in parallel within one tokio test; previously shared process env; post-13.5.3 isolated via per-server `ServerV18Config`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.5.3-01 | T (Tampering) — cross-test env-var contamination | `crates/beava-server/tests/*.rs` | mitigate | Plumb all `BEAVA_*` reads through `ServerV18Config` struct fields; architectural tripwire `phase13_5_3_no_env_var_pokes_in_tests.rs` enforces. |
| T-13.5.3-02 | I (Information disclosure) — leaked set_var leaks values across test binaries | tests/ | accept | `std::env::*` is per-process; cross-binary pollution is impossible. Within a binary, the tripwire forbids set_var so this becomes a non-issue. No PII / secrets in BEAVA_* env vars (size knobs only). |
| T-13.5.3-03 | D (Denial of service) — WAL EEXIST race blocks server bind | `TestServerBuilder::default` WAL dir construction | mitigate | Switch from `temp_dir + (pid, counter)` to `tempfile::tempdir()` (kernel-guaranteed unique mkdtemp). |
| T-13.5.3-04 | R (Repudiation) — operator override interface broken by struct migration | `crates/beava-server/src/main.rs` + `crates/beava-server/src/server.rs::ServerV18Config::from_env` | mitigate | Production main.rs continues to read all `BEAVA_*` env vars at boot via `from_env()`; operator interface UNCHANGED. Existing operator-doc env-var table stays accurate. |
| T-13.5.3-05 | E (Elevation of privilege) — `BEAVA_TEST_MODE` re-evaluated at runtime allowing late escalation | `crates/beava-server/src/server.rs::bind_with_config` | mitigate | Phase 13.4 D-03 USER-LOCKED already mandates boot-time-only resolution; Task 1 removes the second runtime read site in `build_runtime_state` (line ~506-508), consolidating to a single `from_env()` boot-time read. |
</threat_model>

<verification>
1. **Acceptance gate (USER-FACING acceptance criteria from task brief):**
   ```bash
   for i in $(seq 1 5); do cargo test --workspace --features testing 2>&1 | grep -E "(test result|FAILED)" | tail -3 || exit 1; done
   ```
   All 5 iterations show `test result: ok` for every test binary.

2. **Python v0 acceptance:**
   ```bash
   for i in $(seq 1 5); do pytest python/tests/v0/ 2>&1 | tail -3 || exit 1; done
   ```
   All 5 iterations show `89 passed`.

3. **Lint + format gates:**
   ```bash
   cargo clippy --workspace --all-targets --all-features -- -D warnings
   cargo fmt --all --check
   ```

4. **Architectural tripwire:**
   ```bash
   cargo test -p beava-server --test phase13_5_3_no_env_var_pokes_in_tests
   ```
   All tests in this binary GREEN.

5. **set_var grep gate (post-Task-2):**
   ```bash
   grep -rE 'std::env::set_var\(|env::set_var\(' crates/beava-server/tests/ | grep -v phase13_5_3_no_env_var_pokes_in_tests | wc -l
   ```
   Returns `0`.

6. **Production env-var interface preserved:**
   ```bash
   BEAVA_WAL_BUFFERS=8 BEAVA_IO_THREADS=4 BEAVA_TEST_MODE=1 cargo run --release --bin beava-server -- --listen 127.0.0.1:0 &
   # sleep + curl /health + assert booted with the env-overridden values present in the boot log
   ```
   Server boots and emits `wal.config.resolved buffers=8` + `server.test_mode_enabled` log lines.

7. **Probe verification (one-time, optional but recommended):** plant a set_var violation in any beava-server/tests/ file, run `cargo test -p beava-server --test phase13_5_3_no_env_var_pokes_in_tests`, confirm RED with the violating file's path in the violations Vec, revert.
</verification>

<success_criteria>
- 5 consecutive `cargo test --workspace --features testing` runs all GREEN.
- 5 consecutive `pytest python/tests/v0/` runs all 89/89 GREEN.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean.
- `cargo fmt --all --check` clean.
- `cargo test -p beava-server --test phase13_5_3_no_env_var_pokes_in_tests` GREEN.
- Zero `set_var` calls remaining in `crates/beava-server/tests/` (architectural tripwire enforces).
- `TestServerBuilder` exposes `wal_buffers` / `wal_buffer_size_mb` / `wal_tick_ms` / `io_threads` / `test_mode` / `memory_governance_enforce` builder methods.
- Production `main.rs` calls `ServerV18Config::from_env()` then `ServerV18::bind_with_config(...)`; operator-facing env interface unchanged.
- Each task ships as a red commit (failing test) followed by a green commit (impl) per CLAUDE.md §TDD Discipline item #1.
</success_criteria>

<output>
Quick task — no SUMMARY.md required. Final state: 6 atomic commits on `v2/greenfield` (3 tasks × red+green) closing Phase 13.5.3 / Phase 999.1 backlog item. STATE.md "Current Focus" entry updates: append "Phase 13.5.3 CLOSED 2026-05-05 — workspace test determinism Path A architectural fix landed; `BEAVA_*` env vars plumbed through `ServerV18Config`; 5/5 cargo test --workspace + 5/5 pytest v0 GREEN; architectural tripwire `phase13_5_3_no_env_var_pokes_in_tests` prevents regression."
</output>
