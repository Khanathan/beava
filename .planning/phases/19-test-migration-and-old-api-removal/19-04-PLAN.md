---
phase: 19-test-migration-and-old-api-removal
plan: 04
type: execute
wave: 3
depends_on: [19-02, 19-03]
files_modified:
  - python/tally/__init__.py
  - python/tally/_app.py
  - python/tally/_stream.py
  - python/tally/_view.py
  - python/tally/_dataframe.py
  - python/tally/_expr.py
  - python/tests/test_stream.py
  - python/tests/test_view.py
  - python/tests/test_dataframe.py
autonomous: true
requirements: [MIG-01, MIG-02]

must_haves:
  truths:
    - "Test count is >= 744 (cargo test + pytest) BEFORE any deletion"
    - "Old API files (_stream.py, _view.py, _dataframe.py, _expr.py) are deleted"
    - "Old test files (test_stream.py, test_view.py, test_dataframe.py) are deleted"
    - "__init__.py no longer exports stream, view, Column, Expr, EventProxy, DataStream, Table, GroupBy, JoinedTable, Dataset"
    - "_app.py no longer has source(), serve(), register_all() methods"
    - "Test count is still >= 744 AFTER deletion"
    - "No @st.stream or @st.view references exist in any non-archived Python file"
  artifacts:
    - path: "python/tally/__init__.py"
      provides: "Clean exports with only new API symbols"
    - path: "python/tally/_app.py"
      provides: "App class with only register/push/get/set/mset/mget (no DataFrame methods)"
  key_links:
    - from: "python/tally/__init__.py"
      to: "python/tally/_source.py"
      via: "import"
      pattern: "from tally._source import source"
    - from: "python/tally/__init__.py"
      to: "python/tally/_dataset.py"
      via: "import"
      pattern: "from tally._dataset import dataset"
---

<objective>
Verify test count >= 744 before deletion, then delete all old API files and symbols, delete old test files, clean __init__.py and _app.py, and verify test count >= 744 after deletion.

Purpose: This is the critical C-2 pitfall gate. Tests must be verified BEFORE deletion and AFTER deletion to ensure no coverage loss.
Output: Clean SDK with only the new API surface. All tests pass on the new API only.
</objective>

<execution_context>
@/data/home/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-test-migration-and-old-api-removal/19-RESEARCH.md
@.planning/phases/19-test-migration-and-old-api-removal/19-01-SUMMARY.md
@.planning/phases/19-test-migration-and-old-api-removal/19-02-SUMMARY.md
@.planning/phases/19-test-migration-and-old-api-removal/19-03-SUMMARY.md

<interfaces>
Files to DELETE from SDK:
- python/tally/_stream.py (old @stream decorator)
- python/tally/_view.py (old @view decorator)
- python/tally/_dataframe.py (DataFrame API: Stream, Table, GroupBy, JoinedTable, Dataset)
- python/tally/_expr.py (Column, Expr, EventProxy -- only imported by _dataframe.py and __init__.py)

Test files to DELETE:
- python/tests/test_stream.py (replaced by test_source.py + test_dataset.py)
- python/tests/test_view.py (replaced by test_dataset.py)
- python/tests/test_dataframe.py (replaced by test_dataset_behaviors.py)

__init__.py symbols to REMOVE:
- from tally._stream import stream
- from tally._view import view
- from tally._expr import Column, Expr, EventProxy
- from tally._dataframe import Stream as DataStream, Table, GroupBy, JoinedTable, Dataset
- __all__ entries: "stream", "view", "Column", "Expr", "EventProxy", "DataStream", "Table", "GroupBy", "JoinedTable", "Dataset"

_app.py methods to REMOVE:
- source() method (creates DataFrame Stream, NOT @tl.source)
- serve() method
- register_all() method
- Any import of Dataset from _dataframe
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deletion test count verification</name>
  <files></files>
  <read_first>
    - python/tests/conftest.py (verify it uses new API from Plan 01)
  </read_first>
  <action>
Run the FULL test suite and record counts BEFORE any deletion:

1. `PYTHONPATH=python python3 -m pytest python/tests --co -q 2>&1 | tail -3` -- record Python test count
2. `cargo test 2>&1 | tail -5` -- record Rust test count
3. Sum the two counts. The total MUST be >= 744.

If the total is < 744, STOP and investigate. Do NOT proceed to Task 2.

Also run the full test suite to confirm all pass:
4. `PYTHONPATH=python python3 -m pytest python/tests -q`
5. `cargo test`

Record the exact counts in a comment for Task 2 to reference.
  </action>
  <verify>
    <automated>PYTHONPATH=python python3 -m pytest python/tests --co -q 2>&1 | tail -1 && echo "---" && cargo test 2>&1 | grep "test result"</automated>
  </verify>
  <acceptance_criteria>
    - Python test collection count >= 377 (331 original + 46 fixed protocol tests)
    - Rust test count >= 700
    - Total (Python + Rust) >= 744
    - All tests pass (zero failures)
  </acceptance_criteria>
  <done>Pre-deletion test counts recorded. Total >= 744 confirmed. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Delete old API files, clean __init__.py and _app.py</name>
  <files>python/tally/__init__.py, python/tally/_app.py, python/tally/_stream.py, python/tally/_view.py, python/tally/_dataframe.py, python/tally/_expr.py, python/tests/test_stream.py, python/tests/test_view.py, python/tests/test_dataframe.py</files>
  <read_first>
    - python/tally/__init__.py (full file -- identify exact lines to remove)
    - python/tally/_app.py (full file -- identify source/serve/register_all methods and DataFrame import)
  </read_first>
  <action>
**Step 1: Delete old SDK files**
```bash
rm python/tally/_stream.py
rm python/tally/_view.py
rm python/tally/_dataframe.py
rm python/tally/_expr.py
```

**Step 2: Delete old test files**
```bash
rm python/tests/test_stream.py
rm python/tests/test_view.py
rm python/tests/test_dataframe.py
```

**Step 3: Clean __init__.py**
Remove these imports:
- `from tally._stream import stream`
- `from tally._view import view`
- `from tally._expr import Column, Expr, EventProxy`
- `from tally._dataframe import Stream as DataStream, Table, GroupBy, JoinedTable, Dataset`

Remove these from __all__:
- `"stream"`, `"view"`, `"Column"`, `"Expr"`, `"EventProxy"`, `"DataStream"`, `"Table"`, `"GroupBy"`, `"JoinedTable"`, `"Dataset"`

Remove the `# DataFrame-style API` comment section.
Remove the `# Decorators (legacy API)` comment and entries.

Keep ALL new API imports (source, dataset, group_by, union, validate, EventSet, FeatureSet, Field).
Keep ALL operator imports (count, sum, avg, etc.).
Keep ALL type/protocol imports.

**Step 4: Clean _app.py**
- Remove `from tally._dataframe import Dataset` import (or similar)
- Remove the `source()` instance method (DataFrame API: creates a DataFrame Stream)
- Remove the `serve()` instance method
- Remove the `register_all()` instance method
- Keep `register()` method (supports new API via _collect_registrations/_to_register_json protocols)
- Keep push(), get(), set(), mset(), mget(), close() methods

**Step 5: Verify no stale references**
Run: `grep -r "_stream\|_view\|_dataframe\|_expr" python/tally/*.py` -- only hits should be in __pycache__ or non-existent files. Check that no remaining .py file imports deleted modules.

**Step 6: Run full test suite after deletion**
```bash
PYTHONPATH=python python3 -m pytest python/tests -q
cargo test
```

**Step 7: Verify old API symbols are truly gone**
```bash
python3 -c "import sys; sys.path.insert(0, 'python'); import tally; print(hasattr(tally, 'stream'))"  # must print False
python3 -c "import sys; sys.path.insert(0, 'python'); import tally; print(hasattr(tally, 'view'))"    # must print False
python3 -c "import sys; sys.path.insert(0, 'python'); import tally; print(hasattr(tally, 'DataStream'))"  # must print False
```

**Step 8: Grep verification -- no old API references in non-archived files**
```bash
grep -r "st\.stream\|st\.view\|@stream\|@view" python/ --include="*.py" | grep -v __pycache__ | grep -v "\.pyc"
```
This should return zero results.
  </action>
  <verify>
    <automated>PYTHONPATH=python python3 -m pytest python/tests --co -q 2>&1 | tail -1 && echo "---" && cargo test 2>&1 | grep "test result" && echo "---" && python3 -c "import sys; sys.path.insert(0, 'python'); import tally; print('stream' not in dir(tally) and 'view' not in dir(tally) and 'DataStream' not in dir(tally))"</automated>
  </verify>
  <acceptance_criteria>
    - ls python/tally/_stream.py 2>/dev/null returns error (file deleted)
    - ls python/tally/_view.py 2>/dev/null returns error (file deleted)
    - ls python/tally/_dataframe.py 2>/dev/null returns error (file deleted)
    - ls python/tally/_expr.py 2>/dev/null returns error (file deleted)
    - ls python/tests/test_stream.py 2>/dev/null returns error (file deleted)
    - ls python/tests/test_view.py 2>/dev/null returns error (file deleted)
    - ls python/tests/test_dataframe.py 2>/dev/null returns error (file deleted)
    - grep "stream\|view\|Column\|Expr\|EventProxy\|DataStream\|Table\|GroupBy\|JoinedTable\|Dataset" python/tally/__init__.py finds only new API references (source, dataset, etc.)
    - grep -c "def source\|def serve\|def register_all" python/tally/_app.py returns 0
    - python3 -c "import tally; tally.stream" raises AttributeError
    - PYTHONPATH=python python3 -m pytest python/tests -q passes all tests
    - cargo test passes all tests
    - Total test count (Python + Rust) >= 744
    - grep -r "st\.stream\|st\.view\|@stream\|@view" python/ --include="*.py" returns 0 results (excluding __pycache__)
  </acceptance_criteria>
  <done>Old API completely removed from SDK. All tests pass on new API only. Total test count >= 744. No old API references remain in Python source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | N/A | N/A | accept | No security-relevant changes -- API removal only |
</threat_model>

<verification>
1. Pre-deletion total >= 744 (Task 1 gate)
2. Post-deletion: `PYTHONPATH=python python3 -m pytest python/tests -q && cargo test` -- all pass
3. Post-deletion total >= 744
4. `python3 -c "import tally; tally.stream"` -- AttributeError
5. `grep -r "st\.stream\|st\.view\|@stream\|@view" python/ --include="*.py"` -- zero results
6. Old files do not exist on disk
</verification>

<success_criteria>
- Pre-deletion test count verified >= 744
- 4 SDK files deleted (_stream.py, _view.py, _dataframe.py, _expr.py)
- 3 test files deleted (test_stream.py, test_view.py, test_dataframe.py)
- __init__.py exports only new API symbols
- _app.py has no DataFrame API methods
- Post-deletion test count >= 744
- All tests pass
- Zero old API references in Python source (grep verified)
</success_criteria>

<output>
After completion, create `.planning/phases/19-test-migration-and-old-api-removal/19-04-SUMMARY.md`
</output>
