---
phase: 19-test-migration-and-old-api-removal
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - python/tests/test_stream.py
  - python/tests/test_view.py
  - python/tests/test_source.py
  - python/tests/test_dataset.py
autonomous: true
requirements: [MIG-01]

must_haves:
  truths:
    - "test_stream.py is replaced by test_source.py and test_dataset.py with equivalent coverage"
    - "test_view.py is replaced by additional tests in test_dataset.py covering view-equivalent patterns"
    - "All error-handling tests from test_stream.py have equivalent tests for new API error cases"
    - "Test count does not decrease after rewrite"
  artifacts:
    - path: "python/tests/test_source.py"
      provides: "@tl.source decorator tests (keyless source definition, naming, compilation)"
      min_lines: 30
    - path: "python/tests/test_dataset.py"
      provides: "@tl.dataset decorator tests (keyed aggregation, depends_on, derive-only views, error handling)"
      min_lines: 100
  key_links:
    - from: "python/tests/test_source.py"
      to: "python/tally/_source.py"
      via: "import"
      pattern: "from tally import source"
    - from: "python/tests/test_dataset.py"
      to: "python/tally/_dataset.py"
      via: "import"
      pattern: "from tally import dataset"
---

<objective>
Replace test_stream.py (33 tests) and test_view.py (12 tests) with new test_source.py and test_dataset.py files that test the equivalent behaviors of @tl.source and @tl.dataset.

Purpose: The old tests verified @st.stream and @st.view decorator behavior (metaclass, field registration, naming, JSON compilation, error handling). The new tests must verify the same logical behaviors for @tl.source and @tl.dataset.
Output: test_source.py and test_dataset.py with >= 45 tests total covering all old behaviors plus new API features.
</objective>

<execution_context>
@/data/home/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-test-migration-and-old-api-removal/19-RESEARCH.md
@.planning/phases/19-test-migration-and-old-api-removal/19-01-SUMMARY.md

<interfaces>
From python/tally/_source.py:
- @source decorator: creates SourceDef from a class
- SourceDef._tally_stream_name: class name (str)
- SourceDef._to_register_json(): returns dict for RegisterRequest
- SourceDef._collect_registrations(): yields self

From python/tally/_dataset.py:
- @dataset(depends_on=[...]) decorator: creates DatasetDef
- group_by("key").agg(**operators): defines keyed aggregation
- DatasetDef._tally_stream_name: class name (str)
- DatasetDef._compile(): returns dict for RegisterRequest
- DatasetDef._collect_registrations(): yields self + dependencies
- DatasetDef with derive-only (no agg operators) = view-equivalent

From python/tally/_schema.py:
- EventSet: typed input schema base class
- FeatureSet: typed output schema base class
- Field: descriptor for schema fields
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_source.py and dataset decorator unit tests</name>
  <files>python/tests/test_source.py, python/tests/test_dataset.py</files>
  <read_first>
    - python/tests/test_stream.py (full file -- catalog every test case and what it tests)
    - python/tally/_source.py (full file -- understand all behaviors to test)
    - python/tally/_dataset.py (full file -- understand all behaviors to test)
    - python/tally/_schema.py (first 100 lines -- EventSet/FeatureSet basics)
  </read_first>
  <action>
**Create test_source.py** with tests covering:
- Basic @source definition creates a SourceDef
- SourceDef._tally_stream_name matches class name
- SourceDef._to_register_json() produces correct JSON shape (keyless stream, no operators)
- _collect_registrations() yields self
- @source with EventSet schema (if supported)
- Error case: @source with key parameter raises error (sources are keyless)

Map from test_stream.py test cases: every test that tested keyless @st.stream() behavior maps to test_source.py.

**Create test_dataset.py** with tests covering:
- Basic @dataset(depends_on=[Source]) with group_by("key").agg(count=tl.count(window="1h"))
- DatasetDef._tally_stream_name matches class name
- DatasetDef._compile() produces correct RegisterRequest JSON (key_field, features, depends_on)
- _collect_registrations() yields self + dependencies
- Multiple operators in agg(): count, sum, avg, min, max, distinct_count, last
- derive features alongside agg features
- group_by().agg() with no operators but with derives = view-equivalent
- tl.union(source_a, source_b) produces multi-parent depends_on
- Error cases: missing depends_on, circular depends_on (if checkable at class level), invalid operator in agg
- FeatureSet schema on dataset (if supported)
- Dataset with where-clause filters
- Dataset with cross-stream derive expressions (e.g., "OtherStream.feature > 5")

Map from test_stream.py: keyed @st.stream tests -> dataset tests. Map from test_view.py: @st.view tests -> derive-only dataset tests.

Target: >= 45 tests total across both files (33 from test_stream + 12 from test_view, with some consolidation and some new cases).

Run: `PYTHONPATH=python python3 -m pytest python/tests/test_source.py python/tests/test_dataset.py -x -q`
  </action>
  <verify>
    <automated>PYTHONPATH=python python3 -m pytest python/tests/test_source.py python/tests/test_dataset.py -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - python/tests/test_source.py exists with >= 5 test functions
    - python/tests/test_dataset.py exists with >= 30 test functions
    - grep -c "st\.stream\|st\.view\|@stream\|@view" python/tests/test_source.py python/tests/test_dataset.py returns 0
    - All tests pass
  </acceptance_criteria>
  <done>test_source.py and test_dataset.py exist with >= 45 tests total, all passing, covering all behaviors from the old test_stream.py and test_view.py.</done>
</task>

<task type="auto">
  <name>Task 2: Mark old test_stream.py and test_view.py for deletion</name>
  <files>python/tests/test_stream.py, python/tests/test_view.py</files>
  <read_first>
    - python/tests/test_stream.py (first 10 lines -- confirm what it tests)
    - python/tests/test_view.py (first 10 lines -- confirm what it tests)
  </read_first>
  <action>
Do NOT delete these files yet (deletion happens in Plan 04 after count verification). Instead:

1. Add a module-level comment at the top of test_stream.py: `# DEPRECATED: Replaced by test_source.py and test_dataset.py. Will be deleted in Plan 04.`
2. Add a module-level comment at the top of test_view.py: `# DEPRECATED: Replaced by test_dataset.py. Will be deleted in Plan 04.`
3. Verify the full test suite still passes with both old and new test files present: `PYTHONPATH=python python3 -m pytest python/tests -x -q`
4. Count total tests: `PYTHONPATH=python python3 -m pytest python/tests --co -q 2>&1 | tail -1` -- should show more than 377 (original 331 + 46 protocol + new source/dataset tests)
  </action>
  <verify>
    <automated>PYTHONPATH=python python3 -m pytest python/tests --co -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep "DEPRECATED" python/tests/test_stream.py returns match
    - grep "DEPRECATED" python/tests/test_view.py returns match
    - Full test suite passes (both old and new test files coexist)
    - Test collection count is > 377
  </acceptance_criteria>
  <done>Old test files marked for deletion but still present. New replacement test files verified. Total test count increased.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | N/A | N/A | accept | No security-relevant changes -- test file rewrite only |
</threat_model>

<verification>
1. `PYTHONPATH=python python3 -m pytest python/tests/test_source.py python/tests/test_dataset.py -q` -- all pass
2. `PYTHONPATH=python python3 -m pytest python/tests --co -q 2>&1 | tail -1` -- total > 377
3. `grep -c "st\.stream\|st\.view" python/tests/test_source.py python/tests/test_dataset.py` -- 0 matches
</verification>

<success_criteria>
- test_source.py has >= 5 tests covering @source decorator behavior
- test_dataset.py has >= 30 tests covering @dataset, group_by, derives, views, union, error cases
- All new tests pass
- Full suite passes with old + new test files coexisting
- Test count has increased (old not deleted yet, new added)
</success_criteria>

<output>
After completion, create `.planning/phases/19-test-migration-and-old-api-removal/19-02-SUMMARY.md`
</output>
