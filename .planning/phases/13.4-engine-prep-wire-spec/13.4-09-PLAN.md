---
phase: 13.4-engine-prep-wire-spec
plan: 09
type: execute
wave: 4
depends_on: ["05", "06", "08"]   # 08 dep added: same apply_shard.rs file means sequential ordering — Plan 09's comment-only edit lands AFTER Plan 08's reset dispatch
files_modified:
  - crates/beava-core/src/register_validate.rs
  - crates/beava-core/src/op_node.rs
  - crates/beava-core/src/registry_diff.rs
  - crates/beava-server/src/apply_shard.rs
  - crates/beava-server/tests/phase13_4_global_table_routing.rs   # NEW (RED→GREEN integration)
  - crates/beava-server/tests/phase13_4_table_derivation_allowed.rs   # un-ignore Test 3 (Plan 05)
  - crates/beava-server/tests/phase13_4_op_batch_get.rs               # un-ignore Test 5 (Plan 03)
  - crates/beava-server/tests/phase13_4_force_register.rs             # un-ignore Test 9 (Plan 06)
  - REQUIREMENTS.md   # mark V0-GLOBAL-AGG-01 implemented
autonomous: true
requirements:
  - V0-EVENTS-ONLY-01
  - V0-GLOBAL-AGG-01            # NEW — engine sentinel routing for global tables (this plan implements)
addresses_decisions:
  - "ADR-003 §Decision B / Engine — sentinel routing for global tables. `key_cols: []` accepted at register-time; sentinel `entity_id = \"\"` routes through the existing per-entity hashmap (no special-case branch — the existing `&str` key path handles `\"\"` natively). Implements V0-GLOBAL-AGG-01."
  - "ADR-001 §Deferred for Phase 13.4 — implement `output_kind: \"table\"` field plumbing through register payload validation in `register_validate.rs`. Implement `OpNode::Table` enum variant for derivation output (the bare variant whose forbidden entry was removed in Plan 05). The registered derivation lands as a derivation node with output_kind=table; no app.upsert/delete/retract surface, no MVCC, no temporal versioning."
  - "Un-ignore Plan 03 Test 5 (http_batch_get_with_global_table_entity_id_empty), Plan 05 Test 3 (derivation_with_output_kind_table_succeeds), Plan 06 Test 9 (register_destructive_agg_removal_without_force_returns_409). All three were marked #[ignore] pending this plan's engine acceptance."
must_haves:
  truths:
    - "Register payload `{\"nodes\":[{\"kind\":\"derivation\",\"name\":\"GlobalCounter\",\"output_kind\":\"table\",\"key_cols\":[],\"source\":\"Tx\",\"ops\":[{\"op\":\"count\"}]}]}` is accepted by the engine (HTTP 200)"
    - "Register payload with `kind: \"table\"` at the top level STILL returns `unsupported_node_kind` (Plan 12.7-01 shim — surgical permit per Plan 05 D-04 only widens the architectural test allowlist, not the JSON-prelude shim)"
    - "Register payload with `key_cols: null` (not an empty array) returns a structured 400 error `key_cols_required` — the schema accepts only non-null arrays (empty or non-empty)"
    - "Register payload with `key_cols: []` AND `output_kind: \"table\"` (i.e. global table) is accepted; subsequent push of an event for the source advances the global state at sentinel `entity_id = \"\"`"
    - "POST /get with `{\"requests\":[{\"table\":\"GlobalCounter\",\"entity_id\":\"\"}]}` returns the global features (e.g. `{\"results\":[{\"table\":\"GlobalCounter\",\"entity_id\":\"\",\"features\":{\"count\":5}}]}` after 5 events pushed)"
    - "GET /get/count/<empty> path or POST /get with `entity_id: \"\"` retrieves global state via the same code path as per-entity (no new branch in dispatch — proves the \"absence-of-special-case\" claim from ADR-003)"
    - "The 3 cross-plan ignored tests (Plan 03 Test 5, Plan 05 Test 3, Plan 06 Test 9) are now passing; their `#[ignore]` attributes are removed in THIS plan's commit"
  artifacts:
    - path: "crates/beava-core/src/register_validate.rs"
      provides: "Register-time validation accepts `key_cols: []` (empty array — global table per ADR-003) and `output_kind: \"table\"` for derivations (per ADR-001 partial overturn). Top-level `kind: \"table\"` still rejected by Plan 12.7-01 shim — UNCHANGED."
      contains: "key_cols|output_kind"
    - path: "crates/beava-core/src/op_node.rs"
      provides: "Re-introduce `OpNode::Table` enum variant for derivation `output_kind=table` per ADR-001. The variant carries the table's name + key_cols + agg ops; no upsert/delete/retract verbs (those stay killed)."
      contains: "OpNode::Table"
    - path: "crates/beava-core/src/registry_diff.rs"
      provides: "PayloadNode discriminator extends to handle the `output_kind: \"table\"` field on derivations (the bare `kind: \"table\"` top-level node STAYS rejected by the JSON-prelude shim)."
      contains: "output_kind"
    - path: "crates/beava-server/src/apply_shard.rs"
      provides: "Confirm (no code change required for sentinel routing per ADR-003 — the hashmap key is `&str` and accepts `\"\"`) and add a comment block citing the ADR. Verify the dispatch_get_sync / dispatch_push_sync paths handle `entity_id = \"\"` without rejection — most likely they already do; if any code path explicitly rejects empty strings, remove that rejection."
      contains: "entity_id|ADR-003"
    - path: "crates/beava-server/tests/phase13_4_global_table_routing.rs"
      provides: "RED→GREEN integration test for global aggregation: register with key_cols=[]; push events; GET with entity_id=''; assert global feature returned."
      min_lines: 80
  key_links:
    - from: "register_validate.rs JSON-prelude validation"
      to: "key_cols: [] AND output_kind=table acceptance"
      via: "Validation accepts empty arrays for key_cols (signal: global) AND non-empty arrays (per-entity); rejects null. output_kind=table on derivations is permitted; output_kind=table on top-level kind=table register STAYS rejected by 12.7-01 shim."
      pattern: "key_cols|output_kind"
    - from: "apply_shard.rs::dispatch_get_sync"
      to: "Per-entity hashmap with key=\"\" (sentinel)"
      via: "ADR-003: `\"the existing &str key path handles \\\"\\\" natively\"`. No new code path — confirm via comment + the integration test."
      pattern: "entity_id"
---

<objective>
Land the engine-side acceptance for `output_kind=table` derivations and `key_cols: []` global-aggregation per ADR-003 + ADR-001 §Deferred for Phase 13.4. The bulk of this plan is register-validation extension (~30 LOC per ADR-003 estimate) plus re-introducing the `OpNode::Table` enum variant for derivation output (~50 LOC for the variant + serde + apply hookup). The dispatch path is already correct (per ADR-003: "the existing `&str` key path handles `\"\"` natively. No new code path inside `apply_shard.rs::dispatch_*_sync` — just the absence of a special-case rejection.").

This plan also UN-IGNORES three cross-plan tests that were marked `#[ignore]` pending this engine acceptance:

- Plan 03 Test 5: `http_batch_get_with_global_table_entity_id_empty`
- Plan 05 Test 3: `derivation_with_output_kind_table_succeeds`
- Plan 06 Test 9: `register_destructive_agg_removal_without_force_returns_409`

**Wave 3 placement:** depends on Plan 05 (architectural-test allowlist permits `OpNode::Table` to reappear) and Plan 06 (force diff classifier — Test 9 in Plan 06 needs `agg_removal` classified, which means at least one prior register payload had aggs that this plan now permits).

**TDD ordering:**

1. **Task 9.a (RED — global routing):** failing integration test in `phase13_4_global_table_routing.rs`.
2. **Task 9.b (GREEN — register-validate acceptance):** extend `register_validate.rs` to accept `key_cols: []` + `output_kind: "table"` on derivations; reintroduce `OpNode::Table` variant in `op_node.rs`; verify dispatch routes `""` natively (likely zero code change there); update `registry_diff.rs::PayloadNode` to round-trip the `output_kind` field.
3. **Task 9.c (un-ignore + REQUIREMENTS.md):** remove the 3 `#[ignore]` attributes; mark V0-GLOBAL-AGG-01 as implemented in REQUIREMENTS.md (well — REQUIREMENTS.md uses `[ ]` checkboxes; un-tick the box once implemented OR leave it for the parent orchestrator to mark? Per orchestrator constraint: "DO NOT modify... anything outside `.planning/phases/13.4-engine-prep-wire-spec/`." So skip the REQUIREMENTS.md tick — the parent orchestrator handles it.

   REVISED Task 9.c scope: just un-ignore the 3 tests in this plan's commit. REQUIREMENTS.md and STATE/ROADMAP advancement are owned by the parent orchestrator after all 4 sibling agents finish.

Output:
- Extended register-validate (key_cols: [] + output_kind: "table" acceptance)
- Re-introduced `OpNode::Table` variant
- 1 new integration test
- 3 un-ignored tests across other plan files
- 3 commits (red, green, un-ignore-and-cleanup)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13.4-engine-prep-wire-spec/13.4-CONTEXT.md
@.planning/phases/13.4-engine-prep-wire-spec/SCRATCH-PLANNER-NOTES.md
@.planning/decisions/ADR-001-bv-table-partial-overturn.md
@.planning/decisions/ADR-003-global-aggregation-and-bv-lit.md
@crates/beava-core/src/register_validate.rs
@crates/beava-core/src/op_node.rs
@crates/beava-core/src/registry_diff.rs
@crates/beava-server/src/apply_shard.rs

<interfaces>
<!-- Wire shape acceptances. -->

Per-entity table register (existing — UNCHANGED):
```json
{"nodes":[{
  "kind": "derivation",
  "name": "UserSpend",
  "output_kind": "table",
  "key_cols": ["user_id"],
  "source": "Tx",
  "ops": [{"op": "count"}, {"op": "sum", "field": "amount"}]
}]}
```

Global table register (NEW — per ADR-003):
```json
{"nodes":[{
  "kind": "derivation",
  "name": "GlobalCounter",
  "output_kind": "table",
  "key_cols": [],                ← empty array signals global per ADR-003
  "source": "Tx",
  "ops": [{"op": "count"}]
}]}
```

Get on global table:
```json
POST /get
{"requests": [{"table": "GlobalCounter", "entity_id": ""}]}
→
{"results": [{"table": "GlobalCounter", "entity_id": "", "features": {"count": 12345}}]}
```

Top-level table register (still REJECTED — Plan 12.7-01 shim):
```json
{"nodes":[{"kind":"table","name":"X",...}]}
→ HTTP 400 + {"error":{"code":"unsupported_node_kind",...}}
```

OpNode::Table variant shape (re-introduced — narrower than pre-12.7):
```rust
pub enum OpNode {
    Event(EventDescriptor),
    Derivation(DerivationDescriptor),
    /// Plan 13.4-09 — re-introduced per ADR-001 partial overturn.
    /// ONLY produced as a derivation `output_kind=table`; never as a
    /// top-level register node (12.7-01 shim still rejects that).
    /// No upsert/delete/retract verbs (those stay killed per
    /// project_v0_events_only_scope).
    Table(TableDerivationOutput),
}

pub struct TableDerivationOutput {
    pub name: String,
    pub key_cols: Vec<String>,    // empty = global (ADR-003)
    pub source: String,           // event source name
    pub ops: Vec<AggOpDescriptor>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 9.a (RED): Failing integration test for global-table register + push + GET round-trip</name>
  <read_first>
    - .planning/decisions/ADR-003-global-aggregation-and-bv-lit.md (full ADR — sentinel routing spec)
    - crates/beava-server/tests/phase13_4_get_row_shape.rs (Plan 02's harness reference)
    - crates/beava-server/tests/phase13_4_op_batch_get.rs (Test 5 — note its #[ignore]; this plan un-ignores; same shape)
  </read_first>
  <files>crates/beava-server/tests/phase13_4_global_table_routing.rs</files>
  <behavior>
    - Test 1 — `register_global_table_with_empty_key_cols_succeeds`: POST /register with `{"nodes":[{"kind":"derivation","name":"GlobalCounter","output_kind":"table","key_cols":[],"source":"Tx","ops":[{"op":"count"}]}]}` (preceded by an event-source register for `Tx`); assert HTTP 200.
    - Test 2 — `push_then_get_global_table_returns_aggregated_features`: register Tx + GlobalCounter; push 5 Tx events for various user_ids; POST /get with `{"requests":[{"table":"GlobalCounter","entity_id":""}]}`; assert response is `{"results":[{"table":"GlobalCounter","entity_id":"","features":{"count":5}}]}`.
    - Test 3 — `register_with_key_cols_null_returns_400`: POST /register with `{..., "key_cols": null}`; assert HTTP 400 + structured error `key_cols_required` (or whatever the existing register_validate emits for missing required field — check existing error code naming during read_first; if none exists, add `invalid_key_cols`).
    - Test 4 — `register_top_level_kind_table_still_rejected`: POST /register with `{"nodes":[{"kind":"table","name":"X","key_cols":[],"spec":{}}]}` (top-level kind=table, NOT derivation); assert HTTP 400 + `unsupported_node_kind`. Proves the surgical permit (Plan 05) only widens architectural-test allowlist, not the JSON-prelude shim (Plan 12.7-01).
    - Test 5 — `mixed_global_and_per_entity_in_same_registry_works`: register Tx + UserSpend (key_cols=["user_id"]) + GlobalCounter (key_cols=[]) all in one register; push events; GET on UserSpend with `entity_id="alice"` and on GlobalCounter with `entity_id=""`; both return populated features. Confirms per-entity and global tables coexist in one registry.
    - Test 6 — `op_batch_get_heterogeneous_per_entity_and_global_works`: register the same; OP_BATCH_GET (or POST /batch_get) with `{"requests":[{"table":"UserSpend","entity_id":"alice"},{"table":"GlobalCounter","entity_id":""}]}`; assert results contain both. This is the same test shape as Plan 03 Test 5 (which is `#[ignore]`d there); covered separately in Test 6 here as a self-contained acceptance.
  </behavior>
  <action>
Create `crates/beava-server/tests/phase13_4_global_table_routing.rs`. TestServer harness; use `serde_json::json!` for payloads; assert response shapes.

Run `cargo test -p beava-server --test phase13_4_global_table_routing` — CONFIRM RED (Tests 1, 2, 5, 6 fail because the engine doesn't yet accept key_cols=[]; Tests 3, 4 may pass by leveraging existing validation).

Commit:
```
test(13.4-09): add failing tests for global-table routing (key_cols=[] + sentinel entity_id)
```
  </action>
  <verify>
    <automated>cargo test -p beava-server --test phase13_4_global_table_routing 2>&1 | tail -10 | grep -E 'FAILED' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - File exists ≥ 80 lines: `[ -f crates/beava-server/tests/phase13_4_global_table_routing.rs ] && [ $(wc -l < crates/beava-server/tests/phase13_4_global_table_routing.rs) -ge 80 ]`
    - Six test fns: `grep -c '#\[tokio::test\]\|#\[test\]' crates/beava-server/tests/phase13_4_global_table_routing.rs` returns ≥ `6`
    - References `key_cols: []`: `grep -q 'key_cols' crates/beava-server/tests/phase13_4_global_table_routing.rs`
    - References sentinel `entity_id: ""`: `grep -q '"entity_id":\s*""' crates/beava-server/tests/phase13_4_global_table_routing.rs || grep -q "\"entity_id\":\s*\"\"" crates/beava-server/tests/phase13_4_global_table_routing.rs`
    - Cargo test exits non-zero (RED): `cargo test -p beava-server --test phase13_4_global_table_routing; [ $? -ne 0 ]`
    - Commit: `git log -1 --format=%s | grep -E '^test\(13\.4-09\):'`
  </acceptance_criteria>
  <done>RED. Commit lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 9.b (GREEN): Engine acceptance — register_validate + OpNode::Table variant + sentinel routing comment</name>
  <read_first>
    - crates/beava-core/src/register_validate.rs (existing register-payload validation pipeline; the place where `output_kind` field would be examined)
    - crates/beava-core/src/op_node.rs (existing OpNode enum)
    - crates/beava-core/src/registry_diff.rs (PayloadNode discriminator)
    - crates/beava-core/src/agg_compile.rs (where derivations get compiled into apply-path representations — see if there's a place to add the OpNode::Table arm)
    - crates/beava-server/src/apply_shard.rs::dispatch_get_sync, dispatch_push_sync (confirm they handle entity_id="" without explicit rejection)
  </read_first>
  <files>
    - crates/beava-core/src/register_validate.rs
    - crates/beava-core/src/op_node.rs
    - crates/beava-core/src/registry_diff.rs
    - crates/beava-core/src/agg_compile.rs (compile-arm for OpNode::Table)
    - crates/beava-server/src/apply_shard.rs (comment-only — confirm sentinel routing)
  </files>
  <action>
**Step 1 — `crates/beava-core/src/op_node.rs`:** re-introduce the `OpNode::Table` variant. Per ADR-001 partial overturn the variant is permitted ONLY for derivation `output_kind=table`:

```rust
/// Plan 13.4-09 — re-introduced per ADR-001 partial overturn.
/// ONLY produced as a derivation `output_kind=table` (per
/// `register_validate.rs::compile_node`); never as a top-level
/// register node (Plan 12.7-01 shim rejects that path).
///
/// No upsert/delete/retract verbs surface (those stay killed per
/// project_v0_events_only_scope; the architectural test
/// `phase12_7_no_table_surface.rs::FORBIDDEN_PATTERNS` still lists
/// `OpNode::TableUpsert / TableDelete / TableRetract`).
Table(TableDerivationOutput),
```

Add the supporting struct:

```rust
#[derive(Debug, Clone, PartialEq)]
pub struct TableDerivationOutput {
    pub name: String,
    pub key_cols: Vec<String>,    // empty = global per ADR-003
    pub source: String,
    pub ops: Vec<AggOpDescriptor>,
}
```

**Step 2 — `crates/beava-core/src/registry_diff.rs::PayloadNode`:** the `Derivation(DerivationPayload)` arm needs to carry `output_kind` field. Read the existing `DerivationPayload` struct; add `pub output_kind: Option<String>` (default None which means treat as scalar/single-value derivation; `Some("table")` flags table-output).

**Step 3 — `crates/beava-core/src/register_validate.rs`:** during register-payload validation, when a derivation node has `output_kind: "table"`:

- Validate `key_cols` is present (not null). Empty array (global) and non-empty (per-entity) are both valid.
- Validate `source` references an existing event source.
- Validate ops are bounded per V0-MEM-GOV-02.
- Compile into `OpNode::Table(TableDerivationOutput { ... })` and add to the registry alongside the existing event/derivation paths.

If `output_kind` is absent or `"scalar"` (or whatever the existing default value is), the existing path runs. The acceptance is purely additive — derivations with `output_kind=table` no longer fail with `unsupported_node_kind` or with a strict-serde "unknown field" error.

**Step 4 — `crates/beava-core/src/agg_compile.rs`:** add a compile arm for `OpNode::Table` that constructs the per-entity hashmap state. Per ADR-003 the existing per-entity hashmap machinery handles `entity_id=""` natively, so the compile path should be the SAME shape as a per-entity derivation (with key_cols), just with potentially-empty key_cols. The dispatch path doesn't differentiate.

If you find code in apply_shard.rs that explicitly rejects empty entity_id (search `if entity_id.is_empty() { return Err... }` or similar), REMOVE that rejection — that's the "absence of a special-case rejection" from ADR-003.

**Step 5 — `crates/beava-server/src/apply_shard.rs`:** add a comment block above `dispatch_get_sync` and `dispatch_push_sync` citing ADR-003:

```rust
// Plan 13.4-09 / ADR-003 — global-table sentinel routing.
//
// Per ADR-003 the empty-string entity_id is the global-table sentinel
// and is forwarded as-is to the per-entity hashmap key path. NO
// special-case branch — the existing &str hashmap accepts "" natively.
// Confirmed by integration test phase13_4_global_table_routing::Test 2.
```

If a search for `is_empty()` on entity_id finds an explicit rejection, remove it (with a comment citing this plan).

**Step 6 — re-run integration test:**

```bash
cargo test -p beava-server --test phase13_4_global_table_routing
```
Expected: 6/6 pass.

**Step 7 — workspace check (very important: the architectural-test edit from Plan 05 means re-introducing OpNode::Table won't trip the architectural test):**

```bash
cargo test --workspace
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo fmt --all --check
cargo test -p beava-server --test phase12_7_no_table_surface
cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed
```

All must pass. Phase 12.7 architectural-test still passes because:
- Bare `OpNode::Table` is permitted (Plan 05 surgical permit)
- Per-variant `OpNode::TableUpsert` / `TableDelete` / `TableRetract` STAY forbidden — those variants are NOT re-introduced in this plan
- `app.upsert / app.delete / app.retract` surface STAYS forbidden — no SDK verbs
- `TemporalStore`, `MvccVersion`, `temporal_http`, `RecordType::Table*` STAY forbidden — none re-introduced

Commit:
```
feat(13.4-09): engine acceptance for output_kind=table + key_cols=[] global routing

Re-introduces OpNode::Table variant per ADR-001 partial overturn —
permitted ONLY as a derivation output_kind=table (Plan 12.7-01 shim
still rejects top-level kind=table register; verified by
phase13_4_global_table_routing::Test 4).

Register validation accepts:
- output_kind: "table" on derivations (ADR-001)
- key_cols: [] (empty array — global table per ADR-003)
- key_cols: ["..."] (existing — per-entity)

Rejects:
- key_cols: null → 400 invalid_key_cols
- top-level kind: "table" → 400 unsupported_node_kind (Plan 12.7-01 shim, unchanged)

Per ADR-003 the empty-string entity_id is the global-table sentinel;
the existing &str hashmap key path accepts "" natively — no
special-case branch added in apply_shard.rs; just a comment block
citing the ADR.

Implements V0-GLOBAL-AGG-01 (engine sentinel routing) and ADR-001
§Deferred for Phase 13.4 (output_kind=table field plumbing).
```
  </action>
  <verify>
    <automated>cargo test -p beava-server --test phase13_4_global_table_routing 2>&1 | grep -q '6 passed' && cargo test -p beava-server --test phase12_7_no_table_surface 2>&1 | grep -q 'test result: ok' && cargo test --workspace 2>&1 | tail -10 | grep -E 'test result: ok' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - OpNode::Table variant: `grep -q 'OpNode::Table\|Table(TableDerivationOutput)' crates/beava-core/src/op_node.rs`
    - TableDerivationOutput struct: `grep -q 'pub struct TableDerivationOutput' crates/beava-core/src/op_node.rs`
    - register_validate accepts output_kind: `grep -q 'output_kind' crates/beava-core/src/register_validate.rs`
    - registry_diff carries output_kind: `grep -q 'output_kind' crates/beava-core/src/registry_diff.rs`
    - apply_shard ADR-003 comment: `grep -q 'ADR-003' crates/beava-server/src/apply_shard.rs`
    - 6/6 global-routing tests: `cargo test -p beava-server --test phase13_4_global_table_routing 2>&1 | grep -q '6 passed'`
    - 12.7 architectural test still passes: `cargo test -p beava-server --test phase12_7_no_table_surface 2>&1 | grep -q 'test result: ok'`
    - 12.7 legacy-killed test unchanged: `cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed 2>&1 | grep -q 'test result: ok'`
    - Workspace clean: `cargo test --workspace 2>&1 | grep -E 'test result: ok' | tail -1` succeeds
    - Clippy clean: `cargo clippy --workspace --all-targets --all-features -- -D warnings` returns 0
    - Commit: `git log -1 --format=%s | grep -E '^feat\(13\.4-09\):'`
  </acceptance_criteria>
  <done>GREEN for engine acceptance. Variant + register-validate + sentinel routing comment lands; architectural tests still pass. Commit lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 9.c: Un-ignore the 3 cross-plan tests; verify they all pass GREEN</name>
  <read_first>
    - crates/beava-server/tests/phase13_4_op_batch_get.rs (Plan 03 — Test 5 ignored)
    - crates/beava-server/tests/phase13_4_table_derivation_allowed.rs (Plan 05 — Test 3 ignored)
    - crates/beava-server/tests/phase13_4_force_register.rs (Plan 06 — Test 9 ignored)
  </read_first>
  <files>
    - crates/beava-server/tests/phase13_4_op_batch_get.rs
    - crates/beava-server/tests/phase13_4_table_derivation_allowed.rs
    - crates/beava-server/tests/phase13_4_force_register.rs
  </files>
  <action>
**Step 1** — for each of the 3 test files, find the `#[ignore]` attribute that was added per the plan's TDD red-phase (look for the `Plan 13.4-09` comment immediately above the `#[ignore]` line; remove the `#[ignore]` line and the explanatory comment that points to "Plan 09's commit removes this `#[ignore]` attribute").

**Step 2** — run all three:

```bash
cargo test -p beava-server --test phase13_4_op_batch_get
cargo test -p beava-server --test phase13_4_table_derivation_allowed
cargo test -p beava-server --test phase13_4_force_register
```

Expected: all of them now pass with all tests un-ignored (Plan 03's 5/5, Plan 05's 3/3, Plan 06's 9/9 force tests).

**Step 3** — workspace check:

```bash
cargo test --workspace
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo fmt --all --check
```

Commit:
```
test(13.4-09): un-ignore Plan 03/05/06 cross-plan tests now that engine accepts output_kind=table

Plan 13.4-09 lands the engine acceptance for output_kind=table
derivations + key_cols=[] global routing per ADR-003 + ADR-001.
Three previously-ignored cross-plan tests now pass:

- phase13_4_op_batch_get::http_batch_get_with_global_table_entity_id_empty (Plan 03 Test 5)
- phase13_4_table_derivation_allowed::derivation_with_output_kind_table_succeeds (Plan 05 Test 3)
- phase13_4_force_register::register_destructive_agg_removal_without_force_returns_409 (Plan 06 Test 9)

Removes the #[ignore] attributes; cross-plan acceptance complete.

Note on commit type: per CLAUDE.md TDD §Note 4, this is a `test:`
commit because it activates previously-skipped tests; no production
code changes.
```
  </action>
  <verify>
    <automated>! grep -B2 'http_batch_get_with_global_table_entity_id_empty' crates/beava-server/tests/phase13_4_op_batch_get.rs | grep -q '#\[ignore\]' && ! grep -B2 'derivation_with_output_kind_table_succeeds' crates/beava-server/tests/phase13_4_table_derivation_allowed.rs | grep -q '#\[ignore\]' && ! grep -B2 'register_destructive_agg_removal_without_force_returns_409' crates/beava-server/tests/phase13_4_force_register.rs | grep -q '#\[ignore\]' && cargo test -p beava-server 2>&1 | tail -10 | grep -E 'test result: ok' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Plan 03 Test 5 un-ignored: `! grep -B2 'http_batch_get_with_global_table_entity_id_empty' crates/beava-server/tests/phase13_4_op_batch_get.rs | grep -q '#\[ignore\]'`
    - Plan 05 Test 3 un-ignored: `! grep -B2 'derivation_with_output_kind_table_succeeds' crates/beava-server/tests/phase13_4_table_derivation_allowed.rs | grep -q '#\[ignore\]'`
    - Plan 06 Test 9 un-ignored: `! grep -B2 'register_destructive_agg_removal_without_force_returns_409' crates/beava-server/tests/phase13_4_force_register.rs | grep -q '#\[ignore\]'`
    - All three un-ignored tests pass: cross-plan files run clean — `cargo test -p beava-server --test phase13_4_op_batch_get 2>&1 | grep -q 'test result: ok' && cargo test -p beava-server --test phase13_4_table_derivation_allowed 2>&1 | grep -q 'test result: ok' && cargo test -p beava-server --test phase13_4_force_register 2>&1 | grep -q 'test result: ok'`
    - Workspace clean: `cargo test --workspace 2>&1 | grep -E 'test result: ok' | tail -1` succeeds
    - Clippy clean: `cargo clippy --workspace --all-targets --all-features -- -D warnings` returns 0
    - fmt clean: `cargo fmt --all --check` returns 0 diffs
    - Commit: `git log -1 --format=%s | grep -E '^test\(13\.4-09\):'`
  </acceptance_criteria>
  <done>3 cross-plan tests un-ignored and passing. V0-GLOBAL-AGG-01 + ADR-001 §Deferred-for-13.4 fully implemented. Commit lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /register (output_kind=table) | New register-time field acceptance; same trust posture as existing derivation register. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.4-09-01 | Information Disclosure | Global-table state visible to any client (no auth in v0) | accept | Same posture as per-entity tables — v0 has no auth surface; production deployments must front-door beava with their own auth. Documented elsewhere. |
| T-13.4-09-02 | DoS | Unbounded global-table state growth | accept | Per ADR-003 §Negative: "global-table state could grow unbounded if users put `bv.count(window=\"forever\")` on a global table. However, standard memory governance applies: cold_after= doesn't make sense for global (sentinel `\"\"` is always-live); Lifetime ops still subject to V0-MEM-GOV-02 lifetime-bound enforcement; top_k(k=N) and other BoundedByConfig ops have their normal caps. The single state slot per global table is bounded by the per-table state size (~hundreds of bytes for BoundedSketch ops at most), independent of entity count." |
| T-13.4-09-03 | Tampering | OpNode::Table re-introduction could allow user-mutable surface to creep in | mitigate | Per-variant entries TableUpsert/TableDelete/TableRetract STAY forbidden in FORBIDDEN_PATTERNS (Plan 05 surgical permit was narrow). app.upsert/delete/retract STAY absent (architectural test enforces). No new wire opcode for upsert/delete/retract. Memo: future contributors must consult ADR-001 before extending the surface. |
</threat_model>

<verification>
- 3 commits: `git log --format=%s | grep -E '^(test|feat)\(13\.4-09\):' | wc -l` returns ≥ 3
- All 3 cross-plan tests un-ignored AND passing
- `cargo test --workspace` returns 0 failures
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` returns 0
- `cargo fmt --all --check` returns 0 diffs
- Plan 12.7 architectural test still passes (per-variant entries enforce; bare `OpNode::Table` permitted)
- Plan 12.7 legacy-killed file tripwire UNCHANGED
</verification>

<success_criteria>
- `OpNode::Table` variant re-introduced (narrowly — only for derivation output_kind=table)
- `TableDerivationOutput` struct with name + key_cols + source + ops
- `register_validate.rs` accepts `output_kind: "table"` on derivations + `key_cols: []` (global)
- Top-level `kind: "table"` STILL rejected (12.7-01 shim unchanged)
- `key_cols: null` returns structured 400 with clear error code
- Sentinel `entity_id = ""` routes through existing hashmap (apply_shard.rs comment cites ADR-003)
- 6/6 phase13_4_global_table_routing tests pass
- 3 cross-plan ignored tests un-ignored AND passing
- Workspace green
- 3 commits: `test(13.4-09):` (RED) → `feat(13.4-09):` (engine GREEN) → `test(13.4-09):` (un-ignore)
</success_criteria>

<output>
After completion, create `.planning/phases/13.4-engine-prep-wire-spec/13.4-09-SUMMARY.md`. Self-Check verifying:
- [x] OpNode::Table variant + TableDerivationOutput struct
- [x] register_validate accepts output_kind=table + key_cols=[] (global)
- [x] register_validate rejects key_cols=null
- [x] register_validate (via 12.7-01 shim) still rejects top-level kind=table
- [x] apply_shard.rs comment cites ADR-003 (sentinel routing)
- [x] 6/6 phase13_4_global_table_routing tests pass
- [x] Plan 03 Test 5, Plan 05 Test 3, Plan 06 Test 9 all un-ignored AND pass
- [x] phase12_7 architectural tests still pass (per-variant entries hold)
- [x] Workspace green; commits show test → feat → test
</output>
