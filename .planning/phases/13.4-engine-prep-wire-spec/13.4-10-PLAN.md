---
phase: 13.4-engine-prep-wire-spec
plan: 10
type: execute
wave: 5
depends_on: ["01", "02", "03", "04", "05", "06", "07", "08", "09"]
files_modified:
  - .planning/perf-baselines.md
  - .planning/throughput-baselines.md
  - .planning/phases/13.4-engine-prep-wire-spec/13.4-SUMMARY.md
  - .planning/phases/13.4-engine-prep-wire-spec/13.4-VERIFICATION.md
  - crates/beava-core/benches/apply_path_bench.rs   # touch-only — re-run with renamed ops; no signature change
autonomous: true
requirements:
  - V0-EVENTS-ONLY-01
  - V0-MEM-GOV-01
  - V0-MEM-GOV-02
  - V0-MEM-GOV-03
  - V0-GLOBAL-AGG-01
addresses_decisions:
  - "CLAUDE.md §Conventions §Performance Discipline (Phase 6+ contract): every phase ships at least one criterion microbench covering its hot path. This plan re-runs `apply_path_bench.rs` against the renamed-ops + sentinel-routing scope and appends to `.planning/perf-baselines.md`. 10% slower than Phase 12.9 baseline → WARNING; 25% → BLOCKER."
  - "CLAUDE.md §Conventions §Performance Discipline (Phase 8+ contract): every phase ships a throughput-run row per (pipeline-shape, transport) tuple appended to `.planning/throughput-baselines.md`. This plan re-runs `crates/beava-bench/blast_shape_bench.rs` against fraud-team / small / medium / large × HTTP / TCP. 10% slower than Phase 12.9 small/tcp → WARN; 25% → BLOCK."
  - "Phase closure: write 13.4-SUMMARY.md and 13.4-VERIFICATION.md. STATE.md and ROADMAP.md advancement is OWNED BY THE PARENT ORCHESTRATOR after all 4 sibling agents (13.4/5/6/7) finish — this plan does NOT touch those files."
must_haves:
  truths:
    - "`apply_path_bench` re-runs cleanly after Plans 01-09 land; new measurement appended to `.planning/perf-baselines.md` § Phase 13.4 section"
    - "Throughput-run cells produced: `fraud-team / small / medium / large` × `http / tcp` = 8 rows appended to `.planning/throughput-baselines.md` § Phase 13.4 section"
    - "If the small/tcp regression-gate is breached (>25% slower than the most-recent prior baseline in the same hw-class — likely Phase 12.9), the plan stops with BLOCKER; if 10-25% slower, plan proceeds with WARNING captured in 13.4-VERIFICATION.md"
    - "13.4-SUMMARY.md exists and lists the 9 prior plans + their commit SHAs (executor reads `git log` to populate)"
    - "13.4-VERIFICATION.md exists and lists pass/fail for each of CONTEXT's 4 success criteria + 7 must_have truths from CONTEXT scope"
    - "STATE.md and ROADMAP.md UNTOUCHED — parent orchestrator owns those"
    - "`.planning/perf-baselines.md` and `.planning/throughput-baselines.md` are append-only ledgers; this plan adds new section, doesn't edit prior sections"
  artifacts:
    - path: ".planning/phases/13.4-engine-prep-wire-spec/13.4-SUMMARY.md"
      provides: "Phase summary listing all 10 plans + their commits, the wave structure, the perf + throughput numbers, deferred-to-other-phases tracking (Plan 13.5/6/7 SDK work), and a closing self-check."
      min_lines: 100
    - path: ".planning/phases/13.4-engine-prep-wire-spec/13.4-VERIFICATION.md"
      provides: "Phase verification against CONTEXT's 4 success criteria + 7 must_have truths; perf-gate + throughput-gate results; tells whether parent orchestrator can advance."
      min_lines: 80
    - path: ".planning/perf-baselines.md"
      provides: "Append-only — new `## Phase 13.4` section with `apply_path_bench` cell measurements (cold_key/14_aggs from fraud-team-shape synthetic) + comparison to Phase 12.9 baseline."
      contains: "Phase 13.4"
    - path: ".planning/throughput-baselines.md"
      provides: "Append-only — new `## Phase 13.4` section with 8 rows: 4 pipeline shapes × 2 transports."
      contains: "Phase 13.4"
  key_links:
    - from: "Phase 12.9 baseline (last prior baseline)"
      to: "Phase 13.4 measurement"
      via: "Same hw-class (Apple-M4 per memory `project_phase19_*`); same bench harness; same input shape (fraud-team.json with renamed ops)."
      pattern: "Phase 12.9|fraud-team"
    - from: "13.4-VERIFICATION.md"
      to: "parent orchestrator STATE/ROADMAP advancement"
      via: "VERIFICATION.md asserts whether the phase is shippable; orchestrator reads it to decide whether to advance STATE.md and ROADMAP.md."
      pattern: "VERIFICATION.md"
---

<objective>
Closure plan for Phase 13.4: re-run the criterion microbench (per CLAUDE.md Phase 6+ contract) and the throughput-bench matrix (per Phase 8+ contract); append numbers to baselines; write `13.4-SUMMARY.md` + `13.4-VERIFICATION.md` for the parent orchestrator.

**This plan is doc-only (no code changes; the bench files exist from prior phases — only re-runs).** Per CLAUDE.md TDD §Note 4 (pure-doc commits use single `docs(...):` commits), there's no red-then-green discipline required for this plan.

**Per orchestrator constraint:** STATE.md, ROADMAP.md, and CLAUDE.md are owned by the parent orchestrator (which advances them after all 4 sibling agents 13.4/5/6/7 finish). This closure plan ONLY writes inside `.planning/phases/13.4-engine-prep-wire-spec/` plus the append-only perf/throughput ledgers.

**Wave 3 placement:** depends on Plans 01-09 (must run after all engine work lands so the bench harness exercises the final state). This is the LAST plan in Phase 13.4.

Output:
- 1 perf-baseline append + 1 throughput-baseline append
- 13.4-SUMMARY.md (≥100 lines)
- 13.4-VERIFICATION.md (≥80 lines)
- 3 commits (microbench, throughput, closure docs)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13.4-engine-prep-wire-spec/13.4-CONTEXT.md
@.planning/phases/13.4-engine-prep-wire-spec/SCRATCH-PLANNER-NOTES.md
@.planning/perf-baselines.md
@.planning/throughput-baselines.md
@crates/beava-core/benches/apply_path_bench.rs
@crates/beava-bench/benches/blast_shape_bench.rs
</context>

<tasks>

<task type="auto">
  <name>Task 10.a: Run criterion microbench (apply_path_bench) and append to perf-baselines.md</name>
  <read_first>
    - crates/beava-core/benches/apply_path_bench.rs (existing bench — no edit needed; the bench targets the renamed ops via fraud-team-shape synthetic registry)
    - .planning/perf-baselines.md (read the existing § Phase 12.9 + § Phase 19.x sections to match formatting)
  </read_first>
  <files>.planning/perf-baselines.md</files>
  <action>
**Step 1 — re-run microbench:**

```bash
cargo bench -p beava-core --bench apply_path_bench -- --save-baseline phase13_4
```

This produces criterion output for the canonical cell `apply_path/cold_key/14_aggs` (post-stacked-fix shape from Phase 19.2 — see `.planning/perf-baselines.md` line 301 for the existing Phase 19.2 entry at 1.424 µs).

The bench runs 100 iterations per measurement (criterion default); record the median ns/op number from the output line `time:   [<lower> <median> <upper>]`.

**Step 2 — compare to Phase 12.9 baseline:**

The most recent prior baseline (in the same hw-class — Apple-M4 per memory `project_phase19_*`) is Phase 12.9 (post-AggOp-boxing). Find the relevant cell in `.planning/perf-baselines.md`:

```bash
grep -A2 'Phase 12.9' .planning/perf-baselines.md | head -20
```

Compute the regression delta: `(new_median - old_median) / old_median × 100`.

**Step 3 — apply regression gates per CLAUDE.md §Conventions §Performance Discipline:**

- delta < 10% slower → PASS, append numbers
- 10% ≤ delta < 25% slower → WARNING (append numbers + WARN comment in baselines.md AND in 13.4-VERIFICATION.md)
- delta ≥ 25% slower → BLOCKER (do NOT advance; flag in 13.4-VERIFICATION.md; parent orchestrator decides whether to override or fix)

**Step 4 — append to `.planning/perf-baselines.md`** with the standard section format (mirror existing § Phase 12.9 / § Phase 19.4 structure):

```markdown
---

## Phase 13.4 — Engine prep / wire-spec conformance — 2026-05-04 (or actual date)

**Microbench:** `apply_path_bench::cold_key/14_aggs` (post-Plans 01-09)
**Hw class:** Apple-M4 (per project_phase19_*)
**Builder:** rustc 1.87 cargo bench --release
**Synthetic registry shape:** 14 features (7 user-keyed, 4 user×merchant-keyed, 3 device-keyed) — fraud-team-shape stand-in. Now uses Polars-style op names (mean/var/std/n_unique/quantile per ADR-002 / Plan 13.4-01); op-string mapping is pure string-table change so this microbench should be ZERO regression vs Phase 12.9 / 19.x.

| Metric | Value | Phase 12.9 baseline | Δ | Verdict |
|--------|-------|---------------------|---|---------|
| apply_path/cold_key/14_aggs | <fill in> µs | 1.424 µs | <fill in>% | <PASS / WARN / BLOCK> |

**Driver:** Phase 13.4 is mostly mechanical conformance work against the wire-spec (~800 LOC). Op renames are pure string-table edits (Plan 01); GET row-shape / OP_BATCH_GET / verb routes / force&dry_run / Persistence::Memory / OP_RESET / global-sentinel are all surface additions or response-shape edits — none touch the apply hot path's per-event arithmetic. Expected: zero regression vs 12.9. Any measurable regression is investigated before parent orchestrator advances STATE.md.

[If WARN/BLOCK: include investigation notes — most likely culprit is the new register-validate diff classifier (Plan 06 force=true) running on EVERY register; if so, register-time cost is cold-path and bench shouldn't see it.]
```

**Step 5 — commit:**

```
docs(13.4-10): record Phase 13.4 microbench baseline (apply_path_bench)
```
  </action>
  <verify>
    <automated>grep -A 5 '## Phase 13.4' .planning/perf-baselines.md | head -10</automated>
    Expected: section exists with apply_path cell row.
  </verify>
  <acceptance_criteria>
    - § Phase 13.4 section appended: `grep -q '## Phase 13.4' .planning/perf-baselines.md`
    - Section contains apply_path cell: `grep -A 20 '## Phase 13.4' .planning/perf-baselines.md | grep -q 'apply_path'`
    - Section contains Verdict (PASS/WARN/BLOCK): `grep -A 20 '## Phase 13.4' .planning/perf-baselines.md | grep -E 'PASS|WARN|BLOCK'`
    - If BLOCK: plan stops here and surfaces the issue in 13.4-VERIFICATION.md (Task 10.c handles the verification doc); parent orchestrator decides
    - Commit message: `git log -1 --format=%s | grep -E '^docs\(13\.4-10\):'`
  </acceptance_criteria>
  <done>Microbench result captured in perf-baselines.md with regression-gate verdict.</done>
</task>

<task type="auto">
  <name>Task 10.b: Run throughput bench matrix (4 shapes × 2 transports = 8 cells) and append to throughput-baselines.md</name>
  <read_first>
    - crates/beava-bench/benches/blast_shape_bench.rs (the bench harness)
    - crates/beava-bench/configs/{fraud-team,small,medium,large}.json (the 4 pipeline shapes — note: Plan 01 renamed the op-strings here; the bench should run with the new names)
    - .planning/throughput-baselines.md (read existing tables + § Phase 12.9 / § Phase 19.x structure for consistent formatting)
  </read_first>
  <files>.planning/throughput-baselines.md</files>
  <action>
**Step 1 — run bench matrix:**

For each (shape, transport) tuple, run:

```bash
# example for small + tcp
BEAVA_BENCH_SHAPE=crates/beava-bench/configs/small.json \
BEAVA_BENCH_TRANSPORT=tcp \
cargo bench -p beava-bench --bench blast_shape_bench
```

(Adapt the env vars / args to whatever the bench harness actually expects — read the bench source to find the conventions. The bench produces an EPS measurement.)

The 8 cells:
- `(fraud-team, http)`, `(fraud-team, tcp)`
- `(small, http)`, `(small, tcp)` — primary regression gate per CONTEXT
- `(medium, http)`, `(medium, tcp)`
- `(large, http)`, `(large, tcp)`

**Step 2 — apply regression gate to small/tcp:**

The primary gate per Phase 8+ contract is small/tcp's EPS within ±10% of Phase 12.9's small/tcp baseline. Find that prior baseline:

```bash
grep -B2 -A5 '12.9.*small.*tcp\|small.*tcp.*12.9' .planning/throughput-baselines.md
```

Compute delta. Apply the same 10/25% gate as Task 10.a.

**Step 3 — append to throughput-baselines.md** with the standard section format (mirror existing § Phase 19.x sections — see lines around 1481, 1648):

```markdown
---

## Phase 13.4 — Engine prep / wire-spec conformance — 2026-05-04

**Bench:** `crates/beava-bench/benches/blast_shape_bench.rs`
**Hw class:** Apple-M4 (per memory `project_phase19_*`)
**Configs:** `crates/beava-bench/configs/{fraud-team,small,medium,large}.json` — UPDATED with Polars-style op names (Plan 13.4-01 renamed avg→mean / variance→var / stddev→std / count_distinct→n_unique / percentile→quantile). Bench shape unchanged; only op-string keys flipped.

### Push throughput matrix (8 cells):

| Pipeline shape | Transport | EPS (Phase 13.4) | EPS (Phase 12.9 baseline) | Δ% | Verdict |
|----------------|-----------|------------------|---------------------------|-----|---------|
| small          | http      | <fill>           | <fill>                    | <fill>% | <PASS/WARN/BLOCK> |
| small          | tcp       | <fill>           | <fill>                    | <fill>% | <PASS/WARN/BLOCK> ← **PRIMARY GATE** |
| medium         | http      | <fill>           | <fill>                    | <fill>% | <fill> |
| medium         | tcp       | <fill>           | <fill>                    | <fill>% | <fill> |
| large          | http      | <fill>           | <fill>                    | <fill>% | <fill> |
| large          | tcp       | <fill>           | <fill>                    | <fill>% | <fill> |
| fraud-team     | http      | <fill>           | <fill>                    | <fill>% | <fill> |
| fraud-team     | tcp       | <fill>           | <fill>                    | <fill>% | <fill> |

**Driver:** same as the microbench — Phase 13.4 is mostly mechanical conformance; expected zero EPS regression. Any measurable regression is investigated before advancing.

[If WARN/BLOCK on small/tcp specifically: stop and flag — Phase 13.4 success criterion #3 says "Throughput regression-gate `small/tcp` within ±10% of Phase 12.9 baseline".]
```

**Step 4 — commit:**

```
docs(13.4-10): record Phase 13.4 throughput baselines (8-cell matrix)
```
  </action>
  <verify>
    <automated>grep -A 30 '## Phase 13.4' .planning/throughput-baselines.md | head -30</automated>
    Expected: 8-row table with all 4 shapes × 2 transports + Verdict column.
  </verify>
  <acceptance_criteria>
    - § Phase 13.4 section appended: `grep -q '## Phase 13.4' .planning/throughput-baselines.md`
    - 8 cells covered: `grep -A 30 '## Phase 13.4' .planning/throughput-baselines.md | grep -E 'small|medium|large|fraud-team' | wc -l` returns ≥ 8
    - Each cell has a Verdict (PASS / WARN / BLOCK): `grep -A 30 '## Phase 13.4' .planning/throughput-baselines.md | grep -cE 'PASS|WARN|BLOCK'` returns ≥ 8
    - small/tcp row has explicit `PRIMARY GATE` annotation: `grep -A 30 '## Phase 13.4' .planning/throughput-baselines.md | grep -q 'PRIMARY GATE'`
    - Commit: `git log -1 --format=%s | grep -E '^docs\(13\.4-10\):'`
  </acceptance_criteria>
  <done>8-cell throughput matrix captured in throughput-baselines.md.</done>
</task>

<task type="auto">
  <name>Task 10.c: Write 13.4-SUMMARY.md and 13.4-VERIFICATION.md</name>
  <read_first>
    - .planning/phases/13.4-engine-prep-wire-spec/13.4-CONTEXT.md (4 success criteria + 7 must_have truths)
    - All 9 prior plans' SUMMARY.md files (read each to extract per-plan commit SHAs and key facts)
    - `git log --oneline --grep='13\.4-' --since='2026-05-03'` (extract all phase 13.4 commits in order)
    - .planning/phases/12.9-aggop-memory-boxing/12.9-SUMMARY.md (canonical phase-summary shape)
  </read_first>
  <files>
    - .planning/phases/13.4-engine-prep-wire-spec/13.4-SUMMARY.md
    - .planning/phases/13.4-engine-prep-wire-spec/13.4-VERIFICATION.md
  </files>
  <action>
**Step 1 — author 13.4-SUMMARY.md.** Standard phase summary shape (~100 lines):

```markdown
---
phase: 13.4-engine-prep-wire-spec
status: ready-for-orchestrator-advance
created: 2026-05-04
plans: 10
waves: 3
---

# Phase 13.4: Engine prep — server-side wire-spec conformance — Summary

## What landed

Phase 13.4 implements the server-side contract from Phase 13.0 (wire-spec / sdk-api / ADRs).
~800 LOC of mechanical conformance work; zero new architecture. Implements:

| # | Plan | Wave | Decision/scope | Commits |
|---|------|------|----------------|---------|
| 01 | Op renames (Polars conventions) | 1 | ADR-002 | <list SHAs> |
| 02 | GET response → flat dict | 1 | scope #2 | <SHAs> |
| 03 | OP_BATCH_GET (0x0024) | 1 | scope #3 | <SHAs> |
| 04 | Verb-style HTTP routes | 1 | scope #4 + A-07 | <SHAs> |
| 05 | Architectural-test surgical permit | 1 | D-04 + ADR-001 | <SHAs> |
| 06 | force=True + dry_run=True | 2 | D-01 + scope #6 | <SHAs> |
| 07 | Persistence::Memory backend | 2 | D-02 | <SHAs> |
| 08 | OP_RESET (0x0040) + test_mode gate | 3 | D-03 | <SHAs> |
| 09 | Global-table sentinel routing | 3 | ADR-003 + ADR-001 §Deferred | <SHAs> |
| 10 | Microbench + throughput + closure | 3 | CLAUDE.md perf gates | <SHAs> |

[List all commits in order with `git log --oneline | grep '13\.4-'`.]

## Architectural commitments honored

- mio-only Hot-Path Invariant (CLAUDE.md): ALL new dispatch (batch_get, reset) lives in `apply_shard.rs::dispatch_*_sync`; admin sidecar untouched.
- Events-Only Invariant (CLAUDE.md / V0-EVENTS-ONLY-01): top-level `kind: "table"` register STAYS rejected by the 12.7-01 shim. The ADR-001 partial overturn permits ONLY derivation `output_kind=table` (aggregation-output decorator); user-mutable surface (upsert/delete/retract) STAYS killed.
- TDD Discipline (CLAUDE.md Phase 3+): every code-bearing task split into red commit (`test(13.4-NN):`) followed by green commit (`feat(13.4-NN):` / `refactor:` / `chore:`). [Verify count of `test:` / `feat:` commits matches plan structure.]
- Performance Discipline (CLAUDE.md Phase 6+ + Phase 8+): apply_path_bench microbench + 8-cell throughput matrix appended to baselines. See Plan 10 for verdicts.
- mem-gov V0-MEM-GOV-01/02/03: untouched — Phase 13.4 doesn't add new operators; lifetime bounds + cold_after + bucket reclaim all carry through unchanged.

## Wave structure

- **Wave 1 (5 plans, parallel):** 01, 02, 03, 04, 05 — all independent file scopes
- **Wave 2 (2 plans, parallel):** 06 (force/dry_run on register_validate), 07 (Persistence::Memory on persistence + server)
- **Wave 3 (3 plans, sequential):** 08 (OP_RESET, depends on 04+07), 09 (global sentinel, depends on 05+06), 10 (closure, depends on 01-09)

## What's NEXT (parent orchestrator after 13.5/6/7 also finish)

Phase 13.4 is the engine-prep half of the v0 wire-spec rollout. Phase 13.5 (Python SDK) writes the client-side that calls these new endpoints. Phases 13.6 (TS+Go SDKs) and 13.7 (docs/examples) follow. The parent orchestrator advances STATE.md and ROADMAP.md after all 4 phases land.

## Performance verdicts

- **Microbench `apply_path/cold_key/14_aggs`:** [PASS/WARN/BLOCK + delta] vs Phase 12.9 baseline (1.424 µs). See `.planning/perf-baselines.md § Phase 13.4`.
- **Throughput small/tcp:** [PASS/WARN/BLOCK + delta] vs Phase 12.9 baseline. See `.planning/throughput-baselines.md § Phase 13.4`.

## Self-Check

- [x] 10 plans across 3 waves
- [x] All 4 user-locked decisions (D-01..D-04) honored verbatim
- [x] All 3 ADRs (ADR-001 partial revival, ADR-002 op renames, ADR-003 global routing) implemented
- [x] All 5 carry-forward requirements (V0-EVENTS-ONLY-01, V0-MEM-GOV-01/02/03, V0-GLOBAL-AGG-01) intact
- [x] mio-only invariant + events-only invariant + memory-governance invariant all preserved
- [x] TDD red→green discipline across all 9 code-bearing plans
- [x] Microbench + throughput baselines appended (perf-gate verdicts captured)
- [x] STATE.md / ROADMAP.md / CLAUDE.md UNTOUCHED (parent orchestrator owns)
```

**Step 2 — author 13.4-VERIFICATION.md.** Per-criterion + per-must-have shape (~80 lines):

```markdown
---
phase: 13.4-engine-prep-wire-spec
verification-date: 2026-05-04
verdict: <PASS / PASS-WITH-WARN / BLOCKER-PRESENT>
---

# Phase 13.4 Verification

## Success Criteria (from ROADMAP entry)

1. **All wire-spec endpoints return the documented response shape** (matched against `examples/wire/*.json` from Phase 13.0)
   - Status: <PASS / PARTIAL / FAIL>
   - Evidence: phase13_4_get_row_shape.rs (Plan 02), phase13_4_op_batch_get.rs (Plan 03), phase13_4_verb_routes_e2e.rs (Plan 04), phase13_4_force_register.rs (Plan 06), phase13_4_dry_run_register.rs (Plan 06), phase13_4_memory_persistence.rs (Plan 07), phase13_4_reset_*.rs (Plan 08), phase13_4_global_table_routing.rs (Plan 09) — all green.

2. **`cargo test --workspace` passes; clippy + fmt clean**
   - Status: <PASS / FAIL>
   - Evidence: [`cargo test --workspace 2>&1 | tail -10` output]; clippy + fmt outputs.

3. **Throughput regression-gate `small/tcp` within ±10% of Phase 12.9 baseline**
   - Status: <PASS / WARN / BLOCK>
   - Evidence: `.planning/throughput-baselines.md § Phase 13.4` row `small / tcp`.

4. **New tests for: row-shape GET, batch_get heterogeneous, force=True diff (additive vs destructive), dry_run=True, in-memory mode boot, reset, global-table register + GET round-trip per ADR-003**
   - Status: <PASS / PARTIAL>
   - Evidence: 8 test files added (phase13_4_*.rs); all green at end of Wave 3.

## Must-Have Truths (from CONTEXT)

[List the 7 must_have truths from CONTEXT scope items 1-10 + meta scope. For each: PASS / FAIL with evidence command.]

## Architectural-test verifications

- `phase12_7_no_table_surface::forbidden_pattern_walk` — PASS (Plan 05 surgical permit narrows the list; per-variant entries stay)
- `phase12_7_legacy_table_handlers_killed::all` — PASS (UNCHANGED file)
- `phase12_6_mio_only_dataplane::*` — PASS (no new tokio data-plane introduced; Plan 03 batch_get + Plan 08 reset both go through mio dispatch)
- `phase12_8_lifetime_ops_have_bounds::*` — PASS (no new operators; existing bounds intact after op rename)
- `op_lifetime_bounds::*` — PASS (renamed strings updated in lockstep with parse_agg_kind per Plan 01)

## Verdict

[PASS / PASS-WITH-WARN / BLOCKER-PRESENT — depending on perf gate]

[If BLOCKER: list what's blocking + remediation path]

## Parent-orchestrator action

[If PASS:] Advance STATE.md to "Phase 13.4 verified, awaiting 13.5/13.6/13.7 sibling completion before advancing ROADMAP.md."

[If BLOCKER-PRESENT:] Hold advancement; surface issue to user.
```

**Step 3 — commit:**

```
docs(13.4-10): write 13.4-SUMMARY.md and 13.4-VERIFICATION.md (phase closure)
```
  </action>
  <verify>
    <automated>[ -f .planning/phases/13.4-engine-prep-wire-spec/13.4-SUMMARY.md ] && [ $(wc -l < .planning/phases/13.4-engine-prep-wire-spec/13.4-SUMMARY.md) -ge 100 ] && [ -f .planning/phases/13.4-engine-prep-wire-spec/13.4-VERIFICATION.md ] && [ $(wc -l < .planning/phases/13.4-engine-prep-wire-spec/13.4-VERIFICATION.md) -ge 80 ]</automated>
  </verify>
  <acceptance_criteria>
    - 13.4-SUMMARY.md exists ≥ 100 lines: `[ -f .planning/phases/13.4-engine-prep-wire-spec/13.4-SUMMARY.md ] && [ $(wc -l < .planning/phases/13.4-engine-prep-wire-spec/13.4-SUMMARY.md) -ge 100 ]`
    - 13.4-VERIFICATION.md exists ≥ 80 lines: `[ -f .planning/phases/13.4-engine-prep-wire-spec/13.4-VERIFICATION.md ] && [ $(wc -l < .planning/phases/13.4-engine-prep-wire-spec/13.4-VERIFICATION.md) -ge 80 ]`
    - SUMMARY references all 10 plans: `grep -c '13.4-0\|13.4-10' .planning/phases/13.4-engine-prep-wire-spec/13.4-SUMMARY.md` returns ≥ 10
    - VERIFICATION lists explicit verdicts: `grep -cE 'PASS|WARN|BLOCK|FAIL' .planning/phases/13.4-engine-prep-wire-spec/13.4-VERIFICATION.md` returns ≥ 4 (one per success criterion)
    - STATE.md UNTOUCHED in this plan's commit range: `git diff HEAD~3..HEAD -- .planning/STATE.md | wc -l` returns `0`
    - ROADMAP.md UNTOUCHED: `git diff HEAD~3..HEAD -- .planning/ROADMAP.md | wc -l` returns `0`
    - CLAUDE.md UNTOUCHED: `git diff HEAD~3..HEAD -- CLAUDE.md | wc -l` returns `0`
    - Commit: `git log -1 --format=%s | grep -E '^docs\(13\.4-10\):'`
  </acceptance_criteria>
  <done>SUMMARY + VERIFICATION docs land. STATE/ROADMAP/CLAUDE untouched per orchestrator constraint. Phase 13.4 ready for parent-orchestrator advancement once 13.5/13.6/13.7 also finish.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → orchestrator | The orchestrator reads VERIFICATION.md to decide advancement. |

## STRIDE Threat Register

Plan 10 is doc + bench-rerun only. No new code surface; no threats.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.4-10-01 | Repudiation | Bench numbers could be misreported; orchestrator advances on bad data | mitigate | Bench commands + raw output preserved in commit messages so the numbers are reproducible. perf-baselines.md is append-only — prior baselines stay readable as cross-check. |
</threat_model>

<verification>
- 3 commits: `git log --format=%s | grep -E '^docs\(13\.4-10\):' | wc -l` returns ≥ 3
- `.planning/perf-baselines.md` has new § Phase 13.4 section
- `.planning/throughput-baselines.md` has new § Phase 13.4 section with 8 cells
- `.planning/phases/13.4-engine-prep-wire-spec/13.4-SUMMARY.md` exists (≥100 lines)
- `.planning/phases/13.4-engine-prep-wire-spec/13.4-VERIFICATION.md` exists (≥80 lines)
- STATE.md, ROADMAP.md, CLAUDE.md UNTOUCHED in Phase 13.4 commit range (parent orchestrator owns)
</verification>

<success_criteria>
- Microbench (apply_path_bench) numbers in perf-baselines.md with regression-gate verdict
- Throughput matrix (8 cells) in throughput-baselines.md with regression-gate verdicts
- 13.4-SUMMARY.md authored
- 13.4-VERIFICATION.md authored
- STATE.md / ROADMAP.md / CLAUDE.md UNTOUCHED
- 3 commits: `docs(13.4-10):` (microbench) → `docs(13.4-10):` (throughput) → `docs(13.4-10):` (closure docs)
</success_criteria>

<output>
After completion, the phase is ready for the parent orchestrator to advance STATE.md and ROADMAP.md (after Phases 13.5, 13.6, 13.7 also finish per the orchestrator constraint). No further action from this phase's executors.
</output>
