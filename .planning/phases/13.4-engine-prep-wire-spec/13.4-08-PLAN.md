---
phase: 13.4-engine-prep-wire-spec
plan: 08
type: execute
wave: 3
depends_on: ["04", "07"]   # 04 is Wave 2 (router), 07 is Wave 1 (Persistence::Memory + Config); max+1 = Wave 3
files_modified:
  - crates/beava-core/src/wire.rs
  - crates/beava-runtime-core/src/wire_request.rs
  - crates/beava-runtime-core/src/router.rs
  - crates/beava-runtime-core/src/http_listener.rs
  - crates/beava-runtime-core/src/tcp_listener.rs
  - crates/beava-server/src/apply_shard.rs
  - crates/beava-server/src/server.rs
  - crates/beava-server/tests/phase13_4_reset_gated_env_var.rs        # NEW
  - crates/beava-server/tests/phase13_4_reset_gated_programmatic.rs   # NEW
  - crates/beava-server/tests/phase13_4_reset_default_rejected.rs     # NEW
  - docs/error-codes.md   # add `reset_disabled_in_production`
autonomous: true
requirements:
  - V0-EVENTS-ONLY-01
addresses_decisions:
  - "D-03 (USER-LOCKED) — `OP_RESET (0x0040)` + `POST /reset` route. Wire payload is empty `{}`. Effect when in test_mode: deletes ALL entity state + deregisters ALL descriptors; user must re-register before next push; `registry_version` bumps. Effect when NOT in test_mode: returns structured error `reset_disabled_in_production` (HTTP 403 / wire opcode 0xFFFF). Server-side test_mode gate is set THREE ways (any one enables): shell env var `BEAVA_TEST_MODE=1`; programmatic at server startup `Server::new(Config { test_mode: true, .. })` (Phase 13.4-07's Config struct); programmatic from SDK `bv.App(test_mode=True)` (network mode ignores with warning). Reset must be entirely controllable from test code without depending on shell env vars. Production-by-default semantics preserved."
must_haves:
  truths:
    - "TCP `OP_RESET (0x0040)` opcode constant exported from `beava_core::wire`"
    - "`POST /reset` HTTP route exists and accepts an empty `{}` body"
    - "When server boots WITHOUT `BEAVA_TEST_MODE=1` env var AND WITHOUT `Config { test_mode: true }`: POST /reset returns HTTP 403 + `{\"error\":{\"code\":\"reset_disabled_in_production\", ...}}`. TCP OP_RESET returns the same error in a 0xFFFF error frame."
    - "When server boots WITH `BEAVA_TEST_MODE=1` env var: POST /reset returns 200 + `{\"reset\":true}`; subsequent state checks confirm all entity state is gone and the registry is empty (next push without re-register fails with `pipeline_not_registered` or equivalent existing code)."
    - "When server boots WITH `Config { test_mode: true }` (programmatic — Phase 13.4-07's Config struct): same 200 + `{\"reset\":true}` behavior."
    - "Either gate (env var OR programmatic) enables reset — `OR` not `AND`. The Server's effective test_mode flag is computed at boot time as `cfg.test_mode || env::var(\"BEAVA_TEST_MODE\").map(|v| v == \"1\").unwrap_or(false)`."
    - "`docs/error-codes.md` contains `reset_disabled_in_production` entry"
    - "TCP and HTTP behave identically — both transports gate identically and return identical structured error code"
    - "Reset bumps `registry_version` to a new value (or resets to 0 — planner discretion per CONTEXT) so any in-flight client cache observes the change"
  artifacts:
    - path: "crates/beava-core/src/wire.rs"
      provides: "`pub const OP_RESET: u16 = 0x0040;` + opcode_name lookup entry."
      contains: "OP_RESET"
    - path: "crates/beava-runtime-core/src/wire_request.rs"
      provides: "`WireRequest::TcpReset { body: Bytes, body_format: u8 }` (body should be empty `{}`) + `WireRequest::HttpReset { body: Bytes }` variants."
      contains: "TcpReset|HttpReset"
    - path: "crates/beava-server/src/apply_shard.rs"
      provides: "`dispatch_reset_sync` — checks test_mode flag from AppState; if disabled, returns structured `reset_disabled_in_production` response; if enabled, clears entity state + deregisters all descriptors + bumps registry_version."
      contains: "dispatch_reset_sync"
    - path: "crates/beava-server/src/server.rs"
      provides: "`Config::test_mode` interpretation at boot — effective test_mode = `cfg.test_mode || env::BEAVA_TEST_MODE`. AppState carries the effective flag; `dispatch_reset_sync` reads it."
      contains: "BEAVA_TEST_MODE|test_mode"
    - path: "docs/error-codes.md"
      provides: "Append `reset_disabled_in_production` entry per D-03."
      contains: "reset_disabled_in_production"
  key_links:
    - from: "Boot-time test_mode resolution"
      to: "AppState.effective_test_mode bool field"
      via: "ServerV18::bind_with_config reads cfg.test_mode + env::var(\"BEAVA_TEST_MODE\"); ORs them; stores in AppState; dispatch_reset_sync reads AppState.effective_test_mode."
      pattern: "BEAVA_TEST_MODE|effective_test_mode"
    - from: "POST /reset HTTP route"
      to: "WireRequest::HttpReset → dispatch_reset_sync"
      via: "Router → http_listener → apply_shard. Mirrors the OP_BATCH_GET pattern from Plan 03."
      pattern: "Route::Reset|HttpReset|dispatch_reset_sync"
---

<objective>
Implement D-03 (USER-LOCKED) — `OP_RESET (0x0040)` opcode + `POST /reset` route, gated on `test_mode` (OR-of env-var and programmatic-Config) for production safety. Three enabling paths must work: shell env (`BEAVA_TEST_MODE=1`), programmatic Rust (`Config { test_mode: true }` from Plan 07's struct), programmatic SDK (Phase 13.5's `bv.App(test_mode=True)` passes the env var to the spawned embed binary; network mode ignores with warning — that side is Phase 13.5, not this plan).

**Wave 3 placement:** depends on Plan 04 (HTTP route table; we add `Route::Reset`) and Plan 07 (`Config { test_mode }` struct exists). Independent of Plan 06 (force/dry_run touches register_validate; reset touches the dispatch).

**TDD ordering — three sibling test files (one per gate path):**

1. **Task 8.a (RED — env var path):** `phase13_4_reset_gated_env_var.rs` — failing test with `BEAVA_TEST_MODE=1` boot.
2. **Task 8.b (RED — programmatic path):** `phase13_4_reset_gated_programmatic.rs` — failing test with `Config { test_mode: true }` boot.
3. **Task 8.c (RED — default rejected):** `phase13_4_reset_default_rejected.rs` — failing test with default boot, asserts 403 + `reset_disabled_in_production`.
4. **Task 8.d (GREEN — single commit):** add `OP_RESET` constant + `WireRequest::TcpReset/HttpReset` + Route::Reset + `dispatch_reset_sync` + the boot-time test_mode resolution. All three test files turn green.

   The three RED commits before the single GREEN commit form a valid red→green chain per CLAUDE.md TDD §Note 2 (`test:` commits precede `feat:` commits — multiple reds before a single green is acceptable as long as ordering is preserved).

Output:
- 1 new opcode + 2 WireRequest variants + 1 Route variant + 1 dispatch fn
- 3 new test files
- 1 docs append
- 4 commits (3 red + 1 green)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13.4-engine-prep-wire-spec/13.4-CONTEXT.md
@.planning/phases/13.4-engine-prep-wire-spec/SCRATCH-PLANNER-NOTES.md
@.planning/phases/13.4-engine-prep-wire-spec/13.4-04-PLAN.md
@.planning/phases/13.4-engine-prep-wire-spec/13.4-07-PLAN.md
@docs/error-codes.md
@crates/beava-core/src/wire.rs
@crates/beava-runtime-core/src/wire_request.rs
@crates/beava-runtime-core/src/router.rs
@crates/beava-server/src/apply_shard.rs
@crates/beava-server/src/server.rs

<interfaces>
<!-- Wire shape for reset. -->

TCP frame:
```
[u32 len][u16 op=0x0040][u8 ct=CT_JSON][payload="{}"]
```

HTTP:
```
POST /reset HTTP/1.1
Content-Type: application/json

{}
```

Response when test_mode enabled:
```json
{"reset": true, "registry_version": 0}
```

Response when test_mode disabled:
```json
HTTP/1.1 403 Forbidden
{"error":{"code":"reset_disabled_in_production","reason":"OP_RESET requires server test_mode (set BEAVA_TEST_MODE=1 or pass test_mode: true at server construction)"}}
```

From Plan 07's Config struct (existing AT THIS WAVE):
```rust
pub struct Config {
    pub persistence: beava_persistence::Persistence,
    pub test_mode: bool,    // ← this plan reads it at boot, then OR's with env var
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 8.a (RED): Failing test with BEAVA_TEST_MODE=1 env var path</name>
  <read_first>
    - .planning/phases/13.4-engine-prep-wire-spec/13.4-CONTEXT.md (D-03 spec)
    - crates/beava-server/tests/phase13_4_get_row_shape.rs (TestServer harness)
    - crates/beava-server/src/testing.rs (harness — see if env var is settable per-spawn)
  </read_first>
  <files>crates/beava-server/tests/phase13_4_reset_gated_env_var.rs</files>
  <behavior>
    - Test 1 — `reset_with_env_var_enabled_succeeds_and_clears_state`: set `std::env::set_var("BEAVA_TEST_MODE", "1");` BEFORE spawning TestServer; register a pipeline; push 3 events; POST /reset with `{}`; assert HTTP 200 + `{"reset":true,...}`; subsequent GET on the prior entity returns cold-start (state cleared); subsequent push WITHOUT re-register returns an error (descriptors deregistered).
    - Test 2 — `tcp_reset_with_env_var_enabled_succeeds`: same env var; open TCP; send framed `OP_RESET (0x0040)` with `{}` payload; parse response; assert `OP_GET_RESPONSE` (or whatever success-frame opcode is conventional) with `{"reset":true,...}`.
       NOTE: the response frame opcode for a reset success is planner discretion — pick `OP_GET_RESPONSE` (0x0023) since it's the existing generic-JSON-response frame, or define a new `OP_RESET_RESPONSE`. Auto-decision: reuse `OP_GET_RESPONSE` for symmetry — every JSON success body uses the same response frame. The 0xFFFF error frame is used for the disabled-in-production case.
    - Test 3 — `env_var_value_other_than_1_does_not_enable`: set `BEAVA_TEST_MODE=true` (NOT `=1`); spawn server; POST /reset; assert 403 + `reset_disabled_in_production`. The check is exactly `env_var == "1"` per CONTEXT D-03.

    **Caution on env-var test isolation:** `std::env::set_var` is process-global and not thread-safe across parallel tokio tests. Use `serial_test` crate or a `static MUTEX` to serialize the env-var-mutating tests, OR set `BEAVA_TEST_MODE` via `Command::env` if the test spawns a subprocess server (which is more robust). Picking the simpler path: use `serial_test::serial` attribute on each test in this file. If `serial_test` isn't already a dev-dep in beava-server, add it to `Cargo.toml [dev-dependencies]`.
  </behavior>
  <action>
Create `crates/beava-server/tests/phase13_4_reset_gated_env_var.rs`. Add `serial_test` to `crates/beava-server/Cargo.toml [dev-dependencies]` if missing. Use `#[serial_test::serial]` attribute to serialize env-var-mutating tests (env var is process-global).

Each test:
```rust
#[tokio::test]
#[serial_test::serial]
async fn reset_with_env_var_enabled_succeeds_and_clears_state() {
    std::env::set_var("BEAVA_TEST_MODE", "1");
    let _guard = scopeguard::guard((), |_| std::env::remove_var("BEAVA_TEST_MODE"));
    // ... harness body ...
}
```

Run `cargo test -p beava-server --test phase13_4_reset_gated_env_var` — CONFIRM RED (no `OP_RESET` opcode yet).

Commit:
```
test(13.4-08): add failing reset-via-env-var integration test
```
  </action>
  <verify>
    <automated>cargo test -p beava-server --test phase13_4_reset_gated_env_var 2>&1 | tail -10 | grep -E 'FAILED|error\[' | head -1</automated>
    EXPECTED: compile error or test failure — RED.
  </verify>
  <acceptance_criteria>
    - File exists ≥ 80 lines: `[ -f crates/beava-server/tests/phase13_4_reset_gated_env_var.rs ] && [ $(wc -l < crates/beava-server/tests/phase13_4_reset_gated_env_var.rs) -ge 80 ]`
    - Three test fns: `grep -c '#\[tokio::test\]\|#\[test\]' crates/beava-server/tests/phase13_4_reset_gated_env_var.rs` returns ≥ `3`
    - `serial` attribute on each test: `grep -c '#\[serial' crates/beava-server/tests/phase13_4_reset_gated_env_var.rs` returns ≥ `3`
    - Cargo test fails: `cargo test -p beava-server --test phase13_4_reset_gated_env_var; [ $? -ne 0 ]`
    - Commit: `git log -1 --format=%s | grep -E '^test\(13\.4-08\):'`
  </acceptance_criteria>
  <done>RED for env-var path. Commit lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8.b (RED): Failing test with programmatic Config { test_mode: true } path</name>
  <read_first>
    - .planning/phases/13.4-engine-prep-wire-spec/13.4-07-PLAN.md (Plan 07's Config struct + bind_with_config)
    - crates/beava-server/src/server.rs (existing bind_with_config from Plan 07; this plan reads test_mode from Config)
  </read_first>
  <files>crates/beava-server/tests/phase13_4_reset_gated_programmatic.rs</files>
  <behavior>
    - Test 1 — `reset_with_config_test_mode_true_succeeds_and_clears_state`: spawn ServerV18 directly via `bind_with_config(http, tcp, admin, Config { test_mode: true, persistence: Persistence::Memory })` — note Memory persistence so reset doesn't have to deal with WAL/snapshot cleanup; register; push; reset; assert state cleared.
    - Test 2 — `reset_with_config_test_mode_false_and_no_env_var_returns_403`: spawn with `Config { test_mode: false, ... }` and `BEAVA_TEST_MODE` UNSET (use `std::env::remove_var` + `serial_test`); POST /reset; assert 403 + `reset_disabled_in_production`.
    - Test 3 — `reset_with_config_test_mode_true_overrides_env_var_unset`: spawn with `Config { test_mode: true }`, env var unset; reset succeeds. Proves the OR semantic — programmatic alone is enough.
  </behavior>
  <action>
Create `crates/beava-server/tests/phase13_4_reset_gated_programmatic.rs`. The test spawns ServerV18 with `bind_with_config` and a custom Config; it does NOT use TestServer (since TestServer's defaults bake in `Config::default()`). Use the lower-level `ServerV18::bind_with_config(...).await` directly.

For Tests 2 + 3, use `#[serial_test::serial]` to ensure no other parallel test sets the env var.

Run `cargo test -p beava-server --test phase13_4_reset_gated_programmatic` — CONFIRM RED.

Commit:
```
test(13.4-08): add failing reset-via-programmatic-config integration test
```
  </action>
  <verify>
    <automated>cargo test -p beava-server --test phase13_4_reset_gated_programmatic 2>&1 | tail -10 | grep -E 'FAILED|error\[' | head -1</automated>
    EXPECTED: compile or test failure — RED.
  </verify>
  <acceptance_criteria>
    - File exists ≥ 80 lines: `[ -f crates/beava-server/tests/phase13_4_reset_gated_programmatic.rs ] && [ $(wc -l < crates/beava-server/tests/phase13_4_reset_gated_programmatic.rs) -ge 80 ]`
    - Three test fns: `grep -c '#\[tokio::test\]\|#\[test\]' crates/beava-server/tests/phase13_4_reset_gated_programmatic.rs` returns ≥ `3`
    - References `bind_with_config`: `grep -q 'bind_with_config' crates/beava-server/tests/phase13_4_reset_gated_programmatic.rs`
    - Cargo test fails: `cargo test -p beava-server --test phase13_4_reset_gated_programmatic; [ $? -ne 0 ]`
    - Commit: `git log -1 --format=%s | grep -E '^test\(13\.4-08\):'`
  </acceptance_criteria>
  <done>RED for programmatic path. Commit lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8.c (RED): Failing test with default-config (no gate) path — assert 403</name>
  <read_first>
    - crates/beava-server/tests/phase13_4_reset_gated_env_var.rs (Task 8.a — Test 3 covers a similar default-rejected case; this file is the dedicated default-rejected test isolating the negative path with no env-var setup at all)
  </read_first>
  <files>crates/beava-server/tests/phase13_4_reset_default_rejected.rs</files>
  <behavior>
    - Test 1 — `default_config_no_env_var_post_reset_returns_403_structured`: ensure `BEAVA_TEST_MODE` is UNSET (defensive: `std::env::remove_var(...)` + `#[serial_test::serial]`); spawn TestServer with `Config::default()` (no test_mode, no env); POST /reset; assert HTTP 403; assert body is exactly `{"error":{"code":"reset_disabled_in_production","reason":"<some reason text>"}}`. The `reason` text is fixed in CONTEXT/Plan; verify it contains the substrings `"BEAVA_TEST_MODE"` and `"test_mode"` so users see actionable error text.
    - Test 2 — `tcp_default_config_op_reset_returns_0xFFFF_error_frame`: open TCP; send `OP_RESET` (0x0040) frame; parse response; assert it's a 0xFFFF error frame whose payload is `{"error":{"code":"reset_disabled_in_production",...}}`.
    - Test 3 — `default_config_get_reset_returns_405_method_not_allowed`: GET /reset returns 405 (router-level method check; reset is POST-only).
  </behavior>
  <action>
Create `crates/beava-server/tests/phase13_4_reset_default_rejected.rs`. Same pattern.

Run `cargo test -p beava-server --test phase13_4_reset_default_rejected` — CONFIRM RED.

Commit:
```
test(13.4-08): add failing reset-default-rejected integration test (production safety)
```
  </action>
  <verify>
    <automated>cargo test -p beava-server --test phase13_4_reset_default_rejected 2>&1 | tail -10 | grep -E 'FAILED|error\[' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - File exists ≥ 60 lines: `[ -f crates/beava-server/tests/phase13_4_reset_default_rejected.rs ] && [ $(wc -l < crates/beava-server/tests/phase13_4_reset_default_rejected.rs) -ge 60 ]`
    - Three test fns: `grep -c '#\[tokio::test\]\|#\[test\]' crates/beava-server/tests/phase13_4_reset_default_rejected.rs` returns ≥ `3`
    - References `reset_disabled_in_production`: `grep -q 'reset_disabled_in_production' crates/beava-server/tests/phase13_4_reset_default_rejected.rs`
    - Cargo test fails: `cargo test -p beava-server --test phase13_4_reset_default_rejected; [ $? -ne 0 ]`
    - Commit: `git log -1 --format=%s | grep -E '^test\(13\.4-08\):'`
  </acceptance_criteria>
  <done>RED for default-rejected path. Commit lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8.d (GREEN): Implement OP_RESET + Route::Reset + dispatch_reset_sync + boot-time test_mode resolution</name>
  <read_first>
    - All three RED test files from Tasks 8.a/b/c
    - crates/beava-core/src/wire.rs (existing op constants for OP_RESET addition)
    - crates/beava-runtime-core/src/wire_request.rs (TcpReset / HttpReset variant pattern — model on TcpGet)
    - crates/beava-runtime-core/src/router.rs (Route::Reset addition pattern — model on Plan 04's Route::PushVerb)
    - crates/beava-runtime-core/src/tcp_listener.rs (frame parser dispatch for op u16)
    - crates/beava-runtime-core/src/http_listener.rs (route → WireRequest dispatch)
    - crates/beava-server/src/apply_shard.rs (dispatch_one — add reset arm)
    - crates/beava-server/src/server.rs (bind_with_config — add env-var resolution + AppState carrying effective_test_mode)
  </read_first>
  <files>
    - crates/beava-core/src/wire.rs
    - crates/beava-runtime-core/src/wire_request.rs
    - crates/beava-runtime-core/src/router.rs
    - crates/beava-runtime-core/src/http_listener.rs
    - crates/beava-runtime-core/src/tcp_listener.rs
    - crates/beava-server/src/apply_shard.rs
    - crates/beava-server/src/server.rs
    - docs/error-codes.md
  </files>
  <action>
**Step 1 — `crates/beava-core/src/wire.rs`:** add the constant + opcode-name lookup entry:

```rust
/// OP_RESET — full state + registry clear (Plan 13.4-08 / D-03).
/// Gated on server test_mode (BEAVA_TEST_MODE=1 env var OR
/// Config { test_mode: true } at server construction). Returns
/// reset_disabled_in_production (HTTP 403 / wire 0xFFFF) when
/// gate is closed.
pub const OP_RESET: u16 = 0x0040;
```

Add to `opcode_name`: `OP_RESET => Some("reset"),`.

**Step 2 — `crates/beava-runtime-core/src/wire_request.rs`:** add variants:

```rust
TcpReset { body: Bytes, body_format: u8 },   // body should be {} per D-03
HttpReset { body: Bytes },
```

**Step 3 — `crates/beava-runtime-core/src/tcp_listener.rs`:** add parser arm for op = 0x0040 → `WireRequest::TcpReset`.

**Step 4 — `crates/beava-runtime-core/src/router.rs`:** add `Route::Reset` variant; add the `path == "/reset"` arm:

```rust
if path == "/reset" {
    return if method == "POST" {
        Route::Reset
    } else {
        Route::MethodNotAllowed
    };
}
```

**Step 5 — `crates/beava-runtime-core/src/http_listener.rs`:** when router returns `Route::Reset`, build `WireRequest::HttpReset { body }`.

**Step 6 — `crates/beava-server/src/server.rs`:** at boot in `bind_with_config`, compute effective_test_mode:

```rust
let effective_test_mode = cfg.test_mode
    || std::env::var("BEAVA_TEST_MODE").map(|v| v == "1").unwrap_or(false);
```

Store this on `AppState` (add field `pub effective_test_mode: bool`).

**Step 7 — `crates/beava-server/src/apply_shard.rs`:** add `dispatch_reset_sync`:

```rust
/// Plan 13.4-08 / D-03 — OP_RESET dispatch.
pub fn dispatch_reset_sync(state: &mut AppState) -> GlueResponse {
    if !state.effective_test_mode {
        return GlueResponse::Json(
            serde_json::to_vec(&serde_json::json!({
                "error": {
                    "code": "reset_disabled_in_production",
                    "reason": "OP_RESET requires server test_mode (set BEAVA_TEST_MODE=1 or pass Config { test_mode: true } at server construction). See docs/error-codes.md."
                }
            })).unwrap().into(),
            403,
        );
    }
    // 1. Clear all entity state across all tables.
    state.entities.clear();   // adapt to actual hashmap names
    // 2. Deregister all descriptors — empty the registry.
    state.registry.clear();   // bumps registry_version internally
    // 3. Build success response.
    GlueResponse::Json(
        serde_json::to_vec(&serde_json::json!({
            "reset": true,
            "registry_version": state.registry.version()
        })).unwrap().into(),
        200,
    )
}
```

In `dispatch_one`, add arms:

```rust
WireRequest::TcpReset { .. } | WireRequest::HttpReset { .. } => {
    dispatch_reset_sync(state)
}
```

For the TCP error path (test_mode disabled): the TCP encoder should turn the 403/JSON response into a 0xFFFF error frame whose payload is the same JSON body. If the existing TCP encoder's mapping doesn't already do `status >= 400` → 0xFFFF, add a special-case for the reset error path. Or, simpler: introduce `GlueResponse::TcpError { code: u16, body: Bytes }` and have the TCP encoder emit a 0xFFFF frame with the body verbatim.

**Step 8 — `docs/error-codes.md`:** append:

```markdown
## `reset_disabled_in_production`

**HTTP status:** 403 Forbidden
**Wire opcode (TCP):** 0xFFFF (error frame; payload is JSON body below)
**When:** A POST /reset (HTTP) or OP_RESET (TCP, opcode 0x0040) request is received when the server is NOT in test_mode. Test mode is enabled by either the `BEAVA_TEST_MODE=1` shell env var OR `Config { test_mode: true }` at server construction.
**Resolution:** Test fixtures should set the env var or pass `test_mode: true` to the constructor (Rust `Config { test_mode: true }`; Python SDK `bv.App(test_mode=True)` for embed mode). Production servers reject all reset requests by default — this is intentional production-safety per D-03.
**Source:** Phase 13.4 Plan 08 / D-03.

Response body shape:
```json
{
  "error": {
    "code": "reset_disabled_in_production",
    "reason": "OP_RESET requires server test_mode (set BEAVA_TEST_MODE=1 or pass Config { test_mode: true } at server construction). See docs/error-codes.md."
  }
}
```
```

**Step 9 — re-run all three RED tests:**

```bash
cargo test -p beava-server --test phase13_4_reset_gated_env_var
cargo test -p beava-server --test phase13_4_reset_gated_programmatic
cargo test -p beava-server --test phase13_4_reset_default_rejected
```
Expected: all pass.

**Step 10 — workspace check.**

Commit:
```
feat(13.4-08): implement OP_RESET (0x0040) + POST /reset + test_mode gate (D-03)

- OP_RESET wire constant 0x0040; WireRequest::TcpReset + HttpReset
- Route::Reset (POST /reset only); GET returns 405
- dispatch_reset_sync clears entity state + registry; bumps version
- Boot-time effective_test_mode = cfg.test_mode || env::BEAVA_TEST_MODE=="1"
- Default boot (no env, no config flag) returns 403
  reset_disabled_in_production for both HTTP and TCP

Per D-03 USER-LOCKED: test fixtures can enable reset entirely in
test code without depending on shell env vars (Config { test_mode:
true }) or via env var (BEAVA_TEST_MODE=1) — OR semantic.
Production-by-default safety preserved: default Config has
test_mode=false; env var must be set explicitly.

docs/error-codes.md updated with reset_disabled_in_production entry
per A-04 in SCRATCH-PLANNER-NOTES.md.
```
  </action>
  <verify>
    <automated>cargo test -p beava-server --test phase13_4_reset_gated_env_var 2>&1 | grep -q 'test result: ok' && cargo test -p beava-server --test phase13_4_reset_gated_programmatic 2>&1 | grep -q 'test result: ok' && cargo test -p beava-server --test phase13_4_reset_default_rejected 2>&1 | grep -q 'test result: ok' && cargo test --workspace 2>&1 | tail -10 | grep -E 'test result: ok' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - OP_RESET constant: `grep -q 'pub const OP_RESET: u16 = 0x0040' crates/beava-core/src/wire.rs`
    - WireRequest::TcpReset + HttpReset: `grep -E 'TcpReset|HttpReset' crates/beava-runtime-core/src/wire_request.rs | wc -l` returns ≥ 2
    - Route::Reset: `grep -q 'Route::Reset' crates/beava-runtime-core/src/router.rs`
    - dispatch_reset_sync: `grep -q 'pub fn dispatch_reset_sync' crates/beava-server/src/apply_shard.rs`
    - effective_test_mode: `grep -q 'effective_test_mode' crates/beava-server/src/server.rs`
    - env var read: `grep -q 'BEAVA_TEST_MODE' crates/beava-server/src/server.rs`
    - 3/3 reset test files pass: `cargo test -p beava-server --test phase13_4_reset_gated_env_var 2>&1 | grep -q 'test result: ok' && cargo test -p beava-server --test phase13_4_reset_gated_programmatic 2>&1 | grep -q 'test result: ok' && cargo test -p beava-server --test phase13_4_reset_default_rejected 2>&1 | grep -q 'test result: ok'`
    - docs/error-codes.md updated: `grep -q 'reset_disabled_in_production' docs/error-codes.md`
    - Workspace clean: `cargo test --workspace 2>&1 | grep -E 'test result: ok' | tail -1` succeeds
    - Clippy clean: `cargo clippy --workspace --all-targets --all-features -- -D warnings` returns 0
    - fmt clean: `cargo fmt --all --check` returns 0 diffs
    - Commit: `git log -1 --format=%s | grep -E '^feat\(13\.4-08\):'`
  </acceptance_criteria>
  <done>GREEN for all three reset gate paths. OP_RESET works for both transports under both gate enablement methods; default boot rejects with structured 403. Plan complete. Commit lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /reset | Untrusted client could destroy all state; gate is the load-bearing protection. |
| ops → BEAVA_TEST_MODE env var | Operator-controlled; correct deployment must NOT set this in prod. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.4-08-01 | Tampering | Client triggers production state wipe via /reset | mitigate | The test_mode gate is exactly the defense per D-03. Default config has test_mode=false; env var must be set explicitly. The 403 response is structured + actionable. Production deployment runbooks (out of scope for this plan) must document NOT setting BEAVA_TEST_MODE in prod. |
| T-13.4-08-02 | Elevation of Privilege | Misconfigured client could pass `test_mode=true` to override server | accept | Per D-03: "Network mode (`bv.App(url=..., test_mode=True)`) ignores the kwarg with a warning (the server controls; client can't override)." Server is the gate; client requests are rejected if server's effective_test_mode is false, regardless of any client-side flag. This plan implements server-side gate; SDK-side ignore-with-warning is Phase 13.5. |
| T-13.4-08-03 | Spoofing | Env var read at boot only; can't be changed at runtime | accept | This is the design — boot-time-only reading prevents runtime escalation. Operator who controls env at boot is already trusted. |
</threat_model>

<verification>
- 4 commits: `git log --format=%s | grep -E '^(test|feat)\(13\.4-08\):' | wc -l` returns ≥ 4
- `cargo test --workspace` returns 0 failures
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` returns 0
- `cargo fmt --all --check` returns 0 diffs
- `docs/error-codes.md` contains `reset_disabled_in_production` entry
</verification>

<success_criteria>
- OP_RESET (0x0040) + WireRequest variants + Route::Reset + dispatch_reset_sync all present
- Boot reads `cfg.test_mode || env::BEAVA_TEST_MODE=="1"` and stores on AppState
- Reset succeeds (200 + state cleared + registry empty) when either gate is open
- Reset returns 403 + `reset_disabled_in_production` when both gates are closed
- TCP and HTTP behave identically (both rejected via same code path)
- All 9 tests across 3 test files pass
- Workspace green
- 4 commits: `test(13.4-08):` × 3 (env-var RED, programmatic RED, default-rejected RED) → `feat(13.4-08):` (single GREEN landing all paths)
</success_criteria>

<output>
After completion, create `.planning/phases/13.4-engine-prep-wire-spec/13.4-08-SUMMARY.md`. Self-Check verifying:
- [x] OP_RESET constant + variants + route + dispatch all land
- [x] effective_test_mode = OR of env var + Config flag
- [x] All 3 gate paths covered by 3 test files (9 tests total)
- [x] docs/error-codes.md updated
- [x] Workspace green; commits show 3 reds → 1 green
</output>
