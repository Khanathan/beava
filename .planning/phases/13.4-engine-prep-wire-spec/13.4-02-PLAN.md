---
phase: 13.4-engine-prep-wire-spec
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/beava-server/src/feature_query.rs
  - crates/beava-server/tests/phase13_4_get_row_shape.rs   # NEW (RED→GREEN integration)
autonomous: true
requirements:
  - V0-EVENTS-ONLY-01           # carry-forward — response-shape change does not widen surface
addresses_decisions:
  - "CONTEXT scope item #2 — GET response payload changes to row-shape (flat dict, no `row` envelope) per Phase 13.0-15 wire-spec."
must_haves:
  truths:
    - "POST /get with body `{\"table\":\"UserSpend\",\"entity_id\":\"alice\"}` returns a flat JSON object `{\"feature_a\": <val>, \"feature_b\": <val>, ...}` — NOT `{\"row\": {...}}` and NOT `{\"value\": <single>}` for the multi-feature case"
    - "GET /get/:feature/:key (single-feature) still returns `{\"value\": <val>}` — that path is unchanged (only the multi-feature batch shape flips)"
    - "Cold-start (no events for the entity yet) returns the same flat-dict shape with all feature keys present mapped to their cold-start defaults (e.g. counts → 0, sums → 0, mean → null)"
    - "An unknown table name returns a structured 404 error `{\"error\":{\"code\":\"unknown_table\", ...}}` — not a 200 with empty body"
    - "Existing TCP `OP_GET` (0x0020) path returns the same flat-dict shape via TCP — both transports speak the same wire JSON"
  artifacts:
    - path: "crates/beava-server/src/feature_query.rs"
      provides: "GET response encoder for HTTP `POST /get` and TCP `OP_GET`. Returns flat dict per Phase 13.0-15 wire-spec — `{\"row\":...}` envelope removed."
      contains: "feature_query"
    - path: "crates/beava-server/tests/phase13_4_get_row_shape.rs"
      provides: "RED→GREEN integration test asserting the flat-dict shape for both HTTP and TCP transports across populated + cold-start + unknown-table cases."
      min_lines: 80
  key_links:
    - from: "POST /get HTTP handler (apply_shard.rs::dispatch_get_*)"
      to: "feature_query.rs::format_get_response"
      via: "dispatch builds `FeatureQueryResult` → encoder turns it into JSON. The encoder change happens here; the dispatch path is unchanged."
      pattern: "format_get_response|GetResponse|FeatureQueryResult"
---

<objective>
Flip the `OP_GET` / `POST /get` response payload from `{"row": {feat_a: v, feat_b: v, ...}}` (envelope) to `{feat_a: v, feat_b: v, ...}` (flat dict) per Phase 13.0-15 wire-spec. Single-feature `GET /get/:feature/:key` retains its `{"value": <val>}` shape — only the multi-feature batched read flips.

**TDD ordering:**

1. **Task 1.a (RED):** add `crates/beava-server/tests/phase13_4_get_row_shape.rs` posting a `/get` request and asserting the flat-dict shape (no `row` key in response). Test fails because current encoder still emits the envelope.
2. **Task 1.b (GREEN):** edit `feature_query.rs::format_get_response` to drop the `row` envelope and emit the dict directly.

**Wave 1 placement:** Independent of Plans 01 (op renames touch different files), 03 (OP_BATCH_GET is a NEW opcode — no shape conflict), 04 (verb routes also touch http_listener.rs but the encoder path is the same), 05 (architectural test). The GET-shape change is contained to `feature_query.rs`.

Output:
- Updated `format_get_response` returning flat dict
- 1 new integration test (HTTP + TCP coverage)
- 2 commits (red, green)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13.4-engine-prep-wire-spec/13.4-CONTEXT.md
@.planning/phases/13.4-engine-prep-wire-spec/SCRATCH-PLANNER-NOTES.md
@docs/wire-spec.md
@examples/wire/get-response.json
@crates/beava-server/src/feature_query.rs
@crates/beava-server/src/apply_shard.rs

<interfaces>
<!-- Key shape contracts. The wire-spec docs are the source of truth. -->

Existing GET response shape (pre-13.4 — to be changed):
```json
{"row": {"tx_count_1h": 12, "tx_sum_1h": 145.50, "is_fraud_score": 0.04}}
```

Target GET response shape (Phase 13.0-15 wire-spec — to be implemented here):
```json
{"tx_count_1h": 12, "tx_sum_1h": 145.50, "is_fraud_score": 0.04}
```

Cold-start (no events yet for entity_id="alice"):
```json
{"tx_count_1h": 0, "tx_sum_1h": 0, "is_fraud_score": null}
```

Single-feature GET stays as-is:
```json
GET /get/tx_count_1h/alice → {"value": 12}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.a (RED): Failing integration test for flat-dict GET response shape</name>
  <read_first>
    - .planning/phases/13.4-engine-prep-wire-spec/13.4-CONTEXT.md (scope item #2)
    - docs/wire-spec.md (Phase 13.0-15 GET response section — search for "GET response" or "row")
    - examples/wire/get-response.json (canonical example fixture from Phase 13.0)
    - crates/beava-server/src/feature_query.rs (existing format_get_response — note where the `row` envelope is constructed)
    - crates/beava-server/tests/phase12_7_unsupported_node_kind.rs (canonical TestServer harness pattern — ~lines 1-80)
    - crates/beava-server/src/testing.rs (TestServer harness — register/post helpers)
  </read_first>
  <files>crates/beava-server/tests/phase13_4_get_row_shape.rs</files>
  <behavior>
    - Test 1 — `http_get_returns_flat_dict_no_row_envelope`: register a simple pipeline with two features (e.g. `cnt = count()` and `total = sum(amount)`); push 2 events for entity_id=`alice`; POST `/get` with body `{"keys":["alice"], "features":["cnt","total"]}` (or whatever the existing batch GET shape is — check `feature_query.rs`); assert response body is `{"alice": {"cnt": 2, "total": <sum>}}` — NO `"row"` key anywhere in the JSON tree.
    - Test 2 — `http_get_cold_start_returns_flat_dict_with_defaults`: same registration, NO events pushed; POST `/get` for entity_id `nobody`; assert response is `{"nobody": {"cnt": 0, "total": 0}}` — flat dict, with cold-start defaults.
    - Test 3 — `http_get_unknown_table_returns_404`: POST `/get` for an unregistered table name; assert HTTP 404 + `{"error":{"code":"unknown_table", ...}}`.
    - Test 4 — `tcp_get_returns_same_flat_dict_shape`: open a TCP connection, send framed `OP_GET` (0x0020) with the same body shape, parse the response — assert the same flat dict shape (no envelope). Both transports speak the same wire JSON.
    - Test 5 — `single_feature_http_get_still_returns_value_envelope`: GET `/get/cnt/alice` returns `{"value": 2}` (single-feature path UNCHANGED — only the multi-feature batch path flips).
  </behavior>
  <action>
Create `crates/beava-server/tests/phase13_4_get_row_shape.rs` modeled on `crates/beava-server/tests/phase12_7_unsupported_node_kind.rs` (canonical TestServer harness). Use the standard `TestServer::spawn().await` helper, register a tiny pipeline (one event source `Tx { user_id: String, amount: f64 }` + one derivation table `UserSpend` with `cnt = count()` + `total = sum(amount)` keyed by `user_id`), push events via `/push/Tx`, then POST `/get` to assert the response shape.

Each test asserts the JSON body shape with `serde_json::Value` parsing — `assert!(body.get("row").is_none(), "row envelope must be absent")` is the load-bearing check. Cold-start test asserts the shape AND that values are present (cnt=0, total=0.0 — not missing keys).

Test 4 (TCP) uses the existing TCP frame builder pattern from `crates/beava-runtime-core/tests/` (search for `OP_GET` usage). The frame format is `[u32 length][u16 op=0x0020][u8 content_type=CT_JSON=0x01][payload]`; the TCP read loop should give back an `OP_GET_RESPONSE` (0x0023) frame whose payload is the same JSON body.

Run `cargo test -p beava-server --test phase13_4_get_row_shape` and CONFIRM RED (the populated and cold-start tests fail because the encoder still emits `{"row": ...}`; the unknown-table and single-feature tests may already pass).

Commit:
```
test(13.4-02): add failing test for flat-dict GET response shape (no row envelope)
```
  </action>
  <verify>
    <automated>cargo test -p beava-server --test phase13_4_get_row_shape 2>&1 | grep -E 'test result: FAILED|FAILED|test result: ok' | head -1</automated>
    EXPECTED: `test result: FAILED` (at least one test fails — RED gate confirmed)
  </verify>
  <acceptance_criteria>
    - File exists: `[ -f crates/beava-server/tests/phase13_4_get_row_shape.rs ]`
    - Five test fns present: `grep -c '#\[tokio::test\]\|#\[test\]' crates/beava-server/tests/phase13_4_get_row_shape.rs` returns ≥ `5`
    - Test asserts no `row` envelope: `grep -q 'is_none\|.get("row")\|"row"' crates/beava-server/tests/phase13_4_get_row_shape.rs`
    - File at minimum 80 lines: `[ $(wc -l < crates/beava-server/tests/phase13_4_get_row_shape.rs) -ge 80 ]`
    - `cargo test -p beava-server --test phase13_4_get_row_shape` exits non-zero (RED gate)
    - Commit: `git log -1 --format=%s | grep -E '^test\(13\.4-02\):'`
  </acceptance_criteria>
  <done>RED gate: integration test exists, fails because encoder still emits `{"row": ...}`. Commit lands with `test(13.4-02):` prefix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.b (GREEN): Drop the `row` envelope from format_get_response</name>
  <read_first>
    - crates/beava-server/src/feature_query.rs (the encoder body — search for `"row"` to find the envelope construction site)
    - crates/beava-server/src/apply_shard.rs (dispatch path — confirm whether the envelope is built in feature_query.rs or in the dispatch layer; either way, fix at the lowest level)
    - examples/wire/get-response.json (target canonical shape from Phase 13.0)
    - docs/wire-spec.md (target shape spec)
  </read_first>
  <files>crates/beava-server/src/feature_query.rs</files>
  <action>
Locate the envelope construction in `crates/beava-server/src/feature_query.rs`. The current shape produces `serde_json::json!({"row": <inner>})`. Change to emit `<inner>` directly. The keyed multi-entity batch case (response keyed by entity_id) stays — flip is at the per-entity level: each entity's value is now the flat feature-dict, not a wrapper.

Concrete edit (find the function that constructs the per-entity row JSON):

```rust
// BEFORE (envelope):
let row_json = serde_json::json!({"row": features_map});

// AFTER (flat dict — Phase 13.0-15):
let row_json = serde_json::Value::Object(features_map);
```

If the response is HTTP-only and the TCP path takes a different code path, find the TCP encoder too — both must produce the same JSON body shape. Search:

```bash
grep -rn '"row"' crates/beava-server/src/ crates/beava-runtime-core/src/
```

Update every site that adds the `"row"` key. Per `examples/wire/get-response.json`, the target canonical shape is the flat per-entity dict.

Re-run the integration test:
```bash
cargo test -p beava-server --test phase13_4_get_row_shape
```
Expected: `test result: ok. 5 passed`.

Workspace regression check:
```bash
cargo test --workspace
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo fmt --all --check
```

If existing tests assert the OLD shape (`grep -rn '"row"' crates/`), update them in lockstep — the OLD shape is no longer the contract. Each updated test must continue to assert behavior (not just delete the assertion); replace `body["row"]["feat"]` with `body["feat"]`.

Commit:
```
feat(13.4-02): drop row envelope from GET response — flat dict per Phase 13.0-15

Server-side conformance with the v0 wire-spec for `OP_GET` / POST /get:
the multi-feature batch response is now a flat per-entity dict
`{entity_id: {feature_a: v, ...}}` instead of the wrapped form
`{entity_id: {"row": {feature_a: v, ...}}}`. Single-feature GET
(`GET /get/:feature/:key` → `{"value": v}`) is unchanged.

Examples: see examples/wire/get-response.json (Phase 13.0 fixture).
```
  </action>
  <verify>
    <automated>cargo test -p beava-server --test phase13_4_get_row_shape 2>&1 | grep -q 'test result: ok' && cargo test --workspace 2>&1 | tail -10 | grep -E 'test result: ok' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - No `"row"` envelope key remains in the encoder: `! grep -q 'json!({"row"' crates/beava-server/src/feature_query.rs` and `! grep -q 'json!({"row"' crates/beava-runtime-core/src/`
    - Test from Task 2.a passes GREEN: `cargo test -p beava-server --test phase13_4_get_row_shape 2>&1 | grep -q 'test result: ok'`
    - Workspace clean: `cargo test --workspace 2>&1 | grep -E 'test result: ok' | tail -1` succeeds
    - Clippy clean: `cargo clippy --workspace --all-targets --all-features -- -D warnings` returns 0
    - fmt clean: `cargo fmt --all --check` returns 0 diffs
    - Commit: `git log -1 --format=%s | grep -E '^feat\(13\.4-02\):'`
  </acceptance_criteria>
  <done>GREEN gate: encoder emits flat dict; new test passes; workspace green. Commit lands with `feat(13.4-02):` prefix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /get | JSON read response crosses here; encoder shape is the only change. |

## STRIDE Threat Register

Plan 02 is a response-shape edit. Surface does not widen.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.4-02-01 | Information Disclosure | Flat-dict response | accept | Same data exposed as before — only the wrapping changes. No new fields surfaced; no new entity IDs leaked. |
| T-13.4-02-02 | Tampering | JSON encode path | accept | `serde_json::Value::Object` constructor is the standard path; no custom serialization. Existing test coverage for special characters / Unicode in entity IDs / feature names continues to apply. |
</threat_model>

<verification>
- Both commits exist: `git log --format=%s | grep -E '^(test|feat)\(13\.4-02\):' | head -2`
- `cargo test --workspace` returns 0 failures
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` returns 0 warnings
- `cargo fmt --all --check` returns 0 diffs
- `examples/wire/get-response.json` shape matches what the encoder now emits (manual cross-check)
</verification>

<success_criteria>
- Multi-feature `POST /get` returns `{entity_id: {feat_a: v, ...}}` (flat dict per entity, no envelope)
- TCP `OP_GET` (0x0020) returns the same JSON body shape
- Single-feature `GET /get/:feature/:key` still returns `{"value": v}` (unchanged)
- Cold-start returns flat dict with cold-start defaults present (no missing keys)
- Unknown table returns structured 404
- Workspace green (any in-tree tests asserting the OLD envelope shape were updated in lockstep)
- 2 commits: `test(13.4-02):` → `feat(13.4-02):`
</success_criteria>

<output>
After completion, create `.planning/phases/13.4-engine-prep-wire-spec/13.4-02-SUMMARY.md`. Self-Check verifying:
- [x] `format_get_response` no longer wraps in `{"row": ...}`
- [x] Both HTTP and TCP transports return identical flat-dict shape
- [x] Single-feature GET path unchanged (`{"value": ...}`)
- [x] Cold-start + unknown-table cases covered by tests
- [x] Workspace green; commits show test → feat
</output>
