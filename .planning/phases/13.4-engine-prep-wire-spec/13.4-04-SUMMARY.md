---
phase: 13.4-engine-prep-wire-spec
plan: 04
status: complete
landed: 2026-05-04
note: executor stalled on watchdog after GREEN landed; SUMMARY written inline by orchestrator
---

# Plan 13.4-04 — Verb-style HTTP routes

## Outcome

CONTEXT scope item #4 honored. Verb-style POST routes added per Phase 13.0 http-api spec. Per A-07 (SCRATCH-PLANNER-NOTES.md), verb routes were ADDED — legacy `POST /push/:event_name` etc. routes stay alive so ~20 in-tree tests keep passing.

## Verb routes added (this plan)

| Method + path | Body fields | Notes |
|---|---|---|
| `POST /ping` | `{}` | New route, returns 200 OK |
| `POST /push` | `{event_name, fields}` | Event name moves to body |
| `POST /push-sync` | `{event_name, fields}` | Verb variant of push-sync |

Pre-existing from sibling plans:

| Method + path | Source plan |
|---|---|
| `POST /get` | Plan 02 (already verb-style) |
| `POST /batch_get` | Plan 03 (new opcode + route) |
| `POST /register` | Pre-existing |
| `POST /reset` | Plan 08 (Wave 3, not yet landed) |

## Commits (4)

- `9b77439` test(13.4-04): add failing router test for verb-style routes (POST /ping, POST /push)
- `6a75057` feat(13.4-04): add verb-style route variants — Route::Ping / PushVerb / PushSyncVerb
- `599a73c` test(13.4-04): add failing e2e test for POST /ping and POST /push verb-style
- `6572dee` feat(13.4-04): wire verb-style POST /ping and POST /push through dispatch

## Test count

| File | Tests | Status |
|---|---|---|
| `crates/beava-runtime-core/tests/phase13_4_verb_routes.rs` | 7 | 7/7 GREEN |
| `crates/beava-server/tests/phase13_4_verb_routes_e2e.rs` | 5 | 5/5 GREEN (`--features testing` required) |

## Architectural invariants

- mio-only dataplane: GREEN
- events-only no table surface: GREEN (legacy and verb routes both go through events-only register paths)

## Files modified

- `crates/beava-runtime-core/src/router.rs` — `Route::Ping / PushVerb / PushSyncVerb` variants
- `crates/beava-runtime-core/src/http_listener.rs` — verb-route arms
- `crates/beava-runtime-core/src/wire_request.rs` — `WireRequest::HttpPing / HttpPushVerb / HttpPushSyncVerb` variants
- `crates/beava-runtime-core/tests/phase13_4_verb_routes.rs` — NEW
- `crates/beava-server/tests/phase13_4_verb_routes_e2e.rs` — NEW

## Cross-plan handoff

Plan 13.5 SDK uses verb-style routes for new clients; Plan 13.6 TS+Go SDKs same. Legacy routes can be removed in v0.0.x follow-up.

Plan 13.4-08 adds `POST /reset` verb route (last hole in the verb-style table).
