---
phase: 26-test-migration-bench-docs-demo
plan: 04
type: execute
wave: 3
depends_on: ["26-01", "26-02", "26-03"]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/milestones/v0-ROADMAP.md
  - .planning/phases/26-test-migration-bench-docs-demo/26-SIGNOFF.md
  - .planning/phases/26-test-migration-bench-docs-demo/26-04-SUMMARY.md
autonomous: true
requirements: [P26-signoff]

must_haves:
  truths:
    - "All 11 sign-off criteria from 26-CONTEXT.md are checked with evidence"
    - "STATE.md shows v0 Restructure = Complete and v2.1 Launch = Active (resumable)"
    - "ROADMAP.md v0 milestone is marked Complete with every phase checkbox filled"
    - "v0 milestone snippet archived to .planning/milestones/v0-ROADMAP.md"
    - "v2.1 Launch resume instructions captured: recompile binary, push, run deploy/provision.sh + deploy/smoke.sh"
  artifacts:
    - path: ".planning/milestones/v0-ROADMAP.md"
      provides: "Archived v0 milestone roadmap (per GSD milestone-archive convention)"
      contains: "v0 -- Restructure"
    - path: ".planning/phases/26-test-migration-bench-docs-demo/26-SIGNOFF.md"
      provides: "11-item sign-off checklist with evidence per item"
      contains: "cargo test"
    - path: ".planning/phases/26-test-migration-bench-docs-demo/26-04-SUMMARY.md"
      provides: "Phase 26 closeout + milestone-close log"
      contains: "v2.1 Launch"
  key_links:
    - from: ".planning/STATE.md"
      to: "v0 Restructure = Complete"
      via: "status line"
      pattern: "v0.*Complete"
    - from: ".planning/ROADMAP.md"
      to: "Phase 26 checkbox = [x]"
      via: "milestone phase list"
      pattern: "\\[x\\].*Phase 26"
---

<objective>
# PLAN 26-04: Sign-off + milestone close

## Goal
Close out the v0 Restructure milestone: verify all 11 sign-off criteria, update `.planning/STATE.md` and `.planning/ROADMAP.md`, archive the v0 roadmap snippet to `.planning/milestones/v0-ROADMAP.md` per GSD convention, and capture resume instructions for v2.1 Launch (deploy artifacts already exist — the launch just needs a recompiled binary + `provision.sh` + `smoke.sh` run).

Purpose: final sign-off. After this plan, the v0 milestone is done and v2.1 Launch can resume.

Output:
  - `26-SIGNOFF.md` (11-criteria checklist with evidence per item)
  - Updated `.planning/STATE.md`
  - Updated `.planning/ROADMAP.md`
  - `.planning/milestones/v0-ROADMAP.md` (archived snippet)
  - `26-04-SUMMARY.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-test-migration-bench-docs-demo/26-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v2.1-PAUSED-ROADMAP.md
</context>

## Success Criteria

1. `26-SIGNOFF.md` exists with all 11 criteria from 26-CONTEXT.md `<sign-off>` block, each checked `[x]` with a specific evidence pointer (command output, file path, or plan SUMMARY ref).
2. `.planning/STATE.md` updated:
   - v0 Restructure milestone → Complete (with completion date 2026-04-12 or the actual run date)
   - v2.1 Launch → Active (resumable)
   - Current position points to v2.1 Launch resume
3. `.planning/ROADMAP.md`:
   - `- [ ] v0 -- Restructure` → `- [x] v0 -- Restructure (Phases 21-26) -- Complete YYYY-MM-DD -- .planning/milestones/v0-ROADMAP.md`
   - Every phase checkbox in the v0 section is `[x]`
   - Progress table row for Phase 26 reads `| 26. Test migration, bench, docs, demo | v0 | 4/4 | Complete | YYYY-MM-DD |`
   - The detailed v0 section can be collapsed/archived (per GSD convention — the full history lives in `v0-ROADMAP.md`)
4. `.planning/milestones/v0-ROADMAP.md` exists and contains the full v0 milestone snippet (goal, decisions, phases 21-26 with their success criteria + plan lists).
5. `26-04-SUMMARY.md` contains v2.1 Launch resume instructions: `git pull && cargo build --release --bin tally && ssh ...` (or equivalent) + pointers to `deploy/provision.sh` and `deploy/smoke.sh`.

## Files Touched

- `/data/home/tally/.planning/STATE.md`
- `/data/home/tally/.planning/ROADMAP.md`
- `/data/home/tally/.planning/milestones/v0-ROADMAP.md` (new)
- `/data/home/tally/.planning/phases/26-test-migration-bench-docs-demo/26-SIGNOFF.md` (new)
- `/data/home/tally/.planning/phases/26-test-migration-bench-docs-demo/26-04-SUMMARY.md` (new)

## Implementation Steps

<tasks>

<task type="auto">
  <name>Task 1: Produce the 11-criteria sign-off checklist with evidence</name>
  <files>
    .planning/phases/26-test-migration-bench-docs-demo/26-SIGNOFF.md
  </files>
  <action>
    Walk the 11 sign-off criteria from 26-CONTEXT.md. For each, gather hard evidence and write it into `26-SIGNOFF.md` as a checklist.

    Re-run the gates live, don't trust prior SUMMARY text:

    ```
    # 1. cargo test green
    cargo test --workspace -q 2>&1 | tail -20 > /tmp/26-04-cargo.txt
    # 2. pytest python/tests/ green
    pytest python/tests/ -q 2>&1 | tail -10 > /tmp/26-04-pypy.txt
    # 3. pytest tests/integration/ green
    pytest tests/integration/ -q 2>&1 | tail -10 > /tmp/26-04-pyint.txt
    # 4. grep assertion (zero old-API refs)
    rg -n "@tl\.(source|dataset)|EventSet|FeatureSet" --glob '!.planning/**' --glob '!target/**' python/ tests/ benchmark/ docs/ > /tmp/26-04-grep.txt || true
    # 5/6. bench matrix + MATRIX-V0-FINAL.json exists + gate passed
    test -s .planning/phases/26-test-migration-bench-docs-demo/MATRIX-V0-FINAL.json
    # 7. criterion micro within targets
    test -s .planning/phases/26-test-migration-bench-docs-demo/MICRO-V0-FINAL.json
    # 8. blog rewritten
    wc -l docs/blog/streaming-shouldnt-require-a-platform-team.md
    rg -q "Deferred" docs/blog/streaming-shouldnt-require-a-platform-team.md
    # 9. docs/ site updated with SDK reference + migration note — check for the existence of those pages
    ls docs/
    # 10. phase 20 ports: replay + demo + smoke all green — confirm via 26-03 SUMMARY
    test -s .planning/phases/26-test-migration-bench-docs-demo/26-03-SUMMARY.md
    # 11. deploy-ready — artifacts unchanged
    git diff --stat deploy/tally.service deploy/Caddyfile deploy/provision.sh deploy/README.md
    ```

    Write `26-SIGNOFF.md` with shape:

    ```markdown
    # Phase 26 Sign-off

    Date: YYYY-MM-DD
    Box: (uname -a one-liner)

    ## Criteria

    - [x] **1. `cargo test` green** — <N> tests passed, 0 failed (see /tmp/26-04-cargo.txt)
    - [x] **2. `pytest python/tests/` green** — <N> passed
    - [x] **3. `pytest tests/integration/` green** — <N> passed (replay suite included)
    - [x] **4. No old-API references** — grep returns 0 lines
    - [x] **5. 9-cell matrix within −5% of BASELINE** — see MATRIX-V0-FINAL.json (worst cell: <name> at <delta>%)
    - [x] **6. MATRIX-V0-FINAL.json committed** — path exists
    - [x] **7. Criterion micro within targets** — UDDSketch <N>ns ≤ 500, CMS <N>ns ≤ 200, HLL <N>ns ≤ 200
    - [x] **8. Blog honest** — no TODO/TK, deferred list present, competitive framing present
    - [x] **9. docs/ SDK reference + migration note** — (pointer to files) OR note that docs site is still internal (CONTEXT.md says "if exists")
    - [x] **10. Phase 20 ports green** — per 26-03-SUMMARY (replay CLI + demo + smoke)
    - [x] **11. Deploy-ready** — `git diff --stat` on 4 untouched files = empty; binary recompiles cleanly
    ```

    If ANY criterion is red, STOP. Do not proceed to Task 2. Write the red criterion with its failure evidence and escalate (operator decides: revise the phase vs. accept with caveats).
  </action>
  <verify>
    <automated>bash -c 'test -s .planning/phases/26-test-migration-bench-docs-demo/26-SIGNOFF.md && ! rg -q "\[ \]" .planning/phases/26-test-migration-bench-docs-demo/26-SIGNOFF.md'</automated>
  </verify>
  <done>26-SIGNOFF.md exists; all 11 criteria marked [x] with evidence; no unchecked boxes.</done>
</task>

<task type="auto">
  <name>Task 2: Archive v0 roadmap snippet + update STATE.md + update ROADMAP.md</name>
  <files>
    .planning/milestones/v0-ROADMAP.md, .planning/STATE.md, .planning/ROADMAP.md
  </files>
  <action>
    1. **Archive v0 roadmap snippet.** Read the current `.planning/ROADMAP.md` "v0 Restructure (Active)" block (from `### v0 Restructure (Active)` through `### Phase 26` inclusive, plus the dependency graph diagram). Copy it verbatim into a new file `.planning/milestones/v0-ROADMAP.md` with a header:

       ```markdown
       # v0 Restructure Milestone — Archived Roadmap

       **Status:** Complete YYYY-MM-DD
       **Phases:** 21-26
       **Outcome:** Two-type (Stream + Table) API, DataFrame-parity operators, hybrid sketches, 5s watermarks; ships as public v0 launch. See Phase 26 SIGNOFF.

       ---

       <pasted ROADMAP section here, with every phase checkbox [x]>
       ```

       Follow the same pattern as `.planning/milestones/v2.0-ROADMAP.md` (already exists — read it for exact shape).

    2. **Update `.planning/ROADMAP.md`:**
       - Top `## Milestones` list:
         - Change `- [ ] **v0 -- Restructure** (Phases 21-26) -- Active...` to `- [x] **v0 -- Restructure** (Phases 21-26) -- Complete YYYY-MM-DD -- .planning/milestones/v0-ROADMAP.md`
         - Change `- [⏸] **v2.1 -- Launch** ...` to `- [ ] **v2.1 -- Launch** (Phase 20) -- Active (resuming; v0 unblocks deploy)`
       - Every phase checkbox in the v0 section `[x]` (21, 22, 23, 24, 25, 26).
       - Progress table:
         - Phase 23: `3/3 | Complete | YYYY-MM-DD`
         - Phase 24: `5/5 | Complete | YYYY-MM-DD`
         - Phase 25: `3/3 | Complete | YYYY-MM-DD`
         - Phase 26: `4/4 | Complete | YYYY-MM-DD`
       - The detailed `### v0 Restructure (Active)` section can be shortened to a single-line pointer to `.planning/milestones/v0-ROADMAP.md` (match convention from the prior v1.0 / v1.1–v1.3 / v2.0 milestones, which only appear as a single top-level bullet once archived).

    3. **Update `.planning/STATE.md`:**
       - Current position: "v2.1 Launch resuming (deploy-ready; v0 ships)"
       - Recent completions: add "v0 Restructure — Complete YYYY-MM-DD (Phases 21-26)"
       - Active milestone: v2.1 Launch
       - Active phase: Phase 20 — unpause
       - Blockers: none
       - Decisions carried forward: all v0 locked decisions still apply (Stream+Table, DataFrame operators, hybrid sketches, 5s watermark); retraction propagation + Table agg + outer joins deferred to v0.1
  </action>
  <verify>
    <automated>bash -c 'test -s .planning/milestones/v0-ROADMAP.md && rg -q "v0 -- Restructure.*Complete" .planning/ROADMAP.md && rg -q "v0 Restructure.*Complete" .planning/STATE.md && rg -q "Phase 26.*4/4.*Complete" .planning/ROADMAP.md'</automated>
  </verify>
  <done>v0 milestone archived to .planning/milestones/v0-ROADMAP.md; ROADMAP.md top list + progress table reflect Complete; STATE.md shows v2.1 Launch active.</done>
</task>

<task type="auto">
  <name>Task 3: Write 26-04-SUMMARY with v2.1 resume instructions</name>
  <files>
    .planning/phases/26-test-migration-bench-docs-demo/26-04-SUMMARY.md
  </files>
  <action>
    Write `26-04-SUMMARY.md` with:

    1. **Phase 26 outcome** — link to 26-01/02/03 SUMMARYs + 26-SIGNOFF.md.
    2. **Milestone close evidence** — final test counts, MATRIX-V0-FINAL worst cell, criterion bench numbers, blog word count.
    3. **v2.1 Launch resume instructions** — a copy-pasteable checklist:

       ```markdown
       ## Resuming v2.1 Launch

       Deploy artifacts already written during Phase 20 and preserved through v0. Nothing to re-provision.

       1. Fetch + build release binary:
          ```
          git pull
          cargo build --release --bin tally
          ```
       2. Copy binary to Hetzner box (see `deploy/README.md` for target path).
       3. First-time only (if the box was never provisioned): `sudo bash deploy/provision.sh`
       4. Reload systemd unit: `sudo systemctl restart tally`
       5. Run smoke: `bash deploy/smoke.sh` — expect all 6 invariants pass.
       6. Register the traction demo pipeline: `python benchmark/replay/generator.py --register-only --target <host>`
       7. Warm the replay: `python benchmark/replay/replay_30d.py --speed 1000x --target <host>`
       8. Load the demo page: `https://<caddy-domain>/` — confirm features render.
       9. Publish the blog post (`docs/blog/streaming-shouldnt-require-a-platform-team.md`) and link back to the live demo.

       Unchanged from Phase 20 (do not re-edit):
       - `deploy/tally.service`
       - `deploy/Caddyfile`
       - `deploy/provision.sh`
       - `deploy/README.md`
       ```

    4. **Deferred to v0.1** — restate the explicit deferred list from the blog, so nobody loses track:
       - Table-input aggregation (`.group_by().agg()` on Table inputs) + full retraction propagation
       - Outer joins
       - Session windows
       - CEP patterns
       - SCAN / SUBSCRIBE opcodes
       - CI/CD integration for the regression gate
       - Multi-platform test matrix (macOS/Linux/Windows)

    5. **Retrospective hooks** — note any surprising issue found during closeout (perf regression that was fixed, test that had to be reclassified, blog language that shifted) so the next `/gsd-retro` has material.
  </action>
  <verify>
    <automated>bash -c 'test -s .planning/phases/26-test-migration-bench-docs-demo/26-04-SUMMARY.md && rg -q "Resuming v2.1" .planning/phases/26-test-migration-bench-docs-demo/26-04-SUMMARY.md && rg -q "provision.sh" .planning/phases/26-test-migration-bench-docs-demo/26-04-SUMMARY.md'</automated>
  </verify>
  <done>26-04-SUMMARY.md exists with resume instructions + deferred list + retro hooks.</done>
</task>

</tasks>

## Testing

- **Automated**: the three task verify commands chain together into the milestone-close gate. If any fails, the milestone is NOT closed.
- **Manual**: operator re-reads `26-SIGNOFF.md` and `26-04-SUMMARY.md` end-to-end before declaring done.

## Risks & Mitigations

| Risk | Mitigation |
|---|---|
| A sign-off criterion is red at this point | Stop, escalate. Do not mutate STATE.md / ROADMAP.md until every criterion passes — an honest failed sign-off is better than a fake close. |
| Rewriting ROADMAP.md collapses information the operator wants in place | Mirror the exact shape used for v2.0: top list bullet with milestone-file pointer + full detail archived in `.planning/milestones/v0-ROADMAP.md`. Detailed section in ROADMAP.md is replaced by a 1-line pointer, not deleted outright — the archive preserves it. |
| v2.1 resume instructions drift from the actual `deploy/` artifacts | Cross-check step 1-5 against `deploy/README.md` literally before writing. If anything in `deploy/README.md` is stale, fix it there, don't paper over in the resume note. |
| STATE.md schema doesn't match current conventions | Read existing STATE.md first, mirror its shape exactly — don't invent fields. |

## Dependencies

- 26-01, 26-02, 26-03 all Complete.
- `MATRIX-V0-FINAL.json` + `MICRO-V0-FINAL.json` present (from 26-02).
- Blog + demo port done (from 26-03).
- Clean suites (from 26-01).
