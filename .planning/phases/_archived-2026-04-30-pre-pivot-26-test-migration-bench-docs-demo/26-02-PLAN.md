---
phase: 26-test-migration-bench-docs-demo
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - .planning/phases/26-test-migration-bench-docs-demo/MATRIX-V0-FINAL.json
  - .planning/phases/26-test-migration-bench-docs-demo/MICRO-V0-FINAL.json
  - .planning/phases/26-test-migration-bench-docs-demo/26-02-SUMMARY.md
  - docs/blog/streaming-shouldnt-require-a-platform-team.md
  - src/**/*.rs            # only if a regression is found and must be fixed in place
autonomous: true
requirements: [P26-SC3, P26-SC4]

must_haves:
  truths:
    - "bench_v0.py 9-cell matrix runs end-to-end on a clean release rebuild"
    - "Every cell is within −5% of the corresponding BASELINE.json cell (worst-case eps_median / eps_baseline ≥ 0.95)"
    - "1c cells use 7-run medians per 22-04 protocol; 4c/8c cells use ≥3-run medians"
    - "Criterion micro-benches meet Phase 22-03 targets: UDDSketch insert ≤ 500ns, CMS insert ≤ 200ns, HLL insert ≤ 200ns"
    - "If any cell regresses, the root cause is profiled (cargo flamegraph) and fixed in place — not papered over by loosening the gate"
    - "docs/blog/streaming-shouldnt-require-a-platform-team.md rewritten with v0 story (Stream+Table, DataFrame operators, 5s watermark)"
    - "Blog lists explicit v0.1 deferrals (Table aggregation, DAG retraction propagation, outer joins, session windows, CEP, SCAN/SUBSCRIBE)"
    - "Blog uses new-API code examples only — zero grep hits for @tl.source / @tl.dataset / EventSet / FeatureSet"
    - "Blog headline perf numbers sourced from MATRIX-V0-FINAL.json, not placeholder text"
    - "Blog contains literal {{DEMO_URL}} placeholder (to be filled at v2.1 Launch post-deploy)"
  artifacts:
    - path: ".planning/phases/26-test-migration-bench-docs-demo/MATRIX-V0-FINAL.json"
      provides: "Final 9-cell matrix result vs BASELINE.json with per-cell delta"
      contains: "eps_median"
    - path: ".planning/phases/26-test-migration-bench-docs-demo/MICRO-V0-FINAL.json"
      provides: "Final criterion sketch micro-bench numbers vs targets"
      contains: "uddsketch_insert_ns"
    - path: "docs/blog/streaming-shouldnt-require-a-platform-team.md"
      provides: "Rewritten launch blog (honest v0 story)"
      min_lines: 100
    - path: ".planning/phases/26-test-migration-bench-docs-demo/26-02-SUMMARY.md"
      provides: "Gate pass/fail + any regression-fix narrative + blog rewrite log"
      contains: "gate_passed"
  key_links:
    - from: "MATRIX-V0-FINAL.json"
      to: "BASELINE.json"
      via: "per-cell eps_median ratio"
      pattern: "gate_passed.*true"
    - from: "docs/blog/streaming-shouldnt-require-a-platform-team.md"
      to: "MATRIX-V0-FINAL.json"
      via: "headline eps + p99 numbers copied from a specific cell"
      pattern: "events?/sec|eps"
---

<objective>
# PLAN 26-02: Benchmark regression gate + blog rewrite

## Goal
Two outputs, one plan because they are tightly coupled: (a) the definitive pre-launch perf gate — run `bench_v0.py` full 9-cell matrix + criterion sketch micro-benches against `BASELINE.json`, fix any regression in place, capture `MATRIX-V0-FINAL.json` / `MICRO-V0-FINAL.json`; (b) rewrite `docs/blog/streaming-shouldnt-require-a-platform-team.md` into the honest v0 launch story, pulling the headline number directly from the matrix result captured in (a).

Purpose: sign-off criteria 5, 6, 7, 8 from 26-CONTEXT.md.

Output:
  - `MATRIX-V0-FINAL.json` with `gate_passed=true`
  - `MICRO-V0-FINAL.json` with sketch medians under target
  - Rewritten `docs/blog/streaming-shouldnt-require-a-platform-team.md` with `{{DEMO_URL}}` placeholder
  - `26-02-SUMMARY.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-test-migration-bench-docs-demo/26-CONTEXT.md
@.planning/phases/22-stream-aggregation-engine/BASELINE.json
@.planning/phases/25-query-ttl-warnings/MATRIX-V0-POST-25.json
@.planning/research/v0-restructure-spec.md
@.planning/research/flink-kafka-gap-analysis.md
@.planning/research/retraction-literature-survey.md
@benchmark/tally-throughput/bench_v0.py
@docs/blog/streaming-shouldnt-require-a-platform-team.md
@CLAUDE.md
</context>

<interfaces>
<!-- bench_v0.py CLI (from Phase 22-04, carried through 24/25) -->

```
python3 benchmark/tally-throughput/bench_v0.py --matrix --events 30000 --runs 3
python3 benchmark/tally-throughput/bench_v0.py --pipeline small --clients 1 --events 30000
```

If `--cells` / `--output` / `--runs` aren't already present, patch bench_v0.py minimally (argparse additions) rather than fork the module.

<!-- BASELINE.json shape (reference) -->

```json
{
  "label": "baseline-v2.0-pre-v0",
  "cells": {
    "<pipeline>_<clients>c": {
      "pipeline": "small|medium|large",
      "clients": 1|4|8,
      "runs": N,
      "eps_median": float,
      "eps_all": [float, ...],
      "p99_median": float,
      "p50_median": float
    }
  }
}
```

<!-- MATRIX-V0-FINAL.json shape (mirrors MATRIX-V0-POST-25.json, one new field per cell) -->

```json
{
  "label": "v0-final-post-phase-26",
  "timestamp": "<iso8601>",
  "baseline_ref": ".planning/phases/22-stream-aggregation-engine/BASELINE.json",
  "gate_threshold_pct": -5.0,
  "gate_passed": true|false,
  "worst_cell": {"name": "<cell>", "delta_pct": float},
  "box": {"uname": "...", "cpu": "...", "mem_kb": int},
  "cells": {
    "<cell>": {
      "pipeline": "small|medium|large", "clients": 1|4|8, "runs": N,
      "eps_median": float, "eps_all": [float,...],
      "p99_median": float, "p50_median": float,
      "baseline_eps_median": float, "delta_pct": float, "passed": bool
    }
  }
}
```

<!-- Criterion benches in /data/home/tally/benches/ -->

```
cargo bench --bench uddsketch_ops   # insert / estimate / merge
cargo bench --bench cms_ops         # insert / top_k
cargo bench --bench hll_ops         # insert / estimate / merge
```

Targets (Phase 22-03):
- UDDSketch insert ≤ 500 ns
- CMS insert ≤ 200 ns
- HLL insert ≤ 200 ns
</interfaces>

## Success Criteria

1. `bench_v0.py --matrix` runs all 9 cells on a freshly-built `cargo build --release --bin tally`.
2. 1c cells use 7-run medians (22-04 protocol); 4c/8c cells use 3-run medians (BASELINE convention).
3. `MATRIX-V0-FINAL.json` exists with per-cell `delta_pct`, top-level `gate_passed`, `worst_cell`, and `box` fields populated.
4. `gate_passed == true` — every cell within −5%. If any cell regresses, flame-graph + fix in place; **do not** lower the threshold.
5. `MICRO-V0-FINAL.json` exists with UDDSketch/CMS/HLL insert medians; all three meet target; `all_pass == true`.
6. `docs/blog/streaming-shouldnt-require-a-platform-team.md` rewritten:
   - All code fences use `@tl.stream` / `@tl.table` / `tl.col` / `.group_by().agg()` / `tl.count` / `tl.sum` / `tl.avg` / `tl.percentile` / `tl.count_distinct` / `tl.top_k`
   - Explicit "v0 ships / v0.1 adds" section covering: Table-input aggregation, DAG retraction propagation, outer joins, session windows, CEP, SCAN/SUBSCRIBE, multi-platform CI
   - Headline eps + p99 numbers copied from a specific cell of `MATRIX-V0-FINAL.json` with the box spec cited
   - 3-5 concrete code examples using the new API
   - Competitive framing paragraph (Flink / ksqlDB / Materialize / Fennel) sourced from `flink-kafka-gap-analysis.md` + `retraction-literature-survey.md`
   - Literal `{{DEMO_URL}}` placeholder present
7. `rg -n "@tl\.(source|dataset)|EventSet|FeatureSet" docs/blog/streaming-shouldnt-require-a-platform-team.md` returns zero lines.
8. `26-02-SUMMARY.md` captures: box spec, build flags, per-cell delta table, any regression-and-fix narrative, criterion table, blog word count + code-example count.

## Files Touched

- `/data/home/tally/.planning/phases/26-test-migration-bench-docs-demo/MATRIX-V0-FINAL.json` (new)
- `/data/home/tally/.planning/phases/26-test-migration-bench-docs-demo/MICRO-V0-FINAL.json` (new)
- `/data/home/tally/docs/blog/streaming-shouldnt-require-a-platform-team.md` (full rewrite)
- `/data/home/tally/.planning/phases/26-test-migration-bench-docs-demo/26-02-SUMMARY.md` (new)
- `/data/home/tally/src/**/*.rs` — only if a regression must be fixed in place

## Implementation Steps

<tasks>

<task type="auto">
  <name>Task 1: Run 9-cell matrix + criterion benches; capture MATRIX-V0-FINAL.json + MICRO-V0-FINAL.json; fix any regression in place</name>
  <files>
    .planning/phases/26-test-migration-bench-docs-demo/MATRIX-V0-FINAL.json,
    .planning/phases/26-test-migration-bench-docs-demo/MICRO-V0-FINAL.json,
    src/**/*.rs
  </files>
  <action>
    1. **Clean release build:**
       ```
       cargo clean -p tally 2>/dev/null || true
       cargo build --release --bin tally
       ```

    2. **Record box profile** — `uname -a`, `nproc`, `lscpu | head -20`, `grep MemTotal /proc/meminfo`. Save to `/tmp/26-02-box.txt` for the SUMMARY.

    3. **Start the server** for the matrix run:
       ```
       TALLY_SNAPSHOT_DIR=$(mktemp -d) target/release/tally &
       TALLY_PID=$!
       sleep 1
       ```

    4. **Run matrix** in two passes:
       - 1c cells → 7 runs (per 22-04 protocol)
       - 4c/8c cells → 3 runs (matches BASELINE.json)
       ```
       python3 benchmark/tally-throughput/bench_v0.py \
           --matrix --events 30000 --runs 7 \
           --cells small_1c medium_1c large_1c \
           --output /tmp/26-02-matrix-1c.json

       python3 benchmark/tally-throughput/bench_v0.py \
           --matrix --events 30000 --runs 3 \
           --cells small_4c small_8c medium_4c medium_8c large_4c large_8c \
           --output /tmp/26-02-matrix-multic.json
       ```
       If bench_v0.py lacks any of `--cells` / `--output` / `--runs`, extend it (minimal argparse addition).

    5. **Tear down server:**
       ```
       kill $TALLY_PID; wait $TALLY_PID 2>/dev/null
       ```

    6. **Merge + compute deltas** into `MATRIX-V0-FINAL.json`. For each cell:
       ```
       delta_pct = (final.eps_median - baseline.eps_median) / baseline.eps_median * 100
       passed    = final.eps_median / baseline.eps_median >= 0.95
       ```
       Set top-level `gate_passed = all(passed)`; `worst_cell = argmin(delta_pct)`; include `box`, `timestamp`, `gate_threshold_pct: -5.0`, `baseline_ref`.

    7. **Run criterion sketch benches** (in-process, no server):
       ```
       cargo bench --bench uddsketch_ops -- --save-baseline v0-final 2>&1 | tee /tmp/26-02-crit-udd.log
       cargo bench --bench cms_ops       -- --save-baseline v0-final 2>&1 | tee /tmp/26-02-crit-cms.log
       cargo bench --bench hll_ops       -- --save-baseline v0-final 2>&1 | tee /tmp/26-02-crit-hll.log
       ```
       Parse insert-bench medians from `target/criterion/<bench>/<case>/new/estimates.json`. Write `MICRO-V0-FINAL.json`:
       ```json
       {
         "timestamp": "<iso>",
         "targets": {"uddsketch_insert_ns": 500, "cms_insert_ns": 200, "hll_insert_ns": 200},
         "measured": {"uddsketch_insert_ns": ..., "cms_insert_ns": ..., "hll_insert_ns": ...},
         "all_pass": true
       }
       ```

    8. **Regression handling** — if `gate_passed == false` on MATRIX, or any `measured > target` on MICRO:
       - Localize the failing cell/bench
       - Profile: `cargo flamegraph --bin tally --release -- --bench-mode <pipeline>` (fallback: `perf record -g` or `samply`)
       - Common suspects from this milestone:
         - Watermark tracking (Phase 24) — per-stream `max(event_time) - 5s` on the hot path
         - Signal registry polling (Phase 25) — if PUSH accidentally consults it
         - TTL bloom reinit scan (Phase 25) — if rotation is running hot
         - Config recommendation engine (Phase 25) — should be cold
       - Fix in place: hoist a watermark lookup, gate signal emission behind a counter, batch the eviction scan, `#[inline]` hot helpers, etc.
       - Re-run only the affected cell(s), replace in `MATRIX-V0-FINAL.json`, recompute `gate_passed` / `worst_cell`.
       - Iterate up to 3 fix attempts. If still red, STOP, write `26-02-ESCALATION.md`, and do **not** pass the gate.

    9. **Do NOT:**
       - Lower BASELINE values
       - Reduce `events_per_run` or `runs` below the spec
       - Skip a regressed cell
       - Relax the −5% threshold
  </action>
  <verify>
    <automated>bash -c 'set -e; test -s .planning/phases/26-test-migration-bench-docs-demo/MATRIX-V0-FINAL.json && test -s .planning/phases/26-test-migration-bench-docs-demo/MICRO-V0-FINAL.json && python3 -c "import json,sys; m=json.load(open(\".planning/phases/26-test-migration-bench-docs-demo/MATRIX-V0-FINAL.json\")); assert len(m[\"cells\"])==9; assert m[\"gate_passed\"] is True, m; u=json.load(open(\".planning/phases/26-test-migration-bench-docs-demo/MICRO-V0-FINAL.json\")); assert u[\"all_pass\"] is True, u"'</automated>
  </verify>
  <done>MATRIX-V0-FINAL.json has 9 cells with gate_passed=true; MICRO-V0-FINAL.json has 3 sketch numbers under target with all_pass=true; any source-level fix narrated for SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite docs/blog/streaming-shouldnt-require-a-platform-team.md for v0</name>
  <files>
    docs/blog/streaming-shouldnt-require-a-platform-team.md
  </files>
  <action>
    Rewrite the blog end-to-end. Preserve the founder-voice opening (Viggle/Faire/Fennel framing from existing commit `205414e`) and the closing thesis; rewrite the technical middle against the v0 reality.

    **Required sections (in order):**

    1. **Opening** — polish existing "streaming shouldn't require a platform team" framing. Keep the founder backstory.

    2. **The in-memory single-binary thesis** — keep the 10M-entities-at-8KB math, the "Redis with an aggregation engine" framing (CLAUDE.md architecture section).

    3. **What Tally ships in v0** — NEW bulleted section:
       - Two types: `Stream` (append-only log) + `Table` (keyed current-state with upsert + tombstone)
       - `@tl.stream` / `@tl.table` decorators; class form = source, function form = derivation; DAG auto-discovered from function parameter types
       - DataFrame-parity operators: `filter`, `map`, `select`, `drop`, `rename`, `with_columns`, `cast`, `fillna`, `group_by().agg()`, `join`, `union`
       - Aggregation catalog: `count`, `sum`, `avg`, `variance`, `stddev`, `min`, `max`, `first`, `last`, `first_n`, `last_n`, `ema`, `lag` + hybrid sketches: `percentile` (UDDSketch), `count_distinct` (HLL), `top_k` (CMS+heap)
       - Joins: Stream↔Stream windowed (inner/left, `within=`), Stream↔Table enrichment, Table↔Table same-key
       - Event-time with a fixed 5s watermark; late events dropped with `tally_late_events_dropped_total{stream}` counter
       - Query surface: `GET` / `MGET` / `GET_MULTI` with null-collapse
       - TTL defaults: 30d Tables, 90d Stream history, 7d tombstone grace
       - Unified `/debug/warnings` health feed + `tally suggest-config` CLI

    4. **Three to five concrete code examples** — REQUIRED, new API only. Draw from `benchmark/tally-throughput/bench_v0.py` so the code is known-working. Suggested:
       a. Fraud pipeline (`@tl.stream class RawTxns` + `@tl.table def UserFeatures(raw) -> tl.Table: return raw.group_by("user_id").agg(...)`)
       b. Stream↔Table enrichment
       c. Percentile / count_distinct using hybrid sketches, with the honest α-drift callout at the exact→sketch transition (Phase 22-03)
       d. Reading features via `app.get_multi([UserFeatures, MerchantRisk], key)` with null-collapse
       e. (optional) Late-event handling with `event_time()` builtin + 5s watermark mention

    5. **The performance story** — pull from `MATRIX-V0-FINAL.json`:
       - Single-client sustained EPS (use the **worst** 1c cell — honest)
       - p99 push latency from the same cell
       - Box spec (host block from MATRIX-V0-FINAL.json)
       - Full 9-cell table (same shape as in 25-SUMMARY.md)
       - Sketch insert latencies from `MICRO-V0-FINAL.json`
       Phrasing: "The benchmark shows X on Y box." Do not cherry-pick, do not extrapolate.

    6. **What v0 does NOT do (deferred to v0.1)** — REQUIRED explicit list:
       - Table-input `group_by().agg()` — eliminates Case 3 retraction complexity in v0; full propagation in v0.1
       - DAG retraction propagation across multi-hop derivations
       - Outer joins (right / full) — inner + left only in v0
       - Session windows — tumbling + sliding only in v0
       - CEP / match_recognize pattern matching
       - SCAN / SUBSCRIBE opcodes — reserved at 0x10 / 0x11; not implemented in v0
       - Horizontal scale-out / key-partitioned multi-threading — documented path, not shipped
       - CI/CD integration for the regression gate + multi-platform test matrix
       Phrasing: "This is not in v0." Not "coming soon."

    7. **Competitive framing** — one factual paragraph each on Flink, ksqlDB, Materialize, Fennel. Source language from `.planning/research/flink-kafka-gap-analysis.md` and `.planning/research/retraction-literature-survey.md`. Note where Tally differs (single-binary, in-memory, DataFrame surface, no Kafka dep) and where it overlaps. No mudslinging.

    8. **Try it** — `{{DEMO_URL}}` literal placeholder. Install command + minimal "push event, get feature back" snippet.

    9. **Closing** — return to the thesis in 2 sentences.

    **Writing constraints:**
    - Every code fence uses new-API symbols only (grep must pass)
    - Every quantitative claim backed by a file citation
    - No mockery of Kafka/Flink/Redis — factual comparison only
    - Engineer-honest tone (prior polish baseline: commit `205414e`)
    - No deferred feature described as shipped
    - `{{DEMO_URL}}` appears at least once, literally

    **Final grep check** before saving:
    ```
    rg -n "@tl\.(source|dataset)|EventSet|FeatureSet" docs/blog/streaming-shouldnt-require-a-platform-team.md
    ```
    Must be zero.
  </action>
  <verify>
    <automated>bash -c 'set -e; F=docs/blog/streaming-shouldnt-require-a-platform-team.md; test -s "$F"; ! rg -nq "@tl\.(source|dataset)|EventSet|FeatureSet" "$F"; rg -q "\{\{DEMO_URL\}\}" "$F"; rg -q "v0\.1" "$F"; rg -q "watermark" "$F"; rg -q "Stream" "$F"; rg -q "Table" "$F"; rg -q "@tl\.(stream|table)" "$F"; test "$(wc -l < "$F")" -ge 100'</automated>
  </verify>
  <done>Blog rewritten; zero old-API grep hits; {{DEMO_URL}} placeholder present; v0.1 deferred list explicit; watermark/Stream/Table language present; headline numbers sourced from MATRIX-V0-FINAL.json; ≥100 lines.</done>
</task>

<task type="auto">
  <name>Task 3: Write 26-02-SUMMARY.md</name>
  <files>
    .planning/phases/26-test-migration-bench-docs-demo/26-02-SUMMARY.md
  </files>
  <action>
    Write `26-02-SUMMARY.md`:

    ```markdown
    # Phase 26-02 Summary — Bench gate + blog rewrite

    Date: <ISO>
    Gate result: PASSED
    HEAD: <git rev-parse HEAD>

    ## Box

    <paste /tmp/26-02-box.txt>

    ## Build

    - `cargo build --release --bin tally`

    ## 9-cell matrix (vs BASELINE.json)

    | Cell | Baseline eps | Final eps | Δ% | Passed |
    |---|---|---|---|---|
    | small_1c | ... | ... | ... | ok |
    | ... 8 more ... |

    Worst cell: <name> at <Δ>%. Threshold: −5%.

    ## Criterion sketch micro

    | Op | Target (ns) | Measured (ns) | Passed |
    |---|---|---|---|
    | UDDSketch insert | 500 | ... | ok |
    | CMS insert | 200 | ... | ok |
    | HLL insert | 200 | ... | ok |

    ## Regressions fixed (if any)

    <file : function : before Δ : after Δ : 1-line explanation>
    OR
    "No regressions — all cells + sketches within target on first run."

    ## Blog rewrite

    - File: docs/blog/streaming-shouldnt-require-a-platform-team.md
    - Word count: <N>
    - Line count: <N>
    - Code examples: <N> (all new-API)
    - Headline number: <X eps on Y box> (source: MATRIX-V0-FINAL.json cell <name>)
    - Deferred-to-v0.1 items listed: <count ≥ 7>
    - `{{DEMO_URL}}` placeholder: present
    - Competitive framing sources: flink-kafka-gap-analysis.md, retraction-literature-survey.md

    ## Handoff

    - 26-03 (demo port) runs in the same wave. 26-03 does NOT re-do the blog; that work lives here.
    - 26-04 sign-off consumes MATRIX-V0-FINAL.json + MICRO-V0-FINAL.json paths.
    - `{{DEMO_URL}}` placeholder is resolved at v2.1 Launch post-deploy — not here, not 26-03, not 26-04.

    ## Artifacts

    - .planning/phases/26-test-migration-bench-docs-demo/MATRIX-V0-FINAL.json
    - .planning/phases/26-test-migration-bench-docs-demo/MICRO-V0-FINAL.json
    - docs/blog/streaming-shouldnt-require-a-platform-team.md
    - /tmp/26-02-crit-{udd,cms,hll}.log (transient)
    ```
  </action>
  <verify>
    <automated>bash -c 'test -s .planning/phases/26-test-migration-bench-docs-demo/26-02-SUMMARY.md && rg -q "Gate result: PASSED" .planning/phases/26-test-migration-bench-docs-demo/26-02-SUMMARY.md && rg -q "DEMO_URL" .planning/phases/26-test-migration-bench-docs-demo/26-02-SUMMARY.md'</automated>
  </verify>
  <done>SUMMARY captures gate pass, box, per-cell deltas, criterion numbers, blog rewrite log, and handoff.</done>
</task>

</tasks>

## Testing

- **Bench matrix** — `bench_v0.py --matrix` is the perf gate; `gate_passed` boolean is the pass/fail signal.
- **Criterion** — `cargo bench` on three sketches; `all_pass` boolean is the micro pass/fail.
- **Blog** — grep assertion (zero old-API refs) + required-token asserts (`{{DEMO_URL}}`, `v0.1`, `watermark`, `Stream`, `Table`, `@tl.(stream|table)`). Line count ≥ 100.
- **Manual** — operator reads the blog top-to-bottom before 26-04. Factual correctness is automated; voice is human.

## Risks & Mitigations

| Risk | Mitigation |
|---|---|
| Noisy box: per-cell variance >5% obscures the gate | 7-run medians on 1c; pin with `taskset -c 0`; disable turbo if possible. Document in SUMMARY. |
| `cargo flamegraph` not installed | Fall back to `perf record -g` or `samply`; or bisect via `git log` against BASELINE commit. |
| `bench_v0.py` has drifted since Phase 25 and no longer runs clean | Read it first, fix harness issues before interpreting perf numbers. Phase 25 already extended it once; adding `--cells`/`--output`/`--runs` is a small argparse change. |
| Sketch bench names differ from expected | Read `benches/` directory to get actual `--bench <name>` entries. |
| Regression is small-but-real and a fix costs >3 iterations | Stop, write `26-02-ESCALATION.md`, surface to operator. Don't ship a red gate. |
| Blog rewrite loses founder voice | Preserve opening + closing verbatim where possible; rewrite only the technical middle. Cross-reference commit `205414e`. |
| `{{DEMO_URL}}` accidentally resolved | Use the literal string `{{DEMO_URL}}` (with double braces). verify asserts it's present. |
| Blog headline drifts from matrix | Only pull numbers from `MATRIX-V0-FINAL.json`. Cite the cell name inline. |

## Dependencies

- **26-01 complete** — clean suites; no stale old-API paths biasing the bench.
- **BASELINE.json present** at `.planning/phases/22-stream-aggregation-engine/BASELINE.json`.
- **`benches/` present** — `uddsketch_ops` / `cms_ops` / `hll_ops` from Phase 22-03.

## Notes

- Reference Phase 25's `MATRIX-V0-POST-25.json::gate_passed=true`, worst `large_1c -3.83%`. Expect 26-02 numbers in the same neighborhood; anything materially worse is a flamegraph trigger.
- Blog rewrite is the last chance to fix voice before launch. The 50% context budget assumes one careful pass, not iterative polishing.
- The `{{DEMO_URL}}` placeholder is filled at v2.1 Launch post-deploy, not in this phase. Do not resolve it in 26-03 or 26-04.
