---
phase: 26-test-migration-bench-docs-demo
plan: 03
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - benchmark/replay/generator.py
  - benchmark/replay/replay_30d.py
  - tests/integration/test_replay_*.py
  - src/server/ui/demo.html
  - src/server/ui/demo.js
  - src/server/ui/demo.css
  - deploy/smoke.sh
  - .planning/milestones/v2.1-PAUSED-ROADMAP.md
  - .planning/phases/26-test-migration-bench-docs-demo/26-03-SUMMARY.md
autonomous: true
requirements: [P26-SC5, P26-SC7]

must_haves:
  truths:
    - "benchmark/replay/generator.py + benchmark/replay/replay_30d.py use @tl.stream / @tl.table / new aggregation catalog — zero references to old API"
    - "tests/integration/test_replay_*.py pass under pytest on the new SDK"
    - "src/server/ui/demo.html + demo.js render live features via /public/* endpoints after Phase 24/25 additions"
    - "deploy/smoke.sh all 6 invariants pass locally against a freshly-built v0 binary, including the post-25 /metrics shape (tally_current_eps, tally_push_latency_p99_seconds, tally_events_total)"
    - "Full-stack local run: v0 binary + replay of 100k events + demo.html renders with live counters"
    - "Untouched: deploy/tally.service, deploy/Caddyfile, deploy/provision.sh, deploy/README.md (git diff --stat empty)"
    - "v2.1-PAUSED-ROADMAP.md unpaused → active state for Phase 20 redeployment (but NOT deployed in this plan)"
  artifacts:
    - path: "benchmark/replay/generator.py"
      provides: "v0-SDK event generator for the traction demo replay"
      contains: "@tl.stream"
    - path: "benchmark/replay/replay_30d.py"
      provides: "v0-SDK 30-day time-accelerated replay driver"
      contains: "@tl.stream"
    - path: "src/server/ui/demo.html"
      provides: "Traction demo frontend wired to current /public/* + /metrics shapes"
      contains: "public"
    - path: "deploy/smoke.sh"
      provides: "6-invariant post-deploy check covering post-25 /public/* + /metrics"
      contains: "invariants"
    - path: ".planning/milestones/v2.1-PAUSED-ROADMAP.md"
      provides: "Unpaused v2.1 Launch milestone doc (Phase 20 now re-active, awaiting deploy)"
      contains: "Active"
    - path: ".planning/phases/26-test-migration-bench-docs-demo/26-03-SUMMARY.md"
      provides: "Demo port closeout + full-stack smoke evidence"
      contains: "6/6"
  key_links:
    - from: "benchmark/replay/replay_30d.py"
      to: "@tl.stream / @tl.table"
      via: "decorator imports"
      pattern: "@tl\\.(stream|table)"
    - from: "src/server/ui/demo.js"
      to: "/public/stats + /metrics"
      via: "fetch() polls post-25 JSON shape"
      pattern: "public/stats|metrics"
    - from: "deploy/smoke.sh"
      to: "post-25 /metrics + /public/stats fields"
      via: "invariant 2 + 4 field check"
      pattern: "tally_current_eps|tally_push_latency_p99_seconds"
---

<objective>
# PLAN 26-03: Phase 20 traction demo rebuild against v0 SDK

## Goal
Port the Phase 20 traction demo stack end-to-end to the new `@tl.stream` / `@tl.table` SDK + the post-25 HTTP surface, verify the full stack runs locally, then unpause `.planning/milestones/v2.1-PAUSED-ROADMAP.md` so v2.1 Launch is `Active` (resumable) after 26-04 sign-off. Explicitly do **not** deploy — that's v2.1 Launch resuming post-sign-off.

Four artifacts to port:
- `benchmark/replay/generator.py` — synthetic fraud-event producer
- `benchmark/replay/replay_30d.py` — 30-day time-accelerated replay driver (+ `tests/integration/test_replay_*.py`)
- `src/server/ui/demo.html` + `demo.js` + `demo.css` — minimal read-only demo frontend
- `deploy/smoke.sh` — 6-invariant post-deploy check (extended to cover Phase 24/25 `/metrics` and `/public/*` additions)

Four artifacts to leave untouched (API-agnostic; verified clean via `git diff --stat`):
- `deploy/tally.service`
- `deploy/Caddyfile`
- `deploy/provision.sh`
- `deploy/README.md`

Purpose: sign-off criteria 5 and 7 from 26-CONTEXT.md (Phase 20 ports green; deploy-ready without re-provision). Blog rewrite is NOT in this plan — that's 26-02.

Output:
  - Ported replay CLI + integration tests
  - Ported demo frontend
  - Extended smoke.sh + 6/6 local pass
  - Unpaused v2.1-PAUSED-ROADMAP.md
  - `26-03-SUMMARY.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-test-migration-bench-docs-demo/26-CONTEXT.md
@.planning/milestones/v2.1-PAUSED-ROADMAP.md
@benchmark/replay/generator.py
@benchmark/replay/replay_30d.py
@deploy/smoke.sh
@deploy/README.md
@benchmark/tally-throughput/bench_v0.py
@CLAUDE.md
</context>

<interfaces>
<!-- New v0 SDK surface (from Phase 21-24) -->

```python
import tally as tl

# Source stream (class form)
@tl.stream
class RawTxns:
    user_id: str
    amount: float
    status: str
    merchant_id: str
    country: str

# Derived table (function form)
@tl.table(key="user_id")
def UserFeatures(raw: RawTxns) -> tl.Table:
    return raw.group_by("user_id").agg(
        tx_count_1h=tl.count(window="1h"),
        tx_sum_1h=tl.sum("amount", window="1h"),
        avg_amount_1h=tl.avg("amount", window="1h"),
        max_amount_24h=tl.max("amount", window="24h"),
        failed_count_30m=tl.count(window="30m", where="status == 'failed'"),
    )

# Client
app = tl.App("localhost:6400")
app.register(RawTxns, UserFeatures)
app.push(RawTxns, {"user_id": "u1", "amount": 50.0, "status": "success", ...})
features = app.get("u1")                                     # all features for key
result   = app.get_multi([UserFeatures, MerchantRisk], "u1") # multi-table single round-trip
app.push(SomeTable, key="u1", fields={...})                  # Table upsert (Phase 24)
app.delete(SomeTable, key="u1")                              # Table tombstone (Phase 24)
```

Reference working example: `benchmark/tally-throughput/bench_v0.py` (small/medium/large pipelines).

<!-- HTTP surface after Phase 24/25 additions -->

```
GET  /public/stats          # 6 fields: events_total, current_eps, p99_push_us, p50_push_us, uptime_seconds, keys_total
GET  /public/*              # read-only feature browsing (unchanged from Phase 20; confirm)
GET  /metrics               # Prometheus: tally_events_total, tally_current_eps,
                            #   tally_push_latency_p99_seconds, tally_late_events_dropped_total{stream},
                            #   tally_signals_active (25-03), tally_tombstones_total (24)
GET  /debug/warnings        # 25-03 unified warnings feed
GET  /debug/config-recommendations  # 25-02 suggestion engine
GET  /debug/streams/:name   # 24-04 per-stream watermark state
GET  /health                # {"status":"ok"}
```

<!-- deploy/smoke.sh 6 invariants (current) -->

1. `/health` returns `{"status":"ok"}`
2. `/public/stats` has all 6 required fields
3. Admin endpoint (`POST`/`DELETE /pipelines/*`) returns 401/403/404 from public side
4. `/metrics` exposes `tally_events_total`, `tally_current_eps`, `tally_push_latency_p99_seconds`
5. Crash-recovery: `keys_total` restored within 10% in 15s after `systemctl restart tally` (gated by `TALLY_SSH_HOST`)
6. TCP 6400 NOT reachable on the public interface

Phase 26 addition: verify invariant 4 includes the Phase 24/25 metric additions (`tally_late_events_dropped_total`, `tally_signals_active` if present). Extend field-check in invariant 2 if `/public/stats` gained fields. Do not add new invariants unless post-25 shape demands it — the existing 6 are the operator-facing contract.
</interfaces>

## Success Criteria

1. `benchmark/replay/generator.py` + `benchmark/replay/replay_30d.py` use `@tl.stream` / `@tl.table` / new aggregation catalog. Zero old-API grep hits.
2. `tests/integration/test_replay_*.py` are green on the new SDK. If any test asserts on introspection that no longer exists under the new API, rewrite to the equivalent behavior check — do not silently delete.
3. `src/server/ui/demo.html` + `demo.js` render live features against the current `/public/*` + `/metrics` endpoints. Any JS fetch/parse that referenced old shapes is updated to post-25 shape. `demo.css` likely untouched (confirm via diff).
4. `deploy/smoke.sh` runs locally against a freshly-built `tally` binary and passes 6/6 invariants, with invariant 4's `/metrics` check covering the Phase 24/25 additions.
5. Full-stack local smoke: build v0 binary → start server → register ported pipeline via `generator.py --register-only` → replay 100k events via `replay_30d.py --speed 1000x` → open `demo.html` (or hit the UI route) → verify live counters render.
6. `deploy/tally.service`, `deploy/Caddyfile`, `deploy/provision.sh`, `deploy/README.md` unchanged — `git diff --stat` on these 4 files is empty.
7. `.planning/milestones/v2.1-PAUSED-ROADMAP.md` updated: top milestone line `[⏸] v2.1 -- Launch` → `[ ] v2.1 -- Launch ... Active (resuming post v0 sign-off)`; Phase 20 status `[~]` → `[ ]` or equivalent "active, not yet deployed" marker; add a note at the top indicating v0 unblocks deploy. **Do NOT deploy** in this plan.
8. `26-03-SUMMARY.md` captures: ported-file list, integration test count, full-stack smoke evidence (100k events replayed; which features rendered), 6/6 invariant status, `git diff --stat` on the 4 protected files, v2.1 status change.

## Files Touched

- `/data/home/tally/benchmark/replay/generator.py` (port)
- `/data/home/tally/benchmark/replay/replay_30d.py` (port)
- `/data/home/tally/tests/integration/test_replay_*.py` (port)
- `/data/home/tally/src/server/ui/demo.html` (JS/endpoint refs)
- `/data/home/tally/src/server/ui/demo.js` (fetch/parse against post-25 shapes)
- `/data/home/tally/src/server/ui/demo.css` (likely unchanged; verify)
- `/data/home/tally/deploy/smoke.sh` (extend invariant 2/4 for post-25 fields if needed)
- `/data/home/tally/.planning/milestones/v2.1-PAUSED-ROADMAP.md` (unpause)
- `/data/home/tally/.planning/phases/26-test-migration-bench-docs-demo/26-03-SUMMARY.md` (new)

**Do NOT touch:**
- `/data/home/tally/deploy/tally.service`
- `/data/home/tally/deploy/Caddyfile`
- `/data/home/tally/deploy/provision.sh`
- `/data/home/tally/deploy/README.md`

## Implementation Steps

<tasks>

<task type="auto">
  <name>Task 1: Port replay CLI + integration tests to v0 SDK</name>
  <files>
    benchmark/replay/generator.py,
    benchmark/replay/replay_30d.py,
    tests/integration/test_replay_*.py
  </files>
  <action>
    1. **Read the artifacts first** to understand the Phase 20 event schema + CLI contract:
       - `benchmark/replay/generator.py` — deterministic fraud-event producer (`seed/n/days/now_ms` → reproducible stream)
       - `benchmark/replay/replay_30d.py` — 30-day time-accelerated replay driver; check its flags (`--speed`, `--target`, `--events`, `--workers`, `--register-only`)
       - `tests/integration/test_replay_*.py` — integration tests
       - Reference: `.planning/phases/20-traction-demo/` SUMMARY files for intent

    2. **Port pipeline definitions** in generator.py / replay_30d.py:
       - `from streamlet import ...` → `import tally as tl`
       - `@tl.source class RawTransactions: pass` → `@tl.stream class RawTxns: user_id: str; merchant_id: str; amount: float; status: str; country: str; ts: int` (match the 6-field schema from generator.py docstring)
       - `@tl.dataset(depends_on=[RawTransactions]) class UserTransactions: features = tl.group_by(...).agg(...)` → function-form `@tl.table(key="user_id") def UserFeatures(raw: RawTxns) -> tl.Table: return raw.group_by("user_id").agg(tx_count_1h=tl.count(window="1h"), ...)`
       - Old `st.derive("expr")` strings → `with_columns(...)` using `tl.col("a") / tl.col("b")` expression DSL
       - Match the Phase 20 feature set (tx_count_*, tx_sum_*, avg_amount_*, unique_merchants_*, failure_rate, velocity_spike, last_country, etc.). Re-derive where the new API's aggregation catalog differs in name (e.g., `distinct_count` → `tl.count_distinct`; `derive` → `with_columns` + `tl.col(...)`).

    3. **Preserve CLI flags** (`--speed`, `--target`, `--events`, `--workers`, `--register-only`, `--no-warmup`, `--days`, `--seed`) — `deploy/smoke.sh` invariant 4.5 calls `replay_30d.py --events 1000000 --workers 4 --host 127.0.0.1 --port 6400 --no-warmup`; don't break that surface. If the old API required an init hook the new SDK doesn't, adapt silently.

    4. **Port `tests/integration/test_replay_*.py`:**
       - Update imports + assertions that were tightly bound to old-API shapes
       - Run: `pytest tests/integration/test_replay -q` — must be green
       - If a test asserts `@tl.dataset` introspection that has no new-API equivalent, rewrite to an equivalent behavior check (e.g., assert the Table's `.describe()` output, or the feature value after a known event sequence). Do not silently delete.

    5. **Sanity**: the ported replay_30d.py, run locally against a v0 server with seed=42/n=1000, must produce deterministic features — two runs yield byte-identical `app.get(user_id)` output for the same user.

    6. **Grep assertion** — zero old-API refs under these paths:
       ```
       rg -n "@tl\.(source|dataset)|EventSet|FeatureSet|\.derive\(" benchmark/replay/ tests/integration/
       ```
  </action>
  <verify>
    <automated>bash -c 'set -e; ! rg -nq "@tl\.(source|dataset)|EventSet|FeatureSet" benchmark/replay/ tests/integration/; pytest tests/integration/test_replay -q'</automated>
  </verify>
  <done>generator.py + replay_30d.py use new API; integration tests green; deterministic replay verified; grep clean.</done>
</task>

<task type="auto">
  <name>Task 2: Port demo frontend + extend smoke.sh for post-25 /metrics shape</name>
  <files>
    src/server/ui/demo.html,
    src/server/ui/demo.js,
    src/server/ui/demo.css,
    deploy/smoke.sh
  </files>
  <action>
    1. **Inventory the demo JS** — read `src/server/ui/demo.js` (and `demo.html` for the fetch URL list). Identify every fetch / parse path that consumes server endpoints:
       - `/public/stats` (6 fields per smoke.sh invariant 2)
       - `/public/*` feature-browsing endpoints (if any)
       - `/metrics` (Prometheus text format; JS likely regex-extracts specific labels)
       - Any admin endpoints (should not be called from the public demo; if they are, remove)

    2. **Confirm current server shape** — read `src/server/http.rs` to see what `/public/*` and `/metrics` actually return now (after Phases 24 + 25 additions):
       - `/public/stats` shape (6 fields from smoke.sh)
       - `/metrics` label set including `tally_events_total`, `tally_current_eps`, `tally_push_latency_p99_seconds`, `tally_late_events_dropped_total{stream}` (24-04), and any Phase 25 additions
       - Confirm old shapes the demo expected are still present; if anything was renamed, update the demo fetch/parse

    3. **Update `demo.html` + `demo.js`:**
       - Fix any feature-name references that drifted when the pipeline was ported (Task 1)
       - Align fetch URLs + JSON field names with the current server
       - If the UI polls `/metrics`, ensure the regex matches the Prometheus-format label set (Phase 25 may have added labels; parsing must not break on new ones)
       - `demo.css` likely untouched — verify: `git diff src/server/ui/demo.css` should be empty at Task 2 end unless a feature rename required a new row

    4. **Extend `deploy/smoke.sh`:**
       - Invariant 2 (`/public/stats` fields): confirm the existing 6 fields (`events_total`, `current_eps`, `p99_push_us`, `p50_push_us`, `uptime_seconds`, `keys_total`) are still the contract. If Phase 24/25 added any field to `/public/stats`, check for it here too.
       - Invariant 4 (`/metrics` fields): current script asserts `tally_events_total` + `tally_current_eps` + `tally_push_latency_p99_seconds`. Post-25 the server also exposes `tally_late_events_dropped_total` (24-04) and potentially signal-related metrics. Add these to the assertion so a deploy regression doesn't hide the new observability surface.
       - Invariants 1, 3, 5, 6 unchanged (health / admin-denied / crash-recovery / public-TCP-6400-closed).
       - Total invariant count stays at 6 (extend existing checks, don't proliferate).

    5. **Local full-stack smoke** — proves the whole stack works end-to-end before v2.1 Launch resumes:
       ```
       cargo build --release --bin tally
       TALLY_SNAPSHOT_DIR=$(mktemp -d) target/release/tally &
       TALLY_PID=$!
       sleep 1

       # Register the ported pipeline
       python3 benchmark/replay/generator.py --register-only --target localhost:6400

       # Replay 100k events at 1000x speed
       python3 benchmark/replay/replay_30d.py \
           --events 100000 --workers 4 \
           --host 127.0.0.1 --port 6400 \
           --speed 1000x --no-warmup

       # Load the demo page (server serves it; confirm the route in src/server/http.rs)
       # Verify live counters render via automated HTTP checks, not just visual:
       curl -fsS http://localhost:<http-port>/public/stats | grep -q '"events_total"'
       curl -fsS http://localhost:<http-port>/public/stats | python3 -c 'import sys,json; d=json.load(sys.stdin); assert d["events_total"] >= 100000, d'
       curl -fsS http://localhost:<http-port>/metrics | grep -q "tally_events_total"

       # Run extended smoke
       bash deploy/smoke.sh http://localhost:<http-port>

       kill $TALLY_PID; wait $TALLY_PID 2>/dev/null
       ```
       Save full-stack evidence to `/tmp/26-03-fullstack.log` for the SUMMARY.

    6. **Confirm 4 deploy files untouched:**
       ```
       git diff --stat deploy/tally.service deploy/Caddyfile deploy/provision.sh deploy/README.md
       ```
       Must be empty. If any show up, revert.

    7. **Grep assertion** — no old API in UI or smoke.sh:
       ```
       rg -n "@tl\.(source|dataset)|EventSet|FeatureSet" src/server/ui/ deploy/
       ```
  </action>
  <verify>
    <automated>bash -c 'set -e; ! rg -nq "@tl\.(source|dataset)|EventSet|FeatureSet" src/server/ui/ deploy/; test -z "$(git diff --stat deploy/tally.service deploy/Caddyfile deploy/provision.sh deploy/README.md)"; rg -q "tally_late_events_dropped_total" deploy/smoke.sh; rg -q "invariants" deploy/smoke.sh'</automated>
  </verify>
  <done>demo frontend aligned to post-25 server shapes; smoke.sh extended for Phase 24/25 /metrics additions; full-stack local smoke passed with 100k events; 4 protected deploy files confirmed unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Unpause v2.1-PAUSED-ROADMAP.md + write 26-03-SUMMARY</name>
  <files>
    .planning/milestones/v2.1-PAUSED-ROADMAP.md,
    .planning/phases/26-test-migration-bench-docs-demo/26-03-SUMMARY.md
  </files>
  <action>
    1. **Unpause the v2.1 roadmap snippet.** Edit `.planning/milestones/v2.1-PAUSED-ROADMAP.md`:
       - Top milestone line: `- [ ] **v2.1 -- Launch** (Phase 20) -- Active` (currently flagged PAUSED pending v0)
       - Phase 20 line: update from the paused marker to an active-but-undeployed marker, e.g. `[ ] Phase 20: Traction Demo — v0-ready, awaiting deploy post v0 sign-off`
       - Add a note at the top: `**Status update (2026-04-14):** v0 restructure complete (Phase 26 sign-off); Phase 20 artifacts ported to the v0 SDK (generator.py / replay_30d.py / demo UI / smoke.sh). Deploy-ready: no re-provision needed; see deploy/README.md. Resume instructions land in 26-04-SUMMARY.md.`
       - **Do NOT change the filename** (v2.1-PAUSED-ROADMAP.md stays where it is — 26-04 decides the final archive path per GSD milestone-archive convention). Task is only to flip status markers + add the note.
       - **Do NOT deploy.** This task updates the doc only.

    2. **Write `26-03-SUMMARY.md`:**
       ```markdown
       # Phase 26-03 Summary — Traction demo port + v2.1 unpause

       Date: <ISO>
       HEAD: <git rev-parse HEAD>

       ## Ported artifacts

       | Artifact | Status | Notes |
       |---|---|---|
       | benchmark/replay/generator.py | Ported | @tl.source → @tl.stream; 6-field schema preserved |
       | benchmark/replay/replay_30d.py | Ported | CLI flags preserved (used by smoke.sh invariant 4.5) |
       | tests/integration/test_replay_*.py | Green | <N> tests |
       | src/server/ui/demo.html | Updated | fetch URLs aligned to post-25 shape |
       | src/server/ui/demo.js | Updated | /metrics label parsing extended for 24/25 additions |
       | src/server/ui/demo.css | Unchanged | git diff empty |
       | deploy/smoke.sh | Extended | invariant 4 now checks tally_late_events_dropped_total (24), still 6 invariants total |

       ## Protected deploy files (verified untouched)

       - deploy/tally.service — clean diff
       - deploy/Caddyfile — clean diff
       - deploy/provision.sh — clean diff
       - deploy/README.md — clean diff

       ## Full-stack local smoke

       - Build: `cargo build --release --bin tally` → OK
       - Register: `python3 benchmark/replay/generator.py --register-only` → OK
       - Replay 100k events @ 1000x → completed in <N>s, events_total=<≥100000>
       - demo.html → live counters render (curl assertion passed; HTTP 200)
       - `bash deploy/smoke.sh http://localhost:<port>` → 6/6 invariants PASS
       - Evidence: /tmp/26-03-fullstack.log

       ## v2.1 unpause

       - `.planning/milestones/v2.1-PAUSED-ROADMAP.md` status flipped: `[⏸] PAUSED` → `[ ] Active (awaiting deploy)`
       - Phase 20 marked v0-ready; NOT deployed
       - Deploy instructions pointer: `deploy/README.md` + 26-04-SUMMARY.md (resume checklist)

       ## Surprises / drift

       <anything that required engine or /public/* fixes during the port — 26-04 sign-off should see this>

       ## Handoff to 26-04

       - 26-04 sign-off criterion 10 (Phase 20 ports green) is satisfied by this SUMMARY
       - 26-04 sign-off criterion 11 (deploy-ready, binary-only redeploy) is satisfied by the "Protected deploy files" section
       - 26-04 owns the v0-milestone archive (copying ROADMAP.md v0 snippet → .planning/milestones/v0-ROADMAP.md); this plan only flips the v2.1 PAUSED → Active markers.
       ```
  </action>
  <verify>
    <automated>bash -c 'set -e; test -s .planning/phases/26-test-migration-bench-docs-demo/26-03-SUMMARY.md; rg -q "6/6" .planning/phases/26-test-migration-bench-docs-demo/26-03-SUMMARY.md; rg -q "Active" .planning/milestones/v2.1-PAUSED-ROADMAP.md; ! rg -q "^- \[⏸\] \*\*v2\.1" .planning/milestones/v2.1-PAUSED-ROADMAP.md'</automated>
  </verify>
  <done>v2.1 roadmap snippet status flipped to Active (Phase 20 awaiting deploy); 26-03-SUMMARY.md captures port evidence + full-stack smoke + handoff to 26-04.</done>
</task>

</tasks>

## Testing

- **Integration**: `pytest tests/integration/test_replay_*.py` green on new API.
- **Full-stack smoke**: build → start → register → replay 100k events → verify demo renders + smoke.sh 6/6 pass. Evidence in `/tmp/26-03-fullstack.log`.
- **Static**: grep assertion (zero old-API refs in benchmark/replay, tests/integration, src/server/ui, deploy); `git diff --stat` on 4 protected files is empty.
- **Manual**: operator opens `http://localhost:<port>/` and eyeballs the demo one last time before 26-04 sign-off.

## Risks & Mitigations

| Risk | Mitigation |
|---|---|
| Demo JS fetches an endpoint shape changed in Phase 24/25 (e.g., `/metrics` label set) | Read `src/server/http.rs` before touching JS; align fetch/parse to what the server actually returns now. Test via curl against the live server, not assumption. |
| Replay pipeline semantics drifted when ported — a `st.derive("a/b")` subtly differs under new `tl.col` DSL | Run the ported pipeline against a fixed seed + compare aggregate outputs to the Phase 20 integration-test expected values. Integration tests are the oracle. |
| Phase 20 feature list was tightly bound to old-API classes with class-body operator attrs | Function-form translation: extract each class attribute (`tx_count_1h = st.count(window="1h")`) into an `.agg()` kwarg; derives into `with_columns(tl.col(...))`. Cross-check against `bench_v0.py` pipelines for confirmed-working shape. |
| Accidentally touching one of the 4 protected deploy files | Pre-check with `git status` before edits; post-check with `git diff --stat` in Task 2 verify + SUMMARY. |
| smoke.sh invariant 4 assertion drifts as Phase 25 labels evolve | Anchor the check to the stable labels: `tally_events_total`, `tally_current_eps`, `tally_push_latency_p99_seconds`, `tally_late_events_dropped_total`. If Phase 25 added optional labels, smoke doesn't require them. |
| Full-stack smoke spuriously fails due to port collision / leftover server process | Use `mktemp -d` for snapshot dir; grab the PID explicitly and kill on exit; if port 6400 is busy, fail loud rather than run against a phantom instance. |
| Unpausing v2.1 is misread as "deploy now" | SUMMARY explicitly: "v2.1 marker flipped; deploy is NOT in Phase 26 scope; resume instructions are in 26-04-SUMMARY.md." 26-04 owns the deploy handoff. |

## Dependencies

- **26-01 Complete** — clean new-API surface to port against; cargo + pytest suites green.
- **26-02 concurrent or prior** — runs in same wave. This plan does NOT consume `MATRIX-V0-FINAL.json` (that's the blog's job in 26-02). No hard ordering beyond the shared `depends_on: ["26-01"]`.
- **Phase 20 artifacts present** in `/data/home/tally/benchmark/replay/`, `/data/home/tally/src/server/ui/`, `/data/home/tally/deploy/`.

## Notes

- Phase 20 code is clean; the port is expected to be mechanical (decorator imports + operator-form translation). If the port reveals a genuine engine behavior change, surface it in SUMMARY for 26-04 sign-off — don't silently paper over.
- The 4 protected deploy files (`tally.service`, `Caddyfile`, `provision.sh`, `README.md`) are API-agnostic by design. If you find yourself wanting to edit one, stop — the server port, TCP protocol framing, and ops model have not changed through v0. Any "needed" edit there is probably a sign the port in Task 1/2 is wrong.
- Do **not** deploy. This plan makes v2.1 deployable; the actual 5-day Hetzner run is v2.1 Launch resuming after 26-04 sign-off.
- `{{DEMO_URL}}` placeholder in the blog (owned by 26-02) is NOT resolved here. That's post-deploy, during v2.1 Launch.
