---
phase: 38-mothball-v0-client
plan: 01
waves:
  - id: 1
    tasks: [T1, T2, T3]
depends_on:
  - Phase 35 green
  - Phase 36 green
  - Phase 37 green
---

# Plan 38-01 — Mothball v0 embedded-client surfaces

## Goal

Delete the superseded embedded-client code and the `python-native` Pipeline class. Reduce cognitive surface by ~2k lines; prevent future readers from confusing dead paths with live ones.

## Task T1 — Delete Rust embedded-client modules

**Files:**
- Delete: `src/client/clone.rs`, `src/client/streaming.rs`, `src/client/state.rs`.
- Check-and-maybe-delete: `src/client/session.rs` (keep if Phase 36 reuses its helpers; delete if not).
- Prune `src/client/mod.rs`: remove module re-exports for anything deleted.
- Delete test files: `tests/test_client_streaming.rs`. Possibly `tests/client_engine_roundtrip.rs` if its coverage no longer makes sense after the engine is no longer used via embedded client.

**Subtasks:**
- `grep -r "client::clone\|client::streaming\|client::state\|client::StreamingClient\|client::FrozenClient\|client::run_clone" --include="*.rs" /data/home/tally/src/ /data/home/tally/tests/` — every hit is either a deletion target or a bug. Clean all.
- `cargo build` + `cargo test` still green after deletions.

**Done when:** `grep` returns nothing; full suite green.

## Task T2 — Delete `tally_cli` subcommands and `python-native` crate

**Files:**
- `src/bin/tally_cli.rs` — delete the `clone`, `query`, `inspect`, `sync` subcommands. If Phase 37 put `fork` on `tally_cli` (the fallback location), keep that one; otherwise delete the file.
- `python-native/` — delete entire directory.
- `Cargo.toml` workspace members — remove `python-native`.
- `.github/workflows/ci.yml` — remove `python-native` job.
- `python/tally` symlink — unless still pointing at valid content; likely delete.
- `tests/integration/test_tally_clone.py`, `tests/integration/test_replica_snapshot_fetch_asyncio.py` (**keep** — it tests Phase 27), `tests/integration/test_replica_subscribe_asyncio.py` (**keep** — Phase 27), `tests/integration/test_pipeline_e2e.py` (**delete** — Pipeline class gone).

**Subtasks:**
- Confirm each deleted file + directory.
- `cargo check` + `cargo test` + `scripts/check-feature-builds.sh` green.
- `pytest tests/integration/` green (test count goes DOWN — expected).

**Done when:** `python-native/` is gone; `git ls-files python-native` returns nothing.

## Task T3 — Doc sweep

**Files:**
- `.planning/ROADMAP.md` — phases 28 (sub-plans 02/03/04), 30, 31: add a **SUPERSEDED 2026-04-15 by Option M (Phases 35-37)** banner at the top of each phase block. Don't delete — historical record.
- `.planning/phases/28-client-engine-embedding/28-CONTEXT.md` — prepend SUPERSEDED banner.
- `.planning/phases/30-python-pipeline-api/30-CONTEXT.md` — same.
- `.planning/phases/31-streaming-mode-watch/31-CONTEXT.md` — same.
- `.planning/STATE.md` — update milestone status to reflect Option M is the active path; phase 35/36/37 are the live work; phase 38 = this cleanup.
- Phase SUMMARY.md files for 28/30/31-01 — leave as-is (historical).

**Subtasks:**
- Write the banners.
- Verify ROADMAP.md's progress table is still accurate.

**Done when:** docs reflect reality; no reader mistakes deleted code for live code.

## Verification

- `cargo test` + `pytest tests/integration/` green.
- `scripts/check-feature-builds.sh` green.
- Test count delta: expected ~-30 to -50 tests (obsoleted coverage).
- `find src tests python-native -name "*.rs" -o -name "*.py" | wc -l` — expect substantial drop.
- Atomic commit with message describing what was removed. Write `38-01-SUMMARY.md`.

## Stop-and-report triggers

- A deleted symbol has live downstream references you didn't anticipate (something still calls `FrozenClient::get` outside tests). Report with the grep hit; user decides whether to keep the symbol, refactor the caller, or pivot.
- Deleting `python-native` breaks existing pure-Python SDK imports (the symlink shenanigan from Plan 30-01).

## Budget

≤60 minutes.
