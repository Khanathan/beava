# Plan 13.5-06 — Summary

**Status:** GREEN
**Commits:** `d515381` (red — 13 dataset tests) → `fbe6c4c` (green — generator + 6 data files)

## What landed

### `python/beava/demos/_generate.py` (NEW, 250 LOC)

Deterministic generator (`random.Random(SEED)` per dataset) with three functions:
- `generate_adtech()` — 10K events: 6K Impression / 3K Click / 1K Conversion
- `generate_fraud()` — 10K events: 7.5K Txn / 2.5K Login
- `generate_ecommerce()` — 10K events: 7K PageView / 2K AddToCart / 1K Purchase

Run via `python -m beava.demos._generate`. Uses stdlib `random` only (no numpy dep). Seeds 42, 43, 44 for the three datasets respectively so re-running produces byte-identical output.

### Per-dataset breakdown

| Dataset | Events | events.jsonl size | Op-family coverage |
|---------|--------|-------------------|---------------------|
| adtech  | 10,000 | ~970 KB | count, n_unique (campaign), time_since (last click), quantile (conversion value), decayed_count, top_k (pages), inter_arrival_stats |
| fraud   | 10,000 | ~1.3 MB | sum, n_unique (merchant), geo_velocity, geo_distance, quantile, streak, histogram, z_score |
| ecommerce | 10,000 | ~840 KB | count, top_k (pages/SKUs), last_n, histogram, inter_arrival_stats, event_type_mix, rate_of_change |
| **TOTAL** | **30,000** | **~3.1 MB** | (within ~3 MB D-02 budget) |

### `python/tests/internal/test_demo_data.py` (NEW, 77 LOC)

13 parametric test cases:
- `test_demo_loads[adtech|fraud|ecommerce]` — `bv.demo()` returns dict
- `test_demo_event_count[adtech|fraud|ecommerce]` — each >= 1000 events
- `test_demo_schema_is_list_of_descriptors[adtech|fraud|ecommerce]`
- `test_adtech_has_impression_click_conversion`
- `test_fraud_has_txn_login`
- `test_ecommerce_has_pageview_addtocart_purchase`
- `test_event_lines_are_valid_json` — every line in every JSONL parses

## Tests

| File | Cases | Status |
|------|-------|--------|
| `tests/internal/test_demo_data.py` | 13 | GREEN |
| **All internal tests** | **133** | **GREEN** |

## Two commit SHAs

| SHA | Type | Subject |
|-----|------|---------|
| `d515381` | test | demo dataset round-trip red tests (13 cases) |
| `fbe6c4c` | feat | generate adtech / fraud / ecommerce demo datasets (~3 MB bundled) |

TDD red-then-green discipline observed.

## Reproducibility

`python -m beava.demos._generate` is idempotent. Re-running on a clean tree produces byte-identical files (verified via `git diff` after re-run; expected: empty).
