# Plan 13.5-01 — Summary

**Status:** GREEN
**Commits:** `8f6aa5f` (red test) → `0425f71` (green delete + opcode fix)

## What landed

- **Red commit (8f6aa5f):** `python/tests/internal/test_kept_modules.py` (3 test functions, 8 parametric cases) asserting:
  1. `OP_PUSH == 0x0010` (was failing — opcode was 0x0002)
  2. 4 kept modules import (`_wire`, `_transport`, `_errors`, `_embed`)
  3. 6 deleted modules raise `ModuleNotFoundError` (parametric x6)
- **Green commit (0425f71):** module deletions + opcode fix + `__init__.py` minimal foundation

## LOC removed

8 stale module files deleted (~2400 LOC across 6 listed in CONTEXT + 2 stale dependents `_app.py` + `_types.py`):

| File | Reason |
|------|--------|
| `_events.py` | Pre-13.0 `@bv.event` surface (Plan 03 rewrites) |
| `_agg.py` | Pre-13.0 32-op surface (Plan 04 rewrites with 53 ops) |
| `_schema.py` | Pre-13.0 schema introspection (Plan 03 rewrites) |
| `_validate.py` | Pre-13.0 DAG/topo validator |
| `_col.py` | Pre-13.0 expression DSL (Plan 03 rewrites with `bv.lit`) |
| `_eval_reference.py` | Depends on deleted `_col` surface |
| `_app.py` | Depends on deleted `_validate`; Plan 02 rewrites |
| `_types.py` | Depends on deleted schema surface |

## OP_PUSH wire-spec match

`python/beava/_wire.py`:
```
OP_PUSH: int = 0x0010  # data-plane push (Phase 13.5 Plan 01 — fixed from 0x0002)
```

Also added per docs/wire-spec.md and Phase 13.4 D-03:
- `OP_BATCH_GET = 0x0024`
- `OP_RESET = 0x0040` (test-mode-gated)

## Verification

- `python -m pytest tests/internal/test_kept_modules.py` — 8 passed
- `ls python/beava/*.py | wc -l` — 5 (matches Plan 01 verification target)
- TDD red-then-green discipline observed (Phase 3+ rule per CLAUDE.md)

## Handoff to Plans 02-07

Expected downstream test breakage (until Plans 02-07 land):
- `python/tests/v0/*` — 68 acceptance tests will turn red (Plan 11 green-up)
- `python/tests/test_*.py` (root) — pre-13.0 tests; migrated/removed in Plans 02-07

The public namespace (`beava.__init__`) re-exports only the 3 kept-module errors:
`RegistrationError`, `BinaryNotFoundError`, `ValidationError`. Plan 02 re-introduces
`bv.App`. Plan 03 re-introduces `bv.col` / `bv.lit` / `@bv.event` / `@bv.table`.
