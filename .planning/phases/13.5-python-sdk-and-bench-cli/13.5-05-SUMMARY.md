# Plan 13.5-05 — Summary

**Status:** GREEN
**Commits:** `7b6ed5d` (red — 12 tests across 3 files) → `ce3d452` (green — demo loader + submodules + pyproject force-include)

## What landed

### PEP 563 status: ALREADY IN PLACE (verified)

Plan 03 implemented `_make_event_source` using `get_type_hints(cls, include_extras=True)` (not raw `cls.__annotations__`). Plan 05's PEP 563 tests verify the contract is held — they passed on first run with no changes to `_events.py`. Module-level event sources / derivations resolve `Optional[str]` and `str`/`float` to their real types under `from __future__ import annotations`.

### `python/beava/_demo.py` (NEW, 63 LOC)

`bv.demo(name)` factory:
- Validates `name in {"adtech", "fraud", "ecommerce"}` — `ValueError` otherwise
- Loads `python/beava/demos/<name>/{schema.json, events.jsonl}` via `importlib.resources.files`
- Returns `{"name": <name>, "schema": <list>, "events": <list of dicts>}`
- Plan 06 ships the data files; Plan 05 raises a clean `RuntimeError` mentioning Plan 06 / "not yet bundled" until then (the loader handshake the test parametrizes around)

### `python/beava/test/__init__.py` (NEW, 16 LOC)

Empty submodule reserved for Plan 07's `fixture()` / `replay()` / `assert_features_eq()` / `MockApp`.

### `python/beava/cli/__init__.py` (NEW, 18 LOC)

`main()` placeholder. Plan 08 wires the real argparse subcommand graph (`beava bench {throughput,mixed,memory,fsync}`).

### `python/beava/demos/{__init__,adtech,fraud,ecommerce}/`

Package-data placeholder directories (`.gitkeep`). The empty `beava.demos/__init__.py` makes it an importable package so `importlib.resources.files("beava.demos")` works deterministically.

### `python/pyproject.toml` — wheel force-include

Added per D-02:

```toml
[tool.hatch.build.targets.wheel.force-include]
"beava/demos" = "beava/demos"
```

So `pip install beava` ships the demo data once Plan 06 lands the JSON/JSONL files.

### `python/beava/__init__.py`

Now re-exports `demo`. Total public surface: 4 (App + 3 errors) + 5 (Plan 03 DSL) + 59 (Plan 04 helpers) + 1 (`demo`) = **69 names** in `bv` namespace.

## Tests

| File | Cases | Status |
|------|-------|--------|
| `tests/internal/test_pep563.py` | 3 | GREEN (PEP 563 tripwire) |
| `tests/internal/test_module_layout.py` | 4 | GREEN |
| `tests/internal/test_demo_loader.py` | 5 | GREEN |
| **Plan 05 contribution** | **12** | **GREEN** |
| **All internal tests** | **120** | **GREEN** |

## Two commit SHAs

| SHA | Type | Subject |
|-----|------|---------|
| `7b6ed5d` | test | PEP 563 + module layout + demo loader red tests (12 cases) |
| `ce3d452` | feat | PEP 563 + bv.demo loader skeleton + beava.test/cli submodules |

TDD red-then-green discipline observed (PEP 563 tests act as a regression tripwire — they were green at red-commit time because Plan 03 already used `get_type_hints`; the file is the future regression contract).

## Handoff

- Plan 06 fills `beava/demos/{adtech,fraud,ecommerce}/{schema.json,events.jsonl}`. The loader will then return the populated dict instead of raising RuntimeError.
- Plan 07 populates `beava.test` with fixtures + MockApp.
- Plan 08 wires `beava.cli.main` to argparse.
