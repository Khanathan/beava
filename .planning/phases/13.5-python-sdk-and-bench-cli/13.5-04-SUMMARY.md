# Plan 13.5-04 — Summary

**Status:** GREEN
**Commits:** `c48c7bf` (red — 56 signature tests) → `5fa7ab8` (green — 53 helpers + ema alias + 5 deprecation aliases) → `02d24a6` (tripwire — 5 deprecation tests)

## What landed

### `python/beava/_agg.py` (NEW, 762 LOC)

**8 families × 53 unique ops + ema alias + 5 deprecation aliases:**

| Family | Count | Ops |
|--------|-------|-----|
| Core | 8 | count, sum, mean, min, max, var, std, ratio |
| Sketch | 5 | n_unique, quantile, top_k, bloom_member, entropy |
| Point/ordinal | 5 | first, last, first_n, last_n, lag |
| Recency | 10 | first_seen, last_seen, age, has_seen, time_since, time_since_last_n, streak, max_streak, negative_streak, first_seen_in_window |
| Decay | 6 (+ ema alias) | ewma, ewvar, ew_zscore, decayed_sum, decayed_count, twa |
| Velocity | 9 | rate_of_change, inter_arrival_stats, burst_count, delta_from_prev, trend, trend_residual, outlier_count, value_change_count, z_score |
| Bounded buffers | 7 | histogram, hour_of_day_histogram, dow_hour_histogram, seasonal_deviation, event_type_mix, most_recent_n, reservoir_sample |
| Geo | 4 | geo_velocity, geo_distance, geo_spread, distance_from_home |
| **TOTAL UNIQUE** | **53** | |

**Plus:** 1 alias (`ema` → `ewma`) + 5 ADR-002 deprecation aliases (`avg`, `variance`, `stddev`, `count_distinct`, `percentile`).

### Q1 Path B enforcement

`_enforce_field_str(field, op)` raises `RegistrationError(code='schema_mismatch')` when an `_Expr` is passed as a field name. Every field-bearing op (40+ helpers) gates through this validator. Tested by `test_sum_field_expr_raises_RegistrationError`.

### Validators

- `_validate_window` accepts `\d+(ms|s|m|h|d)` or `forever`; required vs optional gated per op family
- `_validate_half_life` accepts the same window grammar (decay ops require it)
- `quantile.q` ∈ `(0, 1)` open interval
- `top_k.k`, `first_n.n`, `last_n.n`, `lag.n`, `time_since_last_n.n`, `histogram.buckets`, `most_recent_n.n`, `reservoir_sample.k` all `>= 1`

### `python/beava/__init__.py`

Re-exports all 53 unique helpers + `ema` + 5 deprecation aliases (= **59 names** in addition to the Plan 03 surface).

## Tests

| File | Cases | Status |
|------|-------|--------|
| `tests/internal/test_op_helpers_signatures.py` | 56 | GREEN |
| `tests/internal/test_op_helpers_deprecation.py` | 5 | GREEN |
| **Plan 04 contribution** | **61** | **GREEN** |
| **All internal tests** | **108** | **GREEN** |

## ADR-002 trace

| Old name | New name | Behavior |
|----------|----------|----------|
| `avg` | `mean` | Emits `DeprecationWarning`; forwards |
| `variance` | `var` | Emits `DeprecationWarning`; forwards |
| `stddev` | `std` | Emits `DeprecationWarning`; forwards |
| `count_distinct` | `n_unique` | Emits `DeprecationWarning`; forwards |
| `percentile(p=)` | `quantile(q=)` | Emits `DeprecationWarning`; forwards `p`→`q` |

## Three commit SHAs

| SHA | Type | Subject |
|-----|------|---------|
| `c48c7bf` | test | 53-op helper signature red tests (56 cases) |
| `5fa7ab8` | feat | 53 op helpers + ema alias + 5 ADR-002 deprecation aliases |
| `02d24a6` | test | ADR-002 deprecation-alias regression tripwire (5 cases) |

TDD red-then-green discipline observed.

## Handoff

The op helpers each return an `AggDescriptor` with `.to_dict()`. Plan 03's `GroupBy.agg(**named)` already serializes via the `hasattr(agg, "to_dict")` protocol. The integration is end-to-end on the in-process surface; Plan 11 (engine round-trip) is the green-up gate against the real engine.
