---
status: passed
phase: 05.5-perf-harness-retroactive-baselines
verified: 2026-04-23
must_haves_total: 5
must_haves_verified: 5
human_verification: []
gaps: []
---

# Phase 05.5 Verification — PASSED

**Phase Goal:** Set up `criterion` bench harness workspace-wide. Write retroactive microbenches for every prior phase's hot path. Establish baselines in `.planning/perf-baselines.md`. Lock the regression-gate convention (10% warn / 25% block) in CLAUDE.md.

## Success Criteria (5/5)

| SC | Verified |
|---|---|
| SC1: `cargo bench --workspace` runs + produces numbers | ✅ All 28 bench IDs produced via scripts/capture-baselines.sh |
| SC2: perf-baselines.md with hw-class tagging committed | ✅ `apple-m4 / darwin-24.3.0 / 10 cores` section populated |
| SC3: Regression gate convention in CLAUDE.md | ✅ §Performance Discipline subsection (10%/25% thresholds; Phase 6+ mandatory bench; plan-checker contract) |
| SC4: ≥1 bench per phase (2.5/3/4/5) | ✅ Phase 2.5: 6 wire codec benches; Phase 3: 1 SDK compile bench; Phase 4: 10 expr/eval/op-chain benches; Phase 5: 11 agg benches |
| SC5: Phase 13 remains the ship gate (this phase measures only) | ✅ No src/* files modified outside bench-harness wiring; no optimization work |

## Requirements Coverage (10/10)

PERF-HARNESS-01/02/03 + PERF-BENCH-WIRE-01 + PERF-BENCH-SDK-01 + PERF-BENCH-EXPR-01 + PERF-BENCH-OPCHAIN-01 + PERF-BENCH-AGG-01 + PERF-BENCH-WINDOWED-01 + PERF-BENCH-APPLY-01 — all delivered.

## Baseline Highlights (apple-m4 / darwin / 10 cores)

**Phase 5 aggregation hot path** (the most-scrutinized numbers):
- `agg_op/count` 1.8 ns — on target
- `agg_op/sum` 5.7 ns, `avg` 5.5 ns — on target
- `agg_op/variance` 12.1 ns (Welford) — on target
- `windowed/fold_*_1Mevt` ~7-9 ns/event — on target for 3M EPS/core with 5 aggs (fold is query time, not update; update is already ~5ns)
- `apply/3agg_100ent_1Kevt` 1.01 µs / 1K events — **~1M events/sec for 3 aggregations end-to-end**. Phase 13 target of ≥3M EPS/core is in range with headroom.

**Phase 4 expression** (slight watch):
- `eval/arith` 110 ns — marginally above informal <100 ns guidance; within 10% of that threshold. Acceptable for v0.1 baseline; revisit in Phase 13 if perf gate pressure demands.
- `op_chain/apply_4op` 401 ns — ~2.5M 4-op rows/sec/core. Phase 13 target (3M EPS/core) requires either simpler chains (typical) or optimization.

**Phase 2.5 wire codec:**
- `encode/register_small` 21.7 ns — excellent
- `decode/register_near_limit` (1 MiB) 37 µs — acceptable for cold path

**Phase 3 Python SDK:** `test_register_compile_10_descriptors` 110 µs — cold path (runs once per register).

## Gates

- `cargo bench --workspace --no-run`: clean
- `cargo test --workspace`: all pre-existing green + new baseline-smoke green
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: clean
- `cargo fmt --all --check`: clean
- `pytest --benchmark-only tests/bench_register_compile.py`: runs
- ruff + mypy: clean

## Architectural Carry-Forward

- Every phase from 6 onward MUST ship ≥ 1 microbench per the locked CLAUDE.md §Performance Discipline
- Plan-checker contract updated: reject Phase 6+ plans without a bench task
- `scripts/capture-baselines.sh` is the re-usable helper — invoke it at phase-complete time to update baselines
- Regression diff tooling (compare current bench output vs baselines file) is a lightweight Python script TODO — small scope, can land inline with any Phase 6+ plan that needs it

## Pre-existing concerns (not Phase 5.5 regressions)

None introduced. `cli_smoke` timing-race flake noted in Phase 5 VERIFICATION persists but unrelated to bench harness work.
