---
phase: 05.5-perf-harness-retroactive-baselines
plan: "04"
subsystem: beava-core / bench
tags: [bench, criterion, aggregation, hot-path, performance]
dependency_graph:
  requires: ["05.5-01"]
  provides: [PERF-BENCH-AGG-01, PERF-BENCH-WINDOWED-01, PERF-BENCH-APPLY-01]
  affects: [beava-core]
tech_stack:
  added: []
  patterns: [criterion iter_batched, black_box, BenchmarkGroup throughput]
key_files:
  created: []
  modified:
    - crates/beava-core/benches/phase5_agg.rs
decisions:
  - "Use AggOp::update (not update_with_row) for per-op benches — simpler hot path, no Expr overhead"
  - "iter_batched(LargeInput) for WindowedOp fold so each sample starts with fresh state"
  - "Three aggregations in apply bench: windowless count, windowless sum, windowed count (5m) — covers both code paths"
  - "BTreeMap::new (not closure) for iter_batched setup — clippy redundant_closure fix"
metrics:
  duration: ~35 min
  completed: "2026-04-23T19:23:39Z"
  tasks_completed: 2
  files_changed: 1
---

# Phase 5.5 Plan 04: Phase 5 Aggregation Hot-Path Benches Summary

**One-liner:** 11 criterion benches covering AggOp::update per variant (8), WindowedOp 64-bucket fold (2), and apply_event_to_aggregations end-to-end (1).

## What Was Built

Populated `crates/beava-core/benches/phase5_agg.rs` (replacing the plan-01 stub) with a full criterion bench suite for Beava's Phase 5 aggregation hot path.

### Bench Groups (11 total)

| Group | Bench ID | Measurement |
|-------|----------|-------------|
| AggOp per-variant | `agg_op/count` | ~1.3 ns/iter |
| AggOp per-variant | `agg_op/sum` | ~4.9 ns/iter |
| AggOp per-variant | `agg_op/avg` | ~4.3 ns/iter |
| AggOp per-variant | `agg_op/min` | ~6.5 ns/iter |
| AggOp per-variant | `agg_op/max` | ~7.9 ns/iter |
| AggOp per-variant | `agg_op/variance` | ~8.7 ns/iter |
| AggOp per-variant | `agg_op/stddev` | ~9.3 ns/iter |
| AggOp per-variant | `agg_op/ratio` | ~1.5 ns/iter |
| WindowedOp fold | `windowed/fold_count_5m_1Mevt` | 1M events/sample |
| WindowedOp fold | `windowed/fold_sum_5m_1Mevt` | 1M events/sample |
| End-to-end apply | `apply/3agg_100ent_1Kevt` | ~589 µs/1K events |

**Throughput annotations:** Windowed benches use `Throughput::Elements(1_000_000)`; apply bench uses `Throughput::Elements(1_000)`.

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| 1.a RED | `62d1ded` | `test(05.5-04): add phase5 aggregation bench contract (RED)` |
| 1.b GREEN | `3ff9d77` | `feat(05.5-04): phase 5 aggregation hot-path benches (AggOp + WindowedOp + apply)` |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Criterion import line exceeded rustfmt line width**
- **Found during:** Task 1.b — `cargo fmt --all --check`
- **Issue:** `use criterion::{criterion_group, criterion_main, BatchSize, BenchmarkGroup, Criterion, Throughput};` exceeded the default 100-char line limit
- **Fix:** Split into multi-line import block per rustfmt style
- **Files modified:** `crates/beava-core/benches/phase5_agg.rs`

**2. [Rule 1 - Bug] Redundant closure in iter_batched setup**
- **Found during:** Task 1.b — `cargo clippy -- -D warnings`
- **Issue:** `|| BTreeMap::<String, AggStateTable>::new()` is a redundant closure wrapping an associated fn
- **Fix:** Replaced with `BTreeMap::<String, AggStateTable>::new` (function pointer directly)
- **Files modified:** `crates/beava-core/benches/phase5_agg.rs`

## Known Stubs

None — all bench functions are fully wired to live crate code. No placeholder data sources.

## Threat Flags

None — bench-only file, no new network endpoints, auth paths, or schema changes.

## Self-Check

- [x] `crates/beava-core/benches/phase5_agg.rs` exists and is 290+ lines
- [x] `cargo bench -p beava-core --bench phase5_agg --no-run` compiles clean
- [x] `cargo bench -p beava-core --bench phase5_agg -- --quick` emits 11 bench IDs
- [x] `cargo fmt --all --check` passes
- [x] `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes
- [x] RED commit `62d1ded` precedes GREEN commit `3ff9d77`
- [x] Only `phase5_agg.rs` modified

## Self-Check: PASSED
