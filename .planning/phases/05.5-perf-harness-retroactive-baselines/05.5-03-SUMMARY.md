---
phase: "05.5-perf-harness-retroactive-baselines"
plan: "03"
subsystem: "beava-core / benchmarks"
tags: ["criterion", "benchmarks", "phase4", "expr", "eval", "op-chain", "tdd"]
dependency_graph:
  requires: ["05.5-01"]
  provides: ["PERF-BENCH-EXPR-01", "PERF-BENCH-OPCHAIN-01"]
  affects: ["crates/beava-core/benches/phase4_expr.rs"]
tech_stack:
  added: []
  patterns: ["criterion benchmark groups", "black_box anti-folding", "pre-parsed Expr fixtures", "row clone-per-iteration pattern"]
key_files:
  modified:
    - path: "crates/beava-core/benches/phase4_expr.rs"
      description: "Full criterion bench: 3 parse + 5 eval + 2 op_chain groups (215 lines)"
decisions:
  - "Parser uses single-quoted strings only — all expr fixtures use `'ok'` not `\"ok\"`"
  - "OpNode::WithColumns takes BTreeMap<String, String> not Vec — fixture corrected from plan skeleton"
  - "OpChain::apply takes owned Row (not &Row) — apply bench clones row per iteration"
  - "Schema from schema_propagate (not schema.rs) — required for OpChain::compile"
  - "eval/cast bench uses `(cast(x, float) > 0)` — wrap in comparison to avoid BareIdent result short-circuiting black_box"
  - "EXPR_DEEP uses concat! macro to keep lines short while producing ~150-char expression"
  - "RED signal: top-level `use phase4_expr_benches::EXPECTED_GROUPS as _` produces E0432 compile error"
metrics:
  duration: "~25 minutes"
  completed_date: "2026-04-23"
  tasks_completed: 2
  files_modified: 1
---

# Phase 05.5 Plan 03: Phase 4 Expr Parse + Eval + OpChain Benches Summary

Populated `crates/beava-core/benches/phase4_expr.rs` with 10 criterion bench groups covering Phase 4's hot paths: expression parse cost, per-operator-family eval cost, and OpChain compile + apply cost for a realistic 4-op chain.

## What Was Built

**File:** `crates/beava-core/benches/phase4_expr.rs` (215 lines, replaces 2-line stub)

### Parse benches (3 groups)

| Bench | Expression | Baseline (apple-m1-pro, 2026-04-23) |
|-------|-----------|--------------------------------------|
| `parse/small` | `(amount > 100)` — 14 chars | ~675 ns/iter |
| `parse/medium` | `((amount > 100) and ((status == 'ok') and (country != 'IR')))` — 62 chars | ~1.79 µs/iter |
| `parse/deep` | 12-level nested `and` chain mixing arith, boolean, isnull — ~150 chars | ~15.4 µs/iter |

### Eval benches (5 groups — pre-parsed Expr, steady-state only)

| Bench | Expression | Baseline |
|-------|-----------|----------|
| `eval/arith` | `((a + (b * c)) - (d / 2))` against I64 row | ~125 ns/iter |
| `eval/compare` | `(amount > 100)` against F64(250) row | ~29 ns/iter |
| `eval/boolean` | `((a > 1) and ((b < 2) and (c == 3)))` against I64 row | ~134 ns/iter |
| `eval/nullcheck` | `isnull(x)` against `{x: Null}` row | ~50 ns/iter |
| `eval/cast` | `(cast(x, float) > 0)` against `{x: I64(42)}` row | ~95 ns/iter |

### OpChain benches (2 groups)

| Bench | Description | Baseline |
|-------|-------------|----------|
| `op_chain/compile_4op` | `Filter → WithColumns → Select → Cast` compile cost | ~4.7 µs/iter |
| `op_chain/apply_4op` | Same 4-op chain per-row apply (row cloned each iter) | ~556 ns/iter |

## Commits

| Task | Commit | Message |
|------|--------|---------|
| 1.a (RED) | `11745e7` | `test(05.5-03): add phase4 expr/eval/op-chain bench contract (RED)` |
| 1.b (GREEN) | `b9d0659` | GREEN content landed via parallel agent's fmt sweep; content verified correct |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] OpNode::WithColumns uses BTreeMap not Vec**
- **Found during:** Task 1.b
- **Issue:** Plan skeleton showed `WithColumns { exprs: vec![("is_big".into(), ...)] }` but actual type is `BTreeMap<String, String>`
- **Fix:** Used `BTreeMap::new()` + `.insert()` for all WithColumns/Map fixtures
- **Files modified:** `crates/beava-core/benches/phase4_expr.rs`

**2. [Rule 1 - Bug] OpChain::apply takes owned Row not &Row**
- **Found during:** Task 1.b
- **Issue:** Plan skeleton used `chain.apply_row(black_box(&row))` but actual method is `apply(row: Row)` (takes by value)
- **Fix:** Added `row.clone()` inside the bench closure; method name corrected to `apply`

**3. [Rule 1 - Bug] Parser uses single-quoted strings**
- **Found during:** Task 1.b
- **Issue:** Plan skeleton used double-quoted strings inside expressions (e.g., `"ok"`) but parser only supports single-quoted string literals
- **Fix:** All string literals in expressions use single quotes: `'ok'`, `'IR'`

**4. [Rule 1 - Bug] RED signal approach**
- **Found during:** Task 1.a
- **Issue:** `#[cfg(test)]` block referencing unresolved module does not fail in `harness = false` bench builds (test cfg not active)
- **Fix:** Placed `use phase4_expr_benches::EXPECTED_GROUPS as _` at top-level to produce E0432 compile error

### Out-of-scope deferred items

Pre-existing `clippy::redundant_closure` warning in `crates/beava-core/benches/phase5_agg.rs:259` logged to `deferred-items.md`. Not caused by this plan.

## Known Stubs

None — all bench groups are fully wired with real fixtures and real function calls.

## Threat Flags

None — bench file only calls existing library functions with fixed test fixtures. No new network endpoints, auth paths, or schema changes.

## Self-Check

- [x] `crates/beava-core/benches/phase4_expr.rs` exists (215 lines, GREEN content)
- [x] RED commit `11745e7` exists
- [x] GREEN content at `b9d0659` (HEAD)
- [x] `cargo bench -p beava-core --bench phase4_expr -- --quick` emits all 10 bench IDs
- [x] `cargo clippy -p beava-core --bench phase4_expr -- -D warnings` clean
- [x] `cargo fmt --all --check` clean

## Self-Check: PASSED
