---
phase: 05.5-perf-harness-retroactive-baselines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/beava-core/Cargo.toml
  - crates/beava-server/Cargo.toml
  - crates/beava-core/benches/phase4_expr.rs
  - crates/beava-core/benches/phase5_agg.rs
  - crates/beava-server/benches/phase25_wire.rs
  - .planning/perf-baselines.md
  - CLAUDE.md
autonomous: true
requirements:
  - PERF-HARNESS-01
  - PERF-HARNESS-02
  - PERF-HARNESS-03

must_haves:
  truths:
    - "`criterion` is a workspace dev-dep usable by any member crate"
    - "Each crate that will host benches has `harness = false` bench targets wired"
    - ".planning/perf-baselines.md exists with a hw-class header template and is committed"
    - "CLAUDE.md §Conventions documents §Performance Discipline (10% warn / 25% block + mandatory bench per Phase 6+)"
  artifacts:
    - path: "Cargo.toml"
      provides: "criterion workspace dev-dep"
      contains: 'criterion = { version = "0.5"'
    - path: "crates/beava-core/Cargo.toml"
      provides: "bench target registration for beava-core benches"
      contains: "[[bench]]"
    - path: "crates/beava-server/Cargo.toml"
      provides: "bench target registration for beava-server benches"
      contains: "[[bench]]"
    - path: ".planning/perf-baselines.md"
      provides: "hw-class-tagged baseline ledger"
      min_lines: 20
    - path: "CLAUDE.md"
      provides: "§Performance Discipline convention with 10/25% thresholds"
      contains: "Performance Discipline"
  key_links:
    - from: "crates/beava-core/Cargo.toml"
      to: "Cargo.toml workspace dev-deps"
      via: "criterion = { workspace = true }"
      pattern: 'criterion\s*=\s*\{\s*workspace\s*=\s*true'
    - from: "crates/beava-server/Cargo.toml"
      to: "Cargo.toml workspace dev-deps"
      via: "criterion = { workspace = true }"
      pattern: 'criterion\s*=\s*\{\s*workspace\s*=\s*true'
    - from: "CLAUDE.md"
      to: ".planning/perf-baselines.md"
      via: "§Performance Discipline references the baselines file by path"
      pattern: '\.planning/perf-baselines\.md'
---

<objective>
Set up the shared `criterion` bench harness workspace-wide, create the empty hw-class-tagged baselines ledger, and codify §Performance Discipline in CLAUDE.md so every downstream bench plan (02–05) and every future phase (6+) has a home to land in. This plan ships the **scaffolding only** — no benches yet.

Purpose: implement D-01 (criterion crate), D-02 (per-crate bench file layout), D-03 (baselines file shape), and D-05 (regression convention) from 05.5-CONTEXT.md. Locks the convention before benches land so plans 02–05 can assume it.
Output: criterion wired; bench targets declared per crate (pointing at yet-to-land files with `harness = false`); `.planning/perf-baselines.md` with schema + example entry; CLAUDE.md amended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.5-perf-harness-retroactive-baselines/05.5-CONTEXT.md
@CLAUDE.md
@Cargo.toml
@crates/beava-core/Cargo.toml
@crates/beava-server/Cargo.toml

<interfaces>
<!-- Key existing conventions the executor must preserve. -->

Cargo.toml workspace dev-deps currently pinned (keep style consistent):
```toml
[workspace.dependencies]
proptest = "1"
tempfile = "3"
```

Per-crate Cargo.toml pattern (beava-core today):
```toml
[dev-dependencies]
tempfile = { workspace = true }
proptest = { workspace = true }
```

Per-crate bench target pattern to add (criterion requires `harness = false`):
```toml
[[bench]]
name = "phase4_expr"
harness = false
# path defaults to benches/phase4_expr.rs
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (red) — Add smoke test that fails because criterion + bench targets + baselines file are absent</name>
  <files>crates/beava-server/tests/phase5_5_harness_smoke.rs, crates/beava-server/Cargo.toml</files>
  <read_first>
    - crates/beava-server/tests/phase2_5_smoke.rs (existing integration-test conventions, `#[tokio::test]`, `required-features = ["testing"]` gating)
    - crates/beava-server/Cargo.toml (how `[[test]]` entries are declared)
  </read_first>
  <behavior>
    - A new integration test `phase5_5_harness_smoke.rs` asserts, from a plain `#[test]` (no tokio needed):
      1. Workspace `Cargo.toml` contains the literal substring `criterion = { version = "0.5"` in its `[workspace.dependencies]` section (read file relative to `CARGO_MANIFEST_DIR/../../Cargo.toml`)
      2. `crates/beava-core/Cargo.toml` contains at least one `[[bench]]` section AND `criterion = { workspace = true }` in `[dev-dependencies]`
      3. `crates/beava-server/Cargo.toml` contains at least one `[[bench]]` section AND `criterion = { workspace = true }` in `[dev-dependencies]`
      4. `.planning/perf-baselines.md` exists at repo root (path resolved from `CARGO_MANIFEST_DIR/../../.planning/perf-baselines.md`) AND contains the literal string `## hw-class:` (the template header)
      5. `CLAUDE.md` at repo root contains the literal string `## Performance Discipline` (or a sub-heading `### Performance Discipline` under §Conventions)
    - All five assertions currently fail (no deps, no bench targets, no baselines file, no heading).
  </behavior>
  <action>
    1. Create `crates/beava-server/tests/phase5_5_harness_smoke.rs` as a plain `#[test] fn harness_scaffolding_present()`. Use `std::fs::read_to_string` with paths built from `env!("CARGO_MANIFEST_DIR")`. Use simple `assert!(contents.contains("..."), "expected ... in {path}")` for each of the 5 checks. Do NOT use any external crates.
    2. Wire the test in `crates/beava-server/Cargo.toml` under the existing `[[test]]` block pattern, gated behind `required-features = ["testing"]` for consistency with siblings:
       ```toml
       [[test]]
       name = "phase5_5_harness_smoke"
       path = "tests/phase5_5_harness_smoke.rs"
       required-features = ["testing"]
       ```
    3. Run `cargo test -p beava-server --features testing phase5_5_harness_smoke -- --exact harness_scaffolding_present` and CONFIRM RED (at least one of the 5 asserts fails because the deps/files do not yet exist).
    4. Commit with message `test(05.5-01): add harness scaffolding smoke (RED)`.
  </action>
  <acceptance_criteria>
    - File `crates/beava-server/tests/phase5_5_harness_smoke.rs` exists and contains exactly one `#[test]` function `harness_scaffolding_present`
    - `cargo test -p beava-server --features testing phase5_5_harness_smoke` FAILS (non-zero exit) before task 1.b runs
    - Commit message matches regex `^test\(05\.5-01\):`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (green) — Wire criterion workspace dev-dep, per-crate bench targets, baselines file, CLAUDE.md §Performance Discipline</name>
  <files>Cargo.toml, crates/beava-core/Cargo.toml, crates/beava-server/Cargo.toml, .planning/perf-baselines.md, CLAUDE.md</files>
  <read_first>
    - Cargo.toml (current workspace.dependencies block — append criterion consistently with existing style; pin version to `0.5`, enable default features only)
    - crates/beava-core/Cargo.toml and crates/beava-server/Cargo.toml (current `[dev-dependencies]` style — add `criterion = { workspace = true }` and `[[bench]]` blocks)
    - CLAUDE.md (§Conventions has `### TDD Discipline` — add `### Performance Discipline` as a sibling subsection under §Conventions, NOT under §Project or §Architecture)
    - .planning/phases/05.5-perf-harness-retroactive-baselines/05.5-CONTEXT.md D-03 for the exact baselines-file schema
    - crates/beava-server/tests/phase5_5_harness_smoke.rs from Task 1.a (the 5 checks drive exactly what to write)
  </read_first>
  <action>
    1. **Workspace Cargo.toml** — add under `[workspace.dependencies]`:
       ```toml
       criterion = { version = "0.5", default-features = false, features = ["html_reports"] }
       ```
       (Keep `html_reports` so developers get HTML summaries locally; criterion's default `rayon` feature is off because our invariant is single-thread — the bench process itself can still use threads but we don't need rayon auto-parallelism by default.)

    2. **crates/beava-core/Cargo.toml** — add under `[dev-dependencies]`:
       ```toml
       criterion = { workspace = true }
       ```
       Append bench targets (each with `harness = false`) for files plans 03/04 will create:
       ```toml
       [[bench]]
       name = "phase4_expr"
       harness = false

       [[bench]]
       name = "phase5_agg"
       harness = false
       ```
       Create empty placeholder files `crates/beava-core/benches/phase4_expr.rs` and `crates/beava-core/benches/phase5_agg.rs` with only:
       ```rust
       // Stub — populated by plan 05.5-03 / 05.5-04.
       fn main() {}
       ```
       This lets `cargo bench --workspace --no-run` compile today; downstream plans replace `main` with a `criterion_group!/criterion_main!` pair.

    3. **crates/beava-server/Cargo.toml** — add under `[dev-dependencies]`:
       ```toml
       criterion = { workspace = true }
       ```
       Append:
       ```toml
       [[bench]]
       name = "phase25_wire"
       harness = false
       ```
       Create stub `crates/beava-server/benches/phase25_wire.rs` with `fn main() {}` (plan 05.5-02 populates it).

    4. **.planning/perf-baselines.md** — create with this exact skeleton (D-03):
       ```markdown
       # Beava v2 — Performance Baselines

       **Created:** 2026-04-23 (Phase 5.5 plan 01)
       **Regression gates:** 10% slower than baseline in same hw-class = WARNING; 25% slower = BLOCKER. See CLAUDE.md §Performance Discipline.

       ## How to read this file

       Baselines are recorded per **hw-class**, not per machine. A hw-class is the tuple
       `(cpu-arch-family, OS family, core count bucket)` — e.g. `apple-m1-pro / darwin-24.3.0 / 10 cores`.
       Regression checks compare a new bench run against the same hw-class only.

       To capture a hw-class string on macOS:
       ```bash
       echo "$(sysctl -n machdep.cpu.brand_string | tr ' ' '-') / $(uname -sr | tr ' ' '-') / $(sysctl -n hw.ncpu) cores"
       ```

       On Linux:
       ```bash
       echo "$(lscpu | awk -F: '/Model name/ {print $2}' | xargs | tr ' ' '-') / $(uname -sr | tr ' ' '-') / $(nproc) cores"
       ```

       ## hw-class: <FILL AT PHASE-END>

       | Bench | Median (ns/iter or time) | Captured | Phase | Notes |
       |---|---|---|---|---|
       | _populated by plan 05.5-06_ |  |  |  |  |
       ```
       The literal string `## hw-class:` on a line satisfies Task 1.a assertion 4.

    5. **CLAUDE.md** — add, as a **new sub-section** under the existing `## Conventions` block (after `### TDD Discipline`, before the closing `<!-- GSD:conventions-end -->` marker):
       ```markdown
       ### Performance Discipline (regression gates, enforced Phase 6 onward)

       **Rule:** Every phase from Phase 6 onward ships at least one `criterion` microbench covering its hot path. Baselines live in `.planning/perf-baselines.md` keyed by hw-class. Verification compares current numbers to the same hw-class's prior baseline:

       - **10% slower** than prior baseline → WARNING in the phase's VERIFICATION.md (must be investigated before Phase 13)
       - **25% slower** than prior baseline → BLOCKER (phase verification fails; must fix or explicit override)

       **Scope:** measurement, not optimization. Phase 13 is the final ship gate (≥3M EPS/core, P99 < 10ms batch-get). Phase 5.5's benches are a regression tripwire, not a target.

       **Plan-checker contract:** plans for Phase 6+ must include at least one task whose `files_modified` contains a path under `crates/*/benches/` (or language-equivalent). Plans that don't are rejected.

       **Do NOT bench:** cold-path code (register compile, schema propagation at register-time), I/O paths that will change (WAL pre-Phase 6, snapshot pre-Phase 7), end-to-end network roundtrips (that's Phase 13).

       **Retroactive coverage landed in Phase 5.5:** Phase 2.5 frame codec, Phase 3 SDK REGISTER compile (Python), Phase 4 expr parse/eval/op-chain, Phase 5 AggOp::update / WindowedOp fold / apply_event_to_aggregations.
       ```
       Keep indentation + heading depth consistent with the existing `### TDD Discipline` block.

    6. Run `cargo fmt --all`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and `cargo build --workspace --benches` to confirm the stub benches compile.

    7. Run the 1.a smoke test and CONFIRM GREEN:
       ```bash
       cargo test -p beava-server --features testing phase5_5_harness_smoke -- --exact harness_scaffolding_present
       ```

    8. Commit with message `feat(05.5-01): wire criterion workspace harness + baselines + §performance discipline`.
  </action>
  <acceptance_criteria>
    - `cargo test -p beava-server --features testing phase5_5_harness_smoke` PASSES
    - `cargo build --workspace --benches` succeeds (no compile errors; stub benches have `fn main() {}`)
    - `cargo fmt --all --check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` both clean
    - `.planning/perf-baselines.md` exists and starts with `# Beava v2 — Performance Baselines`
    - CLAUDE.md diff contains `### Performance Discipline (regression gates, enforced Phase 6 onward)`
    - Commit message matches regex `^(feat|chore)\(05\.5-01\):`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cargo test --workspace` green (including the new `phase5_5_harness_smoke`)
- `cargo build --workspace --benches` green (stub benches compile; downstream plans flesh them out)
- `cargo fmt --all --check` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean
- `.planning/perf-baselines.md` committed, non-empty
- CLAUDE.md §Performance Discipline committed
</verification>

<success_criteria>
ROADMAP SC3 partially earned (§Performance Discipline codified; benches land in plans 02–05; baselines file populated in plan 06).
Scaffolding in place so plans 02–05 can add concrete benches with zero extra cargo/toml churn.
</success_criteria>

<output>
After completion, create `.planning/phases/05.5-perf-harness-retroactive-baselines/05.5-01-SUMMARY.md`.
</output>
