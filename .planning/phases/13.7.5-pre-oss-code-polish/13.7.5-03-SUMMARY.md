# Plan 13.7.5-03 — Comment hygiene `crates/beava-server/`

**Status:** PASS
**Wave:** 2
**Type:** doc-only (CLAUDE.md TDD §item #7 doc-only-plan exemption — single `chore:` commit)

## Salvage note

This plan's executor stalled on the 600s stream watchdog while reading `apply_shard.rs`. Per orchestrator review, the worktree's diff was confirmed safe: edits were comment-only plus a small set of `.expect()` panic-message scrubs (stripping `Phase X Plan Y` prefixes — consistent with the spirit of D-01's "phase/plan references in code" DELETE-pattern). Salvaged into this commit. Verification deferred to end-of-Wave-2 workspace sweep (`cargo test --workspace --features testing` + clippy + fmt + doc).

## What landed

14 files under `crates/beava-server/` audited per the D-01 KEEP/DELETE rubric. Net **+1162 / −1924** = **−762 lines** (largest comment-scrub of the wave; reflects the size of beava-server's pre-13.7.5 commentary footprint).

## Verification

- Patch applied cleanly to current HEAD
- `cargo check -p beava-server --all-targets --features testing`: deferred to end-of-Wave-2 sweep
- (`cargo check -p beava-server --all-targets` *without* `--features testing` fails with pre-existing `beava_server::testing` unresolved-import errors — orthogonal to Plan 03's comment-only diff)

## Preserved (KEEP-pattern)

- WHY-comments documenting the mio-only invariant (Phase 12.6 lock per CLAUDE.md)
- Busy-poll spin-loop reasoning, WAL-LSN ordering, bytes-pool / response-batch SAFETY notes
- Invariant-tripwire comments inside `phase12_6_mio_only_dataplane.rs`, `phase12_7_no_table_surface.rs`, `phase12_8_*.rs` test files
- `///` doc comments on public-API items

## Removed (DELETE-pattern)

- Phase/plan tags in module docstrings (`// Phase 12 ...`, `// Plan 12-07 ...`)
- Phase/plan prefixes inside `.expect()` / `assert!()` panic messages (e.g. `"opcode {op:#06x} (push_sync) is reserved for Phase 12 and not yet implemented"` → `"opcode {op:#06x} (push_sync) is not yet implemented"`)
- Section-marker comments
- Paraphrase trailing comments on wire-codec field reads (`// advances 2`, `// drop length prefix`, `// big-endian by default in 'bytes'`)
- AI-tell phrases
