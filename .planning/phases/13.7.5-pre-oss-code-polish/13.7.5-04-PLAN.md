---
phase: 13.7.5
plan: 04
type: execute
wave: 2
depends_on: [13.7.5-01]
files_modified:
  - crates/beava-runtime-core/src/**/*.rs
  - crates/beava-runtime-core/tests/**/*.rs
autonomous: true
requirements:
  - D-01
tags:
  - comment-audit
  - rust
  - beava-runtime-core

must_haves:
  truths:
    - "Every `.rs` file under `crates/beava-runtime-core/` has been audited against the D-01 KEEP/DELETE rubric."
    - "DELETE-pattern comments removed; KEEP-pattern preserved (especially: WHY-comments on the bytes_pool / work_ring SAFETY contracts, the io_thread / fsync_worker handoff invariants, and the http_listener / tcp_listener accept-loop choices)."
    - "`cargo test -p beava-runtime-core --features testing`, `cargo clippy -p beava-runtime-core --all-targets --all-features -- -D warnings`, `cargo fmt -p beava-runtime-core --check`, `cargo doc --no-deps -p beava-runtime-core` all stay green."
    - "No logic / structure changes."
  artifacts:
    - path: "crates/beava-runtime-core/src/"
      provides: "Cleaned hand-rolled mio runtime (router / wire_request / http_listener / tcp_listener / wal_buffer / wal_writer / fsync_worker / io_thread / bytes_pool / work_ring)"
  key_links:
    - from: "Plan 13.7.5-04 commit"
      to: "docs/_internal/comment-conventions.md"
      via: "Same rubric as Plans 02–03; SAFETY contracts on raw-pointer code are explicit KEEP"
---

<objective>
Apply the D-01 comment-audit rubric to every `.rs` file under `crates/beava-runtime-core/`. This is the hand-rolled mio runtime (no tokio on the data plane per `project_phase18_no_dual_runtime`). Files: `router.rs`, `wire_request.rs`, `http_listener.rs`, `tcp_listener.rs`, `wal_buffer.rs`, `wal_writer.rs`, `wal_lsn.rs`, `fsync_worker.rs`, `io_thread.rs`, `io_thread_worker.rs`, `bytes_pool.rs`, `work_ring.rs`, `event_loop.rs`, `client.rs`, `config.rs`, `response.rs`, `lib.rs`, plus `io_pool.rs` and `io_backend/`.

Purpose: This crate has the heaviest SAFETY-contract density (raw pointers in bytes_pool, lock-free SPSC in work_ring, three-buffer WAL state machine in wal_buffer). KEEP discipline is critical — accidentally deleting a `// SAFETY:` comment breaks `cargo audit`-style review.

Output: One PR-sized commit; all green.

Doc-only-plan exemption: single `chore(13.7.5-04): comment hygiene crates/beava-runtime-core` commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-CONTEXT.md
@docs/_internal/comment-conventions.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit `crates/beava-runtime-core/src/` against the D-01 rubric, KEEP-bias on SAFETY contracts</name>
  <files>crates/beava-runtime-core/src/**/*.rs</files>
  <read_first>
    - `docs/_internal/comment-conventions.md`.
    - `CLAUDE.md` §"mio-only Hot-Path Invariant" — runtime-core IS the mio runtime; WHY-comments documenting the no-tokio-data-plane choice are KEEP.
    - The memory `project_phase18_wal_architecture` guidance (referenced in CLAUDE.md memory list): four-watermark discipline (committed/written/synced/acked), 3-buffer state machine, single writer+fsync thread.
  </read_first>
  <action>
    Walk every `.rs` file under `crates/beava-runtime-core/src/` (use `find crates/beava-runtime-core/src -name '*.rs' -type f`). For each file:

    1. Read end-to-end.
    2. Classify each comment per the D-01 rubric. Apply this crate's KEEP-bias rules aggressively:
       - **`// SAFETY:` comments on `unsafe` blocks** — explicit KEEP (this is a Rust idiom; `cargo clippy` lints against unsafe blocks lacking SAFETY docs in some configs).
       - **WHY-comments on the four-watermark WAL invariants** in `wal_buffer.rs`, `wal_writer.rs`, `wal_lsn.rs`, `fsync_worker.rs`: KEEP — readers cannot infer the watermark ordering from field names alone.
       - **WHY-comments on the 3-buffer state machine** (active / sealed / flushing / free) in `wal_buffer.rs`: KEEP.
       - **WHY-comments on lock-free SPSC handoff** in `work_ring.rs`: KEEP.
       - **WHY-comments on busy-poll vs blocking choice** in `event_loop.rs`, `io_thread.rs`: KEEP.
       - **`O_APPEND` + refuse-on-network-FS** rationale in `wal_writer.rs`: KEEP.
       - **Ordering / Acquire / Release atomic-ordering rationale** in any file using `std::sync::atomic`: KEEP — these are quintessential WHY-comments.

    3. Apply this crate's DELETE-bias rules:
       - **Phase / plan references**: DELETE wholesale. `grep -rnE '// (Phase|Plan) [0-9]' crates/beava-runtime-core/src/` returns zero after.
       - **AI-tell phrases**: DELETE.
       - **Doc comments on private fns/structs that paraphrase the signature**: DELETE.
       - **Section markers**: DELETE.
       - **Closing-brace echoes**: DELETE.

    4. **Conservative when unsure on `//SAFETY` adjacent comments.** Even a comment that LOOKS like it might be paraphrase but sits inside an `unsafe { ... }` block deserves a second classification pass. If the comment is the only thing explaining what makes the unsafe operation valid, it stays — even if phrased as paraphrase.

    5. Remove orphaned blank lines.

    6. **No logic changes.** No re-ordering of code blocks. No symbol renames. No editing of `unsafe` blocks themselves — only comments adjacent.

    7. Single commit at end: `chore(13.7.5-04): comment hygiene crates/beava-runtime-core`.
  </action>
  <verify>
    <automated>cargo test -p beava-runtime-core --features testing 2>&amp;1 | tail -10 &amp;&amp; cargo clippy -p beava-runtime-core --all-targets --all-features -- -D warnings 2>&amp;1 | tail -5 &amp;&amp; cargo fmt -p beava-runtime-core --check &amp;&amp; cargo doc --no-deps -p beava-runtime-core 2>&amp;1 | tail -5 &amp;&amp; test "$(grep -rnE '// (Phase|Plan) [0-9]' crates/beava-runtime-core/src/ | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(grep -rnE '// (Note that|Importantly,|Here we |We then |It should be noted)' crates/beava-runtime-core/src/ | wc -l | tr -d ' ')" = "0" &amp;&amp; SAFETY_COUNT_AFTER=$(grep -rcE '^\s*// SAFETY:' crates/beava-runtime-core/src/ 2>/dev/null | awk -F: '{s+=$2} END {print s+0}') &amp;&amp; test "$SAFETY_COUNT_AFTER" -gt 0 &amp;&amp; echo "SAFETY count preserved: $SAFETY_COUNT_AFTER"</automated>
  </verify>
  <done>
    Every `.rs` file in `crates/beava-runtime-core/src/` audited; DELETE-pattern removed; SAFETY / atomic-ordering / WAL-watermark / 3-buffer-state-machine WHY-comments preserved. Tests + clippy + fmt + doc green. Zero phase-reference comments. Zero AI-tell comments. SAFETY-comment count > 0 (sanity check that we didn't delete them all).
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit `crates/beava-runtime-core/tests/` against the D-01 rubric</name>
  <files>crates/beava-runtime-core/tests/**/*.rs</files>
  <read_first>
    - `docs/_internal/comment-conventions.md`.
  </read_first>
  <action>
    Same procedure as Task 1, applied to the test tree. (No benches in this crate per current layout — confirm with `ls crates/beava-runtime-core/`; if `benches/` exists, scrub it too.)

    Combine commit with Task 1: single `chore(13.7.5-04): comment hygiene crates/beava-runtime-core` commit.
  </action>
  <verify>
    <automated>cargo test -p beava-runtime-core --features testing 2>&amp;1 | tail -5 &amp;&amp; test "$(grep -rnE '// (Phase|Plan) [0-9]' crates/beava-runtime-core/tests/ 2>/dev/null | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>
    Tests scrubbed alongside src. All `cargo test -p beava-runtime-core` runs pass. Single commit per crate.
  </done>
</task>

</tasks>

<verification>
- `cargo test --workspace --features testing` GREEN.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` GREEN.
- `cargo fmt --all --check` GREEN.
- `cargo doc --no-deps -p beava-runtime-core` GREEN.
- All `// SAFETY:` comments preserved (count >0).
- Phase / plan / AI-tell comment patterns return zero grep matches.
- Single commit `chore(13.7.5-04): comment hygiene crates/beava-runtime-core`.
</verification>

<success_criteria>
- Hand-rolled runtime reads as carefully-engineered code, not phase-stamped scaffolding.
- All SAFETY contracts preserved.
- Workspace builds clean; data-plane invariants hold.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-04-SUMMARY.md`.
</output>
