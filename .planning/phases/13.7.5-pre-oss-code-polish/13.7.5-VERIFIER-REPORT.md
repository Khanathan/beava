---
phase: 13.7.5-pre-oss-code-polish
verified: 2026-05-05T00:00:00Z
status: gaps_found
verdict: PASS-WITH-WARN
score: 5/6 must-haves verified
gaps:
  - truth: "D-01 comment audit complete on every `.rs` file under `crates/beava-core/` (per Plan 13.7.5-02 must_haves)"
    status: partial
    reason: "Plan 02 audited only 6 of 39 src files in beava-core; the remaining 33 files carry ~125 residual non-architectural-invariant Plan/Phase tag comments — explicit DELETE-pattern violations under D-01 + the conventions-doc 8th KEEP bullet. Salvage path acknowledged executor stalled mid-plan; the salvaged commit landed only the 6-file partial scrub. Plan 02's frontmatter must_have ‘Every .rs file under crates/beava-core/ has been read and audited against the D-01 KEEP/DELETE rubric’ is NOT satisfied."
    artifacts:
      - path: "crates/beava-core/src/agg_op.rs"
        issue: "35 Plan/Phase tag comments remain (`// Phase 1`, `// Phase 8`, `// Phase 9`, `// Plan 10-05`, `// Phase 19.3-A`, etc.)"
      - path: "crates/beava-core/src/agg_compile.rs"
        issue: "42 Plan/Phase tag comments remain (`// Plan 10-05`, `// Phase 11`, `// Phase 9`, etc.)"
      - path: "crates/beava-core/src/agg_state.rs"
        issue: "12 phase tag comments remain"
      - path: "crates/beava-core/src/agg_apply.rs"
        issue: "14 phase tag comments remain"
      - path: "crates/beava-core/src/register_validate.rs"
        issue: "14 plan/phase tag comments remain (incl. `/// Plan 05-06:`)"
      - path: "crates/beava-core/src/agg_state_table.rs"
        issue: "Multiple `/// Plan 18-11`, `/// Plan 18-16` doc-comment tags remain"
      - path: "crates/beava-core/src/op_chain.rs:100,103"
        issue: "Self-narration `// Phase 1:` / `// Phase 2:` section markers"
    missing:
      - "Audit + scrub the remaining 33 src files in `crates/beava-core/` for Plan/Phase tag DELETE-pattern violations"
      - "Either: (a) extend the conventions-doc 8th KEEP bullet to cover Plan 10-05 sketch-construction-params / Plan 18-11 SmallVec-storage / Plan 18-16 agg_id refs as additional named architectural invariants, OR (b) strip those tags from the affected files"
deferred: []
---

# Phase 13.7.5 Verifier Report

**Phase Goal:** Make the codebase read as engineered, not generated. (A) Per-component comment audit (D-01); (B) test-coverage matrix audit + MUST-FIX gap fill (D-03).

**Verifier:** goal-backward, codebase-evidence first. Started from "what must be TRUE for goal to be achieved" and walked back to artifacts, wiring, and behavioral spot-checks.

**Verdict:** **PASS-WITH-WARN**

D-03 + throughput-gate + architectural-invariants + workspace-gates fully achieved. D-01 partially achieved — beava-core comment scrub is incomplete; ~125 non-architectural-invariant Plan/Phase tag comments remain across 20 src files (out of 33 unscrubbed files in beava-core). The phase goal of "code that doesn't pattern-match as AI-slop" is partially compromised in beava-core's hot operator-catalogue files (`agg_op.rs`, `agg_compile.rs`, `agg_state*.rs`).

---

## Q1 — D-01 (comment audit) — PARTIAL

**Method:** Sampled 5 random source files across all 7 components for comment density (sane); ran the prompt's two greps for residual phase/plan tags + AI-tell phrases. Compared residual file list to Plans 02-08 declared scope.

### AI-tell phrases — CLEAN

```
grep -rnE '// (Note that|Importantly|Here we|We then|It should be noted)' crates/ python/beava/ examples/
```

**0 matches.** All AI-tell phrases scrubbed across all 7 components. ✓

### Residual Plan/Phase tags — NOT CLEAN in beava-core

```
grep -rcE '^\s*//\s*(Plan|Phase) [0-9]+' crates/beava-core/src/ crates/beava-server/src/ crates/beava-runtime-core/src/ crates/beava-persistence/src/ python/beava/ examples/
```

| Component | Files with residuals | Total residual tags | Plan claimed scope |
|---|---:|---:|---|
| `crates/beava-core/src/` | **20 of 39** | **~125** | Plan 02 must_have: "Every `.rs` file under crates/beava-core/" — only 6 files audited |
| `crates/beava-server/src/` | 2 of 18 | 11 | Plan 03 audited 14 of 18; 4 files unscrubbed (testing.rs, apply_shard.rs are partial — only some refs stripped) |
| `crates/beava-runtime-core/src/` | 0 | 0 | Plan 04 — CLEAN ✓ |
| `crates/beava-persistence/src/` | 0 | 0 | Plan 05 — CLEAN ✓ |
| `crates/beava-bench/src/` (and v2) | 0 | 0 | Plan 06 — CLEAN ✓ |
| `python/beava/` | 0 | 0 | Plan 07 — CLEAN ✓ |
| `examples/{python,ts,go}/` | 0 | 0 | Plan 08 — CLEAN ✓ |

### Top offenders in `crates/beava-core/src/`

```
crates/beava-core/src/agg_compile.rs:42  // 'Plan 10-05', 'Phase 11', etc.
crates/beava-core/src/agg_op.rs:35       // 'Phase 1', 'Phase 8', 'Phase 9', 'Plan 10-05'
crates/beava-core/src/register_validate.rs:14  // '/// Plan 05-06:'
crates/beava-core/src/agg_apply.rs:14
crates/beava-core/src/agg_state.rs:12
crates/beava-core/src/sketches/count_distinct.rs:4
crates/beava-core/src/op_chain.rs:4
... (20 files total)
```

### Sample residual lines (not architectural-invariant per the 8th KEEP bullet)

- `crates/beava-core/src/op_chain.rs:100`: `// Phase 1: schema propagation validates all ops.` (self-narration section marker; Phase 1 is not on the KEEP list)
- `crates/beava-core/src/agg_op.rs:539`: `// Phase 8 — point/ordinal` (section marker)
- `crates/beava-core/src/agg_compile.rs:98`: `/// Plan 10-05: sketch construction params parsed from JSON kwargs.` (doc comment with plan tag)
- `crates/beava-core/src/agg_descriptor.rs:45`: `/// Plan 18-16: agg_id is a stable u32 assigned at registration time...` (doc comment with plan tag — Phase 18 IS architectural KEEP, but per the 8th bullet only the lock-source phase numbers 12.6/12.7/12.8/18 are allowed; Plan 18-16 is a sub-plan ref that the rubric does NOT explicitly cover)

### Comment-density spot-check — sane

5 random files (rotation.rs, tcp_listener.rs, fsync_worker.rs, runtime_core_glue.rs, test/__init__.py): comment-to-code ratio reads as engineered. No over-deletion of legitimate WHY-comments observed. No section markers / closing-brace echoes in samples. Public-API `///` doc comments preserved on `pub` items.

### Q1 verdict: PARTIAL (WARN)

The **conventions-doc 8th KEEP bullet** explicitly KEEPs only architectural-invariant references documenting "Phase 12.6 mio-only / Phase 12.7 events-only / Phase 12.8 memory-governance / Phase 18 hand-rolled-runtime commitments." The residuals in beava-core are NOT in that allowlist (`Phase 1`, `Phase 8`, `Phase 9`, `Phase 11`, `Plan 10-05`, `Plan 05-06`, `Plan 18-11`, `Plan 18-16`, `Plan 22-04`, etc. — all DELETE-pattern under D-01).

Plan 02's salvage note (executor stalled at SIGKILL after 5+ parallel cargo builds) and SUMMARY ("6 files modified") confirm Plan 02 finished partial. The orchestrator-level SUMMARY tabulates "−165 lines" for beava-core, vastly under the comment-cruft footprint that a 39-file scrub would have produced (beava-server's 14-file scrub stripped −762 lines for comparison).

**This is a real Plan 02 must_have failure.** It does not block the phase-level goal completely (5 of 7 components are clean; AI-tell phrases are gone everywhere; D-03 + invariants + throughput-gate all pass), but the operator-catalogue hot files (`agg_op.rs`, `agg_compile.rs`, `agg_state*.rs`, `op_chain.rs`, `register_validate.rs`) are the most-read files in the public OSS surface — these are exactly where "doesn't pattern-match as AI-slop" matters most.

**Suggested closure path:** A focused follow-up plan in 13.7.6 (or pre-13.7.6 sweep) to scrub the 20 unscrubbed beava-core src files. ~125 deletions; pure hygiene; doc-only-plan exemption applies.

---

## Q2 — D-03 (coverage MUST-FIX bucket) — VERIFIED ✓

**Method:** Confirmed all 4 new test files exist; ran the 4-file pytest invocation from the prompt.

```
$ python -m pytest python/tests/v0/test_register_flags.py python/tests/v0/test_schema_evolution.py \
    python/tests/v0/test_url_dispatch.py python/tests/v0/test_reconnect.py -v
```

**Result:** 11 passed in 0.84s ✓

| MUST-FIX surface_id | Test | Status |
|---|---|---|
| SDK-APP-02-force | `test_register_flags.py::test_force_replaces_existing` | PASSED |
| SDK-APP-02-dry-run | `test_register_flags.py::test_dry_run_returns_diff_no_commit` | PASSED |
| SRV-REG-03-evolve (additive) | `test_schema_evolution.py::test_additive_succeeds` | PASSED |
| SRV-REG-03-evolve (destructive 409) | `test_schema_evolution.py::test_destructive_returns_409` | PASSED |
| SRV-REG-03-evolve (changed-field 409) | `test_schema_evolution.py::test_changed_field_type_returns_409` | PASSED |
| SDK-WIRE-03 (http) | `test_url_dispatch.py::test_http_url_picks_http_transport` | PASSED |
| SDK-WIRE-03 (tcp) | `test_url_dispatch.py::test_tcp_url_picks_tcp_transport` | PASSED |
| SDK-WIRE-03 (embed) | `test_url_dispatch.py::test_no_url_picks_embed_transport` | PASSED |
| SDK-WIRE-03 (unknown) | `test_url_dispatch.py::test_unknown_scheme_raises_value_error` | PASSED |
| SDK-APP-RECONNECT | `test_reconnect.py::test_transport_recovers_after_server_restart` | PASSED |
| SDK-APP-MAX-RETRIES-0 | `test_reconnect.py::test_max_retries_zero_surfaces_immediately` | PASSED |

**11/11 GREEN.** D-03 MUST-FIX bucket fully closed.

`COVERAGE-MATRIX.csv`: 79 lines (1 header + 78 data rows = 78 entries) — matches SUMMARY's "78 rows × 12 columns." `COVERAGE-GAPS.md` partitions correctly: 0 Rust gaps + 6 Python gaps; 5 DEFER routed to v0.1-deferrals.md.

---

## Q3 — Workspace gates — VERIFIED ✓

| Gate | Result |
|---|---|
| `cargo fmt --all --check` | GREEN (no output) ✓ |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | GREEN (cached, 0.38s) ✓ |
| `pytest python/tests/v0` | 100/100 passed in 13.12s ✓ |
| `mypy --strict beava` (in `python/`) | "Success: no issues found in 19 source files" ✓ |

The pre-13.7.5→post-13.7.5 v0 acceptance count claim (89 → 100) is consistent with the 11 new tests in Plan 11.

---

## Q4 — Architectural invariants — VERIFIED ✓

7 tripwire tests run + GREEN end-to-end:

| Tripwire | File | Test count | Status |
|---|---|---:|---|
| mio-only data plane (12.6) | `phase12_6_mio_only_dataplane.rs` | 3 | GREEN |
| legacy axum killed (12.6-07) | `phase12_6_legacy_axum_killed.rs` | 6 | GREEN |
| events-only / no-table-surface (12.7) | `phase12_7_no_table_surface.rs` | 3 | GREEN |
| legacy table handlers killed (12.7) | `phase12_7_legacy_table_handlers_killed.rs` | 6 | GREEN |
| cold-entity TTL eviction (12.8) | `phase12_8_cold_entity_eviction.rs` | 7 | GREEN |
| lifetime ops have bounds (12.8) | `phase12_8_lifetime_ops_have_bounds.rs` | 3 | GREEN |
| AggOp ≤ 80B per-entity cap (12.9) | `crates/beava-core/tests/per_entity_size_dump.rs::aggop_size_within_cap` | 1 | GREEN |

All 29 tripwire tests pass. No invariant violation introduced by 13.7.5.

---

## Q5 — Throughput regression-gate — VERIFIED ✓

Inspected `.planning/throughput-baselines.md` § Phase 13.7.5 (lines 2105-2145).

**Gate cell:** small/tcp 630,380 EPS median of 3 runs → −1.8% vs Phase 13.5.2 (641,935 EPS) → **PASS** (within ±10% gate band; far from 25% block threshold).

**fraud-team/tcp:** 84,290 median → −20.0% vs Phase 13.5 → **WARN (informational, non-gating).** Root-cause analysis in baselines doc is plausible:

1. Phase 13.7.5 is pure comment hygiene + test additions; zero hot-path semantics changes — verified independently here (Plans 02-08 are doc-only chore commits per CLAUDE.md TDD §item #7; Plan 09 wrote planning files; Plan 10 was no-op; Plan 11 added test files only).
2. fraud-team/tcp is the noisiest cell in the matrix historically (Phase 12.7 ±13%; Phase 12.8 ±13%; Phase 13.7.5 ±29%).
3. Single high run 93,246 EPS sits within post-12.9 / Phase 13.4 band (post-12.9 floor 92,825 EPS NOT superseded).
4. Doc explicitly TENTATIVE-flags the 75,861 EPS post-13.7.5 floor and routes quiescent re-measurement to Phase 13.8 ship-gate validation.

WARN is consistent with documented Apple-M4 dev-box variance; structural regression is implausible given the diff content. **Carry-forward correctly flagged in SUMMARY § Carrying forward.**

---

## Q6 — Phase 12 (cross-SDK conformance) deferral — VERIFIED ✓

`.planning/ideas/v0.1-deferrals.md` line 115 has new section `## Routed from Phase 13.7.5 coverage matrix (2026-05-04)` with 5 DEFER entries:

1. **SRV-API-05** — `POST /push-batch/{event_name}` (OP_PUSH_MANY=0x0012)
2. **WAL-RECOV-EDGE** — WAL replay correctness for every RecordType variant beyond happy path
3. **SNAPSHOT-UPGRADE** — Snapshot FORMAT_VERSION upgrade-path
4. **CROSS-SDK-CONFORM** — Cross-SDK per-op equivalence harness
5. **CROSS-SDK-FLOWS** — Cross-SDK conformance for force/dry_run/reset/schema-evolution flows

Each row has surface_id, surface_name, reason citing D-03, and proposed v0.1 home. Routing complete.

Plan 12 (cross-SDK conformance harness) is correctly OMITTED from commit history per D-03; closure-commit message confirms "Plan 12 cross-SDK conformance OMITTED per D-03 (deferred v0.1+)."

---

## Commit history sanity

```
$ git log --oneline 699fcf44^..HEAD
ba398de1 chore(13.7.5-13): close phase 13.7.5
fbe5e4ac test(13.7.5-11): python gap-fill — 4 test files, 11 cases
9716b3ee docs(13.7.5-10): Rust gap-fill no-op closure
3b4f4478 docs(13.7.5-09): coverage matrix + gap classification
c9e2137b chore(13.7.5-03): comment hygiene beava-server
ed1383ed chore(13.7.5-02): comment hygiene beava-core
664c8f3a chore(13.7.5-06): comment hygiene beava-bench
4eb1ce64 chore(13.7.5-05): comment hygiene beava-persistence
4dd3740f chore(13.7.5-04): comment hygiene beava-runtime-core
ec99d006 chore(13.7.5-07): comment hygiene python/beava
92903271 chore(13.7.5-08): comment hygiene examples
699fcf44 chore(13.7.5-01): comment-audit conventions doc (Wave 1 gate)
```

12 commits total (11 plan + 1 closure). Plan 12 omitted. Closure-commit message lists "12 plans / 13 commits" but actual is 11 plans + 1 closure = 12 commits — minor inconsistency in commit message; SUMMARY also says "12 plan commits + 1 closure commit" but the math is 11 + 1 = 12. Non-blocking; cosmetic.

No AI-attribution lines in commits (memory `feedback_no_ai_attribution_in_commits` honored). Conventional-commits prefixes correct (`chore` / `docs` / `test`).

---

## Summary verdict

**PASS-WITH-WARN.** Phase 13.7.5 delivered:

- ✓ D-03 MUST-FIX bucket fully closed (11/11 new tests GREEN; 4 new files; v0 suite 89→100)
- ✓ Throughput regression-gate sweep performed; gate cell (small/tcp) PASS at −1.8%; fraud-team/tcp WARN correctly classified informational/non-structural
- ✓ All 7 architectural-invariant tripwires GREEN (mio-only, events-only, memory-governance, AggOp 80B cap)
- ✓ All workspace gates GREEN (clippy, fmt, pytest, mypy strict)
- ✓ 5 DEFER items correctly routed to `.planning/ideas/v0.1-deferrals.md`
- ✓ Plans 04, 05, 06, 07, 08 (runtime-core, persistence, bench, python, examples) — comment audit fully clean per D-01
- ⚠ **D-01 PARTIAL on `crates/beava-core/`:** Plan 02 audited only 6 of 39 src files; ~125 non-architectural-invariant Plan/Phase tag comments remain across 20 unscrubbed files. The Plan 02 frontmatter must_have ("Every `.rs` file under `crates/beava-core/`") is NOT satisfied. The hot operator-catalogue files (`agg_op.rs:35 tags`, `agg_compile.rs:42 tags`, `agg_state*.rs`, `register_validate.rs:14 tags`, `op_chain.rs`) are the most-read OSS-surface files; these are precisely where "doesn't read as AI-slop" matters most.
- ⚠ **D-01 minor on `crates/beava-server/`:** Plan 03 audited 14 of 18 files; 11 residual tags across 2 files (testing.rs, apply_shard.rs) — minor.

The orchestrator's SUMMARY/VERIFICATION did NOT surface the beava-core scope-gap. Plan 02's salvage note acknowledged the executor stall but the orchestrator did not subsequently re-run a focused scrub on the unscrubbed 33 files. The aggregate "−1,658 lines comment cruft scrubbed" headline obscures that beava-core's −165 line delta is the smallest of the audited Rust crates despite being the second-largest crate by file count.

**Recommendation:** PASS the phase as-is for 13.7.6/13.8 unblocking. Schedule a tactical pre-13.8 follow-up (one `chore(13.7.5-X)` commit; doc-only-plan exemption applies; ~125 deletions across 20 files) before the v0 GA tag — the OSS surface should be clean. Alternatively: extend the conventions-doc 8th KEEP bullet to enumerate Plan 10-05 / Plan 18-11 / Plan 18-16 / Plan 05-06 / Plan 22-04 / Plan 04 as named architectural references (if the user accepts these as semantic-locks, not phase-tag noise) — would shrink the residual count substantially.

---

*Verified 2026-05-05 — Claude (gsd-verifier).*
