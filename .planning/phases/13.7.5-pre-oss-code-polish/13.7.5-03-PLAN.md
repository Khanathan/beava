---
phase: 13.7.5
plan: 03
type: execute
wave: 2
depends_on: [13.7.5-01]
files_modified:
  - crates/beava-server/src/**/*.rs
  - crates/beava-server/tests/**/*.rs
  - crates/beava-server/benches/**/*.rs
autonomous: true
requirements:
  - D-01
tags:
  - comment-audit
  - rust
  - beava-server

must_haves:
  truths:
    - "Every `.rs` file under `crates/beava-server/` has been audited against the D-01 KEEP/DELETE rubric."
    - "DELETE-pattern comments removed; KEEP-pattern preserved (especially: WHY-comments documenting mio-only invariant, busy-poll spin-loop reasoning, WAL-LSN ordering, and bytes-pool / response-batch SAFETY notes)."
    - "`cargo test -p beava-server --features testing`, `cargo clippy -p beava-server --all-targets --all-features -- -D warnings`, `cargo fmt -p beava-server --check`, `cargo doc --no-deps -p beava-server` all stay green."
    - "Architectural-invariant tests (`phase12_6_mio_only_dataplane.rs`, `phase12_7_no_table_surface.rs`, `phase12_8_*`) keep passing — invariant-tripwire comments inside those tests are KEEP-pattern (they explain WHY the assertion exists, not what it does)."
    - "No logic / structure changes. Diff is comment-only."
  artifacts:
    - path: "crates/beava-server/src/"
      provides: "Cleaned mio data plane + admin axum sidecar source"
  key_links:
    - from: "Plan 13.7.5-03 commit"
      to: "docs/_internal/comment-conventions.md"
      via: "Same rubric as Plan 02; mio hot-path WHY-comments are textbook KEEP examples"
---

<objective>
Apply the D-01 comment-audit rubric to every `.rs` file under `crates/beava-server/`. This is the mio data plane + axum admin sidecar — recently-rewritten code (Phase 12.6 / 12.7 / 12.8 / 12.9 / 13.4 / 13.4.1) most likely to carry phase-tag comments and AI-tell scaffolding.

Purpose: `crates/beava-server/` is the second-largest scrub target (~50 .rs files in src + tests + benches combined). Recent code → most phase references → highest DELETE yield.

Output: One PR-sized commit; all tests + lint + format + doc green.

Doc-only-plan exemption: single `chore(13.7.5-03): comment hygiene crates/beava-server` commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-CONTEXT.md
@docs/_internal/comment-conventions.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit `crates/beava-server/src/` against the D-01 rubric</name>
  <files>crates/beava-server/src/**/*.rs</files>
  <read_first>
    - `docs/_internal/comment-conventions.md` (rubric).
    - `CLAUDE.md` §"mio-only Hot-Path Invariant" — WHY-comments documenting this invariant are KEEP-pattern; they encode an architectural commitment a reader can't infer from the code alone.
  </read_first>
  <action>
    Walk every `.rs` file under `crates/beava-server/src/` (use `find crates/beava-server/src -name '*.rs' -type f` to enumerate). For each file:

    1. Read end-to-end.
    2. Classify each comment per the D-01 rubric (DELETE / KEEP).
    3. Apply this crate's specific KEEP-bias rules:
       - **mio-only invariant comments** in `apply_shard.rs`, `runtime_core_glue.rs`: KEEP. ("// data-plane entry; do not call from tokio context — see CLAUDE.md mio-only invariant" is a textbook WHY-comment.)
       - **`SAFETY:` blocks** in `bytes_pool.rs` (if any), `apply_shard.rs::dispatch_*` raw-pointer paths: explicit KEEP.
       - **busy-poll / spin-loop reasoning** in `event_loop.rs` (whatever it's called in this crate's runtime-core-glue setup): KEEP. ("we spin instead of blocking because mio's wake() costs ~3 µs per event and we expect tens of millions of events per second" is a WHY-comment.)
       - **WAL-LSN ordering invariants:** KEEP if the comment explains the four-watermark discipline (committed/written/synced/acked); DELETE if it just restates the field name.
    4. Apply this crate's specific DELETE-bias rules:
       - **Phase / plan references** in handler bodies: DELETE wholesale. `grep -rnE '// (Phase|Plan) [0-9]' crates/beava-server/src/` should return zero after.
       - **AI-tell phrases** (`// Note that ...`, `// Importantly, ...`, `// Here we ...`, `// We then ...`): DELETE. zero matches after.
       - **Section markers** in `apply_shard.rs::dispatch_one` and similar large match arms: DELETE. (`// === HTTP Push ===`, `// --- TCP Get ---` style.)
       - **Closing-brace echoes** in nested match arms: DELETE.
       - **Doc comments on private helper functions** (where `pub` is absent and the body is < 5 lines): DELETE.
    5. Remove orphaned blank lines. Collapse to canonical Rust spacing.
    6. **No logic changes.** No symbol renames. No re-ordering of match arms. No re-flowing of multi-line expressions beyond what `cargo fmt` does after blank-line collapse.

    Workflow:
    ```bash
    find crates/beava-server/src -name '*.rs' -type f | sort
    # Read each file; Edit comment-by-comment.
    # After ALL files done in src/: cargo test -p beava-server before moving to Task 2.
    ```
  </action>
  <verify>
    <automated>cargo test -p beava-server --features testing 2>&amp;1 | tail -10 &amp;&amp; cargo clippy -p beava-server --all-targets --all-features -- -D warnings 2>&amp;1 | tail -5 &amp;&amp; cargo fmt -p beava-server --check &amp;&amp; cargo doc --no-deps -p beava-server 2>&amp;1 | tail -5 &amp;&amp; test "$(grep -rnE '// (Phase|Plan) [0-9]' crates/beava-server/src/ | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(grep -rnE '// (Note that|Importantly,|Here we |We then |It should be noted)' crates/beava-server/src/ | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>
    Every `.rs` file in `crates/beava-server/src/` audited; DELETE-pattern removed; mio-only / SAFETY / busy-poll-reasoning KEEP-pattern preserved. Tests + clippy + fmt + doc green. Zero phase-reference comments. Zero AI-tell comments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit `crates/beava-server/tests/` (architectural-tripwire suite) and `benches/`</name>
  <files>crates/beava-server/tests/**/*.rs, crates/beava-server/benches/**/*.rs</files>
  <read_first>
    - `docs/_internal/comment-conventions.md`.
    - The architectural-test files `phase12_6_mio_only_dataplane.rs`, `phase12_7_no_table_surface.rs`, `phase12_8_lifetime_ops_have_bounds.rs` — these explain WHY a tripwire exists; their top-of-file rationale comment is **KEEP-pattern**.
  </read_first>
  <action>
    Same procedure as Task 1, applied to `crates/beava-server/tests/` and `crates/beava-server/benches/`.

    Special handling for architectural-test files:
    - The top-of-file `//!` rationale block in `phase12_6_mio_only_dataplane.rs` and similar invariant tripwires explains WHY the test exists ("revives a parallel data-plane runtime requires explicit user override + new ADR overturning project_phase18_no_dual_runtime"). This is a quintessential KEEP — the reader cannot infer from the assertion alone why the assertion exists.
    - Phase numbers IN THE FILENAME (`phase12_6_*`) are filename conventions, not comments — out of scope; do not rename files.
    - Phase-number references INSIDE comments inside those test files are still DELETE-pattern. The filename already encodes the phase. ("// Phase 12.6 — locked architectural commitment" → DELETE; the rationale stays without the phase tag.)

    Combine commit with Task 1: single `chore(13.7.5-03): comment hygiene crates/beava-server` commit covering src + tests + benches.
  </action>
  <verify>
    <automated>cargo test -p beava-server --features testing 2>&amp;1 | tail -10 &amp;&amp; test "$(grep -rnE '// (Phase|Plan) [0-9]' crates/beava-server/tests crates/beava-server/benches 2>/dev/null | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>
    Tests and benches scrubbed alongside src. All architectural-invariant tests still pass (mio-only / events-only / memory-governance tripwires green). Single commit per crate.
  </done>
</task>

</tasks>

<verification>
- `cargo test --workspace --features testing` GREEN.
- Architectural-invariant tests (`phase12_6_*`, `phase12_7_*`, `phase12_8_*`) GREEN.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` GREEN.
- `cargo fmt --all --check` GREEN.
- `cargo doc --no-deps -p beava-server` GREEN.
- Phase / plan / AI-tell comment patterns return zero grep matches in `crates/beava-server/src/` and `tests/`.
- Single commit `chore(13.7.5-03): comment hygiene crates/beava-server` covers all changes.
</verification>

<success_criteria>
- `crates/beava-server/` reads as engineered code, not phase-stamped scaffolding.
- mio-only / events-only / memory-governance invariants preserved + their WHY-comments preserved.
- Workspace builds clean.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-03-SUMMARY.md`.
</output>
