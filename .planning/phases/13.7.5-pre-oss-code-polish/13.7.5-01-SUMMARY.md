# Plan 13.7.5-01 — Comment-audit conventions doc

**Status:** PASS
**Commit:** (see closure)
**Wave:** 1 (gate)
**Type:** doc-only (CLAUDE.md TDD §item #7 doc-only-plan exemption — single `chore:` commit)

## What landed

`docs/_internal/comment-conventions.md` — operational rubric the seven Wave-2 component-audit plans (02–08) read before scrubbing comments. Codifies the D-01 (USER-LOCKED) KEEP/DELETE checklist verbatim from CONTEXT.md plus the 8th architectural-invariant KEEP bullet that protects WHY-comments documenting Phase 12.6 mio-only / Phase 12.7 events-only / Phase 12.8 memory-governance / Phase 18 hand-rolled-runtime commitments.

Sections:

- Heuristic (single-line CLAUDE.md heuristic at top)
- DELETE patterns (9 verbatim D-01 bullets)
- KEEP patterns (7 verbatim D-01 bullets + 8th architectural-invariant bullet)
- Verification recipe per component (Rust / Python / examples)
- How a Wave-2 executor applies this (5 steps)

## Verification

```bash
F=docs/_internal/comment-conventions.md
test -f $F
grep -q '^Heuristic (CLAUDE.md): \*\*default to no comments; only add when WHY is non-obvious.\*\*' $F
grep -q '^## DELETE patterns' $F
grep -q '^## KEEP patterns' $F
grep -q 'AI-tell phrases' $F
grep -q 'SAFETY' $F
grep -q 'Architectural-invariant WHY-comments documenting Phase 12.6 mio-only / Phase 12.7 events-only / Phase 12.8 memory-governance / Phase 18 hand-rolled-runtime' $F
```

All 7 assertions PASS.

## Unblocks

Wave 2 plans 13.7.5-02 through 13.7.5-08. Each component-audit plan reads this file's KEEP/DELETE rubric before applying the heuristic to its component sources.
