---
phase: 13.7.5
plan: 08
subsystem: examples (python + typescript + go)
tags:
  - comment-audit
  - examples
  - python
  - typescript
  - go
  - oss-polish
requirements: [D-01]
dependency_graph:
  requires:
    - 13.7.5-01 (comment-conventions doc)
  provides:
    - Cleaned reference SDK demos (3 verticals × 3 languages) with no AI-tells, no stale phase/plan refs
  affects:
    - examples/python/, examples/typescript/, examples/go/, examples/test_examples.sh
tech-stack:
  added: []
  patterns:
    - Top-of-file teaching docstring stays (KEEP-pattern: OSS user's first read)
    - Drop "Phase X" / "Plan X" / "Q2 locked answer" / "WARNING N fix" / "BLOCKER N" stale process refs
    - Drop AI-tell phrases ("Note that", "Importantly", "Here we", "We then")
    - Drop signature-paraphrase docstrings on private helpers
    - Drop closing-block echo comments (`} // end of for`)
key-files:
  created:
    - .planning/phases/13.7.5-pre-oss-code-polish/13.7.5-08-SUMMARY.md
  modified:
    - examples/python/_mock.py
    - examples/python/adtech.py
    - examples/python/ecommerce.py
    - examples/python/fraud.py
    - examples/typescript/_mock.ts
    - examples/typescript/adtech.ts
    - examples/typescript/ecommerce.ts
    - examples/typescript/fraud.ts
    - examples/go/_mock.go
    - examples/go/adtech.go
    - examples/go/ecommerce.go
    - examples/go/fraud.go
    - examples/test_examples.sh
decisions:
  - "Top-of-file demo docstrings stay -- they're the OSS user's first read. Phase/plan refs inside them get rewritten to clean WHY-this-demo-exists narratives."
  - "`_mock.py` / `_mock.ts` / `_mock.go` keep their public-class docstrings (drop-in factories users see), drop signature-paraphrase docstrings on private helpers."
  - "`test_examples.sh` keeps the hard-fail-on-missing-toolchain rationale (genuine WHY-content) but drops 'WARNING 7 fix' / 'Phase 13.0' / 'D-03 closure plan 13.0-15' stale process refs."
  - "Go `_mock.go` keeps the `//go:build exclude` rationale + the inlined-mockApp trade-off explanation -- drops 'WARNING 8 fix (Option A)' / 'Q2 locked answer' / 'BLOCKER 4 checker fix' process artifacts."
  - "All comments labelling workflow phases (`# Demo helpers`, `// Demo body.`, `# 5 event types mirror fraud-team.json`) STAY -- per CONTEXT planner rule, tutorial-flow phase markers help OSS readers pattern-match against docs."
metrics:
  duration: ~22 min
  completed: 2026-05-05T21:27:52Z
  files_modified: 13
  commits: 1
---

# Phase 13.7.5 Plan 08: Comment hygiene — examples/{python,typescript,go} Summary

Applied the D-01 KEEP/DELETE rubric to all 12 demo source files plus `examples/test_examples.sh`. Removed AI-tell phrases, stale phase/plan references, signature-paraphrase docstrings on private helpers, and process artifacts (WARNING 7/8, BLOCKER 4, Q2 locked answer). Top-of-file teaching docstrings rewritten as clean WHY-this-demo-exists narratives — KEEP-bias preserved per plan rule (OSS users hit these first). All 9/9 verticals pass `bash examples/test_examples.sh`. Net: 117 insertions / 177 deletions.

## What Changed

### Python (`examples/python/`)

- **`_mock.py`** — top docstring rewritten (drop "Phase 13.0/13.5", "Q2 locked answer + BLOCKER 4 checker fix"). `_AggSpec` / `_Descriptor` private-class docstrings removed (paraphrased the constructor signature). Inline body comments kept where they explained WHY the alternate-shape branch exists ("tolerate alternate descriptor shapes a real SDK may produce") and why the no-op branch exists. Class-level docstrings on public `MockApp`, `App` factory, `table` helper kept (public surface).
- **`adtech.py`, `ecommerce.py`** — top docstring rewritten (drop "Phase 13.0/13.5"). Body comments paraphrasing the next line ("# Push events. Each push goes through MockApp's update logic.", "# Query — computed values, not pre-seeded.", "# Register an event source + a table aggregation.") removed; assertions stand alone. The `# 1 + 2 + 1 + 3` arithmetic-WHY note in ecommerce kept (it explains the magic 7).
- **`fraud.py`** — top docstring rewritten (drop "Phase 13.0/13.5/13.6"). The `# NOTE: ... shown here for shape but the mock no-ops them` block kept and tightened — this WHY-comment is critical because the registered ops (`n_unique`, `quantile`, `geo_velocity`) wouldn't otherwise be explained. The 5 event-type / single-key / "10 transactions for 'alice'" line-comments kept (workflow markers).

### TypeScript (`examples/typescript/`)

- **`_mock.ts`** — top docstring rewritten (drop "Phase 13.0/13.6 + Q2/BLOCKER 4 checker fix"). The `// no-op` body comment on `close()` removed (paraphrased the empty body). `BeavaApp` extends-class JSDoc tightened to a single-line drop-in factory note.
- **`adtech.ts`, `ecommerce.ts`** — top JSDoc rewritten (drop "Phase 13.0/13.6"). Body comment `// Assertions on COMPUTED values.` removed (paraphrased what the next 7 lines do).
- **`fraud.ts`** — top JSDoc rewritten (drop "Phase 13.0/13.6 re-verifies"). Inline `// mock no-op` markers on the three sketch/geo ops kept (genuine WHY-this-line-doesn't-assert info).

### Go (`examples/go/`)

- **`_mock.go`** — top doc-block rewritten (drop "Phase 13.0/13.6 + WARNING 8 fix (Option A) + Q2 locked answer + BLOCKER 4 checker fix"). The `//go:build exclude` rationale + inlined-mockApp trade-off explanation kept (genuine architectural WHY). Method-level Godoc on exported `Register/Push/Get/BatchGet/Reset/Ping/Close` kept (these render on `pkg.go.dev` if the example package is published).
- **`adtech.go`, `ecommerce.go`, `fraud.go`** — top doc-blocks rewritten. The `// --- Demo body.` section markers kept as `// Demo body.` single-line markers (workflow phase marker, useful for an OSS reader scanning the file). The `// --- Inline mockApp (~30 LOC, duplicated...)` rewritten to a clean WHY-note in `adtech.go` and `// Inline mockApp.` minimal markers in the other two (they reference `_mock.go` for canonical detail). The `// Assertions on COMPUTED values.` paraphrase comment removed in `ecommerce.go` / `fraud.go`. The `// n_unique / quantile / geo_velocity are real-engine ops...` WHY-block in `fraud.go` kept (same critical-explanation pattern as the Python/TS counterparts).

### `examples/test_examples.sh`

- Top doc-block rewritten to drop "Phase 13.0", "WARNING 7 fix", "D-03 integration-regression coverage", "13.0 closure", "13.0-15" stale process refs.
- The hard-fail-on-missing-toolchain rationale kept (genuine WHY-content explaining why silent skip would be wrong).
- The `# Pre-flight: assert all toolchains are present (HARD FAIL on missing).` body comment kept and lower-cased ("hard-fail on missing") since the script's stderr message + the comment-block above already document the contract.

## What Stayed (KEEP-pattern preserved)

- **Top-of-file demo docstrings** — every demo (`adtech.py`/`.ts`/`.go`, `ecommerce.*`, `fraud.*`) keeps a 3-7 line use-case + run-instructions docstring. OSS users hit these first.
- **`fraud.*` "real-engine ops shown for shape" WHY-comment** — explains why `n_unique`/`quantile`/`geo_velocity` appear in the registered ops dict but aren't asserted. Critical reader-context.
- **`_mock.py` "Tolerate alternate descriptor shapes" branch comment** — non-obvious; explains why the `else` branch is needed.
- **Public-class docstrings on `MockApp` / `App` / `BeavaApp` / `_mock.go::MockApp`** — these are public-facing API.
- **Workflow-phase markers** (`// Demo body.`, `# 5 event types mirror fraud-team.json`, `# 10 transactions for 'alice'`) — help OSS readers index against the docs.
- **Adtech/ecommerce demo workload comments** explaining the demo's domain choices.
- **`test_examples.sh` exit-code table** in the header.

## Verification

All gates GREEN:

```
$ bash examples/test_examples.sh
PASS  python/adtech.py
PASS  python/fraud.py
PASS  python/ecommerce.py
PASS  typescript/adtech.ts
PASS  typescript/fraud.ts
PASS  typescript/ecommerce.ts
PASS  go/adtech.go
PASS  go/fraud.go
PASS  go/ecommerce.go

OK -- all 9 demos passed (3 langs x 3 verticals)
```

Grep gates per task `<verify>`:

```
$ grep -rnE '# (Phase|Plan) [0-9]' examples/python/ --include='*.py'   → 0 matches
$ grep -rnE '# (Note that|Importantly,|Here we |We then |...)' examples/python/ --include='*.py' → 0 matches
$ grep -rnE '// (Phase|Plan) [0-9]' examples/typescript/ --include='*.ts' → 0 matches
$ grep -rnE '// (Note that|Importantly,|...)' examples/typescript/ --include='*.ts' → 0 matches
$ grep -rnE '// (Phase|Plan) [0-9]' examples/go/ --include='*.go' → 0 matches
$ grep -rnE '// (Note that|Importantly,|...)' examples/go/ --include='*.go' → 0 matches
```

Sweep including docstrings/comment-blocks (not just `# `/`// ` line comments):

```
$ grep -rnE '(Phase|Plan) [0-9]' examples/python/ examples/typescript/ examples/go/ → 0 matches
$ grep -rnE '(Note that|Importantly,|Here we |We then |It should be noted)' examples/python/ examples/typescript/ examples/go/ → 0 matches
```

Per-language sanity:

```
$ cd examples/python && python3 -c "import _mock; import adtech; import ecommerce; import fraud"   → clean import
$ cd examples/go && for f in adtech.go ecommerce.go fraud.go; do go vet "$f"; done                 → clean vet (3/3)
$ tsc --noEmit                                                                                     → skipped (no tsconfig present); covered by tsx via test_examples.sh smoke
```

## Deviations from Plan

None — plan executed exactly as written.

The plan's task `<verify>` blocks specified line-comment grep patterns (`# (Phase...|Plan...)` etc.); I additionally swept docstrings + comment blocks for the same patterns to catch the multi-line headers, since most of the AI-slop in this corpus lived inside `"""..."""` / `/** ... */` blocks rather than `# `/`// ` line comments. Both grep classes return zero matches.

## Self-Check

- examples/python/_mock.py: FOUND
- examples/python/adtech.py: FOUND
- examples/python/ecommerce.py: FOUND
- examples/python/fraud.py: FOUND
- examples/typescript/_mock.ts: FOUND
- examples/typescript/adtech.ts: FOUND
- examples/typescript/ecommerce.ts: FOUND
- examples/typescript/fraud.ts: FOUND
- examples/go/_mock.go: FOUND
- examples/go/adtech.go: FOUND
- examples/go/ecommerce.go: FOUND
- examples/go/fraud.go: FOUND
- examples/test_examples.sh: FOUND
- 9/9 demo smoke: PASS
- Phase/plan ref grep: 0 matches in scope
- AI-tell ref grep: 0 matches in scope

## Self-Check: PASSED
