---
phase: 13.7.5
phase_name: Pre-OSS code polish — comment audit + test coverage
captured: 2026-05-04
mode: batch-discuss (4-phase block 13.5.1 + 13.7.5 + 13.7.6 + 13.8)
prior_phase: 13.5.1 (transport impl + decorator hardening; greens 68 acceptance tests; clean code surface for 13.7.5 audit)
blocks: 13.7.6 (lint sweep runs cleaner after comment audit) → 13.8 GA
---

<domain>
Make the codebase read as engineered, not generated. Two workstreams: (A) per-component comment audit retroactively applying the CLAUDE.md heuristic ("default to no comments; only add when WHY is non-obvious"); (B) test-coverage matrix audit + gap fill for everything on the v0 ship-pitch surface.

Outcome: code that doesn't pattern-match as AI-slop on first read; a comprehensive test inventory before the v0 GA tag.
</domain>

<phase_boundary>
**In scope:**
- Codify the comment-audit KEEP/DELETE checklist as a one-page conventions doc (Plan 13.7.5-01).
- Component-by-component comment scrub on 7 components (Rust crates + Python SDK + examples).
- Coverage matrix CSV (53 ops × 8 dimensions; wire endpoints; register flags; architectural invariants).
- Gap-fill for MUST-FIX classifications.
- No logic / structure changes — pure hygiene + test additions.

**Out of scope:**
- Performance benchmarking — `crates/beava-bench` already covers it; not a coverage concern.
- Security audit — Phase 13.7.6 owns (`/cso` skill or dedicated workstream).
- Doc-style audit (voice consistency on docs site) — Phase 13.7 owned site quality.
- Refactoring beyond comment removal.
- New operators / wire endpoints — scope creep; v0.1+.
- Cross-SDK per-op conformance — DEFER per 7.5-C.
- WAL replay edge cases beyond happy path — DEFER per 7.5-C.
- Snapshot upgrade-path testing — DEFER per 7.5-C.
</phase_boundary>

<decisions>

### D-01 (7.5-A) — Comment-audit KEEP/DELETE checklist locked verbatim from idea doc (USER-LOCKED)

**DELETE patterns (executor agents reject these):**
- Paraphrasing what the code does (`// loop over users`, `// initialize state`)
- Doc comments on every private function (Rust `///` is for **public** API only — not on every helper)
- Section markers (`// === Setup ===`, `// === Cleanup ===`, `// ───────────────`)
- Self-narration sequences (`// First, we ... // Then, we ... // Finally, we ...`)
- Restating type signatures in prose (`/// fn add(a: i32, b: i32) -> i32 — adds two integers`)
- Phase / plan / task references in code (`// added in Phase 13.5 for ...`, `// Plan 12-09 …`)
- AI-tell phrases: "Note that ...", "Importantly, ...", "Here we ...", "We then ...", "It should be noted that ..."
- Multi-paragraph docstrings on a 5-line function
- Closing braces echoing the opening (`} // end of for`, `} // matches if x above`)

**KEEP patterns (executor agents preserve these):**
- Explains WHY a non-obvious constraint exists (`// must be u32 — Python struct lib only handles u32 here`)
- Workaround for a specific bug (link the issue / commit / phase number)
- Invariant a reader can't infer from the code (`// guaranteed > 0 by validate_input in caller`)
- `SAFETY` contracts on `unsafe` blocks (Rust idiom)
- Doc comments on PUBLIC lib/SDK exports (cargo doc / Sphinx render targets)
- Hidden-state warnings (`// mutates self.state while iterating — careful`)
- Single-line "why this isn't obvious" notes

Codified as Plan 13.7.5-01 (`docs/_internal/comment-conventions.md` or equivalent — final path is planner's discretion).

### D-02 (7.5-B) — Per-component plan ordering locked (USER-LOCKED)

**Wave 1 (serial — gate):**
- 13.7.5-01 — Comment-audit conventions doc

**Wave 2 (parallel × 7 — independent components):**
- 13.7.5-02 — `crates/beava-core/` (most stable; biggest surface; biggest payoff)
- 13.7.5-03 — `crates/beava-server/` (mio data plane; recent code; likely heavier comments)
- 13.7.5-04 — `crates/beava-runtime-core/` (router / wire_request / http_listener)
- 13.7.5-05 — `crates/beava-persistence/` (WAL + snapshot)
- 13.7.5-06 — `crates/beava-bench/` + `beava-bench-v2/`
- 13.7.5-07 — `python/beava/` (post-13.5/13.5.1 rewrite scrub)
- 13.7.5-08 — `examples/{python,typescript,go}/`

**Process per component plan:**
1. Read every `.rs` / `.py` / `.ts` / `.go` in scope.
2. Apply the D-01 heuristic to each comment (KEEP / DELETE).
3. Edit in place — keep commits small (one PR-sized commit per crate).
4. Run `cargo test --workspace` (or pytest equivalent) after — must stay green.
5. Verify `cargo doc` still renders for Rust crates (public API doc comments preserved).
6. NO logic changes — pure comment hygiene.

**Wave 2 parallelism:** 7 plans run in independent worktrees (no shared file conflicts). Each plan = one PR-sized commit. Worktree-init bug (initialize-from-stale-branch) is a known anti-pattern (`.continue-here.md`); each agent prompt must include the ancestry-check + `git reset --hard v2/greenfield` snippet.

**Wave 3 (serial — depends on Wave 2):**
- 13.7.5-09 — Coverage matrix + gap analysis (consumes audited code).
- 13.7.5-10 — Fill Rust gaps.
- 13.7.5-11 — Fill Python gaps.
- 13.7.5-12 — (REMOVED — cross-SDK conformance harness DEFERRED per D-03 below).
- 13.7.5-13 — Closure (SUMMARY + VERIFICATION + STATE/ROADMAP advance).

**Total: 12 plans** (down from idea-doc's 13 — Plan 12 cross-SDK conformance moved to v0.1+ deferrals per D-03 narrowing).

### D-03 (7.5-C) — Coverage-matrix MUST-FIX bar = v0 ship-pitch surface (USER-LOCKED)

**MUST-FIX (blocks v0 launch — Plans 13.7.5-10/11 fill gaps):**
- `force=True` register flow — Python integration test
- `dry_run=True` register flow — Python integration test
- `App.reset()` flow — Python integration test (against test_mode-gated server)
- Schema-evolution diff matrix (additive vs destructive) — Python integration test
- URL-scheme dispatch (`http://` vs `tcp://` vs no-URL embed) — Python integration test
- Connection drop + auto-reconnect — Python integration test (transient error path)
- `max_retries=0` default behavior — Python integration test
- Every one of the 53 ops has at least ONE direct test (Rust unit OR Python integration; **NOT** "covered" via architectural test alone)

**DEFER to v0.1+:**
- WAL replay correctness for every `RecordType` variant (happy path covered; edge cases v0.1+).
- Snapshot upgrade path / `FORMAT_VERSION` mismatch handling.
- Cross-SDK per-op equivalence harness (Plan 13.7.5-12 removed; tracked in `.planning/ideas/v0.1-deferrals.md`).
- Cross-SDK conformance for force/dry_run/reset/schema-evolution flows (Python is the canonical reference; TS/Go inherit shared `docs/sdk-api/shared.md` semantics; per-feature equivalence v0.1+).

**Coverage matrix shape (Plan 13.7.5-09 builds; CSV format):**

| Feature/Op/Endpoint | Wire spec | Doc page | JSON example | Rust unit test | Architectural test | Python integration | Demo | Verdict |
|---|---|---|---|---|---|---|---|---|
| bv.count | ✓ | ✓ | ✓ | ? | ✓ | ✓ | ✓ | MUST-FIX if Rust-unit ✗ |
| bv.sum | ✓ | ✓ | ✓ | ? | ✓ | ✓ | ✓ | MUST-FIX if Rust-unit ✗ |
| ... (53 ops) | | | | | | | | |
| OP_REGISTER | ✓ | ✓ | ✓ | ✓ | — | indirect | ✓ | covered |
| force=True | ✓ | ✓ | ✓ | ? | — | ✗ (gap) | ✗ | MUST-FIX |
| dry_run=True | ✓ | ✓ | ? | ? | — | ✗ (gap) | ✗ | MUST-FIX |
| Cold-entity TTL eviction | ✓ | ✓ | — | — | ✓ (12.8) | ? | — | DEFER if covered |
| WAL recovery | — | ✓ | — | ? | ✓ | ✗ (Python doesn't replay) | — | DEFER |

For each `✗` gap → classify MUST-FIX (D-03 list above) vs DEFER (everything else).

</decisions>

<carrying_forward>
- **CLAUDE.md heuristic** ("default to no comments; only add when WHY is non-obvious") — codified in D-01.
- **Phase 13.5.1 pre-cleanup** — Phase 13.5.1 ships clean code surface for python/beava/ comment audit (Plan 13.7.5-07).
- **TDD discipline (CLAUDE.md §Conventions)** — comment-audit plans are per CLAUDE.md TDD §Note 4 doc-only-plan exemption: single `chore(13.7.5-NN): comment hygiene <component>` commit per plan (no red-then-green required for pure hygiene). Coverage gap-fill plans (10/11) DO require red-then-green per task.
- **Perf gates (CLAUDE.md §Performance Discipline)** — comment audit doesn't change runtime behavior; no microbench / throughput-run obligation. Closure plan VERIFICATION notes the doc-only-phase exemption.
- **Architectural invariants** — mio-only (12.6), events-only (12.7), memory-governance (12.8) tripwires are part of the matrix as ✓-covered rows. New gap-fill tests must not violate any.
- **No AI attribution in commits** (memory `feedback_no_ai_attribution_in_commits`) — applies to every commit landed by this phase.
</carrying_forward>

<canonical_refs>
- `.planning/ROADMAP.md` line 61 — Phase 13.7.5 entry
- `.planning/ideas/phase-13.7.5-pre-oss-polish.md` — original capture (this CONTEXT supersedes for execution; idea doc retained for narrative)
- `CLAUDE.md` § Conventions (TDD discipline, comment heuristic, perf gates)
- `docs/operators/index.md` (Plan 13.0) — 53-op catalog (Plan 09 cross-references)
- `docs/wire-spec.md` — 6 endpoint coverage rows in the matrix
- `docs/error-codes.md` — exception hierarchy (matrix coverage check)
- `docs/sdk-api/python.md` — Python SDK contract
- `crates/beava-core/`, `crates/beava-server/`, `crates/beava-runtime-core/`, `crates/beava-persistence/`, `crates/beava-bench/` — audit scope per Plans 02-06
- `python/beava/` — audit scope per Plan 07
- `examples/{python,typescript,go}/` — audit scope per Plan 08
- `crates/beava-server/tests/phase12_*.rs` — architectural-invariant tripwires (matrix ✓ rows)
- `python/tests/v0/*.py` (Plan 13.0-16) — 68 acceptance tests; Plan 13.7.5-09 audits whether they cover everything in D-03 MUST-FIX
- `.planning/ideas/v0.1-deferrals.md` — destination for DEFER classifications

</canonical_refs>

<deferred>
- Cross-SDK conformance harness (per-op equivalence Python vs TS vs Go) — v0.1+; tracked in v0.1-deferrals.md.
- Cross-SDK conformance for force/dry_run/reset/schema-evolution — v0.1+ (Python is canonical reference for v0).
- WAL replay correctness for every `RecordType` variant beyond happy path — v0.1+.
- Snapshot `FORMAT_VERSION` upgrade-path testing — v0.1+.
- Doc-style audit (voice consistency across `docs/`) — v0.1 if needed; Phase 13.7 owned ship-quality.
- Performance benchmark expansion — `crates/beava-bench` covers v0 needs; v0.1+ for new pipelines.
- Refactoring beyond comment removal — out of scope.
</deferred>

<estimate>
~12 plans across 3 waves; ~1-2 weeks wall-clock with parallelism on Wave 2.

- **Wave 1 serial:** 13.7.5-01 conventions doc (~0.5d).
- **Wave 2 parallel × 7:** Plans 02-08 component audits (~1-3d each in parallel worktrees; total wall-clock ~3d if 7 worktrees).
- **Wave 3 serial:** Plan 09 matrix (~1d) → Plans 10/11 gap fill (~2-3d combined; depends on matrix output) → Plan 13 closure (~0.5d).

Plan numbering finalised by planner agent. Plan 12 (cross-SDK conformance) intentionally omitted (deferred per D-03).

Plan-checker contract for this phase:
1. Plans 02-08 (comment audit): no logic changes; `cargo test --workspace --features testing` + `cargo clippy -D warnings` + `cargo fmt --check` + `cargo doc` GREEN after each plan.
2. Plans 10/11 (gap fill): red-then-green per task per CLAUDE.md TDD §Conventions; new tests must use real engine (no MagicMock per 13.5.1 anti-pattern).
3. Closure VERIFICATION: every D-03 MUST-FIX row in the matrix has a covering test.
</estimate>

---

*Phase 13.7.5 — batch-discuss 2026-05-04 alongside 13.5.1 + 13.7.6 + 13.8. 3 user-locked decisions. Resume next: `/gsd-plan-phase 13.7.5` (after 13.5.1 lands, since 13.5.1 changes python/beava/).*
