---
phase: 13.7.5
plan: 06
type: execute
wave: 2
depends_on: [13.7.5-01]
files_modified:
  - crates/beava-bench/src/**/*.rs
  - crates/beava-bench/tests/**/*.rs
  - crates/beava-bench/benches/**/*.rs
  - crates/beava-bench/configs/**/*.json
  - crates/beava-bench/scripts/**/*.sh
autonomous: true
requirements:
  - D-01
tags:
  - comment-audit
  - rust
  - beava-bench

must_haves:
  truths:
    - "Every `.rs` file under `crates/beava-bench/` (which houses both the legacy `beava-bench` binary AND the new `beava-bench-v2` binary, per Cargo.toml's `[[bin]]` section) has been audited against the D-01 KEEP/DELETE rubric."
    - "DELETE-pattern comments removed; KEEP-pattern preserved (especially: WHY-comments on workload-shape choices like zipfian alpha=1.0, hw-class detection, and fsync-bottleneck explanations on macOS)."
    - "Architectural-invariant WHY-comments documenting Phase 12.6 mio-only / Phase 12.7 events-only / Phase 12.8 memory-governance / Phase 18 hand-rolled-runtime are preserved per the conventions doc's 8th KEEP bullet — the phase-number reference inside such comments is allowed (not DELETE) because it points to the canonical lock-source."
    - "`cargo test -p beava-bench --features testing`, `cargo clippy -p beava-bench --all-targets --all-features -- -D warnings`, `cargo fmt -p beava-bench --check`, `cargo doc --no-deps -p beava-bench` all stay green."
    - "Both binaries (`beava-bench` and `beava-bench-v2`) still build and run their `--help` flag."
    - "No logic / structure changes."
  artifacts:
    - path: "crates/beava-bench/src/"
      provides: "Cleaned throughput harness + bench binaries"
  key_links:
    - from: "Plan 13.7.5-06 commit"
      to: "docs/_internal/comment-conventions.md"
      via: "Same rubric; workload-shape rationale comments are KEEP — readers can't infer choice without context"
---

<objective>
Apply the D-01 comment-audit rubric to every `.rs` file under `crates/beava-bench/`. This crate houses two binaries (`beava-bench` and `beava-bench-v2`, both declared in `crates/beava-bench/Cargo.toml`) plus the harness library. CONTEXT.md D-02 lists this as the `crates/beava-bench/` + `beava-bench-v2/` row — but `beava-bench-v2` is a binary inside this crate (verified by `grep -n "name = \"beava-bench-v2\"" crates/beava-bench/Cargo.toml`), not a separate crate. Single audit covers both.

Purpose: Bench code routinely accumulates "// 60s wall-time, count events, not events to process — see THROUGHPUT-WORKLOAD-01" style WHY-comments — those are KEEP. Phase / plan stamps from the iterative bench tuning (12.6 → 13.4.1) are explicit DELETE EXCEPT for the architectural-invariant references called out in the KEEP-bias section below.

Output: One PR-sized commit; all green.

Doc-only-plan exemption per **CLAUDE.md §TDD Discipline item #7**: single `chore(13.7.5-06): comment hygiene crates/beava-bench` commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-CONTEXT.md
@docs/_internal/comment-conventions.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit `crates/beava-bench/src/` (library + both binaries) against the D-01 rubric, KEEP-bias on workload-shape rationale + architectural invariants</name>
  <files>crates/beava-bench/src/**/*.rs</files>
  <read_first>
    - `docs/_internal/comment-conventions.md` (the rubric — pay particular attention to the 8th architectural-invariant KEEP bullet).
    - `crates/beava-bench/Cargo.toml` — note the two `[[bin]]` entries (`beava-bench` at `src/bin/beava-bench.rs` if present, and `beava-bench-v2` at `src/bin/beava-bench-v2.rs`). Both are in scope.
    - `CLAUDE.md` §"mio-only Hot-Path Invariant" + §"Events-Only Invariant" + §"End-to-end throughput regression contract" — context for the architectural-invariant KEEP-bias rules below.
  </read_first>
  <action>
    **Architectural-invariant KEEP-bias for `crates/beava-bench` (mirrors the per-crate KEEP-bias sections in Plans 02 / 03 / 04 / 05):**

    `beava-bench` is the throughput-harness crate; the relevant in-scope architectural invariants are:

    - **Phase 12.6 mio-only data plane** — bench client code that connects to the mio-only data-plane port (the non-admin transport entry) MAY carry WHY-comments referencing the mio-only invariant (e.g., `// drives the mio data-plane port directly — admin sidecar on cfg.admin_addr is bypassed; see CLAUDE.md §mio-only Hot-Path Invariant`). KEEP per the conventions doc's 8th bullet.
    - **Phase 12.7 events-only** — bench pipeline-config loaders that synthesise `@bv.event` schemas (e.g., `fraud-team.json` driver code) may have WHY-comments noting "no @bv.table generation here — events-only per Phase 12.7" — KEEP, not DELETE.
    - **Phase 12.8 memory-governance** — any bench-side memory-instrumentation hooks (peak RSS measurement, per-entity-size dump-trip) may reference the per-entity ~7KB budget invariant — KEEP.
    - **Phase 18 hand-rolled-runtime** — bench code asserting "no tokio in the hot-path measurement loop; bench is a separate process" carries an explicit Phase-18 reference — KEEP per the 8th bullet.
    - **THROUGHPUT-WORKLOAD-01 methodology** (CLAUDE.md §End-to-end throughput regression contract) — the 60s wall-time choice over fixed-event-count, the small/medium/large/fraud-team 4-shape sweep convention, the hw-class tuple definition `(cpu-arch-family, OS family, core count bucket)` — these are externally-visible methodology commitments; WHY-comments documenting them are KEEP. This rule supersedes the generic DELETE-pattern "Phase / plan / task references in code" for these specific methodology references only.
    - **fraud-team primary-tuning-bench commitment** (project memory `project_fraud_team_primary_bench`) — comments noting `fraud-team.json` is the canonical primary tuning shape (not synthetic small/medium/large) are KEEP — they encode an externally-visible benchmarking commitment.

    Walk every `.rs` file under `crates/beava-bench/src/` (use `find crates/beava-bench/src -name '*.rs' -type f`). For each file:

    1. Read the file end-to-end.
    2. Classify each comment per the D-01 rubric. Apply this crate's specific KEEP-bias rules (above) AND these line-level KEEP-bias rules:
       - **Workload-shape rationale** (`// zipfian alpha=1.0 matches Phase 19 fraud-team distribution`) — KEEP if it explains WHY this distribution was chosen; the phase reference can stay if it's the only way to identify the source-of-truth (alternatively rephrase to "matches the canonical fraud-team workload shape" — planner's discretion).
       - **hw-class detection** WHY-comments (the `(cpu-arch-family, OS family, core count bucket)` tuple choice): KEEP.
       - **fsync ceiling on macOS** comments (`// macOS F_FULLSYNC ceiling ~7.4ms; sustained EPS is fsync-bottlenecked here, not apply-bottlenecked`): KEEP.
       - **THROUGHPUT-WORKLOAD-01 rationale** (60s wall-time choice over fixed-event-count): KEEP — this encodes a methodology decision per CLAUDE.md §End-to-end throughput regression contract.

    3. Apply this crate's DELETE-bias rules:
       - **Non-architectural Phase / plan references** in routine paths (e.g., `// added in Phase 12-09 to support msgpack`): DELETE. The methodology-rationale + architectural-invariant references called out in the KEEP-bias section above STAY.
       - **AI-tell phrases**: DELETE.
       - **Doc comments on private fns paraphrasing**: DELETE.
       - **Closing-brace echoes**: DELETE.

    4. Remove orphaned blank lines.

    5. **No logic changes.** No bench-shape changes. No workload-config edits. No flag-default changes.

    6. **Configs and scripts:** scan `crates/beava-bench/configs/` (JSON pipeline configs — fraud-team.json etc.) and `crates/beava-bench/scripts/` (shell scripts) for any AI-tell-y trailing comments and delete them ONLY if the config/script tooling supports comments (JSON does not — skip configs unless they're JSONC; shell scripts: same rubric). Do not touch the data values.

    7. Single commit at end: `chore(13.7.5-06): comment hygiene crates/beava-bench`.
  </action>
  <verify>
    <automated>cargo test -p beava-bench --features testing 2>&amp;1 | tail -10 &amp;&amp; cargo clippy -p beava-bench --all-targets --all-features -- -D warnings 2>&amp;1 | tail -5 &amp;&amp; cargo fmt -p beava-bench --check &amp;&amp; cargo doc --no-deps -p beava-bench 2>&amp;1 | tail -5 &amp;&amp; cargo build -p beava-bench --bin beava-bench-v2 --release 2>&amp;1 | tail -5 &amp;&amp; cargo run -p beava-bench --bin beava-bench-v2 --release -- --help 2>&amp;1 | head -5 &amp;&amp; NON_INV_PHASE=$(grep -rnE '// (Phase|Plan) [0-9]' crates/beava-bench/src/ | grep -vE '(Phase 12\.6|Phase 12\.7|Phase 12\.8|Phase 12\.9|Phase 18|Phase 19|mio-only|events-only|memory-governance|hand-rolled-runtime|THROUGHPUT-WORKLOAD|fraud-team)' | wc -l | tr -d ' ') &amp;&amp; test "$NON_INV_PHASE" = "0" &amp;&amp; test "$(grep -rnE '// (Note that|Importantly,|Here we |We then |It should be noted)' crates/beava-bench/src/ | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>
    Every `.rs` file in `crates/beava-bench/src/` audited; DELETE-pattern removed; workload-shape / hw-class / fsync-ceiling / methodology-rationale / architectural-invariant WHY-comments preserved (per the KEEP-bias section). Both binaries build + `--help` runs. Tests + clippy + fmt + doc green. Zero non-architectural-invariant phase-reference comments. Zero AI-tell comments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit `crates/beava-bench/tests/` and `benches/` against the D-01 rubric</name>
  <files>crates/beava-bench/tests/**/*.rs, crates/beava-bench/benches/**/*.rs</files>
  <read_first>
    - `docs/_internal/comment-conventions.md`.
  </read_first>
  <action>
    Same procedure as Task 1, applied to tests + benches. Same architectural-invariant KEEP-bias applies (the four CLAUDE.md invariants + THROUGHPUT-WORKLOAD-01 + fraud-team primary-tuning-bench).

    Combine commit with Task 1: single `chore(13.7.5-06): comment hygiene crates/beava-bench` commit.
  </action>
  <verify>
    <automated>cargo test -p beava-bench --features testing 2>&amp;1 | tail -5 &amp;&amp; NON_INV_PHASE_TESTS=$(grep -rnE '// (Phase|Plan) [0-9]' crates/beava-bench/tests crates/beava-bench/benches 2>/dev/null | grep -vE '(Phase 12\.6|Phase 12\.7|Phase 12\.8|Phase 12\.9|Phase 18|Phase 19|mio-only|events-only|memory-governance|hand-rolled-runtime|THROUGHPUT-WORKLOAD|fraud-team)' | wc -l | tr -d ' ') &amp;&amp; test "$NON_INV_PHASE_TESTS" = "0"</automated>
  </verify>
  <done>
    Tests + benches scrubbed alongside src. Single commit per crate. Architectural-invariant + methodology WHY-comments preserved.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p beava-bench --features testing` GREEN.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` GREEN.
- `cargo fmt --all --check` GREEN.
- Both `beava-bench` and `beava-bench-v2` binaries build clean and `--help` works.
- Non-architectural-invariant Phase / plan / AI-tell comment patterns return zero grep matches; the architectural-invariant Phase 12.6/12.7/12.8/12.9/18/19 + THROUGHPUT-WORKLOAD + fraud-team references are KEEP-pattern and may remain.
- Single commit `chore(13.7.5-06): comment hygiene crates/beava-bench`.
</verification>

<success_criteria>
- Bench harness reads as engineered tooling.
- Methodology-rationale + architectural-invariant WHY-comments preserved.
- Workspace builds clean; both bench binaries operational.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-06-SUMMARY.md`.
</output>
