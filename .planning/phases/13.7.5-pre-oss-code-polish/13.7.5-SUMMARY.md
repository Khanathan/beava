# Phase 13.7.5 — Pre-OSS code polish — SUMMARY

**Verdict:** PASS
**Closed:** 2026-05-05
**Commits:** 12 plan commits + 1 closure commit on `v2/greenfield`
**Duration:** 1 session (~3-4 hours including agent-stall recovery)

## Goal

Two workstreams to ready the codebase for v0 OSS launch:

- **Workstream A (Plans 02-08):** Per-component comment audit applying CLAUDE.md heuristic ("default to no comments; only add when WHY is non-obvious"). Strip AI-slop paraphrasing + phase/plan-tag references; preserve architectural-invariant WHY-comments (Phase 12.6/12.7/12.8/18 commitments per the conventions doc's 8th KEEP bullet).
- **Workstream B (Plans 09-11):** Test-coverage matrix audit — every v0 surface item × 8 dimensions, classified MUST-FIX vs DEFER per D-03; gap-fill MUST-FIX items.

## What shipped

| Plan | Wave | Headline | Commit |
|---|---|---|---|
| 13.7.5-01 | 1 | `docs/_internal/comment-conventions.md` — D-01 KEEP/DELETE rubric (rubric verbatim from CONTEXT.md + 8th architectural-invariant KEEP bullet) | `699fcf44` |
| 13.7.5-02 | 2 | `crates/beava-core/` comment scrub (6 files, −165 lines) | `ed1383ed` |
| 13.7.5-03 | 2 | `crates/beava-server/` comment scrub (14 files, −762 lines — largest component) | `c9e2137b` |
| 13.7.5-04 | 2 | `crates/beava-runtime-core/` comment scrub (20 files, −197 lines) | `4dd3740f` |
| 13.7.5-05 | 2 | `crates/beava-persistence/` comment scrub (18 files, −97 lines) | `4eb1ce64` |
| 13.7.5-06 | 2 | `crates/beava-bench/` comment scrub (19 files, −105 lines; both legacy + v2 binaries) | `664c8f3a` |
| 13.7.5-07 | 2 | `python/beava/` comment scrub (16 of 19 files, −272 lines, mypy + 89 v0 tests GREEN) | `ec99d006` |
| 13.7.5-08 | 2 | `examples/{python,ts,go}/` comment scrub (13 files, −60 lines, 9/9 verticals GREEN) | `92903271` |
| 13.7.5-09 | 3a | `COVERAGE-MATRIX.csv` (78 rows × 12 cols) + `COVERAGE-GAPS.md` (Rust + Python + DEFER buckets) + `v0.1-deferrals.md` append (5 DEFER items) | `3b4f4478` |
| 13.7.5-10 | 3b | Rust gap-fill **NO-OP** — zero Rust gaps surfaced (every AggKind variant already had Rust unit + Python integration coverage) | `9716b3ee` |
| 13.7.5-11 | 3b | Python gap-fill — 4 new test files, 11 cases, 11/11 GREEN on first run; v0 suite **89 → 100 passing** | `fbe5e4ac` |
| 13.7.5-13 | 4 | Phase closure: SUMMARY + VERIFICATION + 4-shape throughput sweep + STATE/ROADMAP advance + 3 closure-cleanup nits (`fmt` + 2 clippy doc-comment lints) | (this commit) |

Plan 12 (cross-SDK conformance harness) **intentionally OMITTED** per D-03 — DEFERRED to v0.1+.

## Comment-audit deltas

**Aggregate:** −1,658 lines comment cruft scrubbed across 92 files / 7 components.

| Component | Files | Net LOC delta |
|---|---:|---:|
| `crates/beava-core` | 6 | −165 |
| `crates/beava-server` | 14 | −762 |
| `crates/beava-runtime-core` | 20 | −197 |
| `crates/beava-persistence` | 18 | −97 |
| `crates/beava-bench` | 19 | −105 |
| `python/beava` | 16 | −272 |
| `examples/{python,ts,go}` | 13 | −60 |

Removed (DELETE-pattern): phase/plan tags in module docstrings + trailing comments + `.expect()`/`assert!()` panic-message strings; section markers (`// === Setup ===`); self-narration sequences; AI-tell phrases ("Note that", "Importantly"); paraphrase trailing comments; multi-paragraph docstrings on small functions.

Preserved (KEEP-pattern): `SAFETY` contracts on `unsafe` blocks; `///` doc comments on `pub` items (cargo-doc + Sphinx render targets); WHY-comments on non-obvious constraints; architectural-invariant WHY-comments documenting Phase 12.6 mio-only / Phase 12.7 events-only / Phase 12.8 memory-governance / Phase 18 hand-rolled-runtime commitments.

## Coverage matrix headline

- **78 rows × 12 columns** in `COVERAGE-MATRIX.csv` (54 op rows + 10 endpoint rows + 8 ship-pitch flow rows + 6 architectural-invariant rows)
- **71 covered / 6 MUST-FIX / 1 DEFER** verdicts pre-Plan-10/11
- **0 Rust gaps / 6 Python gaps** post-classification
- Plan 11 fills **11 test cases across 4 new files**: `test_register_flags.py` (force/dry_run), `test_schema_evolution.py` (additive/destructive/window-change), `test_url_dispatch.py` (http/tcp/embed/unknown-scheme), `test_reconnect.py` (drop-raises/no-retries — codifies v0's intentional absence-of-reconnect contract)
- All 11/11 PASS on first run against the post-13.5.1 codebase ⇒ zero `SKIPPED-COVERAGE-ROWS.md` entries
- 5 DEFER rows appended to `.planning/ideas/v0.1-deferrals.md` (SRV-API-05 push-batch / WAL-RECOV-EDGE / SNAPSHOT-UPGRADE / CROSS-SDK-CONFORM / CROSS-SDK-FLOWS)

## Throughput-gate sweep (4-shape × 3-run, Apple-M4)

| Cell | EPS (median) | Δ% | Verdict |
|---|---:|---:|---|
| **small/tcp (gate)** | **630,380** | **−1.8%** vs Phase 13.5.2 | **PASS** |
| medium/tcp | 648,022 | +27.4% vs Phase 13.5 | PASS (lift) |
| large/tcp | 546,935 | +18.3% vs Phase 13.5 | PASS (lift) |
| fraud-team/tcp (primary) | 84,290 | −20.0% vs Phase 13.5 | WARN (informational; non-gating) |

Gate cell PASS at −1.8% — closure proceeds. fraud-team/tcp WARN is informational and consistent with documented Apple-M4 dev-box variance on this 14-node × 110-feature pipeline (single high run 93,246 EPS sits within the post-12.9 / Phase 13.4 band; box-load on this run depressed the median). Phase 13.7.5 made zero hot-path semantic changes — comment scrubs + test additions only — so the depression cannot be a structural regression. See `.planning/throughput-baselines.md` § Phase 13.7.5 for full rationale.

## Architectural invariants

All 7 CI tripwires GREEN end-of-phase:

- mio-only data plane (12.6) — `phase12_6_mio_only_dataplane.rs` + `phase12_6_legacy_axum_killed.rs`
- events-only / no-table-surface (12.7) — `phase12_7_no_table_surface.rs` + `phase12_7_legacy_table_handlers_killed.rs`
- cold-entity TTL eviction (12.8) — `phase12_8_cold_entity_eviction.rs`
- lifetime ops have bounds (12.8) — `phase12_8_lifetime_ops_have_bounds.rs`
- AggOp ≤ 80 B per-entity cap (12.9) — `crates/beava-core/tests/per_entity_size_dump.rs::aggop_size_within_cap`

## Carrying forward

- **`docs/operators/index.md` heading off-by-one** — claims "53 unique AggKind variants" but canonical enum has **54**. Matrix and gap docs use the verified 54-variant count. **v0.0.x doc-fix candidate** (non-blocking for v0 GA).
- **fraud-team/tcp throughput WARN** — −20.0% on a noisy cell during a co-resident-load measurement; quiescent re-measurement deferred to Phase 13.8 ship-gate validation. The post-13.7.5 floor (75,861 EPS at 10% under 84,290) is **TENTATIVE**; the post-12.9 floor (92,825 EPS) is NOT superseded — single high run (93,246) sits within the post-12.9 band.
- **Closure-cleanup nits** rolled into the closure commit: `cargo fmt` (apply_shard.rs format!() collapse + feature_query.rs trailing newline) + 2 clippy lints on doc-comment formatting (`registry.rs` `+ chain` markdown-bullet ambiguity → reflow; `server.rs` orphaned doc-block over `ResponseBatchEntry` → demoted to architectural `//` comment).
- **Wave 2 agent stalls** — 5 of the 7 component-audit agents stalled on the 600s stream watchdog mid-cargo-build / cargo-doc verification; their comment-only-safe diffs were salvaged by the orchestrator (verified pure-comment via diff-strip script) and committed as the same `chore(13.7.5-NN):` commits. Workspace verify ran end-of-Wave-2 against the merged result. **Pattern note for Phase 13.7.6:** for plans whose verification step runs `cargo test --workspace` or `cargo doc`, prefer **sequential single-agent execution** over parallel-worktree to avoid memory-contention-driven SIGKILL + watchdog stalls; the salvage path works but adds orchestrator overhead.

## Next

**Phase 13.7.6** — Pre-OSS security + commit-path sanitization + public-facing files. 23 plans authored (workstreams C/D/E/F per CONTEXT.md). Estimated 2-3 days. Then Phase 13.8 — packaging + GA tag.
