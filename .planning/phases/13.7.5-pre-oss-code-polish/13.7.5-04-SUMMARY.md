# Plan 13.7.5-04 — Comment hygiene `crates/beava-runtime-core/`

**Status:** PASS
**Wave:** 2
**Type:** doc-only (CLAUDE.md TDD §item #7 doc-only-plan exemption — single `chore:` commit)

## Salvage note

This plan's first executor stalled on the 600s stream watchdog mid-`cargo doc` run. Per orchestrator review, the worktree's diff was confirmed pure comment-only (zero code-only changes when trailing `// ...` comments are stripped). The diff was salvaged into the main worktree and committed here. The original agent's verification work (cargo test/clippy/fmt) was not completed inside its worktree but is rolled into the end-of-Wave-2 workspace verify.

## What landed

20 `.rs` files under `crates/beava-runtime-core/src/` audited per the D-01 KEEP/DELETE rubric. Net **+294 / −491** = **−197 lines** comment-only.

Files modified:

- `bytes_pool.rs`, `client.rs`, `config.rs`, `event_loop.rs`, `http_listener.rs`
- `io_backend/{io_uring.rs,mio_backend.rs,mod.rs}`, `io_pool.rs`, `io_thread.rs`, `io_thread_worker.rs`
- `lib.rs`, `response.rs`, `router.rs`, `tcp_listener.rs`
- `wal_buffer.rs`, `wal_lsn.rs`, `wal_writer.rs`, `wire_request.rs`, `work_ring.rs`

## Verification

- `cargo check -p beava-runtime-core --all-targets`: PASS (clean compile)
- `cargo clippy/fmt/test/doc`: deferred to end-of-Wave-2 workspace sweep

## Preserved (KEEP-pattern)

- `SAFETY` contracts on raw-pointer code (bytes_pool, work_ring)
- WHY-comments on io_thread / fsync_worker handoff invariants
- WHY-comments on http_listener / tcp_listener accept-loop choices
- Architectural-invariant WHY-comments documenting Phase 18 hand-rolled-runtime commitments

## Removed (DELETE-pattern)

- Phase/plan tags in module docstrings (e.g. `Plan 12-08 (D-C):`)
- Section markers and self-narration sequences
- Paraphrase trailing comments
- AI-tell phrases
