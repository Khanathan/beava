---
phase: 13.7.5
plan: 11
type: execute
wave: 3
depends_on: [13.7.5-09]
files_modified:
  - python/tests/v0/test_register_flags.py
  - python/tests/v0/test_app_reset.py
  - python/tests/v0/test_schema_evolution.py
  - python/tests/v0/test_url_dispatch.py
  - python/tests/v0/test_reconnect.py
  - python/tests/v0/test_buffer.py
  - python/tests/v0/test_core.py
  - python/tests/v0/test_decay.py
  - python/tests/v0/test_geo.py
  - python/tests/v0/test_global.py
  - python/tests/v0/test_lit.py
  - python/tests/v0/test_point_ordinal.py
  - python/tests/v0/test_recency.py
  - python/tests/v0/test_sketch.py
  - python/tests/v0/test_velocity.py
  - .planning/phases/13.7.5-pre-oss-code-polish/SKIPPED-COVERAGE-ROWS.md
autonomous: true
requirements:
  - D-03
tdd: true
tags:
  - gap-fill
  - python
  - tdd-red-then-green
  - real-engine

must_haves:
  truths:
    - "Every Python-gap row in `COVERAGE-GAPS.md` § Python gaps has a covering integration test in `python/tests/v0/`."
    - "Tests use the real engine via `BEAVA_TEST_MODE=1` per Phase 13.5.1 D-05 (`bv.App(test_mode=True)` shared `app` fixture from `python/tests/v0/conftest.py`). NO MagicMock — that was the 13.5 anti-pattern that caused the 0/68 deficit."
    - "Each gap row produces a test that asserts the documented contract; if the test fails against the post-13.5.1 codebase, that's a real bug → STOP, surface to user, do not silently 'fix' production code in a coverage-fill plan."
    - "Per CLAUDE.md §Conventions TDD discipline, each row's task is RED-then-GREEN: the test commit lands first asserting the contract; if the test passes immediately against the existing impl, the green commit is implicit (the impl IS the green); if it fails, route to a separate bug-fix plan and SKIP the row with documented rationale logged into `SKIPPED-COVERAGE-ROWS.md`."
    - "`cd python && pytest tests/v0 -x` GREEN after each commit; the 68 v0 acceptance tests + the new tests all pass."
    - "`mypy --strict python/beava` stays GREEN — new test files use `from __future__ import annotations` + explicit type hints per the v0 acceptance-test convention."
    - "Zero MagicMock occurrences in ANY `python/tests/v0/test_*.py` file — phase-wide guard, not just the 5 new transport/flow files."
    - "Any SKIPPED MUST-FIX row is logged into `.planning/phases/13.7.5-pre-oss-code-polish/SKIPPED-COVERAGE-ROWS.md` (phase-local registry) with table columns: `test-name | class | reason | follow-up`. Plan 13 VERIFICATION grep's it for inventory consistency."
  artifacts:
    - path: "python/tests/v0/test_register_flags.py"
      provides: "force=True + dry_run=True register flow integration tests"
    - path: "python/tests/v0/test_app_reset.py"
      provides: "App.reset() integration test (test_mode-gated)"
    - path: "python/tests/v0/test_schema_evolution.py"
      provides: "Additive vs destructive schema-diff matrix (3 subcases per SRV-REG-03)"
    - path: "python/tests/v0/test_url_dispatch.py"
      provides: "URL-scheme dispatch (http / tcp / no-URL embed / unknown-scheme ValueError)"
    - path: "python/tests/v0/test_reconnect.py"
      provides: "Connection-drop + auto-reconnect + max_retries=0 default"
    - path: ".planning/phases/13.7.5-pre-oss-code-polish/SKIPPED-COVERAGE-ROWS.md"
      provides: "Phase-local registry of SKIPPED MUST-FIX rows (test-name / class / reason / follow-up); appended-to by Plans 10 + 11; grep'd by Plan 13 VERIFICATION"
  key_links:
    - from: "Plan 13.7.5-11 commits"
      to: ".planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-GAPS.md"
      via: "Each new test fills exactly one row from the GAPS.md Python-gaps table"
    - from: "Plan 13.7.5-11"
      to: "Phase 13.5.1 D-05"
      via: "Reuses the test_mode=True real-engine fixture established by 13.5.1"
    - from: "Plan 13.7.5-11 SKIPPED rows"
      to: ".planning/phases/13.7.5-pre-oss-code-polish/SKIPPED-COVERAGE-ROWS.md"
      via: "Append-only registry that Plan 13 VERIFICATION cross-references"
---

<objective>
Fill every Python-gap row from `COVERAGE-GAPS.md` with an integration test in `python/tests/v0/`. Per D-03, MUST-FIX gaps that are transport-or-flow-level concerns (force / dry_run / reset / schema-evolution / URL-dispatch / reconnect / max_retries=0) require a real engine spawn and go to Python integration tests.

Per CLAUDE.md §Conventions TDD discipline (mandatory from Phase 3 onward): each task is RED-then-GREEN. Coverage fill specifics:

1. Write the test asserting expected behavior.
2. Run `cd python && pytest tests/v0/<new_file>::<test_fn> -x`.
3. **If the test PASSES against the post-13.5.1 codebase:** the impl satisfies the contract; commit `test(13.7.5-11): cover <surface_id>` (single commit; the impl IS the green).
4. **If the test FAILS:** STOP. The impl does NOT satisfy the contract → real bug. Surface to user. Do NOT modify production code in a coverage-fill plan. SKIP the row, document in commit message AND append a row to `.planning/phases/13.7.5-pre-oss-code-polish/SKIPPED-COVERAGE-ROWS.md`.

Phase 13.5.1 D-05 established the `BEAVA_TEST_MODE=1` real-engine fixture under `python/tests/v0/conftest.py` — Plan 11 reuses this pattern. **NO MagicMock under any circumstance** (per `feedback_executor_quality_profile` memory and the 13.5 deficit) — phase-wide guard applies to ALL `python/tests/v0/test_*.py` files, not just the 5 new ones.

Output: 5 new test files (transport/flow) created in Task 1; per-op gap fills appended to existing `python/tests/v0/test_<family>.py` files in Task 2; SKIPPED rows logged into `SKIPPED-COVERAGE-ROWS.md`. One commit per surface_id (`test(13.7.5-11): cover <surface_id>`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-CONTEXT.md
@.planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-MATRIX.csv
@.planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-GAPS.md
@.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: For each Python-gap row in COVERAGE-GAPS.md targeting a NEW transport/flow test file, create + run the test (iterates the 5 new transport/flow files: register_flags / app_reset / schema_evolution / url_dispatch / reconnect)</name>
  <files>python/tests/v0/test_register_flags.py, python/tests/v0/test_app_reset.py, python/tests/v0/test_schema_evolution.py, python/tests/v0/test_url_dispatch.py, python/tests/v0/test_reconnect.py</files>
  <read_first>
    - `.planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-GAPS.md` § Python gaps table (the source-of-truth for which (file, fn) pairs to create — proposed_test_path + test_function_name columns).
    - `python/tests/v0/conftest.py` (the `app` fixture from Phase 13.5.1 D-05, real engine, BEAVA_TEST_MODE=1).
    - `python/tests/v0/_helpers.py` (the helpers established by 13.5.1).
    - `python/beava/_app.py`, `python/beava/_transport.py`, `python/beava/_embed.py` for the underlying contracts each test asserts.
    - `docs/sdk-api/python.md` — the documented contract.
    - For each target file's contract details: see the per-file behavior table below.
  </read_first>
  <behavior>
    Iterate the 5 NEW transport/flow test files defined in COVERAGE-GAPS.md § Python gaps. The contracts each must assert (mirrors Plan 10's iterating pattern):

    **`test_register_flags.py`** — covers SDK-APP-02 force + dry_run flows:
    - `test_force_replaces_existing(app)`: register pipeline-A (`@bv.event Click` + table aggregating `bv.count()`); register pipeline-B (same table name, different agg `bv.sum('amount')`) — expect `RegistrationError` (additive-only per SRV-API-02). Re-register B with `force=True`; expect success. Push event; query; assert value reflects pipeline-B.
    - `test_dry_run_returns_diff_no_commit(app)`: register pipeline-A; register pipeline-B with `dry_run=True`; expect structured `{diff: {added, removed, changed}}` response per SRV-API-02. Confirm pipeline-A is still active.
    - `test_force_and_dry_run_combined_returns_diff_no_commit(app)`: per `python/beava/_app.py:271` ("Implies dry_run=True"), `force=True, dry_run=True` returns the diff WITHOUT committing.

    **`test_app_reset.py`** — covers App.reset() flow (test_mode-gated):
    - `test_reset_clears_state(app)`: register pipeline; push 5 events for `user='alice'`; assert count==5; `app.reset()`; assert count==0; re-push 1; assert count==1.
    - `test_reset_in_production_mode_raises(monkeypatch)`: spawn `App(test_mode=False)`; `app.reset()` expects structured error (e.g., `reset_disabled_in_production`). If server doesn't gate reset → SKIP + log to `SKIPPED-COVERAGE-ROWS.md` + surface bug.

    **`test_schema_evolution.py`** — covers SRV-REG-03 diff matrix:
    - `test_additive_succeeds(app)`: register pipeline-A; register pipeline-A + new node N; expect 200 + version bump.
    - `test_destructive_returns_409(app)`: register pipeline-A (3 nodes); register pipeline-A minus 1 node; expect `RegistrationError` 409 with `{diff: {removed: [node_name]}}`.
    - `test_changed_field_type_returns_409(app)`: register `Click: amount: float`; re-register with `amount: int`; expect 409 with `{diff: {changed: [{node, field, from, to}]}}`.

    **`test_url_dispatch.py`** — covers SDK-WIRE-03 URL-scheme dispatch:
    - `test_http_url_picks_http_transport()`: `bv.App("http://localhost:65530")` → `type(app._transport).__name__ == "HttpTransport"`. `app.close()` after.
    - `test_tcp_url_picks_tcp_transport()`: same with `tcp://`.
    - `test_no_url_picks_embed_transport()`: `bv.App()` no arg → `EmbedTransport`. Subprocess cleanup via `app.close()`.
    - `test_unknown_scheme_raises_value_error()`: `bv.App("redis://localhost:6379")` → `ValueError` at construction (per SDK-WIRE-03).

    **`test_reconnect.py`** — covers SDK-APP reconnect + max_retries=0 default:
    - `test_transport_recovers_after_server_restart(app)`: register; push (success); restart underlying server; push again (auto-reconnect succeeds). If fixture has no restart hook → SKIP + log + route to bug-fix plan that adds the hook.
    - `test_max_retries_zero_surfaces_immediately(monkeypatch)`: `App(max_retries=0)` pointed at unreachable URL → immediate failure < 1s, no exponential backoff.
  </behavior>
  <action>
    For each of the 5 transport/flow test files (`test_register_flags.py`, `test_app_reset.py`, `test_schema_evolution.py`, `test_url_dispatch.py`, `test_reconnect.py`):

    1. Create the file. Top of file:
       ```python
       """<surface description> integration tests (D-03 MUST-FIX)."""

       from __future__ import annotations

       import pytest
       import beava as bv
       # imports specific to file (e.g., RegistrationError, monkeypatch fixture)
       ```

    2. Add the test fns described in the behavior block above (typed, real-engine fixture, no MagicMock).

    3. Run pytest:
       ```bash
       cd python && pytest tests/v0/<file_basename>.py -x
       ```

    4. If GREEN → commit `test(13.7.5-11): cover <surface_id_or_subject>` (one commit per file; subject reflects the file's surface — e.g., "register force/dry_run flows", "App.reset() flow", "schema-evolution diff matrix", "URL-scheme dispatch", "reconnect + max_retries=0").

    5. If RED → SKIP the failing test cases:
       - Mark in commit message: `test(13.7.5-11): cover <surface> (SKIPPED <test_fn>: <reason>)` body line for each SKIP.
       - Append a row to `.planning/phases/13.7.5-pre-oss-code-polish/SKIPPED-COVERAGE-ROWS.md` (create the file if it doesn't exist; see registry-format note below). Append in same commit.
       - Surface the bug to the user.

    6. **NO production-code edits in this plan.** Coverage fill does not license bug fixes.

    Use the Edit tool for any pre-existing files; use Write for new files. Iterate one file at a time so each commit is reviewable.

    **`SKIPPED-COVERAGE-ROWS.md` registry format:** if absent, create with this header on first SKIP:
    ```markdown
    # Skipped MUST-FIX coverage rows — Phase 13.7.5

    Append-only registry; logged by Plans 10 + 11; cross-referenced by Plan 13 VERIFICATION.

    | test-name | class | reason | follow-up |
    |---|---|---|---|
    ```
    Then append one row per SKIP. `class` = `python` here (Plan 11) or `rust` (Plan 10). `follow-up` = bug-fix plan ID or "TBD-bug-routing".
  </action>
  <verify>
    <automated>cd python &amp;&amp; pytest tests/v0/test_register_flags.py tests/v0/test_app_reset.py tests/v0/test_schema_evolution.py tests/v0/test_url_dispatch.py tests/v0/test_reconnect.py -x 2>&amp;1 | tail -15 &amp;&amp; cd .. &amp;&amp; mypy --strict python/tests/v0/test_register_flags.py python/tests/v0/test_app_reset.py python/tests/v0/test_schema_evolution.py python/tests/v0/test_url_dispatch.py python/tests/v0/test_reconnect.py 2>&amp;1 | tail -5 &amp;&amp; PHASE_WIDE_MOCK_COUNT=$(grep -rn 'MagicMock\|mock\.' python/tests/v0/ | wc -l | tr -d ' ') &amp;&amp; test "$PHASE_WIDE_MOCK_COUNT" = "0" &amp;&amp; echo "phase-wide MagicMock occurrences in python/tests/v0/: 0"</automated>
  </verify>
  <done>
    All 5 transport/flow test files exist with the contracted test fns. `pytest` GREEN OR documented SKIPs logged in `SKIPPED-COVERAGE-ROWS.md` + commit body. mypy --strict GREEN. Phase-wide MagicMock guard PASS (zero occurrences across ALL `python/tests/v0/test_*.py` files, not just the 5 new ones).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-op direct-test gap fills in existing v0 test files</name>
  <files>python/tests/v0/test_buffer.py, python/tests/v0/test_core.py, python/tests/v0/test_decay.py, python/tests/v0/test_geo.py, python/tests/v0/test_global.py, python/tests/v0/test_lit.py, python/tests/v0/test_point_ordinal.py, python/tests/v0/test_recency.py, python/tests/v0/test_sketch.py, python/tests/v0/test_velocity.py</files>
  <read_first>
    - `COVERAGE-GAPS.md` § Python gaps — the per-op rows where the proposed_test_path points at one of the existing `test_<family>.py` files (D-03 "Every one of the 53 ops has at least ONE direct test"; the rows below the flow-level gaps).
    - The existing test file's convention before adding to it.
  </read_first>
  <behavior>
    For each per-op gap row whose proposed test path is `python/tests/v0/test_<family>.py`:
    1. Open the existing file.
    2. Read existing tests to mimic convention (fixture usage, assertion style).
    3. Append a new test fn `test_<op>_basic` exercising the op end-to-end through the real engine.
    4. Run `pytest tests/v0/test_<family>.py::test_<op>_basic -x`.
    5. Commit one commit per file: `test(13.7.5-11): cover per-op gaps in test_<family>.py`. If gap count < 5 ops total, consolidate into one commit `test(13.7.5-11): cover remaining per-op direct-test gaps`.
    6. SKIPs: same registry-append protocol as Task 1.
  </behavior>
  <action>
    Iterate per-op gap rows. For each, append the test, run pytest, commit per file (or consolidated if small).

    Same MagicMock prohibition; same SKIPPED-COVERAGE-ROWS.md routing for any failures.
  </action>
  <verify>
    <automated>cd python &amp;&amp; pytest tests/v0 -x 2>&amp;1 | tail -15 &amp;&amp; cd .. &amp;&amp; mypy --strict python/beava 2>&amp;1 | tail -5 &amp;&amp; PHASE_WIDE_MOCK_COUNT=$(grep -rn 'MagicMock\|mock\.' python/tests/v0/ | wc -l | tr -d ' ') &amp;&amp; test "$PHASE_WIDE_MOCK_COUNT" = "0" &amp;&amp; echo "phase-wide MagicMock occurrences in python/tests/v0/: 0"</automated>
  </verify>
  <done>
    Every per-op gap row with a `python/tests/v0/test_<family>.py` proposed path now has a covering test in that file. `pytest tests/v0` GREEN. mypy --strict GREEN. Phase-wide MagicMock guard PASS.
  </done>
</task>

</tasks>

<verification>
- `cd python && pytest tests/v0 -x` GREEN — all 68 v0 acceptance tests + new tests from this plan all pass.
- `mypy --strict python/beava` GREEN.
- Every row in `COVERAGE-GAPS.md` § Python gaps has a corresponding `test(13.7.5-11): cover <surface_id>` commit OR a SKIP-with-rationale + bug-fix-routing commit AND a row in `.planning/phases/13.7.5-pre-oss-code-polish/SKIPPED-COVERAGE-ROWS.md`.
- No production code changes in `python/beava/`.
- Real engine used everywhere; **phase-wide** MagicMock guard: `grep -rn 'MagicMock\|mock\.' python/tests/v0/ | wc -l` returns 0 (covers ALL `python/tests/v0/test_*.py` files, not just the 5 new ones).
</verification>

<success_criteria>
- Every D-03 MUST-FIX gap routed to Python now has a covering integration test against the real engine.
- No silent bug-fixes — discrepancies surfaced and logged in `SKIPPED-COVERAGE-ROWS.md`.
- v0 acceptance suite total grows from 68 to 68 + N (N = new tests added across Tasks 1–2).
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-11-SUMMARY.md`.
</output>
