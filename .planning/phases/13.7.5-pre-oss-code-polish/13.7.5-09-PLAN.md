---
phase: 13.7.5
plan: 09
type: execute
wave: 3
depends_on: [13.7.5-02, 13.7.5-03, 13.7.5-04, 13.7.5-05, 13.7.5-06, 13.7.5-07, 13.7.5-08]
files_modified:
  - .planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-MATRIX.csv
  - .planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-GAPS.md
autonomous: true
requirements:
  - D-03
tags:
  - coverage-matrix
  - audit
  - gap-classification

must_haves:
  truths:
    - "`COVERAGE-MATRIX.csv` exists with one row per surface item: 53 operators (per `docs/operators/index.md`) + 6 wire endpoints (per `docs/wire-spec.md`) + the v0 ship-pitch flow surface (force / dry_run / reset / schema-evolution / URL-dispatch / reconnect / max_retries=0) + architectural-invariant tripwires."
    - "Each row carries 8 dimension columns: Wire spec / Doc page / JSON example / Rust unit test / Architectural test / Python integration / Demo / Verdict."
    - "Each `Verdict` cell is one of: `covered` / `MUST-FIX` / `DEFER`. Classification follows D-03 verbatim — MUST-FIX = v0 ship-pitch surface gaps; DEFER = WAL-replay-edge / snapshot-upgrade / cross-SDK conformance."
    - "`COVERAGE-GAPS.md` summarizes the MUST-FIX rows in two buckets: (a) Rust gaps (Plan 10 fills); (b) Python gaps (Plan 11 fills). Each bucket lists exact test files to create + the surface item each fills."
    - "DEFER rows are routed to `.planning/ideas/v0.1-deferrals.md` (append; do not modify existing entries) so v0.1 planning has the inventory."
  artifacts:
    - path: ".planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-MATRIX.csv"
      provides: "Source of truth for every v0 surface item × 7 test-status dimensions × verdict"
      min_rows: 70  # 53 ops + 6 endpoints + ~7 ship-pitch flow rows + ~5 architectural-invariant rows
    - path: ".planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-GAPS.md"
      provides: "MUST-FIX bucket summary partitioned into Rust-gaps + Python-gaps for Plans 10/11"
      contains_sections:
        - "Rust gaps (Plan 10)"
        - "Python gaps (Plan 11)"
        - "DEFER routed to v0.1-deferrals.md"
  key_links:
    - from: "Plan 13.7.5-09 output"
      to: "Plans 13.7.5-10 + 13.7.5-11"
      via: "Plans 10/11 read COVERAGE-GAPS.md to know which tests to write"
    - from: "Plan 13.7.5-09 DEFER rows"
      to: ".planning/ideas/v0.1-deferrals.md"
      via: "Append-only update so v0.1 planning has the catalogue"
---

<objective>
Build the v0 ship-pitch coverage matrix per CONTEXT.md D-03. One CSV row per surface item, 8 dimension columns, verdict per row. Partition the MUST-FIX bucket into Rust-gaps (Plan 10 fills) and Python-gaps (Plan 11 fills). Route DEFER rows to `.planning/ideas/v0.1-deferrals.md`.

Purpose: This plan is the "audit step" — it produces no test code. It produces the inventory that Plans 10/11 work from.

Wave 3 entry; depends on all 7 Wave-2 component-audit plans (02–08) closing first so the matrix audits the post-scrub code surface.

Output: `COVERAGE-MATRIX.csv` + `COVERAGE-GAPS.md`. Single commit: `docs(13.7.5-09): coverage matrix + gap classification`.

Doc-only-plan exemption per **CLAUDE.md §TDD Discipline item #7** (closure-style documentation plan; the matrix IS the verification of upstream component-audit plans). No red-then-green required.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/operators/index.md
@docs/wire-spec.md
@docs/error-codes.md
@docs/sdk-api/python.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate every v0 surface item — 53 ops + 6 wire endpoints + ship-pitch flows + architectural invariants</name>
  <files>.planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-MATRIX.csv</files>
  <read_first>
    - `docs/operators/index.md` — 53-op catalogue (the canonical list per Phase 13.0).
    - `docs/wire-spec.md` — 6 wire endpoints (POST /register, POST /push/{event}, POST /push-sync/{event}, POST /push-batch/{event}, POST /get, POST /set/mset, plus admin: GET /health, GET /ready, GET /metrics, GET /registry — verify the exact 6 from the wire spec).
    - `.planning/REQUIREMENTS.md` — note the SDK-APP-* + SRV-API-* IDs that frame the ship-pitch surface (force/dry_run via SDK-APP-02, reset via SDK-APP — confirm exact ID; URL-scheme dispatch via SDK-WIRE-03; reconnect / max_retries surface from `python/beava/_transport.py`).
    - `docs/error-codes.md` — exception hierarchy (matrix coverage check; e.g., does each error code have a matching test that asserts the structured `{code, path, reason}` shape?).
    - `docs/sdk-api/python.md` — Python SDK contract.
    - The v0 acceptance test suite (`python/tests/v0/*.py`) — this is the inventory of what's ALREADY covered Python-side; the matrix should mark those as Python-integration ✓.
    - The architectural-test files in `crates/beava-server/tests/` (`phase12_6_*`, `phase12_7_*`, `phase12_8_*`).
  </read_first>
  <action>
    Build the CSV with this exact header:
    ```
    surface_id,surface_name,category,wire_spec,doc_page,json_example,rust_unit_test,architectural_test,python_integration,demo,verdict,notes
    ```

    Categories: `op` (53 rows), `endpoint` (6 rows), `ship_pitch_flow` (~7 rows), `architectural_invariant` (~5 rows). Total ≥70 rows.

    For each row fill the 7 test-status columns with one of:
    - `✓` — verified covered (pointer to test file in `notes`)
    - `✗` — known gap (no covering test)
    - `—` — not applicable (e.g., `demo` column for an architectural invariant)
    - `?` — needs investigation (use sparingly; resolve before writing the verdict — no `?` should remain in the final CSV)

    **Discovery procedure for each row:**
    1. **op rows (53):** for `bv.<op_name>`:
       - `wire_spec` — does the op appear in `docs/wire-spec.md`? (likely covered by the OpNode JSON schema section)
       - `doc_page` — `docs/operators/<family>.md` exists with the op?
       - `json_example` — does any registered-pipeline JSON example (in docs or tests) include this op?
       - `rust_unit_test` — `grep -rn "AggKind::<OpName>\|<op_str>" crates/beava-core/tests/ crates/beava-core/src/agg_*.rs | grep test` — does a unit test directly assert behavior?
       - `architectural_test` — covered by the lifetime-bounds tripwire (Plan 12.8)? `grep "<op_str>" crates/beava-server/tests/phase12_8_lifetime_ops_have_bounds.rs crates/beava-core/tests/op_lifetime_bounds.rs`. Almost all 53 ops should ✓ here.
       - `python_integration` — `grep -rn "bv\.<op_name>\b" python/tests/v0/test_*.py` — does any v0 acceptance test exercise this op end-to-end?
       - `demo` — `grep -rn "bv\.<op_name>\b" examples/` — does any of the 3 demos use this op?
       - `verdict` — per D-03: if `python_integration ✗` AND `rust_unit_test ✗` → `MUST-FIX`. If `python_integration ✓` OR `rust_unit_test ✓` → `covered`. Architectural-test alone is NOT covered per D-03 ("Every one of the 53 ops has at least ONE direct test (Rust unit OR Python integration; **NOT** 'covered' via architectural test alone)").

    2. **endpoint rows (6):** for `POST /register`, `POST /push/{event}`, `POST /push-sync/{event}`, `POST /push-batch/{event}`, `POST /get`, `POST /set` — same dimension lookup. Architectural test for endpoints means the wire-format CI tests (`phase12_07_*`, `phase12_09_*`).

    3. **ship_pitch_flow rows (D-03 MUST-FIX list):**
       - `force=True` register flow
       - `dry_run=True` register flow
       - `App.reset()` flow (test_mode-gated)
       - Schema-evolution diff matrix (additive vs destructive)
       - URL-scheme dispatch (`http://` vs `tcp://` vs no-URL embed)
       - Connection drop + auto-reconnect
       - `max_retries=0` default behavior
       - "Every one of the 53 ops has at least ONE direct test" — per-op verdict already on op rows; this row is a meta-invariant noting "53/53 ops have ≥1 direct test post-Plan-10/11"

    4. **architectural_invariant rows (~5):**
       - mio-only data plane (Plan 12.6 tripwire)
       - events-only / no-table-surface (Plan 12.7 tripwire)
       - cold-entity TTL eviction (Plan 12.8 tripwire)
       - lifetime ops have bounds (Plan 12.8 tripwire)
       - WAL recovery happy path (covered by `recovery.rs` integration tests; edge cases DEFER per D-03)

    Verdict column rules (per D-03):
    - `covered` if there's at least one direct test (Rust unit OR Python integration); architectural-test alone does not satisfy.
    - `MUST-FIX` if it's a D-03 ship-pitch surface item AND the relevant test cell is `✗`.
    - `DEFER` if the gap is one of: WAL replay correctness for every `RecordType` variant beyond happy path / Snapshot upgrade-path / Cross-SDK conformance per-op equivalence / Cross-SDK conformance for force/dry_run/reset/schema-evolution.

    Use `notes` column for: file paths to existing tests (`crates/beava-core/tests/agg_buffer_test.rs:42`), or — for gaps — the proposed new test path (`python/tests/v0/test_register_flags.py::test_force_replaces_existing`).

    Save as `.planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-MATRIX.csv`. CSV format: comma-separated, double-quote string values containing commas, no trailing newline within rows.
  </action>
  <verify>
    <automated>test -f .planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-MATRIX.csv &amp;&amp; HEADER=$(head -1 .planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-MATRIX.csv) &amp;&amp; echo "$HEADER" | grep -q "surface_id,surface_name,category,wire_spec,doc_page,json_example,rust_unit_test,architectural_test,python_integration,demo,verdict,notes" &amp;&amp; ROW_COUNT=$(python3 -c "import csv; print(sum(1 for _ in csv.DictReader(open('.planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-MATRIX.csv'))))") &amp;&amp; test "$ROW_COUNT" -ge 70 &amp;&amp; echo "rows: $ROW_COUNT" &amp;&amp; OP_COUNT=$(python3 -c "import csv; print(sum(1 for r in csv.DictReader(open('.planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-MATRIX.csv')) if r.get('category','')=='op'))") &amp;&amp; test "$OP_COUNT" = "53" &amp;&amp; echo "op rows: 53/53" &amp;&amp; UNRESOLVED=$(python3 -c "import csv; cols=['wire_spec','doc_page','json_example','rust_unit_test','architectural_test','python_integration','demo']; print(sum(1 for r in csv.DictReader(open('.planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-MATRIX.csv')) if any(r.get(c,'')=='?' for c in cols)))") &amp;&amp; test "$UNRESOLVED" = "0" &amp;&amp; echo "no unresolved ? cells" &amp;&amp; INVALID_VERDICTS=$(python3 -c "import csv; valid={'covered','MUST-FIX','DEFER'}; print(sum(1 for r in csv.DictReader(open('.planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-MATRIX.csv')) if r.get('verdict','') not in valid))") &amp;&amp; test "$INVALID_VERDICTS" = "0" &amp;&amp; echo "all verdicts valid"</automated>
  </verify>
  <done>
    `COVERAGE-MATRIX.csv` exists with the exact header. ≥70 rows (CSV-aware count). Exactly 53 `op` rows (CSV-aware filter). Zero unresolved `?` cells in the test-status columns. Every row has a verdict ∈ {covered, MUST-FIX, DEFER}.
  </done>
</task>

<task type="auto">
  <name>Task 2: Partition MUST-FIX rows into Rust-gaps and Python-gaps; write `COVERAGE-GAPS.md`</name>
  <files>.planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-GAPS.md</files>
  <read_first>
    - The `COVERAGE-MATRIX.csv` from Task 1.
  </read_first>
  <action>
    Read the CSV. Filter rows where `verdict == MUST-FIX`. Partition into:

    **Rust-gaps (Plan 10 fills):** rows where `rust_unit_test == ✗` AND `python_integration == ✗`. The op surface needs at least one direct test; per D-03 architectural-test alone is not enough; Rust unit is the cheapest fill for op-internal behavior. Rule of thumb: ops without a Python integration test go to Rust unit (cheaper, no transport setup needed); the `force/dry_run/reset/schema-evolution/URL-dispatch/reconnect/max_retries=0` flows go to Python integration (transport-aware).

    **Python-gaps (Plan 11 fills):** rows where the surface item is a transport-or-flow level concern that requires a real engine spawn:
    - `force=True` register flow → `python/tests/v0/test_register_flags.py::test_force_replaces_existing`
    - `dry_run=True` register flow → `python/tests/v0/test_register_flags.py::test_dry_run_returns_diff_no_commit`
    - `App.reset()` → `python/tests/v0/test_app_reset.py::test_reset_clears_state` (test_mode-gated; reuses 13.5.1 `BEAVA_TEST_MODE=1` fixture)
    - Schema-evolution diff matrix → `python/tests/v0/test_schema_evolution.py::test_additive_succeeds` + `::test_destructive_returns_409` + `::test_changed_field_type_returns_409`
    - URL-scheme dispatch → `python/tests/v0/test_url_dispatch.py::test_http_url_picks_http_transport` + `::test_tcp_url_picks_tcp_transport` + `::test_no_url_picks_embed_transport` + `::test_unknown_scheme_raises_value_error`
    - Connection drop + auto-reconnect → `python/tests/v0/test_reconnect.py::test_transport_recovers_after_server_restart`
    - `max_retries=0` default → `python/tests/v0/test_reconnect.py::test_max_retries_zero_surfaces_immediately`
    - Per-op direct test gaps (any op rows where both Rust-unit ✗ AND Python-integration ✗ AND the op-family file in `python/tests/v0/test_*.py` is the natural home — usually the cheaper add) — list them by op family, target `python/tests/v0/test_<family>.py`.

    Write `COVERAGE-GAPS.md` with this structure:

    ```markdown
    # Coverage Gaps — MUST-FIX bucket (D-03)

    Source: `COVERAGE-MATRIX.csv`. Generated by Plan 13.7.5-09.

    ## Rust gaps (Plan 13.7.5-10 fills)

    | surface_id | surface_name | proposed_test_path | test_function_name | rationale |
    |---|---|---|---|---|
    | <e.g. AGG-CORE-06> | bv.variance | crates/beava-core/tests/agg_core_variance.rs | test_variance_welford_basic | Op-internal Welford correctness; cheaper than transport-level integration |
    | ... | | | | |

    ## Python gaps (Plan 13.7.5-11 fills)

    | surface_id | surface_name | proposed_test_path | test_function_name | rationale |
    |---|---|---|---|---|
    | SDK-APP-02-force | force=True register flow | python/tests/v0/test_register_flags.py | test_force_replaces_existing | Server-side flow; needs real engine + register sequence |
    | SDK-APP-02-dry-run | dry_run=True register flow | python/tests/v0/test_register_flags.py | test_dry_run_returns_diff_no_commit | Same, dry-run path |
    | SDK-APP-reset | App.reset() | python/tests/v0/test_app_reset.py | test_reset_clears_state | test_mode-gated; reuses 13.5.1 fixture |
    | SDK-WIRE-03 | URL-scheme dispatch | python/tests/v0/test_url_dispatch.py | test_http_url_picks_http_transport, ... | Per-scheme: http / tcp / embed / unknown |
    | SDK-APP-reconnect | Connection drop + auto-reconnect | python/tests/v0/test_reconnect.py | test_transport_recovers_after_server_restart | Transient-error path |
    | SDK-APP-max-retries-0 | max_retries=0 default | python/tests/v0/test_reconnect.py | test_max_retries_zero_surfaces_immediately | Default-behavior contract |
    | SRV-REG-03-evolve | Schema evolution diff | python/tests/v0/test_schema_evolution.py | test_additive_succeeds, test_destructive_returns_409, test_changed_field_type_returns_409 | Three subcases per SRV-REG-03 |
    | <per-op gaps> | bv.<op> | python/tests/v0/test_<family>.py | test_<op_name>_basic | Per-op direct-test gap |
    | ... | | | | |

    ## DEFER routed to v0.1-deferrals.md

    | surface_id | surface_name | reason |
    |---|---|---|
    | WAL-RECOV-EDGE | WAL replay edge cases per RecordType variant | Per D-03 — beyond happy path; v0.1+ |
    | SNAPSHOT-UPGRADE | Snapshot FORMAT_VERSION upgrade-path | Per D-03; v0.1+ |
    | CROSS-SDK-CONFORM | Cross-SDK per-op equivalence | Per D-03; Phase 12 dropped; v0.1+ |
    | CROSS-SDK-FLOWS | Cross-SDK conformance for force/dry_run/reset/schema-evolution | Per D-03; Python is canonical for v0; v0.1+ |
    | ... | | |
    ```

    After writing the file, append the DEFER rows to `.planning/ideas/v0.1-deferrals.md`. Do not modify existing entries — append a new section header `## Routed from Phase 13.7.5 coverage matrix (2026-05-04)` and list each DEFER row's surface_id + reason.

    Single commit covering Tasks 1 + 2: `docs(13.7.5-09): coverage matrix + gap classification`.
  </action>
  <verify>
    <automated>test -f .planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-GAPS.md &amp;&amp; grep -q '^## Rust gaps' .planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-GAPS.md &amp;&amp; grep -q '^## Python gaps' .planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-GAPS.md &amp;&amp; grep -q '^## DEFER routed to v0.1-deferrals.md' .planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-GAPS.md &amp;&amp; grep -q '^## Routed from Phase 13.7.5 coverage matrix' .planning/ideas/v0.1-deferrals.md &amp;&amp; MUSTFIX_CSV=$(python3 -c "import csv; print(sum(1 for r in csv.DictReader(open('.planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-MATRIX.csv')) if r.get('verdict','')=='MUST-FIX'))") &amp;&amp; MUSTFIX_GAPS=$(grep -cE '^\| [A-Z][A-Z0-9-]+' .planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-GAPS.md || echo 0) &amp;&amp; echo "MUST-FIX in CSV: $MUSTFIX_CSV; rows in GAPS.md: $MUSTFIX_GAPS"</automated>
  </verify>
  <done>
    `COVERAGE-GAPS.md` exists with three required sections (Rust gaps / Python gaps / DEFER). Every MUST-FIX row in the CSV (CSV-aware count) appears in either the Rust-gaps or Python-gaps bucket of the GAPS.md (ignoring the meta "53/53 ops have a direct test" row which decomposes into per-op rows in the matrix). DEFER rows appended to `v0.1-deferrals.md` under a clearly-tagged section header.
  </done>
</task>

</tasks>

<verification>
- `COVERAGE-MATRIX.csv` ≥70 rows (CSV-aware count via `csv.DictReader`), exactly 53 `op` category rows (CSV-aware filter), zero unresolved `?` cells.
- `COVERAGE-GAPS.md` partitions MUST-FIX into Rust + Python buckets with concrete proposed test paths.
- `.planning/ideas/v0.1-deferrals.md` has appended DEFER routing.
- Single commit `docs(13.7.5-09): coverage matrix + gap classification`.
</verification>

<success_criteria>
- Plans 10/11 can read `COVERAGE-GAPS.md` and execute mechanically without revisiting the matrix.
- Every MUST-FIX gap has a proposed test file path + test fn name.
- DEFER inventory is captured in v0.1-deferrals.md for v0.1 planning.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-09-SUMMARY.md`.
</output>
