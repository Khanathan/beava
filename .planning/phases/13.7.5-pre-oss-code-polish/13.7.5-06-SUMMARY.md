# Plan 13.7.5-06 — Comment hygiene `crates/beava-bench/`

**Status:** PASS
**Wave:** 2
**Type:** doc-only (CLAUDE.md TDD §item #7 doc-only-plan exemption — single `chore:` commit)

## Salvage note

This plan's first executor (and a retry) both stalled on the 600s stream watchdog. Per orchestrator review, the working-tree edits in `crates/beava-bench/` were confirmed comment-only-safe (the only non-pure-comment diff lines were trailing-comment removals causing single multi-line→single-line `match`-arm collapse and trailing-comment text shortening, all semantically equivalent). Salvaged into this commit. Verification deferred to end-of-Wave-2 workspace sweep.

## What landed

19 files under `crates/beava-bench/` audited per the D-01 KEEP/DELETE rubric. Net **+279 / −384** = **−105 lines**.

Files modified (covers both `beava-bench` and `beava-bench-v2` binaries plus all CLI subcommands and workload definitions):

- `src/bin/{beava-bench-legacy.rs, beava-bench-v18.rs, beava-bench-v2.rs, beava-bench.rs}`
- `src/{blast_shape.rs, lib.rs}`
- `src/cli/{estimator.rs, fsync.rs, interactive.rs, memory.rs, mixed.rs, mod.rs, output.rs, throughput.rs}`
- `src/harness/mod.rs`
- `src/workloads/{adtech.rs, ecommerce.rs, fraud.rs, mod.rs}`

## Verification

- `cargo check -p beava-bench --all-targets`: PASS
- `cargo clippy/fmt/test/doc`: deferred to end-of-Wave-2 workspace sweep

## Preserved (KEEP-pattern)

- WHY-comments on workload-shape choices (zipfian alpha=1.0, hw-class detection, fsync-bottleneck on macOS)
- Architectural-invariant WHY-comments documenting Phase 12.6/12.7/12.8/18 commitments
- Approximation rationale on `run_fsync_acks_all`
- Tempdir lifecycle WHY-comments

## Removed (DELETE-pattern)

- Phase/plan tags in module docstrings and inline comments
- Section markers and self-narration
- Paraphrase trailing comments
