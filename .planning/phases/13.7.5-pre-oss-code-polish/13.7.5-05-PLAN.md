---
phase: 13.7.5
plan: 05
type: execute
wave: 2
depends_on: [13.7.5-01]
files_modified:
  - crates/beava-persistence/src/**/*.rs
  - crates/beava-persistence/tests/**/*.rs
  - crates/beava-persistence/benches/**/*.rs
autonomous: true
requirements:
  - D-01
tags:
  - comment-audit
  - rust
  - beava-persistence

must_haves:
  truths:
    - "Every `.rs` file under `crates/beava-persistence/` has been audited against the D-01 KEEP/DELETE rubric."
    - "DELETE-pattern comments removed; KEEP-pattern preserved (especially: WHY-comments on the WAL/snapshot format-version invariants, segment rotation thresholds, fsync-policy reasoning, and reader recovery edge cases)."
    - "`cargo test -p beava-persistence --features testing`, `cargo clippy -p beava-persistence --all-targets --all-features -- -D warnings`, `cargo fmt -p beava-persistence --check`, `cargo doc --no-deps -p beava-persistence` all stay green."
    - "No logic / structure changes."
  artifacts:
    - path: "crates/beava-persistence/src/"
      provides: "Cleaned WAL + snapshot persistence layer (writer / reader / segment / rotation / fsync_worker / record / snapshot / snapshot_header)"
  key_links:
    - from: "Plan 13.7.5-05 commit"
      to: "docs/_internal/comment-conventions.md"
      via: "Same rubric; format-version invariants are textbook KEEP examples (Phase 12.7 reset to v1)"
---

<objective>
Apply the D-01 comment-audit rubric to every `.rs` file under `crates/beava-persistence/`. Files: `writer.rs`, `reader.rs`, `segment.rs`, `rotation.rs`, `fsync_worker.rs`, `record.rs`, `snapshot.rs`, `snapshot_header.rs`, `error.rs`, `lib.rs`.

Purpose: WAL + snapshot durability layer. WHY-comments on the format-version reset (Phase 12.7 → `FORMAT_VERSION = 1`, `SNAPSHOT_FORMAT_VERSION = 1`), the rotation thresholds, and the fsync-policy choices are KEEP-pattern. Phase / plan stamps littered around the recent reset are explicit DELETE-pattern.

Output: One PR-sized commit; all green.

Doc-only-plan exemption: single `chore(13.7.5-05): comment hygiene crates/beava-persistence` commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-CONTEXT.md
@docs/_internal/comment-conventions.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit `crates/beava-persistence/src/` against the D-01 rubric</name>
  <files>crates/beava-persistence/src/**/*.rs</files>
  <read_first>
    - `docs/_internal/comment-conventions.md`.
    - `CLAUDE.md` §"Events-Only Invariant" — explains the v0 format-version reset (`FORMAT_VERSION = 1`, `SNAPSHOT_FORMAT_VERSION = 1`, `SNAPSHOT_BODY_FORMAT_VERSION = 1`). WHY-comments encoding this version-bump rationale are KEEP.
  </read_first>
  <action>
    Walk every `.rs` file under `crates/beava-persistence/src/` (use `find crates/beava-persistence/src -name '*.rs' -type f`). For each file:

    1. Read end-to-end.
    2. Classify each comment per the D-01 rubric. Apply this crate's KEEP-bias rules:
       - **Format-version constants** (`FORMAT_VERSION = 1`) — the WHY-comment explaining the v0 reset (pre-12.7 dev WALs / snapshots fail at the version-byte check) is KEEP.
       - **Rotation threshold** (e.g., 16 MiB segment cap) WHY-comments: KEEP if they explain the choice rationale.
       - **fsync-policy** WHY-comments (`F_FULLSYNC` on macOS, `fdatasync` on Linux) — KEEP. Cross-platform fsync semantics is non-obvious.
       - **CRC / checksum invariant** comments (where the comment explains what protects against what kind of corruption): KEEP.
       - **Snapshot-body section markers** that just say "// === entity_state ===" or similar — DELETE; the struct field already names the section.

    3. Apply this crate's DELETE-bias rules:
       - **Phase / plan references**: DELETE. `grep -rnE '// (Phase|Plan) [0-9]' crates/beava-persistence/src/` returns zero after.
       - **AI-tell phrases**: DELETE.
       - **Doc comments on private fns paraphrasing the signature**: DELETE.
       - **Section markers**: DELETE.
       - **Closing-brace echoes**: DELETE.

    4. Remove orphaned blank lines.

    5. **No logic changes.** Format-version constants stay numerically identical. Rotation thresholds unchanged. No re-ordering of `RecordType` variants.

    6. Single commit at end: `chore(13.7.5-05): comment hygiene crates/beava-persistence`.
  </action>
  <verify>
    <automated>cargo test -p beava-persistence --features testing 2>&amp;1 | tail -10 &amp;&amp; cargo clippy -p beava-persistence --all-targets --all-features -- -D warnings 2>&amp;1 | tail -5 &amp;&amp; cargo fmt -p beava-persistence --check &amp;&amp; cargo doc --no-deps -p beava-persistence 2>&amp;1 | tail -5 &amp;&amp; test "$(grep -rnE '// (Phase|Plan) [0-9]' crates/beava-persistence/src/ | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(grep -rnE '// (Note that|Importantly,|Here we |We then |It should be noted)' crates/beava-persistence/src/ | wc -l | tr -d ' ')" = "0" &amp;&amp; grep -rE 'FORMAT_VERSION\s*[:=]\s*1\b' crates/beava-persistence/src/ &gt; /dev/null</automated>
  </verify>
  <done>
    Every `.rs` file in `crates/beava-persistence/src/` audited; DELETE-pattern removed; format-version / fsync-policy / CRC-invariant WHY-comments preserved. Tests + clippy + fmt + doc green. Zero phase-reference comments. Zero AI-tell comments. `FORMAT_VERSION = 1` constant unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit `crates/beava-persistence/tests/` and `benches/` against the D-01 rubric</name>
  <files>crates/beava-persistence/tests/**/*.rs, crates/beava-persistence/benches/**/*.rs</files>
  <read_first>
    - `docs/_internal/comment-conventions.md`.
  </read_first>
  <action>
    Same procedure as Task 1, applied to tests + benches (if benches dir exists; check `ls crates/beava-persistence/`).

    Combine commit with Task 1: single `chore(13.7.5-05): comment hygiene crates/beava-persistence` commit.
  </action>
  <verify>
    <automated>cargo test -p beava-persistence --features testing 2>&amp;1 | tail -5 &amp;&amp; test "$(grep -rnE '// (Phase|Plan) [0-9]' crates/beava-persistence/tests crates/beava-persistence/benches 2>/dev/null | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>
    Tests + benches scrubbed alongside src. Single commit per crate.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p beava-persistence --features testing` GREEN.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` GREEN.
- `cargo fmt --all --check` GREEN.
- `cargo doc --no-deps -p beava-persistence` GREEN.
- Phase / plan / AI-tell comment patterns return zero grep matches.
- `FORMAT_VERSION = 1` unchanged.
- Single commit `chore(13.7.5-05): comment hygiene crates/beava-persistence`.
</verification>

<success_criteria>
- WAL + snapshot durability layer reads as carefully-engineered code.
- Format-version reset rationale preserved as KEEP-pattern WHY-comments.
- Workspace builds clean.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-05-SUMMARY.md`.
</output>
