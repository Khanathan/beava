# Plan 13.7.5-05 — Comment hygiene `crates/beava-persistence/`

**Status:** PASS
**Wave:** 2
**Type:** doc-only (CLAUDE.md TDD §item #7 doc-only-plan exemption — single `chore:` commit)

## Salvage note

This plan's first executor (and a retry) both stalled on the 600s stream watchdog. Per orchestrator review, the working-tree edits in `crates/beava-persistence/` were confirmed comment-only-safe (the only non-pure-comment diff lines were trailing-comment removals causing multi-line→single-line `match`-arm collapses, semantically equivalent). Salvaged into this commit. Verification deferred to end-of-Wave-2 workspace sweep.

## What landed

18 files under `crates/beava-persistence/` audited per the D-01 KEEP/DELETE rubric. Net **+202 / −299** = **−97 lines**.

Files modified:

- `src/{error.rs, fsync_worker.rs, lib.rs, reader.rs, record.rs, rotation.rs, snapshot.rs, snapshot_header.rs, writer.rs}`
- `tests/{fsync_non_blocking.rs, fsync_worker.rs, snapshot_roundtrip.rs, sync_mode.rs, writer_reader.rs}`
- `benches/{phase6_1_periodic.rs, phase6_wal.rs, phase7_snapshot_recovery.rs}`
- `Cargo.toml` (one comment paraphrase)

## Verification

- `cargo check -p beava-persistence --all-targets`: PASS
- `cargo clippy/fmt/test/doc`: deferred to end-of-Wave-2 workspace sweep

## Preserved (KEEP-pattern)

- WHY-comments on WAL/snapshot format-version invariants (Phase 12.7 reset to v1)
- Segment rotation thresholds, fsync-policy reasoning, reader recovery edge cases
- Architectural-invariant WHY-comments documenting Phase 18 WAL architecture commitments
- `SAFETY` notes on `unsafe` blocks

## Removed (DELETE-pattern)

- Phase/plan tags in module docstrings and trailing comments (e.g. `// Plan 18-11:`, `// post-12.7-05 FORMAT_VERSION`)
- Section markers and self-narration
- Paraphrase trailing comments
- Multi-line match arms collapsed to single-line after redundant trailing comment removal
