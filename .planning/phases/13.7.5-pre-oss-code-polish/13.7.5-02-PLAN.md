---
phase: 13.7.5
plan: 02
type: execute
wave: 2
depends_on: [13.7.5-01]
files_modified:
  - crates/beava-core/src/**/*.rs
  - crates/beava-core/tests/**/*.rs
  - crates/beava-core/benches/**/*.rs
autonomous: true
requirements:
  - D-01
tags:
  - comment-audit
  - rust
  - beava-core

must_haves:
  truths:
    - "Every `.rs` file under `crates/beava-core/` has been read and audited against the D-01 KEEP/DELETE rubric in `docs/_internal/comment-conventions.md`."
    - "All DELETE-pattern comments are removed; KEEP-pattern comments are preserved."
    - "`cargo test --workspace --features testing`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all --check`, and `cargo doc --no-deps -p beava-core` all stay green."
    - "Public API surface (items behind `pub` in `lib.rs` re-exports) retains its `///` doc comments — those are KEEP-pattern."
    - "Architectural-invariant WHY-comments documenting Phase 12.6 mio-only / Phase 12.7 events-only / Phase 12.8 memory-governance / Phase 18 hand-rolled-runtime are preserved per the conventions doc's 8th KEEP bullet — the phase-number reference inside such comments is allowed (not DELETE) because it points to the canonical lock-source."
    - "No logic / structure changes — diff is comment-only (whitespace adjustments to close orphaned blank-line gaps after deletion are acceptable)."
    - "Non-architectural-invariant Phase / plan / task references in comments (`// added in Phase 5`, `// Plan 12-08 …`) are removed wholesale — these remain explicit DELETE-pattern under D-01."
  artifacts:
    - path: "crates/beava-core/src/"
      provides: "AI-slop-free Rust source for the operator catalogue + registry + apply machinery"
  key_links:
    - from: "Plan 13.7.5-02 commit"
      to: "docs/_internal/comment-conventions.md"
      via: "Executor reads conventions doc once at start; classifies every comment per D-01 rubric + 8th architectural-invariant KEEP bullet"
---

<objective>
Apply the D-01 comment-audit rubric to every `.rs` file under `crates/beava-core/`. Pure hygiene — remove AI-slop / paraphrase / phase-reference / section-marker / closing-brace-echo comments. Keep WHY / SAFETY / public-API doc / invariant / workaround comments, including the architectural-invariant WHY-comments that document the four locked Phase 12.6 / 12.7 / 12.8 / 18 commitments.

Purpose: `beava-core` is the most stable + biggest-surface crate (244 `.rs` files in workspace; ~80 in this crate). Highest scrub-payoff target. First public-eyeballs target after `pip install beava`.

Output: One PR-sized commit; `cargo test` + `cargo clippy -D warnings` + `cargo fmt --check` + `cargo doc` all green.

Doc-only-plan exemption per **CLAUDE.md §TDD Discipline item #7** (single `chore:` commit; no paired `test:` commit required for pure hygiene). Test suites must stay GREEN — that is the regression check this plan provides.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-CONTEXT.md
@docs/_internal/comment-conventions.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit every `.rs` file under `crates/beava-core/src/` against the D-01 rubric, KEEP-bias on architectural invariants</name>
  <files>crates/beava-core/src/**/*.rs</files>
  <read_first>
    - `docs/_internal/comment-conventions.md` (the rubric — read once, apply per-comment; pay particular attention to the 8th architectural-invariant KEEP bullet).
    - `crates/beava-core/src/lib.rs` (note `pub` re-exports — those items' doc comments are KEEP-pattern public API).
    - `CLAUDE.md` §"mio-only Hot-Path Invariant" + §"Events-Only Invariant" — context for the architectural-invariant KEEP-bias rules below.
  </read_first>
  <action>
    **Architectural-invariant KEEP-bias for `beava-core` (mirrors the per-crate KEEP-bias sections in Plans 03/04/05):**

    `beava-core` houses the operator catalogue + registry + apply machinery; the relevant in-scope architectural invariants are:

    - **Phase 12.7 events-only** — `OpNode` / `RecordType` / register-prelude code in `beava-core` may carry WHY-comments referencing the events-only commitment (e.g., `// no Table* variants — Phase 12.7 events-only invariant; see CLAUDE.md`). These are explicit KEEP per the conventions doc's 8th KEEP bullet.
    - **Phase 12.8 memory-governance** — `agg_state.rs` / boxed-AggOp size invariant (80B per `size_of::<AggOp>()`), lifetime-bounds table (`reservoir_sample` `samples=N` not `k=N`), per-entity ~7KB budget. WHY-comments documenting these are explicit KEEP.
    - **Polars-style op-name lock** (Phase 13.4 ADR-002) — comments documenting the JSON-wire-boundary op-name choice (`avg→mean`, `variance→var`, `stddev→std`, `count_distinct→n_unique`, `percentile→quantile`) are KEEP — they encode an externally-visible API commitment.
    - **Operator handcrafting** (project-memory `project_stateful_architecture` "handcrafted-per-op rule still holds for the operator catalogue") — WHY-comments explaining why a particular op uses Welford / HyperLogLog / Count-Min Sketch / etc. are KEEP.

    The Phase 12.6 mio-only invariant is out-of-scope for this crate (relevant in `beava-server` Plan 03); Phase 18 hand-rolled-runtime is also out-of-scope here. If you encounter a WHY-comment referencing them, KEEP it (the rubric says so) but flag any orphaned ones for context.

    Walk every `.rs` file under `crates/beava-core/src/` (use `find crates/beava-core/src -name '*.rs' -type f` to enumerate; ~30 files). For each file:

    1. Read the file end-to-end.
    2. For every comment line (`//`, `///`, `//!`, `/* */`):
       - DELETE if it matches a D-01 DELETE pattern (paraphrase / private-fn `///` / section marker / self-narration / restated signature / non-architectural phase-or-plan reference / AI-tell / multi-paragraph docstring on tiny fn / closing-brace echo).
       - KEEP if it matches a D-01 KEEP pattern (WHY / SAFETY contract / public API doc / invariant / workaround-with-link / hidden-state warning) OR the 8th architectural-invariant KEEP bullet (Phase 12.6 / 12.7 / 12.8 / 18 commitment with phase reference pointing to the lock-source).
       - When in doubt: DELETE. The CLAUDE.md heuristic defaults to no comments. EXCEPT for ambiguous architectural-invariant comments — when in doubt KEEP and note for review.

    3. Apply specifically to this crate's hot spots:
       - **Non-architectural Phase / plan references:** explicit DELETE — comments like `// added in Phase 5 to support msgpack` or `// Plan 12-08 added this guard` go away. The architectural-invariant comments referencing Phase 12.6/12.7/12.8/18 STAY (per 8th KEEP bullet).
       - **AI-tell phrases:** `grep -rE '// (Note that|Importantly|Here we|We then|It should be noted)' crates/beava-core/src/` should return zero matches after.
       - **Section markers:** `// === ...`, `// ─── ...`, `// --- ...` lines used as visual headers — DELETE.
       - **`/// `-doc on private fns:** if a fn / struct / enum has no `pub` visibility AND its `///` doc just paraphrases the signature, DELETE. Keep `///` only on items the public surface re-exports (check `lib.rs` re-exports — anything re-exported from `lib.rs::pub use ...` keeps its `///`).
       - **SAFETY contracts on `unsafe` blocks:** explicit KEEP — never delete `// SAFETY:` even if it looks paraphrase-y.
       - **Closing-brace echoes:** `} // end of impl Foo`, `} // for entity in entities` — DELETE.

    4. Remove orphaned blank lines left by comment deletion (collapse 3+ consecutive blank lines to 1; a single blank line between items is canonical Rust style).

    5. **No logic changes.** No refactoring. No renaming. No re-ordering items. Diff must show comment + whitespace deletions only (and possibly `cargo fmt` reflows after blank-line collapse).

    6. After the edits, run the verification recipe from Task 2 BEFORE committing. If anything fails → revert the offending change and re-classify.

    7. **Sanity check (before/after non-trivial WHY-comment count):** Before starting Task 1, capture a baseline WHY-comment count by counting lines that pass the D-01 KEEP heuristic (single-line `// ` comments that explain WHY rather than restate WHAT). Use this exact recipe to capture both baseline and post-scrub counts:

       ```bash
       # Baseline (before any edits): captures non-trivial single-line comments that look WHY-shaped
       # Heuristic match: comment line containing one of the WHY-signal markers
       BASELINE_WHY=$(grep -rnE '// [^/].*(must|safety|invariant|because|why|reason|gotcha|workaround|warning|caution|do not|never|prevents|ensures|guarantees|requires|TODO|FIXME|HACK|NOTE:)' crates/beava-core/src/ 2>/dev/null | wc -l | tr -d ' ')
       echo "Pre-scrub WHY-shaped comment lines (baseline): $BASELINE_WHY" > /tmp/13.7.5-02-sanity.txt

       # ... do the scrub ...

       # Post-scrub: same recipe
       POST_WHY=$(grep -rnE '// [^/].*(must|safety|invariant|because|why|reason|gotcha|workaround|warning|caution|do not|never|prevents|ensures|guarantees|requires|TODO|FIXME|HACK|NOTE:)' crates/beava-core/src/ 2>/dev/null | wc -l | tr -d ' ')
       echo "Post-scrub WHY-shaped comment lines: $POST_WHY" >> /tmp/13.7.5-02-sanity.txt
       echo "Delta: $((POST_WHY - BASELINE_WHY))" >> /tmp/13.7.5-02-sanity.txt
       cat /tmp/13.7.5-02-sanity.txt
       ```

       **Interpretation rule (the sanity check, not a verify gate — surface to user not block):**
       - Expected: `POST_WHY ≥ BASELINE_WHY × 0.85` (we should keep most WHY-comments; some borderline ones may be reclassified DELETE — losing >15% of WHY-shaped comments suggests over-aggressive deletion).
       - If `POST_WHY < BASELINE_WHY × 0.5` (lost >50% of WHY-shaped comments): STOP, surface to user — likely classified architectural-invariant comments as DELETE.
       - Record the before / after / delta in the commit message body (not the subject — the commit subject stays the conventional `chore(13.7.5-02): comment hygiene crates/beava-core`).

    8. Commit with message: `chore(13.7.5-02): comment hygiene crates/beava-core` (subject) + a body line `WHY-comment baseline: <N>; post-scrub: <M>; delta: <D>` (no AI attribution per `feedback_no_ai_attribution_in_commits` memory).

    Suggested workflow inside the agent loop:
    ```bash
    # Capture baseline first (per Step 7 above)

    # Enumerate
    find crates/beava-core/src -name '*.rs' -type f | sort

    # For each file: Read → classify → Edit. Use the Edit tool, not sed.
    # Edit tool calls should each carry an old_str / new_str pair for one comment block at a time
    # so the diff is reviewable.
    ```
  </action>
  <verify>
    <automated>cargo test -p beava-core --features testing 2>&amp;1 | tail -20 &amp;&amp; cargo clippy -p beava-core --all-targets --all-features -- -D warnings 2>&amp;1 | tail -10 &amp;&amp; cargo fmt -p beava-core --check &amp;&amp; cargo doc --no-deps -p beava-core 2>&amp;1 | tail -10 &amp;&amp; test -f /tmp/13.7.5-02-sanity.txt &amp;&amp; grep -q 'Pre-scrub' /tmp/13.7.5-02-sanity.txt &amp;&amp; grep -q 'Post-scrub' /tmp/13.7.5-02-sanity.txt &amp;&amp; grep -q 'Delta:' /tmp/13.7.5-02-sanity.txt &amp;&amp; test "$(grep -rnhE '// (Note that|Importantly,|Here we |We then |It should be noted)' crates/beava-core/src/ | wc -l | tr -d ' ')" = "0" &amp;&amp; NON_INV_PHASE=$(grep -rnE '// (Phase|Plan) [0-9]' crates/beava-core/src/ | grep -vE '(Phase 12\.6|Phase 12\.7|Phase 12\.8|Phase 18|mio-only|events-only|memory-governance|hand-rolled-runtime)' | wc -l | tr -d ' ') &amp;&amp; test "$NON_INV_PHASE" = "0" &amp;&amp; echo "non-architectural-invariant phase/plan refs: 0"</automated>
  </verify>
  <done>
    Every `.rs` file under `crates/beava-core/src/` has been audited; DELETE-pattern comments removed; KEEP-pattern preserved (including architectural-invariant WHY-comments per the 8th conventions-doc bullet). Workspace tests + clippy + fmt + doc all green. Zero non-architectural-invariant `// Phase N`-shaped comments remain (the architectural-invariant Phase 12.6/12.7/12.8/18 references are explicitly allowed). Zero AI-tell-phrase comments remain. Sanity check (`/tmp/13.7.5-02-sanity.txt`) captures before/after WHY-comment counts. Diff is comment-only (no logic touch).
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit `crates/beava-core/tests/` and `crates/beava-core/benches/` against the same rubric</name>
  <files>crates/beava-core/tests/**/*.rs, crates/beava-core/benches/**/*.rs</files>
  <read_first>
    - `docs/_internal/comment-conventions.md` (8th architectural-invariant KEEP bullet applies in tests too — `op_lifetime_bounds.rs` etc. carry tripwire WHY-comments).
  </read_first>
  <action>
    Same procedure as Task 1, applied to the test + benchmark trees. Same architectural-invariant KEEP-bias applies (e.g., `crates/beava-core/tests/per_entity_size_dump.rs::aggop_size_within_cap` has a Phase 12.9 invariant tripwire — that WHY-comment is explicit KEEP).

    Test files have one nuance: a `// Test 3: <one-line description>` is borderline — the test name (`#[test] fn test_foo_bar`) usually carries the description. If the comment merely restates the fn name → DELETE. If it explains a non-obvious assertion choice ("we assert > 0 not == 1 because the windowed reset is non-deterministic between runs") → KEEP.

    Bench files: criterion convention has bench-group setup comments; if they say what the code does → DELETE. If they explain why a particular workload shape was chosen ("zipfian alpha=1.0 matches Phase 19 fraud-team distribution") → KEEP.

    Commit at end of Task 1 + 2 combined: single `chore(13.7.5-02): comment hygiene crates/beava-core` commit covering both src + tests + benches. Do NOT split into two commits — D-02 says "one PR-sized commit per crate".
  </action>
  <verify>
    <automated>cargo test -p beava-core --features testing 2>&amp;1 | tail -5 &amp;&amp; NON_INV_PHASE_TESTS=$(grep -rnE '// (Phase|Plan) [0-9]' crates/beava-core/tests crates/beava-core/benches 2>/dev/null | grep -vE '(Phase 12\.6|Phase 12\.7|Phase 12\.8|Phase 12\.9|Phase 18|mio-only|events-only|memory-governance|hand-rolled-runtime)' | wc -l | tr -d ' ') &amp;&amp; test "$NON_INV_PHASE_TESTS" = "0"</automated>
  </verify>
  <done>
    Tests and benches scrubbed alongside src. All `cargo test -p beava-core` runs pass. Benchmarks still compile. Single commit per the D-02 contract. Architectural-invariant WHY-comments (incl. Phase 12.9 AggOp-size tripwire) preserved.
  </done>
</task>

</tasks>

<verification>
- `cargo test --workspace --features testing` GREEN (Task 1 + Task 2 don't break sibling crates).
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` GREEN.
- `cargo fmt --all --check` GREEN.
- `cargo doc --no-deps --workspace` GREEN — public-API doc comments preserved.
- Non-architectural-invariant `// Phase N` / `// Plan N` comments return zero matches in `crates/beava-core/`. Architectural-invariant Phase 12.6/12.7/12.8/12.9/18 references are KEEP-pattern and may remain.
- `grep -rnE '// (Note that|Importantly,|Here we |We then |It should be noted)' crates/beava-core/` returns zero matches.
- `/tmp/13.7.5-02-sanity.txt` exists with Pre-scrub / Post-scrub / Delta lines (sanity check, not a hard gate — informational for the user).
- Single commit `chore(13.7.5-02): comment hygiene crates/beava-core` with WHY-count delta in body (no AI attribution trailer).
</verification>

<success_criteria>
- `crates/beava-core/` no longer reads as AI-generated on first scroll.
- All v0 invariants (mio-only, events-only, memory-governance) tests still pass.
- Workspace builds clean.
- Diff is comment-only — no logic touches.
- Architectural-invariant WHY-comments (8th KEEP bullet) preserved — readers retain the lock-source pointers.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-02-SUMMARY.md`.
</output>
