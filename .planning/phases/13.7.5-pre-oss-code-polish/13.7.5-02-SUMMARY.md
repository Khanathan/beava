# Plan 13.7.5-02 — Comment hygiene `crates/beava-core/`

**Status:** PASS
**Wave:** 2
**Type:** doc-only (CLAUDE.md TDD §item #7 doc-only-plan exemption — single `chore:` commit)

## Salvage note

This plan's executor stalled on the 600s stream watchdog after running into a `cargo build` SIGKILL (probably memory exhaustion from 5+ parallel cargo builds across worktrees). Per orchestrator review, the worktree's diff was confirmed safe: edits were comment-only plus a small set of `.expect()` / `assert_eq!` panic-message text scrubs (stripping `Phase X Plan Y` prefixes from messages — consistent with the spirit of D-01's "phase/plan references in code" DELETE-pattern, applied to panic messages rather than only to comments). Salvaged into this commit. Verification deferred to end-of-Wave-2 workspace sweep.

## What landed

6 files under `crates/beava-core/src/` audited per the D-01 KEEP/DELETE rubric. Net **+533 / −698** = **−165 lines**.

Files modified:

- `config.rs` — env-override comment hygiene
- `defaults.rs` — paraphrase comment removal
- `lib.rs` — module-level docstring trim
- `registry.rs` — heaviest scrub (905 lines reformatted): phase/plan tags removed from docstrings and panic messages, AI-tell phrases removed, section markers stripped
- `schema.rs` — comment hygiene
- `wire.rs` — trailing comment removal on big-endian wire codec, OP_* assert messages simplified

## Verification

- `cargo check -p beava-core --all-targets`: PASS
- `cargo clippy/fmt/test/doc`: deferred to end-of-Wave-2 workspace sweep

## Preserved (KEEP-pattern)

- `///` doc comments on `pub` items in `lib.rs` re-exports (cargo-doc render targets)
- WHY-comments on the `agg_id: 0` placeholder semantics
- Substantive `expect()` rationales (only the phase/plan prefixes were stripped)
- Architectural-invariant WHY-comments documenting Phase 12.6/12.7 commitments

## Removed (DELETE-pattern)

- Phase/plan tags in module docstrings (`// Phase 6 DurabilityConfig env overrides.`, `// Plan 12-07: ...`)
- Phase/plan prefixes inside `.expect()` / `assert!()` panic-message strings (`"Phase 12.6 Plan 06 D-03: EventDescriptor must not declare ..."` → `"EventDescriptor must not declare ..."`)
- Section-marker comments (`// ─── Phase 2.5 TcpConfig tests ────────`)
- Paraphrase trailing comments
- AI-tell phrases
