---
phase: 13.7.5
plan: 07
type: execute
wave: 2
depends_on: [13.7.5-01]
phase_depends_on: [13.5.1]
files_modified:
  - python/beava/__init__.py
  - python/beava/_agg.py
  - python/beava/_app.py
  - python/beava/_col.py
  - python/beava/_demo.py
  - python/beava/_embed.py
  - python/beava/_errors.py
  - python/beava/_events.py
  - python/beava/_table.py
  - python/beava/_transport.py
  - python/beava/_wire.py
  - python/beava/cli/**/*.py
  - python/beava/demos/**/*.py
  - python/beava/test/**/*.py
autonomous: true
requirements:
  - D-01
tags:
  - comment-audit
  - python
  - python-sdk
  - file-conflict-with-13.5.1

must_haves:
  truths:
    - "Plan 13.7.5-07 has not started until Phase 13.5.1 (transport-impl + decorator-hardening) is fully CLOSED. 13.5.1 Plan 04 modifies `python/beava/_table.py` and Plan 05 modifies `python/beava/_transport.py` + `python/beava/_app.py` — those plans must land first to avoid merge conflict."
    - "Every `.py` file under `python/beava/` has been audited against the D-01 KEEP/DELETE rubric in `docs/_internal/comment-conventions.md`."
    - "DELETE-pattern comments + docstrings removed; KEEP-pattern preserved (especially: WHY-comments on the strict TypeError on @bv.table empty annotation per Phase 13.5.1 D-01, the no-event-time pivot rationale, and public-API Sphinx-rendered docstrings on `bv.event` / `bv.table` / `bv.col` / `bv.lit` / `bv.App`)."
    - "`pytest python/tests -x` and `mypy --strict python/beava` both stay green."
    - "All v0 acceptance tests still pass (the 68 tests greened by 13.5.1 stay green after the comment scrub)."
    - "No logic / structure changes — diff is comment + docstring deletions only (whitespace adjustments allowed)."
  artifacts:
    - path: "python/beava/"
      provides: "Cleaned Python SDK source (decorators, pipeline DSL, transports, App client, demo loader)"
  key_links:
    - from: "Plan 13.7.5-07 commit"
      to: "docs/_internal/comment-conventions.md"
      via: "Same rubric; Sphinx-rendered public-API docstrings on bv.* exports are KEEP-pattern"
    - from: "Plan 13.7.5-07 start gate"
      to: "Phase 13.5.1 closure"
      via: "13.5.1 Plans 04+05 touch _table.py, _transport.py, _app.py — file conflict; sequencing checkpoint enforces wait"
---

<objective>
Apply the D-01 comment-audit rubric to every `.py` file under `python/beava/`. Files: `__init__.py`, `_agg.py`, `_app.py`, `_col.py`, `_demo.py`, `_embed.py`, `_errors.py`, `_events.py`, `_table.py`, `_transport.py`, `_wire.py`, plus `cli/`, `demos/`, `test/` subpackages.

**Sequencing gate:** This plan MUST NOT start until Phase 13.5.1 closes. 13.5.1's Plans 04 + 05 (per `.planning/phases/13.5.1-transport-impl-decorator-hardening/`) touch `_table.py`, `_transport.py`, `_app.py` — concurrent edits would conflict. Per CONTEXT.md sibling-parallel-context note: "DO NOT plan any code-touching tasks in `python/beava/_transport.py` / `_app.py` / `_table.py` / v0 acceptance tests during the window when 13.5.1's Plan 05 might also touch them." This plan honors that by carrying `phase_depends_on: [13.5.1]` in the frontmatter.

Purpose: Python SDK is the public-eyeballs surface for `pip install beava` users. Sphinx-rendered docstrings on the public API are KEEP. AI-slop comments inside private helpers are DELETE.

Output: One PR-sized commit; pytest + mypy --strict green.

Doc-only-plan exemption: single `chore(13.7.5-07): comment hygiene python/beava` commit. (No red-then-green required for pure hygiene.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-CONTEXT.md
@.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-CONTEXT.md
@docs/_internal/comment-conventions.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight gate — confirm Phase 13.5.1 is CLOSED</name>
  <what-built>Sequencing checkpoint. No code change yet. The orchestrator must confirm Phase 13.5.1 closed before any executor touches `python/beava/_table.py`, `_transport.py`, or `_app.py`.</what-built>
  <how-to-verify>
    Run from repo root:
    ```bash
    test -f .planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-SUMMARY.md && echo "13.5.1 CLOSED — proceed" || echo "13.5.1 NOT CLOSED — wait"
    ```
    Also sanity-check the workspace is clean and tests are green BEFORE this plan starts:
    ```bash
    git status -uno
    cd python && pytest tests/v0 -x  # 68 v0 acceptance tests must be GREEN (13.5.1 deliverable)
    mypy --strict python/beava
    ```
    If 13.5.1 SUMMARY missing OR v0 acceptance tests not 68/68 GREEN OR mypy not GREEN → BLOCK; do not proceed. Surface the blocker to the user.
  </how-to-verify>
  <resume-signal>Type "13.5.1 closed and v0 tests green — proceed" to unblock the comment scrub.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Audit every `.py` file under `python/beava/` against the D-01 rubric</name>
  <files>python/beava/**/*.py</files>
  <read_first>
    - `docs/_internal/comment-conventions.md` (rubric).
    - `python/beava/__init__.py` — note the public surface re-exports (`bv.event`, `bv.table`, `bv.col`, `bv.lit`, `bv.App`, `bv.Optional`, `bv.Field`, etc.). These items' module-level + decorator-level docstrings are KEEP-pattern (Sphinx renders them on `docs.beava.dev`).
  </read_first>
  <action>
    Walk every `.py` file under `python/beava/` (use `find python/beava -name '*.py' -type f`). For each file:

    1. Read end-to-end.
    2. For every comment / docstring:
       - DELETE if it matches a D-01 DELETE pattern (paraphrase / restated signature / phase-or-plan reference / AI-tell / multi-paragraph docstring on a 5-line function / closing-bracket echo / section marker).
       - KEEP if it matches a D-01 KEEP pattern (WHY / public-API docstring rendered by Sphinx / invariant / workaround / hidden-state warning).

    3. Apply Python-specific KEEP-bias rules:
       - **Public-API docstrings** on `bv.event`, `bv.table`, `bv.col`, `bv.lit`, `bv.App` (and `bv.App.register`, `bv.App.push`, `bv.App.push_sync`, `bv.App.get`, `bv.App.mget`, `bv.App.set`, `bv.App.flush`, `bv.App.close`) — KEEP. These are the surface `pip install beava` users see in REPL `help()` and Sphinx renders.
       - **Strict-TypeError rationale** in `_table.py::_resolve_upstream_proxies` (per 13.5.1 D-01) — the comment explaining why the unannotated-parameter case raises TypeError instead of falling back to a generic proxy is KEEP. (Reader can't infer the design choice from the raise alone.)
       - **No-event-time-pivot rationale** comments anywhere in the SDK (e.g., `_wire.py` docstring noting `event_time` is not on the wire because of the 2026-04-30 pivot) — KEEP.
       - **PEP 563 forward-ref handling** comments in `_events.py` / `_table.py` (annotation evaluation under `from __future__ import annotations`) — KEEP. Non-obvious Python typing edge case.
       - **MagicMock anti-pattern call-out** in `python/tests/v0/conftest.py` (introduced by 13.5.1 D-05) if it explains why the v0 fixture uses real engine spawn instead of mocks — KEEP.

    4. Apply Python-specific DELETE-bias rules:
       - **Phase / plan references** in body comments: DELETE.
       - **AI-tell phrases** in docstrings or inline comments: DELETE.
       - **Restated signature docstrings** like `"""Get a feature for a key. Args: key (str): the key. Returns: dict: the feature."""` on a 3-line getter: DELETE (the type annotations already encode this).
       - **Doc comments paraphrasing private helpers** (`_resolve_upstream_proxies`, `_to_register_json`): if the docstring just paraphrases what the code does, DELETE; if it explains a non-obvious decision (like the strict-TypeError choice above), KEEP.
       - **Section markers** (`# === Setup ===`, `# --- Helpers ---`): DELETE.
       - **Self-narration sequences**: DELETE.

    5. **Avoid touching the v0 acceptance test files (`python/tests/v0/test_*.py`)** unless the comments inside them are obvious AI-slop. Those tests are 13.5.1's deliverable; minimize churn. If a v0 acceptance test has clear DELETE-pattern comments (paraphrase / phase-tag), remove them; otherwise leave alone. The conftest.py and _helpers.py files in that dir ARE in scope — those are infrastructure.

    6. Remove orphaned blank lines. Maintain PEP-8 spacing.

    7. **No logic changes.** No symbol renames. No type-annotation edits. No new imports / removed imports beyond ones that become unused after a docstring deletion (e.g., if a `from typing import TypeAlias` was only used by a docstring — unlikely; flag for review if it happens).

    8. Single commit at end: `chore(13.7.5-07): comment hygiene python/beava`.

    Workflow:
    ```bash
    find python/beava -name '*.py' -type f | sort
    # For each file: Read → classify → Edit. Use Edit tool, not sed.
    # After ALL files done: pytest + mypy.
    ```
  </action>
  <verify>
    <automated>cd python &amp;&amp; pytest tests -x 2>&amp;1 | tail -10 &amp;&amp; cd .. &amp;&amp; mypy --strict python/beava 2>&amp;1 | tail -5 &amp;&amp; test "$(grep -rnE '# (Phase|Plan) [0-9]' python/beava/ | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(grep -rnE '# (Note that|Importantly,|Here we |We then |It should be noted)' python/beava/ | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>
    Every `.py` file in `python/beava/` audited; DELETE-pattern removed; public-API Sphinx-rendered docstrings + 13.5.1 strict-TypeError WHY-comment + no-event-time-pivot rationale preserved. `pytest python/tests -x` GREEN (including all 68 v0 acceptance tests). `mypy --strict python/beava` GREEN. Zero phase-reference comments. Zero AI-tell comments.
  </done>
</task>

</tasks>

<verification>
- `pytest python/tests -x` GREEN — all v0 acceptance tests + internal tests pass.
- `mypy --strict python/beava` GREEN — type-check passes.
- Phase / plan / AI-tell comment patterns return zero grep matches in `python/beava/`.
- Single commit `chore(13.7.5-07): comment hygiene python/beava` covers all changes.
- File-conflict gate honored: 13.5.1 confirmed CLOSED before any python/beava/ edit.
</verification>

<success_criteria>
- Python SDK source reads as engineered code, not phase-stamped scaffolding.
- Public-API docstrings preserved for Sphinx.
- 68 v0 acceptance tests still pass.
- mypy --strict still passes.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-07-SUMMARY.md`.
</output>
