---
phase: 13.7.5
plan: 09
subsystem: pre-oss-code-polish / coverage-audit
tags: [coverage-matrix, audit, gap-classification, doc-only]
duration_minutes: 90
completed: 2026-05-04
requirements:
  - D-03
key_files:
  created:
    - .planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-MATRIX.csv
    - .planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-GAPS.md
    - .planning/phases/13.7.5-pre-oss-code-polish/13.7.5-09-SUMMARY.md
  modified:
    - .planning/ideas/v0.1-deferrals.md
decisions:
  - "Produced 54 op rows reflecting `agg_op.rs::AggKind` enum reality. `docs/operators/index.md` heading claims 53 unique variants — heading is off-by-one (family-counts in same doc sum to 54)."
  - "All 54 ops are `covered` — every AggKind has Rust unit + Python integration tests. No remaining op-direct-test gap."
  - "All 6 MUST-FIX rows are SDK/transport flows (Plan 11 territory); 0 Rust gaps for Plan 10."
  - "5 DEFER items routed to `v0.1-deferrals.md` under new section dated 2026-05-04."
---

# Phase 13.7.5 Plan 09: Coverage Matrix + Gap Classification Summary

Built the v0 ship-pitch coverage matrix per CONTEXT.md D-03 — one CSV row per surface item × 7 test-status dimensions × verdict — and partitioned the MUST-FIX bucket into Rust gaps (0 rows) and Python gaps (6 rows) for Plans 10/11. Routed 5 DEFER rows to `.planning/ideas/v0.1-deferrals.md`.

## What landed

- **`COVERAGE-MATRIX.csv`** — 78 rows × 12 columns. 54 op rows, 10 endpoint rows, 8 ship-pitch flow rows, 6 architectural-invariant rows. 71 covered / 6 MUST-FIX / 1 DEFER.
- **`COVERAGE-GAPS.md`** — Rust-gaps bucket (empty), Python-gaps bucket (6 rows mapped to 4 proposed test files), DEFER bucket (5 items routed).
- **`v0.1-deferrals.md`** — appended new section `## Routed from Phase 13.7.5 coverage matrix (2026-05-04)` with 5 routed surface items: SRV-API-05 push-batch, WAL-RECOV-EDGE, SNAPSHOT-UPGRADE, CROSS-SDK-CONFORM, CROSS-SDK-FLOWS.

## Counts

- CSV row count: **78** (matrix-aware via `csv.DictReader`)
- Op rows: **54** (matches `agg_op.rs::AggKind` enum)
- Endpoint rows: **10** (6 v0 client opcodes + push-sync + ping + health + metrics)
- Ship-pitch flow rows: **8** (force / dry_run / reset / schema-evolution / URL-dispatch / reconnect / max_retries=0 / 53/53-direct-test meta-row)
- Architectural-invariant rows: **6** (mio-only / events-only / cold-TTL / lifetime-bounds / bucket-reclaim / WAL-recovery-happy)
- Verdict counts: **covered: 71**, **MUST-FIX: 6**, **DEFER: 1**
- Rust-gap MUST-FIX (Plan 10): **0**
- Python-gap MUST-FIX (Plan 11): **6** (force / dry_run / schema-evolution / URL-dispatch / reconnect / max_retries=0)
- DEFER routed to v0.1-deferrals.md: **5** (SRV-API-05 + 4 cross-cutting items per D-03)

## MUST-FIX → Plan-11 Python test files (4 files, 11 cases)

| Plan-11 file | Cases | Surface |
|---|---|---|
| `python/tests/v0/test_register_flags.py` | 2 | force=True + dry_run=True register flow |
| `python/tests/v0/test_schema_evolution.py` | 3 | additive→200 + destructive→409 + changed-field-type→409 |
| `python/tests/v0/test_url_dispatch.py` | 4 | http→HTTP + tcp→TCP + no-URL→embed + unknown→ValueError |
| `python/tests/v0/test_reconnect.py` | 2 | dropped-connection-fail-fast + max_retries=0 default |

## Deviations from Plan

### [Rule 1 - Bug] op count is 54 (reality), not 53 (plan/doc claim)

- **Found during:** Task 1 (matrix construction)
- **Issue:** Plan 13.7.5-09 verification asserts `OP_COUNT == "53"`, citing `docs/operators/index.md`. The doc heading says "53 unique AggKind variants + ema alias = 54 page paths". Verified counts: `agg_op.rs::AggKind` enum has 54 variants; `parse_agg_kind` recognises 55 op-strings (54 unique + `ema` alias for `Ewma`); the doc's own family sub-totals (8 core + 5 sketch + 5 point/ordinal + 10 recency + 6 decay + 9 velocity + 11 buffer-geo) sum to 54. Heading is off-by-one.
- **Fix:** Produced 54 op rows reflecting reality. Documented in `COVERAGE-GAPS.md` op-coverage-state preamble + here. Plan-checker `OP_COUNT == "53"` strict check fails by exactly +1; `OP_COUNT >= 53` (plan's `min_rows: 70`-style spirit) passes.
- **Files modified:** `COVERAGE-MATRIX.csv` (54 op rows), `COVERAGE-GAPS.md` (preamble note).
- **Recommended follow-up (out of scope for this plan):** Edit `docs/operators/index.md` heading from "53 unique AggKind variants" to "54 unique AggKind variants" (cosmetic; affects no behavior). Tracked as a comment for the next docs-touching plan.

## Self-Check: PASSED

Files created and verified to exist:
- `.planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-MATRIX.csv` — FOUND (78 rows, 12 columns)
- `.planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-GAPS.md` — FOUND (Rust gaps + Python gaps + DEFER sections)
- `.planning/phases/13.7.5-pre-oss-code-polish/13.7.5-09-SUMMARY.md` — this file
- `.planning/ideas/v0.1-deferrals.md` — modified (new "Routed from Phase 13.7.5 coverage matrix" section)

Verification commands:
- Header check: PASS
- Row count >= 70: PASS (78)
- Op count == 53: FAIL (54; documented Rule 1 deviation — reality is 54 per `agg_op.rs::AggKind`)
- No unresolved `?` cells: PASS (0)
- All verdicts ∈ {covered, MUST-FIX, DEFER}: PASS
- COVERAGE-GAPS sections (Rust / Python / DEFER): PASS
- v0.1-deferrals.md appended section: PASS

## Commit

Will be recorded post-commit: `docs(13.7.5-09): coverage matrix + gap classification` covering CSV + GAPS.md + v0.1-deferrals.md append + this SUMMARY.md. Hoang Phan author/committer; no `Co-Authored-By: Claude` trailer per memory `feedback_no_ai_attribution_in_commits`.
