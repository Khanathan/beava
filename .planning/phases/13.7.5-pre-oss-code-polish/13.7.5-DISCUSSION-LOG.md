# Phase 13.7.5 — Discussion Log (batch-discuss 2026-05-04)

Captured during a 4-phase batch-discuss session. This log captures the 3 decisions affecting Phase 13.7.5.

## 7.5-A — Comment-audit KEEP/DELETE checklist

**Question:** Lock the idea-doc KEEP/DELETE comment-audit checklist as the Plan 13.7.5-01 conventions doc?

**Options presented:**
- (a) Accept idea-doc heuristic as-is (Recommended).
- (b) Tighten — also forbid `///` doc comments on non-public items.
- (c) Loosen — keep paraphrase comments if file <50 LOC.

**User selection:** (a) Accept idea-doc heuristic as-is.

**Notes / follow-ups:** DELETE patterns: paraphrase / section markers / phase refs / AI-tells / multi-paragraph docstrings on tiny functions / closing-brace echoes. KEEP patterns: WHY / workaround-with-link / invariants / SAFETY contracts / public-API doc comments / hidden-state warnings.

---

## 7.5-B — Per-component plan ordering

**Question:** Lock the per-component plan structure for the comment audit?

**Options presented:**
- (a) 8 plans as captured + parallelize compatible ones (Recommended) — conventions serial, then 7 components in parallel worktrees.
- (b) Sequential 8 plans (no parallelism) — slower wall-clock; simpler review.
- (c) Combine into 4 fatter plans — fewer plans; broader blast radius each.

**User selection:** (a) 8 plans + parallelize Wave 2.

**Notes / follow-ups:** Plan 12 (cross-SDK conformance harness from idea doc) is intentionally OMITTED per 7.5-C narrowing — moves to v0.1+ deferrals. Final plan count: 12 (1 conventions + 7 component audits + 1 matrix + 2 gap fill + 1 closure).

---

## 7.5-C — Coverage-matrix MUST-FIX vs DEFER bar

**Question:** What counts as MUST-FIX (blocks launch) vs DEFER (v0.1+) on the coverage matrix?

**Options presented:**
- (a) Anything on the v0 ship-pitch surface (Recommended) — force/dry_run/reset/schema-evolution/URL-dispatch/retry/per-op direct test.
- (b) Wider — also include cross-SDK conformance per-op.
- (c) Narrower — only the 4 register flags + reset.

**User selection:** (a) v0 ship-pitch surface.

**Notes / follow-ups:** Cross-SDK conformance per-op (Plan 12 in idea doc) is dropped from this phase and tracked in v0.1+ deferrals. WAL replay edge cases beyond happy path + snapshot upgrade-path testing also DEFER.

---

## Deferred ideas surfaced during discuss

- Cross-SDK conformance harness (per-op equivalence Python vs TS vs Go) — v0.1+; tracked in `.planning/ideas/v0.1-deferrals.md`.
- WAL replay correctness for every `RecordType` variant beyond happy path — v0.1+.
- Snapshot `FORMAT_VERSION` upgrade-path testing — v0.1+.
- Doc-style audit (voice consistency across `docs/`) — v0.1 if needed.

## Claude's Discretion (planner picks)

- Final path for the conventions doc (Plan 13.7.5-01) — likely `docs/_internal/comment-conventions.md` or `.planning/decisions/comment-conventions.md`; planner picks.
- Per-component commit shape (one PR-sized commit per crate) and exact deletion thresholds — planner picks per actual file inspection.
- Coverage matrix CSV path — likely `.planning/phases/13.7.5-pre-oss-code-polish/COVERAGE-MATRIX.csv`.
- Per-component verification recipe — planner picks (likely `cargo test --workspace --features testing` + `cargo clippy -D warnings` + `cargo fmt --check` + `cargo doc` per crate).

---

*Discussion conducted via AskUserQuestion (single-question turns) on 2026-05-04. 3 decisions locked.*
