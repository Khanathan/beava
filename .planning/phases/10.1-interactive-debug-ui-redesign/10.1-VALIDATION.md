---
phase: 10.1
slug: interactive-debug-ui-redesign
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-10
---

# Phase 10.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust `cargo test` (unit + integration tests) |
| **Config file** | `Cargo.toml` (tests auto-discovered under `tests/` and `#[cfg(test)]`) |
| **Quick run command** | `cargo test --test test_debug_ui` |
| **Full suite command** | `cargo test` |
| **Estimated runtime** | ~30-60 seconds (full), ~5-10 seconds (quick) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test --test test_debug_ui` (quick UI-focused test)
- **After every plan wave:** Run `cargo test` (full suite)
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

> Exact task IDs will be assigned by the planner. Below is the scaffold — the planner populates one row per task in the generated PLAN.md files and the executor fills Status as each task completes.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 10.1-XX-XX | XX | X | DBUI-06 | — | N/A (read-only UI) | unit/integration | `cargo test --test test_debug_ui` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_debug_ui.rs` — extend with assertions for the new split-view/DAG markup, CSS token inheritance, and vendored-asset SHA256 drift (inherits Phase 10 pattern)
- [ ] `tests/test_debug_ui_topology.rs` (new, optional) — topology endpoint JSON schema + operators field + edge throughput numbers if a dedicated test file improves readability
- [ ] No new framework install needed — `cargo test` is already wired by Phase 10

*Existing infrastructure from Phase 10 covers the bulk of phase requirements. Phase 10.1 extends assertions rather than adding new frameworks.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Click-to-select node highlights with 2px ring and updates drill-in panel | DBUI-06 | Visual interaction — unit tests cannot verify SVG ring visual rendering | 1. `cargo run` 2. Open `http://localhost:6401` 3. Click any stream node 4. Verify 2px blue ring appears and drill-in panel shows stream details |
| Edge throughput labels update at 1 Hz without re-rendering DAG | DBUI-06 | Requires observing live polling over time in a browser | 1. Push events via TCP `PUSH` in a loop 2. Observe edge labels refresh every ~1s 3. Verify DAG node positions remain stable (no re-layout) |
| Space key pauses polling, "Polling paused" appears in aria-live region | DBUI-06 | Keyboard interaction requires a real browser | 1. Open UI 2. Click anywhere empty 3. Press Space 4. Verify poll dot stops updating and aria-live announces "Polling paused" |
| Entity lookup shows all 7 sub-states (idle/empty/404/stream-empty/success/network-error/HTTP-error/JSON-error) | DBUI-06 | State transitions require input + server responses not trivially scriptable in unit tests | Manual: trigger each state via real API calls and verify copy matches UI-SPEC §Copywriting |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
