---
phase: 12.8-memory-governance
plan: 07
type: execute
wave: 3
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [V0-MEM-GOV-01, V0-MEM-GOV-02, V0-MEM-GOV-03]
must_haves:
  truths:
    - "3 new positive REQ-IDs added to `.planning/REQUIREMENTS.md` under the existing `### V0-INVARIANT — Architectural commitments locked at CI` subsection (line 200) created by Phase 12.7-07"
    - "V0-MEM-GOV-01 (cold-entity TTL opt-in) cites `crates/beava-server/tests/phase12_8_cold_entity_eviction.rs` as the test anchor + Plan 12.8-02 + Plan 12.8-03"
    - "V0-MEM-GOV-02 (lifetime ops have bounds) cites `crates/beava-server/tests/phase12_8_lifetime_ops_have_bounds.rs` as the test anchor + Plan 12.8-04 + Plan 12.8-01"
    - "V0-MEM-GOV-03 (per-event bucket reclaim) cites Plan 12.8-06 metrics + the existing `WindowedOp::evict_oldest_bucket` mechanism. No new test file (the existing windowed-op tests verify the eviction mechanism); the bucket_reclaim_total metric verifies it fires."
    - "REQUIREMENTS.md frontmatter / structure / other sections UNTOUCHED by this plan — section-ownership contract holds (Plan 12.7-07 is the analog: REQUIREMENTS-only edits, never bleeding into STATE/ROADMAP/CORRECTNESS-PATH)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "3 new V0-MEM-GOV-* positive requirement anchors under § V0-INVARIANT"
      contains: "V0-MEM-GOV-01"
      contains: "V0-MEM-GOV-02"
      contains: "V0-MEM-GOV-03"
  key_links:
    - from: ".planning/REQUIREMENTS.md V0-MEM-GOV-01"
      to: "crates/beava-server/tests/phase12_8_cold_entity_eviction.rs"
      via: "Test anchor citation in REQ description"
      pattern: "phase12_8_cold_entity_eviction"
    - from: ".planning/REQUIREMENTS.md V0-MEM-GOV-02"
      to: "crates/beava-server/tests/phase12_8_lifetime_ops_have_bounds.rs"
      via: "Test anchor citation in REQ description"
      pattern: "phase12_8_lifetime_ops_have_bounds"
---

<objective>
Add 3 positive REQ-IDs to `.planning/REQUIREMENTS.md` under the existing `### V0-INVARIANT — Architectural commitments locked at CI` subsection (created by Phase 12.7-07 at line 200). Each REQ-ID names its CI test anchor + the Plan 12.8 plans that satisfy it. Mirrors the V0-EVENTS-ONLY-01 pattern from Phase 12.7-07.

Per CONTEXT.md `<decisions>` D-locked-autonomously: "REQUIREMENTS.md positive anchors: V0-MEM-GOV-01 (cold-entity TTL opt-in), V0-MEM-GOV-02 (lifetime ops have bounds), V0-MEM-GOV-03 (per-event bucket reclaim)."

Section-ownership contract: This plan ONLY edits REQUIREMENTS.md; closure plan (12.8-09) edits STATE.md / ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md. The boundary mirrors Phase 12.7's Plan 07 / Plan 10 partition.

This plan is doc-only. Per CLAUDE.md TDD §Note 4 doc-only plans use `docs:` commit prefix. No RED→GREEN paired commits — the doc edit IS the deliverable.

Output: REQUIREMENTS.md gains 3 V0-MEM-GOV-* lines beneath the existing V0-EVENTS-ONLY-01 line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.8-memory-governance/12.8-CONTEXT.md

<interfaces>
<!-- Existing V0-INVARIANT subsection — REQUIREMENTS.md lines 200-202 -->

```markdown
### V0-INVARIANT — Architectural commitments locked at CI

- [ ] **V0-EVENTS-ONLY-01**: No `@bv.table` decorator in public `bv` namespace; no `OpNode::Table*` variants; no `TemporalStore` / `MvccVersion`; no `RecordType::TableUpsert/TableDelete/Retract`; no `temporal_http` / `push_table` / `delete_table` / `app.retract` symbols. Enforced at CI runtime by `crates/beava-server/tests/phase12_7_no_table_surface.rs` and `crates/beava-server/tests/phase12_7_legacy_table_handlers_killed.rs` (architectural-test pair, Plan 12.7-02). Reviving any forbidden symbol requires explicit user override + new ADR overturning `project_v0_events_only_scope`.

### SRV-REG — Registry
```

Plan 07 inserts 3 new bullet lines AFTER `V0-EVENTS-ONLY-01` and BEFORE the next subsection `### SRV-REG`. Same one-line-per-REQ format as V0-EVENTS-ONLY-01 — long-form description with embedded test-anchor citations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 7.a: Insert 3 V0-MEM-GOV-* anchors under § V0-INVARIANT in REQUIREMENTS.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md lines 195-210 (V0-INVARIANT subsection + neighboring sections)
    - .planning/phases/12.8-memory-governance/12.8-CONTEXT.md (D-locked-autonomously section — REQ-ID names + scope)
    - .planning/phases/12.7-table-strip/12.7-07-SUMMARY.md (analog plan that introduced V0-EVENTS-ONLY-01) — find via `cat .planning/phases/12.7-table-strip/12.7-07-SUMMARY.md`
  </read_first>
  <action>
    Open `.planning/REQUIREMENTS.md`. Locate line 202 (the V0-EVENTS-ONLY-01 bullet under `### V0-INVARIANT`). After that bullet line and BEFORE the blank line + `### SRV-REG` section header, INSERT exactly:

    ```markdown
    - [ ] **V0-MEM-GOV-01**: `@bv.event(cold_after='<duration>')` is the canonical opt-in cold-entity TTL surface (per-source decorator only — no env-var, no global override). Default `cold_after=None` preserves the no-expiry behavior. Apply hot path lazily evicts the entity (FRESH state on resurrect, Redis TTL pattern) when `now_ms - last_seen_ms > cold_after_ms`. Range enforced at decorator-time: `[1s, 365d]`. No background thread; eviction is inline at apply path's entity-state lookup (preserves `project_no_sharded_apply` single-thread invariant). Enforced at CI by `crates/beava-server/tests/phase12_8_cold_entity_eviction.rs` (7 integration tests covering happy path, FRESH-state-on-resurrect, null-TTL no-op, multi-key entities). Plans 12.8-02 (decorator + EventDescriptor field), 12.8-03 (apply-path eviction logic), 12.8-06 (`beava_cold_entity_evictions_total` metric). Reviving with persisted history requires explicit user override + new ADR.
    - [ ] **V0-MEM-GOV-02**: Every lifetime aggregation operator (windowless mode) declares a finite per-entity memory ceiling at register-time. Memory bounds: `O1` (Phase 5 core / Phase 8 streaks+recency / Phase 9 decay+velocity), `BoundedSketch` (HLL / DDSketch / BloomFilter), `BoundedByRequiredKwarg("n")` (first_n / last_n / lag / most_recent_n / time_since_last_n), `BoundedByRequiredKwarg("samples")` (reservoir_sample), `BoundedByRequiredKwarg("num_buckets")` (histogram), `BoundedByConfig("max_categories", 256)` (event_type_mix / entropy), `BoundedByConfig("k", 10)` (top_k), `BoundedByConfig("samples", 100)` (distance_from_home). Hard register-time rejection via JSON-prelude shim `pre_check_unbounded_op_in_lifetime_mode` (4th shim alongside Plan 12.6-04 / 12.6-06 / 12.7-01) — error code `unbounded_op_in_lifetime_mode` (forward-looking framing per Phase 12.7 D-02 pattern). Default-ON via `BEAVA_MEMORY_GOV_ENFORCE` env-gate (escape hatch: `BEAVA_MEMORY_GOV_ENFORCE=0`). Enforced at CI by `crates/beava-server/tests/phase12_8_lifetime_ops_have_bounds.rs` (architectural-test-as-grep, single-file pattern per CONTEXT.md decisions — no companion deletion-tripwire needed) and `crates/beava-core/tests/op_lifetime_bounds.rs` (15 unit tests covering every AggKind variant). Plans 12.8-01 (shim infrastructure), 12.8-04 (54-row bound table + flip env-gate ON), 12.8-05 (architectural test). Adding a new operator requires extending both `agg_compile.rs::op_str_to_agg_kind` AND `register_validate.rs::lifetime_bound_for_op_str` in lockstep — the architectural test fails if they drift.
    - [ ] **V0-MEM-GOV-03**: Per-event bucket reclaim within active entities is the canonical Tier-2 mechanism (always on, no opt-in). Existing `update_at(now_ms)` per-windowed-op trims trailing buckets that have rolled past the 64-bucket cap; Plan 12.8-06 wires `beava_bucket_reclaim_total` Prometheus counter so operators can observe the eviction rate. No new mechanism in Phase 12.8 (Tier 2 was already shipping); this REQ-ID locks the contract: idle entities continue holding state until either (a) Tier 1 cold-TTL evicts them or (b) next event triggers normal `update_at()` cleanup. Verified via existing windowed-op tests in `crates/beava-core/src/agg_windowed.rs` test mod + the new metric increments. Plan 12.8-06 (`BucketReclaimCounter::inc()` at the eviction site).

    ```

    Constraints:
    - INSERT only the 3 new bullets — do NOT touch V0-EVENTS-ONLY-01 (above).
    - Do NOT touch the `### SRV-REG` heading (below).
    - Do NOT touch any DESCOPED entries elsewhere in the file.
    - Use the same `- [ ] **V0-MEM-GOV-XX**: ...` format as the existing V0-EVENTS-ONLY-01 line.

    Verify the edit:

    1. `grep -n "V0-MEM-GOV" .planning/REQUIREMENTS.md` returns exactly 3 lines (V0-MEM-GOV-01/02/03).
    2. `git diff .planning/REQUIREMENTS.md` shows ONLY the 3-line insertion (no other diff).
    3. `head -210 .planning/REQUIREMENTS.md | tail -15` visually confirms the 3 new lines sit between V0-EVENTS-ONLY-01 and `### SRV-REG`.

    Commit message: `docs(12.8-07): add V0-MEM-GOV-01/02/03 positive REQ anchors under § V0-INVARIANT`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && grep -c "V0-MEM-GOV" .planning/REQUIREMENTS.md && grep -n "V0-MEM-GOV-0[123]" .planning/REQUIREMENTS.md && cargo test --workspace 2>&1 | tail -3</automated>
  </verify>
  <done>
    1. `.planning/REQUIREMENTS.md` contains exactly 3 V0-MEM-GOV-* lines (`grep -c "V0-MEM-GOV-0" returns 3`).
    2. The 3 lines sit between V0-EVENTS-ONLY-01 and `### SRV-REG` heading.
    3. Each new line cites at least one Plan 12.8-XX ID and at least one test anchor file (where applicable).
    4. `git diff .planning/REQUIREMENTS.md` shows ONLY the 3-line insertion.
    5. Workspace stays GREEN: `cargo test --workspace 2>&1 | grep -q "test result: ok"` (this plan touches no Rust source — should be a no-op for tests).
    6. Single commit with `docs:` prefix per CLAUDE.md TDD §Note 4 (doc-only plan exemption).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation only | No production code touched; no attack surface change. |

## STRIDE Threat Register

No STRIDE threats. Doc-only edit.
</threat_model>

<verification>
- `grep -c "V0-MEM-GOV-0[123]" .planning/REQUIREMENTS.md` returns 3.
- `grep -q "phase12_8_cold_entity_eviction" .planning/REQUIREMENTS.md` matches.
- `grep -q "phase12_8_lifetime_ops_have_bounds" .planning/REQUIREMENTS.md` matches.
- `grep -q "BucketReclaimCounter" .planning/REQUIREMENTS.md` matches.
- Workspace tests stay GREEN: `cargo test --workspace 2>&1 | grep -q "test result: ok"`.
</verification>

<success_criteria>
- 3 new V0-MEM-GOV-* anchors land under § V0-INVARIANT in REQUIREMENTS.md.
- Section-ownership contract holds: this plan only edits REQUIREMENTS.md.
- Single `docs:` commit per CLAUDE.md TDD §Note 4.
- No regression: workspace test count unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/12.8-memory-governance/12.8-07-SUMMARY.md` capturing:
- Final commit SHA
- LOC delta (+~10 from 3 long bullet lines)
- Confirmation that section-ownership held (no STATE/ROADMAP/CORRECTNESS-PATH/CLAUDE.md edits in this plan)
- One Lessons-Learned bullet (e.g., "Doc-only REQUIREMENTS plans inherit the 12.7-07 pattern — `docs:` prefix, single commit, REQUIREMENTS-only diff, byte-identical with the V0-EVENTS-ONLY-01 prior")
</output>