---
phase: 12.8-memory-governance
plan: 08
type: execute
wave: 4
depends_on: [03, 04, 06]
files_modified:
  - crates/beava-server/benches/phase12_8_memory_gov_apply.rs
  - crates/beava-server/Cargo.toml                              # new [[bench]] entry registering phase12_8_memory_gov_apply
  - .planning/perf-baselines.md
  - .planning/throughput-baselines.md
autonomous: true
requirements: [V0-MEM-GOV-01, V0-MEM-GOV-02, V0-MEM-GOV-03]
must_haves:
  truths:
    - "A new criterion microbench file `crates/beava-server/benches/phase12_8_memory_gov_apply.rs` (modeled on Phase 12.7's `phase12_6_post_axum_kill_apply.rs` reuse) exercises the apply hot path with cold-TTL ENABLED (`@bv.event(cold_after='30d')`) vs DISABLED (no kwarg). 100-event batches, 100 samples, 3s warm-up, 5s collection."
    - "The microbench captures TWO cells: (a) cold-TTL DISABLED baseline = the post-Plan-04 apply cost; (b) cold-TTL ENABLED = the apply cost INCLUDING the cold-TTL check + last_seen_ms write per Plan 03. Per-event delta must be <5% (per CONTEXT.md 'inline-cheap' constraint)."
    - "Throughput rebaseline runs the standard 8-cell matrix (small/medium/large/fraud-team × http+tcp) using the existing `crates/beava-bench` harness. Regression-gate cell `small/tcp` compared against Phase 12.7 post baseline (751,498 EPS) — 10% warn / 25% block per CLAUDE.md."
    - "Both `.planning/perf-baselines.md` and `.planning/throughput-baselines.md` get a new ` Phase 12.8` section appended in the same template format as Phase 12.7's section (perf-baselines lines 708+, throughput-baselines lines 1502+)."
    - "Verdict logged in both baseline files: PASS / WARN / BLOCK based on the regression-gate cell delta. Phase 12.8 is expected NEUTRAL-TO-SLIGHT-LIFT (cold-TTL DISABLED path is zero new cost on Plan 02's `cold_after_ms = None` field; ENABLED path adds ≤15 ns hashmap lookup)."
    - "If regression-gate cell delta exceeds 25% (BLOCK), execution surfaces a CONCERN in the plan summary; user makes the override-or-fix decision per `feedback_perf_regression_per_phase`. WARN cells are documented inline but don't block."
  artifacts:
    - path: "crates/beava-server/benches/phase12_8_memory_gov_apply.rs"
      provides: "Criterion microbench file with 2 cells (TTL on / TTL off)"
      min_lines: 100
    - path: ".planning/perf-baselines.md"
      provides: "New `### Phase 12.8 — Memory governance apply microbench (Apple-M4)` section appended; 2 cells with verdict"
      contains: "Phase 12.8"
    - path: ".planning/throughput-baselines.md"
      provides: "New `## Phase 12.8 — Memory governance throughput rebaseline (Apple-M4 / Darwin-24.3.0 / 10 cores)` section appended; 8-cell matrix with verdict"
      contains: "Phase 12.8"
  key_links:
    - from: "crates/beava-server/benches/phase12_8_memory_gov_apply.rs"
      to: "ApplyShard::dispatch_one with cold_after enabled / disabled"
      via: "criterion iter_batched on the dispatch path"
      pattern: "phase12_8/cold_ttl_"
---

<objective>
Land Phase 12.8's microbench cell + 8-cell throughput rebaseline per CLAUDE.md §Performance Discipline (Phase 6+ rule: every phase ships a criterion microbench; Phase 8+ rule: every phase ships a throughput run with row append to `.planning/throughput-baselines.md`). Capture 10%/25% regression verdict against Phase 12.7's post-baseline.

The microbench has 2 cells: cold-TTL ENABLED vs DISABLED on the simple-counter shape. The contract is <5% per-event regression in the ENABLED case (CONTEXT.md "inline-cheap" constraint per CONTEXT D-04). The DISABLED case must match Phase 12.7's `simple_counter` baseline within ±5% — confirms Plan 02's Optional<u64> field add is zero-cost.

Throughput rebaseline = mechanical 8-cell run of the standard matrix using `crates/beava-bench`. Same harness as Phases 12.6/12.7 (run via `cargo run -p beava-bench --release -- ...`). Append the rows to `.planning/throughput-baselines.md`.

Output: 2 baseline files updated; 1 new bench file; verdict captured. Plan 09 (closure) reads the verdict for SUMMARY/VERIFICATION roll-up.

Per `feedback_perf_regression_per_phase`: 10% slower → WARN; 25% slower → BLOCK. v0 ship-gate target ≥3M EPS/core is far above Apple-M4 measurements (used as regression tripwire, not absolute target — per `project_no_sharded_apply` Hetzner Linux is the ship-gate hw class).

This plan is doc-and-bench only. No production code changes. Per CLAUDE.md TDD §Note 4 doc/bench-only plans use `chore:` for the bench file commit + `docs:` for the baseline appends. Single bench file written; one bench-creation commit, one baseline-update commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/perf-baselines.md
@.planning/throughput-baselines.md
@.planning/phases/12.8-memory-governance/12.8-CONTEXT.md
@.planning/phases/12.8-memory-governance/12.8-03-PLAN.md
@.planning/phases/12.8-memory-governance/12.8-04-PLAN.md

<interfaces>
<!-- Phase 12.7 microbench template — perf-baselines.md lines 708-756 — REUSE -->

```markdown
### Phase 12.7 — Post-table-strip apply microbench (Apple-M4)

**Captured:** 2026-05-XX (Phase 12.7 Plan 09).
**hw-class:** `Apple-M4 / Darwin-24.3.0 / 10 cores`.
**HEAD at measurement:** `<sha>` (post Plans 12.7-01..N — entire table surface stripped).
**Methodology:** ... (criterion 100 samples, 3s warm-up, 5s collection, 100-event batch).

| Cell | Median (per 100-event batch) | ns/event | Range | Outliers | Comparison baseline | Verdict |
|---|---|---|---|---|---|---|
| ... 3 cells ... |

**Verdict thresholds (CLAUDE.md §Performance Discipline):**
- 10% slower → WARN
- 25% slower → BLOCK
```

<!-- Phase 12.7 throughput template — throughput-baselines.md lines 1502+ — REUSE -->

8-cell matrix template:
- small/http, small/tcp, medium/http, medium/tcp, large/http, large/tcp, fraud-team/http, fraud-team/tcp
- Each cell: EPS post / EPS pre / delta% / verdict.
- Regression-gate row: small/tcp (cell-of-record per Phase 12.6/12.7 convention).

<!-- Existing bench file template — phase12_6_post_axum_kill_apply.rs — REUSE -->

```rust
// crates/beava-server/benches/phase12_6_post_axum_kill_apply.rs (Phase 12.6/12.7 reuse)
mod common;

use beava_runtime_core::wire_request::WireRequest;
use bytes::Bytes;
use criterion::{black_box, criterion_group, criterion_main, BatchSize, Criterion};
use serde_json::json;

const BATCH_SIZE: usize = 100;

fn bench_simple_counter(c: &mut Criterion) {
    let payload = json!({...});  // register payload with Tx event + count derivation
    let server = ApplyShard::test_server_with_register(...);
    let event = json!({...});

    c.bench_function("phase12_6/simple_counter/100_events", |b| {
        b.iter_batched(
            || /* setup batch */,
            |batch| /* dispatch each event */,
            BatchSize::SmallInput,
        );
    });
}

criterion_group!(benches, bench_simple_counter, ...);
criterion_main!(benches);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 8.a: Create criterion microbench `phase12_8_memory_gov_apply.rs` with 2 cells (cold-TTL on / off)</name>
  <read_first>
    - crates/beava-server/benches/phase12_6_post_axum_kill_apply.rs entire file (canonical bench scaffold to mirror — used by both 12.6 + 12.7)
    - crates/beava-server/benches/common.rs (test-server setup helpers)
    - .planning/phases/12.8-memory-governance/12.8-02-PLAN.md (cold_after kwarg shape — used in register payload)
    - .planning/phases/12.8-memory-governance/12.8-03-PLAN.md (cold-TTL apply path)
    - .planning/perf-baselines.md lines 708-756 (Phase 12.7 baseline for comparison)
    - .planning/perf-baselines.md lines 639-707 (Phase 12.6 template referenced by 12.7)
  </read_first>
  <action>
    Create `crates/beava-server/benches/phase12_8_memory_gov_apply.rs`. Copy the structure from `phase12_6_post_axum_kill_apply.rs` (which is reused by Phase 12.7 — same file, both phases ran the same harness). Two cells:

    **Cell 1: `phase12_8/cold_ttl_disabled/100_events`** — register an event source `Tx` WITHOUT `cold_after_ms` (None). Push 100 events for `user_id="alice"`. Measures the post-Plan-04 baseline cost. Expected to match Phase 12.7's `simple_counter` cell within ±5% — confirms Plan 02's Option<u64> field add is zero-cost.

    **Cell 2: `phase12_8/cold_ttl_enabled/100_events`** — register source WITH `cold_after_ms = Some(30 * 86_400_000)` (30 days — well above 100-event-batch wall-clock so no actual eviction fires; just measures the check-cost). Same 100-event batch for `user_id="alice"`. Measures: 1× Option-is-some branch + 1× last_seen_u64 HashMap read + 1× last_seen_u64 HashMap update per event.

    Update `Cargo.toml` (`crates/beava-server/Cargo.toml`) to register the new bench:

    ```toml
    [[bench]]
    name = "phase12_8_memory_gov_apply"
    harness = false
    ```

    Run the bench (or, in dry-run mode, confirm `cargo bench --bench phase12_8_memory_gov_apply --no-run` builds clean):

    ```bash
    cargo bench --bench phase12_8_memory_gov_apply -- --noplot
    ```

    Capture the median ns/event for both cells. Append to `.planning/perf-baselines.md` per Task 8.b.

    Commit message: `chore(12.8-08): add criterion microbench phase12_8_memory_gov_apply with 2 cold-TTL cells`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && cargo bench --bench phase12_8_memory_gov_apply --no-run 2>&1 | tail -5 && grep -c "phase12_8_memory_gov_apply" /Users/petrpan26/work/tally/crates/beava-server/Cargo.toml</automated>
  </verify>
  <done>
    1. File `crates/beava-server/benches/phase12_8_memory_gov_apply.rs` exists with 2 bench cells.
    2. `Cargo.toml` registers the bench.
    3. `cargo bench --bench phase12_8_memory_gov_apply --no-run` builds clean (no compile errors).
    4. Bench actually runs with `cargo bench --bench phase12_8_memory_gov_apply -- --noplot` and produces median ns/event readings for both cells.
    5. Workspace stays GREEN: `cargo test --workspace 2>&1 | grep -q "test result: ok"`.
  </done>
</task>

<task type="auto">
  <name>Task 8.b: Run microbench + 8-cell throughput rebaseline; append both to baseline files with verdict</name>
  <read_first>
    - crates/beava-bench/src/bin/ — bench harness binaries (find via `ls crates/beava-bench/src/bin/`)
    - .planning/throughput-baselines.md lines 1502-1700 (Phase 12.7 8-cell matrix template — REUSE)
    - .planning/perf-baselines.md lines 708-756 (Phase 12.7 microbench template — REUSE)
    - Any per-pipeline JSON config used by beava-bench: `find crates/beava-bench/ -name '*.json' -type f`
  </read_first>
  <action>
    **Step 1 — Run the microbench captured in Task 8.a + record results.**

    1. `cargo bench --bench phase12_8_memory_gov_apply -- --noplot 2>&1 | tee /tmp/12.8-microbench.log`. Extract median ns/event from criterion output for both cells.

    2. Append to `.planning/perf-baselines.md` AT THE END of the file (after the existing Phase 12.7 section). Use the Phase 12.7 template format:

    ```markdown

    ### Phase 12.8 — Memory governance apply microbench (Apple-M4)

    **Captured:** 2026-05-XX (Phase 12.8 Plan 08).
    **hw-class:** `Apple-M4 / Darwin-24.3.0 / 10 cores`.
    **HEAD at measurement:** `<sha>` (post Plans 12.8-01..06 — cold-TTL apply path + bound table + metrics landed).
    **Methodology:** Same harness as Phase 12.6/12.7 — `crates/beava-server/benches/phase12_8_memory_gov_apply.rs`; criterion 100 samples, 3s warm-up, 5s collection, 100-event batch.

    | Cell | Median (per 100-event batch) | ns/event | Range | Outliers | Comparison baseline | Verdict |
    |---|---|---|---|---|---|---|
    | `phase12_8/cold_ttl_disabled/100_events` | <result> | <ns> | <range> | <%> | Phase 12.7 simple_counter (post-12.7 baseline) | <PASS / WARN / BLOCK> |
    | `phase12_8/cold_ttl_enabled/100_events` | <result> | <ns> | <range> | <%> | phase12_8/cold_ttl_disabled (above row, this measurement) | <PASS / WARN / BLOCK> |

    **Verdict thresholds (CLAUDE.md §Performance Discipline):**
    - 10% slower than baseline → WARN
    - 25% slower than baseline → BLOCK

    **Why expected ≤5% regression in cold_ttl_enabled vs disabled:** Plan 03's check is 1 Option-is-some branch (~1 ns) + 1 hashbrown HashMap lookup with FxBuildHasher on a u64 key (~10-15 ns) + 1 HashMap insert/update (~15-20 ns) per event. Total budget ~30-35 ns. On a baseline ~80,000 ns / 100-event batch (= 800 ns/event), 30-35 ns is ~4%. Margin of error within criterion's noise floor.

    **Why expected ~zero regression in cold_ttl_disabled vs Phase 12.7 simple_counter:** Plan 02's `cold_after_ms: Option<u64>` field add to EventDescriptor is zero-cost on the read side when `None` (single Option-is-some branch, ~1 ns); Plan 03's eviction check skips entirely when `cold_after_ms.is_none()` (early return, ~1 ns).
    ```

    Compute `<result>` etc. from criterion output. Compute `Verdict` per the gate thresholds.

    Commit (with the bench results inline) message: `docs(12.8-08): append Phase 12.8 microbench results to perf-baselines.md`.

    **Step 2 — Run the 8-cell throughput rebaseline.**

    3. Build release binaries: `cargo build --release --bin beava-server --bin beava-bench-v18` (or whatever bench-v18 is named — find via `ls crates/beava-bench/src/bin/`).

    4. For each of the 8 cells (small/medium/large/fraud-team × http/tcp), run:

    ```bash
    # small/tcp cell example
    target/release/beava-bench-v18 --pipeline=small --transport=tcp --duration=10s --pipeline-depth=256 --parallel=16 --output=/tmp/12.8-throughput-small-tcp.json
    ```

    Capture EPS for each cell. Use the existing JSON pipeline configs (small.json, medium.json, large.json, fraud-team.json — under `crates/beava-bench/configs/` or similar; find via grep).

    5. Append `## Phase 12.8 — Memory governance throughput rebaseline (Apple-M4 / Darwin-24.3.0 / 10 cores)` section to `.planning/throughput-baselines.md` AT THE END of the file. Use Phase 12.7's section as the template (lines 1502+). Fields to fill:
       - 8-cell matrix table with EPS post / EPS pre (Phase 12.7 baseline) / delta% / verdict per cell
       - Regression-gate cell `small/tcp` named explicitly with the Phase 12.7 baseline (751,498 EPS) for the reference comparison
       - `**Architectural rationale (why we expect neutral)**` paragraph: Phase 12.8 adds <5% per-event cost ONLY when cold_after_ms is set; the harness pipelines do NOT set cold_after_ms, so throughput should match Phase 12.7 post-baseline within run-to-run noise (±5%).
       - Cells flagged for Phase 13 investigation (any borderline -10%) — list them.
       - `### System-load context` (M4 box load conditions during run, per the 12.7 honesty pattern).

    6. Commit message: `docs(12.8-08): append Phase 12.8 8-cell throughput rebaseline to throughput-baselines.md`.

    **Step 3 — Verdict logging.**

    7. Verdict per regression-gate cell (small/tcp):
       - delta within ±10% → PASS
       - delta -10% to -25% → WARN (cell flagged for Phase 13 investigation)
       - delta < -25% → BLOCK (Plan 09 escalates to user; Phase 12.8 closure does NOT proceed without user override or root-cause investigation)

    8. Confirm both baseline files have the Phase 12.8 section: `grep -q "Phase 12.8" .planning/perf-baselines.md && grep -q "Phase 12.8" .planning/throughput-baselines.md`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && grep -c "^### Phase 12.8" .planning/perf-baselines.md && grep -c "^## Phase 12.8" .planning/throughput-baselines.md && grep -c "PASS\|WARN\|BLOCK" .planning/perf-baselines.md | tail -1 && cargo test --workspace 2>&1 | tail -3</automated>
  </verify>
  <done>
    1. `.planning/perf-baselines.md` has a `### Phase 12.8 — Memory governance apply microbench (Apple-M4)` section with both cells filled with measured values + verdict.
    2. `.planning/throughput-baselines.md` has a `## Phase 12.8 — Memory governance throughput rebaseline (Apple-M4 / Darwin-24.3.0 / 10 cores)` section with the 8-cell matrix filled with measured values + verdict.
    3. Microbench cell `phase12_8/cold_ttl_enabled/100_events` is PASS (≤5% over disabled baseline).
    4. Throughput regression-gate cell `small/tcp` is PASS (within ±10% of Phase 12.7 baseline 751,498 EPS).
    5. Workspace stays GREEN: `cargo test --workspace 2>&1 | grep -q "test result: ok"`.
    6. If ANY cell triggers BLOCK, the plan summary surfaces a CONCERN and Plan 09 (closure) does not proceed without user override.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bench harness — runs in-process | Loopback only; no external attack surface. |

## STRIDE Threat Register

No new STRIDE threats. Bench/doc only.
</threat_model>

<verification>
- `grep -c "^### Phase 12.8" .planning/perf-baselines.md` returns 1.
- `grep -c "^## Phase 12.8" .planning/throughput-baselines.md` returns 1.
- Both Phase 12.8 baseline sections contain at least 1 cell row each + verdict.
- `cargo bench --bench phase12_8_memory_gov_apply --no-run` builds clean.
- Workspace tests stay GREEN.
- Microbench cold_ttl_enabled cell verdict = PASS (or surfaced WARN/BLOCK with explicit user override).
- Throughput regression-gate cell `small/tcp` verdict = PASS (or surfaced WARN/BLOCK).
</verification>

<success_criteria>
- New bench file `phase12_8_memory_gov_apply.rs` registered in `Cargo.toml` and runnable.
- Microbench results appended to perf-baselines.md (2 cells with verdict).
- 8-cell throughput rebaseline appended to throughput-baselines.md with verdict.
- Both baselines reference Phase 12.7's post-baseline as the comparison anchor.
- Workspace stays GREEN.
- Per CLAUDE.md TDD §Note 4 doc/bench-only plans: `chore:` commit for bench creation + `docs:` commit for baseline appends.
</success_criteria>

<output>
After completion, create `.planning/phases/12.8-memory-governance/12.8-08-SUMMARY.md` capturing:
- Final commit SHAs
- LOC delta (+~150 from new bench file + ~200 from baseline appends)
- 2 microbench cell readings (ns/event)
- 8-cell throughput readings (EPS) + regression-gate cell verdict
- Box-load context (Apple-M4 conditions during the run)
- One Lessons-Learned bullet (e.g., "Cold-TTL ENABLED cell measures Plan 03's 1×Option-branch + 1×HashMap lookup overhead — landed within X% of disabled baseline / above the predicted 5% gate")
- Hetzner-pending follow-up note if Apple-M4 measurement was the only run
</output>