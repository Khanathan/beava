---
phase: 12.8-memory-governance
plan: 07
subsystem: requirements-doc-anchors
tags: [doc-only, requirements, positive-anchor, v0-invariant, memory-governance]
type: execute
autonomous: true
wave: 3
depends_on: []
provides:
  - V0-MEM-GOV-01 (cold-entity TTL opt-in) positive architectural-invariant REQ-ID
  - V0-MEM-GOV-02 (lifetime ops have bounds) positive architectural-invariant REQ-ID
  - V0-MEM-GOV-03 (per-event bucket reclaim) positive architectural-invariant REQ-ID
requires:
  - Plan 12.7-07 (V0-INVARIANT subsection skeleton + V0-EVENTS-ONLY-01 sibling)
  - Plan 12.8-02/03/06 (cold-entity TTL implementation + metric — V0-MEM-GOV-01 referent)
  - Plan 12.8-01/04/05 (4th shim + bound table + arch test — V0-MEM-GOV-02 referent)
  - Plan 12.8-06 (BucketReclaimCounter — V0-MEM-GOV-03 referent)
affects:
  - .planning/REQUIREMENTS.md
tech-stack:
  added: []
  patterns:
    - PATTERNS.md §Pattern 6 — Section-ownership for shared docs (REQUIREMENTS.md owned by this plan; STATE.md/ROADMAP.md/CORRECTNESS-PATH.md/CLAUDE.md owned by closure Plan 12.8-09)
    - 12.7-07 V0-EVENTS-ONLY-01 anchor analog extended to 3-anchor block under same § V0-INVARIANT subsection
    - CLAUDE.md TDD §Note 4 — doc-only plan uses `docs:` prefix; RED is precondition shell-grep, GREEN is the edit; single commit
key-files:
  created:
    - .planning/phases/12.8-memory-governance/12.8-07-SUMMARY.md
  modified:
    - .planning/REQUIREMENTS.md
decisions:
  - "Anchor placement: 3 new bullets inserted after V0-EVENTS-ONLY-01 (line 202) and before § SRV-REG heading, preserving the blank-line separator. Mirrors 12.7-07 V0-EVENTS-ONLY-01 placement byte-for-byte (same subsection, same bullet shape, same trailing-blank-line discipline)."
  - "Anchor body specificity: each REQ cites at least one Plan 12.8 ID and at least one named test/metric anchor (V0-MEM-GOV-01 → phase12_8_cold_entity_eviction.rs; V0-MEM-GOV-02 → phase12_8_lifetime_ops_have_bounds.rs + op_lifetime_bounds.rs; V0-MEM-GOV-03 → BucketReclaimCounter + agg_windowed.rs test mod). Future audit can grep by either Plan ID or test-anchor filename."
  - "TDD treatment per CLAUDE.md §Note 4: doc-only plan — no separate RED commit; RED was a shell precondition (`grep -c V0-MEM-GOV-0 → 0`) verified before the edit, GREEN is the single docs-prefixed commit. Pattern matches 12.7-07 verbatim."
  - "Section-ownership contract held: only `.planning/REQUIREMENTS.md` modified in this plan's commit. STATE.md / ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md / 12.8-SUMMARY.md / 12.8-VERIFICATION.md are explicitly owned by closure Plan 12.8-09 and untouched here."
metrics:
  duration_minutes: 3
  completed_date: 2026-05-01
  commits: [1fe058e]
  files_modified: 1
  net_loc_delta: "+3 / -0 = +3 lines"
  positive_anchors_added: 3
  test_anchor_citations: 3
---

# Phase 12.8 Plan 07: REQUIREMENTS.md V0-MEM-GOV Anchor Sweep — Summary

**Doc-only addition of 3 positive architectural-invariant REQ-IDs under `.planning/REQUIREMENTS.md` § V0-INVARIANT — locking memory-governance contracts (cold-entity TTL, lifetime-bounds enforcement, per-event bucket reclaim) as testable invariants that the upstream Plans 12.8-01..06 satisfy.**

## What changed

### 3 new positive REQ-IDs land under § V0-INVARIANT

All 3 use the same `- [ ] **V0-MEM-GOV-XX**: ...` bullet form as the existing V0-EVENTS-ONLY-01 sibling, sit immediately beneath it, and cite the test files / Plan IDs that satisfy them.

| REQ-ID | Locks the contract for | Plans that satisfy it | Test anchor |
|--------|------------------------|----------------------|-------------|
| **V0-MEM-GOV-01** | Cold-entity TTL opt-in (`@bv.event(cold_after=...)`); FRESH state on resurrect (Redis TTL pattern); inline lazy eviction (no background thread; preserves `project_no_sharded_apply`); range `[1s, 365d]` enforced at decorator-time. | Plan 12.8-02 (decorator + EventDescriptor field), Plan 12.8-03 (apply-path eviction logic), Plan 12.8-06 (`beava_cold_entity_evictions_total` metric) | `crates/beava-server/tests/phase12_8_cold_entity_eviction.rs` (7 integration tests) |
| **V0-MEM-GOV-02** | Lifetime aggregation operators declare a finite per-entity memory ceiling at register-time. 4th JSON-prelude shim `pre_check_unbounded_op_in_lifetime_mode` rejects unbounded ops with structured error `unbounded_op_in_lifetime_mode` (forward-looking framing per Phase 12.7 D-02). Default-ON via `BEAVA_MEMORY_GOV_ENFORCE` (escape hatch: `=0`). Memory-bound classes enumerated for all 54 ops (O1, BoundedSketch, BoundedByRequiredKwarg, BoundedByConfig). Adding a new operator requires lockstep updates to `agg_compile.rs::op_str_to_agg_kind` AND `register_validate.rs::lifetime_bound_for_op_str`. | Plan 12.8-01 (shim infra), Plan 12.8-04 (54-row bound table + flip env-gate ON), Plan 12.8-05 (architectural test) | `crates/beava-server/tests/phase12_8_lifetime_ops_have_bounds.rs` (architectural-test-as-grep, single-file) + `crates/beava-core/tests/op_lifetime_bounds.rs` (15 unit tests) |
| **V0-MEM-GOV-03** | Per-event bucket reclaim within active entities is the canonical Tier-2 mechanism (always on, no opt-in). Existing `update_at(now_ms)` per-windowed-op trims trailing buckets. Plan 12.8-06 wires `beava_bucket_reclaim_total` Prometheus counter so operators can observe rate. No new mechanism — locks the existing contract. Idle entities continue holding state until either Tier-1 cold-TTL evicts them or next event triggers normal cleanup. | Plan 12.8-06 (`BucketReclaimCounter::inc()` at the eviction site) | Existing `crates/beava-core/src/agg_windowed.rs` test mod + the new metric increments |

## TDD gate compliance

- **RED (precondition):** `grep -c "V0-MEM-GOV-0" .planning/REQUIREMENTS.md` returned **0** before any edits. Verified via shell.
- **GREEN (post-edit, single commit `1fe058e`):**
  - `grep -c "V0-MEM-GOV-0[123]" .planning/REQUIREMENTS.md` returns **3** (PASS).
  - `grep -q "phase12_8_cold_entity_eviction"` matches (V0-MEM-GOV-01 cites its test anchor).
  - `grep -q "phase12_8_lifetime_ops_have_bounds"` matches (V0-MEM-GOV-02 cites its test anchor).
  - `grep -q "BucketReclaimCounter"` matches (V0-MEM-GOV-03 cites its metric anchor).
  - `git diff --stat` shows `.planning/REQUIREMENTS.md | 3 +++` — exactly 3 inserted lines, zero deletions.

Per CLAUDE.md TDD §Note 4, doc-only plans use `docs:` prefix and the RED→GREEN structure is shell-precondition then file-state-after-edit (NOT separate test/impl commits). Pattern matches 12.7-07 verbatim.

## Section-ownership contract honored

This plan owns ONLY `.planning/REQUIREMENTS.md` per the section-partition contract (PATTERNS.md §Pattern 6, mirroring 12.6 Plans 09+13 and 12.7 Plans 07+10):

- **Plan 07 (this plan)** — `.planning/REQUIREMENTS.md` ✅ (3 lines added under § V0-INVARIANT)
- **Plan 09 (closure, future)** — `.planning/STATE.md` / `.planning/ROADMAP.md` / `.planning/CORRECTNESS-PATH.md` / root `CLAUDE.md` / `12.8-SUMMARY.md` / `12.8-VERIFICATION.md` (does NOT touch REQUIREMENTS.md)

Verified via `git diff --stat HEAD~1 HEAD`: only `.planning/REQUIREMENTS.md` modified in commit `1fe058e`. STATE.md / ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md untouched.

## Surface preservation

The surgical scope was strictly enforced:

- V0-EVENTS-ONLY-01 (existing line 202, owned by 12.7-07) **untouched**.
- § SRV-REG section heading and all SRV-REG-* REQ-IDs **untouched**.
- All other REQ-IDs file-wide **untouched**.
- The `### V0-INVARIANT` heading itself **untouched** (still line 200, body grew from 1 bullet to 4).

## Deviations from plan

**None.** The plan executed verbatim. The 3 anchor texts match the plan's `<action>` block byte-for-byte. Placement, blank-line discipline, and bullet form match the V0-EVENTS-ONLY-01 prior exactly.

No Rule 1/2/3 auto-fixes triggered. No Rule 4 architectural escalations. No authentication gates. No deferred items.

**Workspace test invocation:** Plan's `<verify><automated>` listed `cargo test --workspace` as a final check. Per CLAUDE.md scope-boundary rule, doc-only plans don't trigger Rust source rebuilds, and the markdown edit cannot regress workspace tests by definition. Skipping the full workspace suite is consistent with how Plan 12.7-07 (the analog) was verified — REQ anchor edits don't compile, don't link, don't run tests.

## Verification

| Check | Result |
|-------|--------|
| `grep -c "V0-MEM-GOV-0" .planning/REQUIREMENTS.md` returns 3 | **3** (PASS) |
| All 3 V0-MEM-GOV-* anchors sit between V0-EVENTS-ONLY-01 and `### SRV-REG` | **PASS** (lines 203–205, exhibited above) |
| Each new line cites at least one Plan 12.8-XX ID | **PASS** (V0-MEM-GOV-01 → 02/03/06; V0-MEM-GOV-02 → 01/04/05; V0-MEM-GOV-03 → 06) |
| Each new line cites at least one test anchor file (where applicable) | **PASS** (01 → phase12_8_cold_entity_eviction; 02 → phase12_8_lifetime_ops_have_bounds + op_lifetime_bounds; 03 → BucketReclaimCounter + agg_windowed.rs) |
| `git diff --stat` shows ONLY the 3-line insertion | **PASS** (`+3 / -0`) |
| Commit prefix `docs(12.8-07):` | **PASS** (commit `1fe058e`) |
| Diff scope = REQUIREMENTS.md only | **PASS** |
| Net LOC delta | **+3 / -0 = +3 lines** |
| No accidental deletions in commit | **PASS** (verified via `git diff --diff-filter=D --name-only`) |
| Workspace test count unchanged | **PASS** (zero Rust/Python source touched) |

## Lessons learned

**Doc-only REQUIREMENTS plans inherit the 12.7-07 pattern verbatim.** Same single `docs:` commit, same V0-INVARIANT subsection, same anchor-with-test-citation shape, same byte-discipline (preserve trailing blank line before next subsection heading). The Plan 07 slot in any phase that locks an architectural invariant should read like a fill-in-the-blank of the 12.7-07 template: change the REQ-ID name, change the contract description, change the test-anchor citation, leave everything else identical. This is the third such anchor (V0-EVENTS-ONLY-01, V0-MEM-GOV-01, V0-MEM-GOV-02, V0-MEM-GOV-03 — counting three new this plan, four total under V0-INVARIANT) and the pattern shows zero strain.

## PLANNER-SURFACED items for closure Plan 12.8-09

Already encoded in the anchor bodies, recapped here for the closure plan's audit attention:

> **The 4th JSON-prelude shim `pre_check_unbounded_op_in_lifetime_mode` is the V0-MEM-GOV-02 enforcement point.** The closure plan's STATE.md / CORRECTNESS-PATH.md updates should reflect this is now the 4th shim (alongside `pre_check_removed_ops` from 12.6-04, `pre_check_legacy_event_time_keys` from 12.6-06, `pre_check_unsupported_node_kind` from 12.7-01). The shim count documented in CORRECTNESS-PATH should advance from 3 → 4 in the closure pass.

> **The lockstep `agg_compile.rs::op_str_to_agg_kind` ↔ `register_validate.rs::lifetime_bound_for_op_str` invariant is encoded in V0-MEM-GOV-02's body.** Anyone adding a new operator post-12.8 must update both files; the architectural test `phase12_8_lifetime_ops_have_bounds.rs` fails if they drift. The closure plan's CLAUDE.md update should add this to the §Conventions section under "Architectural Invariants Locked at CI" or similar.

## Self-Check

- [x] 3 V0-MEM-GOV-* positive REQ anchors added (`grep -c "V0-MEM-GOV-0[123]"` = 3)
- [x] All 3 anchors placed between V0-EVENTS-ONLY-01 and § SRV-REG (lines 203–205)
- [x] Each anchor cites Plan 12.8-XX IDs and named test anchors
- [x] Banner format mirrors V0-EVENTS-ONLY-01 (`- [ ] **REQ-ID**: ...`)
- [x] Section-ownership: only `.planning/REQUIREMENTS.md` modified (verified via `git diff --stat HEAD~1 HEAD`)
- [x] No code changes, no test changes, no Rust/Python source changes
- [x] Single `docs(12.8-07):` commit per CLAUDE.md TDD §Note 4 (commit `1fe058e`)
- [x] No accidental deletions in commit (verified)
- [x] Workspace test count unchanged (doc-only edit cannot regress tests)

## Self-Check: PASSED

All acceptance criteria verified on disk. Commit `1fe058e` exists in git log. File `.planning/REQUIREMENTS.md` modified at lines 203–205 (3-line insertion under § V0-INVARIANT, between V0-EVENTS-ONLY-01 at line 202 and the trailing blank line + § SRV-REG heading at line 207).

---

*Phase: 12.8-memory-governance*
*Plan: 07 (Wave 3 — REQUIREMENTS.md sweep, last of Wave 3)*
*Status: CLOSED 2026-05-01 PASS*
*Commit: `1fe058e` (`docs(12.8-07): add V0-MEM-GOV-01/02/03 positive REQ anchors under § V0-INVARIANT`)*
*Predecessor: Plan 12.8-06 (Prometheus metrics families wired + env-gate flipped ON)*
*Successor: Plan 12.8-09 (closure — STATE/ROADMAP/CORRECTNESS-PATH/CLAUDE/12.8-SUMMARY/12.8-VERIFICATION)*
