---
phase: 12.8-memory-governance
plan: 09
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06, 07, 08]
files_modified:
  - .planning/phases/12.8-memory-governance/12.8-SUMMARY.md
  - .planning/phases/12.8-memory-governance/12.8-VERIFICATION.md
  - CLAUDE.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/CORRECTNESS-PATH.md
autonomous: true
requirements: [V0-MEM-GOV-01, V0-MEM-GOV-02, V0-MEM-GOV-03]
must_haves:
  truths:
    - "Phase 12.8 SUMMARY.md is created at `.planning/phases/12.8-memory-governance/12.8-SUMMARY.md` with frontmatter (subsystem / tags / provides / affects / tech-stack / key-files / decisions / metrics / plan_count / overall_verdict) + plan-by-plan TOC + headline numbers + decisions-honored block + lessons-learned bullets"
    - "Phase 12.8 VERIFICATION.md is created at `.planning/phases/12.8-memory-governance/12.8-VERIFICATION.md` with mechanical pass/fail per ROADMAP success criterion + per CONTEXT decision (D-01..D-04)"
    - "CLAUDE.md gains a new `### Memory Governance Invariant (locked Phase 12.8)` block as a sibling to the existing `### mio-only Hot-Path Invariant (locked Phase 12.6)` and `### Events-Only Invariant (locked Phase 12.7)` blocks. Same Rule / Why / How enforced / Reviving requires structure as the prior 2 invariant blocks."
    - "STATE.md `Current Focus` advances from `Phase 12.7 CLOSED + Phase 13 NEXT` to `Phase 12.8 CLOSED + Phase 13 NEXT`. Phase-progress block: completed_phases +1; total_plans +9; completed_plans +9; percent recomputed."
    - "ROADMAP.md Phase 12.8 entry (line ~52) flips from `📋 PLANNED 2026-05-01` to `✅ COMPLETED 2026-05-XX (PASS / WARN / BLOCK per Plan 08 verdict)`. v0 critical-path block updated."
    - "CORRECTNESS-PATH.md Phase 12.8 row marked CLOSED + Phase 13 row marked P0 #1."
    - "Section-ownership contract held: Plan 09 OWNS the closure edits (SUMMARY/VERIFICATION/CLAUDE.md memory-gov-block/STATE phase-progress + Current Focus/ROADMAP 12.8 entry/CORRECTNESS-PATH 12.8 row). Plans 01-08 did NOT touch any of these sections (verified via per-plan diffs)."
  artifacts:
    - path: ".planning/phases/12.8-memory-governance/12.8-SUMMARY.md"
      provides: "Phase 12.8 narrative + plan-by-plan TOC + headline numbers + decisions honored + lessons learned + planner-surfaced concerns"
      min_lines: 250
    - path: ".planning/phases/12.8-memory-governance/12.8-VERIFICATION.md"
      provides: "Mechanical pass/fail per ROADMAP success criterion + per CONTEXT decision (D-01..D-04)"
      min_lines: 100
    - path: "CLAUDE.md"
      provides: "New `### Memory Governance Invariant (locked Phase 12.8)` block as sibling to existing 12.6/12.7 invariant blocks"
      contains: "Memory Governance Invariant"
    - path: ".planning/STATE.md"
      provides: "Phase 12.8 CLOSED line + phase-progress increment"
      contains: "Phase 12.8"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 12.8 entry flipped to COMPLETED + v0 critical-path advanced"
      contains: "Phase 12.8"
    - path: ".planning/CORRECTNESS-PATH.md"
      provides: "Phase 12.8 row CLOSED + Phase 13 row P0 #1"
      contains: "Phase 12.8"
  key_links:
    - from: "CLAUDE.md § Memory Governance Invariant (locked Phase 12.8) block"
      to: "crates/beava-server/tests/phase12_8_lifetime_ops_have_bounds.rs + phase12_8_cold_entity_eviction.rs"
      via: "How-enforced citation in the block"
      pattern: "phase12_8_"
    - from: "STATE.md Current Focus line"
      to: "Phase 13 NEXT (final v0 ship)"
      via: "v0 critical-path advance"
      pattern: "Phase 13"
---

<objective>
Close out Phase 12.8 via the section-ownership pattern established by Phases 12.6 + 12.7. This plan owns:

1. **`12.8-SUMMARY.md`** — narrative + plan-by-plan TOC + headline numbers + decisions D-01..D-04 honored + lessons learned + planner-surfaced concerns (if any).
2. **`12.8-VERIFICATION.md`** — mechanical pass/fail per ROADMAP success criterion + per CONTEXT decision.
3. **`CLAUDE.md` § Memory Governance Invariant block** — Rule / Why / How enforced / Reviving requires structure, sibling to 12.6 mio-only + 12.7 Events-Only blocks.
4. **`STATE.md`** — Current Focus advance + phase-progress increment + session continuity.
5. **`ROADMAP.md`** — Phase 12.8 entry flipped to COMPLETED + v0 critical-path advance.
6. **`CORRECTNESS-PATH.md`** — Phase 12.8 row CLOSED + Phase 13 row P0 #1.

Per CONTEXT.md `<decisions>` D-locked-autonomously block: "Section-ownership: Closure plan owns STATE.md / ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md / 12.8-SUMMARY.md / 12.8-VERIFICATION.md. Earlier plans don't touch these (matches 12.6 Plans 09+13, 12.7 Plan 10)."

Output: Phase 12.8 OFFICIALLY CLOSED. v0 critical path advances to Phase 13 final ship.

This plan is doc-only. Per CLAUDE.md TDD §Note 4 doc-only plans use `docs:` commit prefix. Multiple commits acceptable (one per file or grouped logically): SUMMARY, VERIFICATION, CLAUDE.md, STATE+ROADMAP+CORRECTNESS-PATH (the closure trio).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CORRECTNESS-PATH.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/12.8-memory-governance/12.8-CONTEXT.md
@.planning/phases/12.8-memory-governance/12.8-01-PLAN.md
@.planning/phases/12.8-memory-governance/12.8-02-PLAN.md
@.planning/phases/12.8-memory-governance/12.8-03-PLAN.md
@.planning/phases/12.8-memory-governance/12.8-04-PLAN.md
@.planning/phases/12.8-memory-governance/12.8-05-PLAN.md
@.planning/phases/12.8-memory-governance/12.8-06-PLAN.md
@.planning/phases/12.8-memory-governance/12.8-07-PLAN.md
@.planning/phases/12.8-memory-governance/12.8-08-PLAN.md

<interfaces>
<!-- Phase 12.7-10 closure analog — read for SUMMARY/VERIFICATION/CLAUDE.md/STATE/ROADMAP/CORRECTNESS-PATH structure -->

```
.planning/phases/12.7-table-strip/12.7-SUMMARY.md  (260+ lines; frontmatter + narrative + TOC + decisions + lessons)
.planning/phases/12.7-table-strip/12.7-VERIFICATION.md  (mechanical pass/fail)
CLAUDE.md lines 79-87 § mio-only Hot-Path Invariant block (12.6) + § Events-Only Invariant (12.7) as siblings
.planning/STATE.md (worktree map, phase-progress, Current Focus pattern)
.planning/ROADMAP.md (line 52 — Phase 12.8 entry to flip; line 65 — total active phases)
.planning/CORRECTNESS-PATH.md (Phase row table)
```

<!-- Phase 12.7's verdict-PASS template for the SUMMARY headline -->
The SUMMARY.md from 12.7 has the exact frontmatter shape Plan 09 must mirror — read `.planning/phases/12.7-table-strip/12.7-SUMMARY.md` lines 1-150 for the template.

<!-- Phase 12.6 + 12.7 CLAUDE.md invariant block template -->

```markdown
### {Name} Invariant (locked Phase {NN})

**Rule:** {one-paragraph normative statement}

**Why:** {locked memory + architectural rationale}

**How enforced:** {test files / mechanism}

**Reviving {X} requires:** explicit user override + new ADR overturning {memory file}.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 9.a: Create `.planning/phases/12.8-memory-governance/12.8-SUMMARY.md` capturing the phase narrative + plan-by-plan TOC + decisions honored + lessons learned</name>
  <read_first>
    - .planning/phases/12.7-table-strip/12.7-SUMMARY.md (entire file — canonical SUMMARY template; mirror frontmatter / narrative / TOC / decisions-honored / lessons-learned shape)
    - .planning/phases/12.8-memory-governance/12.8-CONTEXT.md (D-01..D-04 + memory-bound classes)
    - All Plan 12.8 SUMMARYs already created (`.planning/phases/12.8-memory-governance/12.8-{01..08}-SUMMARY.md`) — read for plan-by-plan facts
    - .planning/perf-baselines.md Phase 12.8 section (microbench results from Plan 08)
    - .planning/throughput-baselines.md Phase 12.8 section (8-cell rebaseline from Plan 08)
  </read_first>
  <action>
    Write `.planning/phases/12.8-memory-governance/12.8-SUMMARY.md` modeled on `.planning/phases/12.7-table-strip/12.7-SUMMARY.md`. Required sections:

    **Frontmatter** (YAML, 30-50 lines): subsystem, tags, provides (list of capabilities — V0-MEM-GOV-COMMITMENT-LOCKED, COLD-ENTITY-TTL-OPT-IN, LIFETIME-OP-BOUND-TABLE, METRICS-PROMETHEUS-FIVE-FAMILIES, etc.), affects (list of edited files in .planning + CLAUDE.md), tech-stack (added: [], patterns: [...]), key-files (created / modified / deleted), decisions (1 line per CONTEXT decision honored + planner-surfaced concerns), metrics (commits_phase, loc_delta, workspace_test_count_pre/post/delta, microbench cells + delta, throughput cells + verdict, plan_count, overall_verdict).

    **Body sections** (~250-300 lines):

    1. **Executive summary** — 4-paragraph narrative: (i) what landed, (ii) D-04 fresh-state-on-resurrect commitment, (iii) D-03 hard-reject + 4th JSON-prelude shim + 54-row bound table, (iv) metrics + perf rebaseline.

    2. **Headline numbers** — bullet list: total LOC added (mostly tests), workspace test count delta (~+50 from this phase), microbench cells (cold_ttl_disabled vs Phase 12.7 simple_counter; cold_ttl_enabled vs disabled — within X% verdict), throughput regression-gate cell (small/tcp X% delta vs Phase 12.7 751,498 baseline + verdict), 5 new metric families on /metrics.

    3. **Plan-by-plan TOC** — table:

    | Plan | Wave | Description | Commits | LOC delta | SUMMARY |
    |---|---|---|---|---|---|
    | 12.8-01 | 1 | 4th JSON-prelude shim + env-gate | 2 | +N | [link] |
    | ... |

    Read each plan's SUMMARY (`.planning/phases/12.8-memory-governance/12.8-{01..08}-SUMMARY.md`) for the per-plan facts.

    4. **Architectural outcomes** — sub-sections:
       - Locked architectural commitments (post-12.8): cold-TTL opt-in via `@bv.event(cold_after='...')`, lifetime ops have bounds enforced at register-time, env-gate `BEAVA_MEMORY_GOV_ENFORCE=0` escape hatch, 5 Prometheus metrics on /metrics, FRESH-state-on-resurrect locked permanent.
       - Architectural-pivot artifacts: CLAUDE.md § Memory Governance Invariant block, REQUIREMENTS.md V0-MEM-GOV-01/02/03 anchors, STATE/ROADMAP/CORRECTNESS-PATH closure.

    5. **Decisions honored (D-01..D-04 verbatim)** — 4 sub-sections; each cites the plans that landed it + the test anchors + verification evidence.

    6. **Lessons learned** — 5-8 bullets distilled from per-plan summaries:
       - "Env-gate during Wave 1 + flip in Plan 04 = clean variant-add-with-rejection sequence" (Plan 01 lesson)
       - "Sidecar HashMap pattern (last_seen_ms) keeps Vec<AggOp> shape stable" (Plan 03 lesson)
       - "match-exhaustiveness via test (`every_aggkind_has_classified_bound`) + architectural-test (`every_op_str_in_agg_compile_has_bound_in_register_validate`) is the canonical lockstep enforcement" (Plans 04+05 combined lesson)
       - "Process-static atomic counters in beava-core + AdminState plumbing = zero-lock metric reads from admin sidecar" (Plan 06 lesson)
       - "Single-file architectural test sufficient for positive-coverage invariants; companion deletion-tripwire pattern is over-engineered when there's no deletion target" (Plan 05 lesson)
       - Plan 08 box-load + verdict honesty pattern (mirror 12.7-09 framing)
       - Any planner-surfaced concerns from per-plan summaries (e.g. "v0 metrics ship UNLABELED; per-source labels deferred to v0.0.x — this is a known limitation, not a bug")

    7. **Planner-surfaced concerns** (if any) — list any unresolved gotchas surfaced during execution:
       - Per-source label cardinality choice (UNLABELED in v0)
       - `bytes_per_entity_p99` ships as static placeholder
       - top_k classified as `BoundedByConfig("k", 10)` rather than `BoundedByRequiredKwarg("k")` for backward compat (Plan 04 trade-off)
       - Histogram requires `num_buckets` (or `buckets`) — verify whether the cap-where-missing affected any pre-existing tests + how those were swept
       - Hetzner-pending follow-up if Apple-M4 was the only run

    8. **Worktree map / status** — none changed in 12.8 (no new worktrees opened).

    Commit message: `docs(12.8-09): create 12.8-SUMMARY.md capturing phase narrative + plan-by-plan TOC + decisions honored`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && wc -l .planning/phases/12.8-memory-governance/12.8-SUMMARY.md && head -30 .planning/phases/12.8-memory-governance/12.8-SUMMARY.md && grep -c "## " .planning/phases/12.8-memory-governance/12.8-SUMMARY.md && grep -c "Plan 12.8-0" .planning/phases/12.8-memory-governance/12.8-SUMMARY.md</automated>
  </verify>
  <done>
    1. File `.planning/phases/12.8-memory-governance/12.8-SUMMARY.md` exists.
    2. Length ≥ 250 lines.
    3. Has frontmatter (YAML), Executive summary, Headline numbers, Plan-by-plan TOC, Architectural outcomes, Decisions honored, Lessons learned, Planner-surfaced concerns sections.
    4. Cites every Plan 12.8-XX (XX in 01..08) at least once.
    5. Cites all 4 CONTEXT decisions D-01..D-04.
    6. Single `docs:` commit per CLAUDE.md TDD §Note 4.
  </done>
</task>

<task type="auto">
  <name>Task 9.b: Create `.planning/phases/12.8-memory-governance/12.8-VERIFICATION.md` mechanical pass/fail per ROADMAP success criterion + per CONTEXT decision</name>
  <read_first>
    - .planning/phases/12.7-table-strip/12.7-VERIFICATION.md (canonical VERIFICATION template)
    - .planning/ROADMAP.md Phase 12.8 entry (line 52 — extract success criteria from the description)
    - .planning/phases/12.8-memory-governance/12.8-CONTEXT.md (D-01..D-04)
  </read_first>
  <action>
    Write `.planning/phases/12.8-memory-governance/12.8-VERIFICATION.md` modeled on 12.7-VERIFICATION.md. Sections:

    **Frontmatter** (10 lines): phase, status, verdict, date, head SHA.

    **Body** (~150 lines):

    1. **Overall verdict** — PASS / WARN / BLOCK (read from Plan 08 perf result; default PASS if Plan 08 said PASS).

    2. **ROADMAP success criteria** — table:
       | Criterion | Evidence | Verdict |
       |---|---|---|
       | Cold-entity TTL opt-in lands | crates/beava-server/tests/phase12_8_cold_entity_eviction.rs (7 tests) | PASS |
       | Lifetime aggregation contract enforced at register-time | crates/beava-core/src/register_validate.rs (4th shim) + crates/beava-core/tests/op_lifetime_bounds.rs (15 tests) | PASS |
       | 4th JSON-prelude shim landed | register_validate.rs `pre_check_unbounded_op_in_lifetime_mode` | PASS |
       | 54-op bound table populated | `lifetime_bound_for_op_str` 54 match arms | PASS |
       | Architectural test landed GREEN-by-default | phase12_8_lifetime_ops_have_bounds.rs (3 tests, no #[ignore]) | PASS |
       | 5 Prometheus metric families on /metrics | http_admin.rs metrics_handler + phase12_8_metrics_endpoint.rs (7 tests) | PASS |
       | Microbench cell: cold-TTL ENABLED ≤5% over DISABLED | perf-baselines.md Phase 12.8 section | PASS / WARN / BLOCK |
       | Throughput rebaseline: small/tcp regression-gate cell within ±10% | throughput-baselines.md Phase 12.8 section | PASS / WARN / BLOCK |
       | REQUIREMENTS.md V0-MEM-GOV-01/02/03 anchors | grep V0-MEM-GOV REQUIREMENTS.md returns 3 | PASS |
       | CLAUDE.md § Memory Governance Invariant block | grep "Memory Governance Invariant" CLAUDE.md returns ≥ 1 | PASS |
       | Section-ownership held | per-plan diffs verified | PASS |

    3. **CONTEXT decisions D-01..D-04** — table:
       | Decision | Plans | Outcome | Verdict |
       |---|---|---|---|
       | D-01 — `@bv.event(cold_after='7d')` per-source decorator only | 12.8-02, 12.8-03 | Decorator + EventDescriptor.cold_after_ms field; range [1s, 365d]; FRESH-state-on-resurrect | PASS |
       | D-02 — Implicit lifetime via `windowed=` omission | (no SDK change needed; existing behavior) | No new SDK kwarg; doc anchor in operator docs deferred to Phase 13 | PASS (no-op) |
       | D-03 — Hard reject at register-time via 4th JSON-prelude shim | 12.8-01, 12.8-04 | `pre_check_unbounded_op_in_lifetime_mode` + 54-row table; env-gate ON default with `=0` escape | PASS |
       | D-04 — Per-event reclaim via existing `update_at(now_ms)` mechanism | 12.8-06 (metric only — no new mechanism) | `bucket_reclaim_total` counter on /metrics confirms it fires | PASS |

    4. **Workspace tests** — `cargo test --workspace` count delta + 0 failures + 0 ignored.

    5. **Lint / fmt** — `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean + `cargo fmt --all --check` clean.

    6. **Performance verdict** — quote the numbers from Plan 08:
       - Microbench: cold_ttl_disabled vs 12.7 simple_counter: X% delta. Verdict: PASS / WARN / BLOCK.
       - Microbench: cold_ttl_enabled vs disabled: Y% delta. Verdict: PASS / WARN / BLOCK.
       - Throughput regression-gate cell small/tcp: Z% delta vs 751,498 EPS. Verdict: PASS / WARN / BLOCK.

    7. **Architectural-test gate** — `phase12_8_lifetime_ops_have_bounds.rs` GREEN by default; `phase12_8_cold_entity_eviction.rs` GREEN; `phase12_8_metrics_endpoint.rs` GREEN.

    8. **Final verdict line** — single sentence: `Phase 12.8 OFFICIALLY CLOSED 2026-05-XX (PASS|WARN|BLOCK).`

    Commit message: `docs(12.8-09): create 12.8-VERIFICATION.md mechanical pass/fail`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && wc -l .planning/phases/12.8-memory-governance/12.8-VERIFICATION.md && grep -c "^| " .planning/phases/12.8-memory-governance/12.8-VERIFICATION.md && grep -q "PASS\|WARN\|BLOCK" .planning/phases/12.8-memory-governance/12.8-VERIFICATION.md && grep -q "D-01\|D-02\|D-03\|D-04" .planning/phases/12.8-memory-governance/12.8-VERIFICATION.md</automated>
  </verify>
  <done>
    1. File exists; length ≥ 100 lines.
    2. Has overall verdict + ROADMAP success criteria table + CONTEXT decisions table + perf verdict + final verdict line.
    3. All 4 CONTEXT decisions D-01..D-04 cited.
    4. Single `docs:` commit.
  </done>
</task>

<task type="auto">
  <name>Task 9.c: Add `### Memory Governance Invariant (locked Phase 12.8)` block to CLAUDE.md sibling to existing 12.6 + 12.7 invariant blocks</name>
  <read_first>
    - CLAUDE.md (find the existing `### mio-only Hot-Path Invariant (locked Phase 12.6)` and `### Events-Only Invariant (locked Phase 12.7)` blocks via `grep -n "^### " CLAUDE.md`)
    - .planning/phases/12.7-table-strip/12.7-SUMMARY.md (Plan 10's Pattern 8 example)
  </read_first>
  <action>
    Open `CLAUDE.md`. Locate the existing § Events-Only Invariant block (the most recent invariant added by Phase 12.7-10). After that block ends and BEFORE the next section heading (likely `## Architecture` or similar), INSERT exactly:

    ```markdown

    ### Memory Governance Invariant (locked Phase 12.8)

    **Rule:** Every lifetime aggregation operator (windowless mode) declares a finite per-entity memory ceiling at register-time. The 4th JSON-prelude shim `pre_check_unbounded_op_in_lifetime_mode` in `crates/beava-core/src/register_validate.rs` rejects payloads with `unbounded_op_in_lifetime_mode` error code when (a) op-string is unclassified (typo / new op without classification) or (b) op declares `BoundedByRequiredKwarg("<kwarg>")` and `<kwarg>` is missing or non-positive in `params`. The classifier is `lifetime_bound_for_op_str` (54-row match table). Cold-entity TTL is opt-in per-source via `@bv.event(cold_after='<duration>')` (range [1s, 365d]); on event arrival the apply hot path lazily evicts the entity (FRESH state on resurrect, Redis TTL pattern locked permanent — D-04). Per-event bucket reclaim within active entities is the canonical Tier-2 mechanism (always on, not new). 5 Prometheus metric families on `/metrics` track the operational signal: `beava_cold_entity_evictions_total`, `beava_lifetime_op_cap_hit_total`, `beava_entity_count_resident`, `beava_bucket_reclaim_total`, `beava_bytes_per_entity_p99` (v0 ships UNLABELED — per-source labels deferred to v0.0.x). Default ON via env-gate `BEAVA_MEMORY_GOV_ENFORCE` (escape hatch: `BEAVA_MEMORY_GOV_ENFORCE=0`).

    **Why:** Locked architectural commitments — `project_v0_events_only_scope` (locked 2026-04-30) commits v0 to "users size their box up front"; `project_no_sharded_apply` (locked 2026-04-26) precludes background scan threads. Combined with `project_redis_shaped_no_event_time_ever`, v0's memory model is: in-memory state, single-thread apply, register-time-declared bounds, Tier 1 cold-TTL opt-in eviction lazy-on-arrival, Tier 2 windowed-op trailing-bucket reclaim per-event. No SSD overflow / tiered storage / background reclaim — those are explicitly deferred to v0.0.x or v0.1+ if operator demand surfaces.

    **How enforced:** `crates/beava-server/tests/phase12_8_lifetime_ops_have_bounds.rs` walks the workspace at `cargo test` time and fails if any op-string registered in `crates/beava-core/src/agg_compile.rs::op_str_to_agg_kind` is missing from `lifetime_bound_for_op_str` match arms (architectural-test-as-grep, single-file pattern). `crates/beava-server/tests/phase12_8_cold_entity_eviction.rs` covers the 7 happy-path / fresh-state-on-resurrect / null-TTL / multi-key scenarios. `crates/beava-server/tests/phase12_8_metrics_endpoint.rs` verifies the 5 Prometheus families on /metrics. `crates/beava-core/tests/op_lifetime_bounds.rs` enumerates every AggKind variant + asserts non-Unbounded classification (15 tests). CI runs all 4 tests on every PR via `cargo test --workspace`.

    **Reviving an unbounded lifetime aggregation requires:** explicit user override + new ADR overturning `project_v0_events_only_scope` or filing a new memory-governance ADR. Adding a new operator requires extending BOTH `op_str_to_agg_kind` AND `lifetime_bound_for_op_str` in lockstep — the architectural test fails if they drift.

    ```

    Verify the edit: `grep -n "^### " CLAUDE.md` shows the new heading appears immediately after § Events-Only Invariant. `git diff CLAUDE.md` shows ONLY the new block insertion.

    Commit message: `docs(12.8-09): add § Memory Governance Invariant (locked Phase 12.8) block to CLAUDE.md`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && grep -c "Memory Governance Invariant" CLAUDE.md && grep -n "^### " CLAUDE.md | tail -5 && grep -q "phase12_8_lifetime_ops_have_bounds" CLAUDE.md && cargo test --workspace 2>&1 | tail -3</automated>
  </verify>
  <done>
    1. CLAUDE.md contains `### Memory Governance Invariant (locked Phase 12.8)` (matches verbatim).
    2. The block sits as a sibling immediately after § Events-Only Invariant.
    3. The block contains Rule / Why / How enforced / Reviving requires sub-paragraphs.
    4. Cites the test anchor files (`phase12_8_lifetime_ops_have_bounds.rs`, `phase12_8_cold_entity_eviction.rs`, `phase12_8_metrics_endpoint.rs`).
    5. Single `docs:` commit.
  </done>
</task>

<task type="auto">
  <name>Task 9.d: Update STATE.md / ROADMAP.md / CORRECTNESS-PATH.md to advance v0 critical path Phase 12.8 → COMPLETED + Phase 13 → NEXT</name>
  <read_first>
    - .planning/STATE.md (entire file — find phase-progress, Current Focus, session-continuity, worktree-map sections)
    - .planning/ROADMAP.md line 52 (Phase 12.8 entry — flip from PLANNED to COMPLETED)
    - .planning/ROADMAP.md line 65-78 (total active phases + v0 critical-path block)
    - .planning/CORRECTNESS-PATH.md (Phase row table)
    - .planning/phases/12.7-table-strip/12.7-SUMMARY.md frontmatter (for the Phase 12.7 closure pattern that was applied to STATE/ROADMAP/CORRECTNESS-PATH — Plan 10 owned that work; mirror the diff scope)
  </read_first>
  <action>
    **STATE.md edits:**

    1. Update frontmatter `progress:`:
       - `completed_phases: 18 → 19` (Phase 12.8 closes)
       - `total_plans: 157 → 166` (Plans 12.8-01..09 = 9 new)
       - `completed_plans: 130 → 139` (9 new)
       - `percent: 83 → recompute` (139/166 = ~83.7%)
       - `last_updated: <today>`
       - `status: "Phase 12.8 CLOSED — Phase 13 NEXT (final v0 ship)"`

    2. Update top-of-file session-continuity HTML comment block (lines 15-18) to capture Plan 12.8-09 closure: `<!-- Last session: <today> — /gsd-execute-phase 12.8 Plan 09 landed (Wave 4: closure — STATE/ROADMAP/CORRECTNESS-PATH advance + CLAUDE.md § Memory Governance Invariant block + Phase 12.8 SUMMARY/VERIFICATION). Phase 12.8 OFFICIALLY CLOSED <date> (PASS|WARN|BLOCK) at HEAD <sha>. 9 plans landed (Plans 01-09) across 4 waves. -->`. Stop-at and Resume-files lines updated to point to 12.8 artifacts.

    3. Update **Current Focus** section header (~line 105). Replace the existing 12.7-CLOSED narrative with a Phase 12.8 narrative + advance to Phase 13. Mirror the Phase 12.7 closure shape (read 12.7's STATE update via `git log --oneline .planning/STATE.md | head -10` — find the Plan 12.7-10 commit and read its diff for the template).

    4. Update **Architectural pivot 2026-04-30** section line 103 — extend the v0 critical-path bullet:

    ```markdown
    **v0 critical path post-pivot:** ~~Plan 12-10 (push-and-get on mio)~~ DEFERRED per Phase 12.6 D-04 → ~~Phase 12.6 (surface reduction)~~ ✅ **CLOSED 2026-04-30 (PASS-WITH-WARN)** → ~~Phase 12.7 (table strip)~~ ✅ **CLOSED 2026-05-01 (PASS)** → ~~Phase 12.8 (memory governance)~~ ✅ **CLOSED <date> (PASS|WARN|BLOCK)** → **Phase 13 (final v0 ship — SDK polish + perf benchmarks + minimum-viable docs + PyPI/Docker/GitHub Releases) — NEXT**.
    ```

    **ROADMAP.md edits:**

    5. Locate Phase 12.8 entry at line ~52. Flip:
    ```
    | 12.8 | Memory governance ... | ... | ~6-10 | 📋 **PLANNED 2026-05-01** ...
    ```
    to:
    ```
    | 12.8 | Memory governance ... | ... | 9 | ✅ **COMPLETED <date> (PASS|WARN|BLOCK)** — 9 plans across 4 waves; workspace <count>/<failed>/<ignored>; 4th JSON-prelude shim + 54-row bound table + cold-TTL apply path + 5 Prometheus metrics + architectural test + REQUIREMENTS.md anchors V0-MEM-GOV-01/02/03 + CLAUDE.md § Memory Governance Invariant block; microbench cold_ttl_enabled <X>% over disabled; throughput regression-gate small/tcp <Y>% vs Phase 12.7 baseline (751,498 EPS). All 4 CONTEXT decisions D-01..D-04 honored verbatim. SUMMARY + VERIFICATION at `.planning/phases/12.8-memory-governance/`. |
    ```

    6. Update line 65 area: `**Total active phases:** 20 (post-2026-05-XX with Phase 12.8 CLOSED). v0 ship critical path = **Phase 13 (final v0 ship — SDK polish + benchmarks + ship) — NEXT**. Phase 12.8 (memory governance) ✅ CLOSED <date>.`

    7. Update Phase 13 line if needed — it's currently 🟡 PARTIAL; leave that status (Phase 13 is the next phase to plan).

    **CORRECTNESS-PATH.md edits:**

    8. Locate Phase 12.8 row. Flip its status from `Open` (or whatever the current value) to `🟢 CLOSED <date>`. Locate Phase 13 row, mark it `P0 #1`.

    9. Verify section-ownership: `git diff` on each of STATE/ROADMAP/CORRECTNESS-PATH should show ONLY the Phase 12.8 closure edits — no bleeding into 12.6/12.7 entries (which closed previously) or other phase rows.

    Commit message (one combined commit for the trio): `docs(12.8-09): advance v0 critical path — Phase 12.8 CLOSED + Phase 13 NEXT in STATE/ROADMAP/CORRECTNESS-PATH`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && grep -q "completed_phases: 19" .planning/STATE.md && grep -q "Phase 12.8 CLOSED" .planning/STATE.md && grep -q "Phase 12.8.*COMPLETED" .planning/ROADMAP.md && grep -q "Phase 12.8.*🟢 CLOSED" .planning/CORRECTNESS-PATH.md && cargo test --workspace 2>&1 | tail -3</automated>
  </verify>
  <done>
    1. STATE.md frontmatter `progress:` updated (completed_phases +1, total_plans +9, completed_plans +9, percent recomputed, status advanced).
    2. STATE.md `Current Focus` section narrates Phase 12.8 closure + Phase 13 NEXT.
    3. STATE.md session-continuity HTML comment updated.
    4. ROADMAP.md Phase 12.8 entry flipped to COMPLETED with verdict + brief summary.
    5. ROADMAP.md "Total active phases" line updated.
    6. CORRECTNESS-PATH.md Phase 12.8 row marked CLOSED + Phase 13 marked P0 #1.
    7. `git diff` on each file shows ONLY Phase 12.8-related changes (section-ownership held).
    8. Workspace stays GREEN (these are doc-only edits).
    9. Single `docs:` commit covering all 3 files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc-only closure | No production code touched; no attack surface change. |

## STRIDE Threat Register

No STRIDE threats. Doc-only edits.
</threat_model>

<verification>
- 4 doc files written/updated: SUMMARY.md, VERIFICATION.md, CLAUDE.md, STATE+ROADMAP+CORRECTNESS-PATH (3-in-1 trio commit).
- `grep -c "Memory Governance Invariant" CLAUDE.md` returns ≥ 1.
- `grep -c "V0-MEM-GOV-0[123]" .planning/REQUIREMENTS.md` returns 3 (added by Plan 07).
- `grep -q "Phase 12.8 CLOSED" .planning/STATE.md`.
- `grep -q "Phase 12.8.*COMPLETED" .planning/ROADMAP.md`.
- `grep -q "Phase 12.8.*🟢 CLOSED" .planning/CORRECTNESS-PATH.md`.
- Workspace stays GREEN (this plan touches no Rust source).
- `cargo clippy + fmt` clean (no-op for this plan).
</verification>

<success_criteria>
- 12.8-SUMMARY.md created with full narrative + plan-by-plan TOC + decisions honored.
- 12.8-VERIFICATION.md created with mechanical pass/fail per criterion.
- CLAUDE.md gains § Memory Governance Invariant (locked Phase 12.8) block.
- STATE.md / ROADMAP.md / CORRECTNESS-PATH.md advance to Phase 12.8 CLOSED + Phase 13 NEXT.
- Section-ownership held: this plan owns the closure docs; other plans untouched.
- Multiple `docs:` commits (one per logical unit) per CLAUDE.md TDD §Note 4.
- Phase 12.8 OFFICIALLY CLOSED.
</success_criteria>

<output>
After completion, this plan IS the closure — there is no separate `12.8-09-SUMMARY.md` (the work IS the SUMMARY for the phase). However, optionally create a 1-paragraph `.planning/phases/12.8-memory-governance/12.8-09-SUMMARY.md` capturing:
- 4 commit SHAs (one per closure file/group)
- Confirmation that section-ownership held (per-plan diff inspection)
- Pointer to the headline doc: 12.8-SUMMARY.md
- One Lessons-Learned bullet for "section-ownership at closure" (e.g., "4-commit closure pattern — SUMMARY / VERIFICATION / CLAUDE.md / STATE+ROADMAP+CORRECTNESS-PATH trio — kept the diff explicit and reviewable, mirroring 12.6-13 + 12.7-10")
</output>