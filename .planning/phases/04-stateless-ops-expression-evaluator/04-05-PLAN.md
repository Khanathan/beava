---
phase: "04"
plan: "05"
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - crates/beava-core/src/register_validate.rs
  - crates/beava-core/src/registry.rs
  - crates/beava-server/src/register.rs
autonomous: true
requirements:
  - SRV-APPLY-07
  - SDK-COL-07
tags: [register-integration, op-chain-install, http, tcp, validation-pipeline, schema-propagation, invalid-expression-error]

must_haves:
  truths:
    - "Register-time validation parses every Filter/WithColumns/Map expression in the incoming payload via expr::parse (Plan 04-02)"
    - "Register-time validation walks op chains through schema_propagate (Plan 04-04) to derive per-derivation output schemas and reject unknown field references"
    - "A malformed expression in a payload triggers HTTP 400 with error code `invalid_expression` and a `path` pointing to the offending nodes[N].ops[M].expr — satisfies ROADMAP SC5"
    - "The compiled OpChain is cached on the installed DerivationDescriptor (or registry-side parallel structure) so Plan 04-06's acceptance test can prove apply() works for a registered derivation"
    - "Wire contracts for /register (HTTP) and op=register (TCP) do NOT change — the JSON payload shape is identical; only validation surface expands"
    - "Both transports (HTTP + TCP) surface invalid_expression errors identically because both share execute_register from Phase 2.5"
    - "Existing 256+ pre-existing tests still pass (no regressions); prior 400/409/415 paths untouched"
  artifacts:
    - path: "crates/beava-core/src/register_validate.rs"
      provides: "ErrorCode::InvalidExpression + ErrorCode::SchemaPropagationFailure + ErrorCode::UnknownFieldReference + ErrorCode::InvalidCastTarget; new validate_expressions pass extending validate_payload's output with expression-level errors"
      contains: "InvalidExpression"
    - path: "crates/beava-core/src/registry.rs"
      provides: "DerivationDescriptor.compiled_op_chain: Option<OpChain> (skipped on serialize — runtime-only); apply_registration caches the compiled chain when installing"
      contains: "compiled_op_chain"
    - path: "crates/beava-server/src/register.rs"
      provides: "RegisterError::Validation now maps InvalidExpression/SchemaPropagationFailure/UnknownFieldReference/InvalidCastTarget error codes to code='invalid_expression' on the wire with the offending expression path"
      contains: "invalid_expression"
  key_links:
    - from: "crates/beava-core/src/register_validate.rs::validate_payload"
      to: "crates/beava-core/src/op_chain.rs::OpChain::compile"
      via: "New Rule 10 (expression validation) compiles every derivation's op chain; failures produce ValidationError entries"
      pattern: "OpChain::compile"
    - from: "crates/beava-server/src/register.rs::execute_register"
      to: "crates/beava-core/src/registry.rs::apply_registration"
      via: "Successful compile caches the OpChain on the descriptor at install time (atomic with version bump)"
      pattern: "apply_registration"
---

<objective>
Wire Plans 04-02/04-03/04-04 into the server-side register pipeline. Register-time validation now parses every op-chain expression, runs schema propagation, and compiles each derivation's OpChain. Compiled chains are cached on the installed descriptor so Plan 04-06's acceptance test (and the future Phase 6 push path) can call `apply(row)` directly.

Purpose: Makes Plan 04-04's library-level work visible on the wire. The HTTP path `POST /register` and the TCP path `op=register` both already share `execute_register` (Phase 2.5 Plan 03), so a single integration point delivers both transports. SC5 (malformed predicate → 400 with path) is satisfied here. SDK-COL-07 is fully satisfied here because the validation pipeline now walks expression AST → resolves references against the upstream-derived schema → rejects unknown field references at register time with path `nodes[N].ops[M].expr`.

Output: Extended `register_validate.rs` with expression/schema-propagation errors, extended `registry.rs` to cache the compiled OpChain on the DerivationDescriptor, and an extended `register.rs` that maps the new error codes onto the existing `{code, path, reason}` wire shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-stateless-ops-expression-evaluator/04-CONTEXT.md
@.planning/phases/02-sources-registry-version-bumps/02-SUMMARY.md
@.planning/phases/02.5-tcp-wire-listener/02.5-SUMMARY.md
@CLAUDE.md

@crates/beava-core/src/register_validate.rs
@crates/beava-core/src/registry.rs
@crates/beava-core/src/registry_diff.rs
@crates/beava-core/src/op_chain.rs
@crates/beava-core/src/schema_propagate.rs
@crates/beava-core/src/expr.rs
@crates/beava-core/src/op_node.rs
@crates/beava-core/src/schema.rs
@crates/beava-server/src/register.rs
@crates/beava-server/src/tcp.rs

<interfaces>
<!-- From register_validate.rs (Phase 2, still authoritative) — you extend ErrorCode and add a new validation rule -->

```rust
pub enum ErrorCode {
    InvalidRegistration,
    NameDuplicate,
    NameReservedPrefix,
    NameBadPattern,
    NameEmpty,
    NameTooLong,
    EventTimeFieldMissing,
    EventTimeFieldWrongType,
    EventSchemaEmpty,
    TablePrimaryKeyEmpty,
    TablePrimaryKeyTooLong,
    TablePrimaryKeyUnknownField,
    DerivationUpstreamUnknown,
    DerivationSchemaEmpty,
    RegistrationCycle,
    TopologicalOrderViolation,
    DedupeKeyUnknownField,
    DedupeWindowNonPositive,
    DerivationOutputKindTableMissingPrimaryKey,
    // Phase 4 additions:
    InvalidExpression,              // parse error in a Filter/WithColumns/Map expression
    UnknownFieldReference,          // expression references a field not in the upstream schema
    SchemaPropagationFailure,       // type mismatch / rename collision / etc. per propagate_schema
    InvalidCastTarget,              // cast target type string not in {"str","int","float","bool"} or illegal pair
    UnsupportedOpInPhase4,          // GroupBy/Join/Union appearing in an op chain that's not otherwise scoped (optional — may treat as pass-through and defer to Phase 5/12)
}
```

<!-- HTTP/TCP wire shape for validation errors (Phase 2 lock; unchanged): -->

```json
{
  "error": {
    "code": "invalid_registration",
    "path": "nodes[2].ops[1].expr",
    "reason": "col 14: expected ')' but found '>'"
  },
  "registry_version": N
}
```

Phase 4 uses `code: "invalid_expression"` (a new code string distinct from `invalid_registration`) for parse/schema-propagation errors per CONTEXT.md §D-02. Map `ErrorCode::InvalidExpression|UnknownFieldReference|SchemaPropagationFailure|InvalidCastTarget` → wire code `"invalid_expression"`.

<!-- From registry.rs — DerivationDescriptor struct to extend -->

```rust
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct DerivationDescriptor {
    pub name: String,
    pub output_kind: OutputKind,
    pub upstreams: Vec<String>,
    pub ops: Vec<OpNode>,
    pub schema: DerivedSchema,
    pub table_primary_key: Option<Vec<String>>,
    pub registered_at_version: u64,
    // Phase 4 addition (runtime-only; skip in (de)serialize):
    #[serde(skip)]
    pub compiled_op_chain: Option<OpChain>,
}
```

<!-- From Phase 2.5 — execute_register is the shared transport-agnostic core -->

```rust
pub(crate) async fn execute_register(
    registry: &Arc<Registry>,
    payload: RegisterPayload,
) -> RegisterOutcome;
```

You extend execute_register's validation step (between validate_payload and compute_diff) to include expression parsing + schema propagation, and extend apply_registration to cache compiled chains.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (red): Register-integration failing tests — HTTP 400 for bad expressions, TCP parity, successful install caches OpChain</name>
  <files>crates/beava-core/src/register_validate.rs, crates/beava-server/src/register.rs</files>
  <read_first>
    - crates/beava-core/src/register_validate.rs (existing 9 validation rules + ValidationError / ErrorCode / ValidatedPayload)
    - crates/beava-core/src/registry.rs (DerivationDescriptor + apply_registration + PartialEq implications of adding a field)
    - crates/beava-server/src/register.rs (execute_register, map_outcome_to_http, RegisterError::Validation shape)
    - crates/beava-server/src/tcp.rs (handle_register for TCP path — verify it maps ValidationFailed the same way)
    - crates/beava-core/src/op_chain.rs + crates/beava-core/src/schema_propagate.rs (OpChain::compile + PropagationError — the red tests reference these)
    - .planning/phases/04-stateless-ops-expression-evaluator/04-CONTEXT.md §D-02 + §D-06
    - .planning/phases/02-sources-registry-version-bumps/02-SUMMARY.md (existing test patterns)
    - .planning/phases/02.5-tcp-wire-listener/02.5-SUMMARY.md (TCP test patterns)
    - CLAUDE.md §Conventions
  </read_first>
  <behavior>

    Red commit extends two existing files with new failing tests. No production code changes yet (the tests will fail because the integration is unimplemented).

    **In `crates/beava-core/src/register_validate.rs`** — add these tests to the existing `tests_structural` module (or a new `tests_expression` module):

    1. `rule10_fail_filter_with_parse_error` — payload with derivation containing `OpNode::Filter { expr: "(amount > " }` → Err containing `ValidationError { code: ErrorCode::InvalidExpression, path: "nodes[N].ops[0].expr", reason: <non-empty> }`. N is the index of the derivation in the payload.
    2. `rule10_fail_filter_references_unknown_field` — payload with event `A{schema: {amount: F64}}` + derivation `D` upstream `A` with `Filter{"(missing > 0)"}` → Err with ErrorCode::UnknownFieldReference, path `nodes[1].ops[0].expr`, reason mentions "missing".
    3. `rule10_fail_with_columns_type_mismatch` — derivation with `WithColumns{{bad: "(amount and true)"}}` where amount is F64 → Err with ErrorCode::SchemaPropagationFailure.
    4. `rule10_fail_cast_invalid_target` — derivation with `Cast{{amount: "blob"}}` → Err with ErrorCode::InvalidCastTarget.
    5. `rule10_fail_select_unknown_field` — derivation with `Select{["missing"]}` → Err with ErrorCode::SchemaPropagationFailure (or UnknownFieldReference — pick one and test that exactly; spec says UnknownFieldReference for expression-level fields, SchemaPropagationFailure for op-level — document the distinction).
    6. `rule10_pass_valid_filter_and_with_columns_chain` — event A{amount: F64} + derivation D upstream A ops=[Filter{"(amount > 100)"}, WithColumns{{is_big: "(amount > 500)"}}] → validate_payload Ok(ValidatedPayload). The returned ValidatedPayload MUST also carry the compiled chains OR the derivation's schema field in the payload MUST match the propagated schema (bundle the derived schema into validation output — decide during Task 1.b how; for the red test, assert only that validation returns Ok).
    7. `rule10_client_supplied_derivation_schema_must_match_propagated` — payload says derivation D has schema{amount: F64} but propagation infers {amount: F64, is_big: Bool} (because ops include `WithColumns{is_big}`) → either (a) server RESPECTS the propagated schema and ignores client-supplied schema, logging a warning, or (b) server ERRORS when client and propagated diverge. CONTEXT.md §D-06 says "Replaces Phase 2's 'trust client-supplied derived schema' for op chains" — so the server now REPLACES the client schema with the propagated one. Test: Ok(ValidatedPayload) returned, but the inner DerivationDescriptor's schema has been overwritten with {amount, is_big}. (If "overwriting" is hard to assert at the validate layer, defer this test to task 1.b apply_registration path and assert against the installed registry post-install; restructure this test accordingly during 1.b; in the red phase just write the test as described.)
    8. `rule10_empty_ops_is_fine` — derivation with no ops passes schema propagation trivially (derived schema = upstream schema). Ok.
    9. `rule10_fail_soft_collects_expression_errors_per_op` — derivation with TWO bad ops (Filter{"(missing > 0)"} and Cast{{amount: "blob"}}) → Err with TWO ValidationError entries, paths `nodes[N].ops[0].expr` and `nodes[N].ops[1].type_map.amount`.
    10. `rule10_event_or_table_with_ops_is_ignored` — events and tables don't have ops in Phase 2's wire shape; verify the validator ignores any accidental payload field and focuses only on DerivationDescriptor.ops. Trivial passing test; documents the boundary.

    **In `crates/beava-server/src/register.rs`** — add these axum-integration tests:

    11. `test_register_invalid_filter_returns_400_with_invalid_expression_code` — POST a payload with `{kind: derivation, name: D, upstreams: [A], ops: [{op: "filter", expr: "(amount > "}], ...}` → 400; body["error"]["code"] == "invalid_expression"; body["error"]["path"] contains `nodes[1].ops[0].expr`; body["error"]["reason"] mentions col 13 or the parse failure.
    12. `test_register_unknown_field_in_filter_returns_400` — same shape but expression `"(nonexistent > 0)"` → 400 with code="invalid_expression", path points at the op, reason mentions "nonexistent".
    13. `test_register_invalid_cast_target_returns_400` — ops includes `{op: "cast", type_map: {amount: "blob"}}` → 400, code="invalid_expression" (since SC5 says malformed-predicate category), path points at the cast op.
    14. `test_register_with_columns_chain_propagates_schema_and_200s` — payload: event A{amount: F64} + derivation D with ops [`filter (amount>0)`, `with_columns is_big=(amount>500)`, `cast is_big=int`], client-supplied D schema = {amount: F64} (wrong — missing is_big). Server responds 200 with registry_version bumped AND if we re-fetch GET /registry, D's schema now contains {amount: F64, is_big: I64}. (Test requires GET /registry access; dev endpoints flag passed true in the test router like existing tests.)
    15. `test_register_chained_ops_compile_is_cached_on_install` — after the successful register above, registry.derivations["D"].compiled_op_chain MUST be Some(_). Access via a thin helper on Registry (e.g., `registry.get_derivation(name)` that returns a clone of the stored descriptor — noting the skip-serialize tag on compiled_op_chain means it won't be in the JSON, but the in-memory clone carries it). Phase 2's Registry already exposes `snapshot()`; add a helper or use snapshot() and assert `snapshot.derivations["D"].compiled_op_chain.is_some()`.
    16. `test_register_invalid_expression_via_tcp_frame_returns_error_frame` — use TestServer's TCP client (from Phase 2.5) to send a register frame with the same bad-filter payload as test 11; expect an error response frame (op=0xFFFF) with the same `{code, path, reason}` shape mapped over. If TestServer TCP helpers don't already have an assertion builder for error frames, mirror Phase 2.5 smoke's approach.

    All 16 tests MUST fail until Task 1.b implements the integration.

    Do NOT change production code in this subtask. The new `ErrorCode` variants can be added here because Phase 2's ErrorCode enum is public; we simply extend the enum. Adding enum variants is OK — downstream pattern matches will fail to compile BECAUSE the new variants aren't handled, which is fine — we adjust match arms in Task 1.b. If the new variant causes pre-existing tests to fail (compile error) the red commit lands with the VARIANT added but tests stubbed with `panic!("red stub: 04-05 impl pending")` to keep the rest of the workspace building. Prefer this approach: add ErrorCode variants + add the test bodies with `panic!("red: 04-05 impl pending")` bodies so the workspace still compiles and the tests fail loudly with a clear message.

    ALTERNATIVE red pattern: write the real test bodies (they'll fail because the behavior isn't implemented). If the test bodies don't compile (because they call functions that don't exist yet), create tiny stub helpers with `todo!()` bodies. Prefer this over the `panic!(...)` stub because the green commit is cleaner (just replace `todo!()` with real impl, no test-body rewrites).
  </behavior>
  <action>
    Commit: `test(04-05): add failing register-integration tests for expression validation + op-chain caching`.
  
  </action>
  <verify>
    <automated>cargo test -p beava-core register_validate::tests && cargo test -p beava-server register::tests</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p beava-core register_validate --no-run` exits 0.
    - `cargo test -p beava-server register --no-run` exits 0.
    - `cargo test -p beava-core register_validate` exits non-zero (new 10 tests FAIL; pre-existing tests still pass OR also fail IF ErrorCode additions broke their match arms — in that case add `_ => panic!("unhandled in red commit")` arms to keep pre-existing tests green; reviewer sanity check: `cargo test -p beava-core register_validate::tests` should show exactly the 10 new tests as FAILED and every other register_validate test as PASSED).
    - `cargo test -p beava-server register::tests::test_register_invalid_filter_returns_400_with_invalid_expression_code` fails.
    - `cargo test -p beava-server register::tests::test_register_chained_ops_compile_is_cached_on_install` fails.
    - `cargo test -p beava-server phase2_5_smoke` still passes (no regressions in Phase 2.5 TCP smoke).
    - `cargo test -p beava-server phase2_smoke` still passes.
    - `cargo build --workspace` exits 0 (stubs compile).
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0.
    - `cargo fmt --all --check` exits 0.
    - `git log -1 --format='%s' | grep -E '^test\(04-05\):'` matches.
    - `git show --stat HEAD` touches register_validate.rs, register.rs, and possibly registry.rs (if the red commit added the compiled_op_chain field; that's fine — it's a stub addition with #[serde(skip)] default).
  </acceptance_criteria>
  <done>16 new tests RED; workspace still builds + clippy/fmt clean; prior phases' tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (green): Wire register_validate + apply_registration + execute_register to parse expressions, propagate schemas, cache OpChains, and surface invalid_expression errors</name>
  <files>crates/beava-core/src/register_validate.rs, crates/beava-core/src/registry.rs, crates/beava-server/src/register.rs</files>
  <read_first>
    - crates/beava-core/src/register_validate.rs (your own red commit's new tests)
    - crates/beava-core/src/registry.rs (apply_registration + RegistryInner — snapshot/apply semantics)
    - crates/beava-core/src/op_chain.rs + crates/beava-core/src/schema_propagate.rs (the public APIs you compose)
    - crates/beava-server/src/register.rs (execute_register — you hook in after validate_payload)
    - crates/beava-server/src/tcp.rs (handle_register — verify ValidationFailed maps identically)
    - .planning/phases/04-stateless-ops-expression-evaluator/04-CONTEXT.md §D-02, §D-05, §D-06
  </read_first>
  <action>
    **Step 1 — Extend `registry.rs::DerivationDescriptor`**:
    - Add `#[serde(skip)] pub compiled_op_chain: Option<OpChain>,` and make OpChain `Clone + Debug` (should already be from Plan 04-04; if not, add the derives there as a touch-up — but prefer not to: see alternative).
    - Alternative if OpChain is expensive to Clone: stash it on a parallel structure `registry.compiled_chains: BTreeMap<String, OpChain>` keyed by derivation name, and add `Registry::compiled_chain_for(name)` accessor. This avoids making the descriptor-facing structs mutate and avoids the Clone constraint. **PICK THE PARALLEL STRUCTURE** path — less invasive, keeps the descriptor serialization-clean, and fits the existing RwLock<RegistryInner> pattern.
    - So: `RegistryInner.compiled_chains: BTreeMap<String, Arc<OpChain>>` (Arc for cheap sharing with the apply loop in Phase 5+).
    - `impl Registry { pub fn compiled_chain(&self, derivation_name: &str) -> Option<Arc<OpChain>> { self.inner.read().compiled_chains.get(name).cloned() } }`.
    - Update `apply_registration` to ALSO write the compiled chains it was passed by the caller (new signature: `apply_registration(&self, nodes: Vec<PayloadNode>, compiled_chains: Vec<(String, Arc<OpChain>)>) -> u64` — or tuple them in PayloadNode at validation exit time). PICK: validation returns `ValidatedPayload { nodes, compiled_chains: Vec<(String, Arc<OpChain>)>, derived_schemas: Vec<(String, DerivedSchema)> }` — the second and third let apply_registration install everything atomically.

    **Step 2 — Extend `register_validate.rs`**:
    - Extend `ValidatedPayload` struct to carry `compiled_chains: Vec<(String, Arc<OpChain>)>` and `propagated_schemas: Vec<(String, DerivedSchema)>`. Update existing Phase 2 tests that construct `ValidatedPayload(vec)` to `ValidatedPayload { nodes: vec, compiled_chains: vec![], propagated_schemas: vec![] }` as needed — keep the `as_slice()` and `into_inner()` methods working (into_inner now returns all three via a destructured struct or a helper `into_parts()`).
    - Add a new validation pass `validate_expressions` (Rule 10) called AFTER the existing Rules 1–9:
      * For each PayloadNode::Derivation:
        - Resolve upstream schemas: Look up each upstream by name (in payload nodes first, then in current registry). Union them into a single `Schema` (matches SRV-APPLY rules — a derivation whose upstreams are multiple events gets the UNION of their fields; collisions are caught at register-time with SchemaPropagationFailure). If a derivation has zero upstreams (this shouldn't happen per Rule 5 but defensively), skip expression validation.
        - Call `OpChain::compile(&combined_schema, &deriv.ops)`.
        - If Err(errors): translate each PropagationError → ValidationError:
          - `FieldMissing { op_index, field }` → ErrorCode::UnknownFieldReference, path `nodes[N].ops[op_index].expr`, reason `field '{field}' not found in upstream schema`.
          - `TypeMismatch { op_index, reason }` → ErrorCode::SchemaPropagationFailure, path `nodes[N].ops[op_index]`, reason verbatim.
          - `RenameCollision { op_index, new }` → ErrorCode::SchemaPropagationFailure, path `nodes[N].ops[op_index].mapping`, reason `rename collision on field '{new}'`.
          - `InvalidExpr { op_index, parse_error }` → ErrorCode::InvalidExpression, path `nodes[N].ops[op_index].expr`, reason `col {parse_error.col}: {parse_error.reason}`.
          - `UnsupportedOp { op_index, op }` → ErrorCode::UnsupportedOpInPhase4, path `nodes[N].ops[op_index]`, reason `op '{op}' is not supported in Phase 4 (lands in Phase 5/12)`. But: the 3 unsupported ops (group_by, join, union) ARE in Phase 5/12 scope — we should treat them as pass-through during Phase 4 validation, NOT as errors (registering a derivation with a group_by op in Phase 4 should not fail — it should just skip validation of that op, or accept it with a TODO warning). Revise: propagate_schema in Plan 04-04 returns UnsupportedOp, but register-time Rule 10 CATCHES UnsupportedOp and treats it as a pass-through (continues propagation with the current schema unchanged) — logging a WARN. Document this path.
          - `InvalidCastTarget { op_index, target }` (if Plan 04-04 added this variant to PropagationError; if not, it's folded into TypeMismatch — PICK: fold into TypeMismatch, then Rule 10 can distinguish by inspecting reason substring. Simpler: Plan 04-04's PropagationError has a generic TypeMismatch which covers both "cast target unknown" and "boolean op on non-bool operands"; Rule 10 maps all TypeMismatch → ErrorCode::SchemaPropagationFailure; and we drop ErrorCode::InvalidCastTarget as a separate code — test 4 in the red phase asserts SchemaPropagationFailure instead. Rewrite test 4 accordingly in Task 1.a OR land it as SchemaPropagationFailure here; TEST must match IMPL. CHOOSE: keep InvalidCastTarget distinct at the ErrorCode enum level; if Plan 04-04 didn't emit that variant, add a Rule-10-level check that inspects Cast ops BEFORE calling OpChain::compile and maps bad targets directly. Cleanest.)
        - If Ok((compiled_chain, final_schema)): record `(name, Arc::new(compiled_chain))` in `compiled_chains` and `(name, final_schema.into_derived())` in `propagated_schemas`.
      * After all derivations are validated, build `ValidatedPayload { nodes, compiled_chains, propagated_schemas }`.
      * Fail-soft: collect all errors across all derivations, return Err(all_errors) if any.
    - Modify `validate_payload` to call `validate_expressions(&payload, current)` after Rule 7/8 and propagate its errors.

    **Step 3 — Integrate at `apply_registration`**:
    - Update `Registry::apply_registration` signature to accept `ValidatedPayload` as a whole (instead of `Vec<PayloadNode>`) so it sees compiled_chains + propagated_schemas.
    - For each derivation installed, OVERWRITE `deriv.schema` with `propagated_schemas.get(&deriv.name).cloned().unwrap_or(deriv.schema.clone())` before inserting into RegistryInner.derivations.
    - Insert the compiled chains into `RegistryInner.compiled_chains` atomically under the same write-lock acquisition.
    - Bump `version` as before.

    **Step 4 — Integrate at `execute_register`**:
    - The shape change is that validated.into_inner() now yields 3 things. Adjust `execute_register` accordingly: destructure via the new `into_parts()` helper.
    - Call `registry.apply_registration(validated_parts)` at the install step.

    **Step 5 — Map new error codes in `register.rs::map_outcome_to_http`**:
    - Extend `map_outcome_to_http` (and the parallel TCP mapping in `tcp.rs` — probably already delegates to a shared mapper; if it duplicates the logic, refactor to share) so:
      * `ErrorCode::InvalidExpression | UnknownFieldReference | SchemaPropagationFailure | InvalidCastTarget` → wire `code: "invalid_expression"` (DIFFERENT from the Phase 2 existing `"invalid_registration"`).
      * `ErrorCode::UnsupportedOpInPhase4` → should never fire in practice (we treat as pass-through); if it does, map to `"invalid_registration"` + path.
      * Every other ErrorCode continues to map to `"invalid_registration"`.

    **Step 6 — Verify + fix any Phase 2 test breakage**:
    - `cargo test --workspace` — any Phase-2/2.5 tests that constructed `ValidatedPayload(vec)` directly may need to use `ValidatedPayload::from_nodes(vec)` helper. Add the helper:
      ```rust
      impl ValidatedPayload {
          pub fn from_nodes(nodes: Vec<PayloadNode>) -> Self {
              Self { nodes, compiled_chains: vec![], propagated_schemas: vec![] }
          }
          pub fn into_parts(self) -> (Vec<PayloadNode>, Vec<(String, Arc<OpChain>)>, Vec<(String, DerivedSchema)>) {
              (self.nodes, self.compiled_chains, self.propagated_schemas)
          }
      }
      ```
      Keep the old `into_inner()` as an alias for `into_parts().0` to preserve backward compat inside the crate.

    **Step 7 — Run the full gate**:
    ```
    cargo test --workspace
    cargo clippy --workspace --all-targets --all-features -- -D warnings
    cargo fmt --all --check
    ```

    Commit message MUST match `^feat\(04-05\):` (or split into multiple commits if the changes are large — e.g., `feat(04-05): extend ValidatedPayload with compiled chains` + `feat(04-05): map invalid_expression errors on HTTP/TCP`; both must match `^feat\(04-05\):` per CLAUDE.md).
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p beava-core register_validate::tests` exits 0 (all Phase-2 tests + new Phase-4 tests 1–10 pass).
    - `cargo test -p beava-server register::tests` exits 0 (all Phase-2 tests + new Phase-4 tests 11–16 pass).
    - `cargo test -p beava-server phase2_smoke phase2_5_smoke` exits 0 (no regressions on prior smoke tests).
    - `cargo test --workspace` exits 0 (all ≥ 337 tests from earlier plans + the new ~16 integration tests pass).
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0.
    - `cargo fmt --all --check` exits 0.
    - `grep -c "ErrorCode::InvalidExpression\|ErrorCode::UnknownFieldReference\|ErrorCode::SchemaPropagationFailure\|ErrorCode::InvalidCastTarget" crates/beava-core/src/register_validate.rs` ≥ 4 (each variant used in the mapping or emission).
    - `grep "\"invalid_expression\"" crates/beava-server/src/register.rs` matches (wire code is mapped).
    - `grep "compiled_chains\|compiled_chain" crates/beava-core/src/registry.rs` matches (registry caches chains).
    - `grep "OpChain::compile" crates/beava-core/src/register_validate.rs` matches (register-time compilation wires through).
    - `git log -1 --format='%s' | grep -E '^feat\(04-05\):'` matches.
    - Red→green chain: `git log --format='%s' HEAD~1..HEAD | head -1 | grep -E '^feat\(04-05\):'` matches; `git log --format='%s' HEAD~2..HEAD~1 | head -1 | grep -E '^test\(04-05\):'` matches (or an explicit multi-commit green sequence — all prefixed with `feat(04-05):` / `refactor(04-05):` / `fix(04-05):`).
  </acceptance_criteria>
  <done>Integration complete; 16 red tests green; no workspace regressions; wire code `invalid_expression` reachable via both HTTP and TCP for every Phase-4 validation failure; OpChain cached on install; SC5 achieved.</done>
</task>

</tasks>

<verification>
- `cargo test --workspace` — all tests pass (approx ≥ 350 total)
- `cargo test -p beava-server register::tests::test_register_invalid_filter_returns_400_with_invalid_expression_code -- --nocapture` — prints the 400 body and proves path + code shape
- `cargo test -p beava-server register::tests::test_register_invalid_expression_via_tcp_frame_returns_error_frame -- --nocapture` — prints the TCP error frame and proves parity
- `cargo test -p beava-server register::tests::test_register_chained_ops_compile_is_cached_on_install -- --nocapture` — prints `compiled_op_chain: Some(OpChain { .. })`
- `cargo test -p beava-core register_validate::tests::rule10_client_supplied_derivation_schema_must_match_propagated -- --nocapture` — proves propagated schema REPLACES client-supplied one for derivations with ops
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` — clean
- `cargo fmt --all --check` — clean
</verification>

<success_criteria>
The Phase 4 REQ-IDs that involve server wiring (SDK-COL-07 for schema-reference resolution, SRV-APPLY-07 for register-time op-chain compilation) are end-to-end correct. ROADMAP SC5 is satisfied: a malformed predicate returns 400 with a path pointing to the offending expression. HTTP and TCP transports both surface `invalid_expression` errors identically because they share `execute_register`. OpChain is cached on install, so Plan 04-06 can call it in the acceptance test and Phase 6 can reuse it during push.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stateless-ops-expression-evaluator/04-05-SUMMARY.md` documenting: (1) the ValidatedPayload struct change + migration (into_parts vs into_inner helpers); (2) the compiled_chains Arc placement strategy (parallel RegistryInner map vs descriptor field, and why); (3) the wire-code mapping table (each ErrorCode → wire code); (4) the "propagated schema REPLACES client-supplied" decision; (5) interfaces exposed to Plan 04-06 (Registry::compiled_chain accessor, propagated DerivedSchema visible via GET /registry).
</output>
