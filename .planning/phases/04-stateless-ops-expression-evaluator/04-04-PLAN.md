---
phase: "04"
plan: "04"
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - crates/beava-core/src/op_chain.rs
  - crates/beava-core/src/schema_propagate.rs
  - crates/beava-core/src/lib.rs
autonomous: true
requirements:
  - SDK-OPS-01
  - SDK-OPS-02
  - SDK-OPS-03
  - SDK-OPS-04
  - SDK-OPS-05
  - SDK-OPS-06
  - SDK-OPS-07
  - SDK-OPS-08
  - SDK-OPS-10
  - SDK-COL-07
  - SRV-APPLY-07
tags: [op-chain, schema-propagation, filter, select, drop, rename, with_columns, map, cast, fillna, beava-core]

must_haves:
  truths:
    - "OpChain::apply(row) executes the 8 stateless ops in sequence; Filter dropping a row yields None, all other ops yield Some(updated_row)"
    - "Filter uses parsed Expr + evaluator; Filter predicate that returns Null or Bool(false) drops the row; Bool(true) keeps it (matches SQL semantics)"
    - "WithColumns and Map (Map is alias for WithColumns) add/overwrite fields from Expr evaluation; type is inferred at register-time from the Expr"
    - "Select keeps listed fields in order (rejected at register-time if any field missing from input schema)"
    - "Drop removes listed fields (rejected at register-time if any field missing; Table drops its key is a special case left to register-time since op-chain only sees schema, not descriptor kind)"
    - "Rename applies mapping atomically (batch); collisions in the output rejected at register-time"
    - "Cast invokes the cast builtin per field; register-time checks each cast is legal"
    - "Fillna replaces Null values with the literal default; clears the `optional_fields` flag on the affected fields in the schema propagation output"
    - "Schema propagation walks (upstream_schema, ops) and produces the derived Schema — driven by the OpNode variant, validating each op against the current schema at that step"
    - "All 9 ops (including WithColumns + Map as two OpNode variants with shared implementation) are covered; unknown/unsupported ops (GroupBy, Join, Union) pass through the op-chain executor as no-ops and are flagged at register-time but not executed here (they land in Phase 5/12)"
    - "SDK-COL-07 field-reference resolution is load-bearing at this layer: propagate_schema walks every Filter/WithColumns/Map expression's `referenced_fields()` (from Plan 04-02) and returns a PropagationError with op_index + field name when a reference is missing — Plan 04-05 wires this to the `invalid_expression` wire code for the final user-visible error"
  artifacts:
    - path: "crates/beava-core/src/op_chain.rs"
      provides: "pub struct OpChain { ops: Vec<CompiledOp> }, OpChain::compile(ops: &[OpNode]) -> Result<OpChain, CompileError>, OpChain::apply(&self, row: Row) -> Option<Row>"
      contains: "pub struct OpChain"
    - path: "crates/beava-core/src/schema_propagate.rs"
      provides: "pub fn propagate_schema(input: &Schema, ops: &[OpNode]) -> Result<Schema, PropagationError> with per-op rules"
      contains: "pub fn propagate_schema"
    - path: "crates/beava-core/src/lib.rs"
      provides: "pub mod op_chain; pub mod schema_propagate;"
      contains: "pub mod op_chain"
  key_links:
    - from: "crates/beava-core/src/op_chain.rs::OpChain::compile"
      to: "crates/beava-core/src/expr.rs::parse"
      via: "Filter / WithColumns / Map op expressions parsed once at compile time; stored as Expr + original source; runtime calls eval()"
      pattern: "expr::parse"
    - from: "crates/beava-core/src/op_chain.rs::OpChain::apply"
      to: "crates/beava-core/src/eval.rs::eval"
      via: "Filter + WithColumns dispatch to eval per row"
      pattern: "eval::eval"
    - from: "crates/beava-core/src/schema_propagate.rs::propagate_schema"
      to: "crates/beava-core/src/expr.rs::referenced_fields"
      via: "WithColumns / Filter / Map expressions' field references are validated against the current schema at that op step — this is the schema-resolution half of SDK-COL-07 (Plan 04-02 ships the parser + referenced_fields() collector; Plan 04-05 wires the register-time 400 `invalid_expression` error)"
      pattern: "referenced_fields"
    - from: "crates/beava-core/src/schema_propagate.rs::propagate_schema"
      to: "python/beava/_col.py::infer_output_type"
      via: "Rust-side type inference mirrors SDK's; comparison ops → Bool, arithmetic ops follow the same promotion rules"
      pattern: "Arithmetic|Comparison|Boolean"
---

<objective>
Land the Phase 4 op-chain executor + register-time schema propagator in `beava-core`. Satisfies SDK-OPS-01 through SDK-OPS-10 (except 09, which Plan 04-01 structurally enforced) and SRV-APPLY-07 ("Stateless ops chain compiled at registration time into a sequence of row transformations; executed per event before aggregations see it"). Also completes SDK-COL-07 (schema-reference resolution) by walking every expression's referenced_fields against the current propagated schema at register time — this plan's `requirements:` frontmatter claims SDK-COL-07 because the actual field-reference-against-schema check lives in propagate_schema here; Plan 04-02 (parser) also claims SDK-COL-07 for its referenced_fields() collector, and Plan 04-05 (register) claims it again for the wire-error surface. All three plans contribute; the REQ is satisfied end-to-end only once all three land.

Purpose: Bridge between (a) the parsed AST / evaluator (Plans 04-02/04-03), (b) the registry's existing `OpNode` wire types (unchanged), and (c) the Phase 5+ apply loop which will call `OpChain::apply(row)` per event. Plan 04-05 wires this into the register endpoint; Plan 04-06 proves the full chain end-to-end.

Output: Two new `beava-core` modules — `op_chain.rs` (compile + apply) and `schema_propagate.rs` (register-time per-op schema derivation + expression field validation) — with comprehensive unit tests for every op variant + chained-op schema propagation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-stateless-ops-expression-evaluator/04-CONTEXT.md
@CLAUDE.md

@crates/beava-core/src/op_node.rs
@crates/beava-core/src/schema.rs
@crates/beava-core/src/row.rs
@crates/beava-core/src/expr.rs
@crates/beava-core/src/eval.rs
@crates/beava-core/src/expr_builtins.rs
@crates/beava-core/src/register_validate.rs
@crates/beava-core/src/lib.rs
@python/beava/_col.py

<interfaces>
<!-- From crates/beava-core/src/op_node.rs — OpNode variants this plan compiles -->

```rust
pub enum OpNode {
    Filter { expr: String },
    Select { fields: Vec<String> },
    Drop { fields: Vec<String> },
    Rename { mapping: BTreeMap<String, String> },
    WithColumns { exprs: BTreeMap<String, String> },
    Map { exprs: BTreeMap<String, String> },  // alias of WithColumns
    Cast { type_map: BTreeMap<String, String> },
    Fillna { defaults: BTreeMap<String, serde_json::Value> },
    // Phase 5/12 — passed through:
    GroupBy { keys: Vec<String>, agg: BTreeMap<String, AggSpec> },
    Join { other: String, on: Vec<String>, within_ms: Option<u64>, join_type: JoinType },
    Union { others: Vec<String> },
}
```

<!-- Schema types to input/output from propagate_schema -->

```rust
pub enum FieldType { Str, F64, I64, Bool, Bytes, Datetime }

// Neutral schema for propagation (op_chain operates on derivation inputs;
// input schema may come from an Event, Table, or upstream Derivation). Use an
// internal Schema struct that's structurally equal to EventSchema/TableSchema/DerivedSchema:
pub struct Schema {
    pub fields: BTreeMap<String, FieldType>,
    pub optional_fields: Vec<String>,
}
```

<!-- From Plan 04-02 — parser output OpChain::compile consumes -->
```rust
pub fn parse(source: &str) -> Result<Expr, ParseError>;
impl Expr { pub fn referenced_fields(&self) -> BTreeSet<String>; }
```

<!-- From Plan 04-03 — evaluator OpChain::apply drives -->
```rust
pub fn eval(expr: &Expr, row: &Row) -> Value;
```

<!-- SDK-side output type inference (Rust version MUST mirror this) -->
From python/beava/_col.py::infer_output_type:
- Comparison ops (`> >= < <= == !=`): always Bool
- Boolean ops (`and or`): both operands Bool → Bool; else TypeError
- Arithmetic (`+ - * /`): both numeric (I64 or F64); `/` always widens to F64; mixed I64+F64 → F64; I64+I64 → I64 (for + - *)
- Literal::Int → I64, Literal::Float → F64, Literal::Str → Str, Literal::Bool → Bool, Literal::Null → unknown (propagation: treat as matching the other operand's type; if both null → Null fallback).
- `isnull(x)` → Bool
- `cast(x, 'float'|'int'|'str'|'bool')` → Float|Int|Str|Bool respectively
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (red): Op-chain executor + schema-propagator tests, all failing</name>
  <files>crates/beava-core/src/op_chain.rs, crates/beava-core/src/schema_propagate.rs</files>
  <read_first>
    - crates/beava-core/src/op_node.rs (the 8 Phase-4 OpNode variants + the 3 passthrough variants)
    - crates/beava-core/src/schema.rs (FieldType + EventSchema/TableSchema/DerivedSchema — propagator outputs a neutral Schema with the same shape)
    - crates/beava-core/src/expr.rs (parse + referenced_fields — compile calls these)
    - crates/beava-core/src/eval.rs (eval — apply calls this)
    - crates/beava-core/src/row.rs (Row mutation API)
    - .planning/phases/04-stateless-ops-expression-evaluator/04-CONTEXT.md §D-05 (register-time type check), §D-06 (schema propagation rules per op)
    - python/beava/_col.py::infer_output_type (for mirroring type-inference rules in Rust)
    - CLAUDE.md §Conventions
  </read_first>
  <behavior>

    Red commit creates two new modules with type skeletons + all tests, no logic.

    `crates/beava-core/src/schema_propagate.rs`:
    - `pub struct Schema { pub fields: BTreeMap<String, FieldType>, pub optional_fields: Vec<String> }` with `impl Schema { pub fn from_event(&EventSchema) -> Self; pub fn from_table(&TableSchema) -> Self; pub fn from_derived(&DerivedSchema) -> Self; pub fn into_derived(self) -> DerivedSchema }`.
    - `pub enum PropagationError { FieldMissing { op_index: usize, field: String }, TypeMismatch { op_index: usize, reason: String }, RenameCollision { op_index: usize, new: String }, InvalidExpr { op_index: usize, parse_error: ParseError }, UnsupportedOp { op_index: usize, op: &'static str } }`.
    - `pub fn propagate_schema(input: &Schema, ops: &[OpNode]) -> Result<(Schema, Vec<Schema>), Vec<PropagationError>>` — returns the final schema AND the schema AFTER each op (so Plan 04-05 can stash per-step schemas for future phases). `Vec<PropagationError>` collects all errors (fail-soft).
    - Helper: `pub fn infer_expr_type(expr: &Expr, schema: &Schema) -> Result<FieldType, PropagationError>` — mirrors `_col.py::infer_output_type`.
    - `#[cfg(test)] mod tests` with:
      1. `prop_filter_preserves_schema` — propagate_schema with a single Filter leaves schema.fields unchanged.
      2. `prop_filter_expr_unknown_field_errors` — Filter{"(missing > 0)"} against schema without "missing" → PropagationError::FieldMissing { op_index: 0, field: "missing" }.
      3. `prop_filter_expr_parse_error_errors` — Filter{"(amount > "} → PropagationError::InvalidExpr { op_index: 0, parse_error: ... }.
      4. `prop_select_keeps_listed_fields_in_order` — propagate(schema{a,b,c}, [Select{[b,a]}]) → schema{b,a} — iteration preserves the provided order. Since Schema uses BTreeMap which sorts alphabetically, Select must ALSO return a BTreeMap; the test asserts the kept keys are {b,a} as a set AND that the propagator records the order via an ordered representation if a downstream op cares (Phase 4 uses BTreeMap throughout; SDK-OPS-02's "in order" refers to the SERIALIZED JSON — BTreeMap serialization is alphabetical; that matches serde_json default output; document this and assert only the SET equality here).
      5. `prop_select_unknown_field_errors` — Select{[missing]} → FieldMissing.
      6. `prop_drop_removes_fields` — propagate(schema{a,b,c}, [Drop{[b]}]) → schema{a,c}.
      7. `prop_drop_unknown_field_errors` — Drop{[missing]} → FieldMissing.
      8. `prop_rename_swaps_keys` — propagate(schema{a,b}, [Rename{{a→x}}]) → schema{x,b}; optional_fields list is updated too (if "a" was optional, "x" becomes optional).
      9. `prop_rename_collision_errors` — propagate(schema{a,b}, [Rename{{a→b}}]) → RenameCollision { op_index: 0, new: "b" }.
      10. `prop_rename_unknown_field_errors` — Rename{{missing→x}} → FieldMissing.
      11. `prop_with_columns_adds_derived_field` — propagate(schema{amount: F64}, [WithColumns{{is_big: "(amount > 500)"}}]) → schema{amount: F64, is_big: Bool}.
      12. `prop_with_columns_overwrites_existing` — propagate(schema{amount: F64}, [WithColumns{{amount: "cast(amount, int)"}}]) → schema{amount: I64}.
      13. `prop_with_columns_expr_type_mismatch_errors` — propagate(schema{amount: F64}, [WithColumns{{bad: "(amount and true)"}}]) → TypeMismatch (boolean op on non-bool).
      14. `prop_map_is_alias_of_with_columns` — propagate(schema{a: F64}, [Map{{b: "(a + 1)"}}]) is equivalent to the same with WithColumns — both add b: F64.
      15. `prop_cast_replaces_field_type` — propagate(schema{amount: Str}, [Cast{{amount: "float"}}]) → schema{amount: F64}.
      16. `prop_cast_unknown_target_errors` — Cast{{amount: "decimal"}} → TypeMismatch (unknown target type).
      17. `prop_cast_unknown_field_errors` — Cast{{missing: "int"}} → FieldMissing.
      18. `prop_fillna_keeps_field_clears_optional` — propagate(schema{amount: F64, optional_fields: ["amount"]}, [Fillna{{amount: 0.0}}]) → schema{amount: F64, optional_fields: []}.
      19. `prop_fillna_unknown_field_errors` — Fillna{{missing: 0}} → FieldMissing.
      20. `prop_chained_ops_propagate` — input schema {amount: F64}, ops = [Filter{"(amount > 0)"}, WithColumns{{is_big: "(amount > 500)"}}, Cast{{is_big: "int"}}]. Expected output: {amount: F64, is_big: I64}. Assert per-step Vec<Schema> has length 3 and step[2]["is_big"] = I64.
      21. `prop_passthrough_ops_unsupported` — Ops containing GroupBy → Err(UnsupportedOp { op_index: N, op: "group_by" }). Same for Join ("join") and Union ("union"). Phase 4 does not execute these; register-time surfaces the error.
      22. `prop_fail_soft_collects_multiple_errors` — ops = [Select{[missing1]}, Drop{[missing2]}] → Err with at least two PropagationError entries; first is op_index=0, second is op_index=1. (Fail-soft pattern mirrors register_validate.rs.)
      23. `infer_expr_type_arithmetic_promotion` — mirror infer_output_type: I64+I64 → I64; I64+F64 → F64; I64/I64 → F64 (division widens).
      24. `infer_expr_type_comparison_is_bool` — (I64>I64) → Bool; (F64==F64) → Bool.
      25. `infer_expr_type_boolean_requires_bool` — (Bool and Bool) → Bool; (I64 and Bool) → TypeMismatch.
      26. `infer_expr_type_isnull_is_bool` — isnull(I64) → Bool; isnull(Null_literal) → Bool.
      27. `infer_expr_type_cast_returns_target` — cast(x, 'float') → F64; cast(x, 'blob') → TypeMismatch.
      28. `infer_expr_type_unknown_function_errors` — foo(x) → TypeMismatch (register-time rejects unknown fns per D-07).

    `crates/beava-core/src/op_chain.rs`:
    - `pub struct CompiledOp { /* internal: parsed Expr cached + op kind */ }` (private).
    - `pub struct OpChain { ops: Vec<CompiledOp> }`.
    - `pub enum CompileError { /* wraps PropagationError + ParseError */ }` — share with propagate? YES — use `pub use crate::schema_propagate::PropagationError as CompileError;` to avoid two error types.
    - `pub fn OpChain::compile(input_schema: &Schema, ops: &[OpNode]) -> Result<(OpChain, Schema), Vec<PropagationError>>` — returns the compiled chain AND the final output schema. Calls propagate_schema internally for validation + final schema; caches parsed Expr inside each CompiledOp for Filter/WithColumns/Map/Cast.
    - `impl OpChain { pub fn apply(&self, row: Row) -> Option<Row> }` — runs the compiled chain. Filter returning Null/Bool(false) → None. All other ops return Some(updated_row).
    - `#[cfg(test)] mod tests` with:
      29. `chain_filter_keeps_passing_row` — OpChain with Filter{"(amount > 100)"}, apply to row{amount=I64(150)} → Some(row with amount=150).
      30. `chain_filter_drops_failing_row` — same filter, row{amount=I64(50)} → None.
      31. `chain_filter_null_predicate_drops_row` — Filter{"(missing > 0)"} against row{} (missing field evaluates to Null; comparison to Null yields Null; Null filter predicate → drop). Compile should succeed only if "missing" exists in the input schema — so use a row where amount exists but equals Null: schema{amount: F64 optional}, row{amount: Value::Null}, Filter{"(amount > 0)"} → apply returns None.
      32. `chain_select_keeps_listed` — OpChain([Select{[a,b]}]) apply to row{a,b,c} → row{a,b}.
      33. `chain_drop_removes_listed` — row{a,b,c} → Drop{[b]} → row{a,c}.
      34. `chain_rename_applies` — row{a=I64(5)} → Rename{{a→x}} → row{x=I64(5)}.
      35. `chain_with_columns_adds_field` — row{amount=I64(1000)} → WithColumns{{is_big: "(amount > 500)"}} → row{amount=I64(1000), is_big=Bool(true)}.
      36. `chain_map_adds_field_alias` — Map with the same payload as 35 produces the same result.
      37. `chain_cast_converts_value` — row{amount=Str("42")} → Cast{{amount: "int"}} → row{amount=I64(42)}.
      38. `chain_cast_failure_yields_null` — row{amount=Str("abc")} → Cast{{amount: "int"}} → row{amount=Null}.
      39. `chain_fillna_replaces_null` — row{amount=Null} → Fillna{{amount: 0}} → row{amount=I64(0)}.
      40. `chain_fillna_leaves_non_null_unchanged` — row{amount=I64(5)} → Fillna{{amount: 0}} → row{amount=I64(5)}.
      41. `chain_composes_filter_with_columns_cast` — input schema {amount: F64}; ops = [Filter{"(amount > 0)"}, WithColumns{{is_big: "(amount > 500)"}}, Cast{{is_big: "int"}}]; apply to row{amount=F64(1000.0)} → row{amount=F64(1000.0), is_big=I64(1)}; apply to row{amount=F64(-1.0)} → None.
      42. `chain_compile_returns_output_schema` — compile the above chain; the returned Schema contains {amount: F64, is_big: I64}.
      43. `chain_compile_fails_on_bad_expr` — ops = [Filter{"("}] returns Err.

    Tests 29–43 can reuse helpers that build schemas + rows concisely. Write helpers in a `mod fixtures` submodule inside the test module.
  </behavior>
  <action>
    Edit lib.rs: `pub mod op_chain;` and `pub mod schema_propagate;` added.

    Commit: `test(04-04): add failing op-chain + schema-propagation tests`.

  </action>
  <verify>
    <automated>cargo test -p beava-core op_chain::tests schema_propagate::tests</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p beava-core op_chain::tests schema_propagate::tests --no-run` exits 0.
    - `cargo test -p beava-core op_chain::tests schema_propagate::tests` exits non-zero (all 43 tests FAIL).
    - `grep -c "fn prop_\|fn chain_\|fn infer_expr_type_" crates/beava-core/src/op_chain.rs crates/beava-core/src/schema_propagate.rs | tail -1` sums to ≥ 40.
    - `cargo build --workspace` exits 0.
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0.
    - `cargo fmt --all --check` exits 0.
    - `git log -1 --format='%s' | grep -E '^test\(04-04\):'` matches.
    - `git show --stat HEAD` touches exactly `crates/beava-core/src/op_chain.rs`, `crates/beava-core/src/schema_propagate.rs`, `crates/beava-core/src/lib.rs`.
  </acceptance_criteria>
  <done>All 43 tests RED; two new modules registered; commit conforms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (green): Implement op-chain compile + apply, schema propagator, field-reference validator</name>
  <files>crates/beava-core/src/op_chain.rs, crates/beava-core/src/schema_propagate.rs</files>
  <read_first>
    - crates/beava-core/src/op_chain.rs + crates/beava-core/src/schema_propagate.rs (your red commit — tests are spec)
    - crates/beava-core/src/expr.rs (parse + Expr + referenced_fields)
    - crates/beava-core/src/eval.rs (eval)
    - crates/beava-core/src/expr_builtins.rs (BUILTINS / lookup_builtin)
    - crates/beava-core/src/row.rs (Value + Row mutation API)
    - crates/beava-core/src/op_node.rs (OpNode variants)
    - crates/beava-core/src/schema.rs (FieldType + schema types)
    - .planning/phases/04-stateless-ops-expression-evaluator/04-CONTEXT.md §D-05 (register-time type check) + §D-06 (per-op schema transforms)
  </read_first>
  <action>
    **Step 1 — `schema_propagate.rs`**:

    A. Implement `impl Schema { from_event / from_table / from_derived / into_derived }` as copy-over constructors.

    B. Implement `infer_expr_type(expr, schema)`:
       - Expr::Field{name} → look up schema.fields.get(name); missing → FieldMissing.
       - Expr::Literal(Literal::Bool, _) → Bool; Int → I64; Float → F64; Str → Str; Null → return a sentinel that arithmetic/comparison handles (simplest: return a Result<FieldType, _> with a special "NullLiteral" marker in an enum WRAP — actually easier: allow infer_expr_type to return `Option<FieldType>` where None means "null literal, polymorphic at type-check time" — then callers in Arithmetic/Comparison treat None as compatible with any numeric or comparable type). OR return F64/I64/Bool/Str contextually based on the other operand. SIMPLEST: represent it as `FieldType::...` BUT add an `InferredType` wrapper enum `enum InferredType { Known(FieldType), NullLiteral }` and thread that through. PICK: `InferredType` wrapper.
       - Expr::Literal(Literal::BareIdent(s)) → treat as Str (cast's 2nd arg; the dispatch below handles cast specially).
       - Expr::BinOp:
         * Comparison ops → InferredType::Known(Bool). Still type-check children (both sides must be same-kind or one is NullLiteral — mismatch → TypeMismatch). Document the rule: numeric-vs-numeric, string-vs-string, bool-vs-bool, Datetime-vs-Datetime, Bytes-vs-Bytes; NullLiteral-vs-anything is OK (evaluator will short-circuit to Null anyway).
         * Boolean `and`/`or` → both operands must be Bool or NullLiteral; output Bool.
         * Arithmetic `+ - * /` → both operands must be numeric (I64 or F64) or NullLiteral. For `/` → F64. For `+ - *`: I64+I64 → I64; mixed → F64. NullLiteral passes through as the other operand's type.
       - Expr::UnaryOp(Not, x) → x must be Bool or NullLiteral; output Bool.
       - Expr::Call{fn_name, args}:
         * Look up builtin via lookup_builtin; if None → TypeMismatch("unknown function").
         * Arity check via builtin.arity.
         * Builtin-specific type inference:
           - `isnull` → Bool (any input accepted).
           - `cast` → 2nd arg must be Literal::BareIdent or Literal::Str with value ∈ {"str", "int", "float", "bool"}; output type mirrors: Str→Str, Int→I64, Float→F64, Bool→Bool. 2nd arg anything else → TypeMismatch.
         * (Phase 5+ will add more builtins here.)

    C. Implement `propagate_schema(input, ops)`:
       - Walk ops with `op_index`; maintain `current: Schema` that starts as `input.clone()` and is updated per op.
       - Track `per_step: Vec<Schema>` (snapshot after each op).
       - Collect errors into `Vec<PropagationError>`; continue after an op fails with a best-effort carry-forward (e.g., if a Select lists a missing field, skip that field in the synthesized schema so downstream ops still get something to type-check against).
       - Per-op logic:
         * Filter: parse(expr) → AST; check referenced_fields ⊂ current.fields.keys; infer_expr_type(AST, current) must be Bool or NullLiteral; schema unchanged.
         * Select: for each f in fields, check current.fields.contains(f); build new Schema with only those fields in their BTreeMap-sorted order; carry optional_fields ∩ kept_fields.
         * Drop: for each f in fields, check current.fields.contains(f); remove from current.
         * Rename: collect all (old, new) pairs; check every old exists; check no new collides with an existing field that isn't being renamed AWAY in the same batch; apply all atomically.
         * WithColumns / Map: for each (name, expr_source), parse → AST, check referenced_fields ⊂ current.fields.keys, infer_expr_type → FieldType (must be Known; NullLiteral-only exprs are rejected), set current.fields[name] = inferred_type. If name wasn't in optional_fields before, stays non-optional; if it WAS and is now being redefined by a with_columns expression, optional_fields entry is cleared (the new value is whatever the expression yields).
         * Cast: for each (field, target_str), check field exists, check target_str ∈ {"str","int","float","bool"}, check cast legality (CONTEXT.md D-05 — "validate x's type has a legal cast to the target"). Illegal casts (documented matrix — e.g., Bytes → I64 is illegal; Str → Bytes is illegal) → TypeMismatch. Legal cast maps: any numeric/bool/str ↔ str; any numeric/bool/str ↔ int (str may fail at runtime; register-time accepts); any numeric/bool/str ↔ float; numeric/str/bool ↔ bool. Datetime ↔ Int (int holds ms). Bytes is isolated — no casts.
         * Fillna: for each (field, default_json_value), check field exists; build the target Value from default_json_value (Null → error; numeric → I64 or F64 depending on field type; str → Str; bool → Bool); clear the field from optional_fields.
         * GroupBy / Join / Union → UnsupportedOp { op_index, op: "group_by" | "join" | "union" }.
       - Return `Ok((final_schema, per_step))` if errors empty; else `Err(errors)`.

    **Step 2 — `op_chain.rs`**:

    A. Define `CompiledOp` enum (internal) mirroring OpNode's 8 Phase-4 variants, but with parsed `Expr` values where the OpNode had `expr: String`. Passthrough ops (GroupBy/Join/Union) are REJECTED at compile — they never end up in a CompiledOp.

    B. `OpChain::compile(input_schema, ops)`:
       - Call `propagate_schema(input_schema, ops)` first. If Err → return Err (compilation fails when schema propagation fails; single Result path).
       - If Ok((final_schema, _per_step)), walk ops again building the Vec<CompiledOp>:
         * For Filter{expr}, WithColumns{exprs}, Map{exprs}: call expr::parse on each expression string; cache the resulting Expr.
         * For Cast{type_map}: materialize an internal `CompiledCast { field_to_target: BTreeMap<String, CastTarget> }` where CastTarget is a small enum (Str/Int/Float/Bool). Reuse `cast_eval` from expr_builtins by invoking the builtin per field.
         * For Fillna{defaults}: materialize defaults as `BTreeMap<String, Value>` (convert serde_json::Value → Value using the field's type from the propagated per-step schema; on mismatch, return Err — but propagate_schema should have caught most cases already).
         * Select/Drop/Rename: trivial — store the field lists / mappings as-is.
       - Return `Ok((OpChain { ops: compiled }, final_schema))`.

    C. `OpChain::apply(&self, row: Row) -> Option<Row>`:
       - Fold over `self.ops`, threading `row: Row` as the accumulator:
         * Filter(expr_ast) → eval(expr_ast, &row); if Bool(true) → Some(row); else (Bool(false) or Null or any non-bool) → None — early-return from apply.
         * Select(fields) → keep only listed; build a new Row from the BTreeMap filtered.
         * Drop(fields) → row.without_field per field.
         * Rename(mapping) → build a fresh BTreeMap with renames applied atomically (pop each old key, insert under new).
         * WithColumns(exprs) or Map (same code path): for each (name, ast), v = eval(ast, &row); row = row.with_field(name, v).
         * Cast(type_map): for each (field, target), if row has field, call `cast_eval(&[field_value, Value::Str(target)])`; set row[field] = result (possibly Null).
         * Fillna(defaults): for each (field, default_value), if row[field] == Null, set row[field] = default_value.clone().
       - Return Some(final_row).
       - Important detail: Filter short-circuits — once a Filter returns None, subsequent ops are skipped.

    **Step 3 — Run the full gate**:
    ```
    cargo test -p beava-core op_chain schema_propagate
    cargo test --workspace
    cargo clippy --workspace --all-targets --all-features -- -D warnings
    cargo fmt --all --check
    ```

    Commit message MUST match `^feat\(04-04\):`.
  </action>
  <verify>
    <automated>cargo test -p beava-core op_chain schema_propagate</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p beava-core op_chain schema_propagate` exits 0 (all 43 tests pass).
    - `cargo test --workspace` exits 0 (no regressions; total test count now ≥ 337).
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0.
    - `cargo fmt --all --check` exits 0.
    - `grep -c "OpNode::" crates/beava-core/src/schema_propagate.rs` ≥ 11 (11 OpNode variants, each pattern-matched).
    - `grep "eval::eval\|crate::eval::eval" crates/beava-core/src/op_chain.rs` matches (apply delegates to Plan 04-03 evaluator).
    - `grep "expr::parse\|crate::expr::parse" crates/beava-core/src/op_chain.rs` matches (compile delegates to Plan 04-02 parser).
    - `grep "referenced_fields" crates/beava-core/src/schema_propagate.rs` matches (SDK-COL-07 field-reference validation wires through).
    - `git log -1 --format='%s' | grep -E '^feat\(04-04\):'` matches.
    - Red→green chain intact: `git log --format='%s' HEAD~2..HEAD | head -2 | tac` (or equivalent) yields `test(04-04): ...` then `feat(04-04): ...`.
  </acceptance_criteria>
  <done>Op chain compiles + applies; schema propagates correctly; SDK-OPS-01..08,10 + SRV-APPLY-07 + SDK-COL-07 all mechanically satisfied; workspace green.</done>
</task>

</tasks>

<verification>
- `cargo test -p beava-core op_chain schema_propagate` — 43/43 pass
- `cargo test --workspace` — no regressions
- Per-op sanity spot-check: `cargo test -p beava-core op_chain::tests::chain_composes_filter_with_columns_cast -- --nocapture` — this is the SC3 analog test
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` — clean
- `cargo fmt --all --check` — clean
- `grep "pub mod op_chain;" crates/beava-core/src/lib.rs` matches
- `grep "pub mod schema_propagate;" crates/beava-core/src/lib.rs` matches
</verification>

<success_criteria>
Ten of the 13 phase REQ-IDs are now mechanically satisfied at the beava-core library level: SDK-OPS-01..08, SDK-OPS-10, SRV-APPLY-07. SDK-COL-07 is also satisfied at the schema-resolution layer because schema_propagate walks every expression's referenced_fields and errors on unknown field names with pinpointed op_index paths — this plan's requirements frontmatter claims SDK-COL-07 for that reason (alongside Plan 04-02 which ships the parser + referenced_fields collector, and Plan 04-05 which surfaces the wire error). What remains for Plans 04-05 / 04-06 / 04-07: wire propagate_schema + OpChain::compile into the HTTP+TCP `execute_register` pipeline, prove the whole loop end-to-end from a Python SDK registration through the server, and land the SDK op methods with client-side tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stateless-ops-expression-evaluator/04-04-SUMMARY.md` documenting: (1) the legal-cast matrix (which FieldType pairs are castable, which are rejected at register-time); (2) the NullLiteral polymorphism approach for InferredType; (3) the fail-soft carry-forward strategy for propagate_schema; (4) the Filter short-circuit semantics in OpChain::apply; (5) interfaces exported to Plan 04-05 (OpChain::compile, OpChain::apply, propagate_schema, PropagationError); (6) how SDK-COL-07 is shared across 04-02 / 04-04 / 04-05 (each plan contributes; the REQ is end-to-end satisfied only after 04-05 lands the wire-error surface).
</output>
</output>
