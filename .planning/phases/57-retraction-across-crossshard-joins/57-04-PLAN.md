---
phase: 57
plan: 04
type: execute
wave: 4
depends_on: [57-03]
files_modified:
  - benchmark/fraud-pipeline/run_bench.sh
  - scripts/verify-retraction-metrics.sh
  - tests/retraction_perf_smoke.rs
  - .planning/phases/57-retraction-across-crossshard-joins/57-PERF-GATE.md
  - .planning/phases/57-retraction-across-crossshard-joins/57-VERIFICATION.md
  - .planning/phases/57-retraction-across-crossshard-joins/deferred-items.md
  - .planning/phases/57-retraction-across-crossshard-joins/perf-evidence/.gitkeep
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - TPC-CORR-10
tags:
  - wave-4
  - perf-gate
  - verification
  - phase-close
  - ship-gate
must_haves:
  truths:
    - "Perf gate MEASURED: MODE=complex DURATION=60 CPUS=8 CLIENTS=8 BEAVA_SHARD_INBOX_SIZE=1048576 with ZERO retractions firing during the run (standard fraud-pipeline scenario). Floor: ≥ 1,076,322 EPS (= 90% of Phase 56 baseline 1,195,914 EPS = ≤ 10% write-path overhead). PASS or contingency ladder invoked."
    - "57-PERF-GATE.md records candidate EPS, floor, headroom, delta vs Phase 56 baseline; raw bench stdout at 57-*/perf-evidence/<ts>.txt"
    - "57-VERIFICATION.md mirrors 56-VERIFICATION.md format — per-SC rows SC-1..SC-4 + depth guard, each with evidence file reference + status (passed / human_needed / failed)"
    - "scripts/verify-retraction-metrics.sh confirms all 5 retraction counters appear in /metrics after a test run (RETRACTIONS_SENT_TOTAL / APPLIED_TOTAL / NOOPED_TOTAL / BEYOND_HISTORY_TOTAL / DEPTH_EXCEEDED_TOTAL)"
    - "Advisory retraction-firing micro-bench run (D-D4): 1000 paired push + source-table DELETE; records end-to-end retraction latency p50/p99. NOT a gate — advisory number committed to 57-PERF-GATE.md Section §Advisory"
    - "Phase 57 engineering-complete state recorded: ROADMAP.md Phase 57 row flipped Complete (or human_needed if contingency C3 triggered); STATE.md Current Position updated; deferred-items.md lists 57-NEXT items"
    - "All 57-W{0..4} ignore markers removed from tests/"
    - "Contingency ladder executed if gate fails: C1 batch retraction coalesce, C2 inline hot-path fast-check, C3 flag user with human_needed + perf-evidence"
  artifacts:
    - path: benchmark/fraud-pipeline/run_bench.sh
      provides: "No new scenario needed for the default gate (zero retractions firing means standard fraud-pipeline scenario applies directly). For the advisory retraction-firing micro-bench, add BEAVA_RETRACTION_FIRING_MICRO=1 branch that runs a 1000-event paired push + DELETE scenario and records p50/p99 latency."
      contains: "BEAVA_RETRACTION_FIRING_MICRO"
    - path: tests/retraction_perf_smoke.rs
      provides: "BEAVA_PERF_GATE=1 env-gated EPS floor test (1,076,322 EPS); 57-W4 ignore removed; BASELINE_P99_MICROS updated to Phase 56 measured value"
      contains: "retraction_eps_floor"
    - path: scripts/verify-retraction-metrics.sh
      provides: "Shell script: spin up test server with BEAVA_SHARDS=4; push events + trigger retractions; curl /metrics; grep for all 5 retraction counter names; assert non-zero for SENT + APPLIED after the test run"
      contains: "beava_retractions_sent_total|beava_retractions_applied_total"
    - path: .planning/phases/57-retraction-across-crossshard-joins/57-PERF-GATE.md
      provides: "Per Phase 55/56 template — candidate EPS, floor EPS, headroom %, delta vs baseline %, PASS/FAIL verdict, raw stdout path, contingency-ladder decision trail (if any), advisory retraction-latency numbers"
      contains: "1,076,322|1,195,914"
      min_lines: 60
    - path: .planning/phases/57-retraction-across-crossshard-joins/57-VERIFICATION.md
      provides: "Per-SC verification doc — SC-1, SC-2, SC-3, SC-4 + D-B5 depth guard; each row: evidence file, test name, pass/human_needed/failed, TPC-CORR-10 requirement satisfied"
      contains: "TPC-CORR-10|SC-1|SC-2|SC-3|SC-4"
      min_lines: 80
    - path: .planning/phases/57-retraction-across-crossshard-joins/deferred-items.md
      provides: "57-NEXT list: batched retraction coalesce (if perf flagged), async/background retraction for non-critical-path, UI for retraction graphs, rewriting history beyond history_ttl, secondary index for source_table_key → downstream_row_keys (Option B from 57-03 Task 1)"
    - path: .planning/phases/57-retraction-across-crossshard-joins/perf-evidence/
      provides: "Raw bench stdout artifact (YYYYMMDDTHHMMSSZ.txt format matching Phase 55/56)"
    - path: .planning/ROADMAP.md
      provides: "Phase 57 row flipped Complete (or human_needed + carry-forward note if C3 triggered); plan checklist entries checked"
    - path: .planning/STATE.md
      provides: "Current Position advanced to Phase 58 (or stays 57 with human_needed note); Phase 57 entry in Accumulated Context; milestone progress updated"
  key_links:
    - from: benchmark/fraud-pipeline/run_bench.sh
      to: 57-PERF-GATE.md
      via: "Raw stdout tee'd to perf-evidence/<ts>.txt; 57-PERF-GATE.md parses candidate EPS from stdout + computes delta vs 1,195,914 baseline"
      pattern: "perf-evidence|57-PERF-GATE.md"
    - from: scripts/verify-retraction-metrics.sh
      to: /metrics
      via: "Test server; trigger source-table DELETE + entity tombstone + beyond-history event; curl /metrics; grep for all 5 counter names"
      pattern: "beava_retractions_.*_total|beava_retraction_.*_total"
user_setup: []
---

<objective>
Close Phase 57 engineering. Measure the perf gate, record per-SC verification, write the phase SUMMARY, flip ROADMAP.md, update STATE.md, file 57-NEXT items.

This wave does NOT write new production code for the correctness path — Waves 1–3 already delivered TPC-CORR-10 correctness. This wave:

1. Runs the perf gate (standard fraud-pipeline, ZERO retractions firing) — measure Phase 57 code against the Phase 56 baseline. Floor: 1,076,322 EPS (= 90% of 1,195,914 = ≤ 10% write-path overhead).
2. Runs the advisory retraction-firing micro-bench (D-D4): 1000-event burst of paired push + source-table DELETE; records end-to-end retraction latency p50/p99. NOT a gate — advisory for Phase 58+ optimization.
3. Writes VERIFICATION + PERF-GATE docs.
4. Invokes contingency ladder if perf gate fails:
   - **C1 (preferred):** batch retraction coalesce — if multiple RetractDownstream dispatches go to the same target shard within a batch, coalesce into one SPSC send carrying `Vec<(stream_name, row_key, reason)>`. Same shape as Phase 55-01 CascadeBuffer coalesce. ~5-10% win expected.
   - **C2:** inline hot-path fast-check — when the primary event is NOT a tombstone / retract variant (the 99%+ case), skip the fan-out scan entirely via an early-return guard. Eliminates per-event cost in the zero-retraction case.
   - **C3 (last resort):** flag to user with human_needed status in VERIFICATION.md + perf-evidence attachment; do NOT relax the floor silently. Matches the Phase 55 SC-6 precedent.
5. Updates ROADMAP.md Phase 57 row + STATE.md Current Position.
6. Files 57-NEXT items for deferred work (batched retraction coalesce if not needed in C1; async retraction; secondary index for source_table_key→downstream; UI for retraction graphs; rewriting history beyond history_ttl).

Purpose: evidence-bearing close. Every success criterion (SC-1..SC-4 + D-B5 depth guard) transitions to `passed` or `human_needed` with grep-verifiable evidence.

Output:
- Perf-gate evidence (raw stdout + structured 57-PERF-GATE.md)
- 57-VERIFICATION.md per-SC table
- 57-SUMMARY.md for this wave
- ROADMAP.md + STATE.md updates
- deferred-items.md with 57-NEXT entries
- All 57-W{0..4} markers removed
- scripts/verify-retraction-metrics.sh (grep-gate for 5 counters)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-retraction-across-crossshard-joins/57-CONTEXT.md
@.planning/phases/57-retraction-across-crossshard-joins/57-00-PLAN.md
@.planning/phases/57-retraction-across-crossshard-joins/57-01-PLAN.md
@.planning/phases/57-retraction-across-crossshard-joins/57-02-PLAN.md
@.planning/phases/57-retraction-across-crossshard-joins/57-03-PLAN.md
@.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-04-PLAN.md
@.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-04-SUMMARY.md
@.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-PERF-GATE.md
@.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-VERIFICATION.md
@.planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-04-SUMMARY.md
@.planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-PERF-GATE.md
@benchmark/fraud-pipeline/run_bench.sh
@scripts/verify-crossshard-metrics.sh
@scripts/verify-source-lsn-echoed.sh

<interfaces>
Phase 56 reference values (for delta computation in 57-PERF-GATE.md):
- Phase 56 baseline EPS: 1,195,914 (default fraud-pipeline, no cross-shard scenario)
- Phase 57 floor: 1,076,322 EPS (90% of 1,195,914 = ≤ 10% overhead)
- Phase 55 baseline (for deeper reference): 1,246,190 EPS
- Phase 54 baseline: 1,339,446 EPS

Canonical perf-gate invocation (from Phase 56-04):
```bash
MODE=complex DURATION=60 CPUS=8 CLIENTS=8 \
BEAVA_SHARD_INBOX_SIZE=1048576 \
bash benchmark/fraud-pipeline/run_bench.sh 2>&1 | tee \
  .planning/phases/57-retraction-across-crossshard-joins/perf-evidence/$(date -u +%Y%m%dT%H%M%SZ).txt
```

Standard scenario (zero retractions firing) exercises the write path Phase 57 added overhead to — contributing_inputs write on every emit + tombstone detection branch. This is exactly the overhead we want to measure.

Advisory retraction-firing micro-bench scaffold:
```bash
BEAVA_RETRACTION_FIRING_MICRO=1 \
BEAVA_SHARDS=8 \
bash benchmark/fraud-pipeline/run_bench.sh 2>&1 | tee \
  .planning/phases/57-retraction-across-crossshard-joins/perf-evidence/retract-micro-$(date -u +%Y%m%dT%H%M%SZ).txt
```
This scenario pushes 1000 pairs of (push event, DELETE of a prior emit's source-table input) and records the end-to-end retraction latency via existing `beava_retraction_latency_us` histogram (add if not present — piggyback on existing `record_enrich_latency` pattern).

Script template (scripts/verify-retraction-metrics.sh) — mirror scripts/verify-crossshard-metrics.sh:
```bash
#!/usr/bin/env bash
set -euo pipefail
# Phase 57 — verify all 5 retraction counters surface on /metrics
EXPECTED=(
  "beava_retractions_sent_total"
  "beava_retractions_applied_total"
  "beava_retractions_nooped_total"
  "beava_retraction_beyond_history_total"
  "beava_retraction_depth_exceeded_total"
)
for metric in "${EXPECTED[@]}"; do
  curl -s http://localhost:PORT/metrics | grep -qE "^# HELP ${metric}" \
    || { echo "FAIL: ${metric} missing"; exit 1; }
done
echo "OK: all 5 Phase 57 retraction counters present"
```
</interfaces>

<contingency_ladder>
If candidate EPS < 1,076,322:

### C1 — Batch retraction coalesce (preferred, ~5-10% win)
If fan_out_retraction_for_primary / fan_out_retraction_for_source_table dispatch multiple RetractDownstream ops to the SAME target shard in rapid succession, coalesce into one SPSC send. New ShardOp variant `RetractDownstreamBatch { target_shard, entries: Vec<RetractEntry> }`. Apply per-entry on target; single oneshot reply carrying Vec<RetractOutcome>.

Trade-off: 2x LOC vs single-dispatch. Acceptable if perf gate fails by 3-10%.

Rollout as a Task-1 addendum if C1 is needed; document in 57-PERF-GATE.md §Contingency.

### C2 — Inline hot-path fast-check (eliminate zero-retraction overhead)
At the top of push_with_cascade_on_shard, add:
```rust
let is_tombstone_or_retract = /* cheap check on event payload */;
if !is_tombstone_or_retract && primary_event_id.is_assigned() {
    // Skip ALL fan-out scans; the 99%+ hot path with zero retractions firing.
    // contributing_inputs write still happens (cost absorbed by normal emit).
}
```

Zero runtime cost on the hot path when no retractions fire. Most impactful for the perf gate since the gate explicitly runs WITHOUT retractions.

### C3 — human_needed escalation (last resort)
Mark Phase 57 as engineering-complete with SC-4 (perf) `human_needed`. Commit the perf-evidence stdout. File 57-NEXT for deeper optimization (Phase 62 allocator pooling / Phase 58 Tokio rewrite are already on the roadmap and likely fix the overhead). Matches Phase 55 SC-6 + Phase 56 SC-5 precedent.

**Do NOT silently relax the 10% floor.** The user explicitly called this "non-negotiable" in the planning prompt.
</contingency_ladder>

<user_decision_fidelity>
D-D3 perf floor: 1,076,322 EPS = 90% of 1,195,914. This number is derived from the user's planning prompt: "Perf: ≤ 10% overhead on write path with zero retractions firing. Phase 56 baseline 1,195,914 EPS → floor 1,076,322 EPS." Task 1 of this plan MUST validate against exactly this number; C3 is the only acceptable off-ramp if it fails, and only with human_needed tagging (not silent relaxation).
</user_decision_fidelity>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Perf gate + advisory retraction-firing micro-bench + evidence</name>
  <read_first>
    - .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-04-SUMMARY.md (default fraud-pipeline gate execution + evidence pattern)
    - .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-PERF-GATE.md (output format)
    - benchmark/fraud-pipeline/run_bench.sh (Phase 55/56 harness + scenario branching)
    - scripts/verify-crossshard-metrics.sh (script template for scripts/verify-retraction-metrics.sh)
    - .planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-PERF-GATE.md (evidence-file naming convention YYYYMMDDTHHMMSSZ.txt)
  </read_first>
  <files>
    benchmark/fraud-pipeline/run_bench.sh
    tests/retraction_perf_smoke.rs
    scripts/verify-retraction-metrics.sh
    .planning/phases/57-retraction-across-crossshard-joins/57-PERF-GATE.md
    .planning/phases/57-retraction-across-crossshard-joins/perf-evidence/.gitkeep
  </files>
  <action>
### Step 1 — Run the default perf gate

```bash
mkdir -p .planning/phases/57-retraction-across-crossshard-joins/perf-evidence
TS=$(date -u +%Y%m%dT%H%M%SZ)
MODE=complex DURATION=60 CPUS=8 CLIENTS=8 \
  BEAVA_SHARD_INBOX_SIZE=1048576 \
  bash benchmark/fraud-pipeline/run_bench.sh 2>&1 | tee \
  ".planning/phases/57-retraction-across-crossshard-joins/perf-evidence/${TS}.txt"
```

Parse the candidate EPS from stdout (format matches Phase 55/56). If aggregate is extracted from per-client counters (Phase 54-NEXT #2 may still be active), document the method.

### Step 2 — Advisory retraction-firing micro-bench

If BEAVA_RETRACTION_FIRING_MICRO=1 scenario doesn't yet exist in run_bench.sh, add a simple branch that:
1. Spawns a tokio-current-thread server with BEAVA_SHARDS=8
2. Registers a Countries source-table + Txns stream with EnrichFromTable(Countries, on=country_code)
3. Runs 1000 iterations: push Txn event → read latency → DELETE Countries row for the country_code → record retraction latency via the existing metrics histogram
4. Prints p50/p99 latencies

If wire-REGISTER for @bv.source_table is still gated by 56-NEXT #6, use the Rust-API `register_source_table` + inline TCP client to run the micro-bench (no Python SDK needed).

Record the micro-bench latencies into 57-PERF-GATE.md §Advisory. These are NOT a gate.

### Step 3 — Update retraction_perf_smoke.rs

Flip the `#[ignore = "57-W4"]` markers. Wire a BEAVA_PERF_GATE=1 env-gated floor test:

```rust
#[test]
#[ignore = "perf-gate-only: set BEAVA_PERF_GATE=1 to run"]
fn retraction_eps_floor() {
    if std::env::var("BEAVA_PERF_GATE").is_err() {
        return;  // skipped in normal test runs
    }
    // Subprocess run of run_bench.sh + parse stdout for candidate EPS
    // assert!(candidate_eps >= 1_076_322, "EPS floor breach: got {} floor 1_076_322", candidate_eps);
}
```

### Step 4 — scripts/verify-retraction-metrics.sh

Mirror scripts/verify-crossshard-metrics.sh. Make executable (`chmod +x`). Verify all 5 counter names present on /metrics.

### Step 5 — 57-PERF-GATE.md

Create in the Phase 55/56 format:

```markdown
# Phase 57 — Perf Gate Evidence

**Status:** PASSED | FAILED (via C3 human_needed)
**Run:** 2026-04-21T... — MODE=complex DURATION=60 CPUS=8 CLIENTS=8
**Machine:** laptop / Hetzner CCX43 (choose)

## Summary Table

| Metric | Value |
|--------|-------|
| Candidate EPS | NNNN |
| Floor EPS | 1,076,322 (= 90% of Phase 56 baseline 1,195,914) |
| Headroom | +X.X% / -X.X% |
| Delta vs Phase 56 | -X.X% |
| Delta vs Phase 55 | -X.X% |
| Delta vs Phase 54 | -X.X% |

## Raw Evidence

`.planning/phases/57-retraction-across-crossshard-joins/perf-evidence/<TS>.txt`

## Advisory — Retraction-Firing Micro-bench (D-D4, NOT a gate)

| Metric | Value |
|--------|-------|
| Paired push+DELETE p50 latency | NNN µs |
| Paired push+DELETE p99 latency | NNN µs |
| 1000-event burst aggregate | NNN ms |

## Contingency Invoked

None / C1 / C2 / C3 [describe with evidence]
```
  </action>
  <acceptance_criteria>
    - `ls .planning/phases/57-retraction-across-crossshard-joins/perf-evidence/*.txt | wc -l` ≥ 1 (perf run artifact committed)
    - `grep -c "Candidate EPS" .planning/phases/57-retraction-across-crossshard-joins/57-PERF-GATE.md` ≥ 1
    - `grep -c "1,076,322" .planning/phases/57-retraction-across-crossshard-joins/57-PERF-GATE.md` ≥ 1
    - `grep -c "1,195,914" .planning/phases/57-retraction-across-crossshard-joins/57-PERF-GATE.md` ≥ 1 (Phase 56 baseline reference)
    - `test -x scripts/verify-retraction-metrics.sh` (executable bit set)
    - `bash scripts/verify-retraction-metrics.sh --self-check` — smoke-checks the script parses without syntax errors (add a self-check mode if possible; else run with a mock /metrics endpoint)
    - `cargo test --release --test retraction_perf_smoke` — without BEAVA_PERF_GATE, test exits early (no-op). With BEAVA_PERF_GATE=1 it runs and asserts.
    - `grep -rE '#\[ignore = "57-W4"' tests/` → 0 hits (final ignore markers removed)
    - Evidence file content has "EPS" + a numeric value ≥ expected magnitude (candidate_eps ~1M+)
    - If candidate < 1,076,322: exactly one contingency (C1/C2/C3) is documented in 57-PERF-GATE.md with rationale + delta
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/57-retraction-across-crossshard-joins/57-PERF-GATE.md &amp;&amp; grep -q "1,076,322" .planning/phases/57-retraction-across-crossshard-joins/57-PERF-GATE.md &amp;&amp; ls .planning/phases/57-retraction-across-crossshard-joins/perf-evidence/*.txt 2>/dev/null | head -1 | xargs test -s &amp;&amp; [ $(grep -rE '#\[ignore = "57-W4"' tests/ 2>/dev/null | wc -l | tr -d ' ') -eq 0 ]</automated>
  </verify>
  <done>Perf gate run; evidence committed; 57-PERF-GATE.md has structured verdict; scripts/verify-retraction-metrics.sh executable + self-checks; all 57-W4 markers removed; contingency ladder (if any) documented.</done>
</task>

<task type="auto">
  <name>Task 2: VERIFICATION.md + phase-close — ROADMAP + STATE + deferred-items + SUMMARY</name>
  <read_first>
    - .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-VERIFICATION.md (per-SC table format)
    - .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-04-SUMMARY.md (phase-close structure)
    - .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/deferred-items.md (57-NEXT format baseline)
    - .planning/ROADMAP.md Phase 57 section (current plans list)
    - .planning/STATE.md (Current Position + Accumulated Context format)
  </read_first>
  <files>
    .planning/phases/57-retraction-across-crossshard-joins/57-VERIFICATION.md
    .planning/phases/57-retraction-across-crossshard-joins/deferred-items.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <action>
### Step 1 — 57-VERIFICATION.md

Create per-SC verification table:

```markdown
# Phase 57 — VERIFICATION

Engineering-complete: YYYY-MM-DD.
TPC-CORR-10 status: passed | human_needed (perf only)

## Success Criteria

| SC | Description | Test | Status | Evidence |
|----|-------------|------|--------|----------|
| SC-1 | Source-table DELETE retracts downstream enrich | tests/crossshard_source_table_delete_retraction.rs | passed | test passed; commit SHA |
| SC-2 | SSJ tombstone retracts joined outputs | tests/crossshard_ssj_retraction.rs | passed | test passed; commit SHA |
| SC-3 | Late retraction beyond history_ttl warns | tests/late_retraction_warning.rs | passed | /debug/warnings.retraction_beyond_history populated |
| SC-4 | Perf ≤10% overhead | benchmark/fraud-pipeline/run_bench.sh | passed OR human_needed | 57-PERF-GATE.md |
| D-B5 | Depth guard at 16 hops | tests/retraction_depth_guard.rs | passed | test passed; commit SHA |

## TPC-CORR-10 Coverage Checklist

- [x] contributing_inputs: Option<ContribSet> field (D-A1-A4)
- [x] ShardOp::RetractDownstream variant (D-B1)
- [x] RetractReason enum with 3 variants (D-B2)
- [x] try_send + blocking oneshot + ShardOverload on Full (D-B3)
- [x] Idempotency on target — RetractOutcome::NoOp (D-B4)
- [x] Depth cap 16 hops + counter (D-B5)
- [x] history_ttl skip + warn + counter (D-C1)
- [x] StreamDefinition.history_ttl respected (D-C2)
- [x] 60s warning dedupe on /debug/warnings (D-C3)
- [x] 4 RED tests + sharding_parity extension (D-D1)
- [x] 5 correctness metrics (D-D2)
- [x] Perf gate 1,076,322 EPS floor (D-D3) — passed | human_needed
- [x] Advisory retraction-firing micro-bench (D-D4)

## Test Counts (cargo test --release)

| Suite | Count |
|-------|-------|
| --lib | 801 + N passed / 0 failed / 35 ignored |
| crossshard_source_table_delete_retraction | 1 passed |
| crossshard_ssj_retraction | 1 passed |
| late_retraction_warning | 1 passed |
| retraction_depth_guard | 1 passed |
| sharding_parity | 13 + 2 (retraction sub-cases) passed / 0 failed |
| source_table_cdc (serial, --ignored) | 7 passed / 0 failed (Phase 55-02 unregressed) |
| cross_shard_enrich_from_table | 2 passed (Phase 56 unregressed) |
| cross_shard_stream_stream_join | 2 passed (Phase 56 unregressed) |

## Known Pre-existing Issues

[list any issues carried over from Phase 55/56 VERIFICATION that are still open; do not re-investigate]

## 57-NEXT

See deferred-items.md
```

### Step 2 — deferred-items.md

Create with 57-NEXT entries. Baseline:

```markdown
# Phase 57 — Deferred Items (57-NEXT)

Post-engineering-close follow-ups. Priority-ordered.

## #1 — HIGH — [if perf gate via C3] human_needed soak

If Wave 4 invoked C3, this item captures the required perf-optimization work that blocks SC-4 from flipping passed.

## #2 — MED — Secondary index: source_table_key → downstream_row_keys

Today fan_out_retraction_for_source_table scans downstream streams O(N_rows). A secondary index (source_table_key → downstream_row_keys) would turn this into O(|affected|). Not needed at current scale; revisit if perf surface.

## #3 — MED — Batched retraction coalesce (C1 from contingency ladder)

If Wave 4 did NOT require C1 (i.e., gate passed without it), batched retraction coalesce is still a future latency improvement. Lands RetractDownstreamBatch variant + per-target coalesce buffer. ~5-10% latency win expected.

## #4 — LOW — Rewrite history beyond history_ttl

Today beyond-history retractions warn + skip. A future operator may want to rewrite history (replay from event log) for forensic / correction use cases. v2 of retraction.

## #5 — LOW — UI / CLI for inspecting retraction graphs

/debug/retractions endpoint surfacing the per-entity contributing_inputs graph for operator debugging. Out of scope for v1.2.

## #6 — LOW — Async / background retraction for non-critical-path

Today retractions are synchronous on the write path. For high-fan-in tombstones (e.g., user GDPR delete cascading through millions of events), an async queue would absorb the burst. v2.

## #7 — LOW — tracing crate adoption

Phase 56-03 noted eprintln! spam; Phase 57 adds more (emit_retraction_beyond_history_warning uses eprintln!). Adopting tracing would clean up uniformly. Cross-phase cleanup.

## #8 — LOW — Full N=1 ↔ N=8 byte-identical replay proptest with retraction

sharding_parity today enforces the routing invariant for retraction; a full byte-compare N=1 vs N=8 replay would prove retraction determinism end-to-end.
```

### Step 3 — ROADMAP.md Phase 57 row flip

In the Progress table at the bottom:
```markdown
| 57. Retraction across cross-shard joins | 5/5 | **Complete** (or **Engineering-complete — SC-4 human_needed** if C3) | YYYY-MM-DD |
```

In the Phase Details § Phase 57:
- Flip each `[ ]` to `[x]` for plans 57-00 through 57-04
- Add a one-line result blurb matching the Phase 56 entry format

In the Milestone header (top-of-file), if v1.3 milestone now has Phase 57 closed, note progress.

### Step 4 — STATE.md

Update Current Position:
```yaml
stopped_at: Phase 57 closed YYYY-MM-DD (engineering-complete; perf gate EPS NNNN [PASSED|C3 human_needed]; TPC-CORR-10 closed; ...)
```

Add Phase 57 entry to Accumulated Context with the same shape as the Phase 56 entry. Include:
- Aggregate outcome (TPC-CORR-10 closed)
- Per-wave commit SHAs
- Perf gate result
- Test counts
- 57-NEXT items filed
- Next-action recommendation (Phase 58 Tokio rewrite if Phase 57 gate passed; C3 soak work if human_needed)

### Step 5 — 57-04-SUMMARY.md

Follow Phase 56-04 summary format. Record the close.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/57-retraction-across-crossshard-joins/57-VERIFICATION.md` (exists)
    - `grep -c "TPC-CORR-10" .planning/phases/57-retraction-across-crossshard-joins/57-VERIFICATION.md` ≥ 2
    - `grep -c "SC-1\|SC-2\|SC-3\|SC-4" .planning/phases/57-retraction-across-crossshard-joins/57-VERIFICATION.md` ≥ 4
    - `test -f .planning/phases/57-retraction-across-crossshard-joins/deferred-items.md` (exists with ≥ 5 entries)
    - `grep -cE "^\|\s*57\." .planning/ROADMAP.md` — Phase 57 row in progress table has 5/5 or N/5 flipped
    - `grep -c "Phase 57 closed" .planning/STATE.md` ≥ 1
    - `grep -c "57-01\|57-02\|57-03\|57-04" .planning/STATE.md` ≥ 4 (wave commit SHAs recorded)
    - `grep -c "\[x\] 57-\(00\|01\|02\|03\|04\)-PLAN.md" .planning/ROADMAP.md` — 5 (all plans checked)
    - `cargo test --release --lib` — baseline preserved
    - Full regression battery unchanged: Phase 51/55/56 tests all GREEN
    - Atomic close commit on arch/tpc-full-shard
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/57-retraction-across-crossshard-joins/57-VERIFICATION.md &amp;&amp; test -f .planning/phases/57-retraction-across-crossshard-joins/deferred-items.md &amp;&amp; grep -q "Phase 57 closed" .planning/STATE.md &amp;&amp; grep -qE "^\|\s*57\." .planning/ROADMAP.md</automated>
  </verify>
  <done>57-VERIFICATION.md + deferred-items.md + ROADMAP.md Phase 57 flipped + STATE.md updated; close commit atomically lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| perf-evidence artifacts → git | measurements committed to disk; recoverable/auditable |
| ROADMAP.md + STATE.md edits | single source of truth for phase progression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-04-01 | Tampering | Silent floor relaxation (e.g., record 900K EPS as "passed" against 1.076M floor) | mitigate | Task 1 acceptance grep-checks the specific 1,076,322 number; contingency C3 requires explicit human_needed tag — not a floor change |
| T-57-04-02 | Information Disclosure | Perf-evidence artifacts may contain client-specific payload samples | accept | Bench workload is synthetic fraud data; no PII |
| T-57-04-03 | Denial of Service | Perf bench itself could exhaust CPU / disk on the measurement box | accept | Run on developer laptop / Hetzner CCX43; bounded DURATION=60s |
| T-57-04-04 | Repudiation | Missing perf-evidence artifact hides regression | mitigate | Task 1 acceptance requires `ls perf-evidence/*.txt` ≥ 1; Task 2 acceptance requires STATE.md + ROADMAP.md reference the evidence file |
</threat_model>

<verification>
- `cargo build --release` exits 0
- `cargo build --release --features state-inmem` exits 0
- `cargo test --release --lib` — baseline preserved
- `cargo test --release --test retraction_perf_smoke` — GREEN (env-gated, no-op without BEAVA_PERF_GATE)
- `cargo test --release --test crossshard_source_table_delete_retraction --test crossshard_ssj_retraction --test late_retraction_warning --test retraction_depth_guard` — all GREEN
- All Phase 51/55/56 tests still GREEN
- `grep -rE '#\[ignore = "57-W[0-4]"' tests/` → 0 hits
- `scripts/verify-retraction-metrics.sh` exits 0
- 57-PERF-GATE.md + 57-VERIFICATION.md + deferred-items.md committed
- ROADMAP.md Phase 57 row marked Complete (or engineering-complete + human_needed if C3)
- STATE.md Current Position advanced; Phase 57 entry in Accumulated Context
</verification>

<success_criteria>
- Perf gate measured + evidence-backed; candidate EPS ≥ 1,076,322 OR contingency invoked explicitly
- scripts/verify-retraction-metrics.sh executable + covers all 5 counters
- Advisory retraction-firing micro-bench p50/p99 recorded in 57-PERF-GATE.md
- 57-VERIFICATION.md per-SC table + TPC-CORR-10 coverage checklist
- deferred-items.md with priority-ordered 57-NEXT entries
- ROADMAP.md Phase 57 row closed
- STATE.md Current Position advanced + Phase 57 summary in Accumulated Context
- All 57-W{0..4} markers removed from tests/
- 2 atomic commits on arch/tpc-full-shard:
    (1) `perf(57-W4): perf gate measurement + advisory micro-bench + scripts/verify-retraction-metrics.sh`
    (2) `docs(57): close phase — VERIFICATION + deferred-items + ROADMAP/STATE updates`
</success_criteria>

<output>
After completion, create `.planning/phases/57-retraction-across-crossshard-joins/57-04-SUMMARY.md` following the Phase 56-04 summary format. Include:
- Perf gate result + contingency decision trail
- Verification log (cargo test output for all key suites)
- Grep-Count Evidence
- 57-NEXT summary
- Commit SHAs
- Next-action recommendation for post-Phase-57 work (Phase 58 Tokio connection-handling rewrite is next per ROADMAP)
</output>
