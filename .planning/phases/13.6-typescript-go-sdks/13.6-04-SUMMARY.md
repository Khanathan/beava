# Plan 13.6-04 Summary — Go wire layer

**Status:** COMPLETE
**Wave:** 2
**Commits:** 7 (3 RED + 4 GREEN incl. embed)

## What landed

- `sdk/go/wire.go` — frame codec (`EncodeFrame`/`DecodeFrame`) + opcode constants (`OpPing`, `OpRegister`, `OpPush`, `OpGet`, `OpGetResponse`, `OpBatchGet`, `OpReset`, `OpErrorResponse`, `CtJSON`)
- `sdk/go/transport_http.go` — `httpTransport` using `net/http` with structured error envelope decoding (`*RegistrationError`)
- `sdk/go/transport_tcp.go` — `tcpTransport` (Redis-style FIFO via mutex-guarded slice queue, single reader goroutine, length-prefixed framing)
- `sdk/go/embed.go` — `discoverBinary()`, `SpawnEmbeddedServer(ctx, opts)`, `(*SpawnedServer).Teardown(timeout)` — 4-step discovery + stdout JSON bind-event parsing
- Tests: `wire_test.go` (round-trip), `transport_http_test.go` (success + 400 envelope), `transport_tcp_test.go` (FIFO ordering)

## Verification

- `go vet ./sdk/go/...` clean
- `go test ./sdk/go/...` GREEN (4 tests + scaffold)
