# Plan 13.6-03 Summary — TS wire layer

**Status:** COMPLETE
**Wave:** 2
**Commits:** 7 (3 RED + 4 GREEN, including embed)

## What landed

- `sdk/typescript/src/wire.ts` — frame codec (`encodeFrame`/`decodeFrame`) + opcode constants (`OP_PING`, `OP_REGISTER`, `OP_PUSH`, `OP_GET`, `OP_GET_RESPONSE`, `OP_BATCH_GET`, `OP_RESET`, `OP_ERROR_RESPONSE`, `CT_JSON`)
- `sdk/typescript/src/transport.ts` — `HttpTransport` using global `fetch` with structured error envelope parsing
- `sdk/typescript/src/transport-tcp.ts` — `TcpTransport` (Redis-style strict-FIFO queue, `node:net` Socket, length-prefixed framing)
- `sdk/typescript/src/embed.ts` — `discoverBinary()` (4-step), `spawnEmbeddedServer()` (stdout bind-event parsing), `teardownProcess()` (SIGTERM → SIGKILL after 5s)
- `sdk/typescript/src/index.ts` — re-exports wire/transports/embed
- Tests: `wire.test.ts` (round-trip), `transport-http.test.ts` (success + 400 envelope), `transport-tcp.test.ts` (FIFO ordering)

## Verification

- `npm run build` GREEN — `dist/index.js` clean
- `npm test` — 6 passing across 4 files
- HTTP and TCP transports both validate against in-process fake servers
- Embed discovery mirrors `python/beava/_embed.py` 4-step order (BEAVA_BINARY → PATH → walk parents → BinaryNotFoundError)
