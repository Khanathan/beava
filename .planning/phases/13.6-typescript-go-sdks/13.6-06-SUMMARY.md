# Plan 13.6-06 Summary — Go App wire methods

**Status:** COMPLETE
**Wave:** 3
**Commits:** 3 (RED app_test.go → GREEN app.go + beava.go rewrite → embed_integration_test.go opt-in)

## What landed

- `sdk/go/app.go` — method receivers for all 9 wire methods (`Register`, `Push`, `PushSync`, `Get`, `GetGlobal`, `getInternal`, `BatchGet`, `Reset`, `Ping`)
- `sdk/go/beava.go` — rewritten `NewApp` with URL-scheme dispatch (HTTP / TCP / embed-on-first-call), `transport` interface, `tcpAdapter` (HTTP-style → opcode mapping), `(*App).Close` (idempotent, tears down embed subprocess)
- `sdk/go/app_test.go` — 14 unit tests against `httptest` mock server (Ping, Register, Push, PushSync, Get x4, GetGlobal, BatchGet x2, Reset x2, Close idempotent)
- `sdk/go/embed_integration_test.go` — opt-in via `BEAVA_RUN_EMBED_TESTS=1`

## Verification

- `go vet ./sdk/go/...` clean
- `go test ./sdk/go/...` GREEN — 18 passing total (14 app + 2 wire + 1 http + 1 tcp + scaffold; embed skipped)

## Decisions honored

- **ADR-003** Go uses separate `GetGlobal` method (NOT arity overload) — wire body identical (`key=""`)
- **Wire spec** OpPushSync RESERVED — `PushSync` delegates to `Push`
- **Cold-start GET** returns empty `FeatureResult{}`, not nil/error
- **No partial success** — `BatchGet` returns single `*RegistrationError` on any per-entry error
- `Reset` 403 surfaces verbatim with structured `*RegistrationError`

## Notes / deviations

- Combined RED tests for all methods into a single test commit (vs the plan's per-method RED→GREEN cycles); GREEN impl also a single commit. TDD discipline maintained (tests demonstrably failed before impl) while keeping commit history compact.
