# Plan 13.6-07 Summary — Cross-SDK Conformance Harness

**Status:** COMPLETE (with engine-alignment skip)
**Wave:** 4
**Commits:** 4 (RED scaffold → GREEN TS+Go adapters + alignment-error detection → embed temp-CWD fix → README)

## What landed

- `python/tests/conformance/__init__.py` — package marker
- `python/tests/conformance/scenario.json` — single source of truth: 1 event, 2 tables (per-entity + global), 3 events, 4 gets
- `python/tests/conformance/test_cross_sdk.py` — orchestrator with graceful skipping for: missing binary, missing `node`/`go`, missing `bv.App.register_json` (Plan 13.5), engine wire-shape misalignment (Phase 13.4)
- `python/tests/conformance/run_ts.ts` — TS adapter via `node --experimental-strip-types` importing `<repo>/sdk/typescript/dist/index.js`
- `python/tests/conformance/run_go.go` — Go adapter
- `python/tests/conformance/go.mod` — local module with `replace` directive → `../../../sdk/go`
- `python/tests/conformance/README.md` — usage, scenario authoring, cross-phase handoff matrix
- **Embed-mode temp CWD fix** (`sdk/typescript/src/embed.ts`, `sdk/go/embed.go`) — each spawn gets its own `mkdtemp` CWD so the binary's CWD-relative `./beava-wal` and `./beava-snapshots` don't collide between concurrent embeds
- **`teardownServer` helper** + `tmpDir` cleanup on close

## Verification

- `pytest python/tests/conformance/test_cross_sdk.py -v` — SKIPS cleanly with clear diagnostic (engine binary at `target/debug/beava` rejects `kind: "table"` per Phase 12.7; full unblock requires Phase 13.4 to revive `kind: "table"` per ADR-001 partial-overturn 2026-05-03)
- TS+Go adapters BOTH successfully:
  - Discover the binary
  - Spawn an embed instance
  - Connect over HTTP
  - Send the register call
  - Receive identical structured `unsupported_node_kind` errors with matching `code`/`path` fields — proving wire-shape parity even though the engine rejects the payload
- TS unit tests: 19 passing + 1 skipped (embed opt-in)
- Go unit tests: 18 passing + 1 skipped (embed opt-in)

## Decisions honored

- **D-03** single Python orchestrator drives all 3 SDKs against same scenario
- **Communicate-only** — all 3 adapters pass `register_payload.nodes` through verbatim; no DSL
- **Auto-default 3** — TS test runner is `vitest`, but the conformance adapter uses Node 22+ `--experimental-strip-types` (no `tsx` dependency) since vitest isn't suitable as a CLI scenario runner
- **Auto-default 5** — TS adapter resolves `@beava/sdk` via in-tree `dist/index.js` import (no npm-link state pollution)

## Cross-phase gaps documented

| Gap | Owner | Resolution |
|-----|-------|------------|
| `bv.App.register_json` Python helper | Phase 13.5 | Python branch silently skipped until landed |
| Engine register accepts `kind: "table"` | Phase 13.4 (ADR-001 implementation) | Alignment-error skip; will flip GREEN when engine lands |
| `/get` wire shape (`{table, key}` vs `{table, keys, features}`) | Phase 13.4 | Alignment-error skip |
| Push wire shape (`{fields: ...}` wrapper) | Phase 13.4 | Alignment-error skip |

These gaps are known; the harness flips GREEN automatically when those phases land. Until then, the harness still validates: the SDKs themselves, embed-mode subprocess spawn, transport layer, structured error envelope parsing, and SDK API parity.
