# Plan 13.6-01 Summary — TS SDK package scaffold

**Status:** COMPLETE
**Wave:** 1
**Commits:** 2 (test + feat, RED→GREEN)

## What landed

- `sdk/typescript/package.json` — ESM-only `@beava/sdk@0.0.0`, Node >=18, vitest test runner
- `sdk/typescript/tsconfig.json` — `module: esnext`, `target: es2022`, strict, declaration emit
- `sdk/typescript/src/index.ts` — placeholder `BeavaApp` (URL-stored), `RegistrationError`, `BinaryNotFoundError`
- `sdk/typescript/src/types.ts` — `Descriptor` (opaque `Record<string, unknown>`), result interfaces (`RegisterResult`, `PushResult`, `PingResult`), `RegisterOptions`, `AppOptions`
- `sdk/typescript/test/scaffold.test.ts` — vitest smoke test
- `sdk/typescript/.npmignore` + `.gitignore` + `README.md`

## Verification

- `npm install` GREEN
- `npm run build` GREEN — produces `dist/index.js` + `dist/index.d.ts`
- `npm test` GREEN — 1 passing
- No DSL files (`events.ts`/`col.ts`/`agg.ts`/`table.ts`) under `src/` — communicate-only scope intact

## Decisions honored

- **D-01** ESM-only — `"type": "module"`, esnext, single dist
- **D-02** monorepo subdir — `sdk/typescript/`, npm name `@beava/sdk`, repo points to `github.com/beava-dev/beava`
- **Scope amendment** — no DSL surface; `Descriptor` is `Record<string, unknown>`
