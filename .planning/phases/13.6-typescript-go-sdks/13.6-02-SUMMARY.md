# Plan 13.6-02 Summary — Go SDK module scaffold

**Status:** COMPLETE
**Wave:** 1
**Commits:** 2 (test + feat, RED→GREEN)

## What landed

- `sdk/go/go.mod` — module `github.com/beava-dev/beava/sdk/go`, Go 1.22
- `sdk/go/beava.go` — `App` struct, `NewApp(ctx, url, opts...)`, `URL()`, functional options (`WithTimeout`, `WithTestMode`, `WithBinaryPath`)
- `sdk/go/types.go` — `Descriptor` (= `map[string]any`), result structs (`RegisterResult`, `PushResult`, `PingResult`, `GetRequest`), register options (`WithForce`, `WithDryRun`)
- `sdk/go/errors.go` — `RegistrationError`, `ValidationError`, `BinaryNotFoundError`
- `sdk/go/scaffold_test.go` — RED→GREEN smoke test
- `sdk/go/README.md` — install + scope blurb

## Verification

- `go vet ./sdk/go/...` clean
- `go test ./sdk/go/...` passing (1 test)
- No DSL files (`events.go`/`col.go`/`agg.go`/`table.go`) under `sdk/go/`

## Decisions honored

- **D-02** module path `github.com/beava-dev/beava/sdk/go` (NOT `beava-io`)
- **Scope amendment** — no pipeline DSL surface
- **ADR-003** stub — `GetGlobal` will be a separate method (Plan 13.6-06), not arity overload
