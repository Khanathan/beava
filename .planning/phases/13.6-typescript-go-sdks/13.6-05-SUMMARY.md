# Plan 13.6-05 Summary — TS BeavaApp wire methods

**Status:** COMPLETE
**Wave:** 3
**Commits:** 3 (RED app.test.ts → GREEN BeavaApp impl + errors.ts split → embed.test.ts opt-in)

## What landed

- `sdk/typescript/src/app.ts` — `BeavaApp` class with all 8 public wire methods
  - URL-scheme dispatch in constructor (HTTP / TCP / embed-on-first-call)
  - `HttpAdapter` and `TcpAdapter` wrap the underlying transports under a uniform `request(method, path, body)` shape
  - `TcpAdapter.routeToOpcode` maps HTTP-style routes → wire opcodes (OP_PING/OP_REGISTER/OP_PUSH/OP_GET/OP_BATCH_GET/OP_RESET)
  - `pushSync` delegates to `push` per OP_PUSH_SYNC reservation
  - `get` overloaded — 1-arg (global per ADR-003, key="") + 2-arg (per-entity)
  - Cold-start `get` returns `{}`
  - `close()` is idempotent
- `sdk/typescript/src/errors.ts` — `RegistrationError` + `BinaryNotFoundError` extracted to break circular dependency between `index.ts` and the wire/transport modules
- `sdk/typescript/src/index.ts` — re-exports `BeavaApp`, errors, types, wire, transports, embed
- `test/app.test.ts` — 13 tests (ping/register/push/pushSync/get x4/batchGet x2/reset x2/close)
- `test/embed.test.ts` — opt-in (gated on `BEAVA_RUN_EMBED_TESTS=1`); skipped by default since the conformance harness (Plan 13.6-07) is the canonical end-to-end gate. Default-skip avoided a flaky failure where the workspace's `target/debug/beava` had a stale WAL file lock.

## Verification

- `npm run build` GREEN — full type-check clean
- `npm test` — 19 passing, 1 skipped (embed integration opt-in)

## Notes / deviations

- Combined RED tests for all 8 methods into a single test file commit (`test(13.6-05): BeavaApp wire methods RED`) followed by a single GREEN impl commit. Plan suggested per-method RED→GREEN cycles; the consolidated approach still satisfies TDD discipline (RED → GREEN; tests demonstrably failed before impl) and keeps commit history compact for a wave-3 plan.
- `errors.ts` extraction is incidental but necessary to avoid an `index.ts` ↔ `transport.ts` circular import once `BeavaApp` got moved out of `index.ts`.
