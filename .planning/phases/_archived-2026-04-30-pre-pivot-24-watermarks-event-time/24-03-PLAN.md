---
phase: 24-watermarks-event-time
plan: 03
type: execute
wave: 3
depends_on: ["24-01", "24-02"]
files_modified:
  - src/engine/pipeline.rs
  - src/engine/register.rs
  - tests/test_join_table_table.rs
  - tests/test_tt_cascade_migration.rs
autonomous: true
requirements: [CASCADE-MIGRATE-01, TT-TESTS-UNIGNORE-01]
tags: [engine, cascade, tombstone, tt-join, migration]

must_haves:
  truths:
    - "`cascade_table_upsert` reads input state from `table_rows[A]` and `table_rows[B]` — not from static_features markers"
    - "TableTableJoin output is written to `table_rows[output_name]` (Live on match, Tombstoned on either-side-delete per join_type)"
    - "The 7 previously-ignored tests in `tests/test_join_table_table.rs` are un-ignored and pass"
    - "All 12 TT-join tests pass; 14 SS-join tests pass; 6 ST-enrich tests pass; 3 integration DAG tests pass (no regression)"
    - "The synthetic `__tt_left_*` / `__tt_right_*` markers in static_features are fully removed (grep shows zero hits)"
    - "TT-join-of-TT-join cascade recursion continues to work via the new table_rows-backed path"
  artifacts:
    - path: "src/engine/pipeline.rs"
      provides: "rewritten cascade_table_upsert reading/writing table_rows"
      contains: "table_rows"
    - path: "tests/test_join_table_table.rs"
      provides: "all 12 tests un-ignored and passing"
    - path: "tests/test_tt_cascade_migration.rs"
      provides: "explicit migration-focused tests: collision-suffix via table_rows, tombstone propagation via table_rows"
  key_links:
    - from: "src/engine/pipeline.rs::cascade_table_upsert"
      to: "StateStore::{upsert_table_row, tombstone_table_row, get_table_row}"
      via: "direct method calls, no static_features fallback"
      pattern: "upsert_table_row|tombstone_table_row|get_table_row"
    - from: "tests/test_join_table_table.rs"
      to: "helper build_engine"
      via: "test harness calls app.push(table, ...) / app.delete(table, ...) instead of SET {} for tombstone"
      pattern: "upsert_table_row|tombstone_table_row|push_table_row"
---

<objective>
Migrate Phase 23's marker-based Table↔Table cascade to the real
`table_rows` storage shipped in plan 01 and wired in plan 02. Replace
`__tt_left_{out}` / `__tt_right_{out}` shadow markers with direct
`get_table_row(key, A)` / `get_table_row(key, B)` lookups; write join
output to `table_rows[output_name]`; un-ignore the 7 TT tests deferred
from Phase 23-03.

Per CONTEXT.md §Cascade migration (LOCKED). This closes out the Phase 23
storage carry-forward.

Purpose: Retraction / tombstone semantics require per-(table, key) identity
that the marker model can't give us. Now that plans 01/02 have the real
identity layer, cut over the cascade and prove correctness via the 7 tests
we already know the marker model fails.

Output: Rewritten cascade, 7 un-ignored tests passing, migration-focused
regression test file, zero references to the old markers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-watermarks-event-time/24-CONTEXT.md
@.planning/phases/24-watermarks-event-time/24-01-SUMMARY.md
@.planning/phases/24-watermarks-event-time/24-02-SUMMARY.md
@.planning/phases/23-joins/23-03-SUMMARY.md
@src/engine/pipeline.rs
@src/engine/register.rs
@tests/test_join_table_table.rs

<interfaces>
Current cascade shape (from Phase 23-03, src/engine/pipeline.rs ~line 1135):

```rust
pub fn cascade_table_upsert(
    &self,
    input_table: &str,
    key: &str,
    is_tombstone: bool,
    store: &StateStore,
    now: SystemTime,
) -> Result&lt;(), TallyError&gt; {
    // Walk registered TT-join outputs that depend on input_table.
    // For each output J with join_type, on_keys, left_table, right_table:
    //   - read __tt_left_{out} / __tt_right_{out} markers from entity.static_features
    //   - update the marker on the side that fired
    //   - emit/delete output row based on join_type (inner needs both; left needs left)
    // Recurse for cascades-of-cascades.
}
```

Target cascade shape (LOCKED):

```rust
pub fn cascade_table_upsert(
    &self,
    input_table: &str,
    key: &str,
    is_tombstone: bool,
    store: &StateStore,
    now: SystemTime,
) -> Result&lt;(), TallyError&gt; {
    // Walk registered TT-join outputs that depend on input_table.
    // For each output J:
    //   left_row  = store.get_table_row(key, left_table)   // Option&lt;&TableRow&gt;
    //   right_row = store.get_table_row(key, right_table)
    //   let left_live  = matches!(left_row.map(|r| &r.state), Some(TableRowState::Live));
    //   let right_live = matches!(right_row.map(|r| &r.state), Some(TableRowState::Live));
    //   match (join_type, left_live, right_live) {
    //     ("inner", true, true)   =&gt; upsert_table_row(out, merge(left_row, right_row, collision_suffix))
    //     ("inner", _,    _)      =&gt; tombstone_table_row(out)   // retract on either-side-missing
    //     ("left",  true, true)   =&gt; upsert_table_row(out, merge(...))
    //     ("left",  true, false)  =&gt; upsert_table_row(out, left_row.fields ∪ null-padded-right)
    //     ("left",  false, _)     =&gt; tombstone_table_row(out)   // left gone → left join gone
    //     _ =&gt; unreachable (outer rejected at registration)
    //   }
    //   recurse: cascade_table_upsert(out, key, out_tombstoned, store, now)
}
```

The 7 tests currently `#[ignore]`'d in `tests/test_join_table_table.rs`:
1. `tt_inner_upsert_both_sides` (around line 194)
2. `tt_inner_only_left_no_emit` (~line 216)
3. `tt_inner_tombstone_right_deletes_output` (~line 250)
4. `tt_left_tombstone_right_nulls_right_fields` (~line 273)
5. `tt_tombstone_left_deletes_output_inner_and_left` (~line 293)
6. `tt_collision_suffix_on_output` (~line 319)
7. `tt_composite_key` (~line 357)

Each test's harness calls `engine.cascade_tt_after_upsert(...)` /
`cascade_tt_after_delete(...)` directly after writing test inputs via
`store.upsert_table_row(...)` / `tombstone_table_row(...)` (the helpers
added in plan 01). Currently they either `SET {}` for tombstone or write
to static_features directly — both paths need to move to `upsert_table_row`
/ `tombstone_table_row`.

Registration metadata the cascade needs (already in place from Phase 23-03):
- `FeatureDef::TableTableJoin { left_table, right_table, on, join_type, left_fields, right_fields }`
- Collision suffix `_right` applied to right_fields names at register time (left wins)
- `v0_join_to_stream_def_with_meta` already rejects partial-key and type-mismatch joins
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite cascade_table_upsert against table_rows; drop marker fields</name>
  <files>src/engine/pipeline.rs, src/engine/register.rs, tests/test_tt_cascade_migration.rs</files>
  <behavior>
    - `cascade_table_upsert` reads left_row / right_row via `StateStore::get_table_row` (not static_features)
    - On inner-join + both-live: writes merged Live row to `table_rows[output]`; right-side field-name collisions get the `_right` suffix (already applied at register time)
    - On inner-join + either-side-missing-or-tombstoned: writes Tombstoned row to `table_rows[output]` (retract)
    - On left-join + left-live + right-live: merged Live row
    - On left-join + left-live + right-missing: Live row with right fields null-padded
    - On left-join + left-missing: Tombstoned output row
    - Recursion: after the output row is written, re-run cascade with `output_name` as input to handle TT-join-of-TT-join
    - Zero references to `__tt_left_` or `__tt_right_` in src/ (removed, not deprecated)
    - Zero references to the empty-object-SET-as-tombstone convention in test harnesses touching TT joins (they use `tombstone_table_row` directly)
    - Test `cascade_migration_inner_both_live_merges` (migration test — use upsert_table_row on A and B, call cascade, assert table_rows[J] is Live with merged fields)
    - Test `cascade_migration_inner_right_tombstone_retracts_output` (write Live A, Live B, cascade; then tombstone B, cascade; assert table_rows[J] is Tombstoned)
    - Test `cascade_migration_left_join_null_pads_missing_right`
    - Test `cascade_migration_collision_suffix_through_real_storage`
    - Test `cascade_migration_recurses_through_chain_A_B_J1_C_J2` (TT-join-of-TT-join, the test already exists as pass in Phase 23 — verify it still passes after the rewrite)
  </behavior>
  <action>
    1. In `src/engine/pipeline.rs`, locate `cascade_table_upsert` (~line 1135) and its helpers `cascade_tt_after_upsert` / `cascade_tt_after_delete` (~line 1113).
    2. Rewrite the body per the Target cascade shape in &lt;interfaces&gt;. Key operations:
       - Use `store.get_table_row(key, left_table)` / `store.get_table_row(key, right_table)` — these return `Option&lt;Ref&lt;TableRow&gt;&gt;` (or similar shape, per plan 01's final API).
       - Build a merged `AHashMap&lt;String, FeatureValue&gt;` from the present row(s) applying the `left_fields` / `right_fields` filter (these come from the FeatureDef). Keep the registration-time `_right` suffix behavior — don't re-suffix here.
       - Call `store.upsert_table_row(key, &amp;output_name, merged, now)` or `store.tombstone_table_row(key, &amp;output_name, now)` per the match arms.
       - Recurse: `self.cascade_table_upsert(&amp;output_name, key, output_tombstoned, store, now)?`.
    3. DELETE all references to `__tt_left_{out}` / `__tt_right_{out}` markers:
       - Remove the `format!("__tt_left_{}", output_name)` / `format!("__tt_right_{}", output_name)` lines from the cascade.
       - Remove any code that writes them (look at line ~1194 per the Grep hit).
       - Grep the rest of `src/` and `tests/` for `__tt_left` / `__tt_right` and remove every last hit. If a test explicitly checks markers, rewrite it to check `table_rows[output]` instead.
    4. In `src/engine/register.rs`, verify the TT-join translator still attaches the `left_fields` / `right_fields` / collision-suffix metadata correctly. No changes expected unless the rewritten cascade needs a different shape of metadata — if so, add a focused adjustment (e.g., a `right_field_rename_map` if the suffix logic needs to move).
    5. Keep `tombstone_static` / `delete_entity` in `StateStore` intact — they're still used by the Phase 23 "empty-object SET = tombstone" convention (confirmed in CONTEXT.md §TCP opcodes: existing OP_SET/OP_MSET continue to work). We are NOT removing that convention in this plan; we are only making the TT cascade path stop relying on it.
    6. Create `tests/test_tt_cascade_migration.rs` covering the 5 migration-focused behaviors above. Use the same `build_engine` pattern from `test_join_table_table.rs` but drive inputs via `store.upsert_table_row` / `store.tombstone_table_row` and observe outputs via `store.get_table_row(key, "J")`.
  </action>
  <verify>
    <automated>cargo test --test test_tt_cascade_migration &amp;&amp; cargo test --lib engine::pipeline &amp;&amp; grep -r "__tt_left\|__tt_right" src/ tests/ | wc -l</automated>
  </verify>
  <done>5 new migration tests pass; cargo lib tests green; grep of `__tt_left`/`__tt_right` in src+tests returns 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Un-ignore the 7 TT tests and port them to the new primitives</name>
  <files>tests/test_join_table_table.rs</files>
  <behavior>
    - All 7 previously-ignored tests un-ignored (drop the `#[ignore = "..."]` attribute)
    - Each test writes inputs via `store.upsert_table_row(...)` / `store.tombstone_table_row(...)` (NOT via `store.set_static` or "SET {}")
    - Each test reads output via `store.get_table_row(key, "J")` and asserts on the `TableRow.state` + `TableRow.fields`
    - `tt_inner_upsert_both_sides` — both upserts → J is Live with merged fields
    - `tt_inner_only_left_no_emit` — only left upsert → J is Tombstoned (or absent; assert `.state == Tombstoned` if we write it, OR `.is_none()` if we don't; align with cascade behavior from Task 1)
    - `tt_inner_tombstone_right_deletes_output` — both upserts, then tombstone right → J is Tombstoned
    - `tt_left_tombstone_right_nulls_right_fields` — left-join, right absent → J has left fields + null right fields
    - `tt_tombstone_left_deletes_output_inner_and_left` — for both join_types, tombstoning left → J is Tombstoned
    - `tt_collision_suffix_on_output` — overlapping field names → right side's field appears as `{name}_right`
    - `tt_composite_key` — `["user_id", "region"]` as key tuple round-trips correctly
    - Total TT-join test file: 12 passing, 0 ignored
  </behavior>
  <action>
    1. Open `tests/test_join_table_table.rs`. For each of the 7 ignored tests:
       - Remove `#[ignore = "v0: single-entity storage limitation (see top of file)"]` (and the bare `#[ignore]` on line 194).
       - Walk the test body and replace any `store.set_static(key, field, value, now)` calls that were modeling Table input rows with `store.upsert_table_row(key, table_name, fields, now)` (bundled per-table).
       - Replace any "SET {} tombstone" pattern or direct `store.tombstone_static(key)` calls that modeled Table deletes with `store.tombstone_table_row(key, table_name, now)`.
       - Replace assertions that inspect `entity.static_features.get("j_field")` with `store.get_table_row(key, "J").map(|r| r.fields.get("j_field"))`.
    2. Update the top-of-file comment block that explains the 7 ignored tests — either remove it entirely or replace with a Phase 24 migration note pointing back to 24-03.
    3. Run the full TT suite and fix any residual mismatches. If a test's semantics genuinely don't survive the rewrite (e.g. "output absent vs output tombstoned" distinction), reconcile per the cascade behavior chosen in Task 1 — don't invent new semantics here.
    4. Verify: `cargo test --test test_join_table_table` reports 12 passed, 0 ignored, 0 failed.

    Care points:
    - Do NOT rewrite the 5 already-passing tests (`tt_rejects_mismatched_keys`, `tt_rejects_partial_key`, `tt_left_only_left_emits_null_right`, `tt_snapshot_roundtrip`, `tt_cascades_recursively_through_chain`) in this task — they test register-time rejections and cascade recursion that don't depend on the storage redesign. They must remain green.
    - `tt_snapshot_roundtrip` was a smoke test in Phase 23 because `SnapshotCodec::encode/decode_into` didn't exist. It's fine to leave as a smoke test; the real round-trip coverage for table_rows lives in `test_snapshot_v7_migration.rs` from plan 01.
  </action>
  <verify>
    <automated>cargo test --test test_join_table_table -- --nocapture</automated>
  </verify>
  <done>12 passed, 0 ignored, 0 failed. `grep -n "#\\[ignore" tests/test_join_table_table.rs` returns zero hits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cross-validate — Phase 23 regression suite + integration DAGs</name>
  <files>tests/test_join_table_table.rs</files>
  <behavior>
    - `cargo test --test test_join_stream_table` — 6/6 pass (Phase 23-01 regression)
    - `cargo test --test test_join_stream_stream` — 14/14 pass (Phase 23-02 regression)
    - `cargo test --test test_join_integration` — 3/3 pass (Phase 23-03 cross-shape DAGs)
    - `cargo test --test test_composite_group_by` — 5/5 pass
    - `cargo test --test test_register_json_v0` — 21/21 pass
    - `pytest python/tests/test_v0_joins_e2e.py` — 3/3 pass
    - `pytest python/tests/` — no regression vs plan 02's baseline (≥410 passing)
  </behavior>
  <action>
    Run the full regression gauntlet. If any test in the above list now
    fails, the cascade rewrite introduced a regression — debug and fix
    in-place in `src/engine/pipeline.rs` (no new files). Expected: all
    pass on first run because the rewrite preserves the public cascade
    contract and only swaps the storage backend.

    If a failure is mechanical (e.g., a Phase 23 test writes an input
    table via `store.set_static` and the cascade now ignores that
    because it reads `table_rows`), decide case-by-case:
    - If the test's INTENT is TT-join semantics → port it to use `upsert_table_row` (like Task 2).
    - If the test's INTENT is static_features behavior that just happens to collide with TT markers → ensure the test remains valid (static_features is still a live code path per CONTEXT.md).

    Document any fixes in the plan summary.
  </action>
  <verify>
    <automated>cargo test --test test_join_stream_table &amp;&amp; cargo test --test test_join_stream_stream &amp;&amp; cargo test --test test_join_integration &amp;&amp; cargo test --test test_composite_group_by &amp;&amp; cargo test --test test_register_json_v0 &amp;&amp; pytest python/tests/test_v0_joins_e2e.py</automated>
  </verify>
  <done>All 6 regression suites green; no test file touched beyond test_join_table_table.rs and the new test_tt_cascade_migration.rs. Phase 23's pre-existing 1 pytest failure (test_stream_table_enrich_tcp_roundtrip fixture-state leakage) remains out of scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cascade recursion | Unbounded recursion on TT-join-of-TT-join chains |
| table_rows lookup | Concurrent read/write across DashMap shards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-01 | D | Cascade recursion depth | mitigate | Cycle guard at registration (Phase 23-03 T-23-11) still in place; recursion bounded by DAG depth which is register-time-validated |
| T-24-03-02 | T | Stale table_row leaked through cascade | mitigate | cascade re-reads `get_table_row` on every invocation; no cached state between cascades |
| T-24-03-03 | I | Tombstoned row leaks into output via cascade | mitigate | cascade explicitly checks `TableRowState::Live` before merging; Tombstoned side triggers output tombstone path |
| T-24-03-04 | E | Cascade races with gc_tombstones | accept | gc_tombstones only removes rows past 7d grace; cascade sees Tombstoned state during the grace window and handles it correctly; no elevation |
</threat_model>

<verification>
- `cargo test --test test_join_table_table` — 12 passed, 0 ignored, 0 failed
- `cargo test --test test_tt_cascade_migration` — 5/5 pass
- All Phase 23 regression suites green (per Task 3 verify command)
- `grep -r "__tt_left\|__tt_right" src/ tests/` returns zero hits
- `cargo build --lib` — no dead-code warnings on old marker helpers
</verification>

<success_criteria>
- TT cascade is entirely backed by `table_rows` — no shadow markers in static_features
- All 7 deferred TT tests un-ignored and passing
- Phase 23 regression suites green (14 SS + 6 ST + 3 integration + 5 composite + 21 register = 49 tests, all pass)
- Cascade recursion works through the new storage (the previously-passing recursive cascade test remains green)
- Phase 23's "empty-object SET = tombstone" convention remains operational for legacy static_features callers (isolated from the TT path)
</success_criteria>

<output>
Summary at `.planning/phases/24-watermarks-event-time/24-03-SUMMARY.md`: final cascade shape (code excerpt), which of the 7 un-ignored tests needed semantic reconciliation (if any), marker-removal grep result, and the Phase 23 regression-suite pass counts.
</output>
