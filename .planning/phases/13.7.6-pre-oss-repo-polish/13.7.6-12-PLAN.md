---
phase: 13.7.6
plan: 12
type: execute
wave: 4
depends_on: ["13.7.6-11"]
files_modified:
  - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-12-branch-rename.md
autonomous: true
requirements: [D-08-Workstream-D-13]
must_haves:
  truths:
    - "On the rewritten public bare clone (`/tmp/beava-public-bare.git`), HEAD ref is `main` (not `v2/greenfield`)"
    - "All other branches except `main` are deleted from the bare clone (no stale topic branches in the public push)"
    - "Architectural-invariant tests (12.6 mio-only, 12.7 events-only, 12.8 memory-governance, 13.4 events-only-table-derivation, 13.4.1 verb-style get) still exist in the rewritten tree (filter-repo path strips didn't break them)"
  artifacts:
    - path: ".planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-12-branch-rename.md"
      provides: "Branch-rename + branch-pruning + invariant-test-presence report"
      contains: "## HEAD ref state"
  key_links:
    - from: "/tmp/beava-public-bare.git/refs/heads/v2/greenfield"
      to: "/tmp/beava-public-bare.git/refs/heads/main"
      via: "git symbolic-ref HEAD refs/heads/main && git branch -m"
      pattern: "rename"
---

<objective>
On the rewritten public bare clone produced by Plan 11, rename the default branch from `v2/greenfield` to `main` and prune every other ref so the public push contains exactly one branch (`main`). After the rename, smoke-verify that the architectural-invariant test files (Phase 12.6 mio-only, Phase 12.7 events-only, Phase 12.8 memory-governance, Phase 13.4 / 13.4.1 events-only-table-derivation + verb-style get) still exist in the rewritten tree under HEAD — filter-repo path-strips were limited to `.planning/`, `CLAUDE.md`, `.claude/`, and the Plan-08 secrets paths; they MUST NOT have removed test files under `crates/beava-server/tests/`.

Per CONTEXT.md D-08 Workstream-D-13 + CONTEXT.md "Architectural invariants must stay green AFTER history rewrite".

Purpose: Workstream-D-13.

Output: Bare clone with default branch `main`; only `main` ref present; architectural-invariant tests verified present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-CONTEXT.md
@.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-11-rewrite-report.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename HEAD branch + prune other branches in the bare clone</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-12-branch-rename.md</files>
  <read_first>
    - 13.7.6-CONTEXT.md D-08 Workstream-D-13
    - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-11-rewrite-report.md (verifies bare clone exists at /tmp/beava-public-bare.git)
  </read_first>
  <action>
    On the rewritten bare clone, capture pre-state, rename to `main`, and prune everything else:
    Step 1 — capture pre-state: `cd /tmp/beava-public-bare.git && git branch -a > /tmp/branch-pre.txt && git symbolic-ref HEAD > /tmp/head-pre.txt`.
    Step 2 — rename current default to `main`: read current default via `OLD_DEFAULT=$(git symbolic-ref HEAD | sed 's|^refs/heads/||')`; if that string is not already `main`, run `git branch -m "$OLD_DEFAULT" main` then `git symbolic-ref HEAD refs/heads/main`.
    Step 3 — prune every branch except `main`: iterate `git for-each-ref --format='%(refname:short)' refs/heads/ | grep -v '^main$'` and `git branch -D` each one.
    Step 4 — capture post-state: `git branch -a > /tmp/branch-post.txt && git symbolic-ref HEAD > /tmp/head-post.txt`.

    Build artifact `13.7.6-12-branch-rename.md` with sections:
      - `## HEAD ref state` (pre vs post; post MUST be `refs/heads/main`)
      - `## Pre-rename branches` (full list from /tmp/branch-pre.txt)
      - `## Post-rename branches` (MUST be exactly `main`)
      - `## Verification`:
        - `git symbolic-ref HEAD` equals `refs/heads/main`
        - `git for-each-ref --format='%(refname:short)' refs/heads/ | wc -l` equals 1
        - `git for-each-ref --format='%(refname:short)' refs/heads/` equals `main`
  </action>
  <verify>
    <automated>(cd /tmp/beava-public-bare.git &amp;&amp; git symbolic-ref HEAD | grep -E '^refs/heads/main$') &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git for-each-ref --format='%(refname:short)' refs/heads/ | wc -l | tr -d ' ' | grep -E '^1$')</automated>
  </verify>
  <done>HEAD = refs/heads/main; exactly one local branch `main`; report sections populated.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-verify architectural-invariant test files survived the rewrite</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-12-branch-rename.md</files>
  <read_first>
    - CLAUDE.md (lists the architectural-invariant test paths: §mio-only Hot-Path Invariant, §Events-Only Invariant)
    - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-08-extra-strip-paths.txt (verifies no test path was accidentally added to strips)
  </read_first>
  <action>
    Smoke-verify the architectural-invariant test files exist in HEAD of the rewritten bare clone. The CLAUDE.md-locked invariants live at the paths below. The Phase 13.4 / 13.4.1 list was enumerated against `git ls-tree -r HEAD crates/beava-server/tests/ | grep -E 'phase13_4'` AT IETR-2 PLAN-CHECK TIME (2026-05-04) and FROZEN here; if Plan 11's filter-repo accidentally caught any of them, the smoke gate hard-fails and Plan 11 must be re-run.

    **Phase 12.6 — mio-only hot-path invariant:**
      - `crates/beava-server/tests/phase12_6_mio_only_dataplane.rs`
      - `crates/beava-server/tests/phase12_6_legacy_axum_killed.rs`

    **Phase 12.7 — events-only invariant:**
      - `crates/beava-server/tests/phase12_7_no_table_surface.rs`
      - `crates/beava-server/tests/phase12_7_legacy_table_handlers_killed.rs`

    **Phase 12.8 — memory-governance invariant:**
      - `crates/beava-server/tests/phase12_8_lifetime_ops_have_bounds.rs`
      - `crates/beava-server/tests/phase12_8_cold_entity_eviction.rs`
      - `crates/beava-server/tests/phase12_8_unbounded_op_in_lifetime_mode.rs`
      - `crates/beava-server/tests/phase12_8_metrics_endpoint.rs`

    **Phase 13.4 — events-only table-derivation + register/reset invariants (frozen 2026-05-04):**
      - `crates/beava-server/tests/phase13_4_dry_run_register.rs`
      - `crates/beava-server/tests/phase13_4_force_register.rs`
      - `crates/beava-server/tests/phase13_4_get_row_shape.rs`
      - `crates/beava-server/tests/phase13_4_global_table_routing.rs`
      - `crates/beava-server/tests/phase13_4_memory_persistence.rs`
      - `crates/beava-server/tests/phase13_4_op_batch_get.rs`
      - `crates/beava-server/tests/phase13_4_reset_default_rejected.rs`
      - `crates/beava-server/tests/phase13_4_reset_gated_env_var.rs`
      - `crates/beava-server/tests/phase13_4_reset_gated_programmatic.rs`
      - `crates/beava-server/tests/phase13_4_table_derivation_allowed.rs`
      - `crates/beava-server/tests/phase13_4_verb_routes_e2e.rs`

    **Phase 13.4.1 — verb-style get / batch-get / entity_id alias-warn (frozen 2026-05-04):**
      - `crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs`
      - `crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs`
      - `crates/beava-server/tests/phase13_4_1_verb_style_get.rs`

    **At-execution-time freshness check:** before running the per-file checks below, the executor SHOULD run `git -C /Users/petrpan26/work/tally ls-tree -r HEAD crates/beava-server/tests/ | grep -E 'phase13_4'` and append the actual list to the artifact's `## Frozen-list freshness check` section. If a NEW phase13_4_*.rs file has landed in the dev tree since 2026-05-04 that's not in the frozen list above, surface as a planner-concern (not a hard failure — the rewrite still preserved everything that was in the dev tree at clone time).

    For each path, verify presence in the rewritten bare clone HEAD using `git cat-file -e HEAD:<path>` (exit 0 = present, exit 128 = missing). Build a table in the artifact's `## Architectural-invariant test presence` section with rows: `path | present? (Y/N) | git cat-file exit code`. Every row MUST be Y. If any is N, this is a hard failure — Plan 11's filter-repo invocation accidentally caught a test file in its strip set; rollback per Plan 11's report and re-run with corrected strip paths.

    NOTE: this plan does NOT compile or run the tests against the bare clone (a bare clone has no working tree). Compile-and-run verification is Plan 23's closure smoke-clone task against a fresh non-bare clone. This task verifies file PRESENCE only, which is sufficient to detect filter-repo strip-list errors.
  </action>
  <verify>
    <automated>(cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase12_6_mio_only_dataplane.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase12_6_legacy_axum_killed.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase12_7_no_table_surface.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase12_7_legacy_table_handlers_killed.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase12_8_lifetime_ops_have_bounds.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase12_8_cold_entity_eviction.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase12_8_unbounded_op_in_lifetime_mode.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase12_8_metrics_endpoint.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase13_4_dry_run_register.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase13_4_force_register.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase13_4_get_row_shape.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase13_4_global_table_routing.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase13_4_memory_persistence.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase13_4_op_batch_get.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase13_4_reset_default_rejected.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase13_4_reset_gated_env_var.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase13_4_reset_gated_programmatic.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase13_4_table_derivation_allowed.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase13_4_verb_routes_e2e.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs) &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; git cat-file -e HEAD:crates/beava-server/tests/phase13_4_1_verb_style_get.rs)</automated>
  </verify>
  <done>All listed architectural-invariant test paths (Phase 12.6/12.7/12.8/13.4/13.4.1 — 22 files total) exist in HEAD of the rewritten bare clone; presence-table populated; freshness-check captured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filter-repo path-strip set → architectural-invariant tests | Wide path-strip globs could accidentally remove test files; presence check catches this before launch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.7.6-12-01 | T (Tampering) | filter-repo accidentally strips an architectural-invariant test | mitigate | Task 2 presence check covering Phase 12.6/12.7/12.8/13.4/13.4.1 (22 files); mismatch triggers rollback |
| T-13.7.6-12-02 | I (Info Disclosure) | residual stale branches in public push | mitigate | Task 1 prunes everything except `main` |
</threat_model>

<verification>
- HEAD = refs/heads/main on bare clone
- Exactly one local branch (`main`)
- All 22 listed architectural-invariant test files present in HEAD
- Frozen-list freshness check captured (warns if dev tree gained new phase13_4 tests since iter-2 plan-check)
</verification>

<success_criteria>
Public bare clone has the canonical default branch (`main`) and contains the architectural-invariant tests across Phase 12.6/12.7/12.8/13.4/13.4.1.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-12-SUMMARY.md`
</output>
</content>
</invoke>