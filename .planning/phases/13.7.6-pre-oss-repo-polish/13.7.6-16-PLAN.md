---
phase: 13.7.6
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
autonomous: true
requirements: [D-08-Workstream-E-17, D-06]
must_haves:
  truths:
    - "CONTRIBUTING.md per D-06 minimal scope: test commands + lint commands + PR workflow ONLY"
    - "Architectural invariants (mio-only, events-only, memory governance) NOT documented (per D-06 user override)"
    - "Existing CONTRIBUTING.md fields fixed: 'TCP port 6400 / HTTP 6401' → correct ports per current config.rs (likely 7380/7379); `petrpan26/beava` URLs → `beava-dev/beava`; obsolete `cargo test -- --test-threads=1` requirement audited (post-Phase 12 single-thread apply removed serial-test-port-bind requirement?)"
    - "Test commands match what CI actually runs (`cargo test --workspace --features testing` per CLAUDE.md TDD section + 13.7.5-A heuristic)"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Minimal contributor guide: setup + test/lint commands + PR workflow"
      min_lines: 40
      contains: "## Running Tests"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "actual CI test invocation"
      via: ".github/workflows/ci.yml run lines"
      pattern: "command parity"
---

<objective>
Rewrite CONTRIBUTING.md per CONTEXT.md D-06 (USER OVERRIDE: skip public architectural-invariant docs). Final shape: minimal contributor guide covering build setup, test commands, lint commands, PR workflow. Architectural invariants are NOT documented; tripwire tests in CI surface them organically when contributors break them.

Per CONTEXT.md D-08 Workstream-E-17 + D-06.

Existing CONTRIBUTING.md (106 LOC) has stale ports + URLs + repo path; this plan corrects them.

Purpose: Workstream-E-17.

Output: Minimal CONTRIBUTING.md aligned to actual CI commands + new repo URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-CONTEXT.md
@CONTRIBUTING.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit current CI commands + actual port defaults</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-16-contrib-audit.md</files>
  <read_first>
    - CONTRIBUTING.md (existing 106 LOC)
    - .github/workflows/ci.yml (canonical CI invocation lines)
    - .github/workflows/pr.yml (any extra PR-only checks)
    - CLAUDE.md §TDD Discipline (lists `cargo test --workspace --features testing` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` + `cargo fmt --all --check` as the canonical gates)
    - crates/beava-server/src/config.rs OR similar (canonical default ports — README and SECURITY.md disagree; verify which is current)
  </read_first>
  <action>
    Build audit report at `.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-16-contrib-audit.md` with:
      - `## Current vs canonical commands` — table mapping CONTRIBUTING.md line → CI workflow command → match? If mismatch, the CONTRIBUTING.md line is wrong.
      - `## Default ports` — actual current defaults from config (HTTP / TCP / admin); CONTRIBUTING.md "TCP 6400 / HTTP 6401" line is almost certainly stale.
      - `## Stale URLs` — every `petrpan26/...` reference in CONTRIBUTING.md.
      - `## Obsolete content` — `--test-threads=1` requirement: verify whether tests still need serial execution post-Phase 12+13. If parallel-safe now, drop. If still needed, keep + cite which test still binds a fixed port.
  </action>
  <verify>
    <automated>test -f .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-16-contrib-audit.md</automated>
  </verify>
  <done>Audit report exists with all four sections.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CONTRIBUTING.md aligned to canonical CI commands</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-16-contrib-audit.md (Task 1 audit)
    - .github/workflows/ci.yml (canonical commands)
    - CLAUDE.md §TDD Discipline (test commands, but DO NOT propagate the TDD-discipline content — D-06 says skip the architectural-invariant docs; per `feedback_no_ai_attribution_in_commits` the TDD rule belongs in private CLAUDE.md, not public CONTRIBUTING.md)
  </read_first>
  <action>
    Replace CONTRIBUTING.md with this structure (planner picks final wording; outline):
      - `# Contributing to Beava`
      - One-paragraph welcome (1-2 sentences pointing to README for product overview)
      - `## Development Setup` — Rust + Python prereqs (rustup; rustfmt/clippy components; Python 3.10+; OS deps)
      - `## Build` — `git clone git@github.com:beava-dev/beava.git`; `cd beava`; `cargo build`; `cd python && pip install -e .`
      - `## Run the server` — `cargo run` (or `./target/debug/beava`); current default ports per Task 1 audit
      - `## Running Tests` — three subsections:
        - Rust: `cargo test --workspace --features testing` (matches CI)
        - Python: `cd python && python -m pytest tests/ -q`
        - Format + lint: `cargo fmt --all --check && cargo clippy --workspace --all-targets --all-features -- -D warnings`
      - `## Project Structure` — short directory tree (`crates/`, `python/`, `sdk/typescript/`, `sdk/go/`, `examples/`, `docs/`)
      - `## Code Style` — Rust = rustfmt defaults, clippy clean; Python = ruff + mypy; comments only when explaining non-obvious WHY (per repo convention; phrased without referencing CLAUDE.md)
      - `## Pull Request Process` — fork; feature branch; ensure local CI green; open PR against `main`; describe what + why
      - `## Reporting Issues` — `https://github.com/beava-dev/beava/issues`; bug-report + feature-request templates
      - `## License` — Apache 2.0; contributor agrees by submitting

    Strip every `petrpan26/...` reference; replace with `beava-dev/beava`. Strip TCP-port-6400/HTTP-6401 references; use the actual current defaults from Task 1. Remove `--test-threads=1` if Task 1 confirms it's obsolete; keep with rationale if not.

    NO mentions of: TDD red→green discipline, mio-only invariant, events-only invariant, memory governance, perf gates, throughput baselines (per D-06).
  </action>
  <verify>
    <automated>head -1 CONTRIBUTING.md | grep -E '^# Contributing to Beava$' &amp;&amp; ! grep -E 'petrpan26/' CONTRIBUTING.md &amp;&amp; grep -c 'cargo test --workspace' CONTRIBUTING.md &amp;&amp; grep -c 'beava-dev/beava' CONTRIBUTING.md &amp;&amp; ! grep -iE '(mio-only|events-only|memory governance|TDD discipline|perf gates|throughput baseline)' CONTRIBUTING.md</automated>
  </verify>
  <done>CONTRIBUTING.md rewritten; no stale URLs/ports; no architectural-invariant docs (per D-06); commands match CI; new repo URL throughout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CONTRIBUTING.md → first PR contributor | Stale commands waste contributor time; over-documentation (D-06) creates maintenance debt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.7.6-16-01 | R (Repudiation) | stale CONTRIBUTING commands | mitigate | Task 1 audit + Task 2 rewrite from CI workflow lines |
</threat_model>

<verification>
- CONTRIBUTING.md exists with expected sections
- Commands match CI (cargo test --workspace --features testing per CLAUDE.md)
- No `petrpan26/` references
- No architectural-invariant content (per D-06)
</verification>

<success_criteria>
Minimal contributor guide; new contributor can build + test + open PR using CONTRIBUTING.md alone.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-16-SUMMARY.md`
</output>
