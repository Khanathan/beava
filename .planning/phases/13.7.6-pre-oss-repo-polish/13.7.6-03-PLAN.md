---
phase: 13.7.6
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-03-python-lint.md
  - "(python/beava/*.py or python/tests/*.py touched only if ruff or mypy --strict surfaces a regression that needs fixing; concrete files enumerated post-run)"
  - "(pyproject.toml only if ruff config needs sharpening; planner discretion)"
autonomous: true
requirements: [D-08-Workstream-C-3]
must_haves:
  truths:
    - "`ruff check python/` exits 0"
    - "`mypy --strict python/beava` exits 0"
  artifacts:
    - path: ".planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-03-python-lint.md"
      provides: "ruff + mypy --strict re-verify report (post-13.5 / 13.5.1 / 13.7.5)"
      contains: "## ruff output"
  key_links:
    - from: "python/beava/"
      to: "ruff lint catalogue"
      via: "ruff check"
      pattern: "All checks passed"
    - from: "python/beava/"
      to: "PEP 484 type contract"
      via: "mypy --strict"
      pattern: "Success: no issues found"
---

<objective>
Re-verify the Python SDK + tests against `ruff check` and `mypy --strict` after Phase 13.5 / 13.5.1 / 13.7.5 land. Phase 13.5.1 already shipped `mypy --strict` GREEN as an acceptance gate; this plan re-runs both tools, captures the output to an artifact, and surfaces / fixes any drift introduced after that gate. If the runs are clean, the artifact is the audit evidence; if anything regressed, this plan fixes it.

Purpose: Workstream-C requirement per CONTEXT.md D-08 Workstream-C-3. Public OSS Python SDK must be lint-clean.

Output: Python SDK passes `ruff check` + `mypy --strict`; artifact captures the run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-CONTEXT.md
@python/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run ruff check + capture output</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-03-python-lint.md</files>
  <read_first>
    - python/pyproject.toml (existing ruff config — informs which rules are active)
    - 13.7.6-CONTEXT.md D-08 Workstream-C-3
  </read_first>
  <action>
    From workspace root: `cd python && ruff check . 2>&1 | tee /tmp/ruff.txt`. Build artifact at `13.7.6-03-python-lint.md` (under `.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/`) with `## ruff output` section capturing full text. If exit code != 0, fix in source — prefer fixing root cause; only suppress with `# noqa: RULE-CODE — reason` if there's a structural reason (each suppression needs an inline reason). Re-run until exit 0.
  </action>
  <verify>
    <automated>cd python &amp;&amp; ruff check . 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <done>`ruff check` exits 0 from `python/`; artifact section `## ruff output` populated.</done>
</task>

<task type="auto">
  <name>Task 2: Run mypy --strict + capture output</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-03-python-lint.md</files>
  <read_first>
    - 13.5.1-SUMMARY.md (mypy --strict already GREEN per Phase 13.5.1 closure)
    - 13.7.6-CONTEXT.md D-08 Workstream-C-3
  </read_first>
  <action>
    From workspace root: `cd python && mypy --strict beava 2>&1 | tee /tmp/mypy.txt`. Append `## mypy --strict output` section to the artifact file from Task 1 with full text capture. If exit != 0, fix in source. Avoid `# type: ignore[code] — reason`; prefer adding the missing annotation. Each `# type: ignore` MUST carry an inline reason. Re-run until exit 0.
  </action>
  <verify>
    <automated>cd python &amp;&amp; mypy --strict beava 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <done>`mypy --strict beava` exits 0; artifact has both `## ruff output` + `## mypy --strict output` sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python SDK → public PyPI | Lint regressions ship to all OSS users; type drift causes runtime AttributeError surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.7.6-03-01 | T (Tampering) | Python SDK type contracts | mitigate | mypy --strict locks the type surface; ruff catches style + bug-class regressions before publish |
</threat_model>

<verification>
- `cd python && ruff check .` exits 0
- `cd python && mypy --strict beava` exits 0
- Artifact has both tool outputs captured
</verification>

<success_criteria>
Python SDK passes ruff + mypy --strict cleanly; v0 OSS publish gate.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-03-SUMMARY.md`
</output>
