---
phase: 13.7.6
plan: 16
subsystem: pre-oss-repo-polish
tags: [public-files, contributor-docs, workstream-e]
requires:
  - "Plan 13.7.6 CONTEXT (D-06 USER OVERRIDE: skip public-facing architectural-invariant docs)"
provides:
  - "CONTRIBUTING.md aligned to canonical CI commands + new repo URL (beava-dev/beava)"
  - "Audit artifact: .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-16-contrib-audit.md"
affects:
  - "CONTRIBUTING.md"
tech-stack:
  added: []
  patterns:
    - "Doc-only-plan exemption (CLAUDE.md §TDD Discipline item #7) — single chore: commit"
key-files:
  created:
    - ".planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-16-SUMMARY.md"
    - ".planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-16-contrib-audit.md"
  modified:
    - "CONTRIBUTING.md"
decisions:
  - "Per CONTEXT D-06 USER OVERRIDE: CONTRIBUTING.md is test+lint+PR-workflow basics only; tripwire CI tests educate breakers organically (no public mio-only / events-only / memory-governance / TDD-discipline / perf-gate / throughput-baseline content)."
  - "Canonical Rust test command = `cargo test --workspace --features testing` (CLAUDE.md §TDD Discipline item #5)."
  - "Canonical clippy gate = `cargo clippy --workspace --all-targets --all-features -- -D warnings`."
  - "Canonical fmt gate = `cargo fmt --all --check`."
  - "Default ports corrected: HTTP=`127.0.0.1:7379`, TCP=`127.0.0.1:7380` (was stale `6400/6401` in prior CONTRIBUTING.md)."
  - "All `petrpan26/beava` URLs → `beava-dev/beava` (CONTEXT D-03 repo rename)."
  - "Dropped `--test-threads=1` knob (post-Phase 12 single-threaded data plane removed serial-bind requirement; CI `ci.yml` uses `cargo nextest run` in parallel)."
metrics:
  duration: "~6 min"
  completed: "2026-05-06"
  tasks: 2
  files: 3
---

# Phase 13.7.6 Plan 16: CONTRIBUTING.md rewrite Summary

Rewrote `CONTRIBUTING.md` per CONTEXT D-06 USER OVERRIDE — minimal contributor guide covering build setup, canonical CI test/lint commands, and PR workflow only. Architectural invariants (mio-only, events-only, memory governance) are intentionally NOT publicly documented; the existing CI tripwire tests (`phase12_6_mio_only_dataplane.rs`, `phase12_7_no_table_surface.rs`, `aggop_size_within_cap`) educate breakers organically.

## What changed

- Replaced 106-LOC stale `CONTRIBUTING.md` with a 108-LOC lean rewrite.
- Sections: Prerequisites (Rust 1.94+, Python 3.10+, OS deps); Build (`cargo build --workspace`, editable Python SDK); Run the server (current 7379/7380 defaults); Run the tests (`cargo test --workspace --features testing`, `pytest tests/v0`); Lint and format (`cargo fmt --all --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `ruff check`, `ruff format --check`, `mypy --strict`); Pull-request workflow (fork → branch → conventional-commits → PR); Reporting bugs; Reporting security vulnerabilities (short cross-link to SECURITY.md, no duplication); License (Apache 2.0 inbound).
- All stale `petrpan26/beava` URLs replaced with `beava-dev/beava` (3 occurrences).
- Stale `TCP 6400 / HTTP 6401` claim corrected to `127.0.0.1:7380` / `127.0.0.1:7379` (matches current `crates/beava-server/src/cli.rs` + README.md).
- Dropped obsolete `cargo test -- --test-threads=1` requirement (post-Phase 12 single-threaded data plane removed the fixed-port serial-bind constraint; CI `ci.yml` now uses `cargo nextest run` in parallel without the flag).

## Tasks completed

| Task | Name | Commit | Files |
|---|---|---|---|
| 1 | Audit current CI commands + actual port defaults | (folded into chore commit) | `.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-16-contrib-audit.md` |
| 2 | Rewrite CONTRIBUTING.md aligned to canonical CI commands | (folded into chore commit) | `CONTRIBUTING.md` |

This plan ships under the **CLAUDE.md §TDD Discipline item #7** doc-only-plan exemption — single `chore(13.7.6-16): CONTRIBUTING.md rewrite` commit, no paired `test:` commit (no code or behavior change; pure public-facing documentation refresh validated by the verification predicates in the plan and execution prompt).

## Verification

All verification predicates from the plan and the execution prompt passed (run on the final tree):

```text
exists: PASS                  # test -f CONTRIBUTING.md
lines=108                     # 50 ≤ LINES ≤ 200
lines-range: PASS
header: PASS                  # ^# Contributing
exact-header: PASS            # ^# Contributing to Beava$
cargo-test: PASS              # contains 'cargo test'
cargo-test-workspace: 2       # 'cargo test --workspace' appears 2×
pytest: PASS                  # contains 'pytest'
security: PASS                # contains 'SECURITY.md'
apache: PASS                  # matches /Apache.?2\.?0/
no-tripwire-content: PASS     # NO match for /mio-only|events-only|memory-governance/
no-architectural-invariant: PASS  # NO match for TDD discipline / perf gates / throughput baseline either
no-petrpan26: PASS            # NO match for petrpan26/
beava-dev/beava: 3            # 3 references to canonical repo path
```

## Deviations from Plan

None — plan executed as written. No Rule 1 / 2 / 3 / 4 deviations triggered.

## Self-Check: PASSED

- `CONTRIBUTING.md` exists (verified 108 LOC, header matches).
- `.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-16-contrib-audit.md` exists.
- `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-16-SUMMARY.md` exists (this file).
- Single `chore(13.7.6-16):` commit hash recorded after staging (see commit log).
