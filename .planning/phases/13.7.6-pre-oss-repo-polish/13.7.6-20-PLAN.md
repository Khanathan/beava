---
phase: 13.7.6
plan: 20
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements: [D-08-Workstream-E-21]
must_haves:
  truths:
    - "`.gitignore` includes patterns for IDE configs (`.vscode/`, `.idea/`, `*.swp`), OS junk (`.DS_Store`, `Thumbs.db`), build outputs (`target/`, `dist/`, `*.pyc`, `__pycache__/`, `node_modules/`), local-secret patterns (`.env`, `.env.local`), and tooling caches (`.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`)"
    - "`.gitignore` does NOT exclude any path that's part of D-01's filter-repo strip set in a way that would mask leak detection (i.e. don't add `.planning/` to gitignore — that should be tracked-and-stripped, not gitignored, since the filter-repo strip is explicit and surveyable)"
    - "Cross-reference: every D-01-stripped path (`.planning/`, `CLAUDE.md`, `.claude/`) is documented in the audit artifact whether it appears in `.gitignore` (it shouldn't — they're tracked-but-stripped artifacts)"
  artifacts:
    - path: ".planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-20-gitignore-audit.md"
      provides: "Pre/post .gitignore audit; pattern coverage; cross-reference with D-01 strip set"
      contains: "## Coverage matrix"
  key_links:
    - from: "untracked sensitive files (.env, .vscode, etc.)"
      to: ".gitignore patterns"
      via: "pattern coverage"
      pattern: "ignored before commit"
---

<objective>
Audit `.gitignore` (existing 71 LOC) for coverage gaps. Add patterns for: IDE configs, OS metadata, build outputs, secret-files, tooling caches. Cross-check that D-01-stripped paths (`.planning/`, `CLAUDE.md`, `.claude/`) are NOT in `.gitignore` (they're explicit-strip-set, not gitignored — gitignoring them would mask future commits to those paths).

Per CONTEXT.md D-08 Workstream-E-21.

Purpose: Workstream-E-21. Public repo norm: standard ignore set; surface accidentally-tracked junk to a clean repo.

Output: `.gitignore` with comprehensive coverage; audit artifact tracking patterns added/removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-CONTEXT.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit existing .gitignore + identify coverage gaps</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-20-gitignore-audit.md</files>
  <read_first>
    - .gitignore (current 71 LOC)
    - 13.7.6-CONTEXT.md D-08 Workstream-E-21
  </read_first>
  <action>
    Build audit at `.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-20-gitignore-audit.md` with:

    Section 1 — `## Pre-audit .gitignore content` (full file capture).

    Section 2 — `## Coverage matrix` — table with rows for each category, columns `category | example pattern | currently in .gitignore? (Y/N) | action (KEEP / ADD)`. Categories:
      - **Build outputs**: `target/`, `dist/`, `*.pyc`, `__pycache__/`, `node_modules/`, `.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`
      - **IDE configs**: `.vscode/`, `.idea/`, `*.swp`, `*.swo`, `.cursor/`
      - **OS metadata**: `.DS_Store`, `Thumbs.db`
      - **Local secrets**: `.env`, `.env.local`, `.env.*.local`, `*.pem`, `*.key`, `secrets/`
      - **Logs / tmp**: `*.log`, `tmp/`, `/tmp.*`
      - **Coverage / profiling**: `coverage.xml`, `.coverage`, `htmlcov/`, `*.lcov`, `flamegraph.svg`
      - **Local Beava state**: `.beava/wal/`, `.beava/snapshots/` (these are runtime artifacts, NOT source)
      - **Phase 13.5 demo cache** (if any): `python/beava/datasets/_cache/` etc. — probe at write time

    Section 3 — `## D-01 strip-set cross-check`:
      - "`.planning/` — present in .gitignore? Should be NO (tracked + filter-repo-stripped per D-01)."
      - "`CLAUDE.md` — present in .gitignore? Should be NO (tracked + filter-repo-stripped per D-05)."
      - "`.claude/` — present in .gitignore? Should be NO (tracked + filter-repo-stripped per D-01)."
      - If any of those ARE in .gitignore, that's a coverage bug masking the strip operation; remove.

    Section 4 — `## Sample untracked + tracked check` — `git status --short --ignored | head -50`; identify any junk currently tracked that should be ignored.
  </action>
  <verify>
    <automated>test -f .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-20-gitignore-audit.md &amp;&amp; grep -c 'Coverage matrix' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-20-gitignore-audit.md</automated>
  </verify>
  <done>Audit complete; coverage matrix populated; D-01 cross-check documented.</done>
</task>

<task type="auto">
  <name>Task 2: Apply ADD-rows from coverage matrix to .gitignore</name>
  <files>.gitignore</files>
  <read_first>
    - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-20-gitignore-audit.md (Task 1 audit)
  </read_first>
  <action>
    For each row classified ADD in the coverage matrix, append the pattern to `.gitignore` under an appropriately-labeled comment heading. Example final shape:

    ```
    # ... existing patterns preserved ...

    # IDE configs
    .vscode/
    .idea/
    .cursor/
    *.swp
    *.swo

    # OS metadata
    .DS_Store
    Thumbs.db

    # Local secrets
    .env
    .env.local
    .env.*.local
    *.pem
    *.key
    secrets/

    # Tool caches
    .pytest_cache/
    .mypy_cache/
    .ruff_cache/

    # Coverage / profiling
    coverage.xml
    .coverage
    htmlcov/
    *.lcov
    flamegraph.svg

    # Local Beava runtime state
    .beava/wal/
    .beava/snapshots/
    ```

    Verification: `git check-ignore` for each newly-added pattern returns 0 against a hypothetical file matching the pattern. Don't accidentally add `.planning/` or `CLAUDE.md` or `.claude/` here — they're explicitly TRACKED for the dev workflow + STRIPPED via filter-repo for the public release.
  </action>
  <verify>
    <automated>grep -c '\.env' .gitignore &amp;&amp; grep -c '\.vscode/' .gitignore &amp;&amp; grep -c '\.DS_Store' .gitignore &amp;&amp; grep -c 'target/' .gitignore &amp;&amp; (! grep -E '^\.planning/$' .gitignore) &amp;&amp; (! grep -E '^CLAUDE\.md$' .gitignore) &amp;&amp; (! grep -E '^\.claude/$' .gitignore)</automated>
  </verify>
  <done>.gitignore covers all categories from the matrix; D-01-strip paths NOT added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree → tracked git repo | Untracked secrets / IDE configs / OS junk leak via `git add .` accidents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.7.6-20-01 | I (Info Disclosure) | accidentally-committed `.env` | mitigate | gitignore + Plan 08 secrets sweep |
| T-13.7.6-20-02 | T (Tampering) | gitignoring D-01 strip-paths masks future leaks | mitigate | Task 1 cross-check + Task 2 negative grep |
</threat_model>

<verification>
- All standard pattern categories present
- D-01 strip-set NOT added to .gitignore
- audit artifact present
</verification>

<success_criteria>
.gitignore is comprehensive; D-01 boundary respected.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-20-SUMMARY.md`
</output>
