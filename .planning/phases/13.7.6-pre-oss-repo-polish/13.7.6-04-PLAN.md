---
phase: 13.7.6
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-04-typescript-lint.md
  - "(sdk/typescript/src/*.ts touched only if tsc --noEmit or eslint surface a regression; concrete files enumerated post-run)"
autonomous: true
requirements: [D-08-Workstream-C-4]
must_haves:
  truths:
    - "`tsc --noEmit` exits 0 from `sdk/typescript/`"
    - "`eslint .` exits 0 from `sdk/typescript/` if eslint config exists; otherwise documented as 'no-config, tsc-only is sufficient'"
  artifacts:
    - path: ".planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-04-typescript-lint.md"
      provides: "tsc + eslint re-verify report"
      contains: "## tsc --noEmit output"
  key_links:
    - from: "sdk/typescript/src/*.ts"
      to: "TypeScript type checker"
      via: "tsc --noEmit"
      pattern: "no errors"
---

<objective>
Re-verify the TypeScript SDK against `tsc --noEmit` and (if configured) `eslint`. Phase 13.6 shipped the TS SDK; this plan re-runs the type-check + lint gate after any 13.7.5 comment-audit edits and captures evidence to an artifact.

Purpose: Workstream-C requirement per CONTEXT.md D-08 Workstream-C-4.

Output: TS SDK passes tsc + eslint cleanly; artifact captures the run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-CONTEXT.md
@sdk/typescript/package.json
@sdk/typescript/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify TS SDK location + run tsc --noEmit</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-04-typescript-lint.md</files>
  <read_first>
    - sdk/typescript/package.json (or wherever the TS SDK actually lives — Phase 13.6 set this; verify path before running)
    - sdk/typescript/tsconfig.json (strict-mode posture)
    - 13.7.6-CONTEXT.md D-08 Workstream-C-4
  </read_first>
  <action>
    First, locate the TS SDK directory: try `sdk/typescript/`, fall back to `find . -name 'tsconfig.json' -not -path '*/node_modules/*' -not -path '*/beava-website/*' -not -path '*/beava-design-system/*'`. If found at a different path, use that; if NOT found (TS SDK absent or moved), record "TS SDK not present at expected paths; skipping tsc check" in the artifact + exit. From the SDK directory: `npm install` (or `pnpm install` / `yarn` based on lockfile presence) then `npx tsc --noEmit 2>&1 | tee /tmp/tsc.txt`. Build artifact at `13.7.6-04-typescript-lint.md` with `## SDK path`, `## tsc --noEmit output` (full capture). Fix any errors at source; prefer real fix over `// @ts-ignore` (each ts-ignore MUST carry inline reason). Re-run until exit 0.
  </action>
  <verify>
    <automated>(cd sdk/typescript 2&gt;/dev/null &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | tail -3) || grep -q 'TS SDK not present' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-04-typescript-lint.md</automated>
  </verify>
  <done>tsc --noEmit exits 0 from the TS SDK directory (or artifact records absence + skip); artifact has SDK path + tsc output sections.</done>
</task>

<task type="auto">
  <name>Task 2: Run eslint if configured + capture output</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-04-typescript-lint.md</files>
  <read_first>
    - sdk/typescript/.eslintrc* / sdk/typescript/eslint.config.* (probe for eslint config presence)
    - 13.7.6-CONTEXT.md (D-08 says "optional eslint")
  </read_first>
  <action>
    From the same SDK directory: probe for eslint config (`.eslintrc.json`, `.eslintrc.js`, `eslint.config.js`, `eslint.config.mjs`). If config exists: `npx eslint . 2>&1 | tee /tmp/eslint.txt`; fix issues until exit 0; append `## eslint output` section to artifact. If NO eslint config exists: append `## eslint status` section with text "No eslint config found; tsc-only is sufficient per CONTEXT D-08 Workstream-C-4 ('optional eslint')". Do NOT introduce a new eslint config in this plan — that would expand scope.
  </action>
  <verify>
    <automated>grep -qE '## eslint (output|status)' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-04-typescript-lint.md</automated>
  </verify>
  <done>Artifact has eslint section (output or absence-rationale).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TypeScript SDK → public npm | Type errors and lint regressions ship to all `@beava/sdk` consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.7.6-04-01 | T (Tampering) | TypeScript SDK type contracts | mitigate | tsc --noEmit gate; eslint where configured |
</threat_model>

<verification>
- `tsc --noEmit` exits 0 from TS SDK directory (or absence documented)
- eslint output captured (or absence documented)
- Artifact has both sections
</verification>

<success_criteria>
TS SDK type-checks cleanly; v0 OSS npm publish gate.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-04-SUMMARY.md`
</output>
