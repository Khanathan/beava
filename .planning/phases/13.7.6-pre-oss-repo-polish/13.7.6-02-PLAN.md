---
phase: 13.7.6
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-02-cargo-audit.md
  - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-02-cargo-deny.md
  - deny.toml
  - "(if cargo audit / deny surface advisories or license incompatibilities, Cargo.toml or workspace dependency files may need patches; concrete files enumerated in artifacts post-run)"
autonomous: true
requirements: [D-08-Workstream-C-2]
must_haves:
  truths:
    - "`cargo audit` exits 0 OR every advisory is documented with disposition in the artifact"
    - "`cargo deny check` exits 0 OR every flagged license / advisory / source has disposition in the artifact"
    - "`deny.toml` exists at workspace root and codifies the allowed-license / banned-source / advisory-ignore policy"
  artifacts:
    - path: ".planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-02-cargo-audit.md"
      provides: "cargo-audit advisory inventory + disposition"
    - path: ".planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-02-cargo-deny.md"
      provides: "cargo-deny license / advisory / source policy + flagged-deps disposition"
    - path: "deny.toml"
      provides: "Workspace cargo-deny policy"
      contains: "[licenses]"
  key_links:
    - from: "Cargo.lock"
      to: "RustSec advisory DB"
      via: "cargo audit"
      pattern: "advisories: 0"
    - from: "Cargo.lock"
      to: "deny.toml policy"
      via: "cargo deny check"
      pattern: "all checks passed"
---

<objective>
Run `cargo audit` against the RustSec advisory database and `cargo deny check` against a new workspace `deny.toml` policy. Codify the allowed-license set (Apache-2.0 + MIT + BSD-2-Clause + BSD-3-Clause + ISC + Unicode-DFS-2016, planner's discretion), banned-source list (none), and any advisory ignore-list (each ignore must carry a one-line reason). Capture two artifact files documenting the inventory and disposition. If any advisory is unfixable in v0 (no upstream patch available, transitive-only, low impact) it gets ignored explicitly with reason; otherwise dependencies must be bumped.

Purpose: Workstream-C requirement per CONTEXT.md D-08 Workstream-C-2. v0 OSS launch must not ship CVE'd dependencies and must declare its license-compatibility posture publicly.

Output: cargo-audit + cargo-deny green; `deny.toml` checked in; two artifact files documenting the policy + any disposition rationale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-CONTEXT.md
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pinned cargo-audit + cargo-deny + run cargo audit</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-02-cargo-audit.md</files>
  <read_first>
    - https://embarkstudios.github.io/cargo-deny/ (canonical docs, referenced from CONTEXT.md)
    - 13.7.6-CONTEXT.md D-08 Workstream-C-2
  </read_first>
  <action>
    Install pinned tool versions: `cargo install --locked cargo-audit@0.21.1` and `cargo install --locked cargo-deny@0.16.4` (planner's discretion on exact pin; pick the latest stable as of phase start, document in artifact). Run `cargo audit --color=never 2>&1 | tee /tmp/cargo-audit.txt`. Build artifact at `13.7.6-02-cargo-audit.md` with sections: `## Tool versions` (pinned versions), `## Advisory output` (full text capture), `## Disposition` (per-advisory: vulnerable | unmaintained | yanked → action: bump to vN.N | ignore w/ reason | suppress in deny.toml). For every advisory NOT directly bumped, the disposition row MUST include a one-line reason. If `cargo audit` exits 0, record "no advisories" in disposition section.
  </action>
  <verify>
    <automated>test -f .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-02-cargo-audit.md &amp;&amp; grep -q 'Tool versions' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-02-cargo-audit.md &amp;&amp; grep -q 'Advisory output' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-02-cargo-audit.md &amp;&amp; grep -q 'Disposition' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-02-cargo-audit.md</automated>
  </verify>
  <done>cargo-audit installed at pinned version; advisory output captured; per-advisory disposition documented (or "no advisories" recorded).</done>
</task>

<task type="auto">
  <name>Task 2: Create deny.toml + run cargo deny + bump or ignore flagged deps</name>
  <files>deny.toml, .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-02-cargo-deny.md, Cargo.toml (only if a license-incompatible dep needs swap-out)</files>
  <read_first>
    - LICENSE (workspace is Apache-2.0; informs the allowed-license set)
    - 13.7.6-CONTEXT.md D-08 Workstream-C-2
    - https://embarkstudios.github.io/cargo-deny/checks/cfg.html
  </read_first>
  <action>
    Create `deny.toml` at workspace root with:
      - `[licenses]`: `allow = ["Apache-2.0", "MIT", "BSD-2-Clause", "BSD-3-Clause", "ISC", "Unicode-DFS-2016", "Zlib"]`; `confidence-threshold = 0.93`. Add `exceptions` for any specific crate that ships under a non-allow-listed license but is uncontroversial (e.g. `Unicode-3.0` for `unicode-ident` if hit). Each exception MUST carry an inline TOML comment with the reason.
      - `[advisories]`: `db-urls = ["https://github.com/rustsec/advisory-db"]`; `yanked = "deny"`; `ignore = []` (start empty; populate ONLY with reasons from Task 1's audit disposition).
      - `[bans]`: `multiple-versions = "warn"`; `wildcards = "deny"`. Allow-list any approved duplicate (e.g. `syn` major-version skew during transition windows) with reason.
      - `[sources]`: `unknown-registry = "deny"`; `unknown-git = "deny"` (no random git deps).
    Run `cargo deny check 2>&1 | tee /tmp/cargo-deny.txt`. For every flagged item: either swap the dep (planner picks; document in artifact) or add an explicit ignore/exception with reason. Build artifact at `13.7.6-02-cargo-deny.md` with: `## Policy summary` (license list, ban policy, source policy), `## Flagged items` (full output capture), `## Disposition` (per-item: action + reason). Final state: `cargo deny check` exits 0.
  </action>
  <verify>
    <automated>cargo deny check --hide-inclusion-graph 2&gt;&amp;1 | tail -10 &amp;&amp; test -f deny.toml &amp;&amp; grep -q '\[licenses\]' deny.toml &amp;&amp; grep -q '\[advisories\]' deny.toml &amp;&amp; grep -q '\[bans\]' deny.toml &amp;&amp; grep -q '\[sources\]' deny.toml</automated>
  </verify>
  <done>`deny.toml` exists with all four sections; `cargo deny check` exits 0; artifact documents policy + flagged-item disposition with reasons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependency tree → ship binary | Transitive deps may carry unpatched CVEs or license-incompatible code; OSS users inherit them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.7.6-02-01 | I (Information Disclosure) | transitive deps with known CVEs | mitigate | `cargo audit` runs as gate; deny.toml `yanked = "deny"` blocks yanked-version regressions |
| T-13.7.6-02-02 | T (Tampering) | unknown-source git deps | mitigate | deny.toml `unknown-git = "deny"` |
| T-13.7.6-02-03 | R (Repudiation) | license-incompatible deps shipped | mitigate | deny.toml allow-list locks acceptable licenses; CI check enforces |
</threat_model>

<verification>
- `cargo audit` exits 0 (or every advisory is suppressed in deny.toml with reason)
- `cargo deny check` exits 0
- `deny.toml` exists at workspace root with all four sections
- Both artifact files document policy + disposition
</verification>

<success_criteria>
Public clone passes `cargo audit` + `cargo deny check` cleanly; license + advisory + source policy is auditable.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-02-SUMMARY.md`
</output>
