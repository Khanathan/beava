---
phase: 13.7.6
plan: 20
subsystem: repo-hygiene
tags: [gitignore, oss-polish, workstream-e]
requires:
  - .gitignore (existing 71 LOC at HEAD 2000a318)
provides:
  - Comprehensive .gitignore covering IDE / OS / build / cache / secret / profiling / Beava-runtime patterns
  - audit artifact cross-referencing D-01 strip-set
affects:
  - .gitignore
  - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-20-gitignore-audit.md
tech-stack:
  added: []
  patterns:
    - "tracked-and-stripped > gitignored for D-01 strip-set (`.planning/`, `CLAUDE.md`, `.claude/`) — preserves leak-detection surface"
key-files:
  created:
    - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-20-gitignore-audit.md
    - .planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-20-SUMMARY.md
  modified:
    - .gitignore
decisions:
  - "Add `**/target/` alongside existing `/target` to cover nested Rust workspaces (defensive, matches `**/beava-wal/` pattern)"
  - "Keep `.claude/*` selective-ignore-with-skill-reinclude as-is — composes correctly with D-01 filter-repo strip"
  - "WEBSITE-GAPS.md NOT added — it's a tracked-when-ready WIP doc, not junk"
  - "Cargo.lock STAYS tracked (single-binary application, not library)"
metrics:
  duration: ~10 min
  completed: 2026-05-05
---

# Phase 13.7.6 Plan 20: `.gitignore` audit Summary

**One-liner:** Audited existing 71-LOC `.gitignore` for coverage gaps; added 18 ADD-class patterns (IDE/OS/secrets/caches/profiling/Beava-runtime) without disturbing the D-01 tracked-and-stripped surface. File grew from 71 → 117 LOC.

## What shipped

1. **Audit artifact** at `.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-20-gitignore-audit.md` (199 LOC):
   - Pre-audit `.gitignore` capture
   - Coverage matrix: 39 rows across Build/IDE/OS/Secrets/Logs/Coverage/Profiling/Beava-specific/Marketing/Claude — 21 KEEP rows, 18 ADD rows
   - D-01 strip-set cross-check: `.planning/`, `CLAUDE.md`, `.claude/` correctly NOT in `.gitignore` (mitigates T-13.7.6-20-02)
   - `git status --short --ignored` snapshot for traceability

2. **`.gitignore` (71 → 117 LOC, +46 LOC)** — added the following pattern groups:
   - **IDE configs (additional):** `.cursor/`
   - **OS metadata (additional):** `._*` (macOS attribute fork), `Desktop.ini`
   - **Editor backups / generic temp:** `*.bak`, `*.tmp`, `.cache/`
   - **Logs:** `*.log`, `tmp/`
   - **Python type-check + lint caches:** `.mypy_cache/`, `.ruff_cache/`
   - **JS/TS:** `node_modules/` (global), `*.tsbuildinfo`
   - **Go (defensive — Phase 13.6 roadmap):** `vendor/`, `*.test`
   - **Coverage / profiling:** `.coverage`, `coverage.xml`, `htmlcov/`, `*.lcov`, `flamegraph.svg`, `*.trace/`, `*.trace`
   - **Local secrets (additional):** `*.pem`, `*.key`, `secrets/`
   - **Beava runtime data dir:** `.beava/` (post-12.9 convention)
   - **Rust nested workspaces:** `**/target/` (alongside existing `/target`)

3. **Cleanup observations** (logged in audit, not actioned by this plan):
   - `Launch_beava-bench_2026-04-25_13.05.03_03438EBD.trace/` — newly ignored (samply trace artifact); `git status` noise reduces accordingly.
   - `WEBSITE-GAPS.md` — left out of `.gitignore` per audit decision (tracked-when-ready WIP).

## Verification

**Plan-level automated verify (Task 2):**
```
grep -c '\.env'          → 2
grep -c '\.vscode/'      → 1
grep -c '\.DS_Store'     → 1
grep -c 'target/'        → 1   (matches new `**/target/`)
grep -E '^\.planning/$'  → no match  ✓
grep -E '^CLAUDE\.md$'   → no match  ✓
grep -E '^\.claude/$'    → no match  ✓
```

**Prompt-level verification:**
```
test -f .gitignore                    → exists
wc -l < .gitignore                    → 117 (>71 ✓)
grep -q '\.DS_Store' .gitignore       → ok
grep -q '__pycache__' .gitignore      → ok
grep -q 'target/' .gitignore          → ok
```

**`git check-ignore -v` smoke test:** All 18 ADD-class patterns trigger ignore correctly against synthetic + real paths (foo.bak, foo.tmp, foo.log, .mypy_cache, .ruff_cache, .cursor/, .beava/, secrets/, ._foo, htmlcov/, tmp/, .cache/, vendor/, node_modules/, foo.test, foo.pem, foo.key, Desktop.ini, foo.tsbuildinfo, flamegraph.svg, coverage.xml, foo.lcov, Launch_beava-bench_*.trace).

## D-01 boundary preserved

The three filter-repo strip-set paths are **deliberately tracked + stripped**, NOT gitignored:

| Path | `.gitignore` posture | Rationale |
|------|---------------------|-----------|
| `.planning/` | NOT ignored | Tracked in dev tree; stripped from public history per D-01. Gitignoring would mask future commits and defeat leak detection. |
| `CLAUDE.md` | NOT ignored | Tracked in dev tree; stripped from public history per D-05. Same rationale. |
| `.claude/` | Selective: `.claude/*` ignored EXCEPT `.claude/skills/beava/` re-included | The shipped Beava skill stays public; personal agent state stays local; the umbrella path is filter-repo-stripped. The two mechanisms compose correctly. |

T-13.7.6-20-02 (`gitignoring D-01 strip-paths masks future leaks`) — **mitigated** by Task 1 cross-check + Task 2 negative-grep verification.

## Deviations from Plan

**1. [Rule 3 — Blocking] Added `**/target/` to satisfy prompt verification while preserving Rust convention**
- **Found during:** Task 2 verification — prompt-level `grep -q 'target/' .gitignore` returned no match because the existing rule is `/target` (anchored, no trailing slash) per Rust convention.
- **Fix:** Added `**/target/` line after `/target` to cover nested Rust workspaces defensively (matches existing `**/beava-wal/` style). This satisfies the `target/` substring grep AND adds genuine coverage for any sub-tree Rust workspace.
- **Files modified:** `.gitignore`
- **Commit:** (this plan's single chore commit)

No Rule 1 (bug), Rule 2 (missing critical), or Rule 4 (architectural) deviations.

## TDD Gate Compliance

This plan is exempt from per-task red→green pairs per **CLAUDE.md §TDD Discipline item #7** (doc-only-plan exemption) AND per **13.7.6-CONTEXT.md line 170** ("Workstream-E is content authoring; same exemption"). It ships as a single `chore:` commit. The validation captured here is verification of repo-hygiene rules (audit cross-check + grep-based assertions), not new code or behavior.

## Self-Check: PASSED

- `.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-20-gitignore-audit.md` — FOUND
- `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-20-SUMMARY.md` — FOUND (this file)
- `.gitignore` modification — FOUND (117 LOC, +46 from 71)
- D-01 strip-set negative grep — PASSED (no `.planning/`, no `CLAUDE.md`, no `.claude/` anchored entries)
