# Plan 13.7.6-03 — Python lint + type-check (ruff + mypy --strict)

**Status:** PASS
**Wave:** 1 (Workstream-C-3)
**Type:** code fixes (ruff lints) — `chore:` commit

## What landed

3 ruff lints in `python/beava/`, all fixed:

- `_agg.py:18` — I001 unsorted import block; auto-fixed via `ruff check --fix` (3 import lines reordered).
- `_events.py:260` — B905 `zip()` without `strict=` parameter; manually fixed: `zip(freevars, closure)` → `zip(freevars, closure, strict=False)`.
- `_table.py:60` — B905 same pattern; same fix.

`strict=False` was chosen (vs `strict=True`) because `freevars` and `closure` are guaranteed equal-length by Python's closure-cell semantics — adding `strict=True` would be defensive programming for an invariant Python itself enforces. `strict=False` is the explicit-no-op marker that satisfies the lint.

## Verification

```bash
ruff check beava/                # All checks passed!
mypy --strict beava/             # Success: no issues found in 19 source files
pytest tests/v0 -q               # 100 passed in 14.36s
```

All three workspace gates GREEN.

## Files modified

- `python/beava/_agg.py` (import reorder; +0/-0 net, 3 lines moved)
- `python/beava/_events.py` (B905 fix; +1/-1)
- `python/beava/_table.py` (B905 fix; +1/-1)

## Notes

Plan invokes CLAUDE.md TDD §item #7 doc-only-plan exemption framing (single `chore:` commit) — note that Plan 03 isn't strictly doc-only; it makes 2 LOC of source code edits (the B905 fixes). However, the edits don't change behavior (Python's zip already runs in non-strict mode by default); they're explicit-marker comments to satisfy the lint. The §item #7 single-commit framing applies as an analogy: pure hygiene with no semantic change.
