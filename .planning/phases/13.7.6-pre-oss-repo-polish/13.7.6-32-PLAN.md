---
phase: 13.7.6
plan: 32
type: execute
wave: 1.5
depends_on: [13.7.6-24, 13.7.6-25, 13.7.6-26, 13.7.6-27, 13.7.6-28]
files_modified:
  - crates/beava-bench/Cargo.toml
  - crates/beava-bench/src/bin/beava-bench.rs
  - crates/beava-bench/src/bin/beava-bench-v18.rs (DELETE post-migration)
  - crates/beava-bench/src/bin/beava-bench-legacy.rs (DELETE)
  - crates/beava-bench/src/bin/beava-bench-v2.rs (DELETE)
  - crates/beava-bench/src/cli/throughput.rs
  - crates/beava-bench/src/harness/mod.rs
  - crates/beava-bench/README.md
  - .planning/throughput-baselines.md (Reproduce section)
  - crates/beava-bench/tests/parallel_flag_removed.rs (UPDATE — refs beava-bench-v18)
autonomous: false  # USER-AUTHORED PLAN; needs user dispatch
requirements:
  - WEBSITE-GAPS-44
tdd: true
tags:
  - bench-consolidation
  - cli-cleanup
  - user-directive

must_haves:
  truths:
    - "Single canonical bench binary `beava-bench` with subcommands: throughput / mixed / memory / fsync."
    - "`beava-bench-legacy` + `beava-bench-v2` deleted outright (archival/early-stage; not v0 scope)."
    - "`beava-bench-v18`'s production harness merged into `beava-bench throughput` subcommand; v18 binary deleted."
    - "`beava-bench throughput --parallel N` works correctly (Option A from WEBSITE-GAPS Gap 44 — wire through, NOT strip). Reverses Plan 13.7.6-24's Option-B-strip choice."
    - "Sustained-rate measurement (`--duration-secs 60` no `--total-events` cap) produces same numbers as Plan 28's rebaseline (660K EPS small/tcp on Apple-M4) via the unified binary."
    - "`crates/beava-bench/Cargo.toml` has exactly ONE `[[bin]]` entry for `beava-bench`. `default-run = \"beava-bench\"` (self-references)."
    - "`cargo run -p beava-bench --release -- throughput --pipeline small --transport tcp --parallel 16 --duration-secs 60` produces sustained_eps output (no smoke-test ~1K EPS regression)."
    - "Workspace gates GREEN: `cargo test --workspace --features testing`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all --check`."
---

<objective>
**USER DIRECTIVE 2026-05-05 LATE:** "deprecate all the bench commands and keeping only beava-bench". Consolidate the 4-binary cruft (`beava-bench`, `beava-bench-legacy`, `beava-bench-v18`, `beava-bench-v2`) into a single canonical `beava-bench` binary. Reverses Plan 13.7.6-24's Option-B-strip choice (which left `beava-bench throughput --parallel N` as a no-op smoke-test) — restore `--parallel` and wire it through to v18's production harness, then merge that harness into `beava-bench throughput` and delete the v18 binary.

**Why:** The 4-binary split was cruft documented as a feature. `crates/beava-bench/README.md` (Plan 26) documents a "decision tree" between 4 binaries — that's documenting cruft. The right shape is one canonical CLI with subcommands; `beava-bench throughput` IS the production benchmark, gated by `--parallel`.

**Estimate:** 1-2 days. Heavy harness merge + tests + docs.
</objective>

<context>
- Plan 13.7.6-24 (commits f418a579 RED + 3c9eacde GREEN + c4d64c18 GREEN-2) stripped `--parallel` from `beava-bench throughput`. **This plan reverses that** — restores `--parallel` and wires it through correctly.
- Plan 13.7.6-25 added `default-run = "beava-bench-v18"` to Cargo.toml. **This plan flips it to `default-run = "beava-bench"`.**
- Plan 13.7.6-26 wrote `crates/beava-bench/README.md` documenting 4 binaries. **This plan rewrites it to document one binary with subcommands.**
- Plan 13.7.6-27 fixed v18's sustained-mode hang (`sem.close()` in deadline path) and added burst-vs-sustained label discipline. **The same fix needs to land in the consolidated `beava-bench throughput` harness post-migration.**
- Plan 13.7.6-28 produced the honest 660K EPS sustained baseline using v18. **The unified `beava-bench throughput --parallel 16` must produce equivalent numbers post-consolidation.**

**Pre-flight investigation already done in this session (revert-clean; nothing committed):**
- `beava-bench-legacy.rs` is 658 LOC; `beava-bench-v2.rs` is 1126 LOC. Both archival/early-stage.
- `beava-bench-v18.rs` is 1803 LOC; main entry at line 304 (`#[tokio::main(flavor = "multi_thread")] async fn main()`).
- Cross-references to delete-targets exist in: `crates/beava-bench/README.md`, `crates/beava-bench/src/bin/beava-bench.rs`, `crates/beava-bench/Cargo.toml`, `crates/beava-bench/tests/parallel_flag_removed.rs`, `crates/beava-bench/src/harness/mod.rs`. (Plus `.planning/` references — historical; safe to leave.)
- No `.github/workflows` references to delete-targets — workflows do not invoke v18/legacy/v2 directly.
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Test asserting `beava-bench throughput --parallel 32` produces sustained EPS &gt; 100K (real parallelism, not smoke)</name>
  <files>crates/beava-bench/tests/parallel_wired_through.rs (new) + parallel_flag_removed.rs (DELETE — opposite contract)</files>
  <read_first>
    - 13.7.6-27 SUMMARY.md — sustained-mode hang fix details
    - 13.7.6-24 SUMMARY.md — what Plan 24 stripped (this plan reverses)
  </read_first>
  <action>
    DELETE `crates/beava-bench/tests/parallel_flag_removed.rs` — its contract ("--parallel must be rejected by clap") is REVERSED by this plan. Add a deletion comment in the commit message citing Plan 32.

    CREATE `crates/beava-bench/tests/parallel_wired_through.rs`:
    - `throughput_help_advertises_parallel_flag` — assert `beava-bench throughput --help` stdout DOES contain `--parallel`
    - `throughput_accepts_parallel_argument` — assert `beava-bench throughput --parallel 32 --duration-secs 5 ...` runs successfully (no clap rejection)
    - `throughput_parallel_produces_sustained_eps` — full integration: spawn the bench against an in-process server with `--parallel 16 --duration-secs 5`; assert `sustained_eps:` is in stdout AND parsed value > 100,000 EPS (proves --parallel is wired through, not a smoke-test no-op)

    Run the tests — all 3 should FAIL against current code (because Plan 24 stripped --parallel).

    Commit: `test(13.7.6-32): assert beava-bench throughput --parallel is wired through`
  </action>
  <verify>
    <automated>cargo test -p beava-bench --test parallel_wired_through 2>&amp;1 | grep -E 'failed' &amp;&amp; echo "RED confirmed"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Migrate v18's production harness into `beava-bench throughput` + restore --parallel</name>
  <files>crates/beava-bench/src/cli/throughput.rs, crates/beava-bench/src/harness/mod.rs (new sub-module: harness/production.rs), crates/beava-bench/src/bin/beava-bench-v18.rs (DELETE post-migration)</files>
  <action>
    **Migration strategy:** v18's `main()` is monolithic (1803 LOC). Refactor into:
    - `crates/beava-bench/src/harness/production.rs` (new) — extract v18's worker-pool, frame-encoder, parallel-push loop, deadline-handling (with Plan 27's `sem.close()` fix preserved), HDR histogram capture, and BenchResult composition. Returns `Result&lt;BenchResult&gt;` like the existing smoke-test harness.
    - `crates/beava-bench/src/cli/throughput.rs::ThroughputArgs` — restore `pub parallel: u32` field (`#[arg(long, default_value = "16")]`). Update `run_throughput()` to dispatch:
      - `if args.parallel == 1`: existing smoke-test harness (single-threaded; CI-friendly)
      - `if args.parallel > 1`: new production harness from harness/production.rs
    - DELETE `crates/beava-bench/src/bin/beava-bench-v18.rs` (1803 LOC; logic now in harness/production.rs)
    - DELETE Cargo.toml `[[bin]]` entry for `beava-bench-v18`
    - Flip `default-run = "beava-bench-v18"` → `default-run = "beava-bench"`

    **Verification gates (must all PASS):**
    - `cargo build --workspace --release`
    - `cargo test -p beava-bench --test parallel_wired_through` (3/3 pass)
    - `cargo test -p beava-bench` (all binaries pass — including sustained_mode_terminates from Plan 27)
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    - `cargo fmt --all --check`
    - End-to-end: `cd crates/beava-bench && ../../target/release/beava-bench throughput --pipeline small --transport tcp --wire-format msgpack --duration-secs 60 --parallel 16 --pipeline-depth 1024 --no-ledger` produces `sustained_eps: ~660000` matching Plan 28 baseline (within ±10% variance band)

    Commit: `fix(13.7.6-32): migrate v18 production harness into beava-bench throughput; restore --parallel; delete v18 binary`
  </action>
</task>

<task type="auto">
  <name>Task 3: Delete `beava-bench-legacy` + `beava-bench-v2`</name>
  <files>crates/beava-bench/src/bin/beava-bench-legacy.rs (DELETE), crates/beava-bench/src/bin/beava-bench-v2.rs (DELETE), crates/beava-bench/Cargo.toml ([[bin]] entries removed)</files>
  <action>
    Pure deletion — these are archival/early-stage binaries not used in v0:
    - `rm crates/beava-bench/src/bin/beava-bench-legacy.rs` (658 LOC; Phase 7.5 archival)
    - `rm crates/beava-bench/src/bin/beava-bench-v2.rs` (1126 LOC; multi-shard early-stage; v0.1+ scope)
    - Remove the corresponding `[[bin]]` entries from `crates/beava-bench/Cargo.toml`

    No-op test: search for any cross-references; update any remaining doc-only references (already pre-flighted in this session — confirmed safe).

    Commit: `chore(13.7.6-32): delete beava-bench-legacy + beava-bench-v2 (archival, not v0 scope)`
  </action>
</task>

<task type="auto">
  <name>Task 4: Rewrite README + throughput-baselines Reproduce</name>
  <files>crates/beava-bench/README.md, .planning/throughput-baselines.md</files>
  <action>
    Rewrite `crates/beava-bench/README.md`:
    - Remove the 4-binary decision-tree table from Plan 26
    - New shape: one canonical `beava-bench` with subcommands (`throughput | mixed | memory | fsync`); each subcommand has a 3-paragraph guide
    - Throughput subcommand: explicit `--parallel N` flag wiring; smoke vs production guidance via parallelism level
    - Sustained vs burst note (Plan 27 label discipline)
    - Hardware-conditional EPS note (Plan 28 baseline)

    Update `.planning/throughput-baselines.md` Reproduce section:
    - Replace any `beava-bench-v18` invocation with `beava-bench throughput --parallel 16 --pipeline X --transport tcp --wire-format msgpack --duration-secs 60 --pipeline-depth 1024 --no-ledger`
    - Note the consolidation in a "Reproduce — post-13.7.6-32" sub-section; keep the historical v18 invocation marker for archival baseline rows

    Commit: `docs(13.7.6-32): README + throughput-baselines Reproduce post-consolidation`
  </action>
</task>

<task type="auto">
  <name>Task 5: Closure SUMMARY</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-32-SUMMARY.md</files>
  <action>
    Document the consolidation: 4-binary count → 1; v18 LOC migrated; legacy + v2 LOC deleted; --parallel restoration; baseline-equivalence verification (660K EPS small/tcp matches Plan 28).
  </action>
</task>

</tasks>

<verification>
- `crates/beava-bench/Cargo.toml` has exactly ONE `[[bin]]` entry (named `beava-bench`)
- `default-run = "beava-bench"` (self-references)
- `crates/beava-bench/src/bin/` contains ONE file: `beava-bench.rs`
- `cargo run -p beava-bench --release -- throughput --pipeline small --transport tcp --parallel 16 --duration-secs 60` produces `sustained_eps:` between 595K–725K (660K ±10%, matching Plan 28's rebaseline)
- `cargo test --workspace --features testing` GREEN
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` GREEN
- `cargo fmt --all --check` GREEN
- `pytest python/tests/v0` 100/100 pass
</verification>

<success_criteria>
- Single canonical `beava-bench` binary
- `--parallel` wired through (Option A from Gap 44, reversing Plan 24's Option-B-strip)
- Plan 28's sustained EPS numbers reproducible via the unified binary
- Phase 13.8 packaging ships ONE bench binary (not four)
</success_criteria>

<output>
After completion, write `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-32-SUMMARY.md`.
</output>
