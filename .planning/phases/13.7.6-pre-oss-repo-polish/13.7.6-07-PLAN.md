---
phase: 13.7.6
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-07-threat-model.md
autonomous: true
requirements: [D-08-Workstream-C-7]
must_haves:
  truths:
    - "Single-page ASVS Level-1 narrative documenting Beava v0's trust boundary, asset inventory, threat model, and mitigations"
    - "Trust boundary stated as 'single-tenant; operator owns network isolation; no auth in v0'"
    - "Per-mitigation cross-references to actual code (file:line OR architectural-test path)"
  artifacts:
    - path: ".planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-07-threat-model.md"
      provides: "ASVS L1 threat-model narrative for Beava v0"
      contains: "## Trust boundary"
  key_links:
    - from: ".planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-07-threat-model.md"
      to: "SECURITY.md (Plan 18) scope section"
      via: "documented same-phase cross-reference"
      pattern: "trust boundary"
---

<objective>
Produce a single-page ASVS Level-1 threat-model narrative for Beava v0. ASVS L1 is the lightest tier (low-risk apps, opportunistic attacker model) and is the right fit for a single-tenant in-memory feature server with documented "operator owns network isolation". The artifact lives at `.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-07-threat-model.md` (per planner discretion in CONTEXT.md "Claude's Discretion": "internal-only since D-06 skips public invariant docs"). Plan 18 (SECURITY.md) cross-references this file from its `## Scope` section without duplicating content.

Purpose: Workstream-C requirement per CONTEXT.md D-08 Workstream-C-7. Per CONTEXT.md D-06, public-facing architectural-invariant docs are skipped — this threat model lives internally; SECURITY.md hyperlinks it.

Output: Single-page threat-model artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-CONTEXT.md
@SECURITY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ASVS L1 threat-model narrative</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-07-threat-model.md</files>
  <read_first>
    - SECURITY.md (existing 43 LOC; informs current scope statement)
    - CLAUDE.md (mio-only invariant, events-only invariant — these ARE the architectural mitigations)
    - https://owasp.org/www-project-application-security-verification-standard/ (ASVS L1 chapter list)
    - 13.7.6-CONTEXT.md D-08 Workstream-C-7
  </read_first>
  <action>
    Write the artifact with this structure:

    ```
    # Beava v0 — ASVS Level-1 Threat Model

    ## ASVS Level
    L1 (opportunistic attacker; single-tenant; low-risk).

    ## Trust boundary
    - Single tenant per process; operator owns network isolation
    - No auth in v0 (HTTP + TCP); SECURITY.md documents this
    - HTTP admin sidecar binds a separate port (/health, /ready, /metrics, /registry); operator can firewall it independently from data-plane

    ## Asset inventory
    - In-memory entity-feature state (per process)
    - WAL on disk (.beava/wal/)
    - Snapshot files (.beava/snapshots/)
    - Registry (in-memory + WAL-replayed)

    ## Threat model (STRIDE)
    | Category | Threat | Mitigation | Code reference |
    |----------|--------|-----------|----------------|
    | S Spoofing | client identity not authenticated | accept (single-tenant; operator owns network) | SECURITY.md §Scope |
    | T Tampering | predicate-string injection at /register | mitigate via JSON-prelude shim chain | crates/beava-runtime-core/src/wire_request.rs ; cross-ref artifact 13.7.6-06 OWASP A03 |
    | T Tampering | TCP frame oversized → memory blowup | mitigate via 4 MiB default + frame_too_large + close | README.md §Connection model |
    | R Repudiation | no audit log | accept (v0 single-tenant) | n/a |
    | I Info Disclosure | log lines interpolating user-supplied IDs | mitigate via grep audit (Plan 06 Task 1) | tracing::warn! / error! sites |
    | D DoS | unbounded register predicates | mitigate via memory-governance lifetime-bound (V0-MEM-GOV-02) | crates/beava-server/tests/phase12_8_lifetime_ops_have_bounds.rs |
    | D DoS | unbounded entity-state | mitigate via cold-entity-TTL (V0-MEM-GOV-01) opt-in via @bv.event(cold_after=) | crates/beava-server/tests/phase12_8_cold_entity_eviction.rs |
    | E Elevation | force=True destructive register | gate behind explicit kwarg | docs/wire-spec.md §register; cross-ref artifact 13.7.6-06 OWASP A05 |
    | E Elevation | OP_RESET destructive opcode | gate behind test_mode flag | crates/beava-server/src/apply_shard.rs::dispatch_reset_sync ; cross-ref artifact 13.7.6-06 |

    ## Architectural invariants protecting the threat model
    - mio-only data plane (locked Phase 12.6) — `crates/beava-server/tests/phase12_6_mio_only_dataplane.rs` blocks introduction of a parallel runtime
    - events-only surface (locked Phase 12.7) — `crates/beava-server/tests/phase12_7_no_table_surface.rs` blocks revival of unmaintained table-mutation surface
    - memory governance (locked Phase 12.8) — V0-MEM-GOV-01/02/03; see CLAUDE.md §Memory Governance Invariant

    ## ASVS L1 controls coverage
    | ASVS chapter | Coverage |
    |--------------|----------|
    | V1 Architecture | mio-only invariant; STRIDE table above |
    | V2 Authentication | DEFERRED — v0 single-tenant; no auth (documented in SECURITY.md §Scope) |
    | V3 Session | n/a (stateless wire) |
    | V4 Access Control | accept — single-tenant |
    | V5 Validation | predicate-injection mitigation; cargo audit / deny |
    | V7 Cryptography | n/a (no auth, no encryption-at-rest in v0; TLS terminated upstream) |
    | V8 Errors+Logging | structured tracing; PII grep audit |
    | V9 Communications | TLS-terminate-upstream (operator config) |
    | V10 Malicious Code | cargo audit + cargo deny |
    | V11 Business Logic | force=True / OP_RESET gating |
    | V12 Files+Resources | WAL+snapshot path scoped to .beava/ |
    | V13 API | wire-spec.md JSON Schema 2020-12 contracts |
    | V14 Configuration | env-var BEAVA_* + CLI flags; deny.toml policy |

    ## Cross-references
    - SECURITY.md §Scope — public-facing scope statement (consistent with this internal threat model)
    - artifact 13.7.6-06-owasp-review.md — Top-10 + LLM Top-10 review
    - artifact 13.7.6-08-secrets-sweep.md — secrets sweep on full history
    ```

    Adjust file:line references after probing actual code paths. Don't fabricate references.
  </action>
  <verify>
    <automated>test -f .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-07-threat-model.md &amp;&amp; grep -c '## Trust boundary' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-07-threat-model.md &amp;&amp; grep -c 'STRIDE' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-07-threat-model.md &amp;&amp; grep -c 'ASVS' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-07-threat-model.md</automated>
  </verify>
  <done>Threat-model artifact exists; trust boundary stated; STRIDE table populated; ASVS L1 chapter coverage documented; cross-refs to OWASP review + SECURITY.md present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal-only artifact → public SECURITY.md | Threat-model is internal per D-06; SECURITY.md hyperlinks must remain accurate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.7.6-07-01 | I (Info Disclosure) | private threat-model leaks via SECURITY.md | accept | This artifact lives in .planning/ which is path-stripped from public history per D-01; SECURITY.md section pointing to "internal" doc is acceptable since users only see SECURITY.md |
</threat_model>

<verification>
- Artifact at expected path
- All sections present (trust boundary / asset inventory / STRIDE / ASVS coverage / cross-refs)
</verification>

<success_criteria>
ASVS L1 narrative shipped; v0 attack surface has a documented threat model.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-07-SUMMARY.md`
</output>
