---
phase: 13.7.6
plan: 22
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/pr.yml
  - .github/workflows/docs.yml
  - .github/workflows/bench-nightly.yml
  - .github/workflows/release.yml
  - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-22-workflows-audit.md
autonomous: true
requirements: [D-08-Workstream-E-23]
must_haves:
  truths:
    - "Every workflow file has been audited for: (a) repo URL references (must be `beava-dev/beava` not `petrpan26/`); (b) secret usage (`secrets.*` references documented in artifact, with corresponding GitHub Actions secret-name list for the user to populate at the new repo); (c) hardcoded internal URLs (any private grafana / prometheus / dashboard URL stripped); (d) third-party-action SHA pins (every `uses: foo/bar@vX` should be a SHA pin or a major-version pin documented in artifact)"
    - "ci.yml runs `cargo test --workspace --features testing` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` + `cargo fmt --all --check` per CLAUDE.md TDD §"
    - "release.yml is unchanged in scope here — Phase 13.8 extends to multi-arch wheels; this plan only audits public-readiness, NOT extends scope"
  artifacts:
    - path: ".planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-22-workflows-audit.md"
      provides: "Per-workflow audit (URL refs / secrets / SHA pins / scope-OK-for-public)"
      contains: "## Per-workflow audit table"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "canonical test commands"
      via: "command parity"
      pattern: "cargo test --workspace"
---

<objective>
Audit all five workflow YAML files under `.github/workflows/` for public-launch readiness:
- `ci.yml` (90 LOC) — main test/lint pipeline on PR + push
- `pr.yml` (131 LOC) — PR-only checks
- `docs.yml` (48 LOC) — docs build / deploy (Phase 13.7 wired this; verify it points to the new repo)
- `bench-nightly.yml` (95 LOC) — nightly perf bench
- `release.yml` (84 LOC) — release pipeline (Phase 13.8 extends this; THIS plan only audits public-readiness, doesn't extend)

Per CONTEXT.md D-08 Workstream-E-23. Phase 13.8 owns multi-arch-wheel publish jobs; this plan is audit-only.

For every workflow:
1. Strip any `petrpan26/...` reference; replace with `beava-dev/beava`.
2. Inventory `${{ secrets.* }}` references — document which secrets the new repo needs (CARGO_REGISTRY_TOKEN, NPM_TOKEN, DOCKERHUB_*, etc.). Surface as a launch-checklist item in the audit artifact.
3. Strip any hardcoded private URLs (internal grafana / prometheus / dashboard endpoints; private packagecloud / staging registries).
4. Audit third-party `uses: <action>@<ref>` lines — document each pin (SHA pin or major-version-tag) in the audit table; if a known-vulnerable action is in use, replace.
5. Verify the test commands run by ci.yml match CLAUDE.md TDD § canonical: `cargo test --workspace --features testing`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all --check`.

Purpose: Workstream-E-23.

Output: Five workflows audited + corrected for public; one audit artifact tabulating findings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-CONTEXT.md
@.github/workflows/ci.yml
@.github/workflows/pr.yml
@.github/workflows/docs.yml
@.github/workflows/bench-nightly.yml
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-workflow audit + emit audit table</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-22-workflows-audit.md</files>
  <read_first>
    - All five workflow YAMLs
    - 13.7.6-CONTEXT.md D-08 Workstream-E-23
    - CLAUDE.md §TDD Discipline (canonical test commands)
  </read_first>
  <action>
    Build audit at `.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-22-workflows-audit.md` with one table per workflow:

    For each workflow:
      - `## ci.yml` (and similarly for the others)
      - `### URL references`: every `petrpan26/...` line | replacement
      - `### Secrets used`: list of `${{ secrets.NAME }}` references | "must be added to repo secrets at beava-dev/beava"
      - `### Hardcoded private URLs`: any internal endpoint | strip-or-replace
      - `### Third-party action pins`: `uses:` lines | current ref (sha/tag) | known-good?
      - `### Command parity (ci.yml only)`: each test/lint command | matches CLAUDE.md canonical?
      - `### Scope OK for public`: yes/no — if no, the disposition flagged for Phase 13.8

    After audit, append `## Launch-checklist secrets` section: "Before Phase 13.8 GA, the following GitHub Actions secrets must be configured at github.com/beava-dev/beava/settings/secrets/actions: <list aggregated from per-workflow Secrets Used>". This is the master secret list the user populates pre-launch.
  </action>
  <verify>
    <automated>test -f .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-22-workflows-audit.md &amp;&amp; grep -c '## ci.yml' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-22-workflows-audit.md &amp;&amp; grep -c '## release.yml' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-22-workflows-audit.md &amp;&amp; grep -c 'Launch-checklist secrets' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-22-workflows-audit.md</automated>
  </verify>
  <done>Audit artifact has table for every workflow; Launch-checklist secrets section present.</done>
</task>

<task type="auto">
  <name>Task 2: Apply audit corrections to workflow YAMLs</name>
  <files>.github/workflows/ci.yml, .github/workflows/pr.yml, .github/workflows/docs.yml, .github/workflows/bench-nightly.yml, .github/workflows/release.yml</files>
  <read_first>
    - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-22-workflows-audit.md (Task 1)
  </read_first>
  <action>
    For each workflow, apply each correction documented in Task 1's audit:
      - Replace `petrpan26/...` with `beava-dev/beava`
      - Strip hardcoded private URLs
      - Update test commands in ci.yml to match canonical (if drift exists)
      - SHA-pin any unpinned third-party action (planner picks: pin to commit SHA from GitHub release; document version comment alongside)

    DO NOT touch:
      - Phase 13.8 owns extending release.yml to multi-arch wheels — leave the existing scope intact, just audit it
      - Triggers (push / pull_request branches) — leave as-is; Phase 13.8 may adjust for tag-driven release

    After edits, do a final repo-wide grep: `grep -rE 'petrpan26/' .github/` MUST return 0 lines.
  </action>
  <verify>
    <automated>(! grep -rE 'petrpan26/' .github/) &amp;&amp; grep -c 'cargo test --workspace' .github/workflows/ci.yml</automated>
  </verify>
  <done>All five workflows corrected per audit; grep -rE 'petrpan26/' .github/ returns 0 lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow YAML → CI runtime → secrets | Misconfigured workflows leak secrets to forks; hardcoded private URLs reveal infra |
| third-party actions → workspace privileges | Unpinned `uses:` lines accept arbitrary upstream changes; SHA pins gate this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.7.6-22-01 | I (Info Disclosure) | private internal URLs in workflow YAMLs | mitigate | Task 1 audit + Task 2 strip |
| T-13.7.6-22-02 | T (Tampering) | unpinned third-party action | mitigate | Task 1 audit; SHA-pin via Task 2 |
| T-13.7.6-22-03 | E (Elevation) | secrets exposed to PR-from-forks via pull_request_target | mitigate | Task 1 audits trigger types; flag any `pull_request_target` use |
</threat_model>

<verification>
- All five workflows pass `grep -E 'petrpan26/'` negative
- ci.yml uses canonical commands
- Audit artifact present
- Launch-checklist secrets section present
</verification>

<success_criteria>
GitHub Actions are public-ready; secrets-list is documented for launch checklist.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-22-SUMMARY.md`
</output>
