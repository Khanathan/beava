# Phase 13.7.6 — Discussion Log (batch-discuss 2026-05-04)

Captured during a 4-phase batch-discuss session. This log captures the 6 decisions affecting Phase 13.7.6.

## 7.6-A — D-01 history shape

**Question:** What history ships in the public github.com/beava-dev/beava repo?

**Options presented:**
- (a) Keep history + strip `.planning/` via git filter-repo (Recommended).
- (b) Squash to fresh `main` (single Initial commit).
- (c) Curated 20-30 commits (squash to logical milestones).
- (d) Keep everything including `.planning/`.

**User selection:** (a) Keep history + strip `.planning/`.

**Notes / follow-ups:** Combined with 7.6-E user override → also strip `CLAUDE.md` + `.claude/`. Result: `git filter-repo --invert-paths --path .planning/ --path CLAUDE.md --path .claude/`.

---

## 7.6-B — D-04 subject-rewrite scope

**Question:** How aggressive should the AI-attribution scrub be on commit subjects/bodies?

**Options presented:**
- (a) Trailer-only — surgical (Recommended) — strip ONLY `Co-Authored-By: Claude` + `🤖 Generated with` lines.
- (b) Trailer + rewrite Claude-mentions in subjects/bodies — risk of corrupting legitimate `CLAUDE.md` filename refs.

**User selection:** (a) Trailer-only surgical strip.

**Notes / follow-ups:** Commit subjects mentioning Claude (e.g., "fix: align Phase 12.7 tripwire after Claude resumed mid-Phase") are PRESERVED — historical accuracy outweighs cosmetic purity. CLAUDE.md filename refs in commit messages survive even though the file itself is stripped from trees.

---

## 7.6-C — Repo rename `tally → beava`

**Question:** Rename the GitHub repo `tally` → `beava` before launch?

**Options presented:**
- (a) Yes, rename pre-launch (Recommended).
- (b) Defer to v0.0.x — launch as `tally` first.
- (c) No — keep as `tally` permanently.

**User selection:** (a) Yes, rename pre-launch.

**Notes / follow-ups:** Confirms Phase 13.6 D-02 monorepo path `github.com/beava-dev/beava`. Pre-launch user-action item: verify or create the `beava-dev` GitHub org.

---

## 7.6-D — Author email on rewritten history

**Question:** Which email should appear as commit author on the public-history rewrite?

**Options presented:**
- (a) `hoang@beava.dev` (Recommended).
- (b) `hoang.phan@viggle.ai` (current local config).
- (c) GitHub `noreply` style.

**User selection:** (a) `hoang@beava.dev`.

**Notes / follow-ups:** Pre-launch user-action: add + verify `hoang@beava.dev` on the GitHub account that owns `beava-dev/beava` so commits link to the account in the contributors graph. mailmap.txt in Plan 13.7.6-10 maps every historical email → `hoang@beava.dev`.

---

## 7.6-E — `CLAUDE.md` filename

**Question:** Keep `CLAUDE.md` as the project conventions filename, or rename?

**Options presented:**
- (a) Keep as `CLAUDE.md` (Recommended).
- (b) Rename to `AGENTS.md`.
- (c) Rename to `CONVENTIONS.md`.

**User selection:** OVERRIDE — none of the above; "dont add claude.md in the repo".

**Resolution:** Strip `CLAUDE.md` entirely from public repo via `git filter-repo --invert-paths --path CLAUDE.md`. No rename; no replacement file. The file remains in the private dev branch as project conventions reference for Claude Code sessions; public contributors never see it.

---

## 7.6-E follow-up — Public-facing distillation of CLAUDE.md content

**Question:** Where do TDD discipline / perf gates / architectural invariants live for public contributors?

**Options presented:**
- (a) Distill into CONTRIBUTING.md (Recommended) — move TDD red→green / perf gates / 3 architectural invariants and their tripwire test paths into CONTRIBUTING.md.
- (b) Split: invariants in `docs/architecture/`, contrib bar in CONTRIBUTING.md.
- (c) Skip — don't document invariants publicly in v0.

**User selection:** (c) Skip — don't document invariants publicly in v0.

**Resolution:** Architectural invariants (mio-only, events-only, memory-governance) are NOT documented for public contributors in v0. CONTRIBUTING.md ships only test / lint / workflow basics. Invariants are enforced by tripwire tests in CI; first PR breaking one will surface the rule organically. Saves CONTRIBUTING / docs/architecture/ writing; lower maintenance cost; first-failing-PR contributors absorb the rule from CI output rather than reading a 500-line conventions doc.

---

## Deferred ideas surfaced during discuss

- v0.0.x bug fixes (HTTP-transport regression, conformance harness payload).
- Repo logo / brand assets / extended marketing material (Phase 13.7 already shipped beava-website).
- CLAUDE.md / docs translations.
- Cross-tool conventions standardization (`AGENTS.md` / `CONVENTIONS.md`) — moot per D-05 strip.
- Public architectural-invariant docs (per D-06 skip).

## Claude's Discretion (planner picks)

- Final secrets-sweep tooling order — `git secrets` first vs `trufflehog` first (planner picks; both run).
- Threat-model document path — likely internal-only since D-06 skips public invariant docs; could optionally surface as `SECURITY.md` cross-link (planner picks).
- Mailmap exact format and path — likely `mailmap.txt` at filter-repo run-root (planner picks).
- README.md hero rewrite scope — minimal "what / install / link" vs longer pitch (Phase 13.8 also touches; planner aligns with 13.8-06 expectations).
- CHANGELOG.md voice — terse vs descriptive (planner picks; likely terse with link to `.planning/phases/*/SUMMARY.md` is INVALID since `.planning/` strips; reword to link to phase sections).

---

*Discussion conducted via AskUserQuestion (single-question turns) on 2026-05-04. 6 decisions locked (5 + 1 follow-up). 1 user override on 7.6-E.*
