---
phase: 13.7.6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-01-clippy-report.md
  - "(allow-attribute audit may touch any file under crates/*/src/ or crates/*/tests/ if an unjustified #[allow(...)] is removed; concrete edits enumerated in artifacts/13.7.6-01-clippy-report.md)"
autonomous: true
requirements: [D-08-Workstream-C-1]
must_haves:
  truths:
    - "`cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0"
    - "Every remaining `#[allow(...)]` attribute under `crates/` carries a one-line `// reason: ...` comment or a tracking-issue link"
  artifacts:
    - path: ".planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-01-clippy-report.md"
      provides: "Pre-fix vs post-fix clippy diagnostic counts + table of every #[allow(...)] in crates/ with disposition"
      contains: "## clippy sweep summary"
  key_links:
    - from: "crates/ source tree"
      to: "clippy lint catalogue"
      via: "cargo clippy --workspace --all-targets --all-features -- -D warnings"
      pattern: "warnings emitted as errors"
---

<objective>
Run a full `cargo clippy` sweep across the workspace under `-D warnings` with all features enabled, fix every diagnostic, and audit every `#[allow(clippy::*)]` / `#[allow(dead_code)]` / `#[allow(unused)]` attribute under `crates/` so each one is either deleted or carries a one-line justification comment. Produces an artifact (`13.7.6-01-clippy-report.md`) capturing the before/after diagnostic counts and the disposition for every remaining `#[allow(...)]`.

Purpose: Workstream-C requirement that the public clone is clippy-clean with no unjustified suppression. Per D-08 Workstream-C-1 in CONTEXT.md.

Output: Clippy-clean workspace + auditable allow-attribute table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-fix clippy diagnostic baseline + allow-attribute inventory</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-01-clippy-report.md</files>
  <read_first>
    - CLAUDE.md §Conventions → Performance Discipline (clippy is a merge gate)
    - 13.7.6-CONTEXT.md D-08 Workstream-C-1
  </read_first>
  <action>
    Create the artifacts directory if missing. Run `cargo clippy --workspace --all-targets --all-features -- -D warnings 2>&1 | tee /tmp/clippy-pre.txt` (the existing baseline state — this MAY already exit 0; if so the diagnostic count is 0 and the rest of this plan focuses on the allow-attribute audit). Run `grep -rn --include='*.rs' '#\[allow(' crates/ | grep -v '^Binary file' > /tmp/clippy-allows-pre.txt`. Build the artifact file with:
      - `## Pre-fix diagnostic count`: line count from `/tmp/clippy-pre.txt`'s `warning:` / `error:` line greps.
      - `## Allow-attribute inventory (pre-fix)`: a table with columns `file:line | attribute | justification-comment? (Y/N) | disposition (KEEP / REMOVE / ADD-COMMENT)`. Initial disposition is KEEP everywhere; Task 2 fills in REMOVE / ADD-COMMENT.
    Note: if the pre-fix sweep is already clean (exit 0, 0 warnings), record that fact in the artifact; the audit value is the allow-attribute table, not the lint count.
  </action>
  <verify>
    <automated>test -f .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-01-clippy-report.md &amp;&amp; grep -c 'Pre-fix diagnostic count' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-01-clippy-report.md</automated>
  </verify>
  <done>Artifact file exists; pre-fix clippy output captured (count or "exit 0"); allow-attribute inventory table present with one row per `#[allow(...)]` site.</done>
</task>

<task type="auto">
  <name>Task 2: Fix clippy diagnostics + classify every allow-attribute KEEP/REMOVE/ADD-COMMENT</name>
  <files>crates/*/src/**/*.rs (exact files determined by Task 1's diagnostic list and allow-attribute inventory; enumerate post-fix in artifacts/13.7.6-01-clippy-report.md), .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-01-clippy-report.md</files>
  <read_first>
    - /tmp/clippy-pre.txt (pre-fix diagnostic list from Task 1)
    - /tmp/clippy-allows-pre.txt (allow-attribute inventory)
    - CLAUDE.md §Conventions → "no AI-generated comments / restating-the-obvious comments" — the justification-comment for a kept `#[allow(...)]` MUST explain WHY suppression is correct, not WHAT clippy flagged
  </read_first>
  <action>
    For every diagnostic in `/tmp/clippy-pre.txt`: fix in source. Prefer fixing the underlying code over adding a new `#[allow(...)]`. Acceptable suppression cases: false-positive on a generic-bound (rare), a clippy lint that fights an architectural invariant the workspace deliberately violates (must reference the invariant in the comment), test-only code with intentionally-redundant patterns. For every `#[allow(...)]` in the inventory: classify as KEEP (with `// reason: <why>` comment added if missing) or REMOVE (delete the attribute and verify clippy still passes). Update the table in the artifact file with final disposition for every row + the post-fix diagnostic count (target: 0). NO `#[allow(...)]` in the workspace post-fix may exist without an immediately-adjacent reason comment OR a tracking issue link.
  </action>
  <verify>
    <automated>cargo clippy --workspace --all-targets --all-features -- -D warnings 2&gt;&amp;1 | tail -5 &amp;&amp; cargo fmt --all --check &amp;&amp; (! grep -rn --include='*.rs' -B1 '#\[allow(' crates/ | grep -A1 -E '^[^/]*\.rs-[0-9]+-$' &gt; /dev/null) || echo 'OK if grep found nothing'</automated>
  </verify>
  <done>Workspace clippy passes under `-D warnings` with all features; `cargo fmt --all --check` passes; every remaining `#[allow(...)]` has a justification comment or tracking-issue link; artifact updated with post-fix counts + final disposition table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → ship | clippy is the last lint gate; an unjustified suppression can hide a real issue that ships to OSS users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.7.6-01-01 | T (Tampering) | `#[allow(...)]` attributes | mitigate | Every `#[allow(...)]` requires a reason comment or tracking-issue link; clippy run with `-D warnings` enforces no silent regressions |
</threat_model>

<verification>
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0
- `cargo fmt --all --check` exits 0
- Artifact file `13.7.6-01-clippy-report.md` exists with pre/post counts + allow-attribute disposition table
- No new `#[allow(...)]` introduced without a reason comment
</verification>

<success_criteria>
Workspace is clippy-clean under `-D warnings` with all features enabled and every remaining `#[allow(...)]` is auditable.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-01-SUMMARY.md`
</output>
