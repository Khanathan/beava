---
phase: 13.7.6
plan: 21
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/ISSUE_TEMPLATE/bug_report.md
  - .github/ISSUE_TEMPLATE/feature_request.md
  - .github/ISSUE_TEMPLATE/config.yml
  - .github/PULL_REQUEST_TEMPLATE.md
autonomous: true
requirements: [D-08-Workstream-E-22]
must_haves:
  truths:
    - "Issue templates ask for Beava version + transport (HTTP/TCP) + reproduction steps + expected vs actual"
    - "Feature-request template links to docs/operators/ for the v0 op catalog and surfaces 'is this v0.1+ deferred?' framing"
    - "PR template requires: linked issue (if any), test command run locally, lint command run locally, screenshot/log if user-visible"
    - "All templates strip `petrpan26/` references; replace with `beava-dev/beava`"
    - "config.yml `contact_links` point to actual venues (Discussions, Discord if claimed) or are stripped if not yet active"
  artifacts:
    - path: ".github/ISSUE_TEMPLATE/bug_report.md"
      provides: "Public bug-report template"
    - path: ".github/ISSUE_TEMPLATE/feature_request.md"
      provides: "Public feature-request template"
    - path: ".github/ISSUE_TEMPLATE/config.yml"
      provides: "Issue config (contact links)"
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "Public PR template"
  key_links:
    - from: ".github/PULL_REQUEST_TEMPLATE.md"
      to: "CONTRIBUTING.md test commands"
      via: "checklist parity"
      pattern: "cargo test --workspace"
---

<objective>
Audit + refresh the four template files under `.github/`:
- `.github/ISSUE_TEMPLATE/bug_report.md` (38 LOC)
- `.github/ISSUE_TEMPLATE/feature_request.md` (23 LOC)
- `.github/ISSUE_TEMPLATE/config.yml` (5 LOC)
- `.github/PULL_REQUEST_TEMPLATE.md` (28 LOC)

Required corrections per CONTEXT.md D-08 Workstream-E-22:
1. URL fixes — strip `petrpan26/`; use `beava-dev/beava`.
2. Bug report — version field updated for v0.0.x semver; transport field (HTTP / TCP / embed) added; reproduction-steps section preserved.
3. Feature request — link to v0 op catalog (`docs/operators/`); add "is this v0.1+ deferred?" question (deferred surface is documented).
4. PR template — checklist parity with CONTRIBUTING.md (Plan 16) test/lint commands; remove any "Phase NN" references.
5. config.yml — verify `contact_links` are accurate (Discussions if enabled; Discord/Slack only if claimed).

Purpose: Workstream-E-22.

Output: Four refreshed templates aligned to v0 surface + new repo URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-CONTEXT.md
@.github/ISSUE_TEMPLATE/bug_report.md
@.github/ISSUE_TEMPLATE/feature_request.md
@.github/ISSUE_TEMPLATE/config.yml
@.github/PULL_REQUEST_TEMPLATE.md
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh bug_report.md</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.md</files>
  <read_first>
    - .github/ISSUE_TEMPLATE/bug_report.md (current 38 LOC)
    - 13.7.6-CONTEXT.md D-08 Workstream-E-22
  </read_first>
  <action>
    Rewrite as standard issue-template with frontmatter + sections:

    ```
    ---
    name: Bug report
    about: Report a defect or regression
    title: "[bug] "
    labels: ["bug"]
    ---

    ## What happened
    <Concrete description of the unexpected behavior.>

    ## Reproduction
    Minimal steps to reproduce. Include:
    - Beava version: `<v0.0.x>` (or commit SHA)
    - Transport: `http` | `tcp` | `embed`
    - Operating system: `<Linux x86_64 / Linux ARM64 / macOS ARM64>`
    - Pipeline definition (Python or wire-spec JSON; redact any PII)
    - Sequence of pushes / gets that triggers the bug

    ## Expected
    <What you expected to happen.>

    ## Actual
    <What actually happened. Include error messages, stack traces, or `/metrics` output if relevant.>

    ## Logs
    <Server stderr at INFO/WARN/ERROR — first 50 lines around the failure.>

    ## Additional context
    <Anything else.>
    ```

    Strip any `petrpan26/` URL.
  </action>
  <verify>
    <automated>head -1 .github/ISSUE_TEMPLATE/bug_report.md | grep -E '^---$' &amp;&amp; (! grep -E 'petrpan26/' .github/ISSUE_TEMPLATE/bug_report.md) &amp;&amp; grep -c 'Transport:' .github/ISSUE_TEMPLATE/bug_report.md</automated>
  </verify>
  <done>bug_report.md refreshed; version + transport fields present; no stale URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh feature_request.md + config.yml</name>
  <files>.github/ISSUE_TEMPLATE/feature_request.md, .github/ISSUE_TEMPLATE/config.yml</files>
  <read_first>
    - .github/ISSUE_TEMPLATE/feature_request.md (current 23 LOC)
    - .github/ISSUE_TEMPLATE/config.yml (current 5 LOC)
    - docs/operators/index.md if exists
    - REQUIREMENTS.md `## v2 Requirements` (deferred surface)
  </read_first>
  <action>
    Rewrite feature_request.md:

    ```
    ---
    name: Feature request
    about: Propose a new operator, decorator, or wire-surface change
    title: "[feature] "
    labels: ["enhancement"]
    ---

    ## Summary
    <One-sentence what + why.>

    ## Use case
    <Concrete scenario. What pipeline are you trying to author? What's missing?>

    ## Proposed surface
    <If you have a sketch of the API, paste it here. Otherwise leave blank.>

    ## Already deferred?
    Some surfaces are intentionally not in v0. Check before opening:
    - 53-op catalog: https://github.com/beava-dev/beava/tree/main/docs/operators
    - v0 scope: tables (no upsert/delete/retract), joins, session windows, event-time / watermarks, `bv.fork` are descoped to v0.1+ or permanently.

    ## Alternatives considered
    <Any workaround you've tried.>
    ```

    Rewrite config.yml — verify which contact links are real:

    ```
    blank_issues_enabled: false
    contact_links:
      - name: GitHub Discussions
        url: https://github.com/beava-dev/beava/discussions
        about: Questions, ideas, design discussions, and "is this a bug or expected behavior?" before filing
      - name: Security report
        url: https://github.com/beava-dev/beava/security/advisories/new
        about: Use this for ANY security concern — not the public issue tracker
    ```

    (Drop Discord/Slack links UNLESS Phase 13.8 marketing CONTEXT confirmed them as launch venues — planner errs toward stripping non-existent venues; can re-add post-launch.)
  </action>
  <verify>
    <automated>head -1 .github/ISSUE_TEMPLATE/feature_request.md | grep -E '^---$' &amp;&amp; grep -c 'beava-dev/beava' .github/ISSUE_TEMPLATE/feature_request.md &amp;&amp; (! grep -E 'petrpan26/' .github/ISSUE_TEMPLATE/feature_request.md) &amp;&amp; grep -c 'beava-dev/beava' .github/ISSUE_TEMPLATE/config.yml &amp;&amp; (! grep -E 'petrpan26/' .github/ISSUE_TEMPLATE/config.yml)</automated>
  </verify>
  <done>feature_request.md + config.yml refreshed; contact links accurate; no stale URLs.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh PR template</name>
  <files>.github/PULL_REQUEST_TEMPLATE.md</files>
  <read_first>
    - .github/PULL_REQUEST_TEMPLATE.md (current 28 LOC)
    - CONTRIBUTING.md (refreshed in Plan 16; canonical command list)
  </read_first>
  <action>
    Rewrite as a brief checklist (template; PR author fills out):

    ```
    ## Summary
    <1-2 sentences. What does this PR change? Why?>

    ## Linked issues
    <Closes #N — if applicable.>

    ## Test plan
    - [ ] `cargo test --workspace --features testing` passes locally
    - [ ] `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes
    - [ ] `cargo fmt --all --check` passes
    - [ ] `cd python && python -m pytest tests/ -q` passes (if Python touched)
    - [ ] If user-visible behavior changed, attach a curl session or screenshot demonstrating the new behavior
    - [ ] CHANGELOG.md updated under `## [Unreleased]` (if user-visible change)

    ## Notes for reviewer
    <Anything reviewer should pay attention to.>
    ```

    Strip any references to internal phase numbers, `Phase NN`, `.planning/`, `CLAUDE.md`, or AI tooling.
  </action>
  <verify>
    <automated>grep -c 'cargo test --workspace' .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; grep -c 'CHANGELOG.md' .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; (! grep -E 'Phase 1[0-9]\.' .github/PULL_REQUEST_TEMPLATE.md) &amp;&amp; (! grep -E '\.planning/' .github/PULL_REQUEST_TEMPLATE.md)</automated>
  </verify>
  <done>PR template aligned to CONTRIBUTING.md commands; no internal references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| issue/PR templates → first-time contributor | Stale templates create friction; bad URLs surface 404s |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.7.6-21-01 | R (Repudiation) | broken contact links | mitigate | per-link verification; strip non-existent venues |
</threat_model>

<verification>
- All four template files refreshed
- No `petrpan26/` references
- Test commands match CONTRIBUTING.md
</verification>

<success_criteria>
Issue + PR templates are professional, link to canonical channels, ship without 404s.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-21-SUMMARY.md`
</output>
