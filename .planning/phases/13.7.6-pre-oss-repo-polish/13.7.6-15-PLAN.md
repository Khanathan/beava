---
phase: 13.7.6
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [D-08-Workstream-E-16]
must_haves:
  truths:
    - "README.md hero matches v0 messaging from CLAUDE.md §Project (single-binary real-time feature server; events-only post-pivot; no event-time / no joins)"
    - "All hyperlinks resolve (no `petrpan26/tally` references; no `docs/event-time.md` reference — that doc was removed in the 2026-04-30 pivot; no `GOVERNANCE.md` link if file doesn't exist)"
    - "60-second quickstart uses the v0 wire surface (POST /push/<event>, POST /get with verb-style body, POST /register) NOT the legacy `/features/<key>` shape"
    - "References to event-time, table push (`/push-table`, `/delete-table`), and `tally fork` are removed (consistent with Phase 12.6+12.7 surface reductions and the rebrand)"
    - "Badges point to `beava-dev/beava` (not `petrpan26/beava` or `petrpan26/tally`)"
  artifacts:
    - path: "README.md"
      provides: "Public-facing repo-root hero + quickstart + link tree"
      contains: "# Beava"
      min_lines: 50
  key_links:
    - from: "README hero"
      to: "60-second quickstart"
      via: "docker run + curl"
      pattern: "60-second"
    - from: "README link tree"
      to: "docs/ folder"
      via: "all referenced docs/ paths exist in repo"
      pattern: "no broken links"
---

<objective>
Rewrite README.md to align with the v0 Beava messaging post-pivot. The current README (114 LOC) is stale — references event-time, `tally fork`, `/features/<key>` legacy endpoint, table push opcodes, and `petrpan26/beava` badge URLs. Phase 13.7 already shipped beava.dev marketing copy; the repo-root README is intentionally distinct (per CONTEXT.md "<canonical_refs>" — the website-hero README is shorter and more dev-ops-shaped, the repo-root README is install + 60-second smoke + link tree).

Per CONTEXT.md D-08 Workstream-E-16 + "Claude's Discretion" "README.md hero rewrite scope — minimal 'what / install / link' vs longer pitch (Phase 13.8 also touches; planner aligns with 13.8-06 expectations)" — planner picks the **minimal install + 60-second smoke + link-tree** style for the repo-root README, leaving the longer pitch / hero copy to beava-website (Phase 13.7).

Stripped/replaced content:
1. Event-time references (no longer exist post-pivot)
2. `tally fork` subcommand (rebrand: `beava fork`; though per memory Phase 13.7 retired FeedBeaver and the docs surface uses different framing — verify what the v0 fork command IS at write-time)
3. Table push opcodes 0x0013/0x0014 (descoped per V0-EVENTS-ONLY-01)
4. `/features/<key>` legacy endpoint (replaced by POST /get verb-style)
5. `petrpan26/...` badge URLs (replaced by `beava-dev/beava`)
6. `GOVERNANCE.md` link (file doesn't exist; remove)

Purpose: Workstream-E-16.

Output: README.md aligned to v0 surface + new repo URL + correct opcode table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-CONTEXT.md
@README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Survey existing README + identify all stale references</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-15-readme-stale-refs.md</files>
  <read_first>
    - README.md (current 114 LOC)
    - CLAUDE.md §Project (canonical v0 messaging — "single-binary real-time feature server for fraud, ad-tech, and behavioral analytics")
    - CLAUDE.md §Constraints (perf targets, memory, compatibility — anchor for accurate claims)
    - docs/quickstart.md if exists (canonical 60-second smoke)
    - docs/wire-spec.md (canonical opcode + endpoint table)
    - docs/operators/index.md if exists (catalog link target)
  </read_first>
  <action>
    Build a stale-references inventory at `.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-15-readme-stale-refs.md` listing every line of the existing README that references:
      - `petrpan26/...` (badge URLs, repo links)
      - `tally fork` (subcommand naming)
      - `/features/<key>` (legacy endpoint)
      - opcodes 0x0013/0x0014 (table push/delete; descoped)
      - `event-time` / `docs/event-time.md` (post-pivot)
      - `GOVERNANCE.md` (file doesn't exist)
      - any other docs/ link that doesn't currently exist (probe with `test -f docs/<linked>`)
    For each row: existing-line | proposed-replacement | rationale.
  </action>
  <verify>
    <automated>test -f .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-15-readme-stale-refs.md</automated>
  </verify>
  <done>Stale-refs inventory complete with proposed replacements per row.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md aligned to v0 surface + new repo URL</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-15-readme-stale-refs.md (Task 1 inventory)
    - CLAUDE.md §Project (hero blurb)
    - docs/quickstart.md if exists (canonical 60-second flow)
    - docs/wire-spec.md (current opcode + endpoint shape)
  </read_first>
  <action>
    Replace README.md with the new content following this structure (planner discretion on exact wording; example shape):
      - `# Beava` heading
      - One-paragraph hero: from CLAUDE.md §Project ("real-time feature server for fraud, ad-tech, and behavioral analytics. Push events in over HTTP, beava tracks per-entity features...")
      - Three badges: CI (link to `beava-dev/beava` workflow), Docker Pulls (or strip if Phase 13.8 hasn't published yet — replace with "License" + "Discord" / "GitHub Discussions"), License (Apache 2.0)
      - `## 60-second quickstart` — exact shell session: `pip install beava` → minimal Python pipeline → `bv.App().push()` → `bv.App().get()`. Copy from `docs/quickstart.md` if present; otherwise use the curl flow from CLAUDE.md §Core Value ("declare a feature, push events, query it — under 10 minutes, with curl alone").
      - `## What is Beava` — 3-4 bullet sentences about what it replaces (Postgres triggers + Redis counters + cron) and what it offers (40+ aggregation primitives, single binary, in-memory, declarative)
      - `## TCP wire protocol` (KEEP this section but update opcode table — strip 0x0013/0x0014; correct opcode list per the current wire-spec.md). NOTE: per memory `project_v0_events_only_scope`, table push is descoped; the OPCODE table must reflect that.
      - `## Configuration` — keep BEAVA_TCP_* env vars; align with current `crates/beava-server/src/config.rs` defaults (ports 7379 HTTP / 7380 TCP per existing README OR whatever the actual current defaults are — probe at write-time)
      - `## Learn more` — link tree:
        - `docs/quickstart.md` — 60-second smoke
        - `docs/concepts.md` — events, operators, fork
        - `docs/http-api.md` — wire spec for non-Python users
        - `docs/architecture.md` — single-thread apply, WAL+snapshot, recovery, memory sizing
        - `docs/operators/` — 53 op catalog
        - `docs/wire-spec.md` — JSON Schema 2020-12 contracts
        - `examples/` — vertical demos
        - `CHANGELOG.md` (Plan 19) | `SECURITY.md` | `CONTRIBUTING.md`
      - `[Apache 2.0](LICENSE) · [CHANGELOG](CHANGELOG.md) · [SECURITY](SECURITY.md) · [CONTRIBUTING](CONTRIBUTING.md)` footer (NO `GOVERNANCE.md`)

    Probe each docs/ link with `test -f docs/<path>` before writing — broken links are forbidden. If a link target doesn't exist, OMIT the link rather than write a 404.

    NO emojis (per global instructions). Plain text bullets only.
  </action>
  <verify>
    <automated>head -1 README.md | grep -E '^# Beava$' &amp;&amp; ! grep -E 'petrpan26/tally|petrpan26/beava' README.md &amp;&amp; ! grep -E 'tally fork' README.md &amp;&amp; ! grep -E 'docs/event-time' README.md &amp;&amp; ! grep -E 'GOVERNANCE\.md' README.md &amp;&amp; grep -c '60-second quickstart' README.md &amp;&amp; for link in $(grep -oE '\(docs/[a-zA-Z0-9_/.-]+\)' README.md | tr -d '()'); do test -f "$link" || (echo "BROKEN: $link" &amp;&amp; exit 1); done</automated>
  </verify>
  <done>README.md rewritten; no stale references; all docs/ links resolve to existing files; 60-second quickstart present; no emojis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → first impression | Stale claims (event-time, `tally fork`) erode trust; broken links surface as 404s in GitHub UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.7.6-15-01 | R (Repudiation) | factually-stale README claims | mitigate | stale-refs inventory + per-claim verification before write |
| T-13.7.6-15-02 | I (Info Disclosure) | broken docs/ links surfacing 404 in GitHub | mitigate | per-link `test -f` probe in verify step |
</threat_model>

<verification>
- README.md exists with expected sections
- All grep negatives pass (no stale refs)
- All docs/ links resolve to existing files
</verification>

<success_criteria>
README.md is professional, accurate to v0 surface, and ships with no broken links.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-15-SUMMARY.md`
</output>
