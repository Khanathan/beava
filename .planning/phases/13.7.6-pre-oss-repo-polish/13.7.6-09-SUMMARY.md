---
phase: 13.7.6
plan: 09
subsystem: workstream-D-history-rewrite
tags: [oss-prep, history-rewrite, filter-repo, rehearsal, mailmap, trailer-strip]
requires: [13.7.6-08]
provides: [filter-repo-procedure, mailmap-canonical, trailer-strip-callback, plan-11-go-signal]
affects: []
tech_stack_added: [git-filter-repo-2.47.0]
patterns_used: [destructive-on-sacrificial-clone, bytes-str-discipline, smoke-validation-pre-post-diff]
key_files_created:
  - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-09-mailmap.txt
  - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-09-trailer-strip.py
  - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-09-rehearsal-report.md
key_files_modified: []
decisions:
  - "Filter-repo invocation locked: --invert-paths --path .planning/ --path CLAUDE.md --path .claude/ --mailmap 13.7.6-09-mailmap.txt --commit-callback \"$(cat 13.7.6-09-trailer-strip.py)\" --force"
  - "Plan-08 extra-strip-paths: NONE (single gitleaks finding was a confirmed false positive in .planning/, already covered by D-01 base strip)"
  - "Trailer-strip uses Python callback BODY (not def) per filter-repo convention; bytes/str discipline enforced via explicit decode/encode pair"
  - "Smoke-validation strategy: capture pre-rewrite message of one Co-Authored-By commit, resolve old→new SHA via filter-repo's commit-map, diff post — proves trailer/marker stripped + subject/body preserved + email rewritten"
metrics:
  duration: ~25min wall-clock (Task 1 ~5min author + Task 2 ~20min rehearse+verify+report)
  completed_date: 2026-05-05
  pre_rewrite_commits: 2599
  post_rewrite_commits: 1545
  pre_rewrite_size_mb: 123
  post_rewrite_size_mb: 31
  verification_rows: 8/8 PASS
---

# Phase 13.7.6 Plan 09: Filter-repo dry-run rehearsal Summary

Rehearsed `git filter-repo` against a sacrificial bare clone at `/tmp/beava-rehearsal-bare.git`,
producing the three canonical artifacts (`mailmap.txt`, `trailer-strip.py`, `rehearsal-report.md`)
that Plan 11 will consume verbatim — all eight verification rows PASS, dev tree untouched, GO for
Plan 11.

## What was built

### 1. `13.7.6-09-mailmap.txt` (3 mapping rows + header comment)

git-filter-repo `--mailmap` input mapping every historical (name, email) tuple → canonical
`Hoang Phan <hoang@beava.dev>` (D-04). Three identities surfaced:

```
Hoang Phan <hoang@beava.dev> Hoang Phan <hoang.phan@viggle.ai>
Hoang Phan <hoang@beava.dev> Hoang Phan <phan.minhhoang2606@gmail.com>
Hoang Phan <hoang@beava.dev> petrpan26 <phan.minhhoang2606@gmail.com>
```

### 2. `13.7.6-09-trailer-strip.py` (callback body, ~30 lines)

`git filter-repo --commit-callback` Python BODY implementing D-02 surgical trailer-only strip.
Strips:
- `Co-Authored-By: ...` lines whose value mentions Claude / `noreply@anthropic.com`
- `🤖 Generated with [Claude Code]...` lines (and the bare-text variant)
- Orphaned trailing blank lines created by the above removals

Preserves every other line including `Co-Authored-By:` trailers that don't reference
Claude/Anthropic (T-13.7.6-09-03 mitigation: avoid corrupting legitimate co-authors). Bytes/str
discipline enforced: only one `commit.message.decode('utf-8') / .encode('utf-8')` pair, all
intermediate ops on `str`.

### 3. `13.7.6-09-rehearsal-report.md` (7 sections including verification table + smoke + rollback)

Verification artifact with:
- Pre-rewrite metrics (2 599 commits, 123 MB, 874 Co-Authored-By lines, 1 198 commits touching `.planning/`, etc.)
- Post-rewrite metrics (1 545 commits, 31 MB — 75% reduction, expected and desired)
- Full bash transcript Plan 11 will copy verbatim
- 8-row verification table — all PASS
- Trailer-strip smoke section with verbatim pre/post message dump + diff narrative
- Canonical filter-repo invocation block for Plan 11
- ROLLBACK procedure (dev tree never touched; Plan 11 belt-and-braces tag suggested)

## Verification (8/8 PASS — full table in `13.7.6-09-rehearsal-report.md`)

| Row | Check | Result |
|----:|-------|:------:|
| a | No `.planning/` in any commit tree | **PASS** (0) |
| b | No `CLAUDE.md` in any commit tree | **PASS** (0) |
| c | No `.claude/` in any commit tree | **PASS** (0) |
| d | No `Co-Authored-By: Claude` trailers | **PASS** (0) |
| e | No `🤖 Generated with` markers | **PASS** (0) |
| f | Every author email = `hoang@beava.dev` | **PASS** |
| g | Every committer email = `hoang@beava.dev` | **PASS** |
| h | Trailer-strip smoke (pre/post diff on real commit; subject preserved + trailer/marker stripped + email rewritten) | **PASS** (4/4 sub-checks) |

Sample commit `59007730…` → `15631609…` showed exactly two lines removed at the bottom of the
message (the `Co-Authored-By: Claude…` trailer + its orphaned blank-line separator); 33-line body
preserved verbatim including the literal token "Claude" appearing in narrative prose ("rewrote
from Claude Design handoff stub" — preserved per D-02 rationale).

## Deviations from Plan

### Documented adjustments

**1. [Logistics] Plan-08 extra-strip-paths file does not exist on disk**

- **Found during:** Task 2 read-first checklist
- **Issue:** PLAN.md `<read_first>` references `.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-08-extra-strip-paths.txt`, but Plan 08 didn't produce it (gitleaks finding was a confirmed false positive in `.planning/` already covered by D-01 base strip; per `13.7.6-08-SUMMARY.md` "extra-strip-paths file: empty").
- **Fix:** Treated EXTRA_PATHS as empty array, documented in rehearsal report's "Inputs" section + step-4 comment of the canonical procedure block. Plan 11 picks this up identically (no `--path` args beyond `.planning/`, `CLAUDE.md`, `.claude/`).
- **Files modified:** N/A (procedural decision baked into rehearsal-report.md)
- **Commit:** captured in this plan's single chore: commit

**2. [Logistics] Plan filenames are `13.7.6-09-*` not `mailmap.txt` / `trailer-strip.py` / `rehearsal-report.md`**

- **Found during:** Task 1 / Task 2
- **Issue:** Operator-message used short names (`mailmap.txt`, `trailer-strip.py`, `rehearsal-report.md`); PLAN.md frontmatter `files_modified` uses long names (`13.7.6-09-mailmap.txt`, `13.7.6-09-trailer-strip.py`, `13.7.6-09-rehearsal-report.md`).
- **Fix:** PLAN.md is the canonical source per project convention (artifact files prefixed with plan number for traceability); used long names. Plan 11 references long names in its read_first.
- **Files modified:** N/A
- **Commit:** captured in this plan's single chore: commit

No other deviations. No Rule-1/Rule-2/Rule-3 fixes required (rehearsal completed cleanly on
first attempt).

## Authentication gates

None — all operations local. `git filter-repo` already on PATH at `/opt/homebrew/bin/git-filter-repo`
(version `a40bce548d2c`, equivalent to upstream 2.47.x).

## Key Decisions

1. **Filter-repo invocation locked** for Plan 11 (`--invert-paths --path .planning/ --path CLAUDE.md --path .claude/ --mailmap 13.7.6-09-mailmap.txt --commit-callback "$(cat 13.7.6-09-trailer-strip.py)" --force`). No additional path strips.

2. **Mailmap captures all three historical identities** (`Hoang Phan <hoang.phan@viggle.ai>`, `Hoang Phan <phan.minhhoang2606@gmail.com>`, `petrpan26 <phan.minhhoang2606@gmail.com>`). Confirmed at rehearsal time; if a future commit lands a 4th identity before Plan 11 runs, the mailmap must be re-extended.

3. **Trailer-strip predicate is conservative** (`startswith('co-authored-by:') AND ('claude' in lower OR 'noreply@anthropic.com' in lower)`). Co-Authored-By trailers from human collaborators (none in current history) survive — T-13.7.6-09-03 mitigation.

4. **Rehearsal clone size drop is expected** (2 599 → 1 545 commits, 123 → 31 MB). 1 054 commits prune entirely because their full tree was nothing-but-`.planning/` / `CLAUDE.md` / `.claude/` content (planning artifacts, CLAUDE.md updates, etc.). This is desired: those commits exist solely to author private business reasoning that the public history must not carry.

## Plan-11 readiness

- [x] Mailmap canonicalized
- [x] Trailer-strip callback canonicalized + syntax-validated
- [x] Rehearsal procedure proven mechanically sound on a 2 600-commit / 123 MB workload
- [x] Smoke validation proves trailer strip + subject preservation + email rewrite on a real commit
- [x] Canonical filter-repo invocation captured in `13.7.6-09-rehearsal-report.md` § "Filter-repo invocation flags"
- [x] Rollback procedure documented
- [x] Sacrificial clone removed
- [x] Dev tree HEAD verified unchanged (`791011836a234…`)

**Plan 11: GO.**

## Self-Check: PASSED

- `13.7.6-09-mailmap.txt` — found, contains `hoang@beava.dev` (5 occurrences)
- `13.7.6-09-trailer-strip.py` — found, contains `Co-Authored-By` (3 occurrences) + `commit_callback` ref + `decode.*utf-8` + `encode.*utf-8`; Python AST-parseable when wrapped in `def commit_callback(commit, _meta):`
- `13.7.6-09-rehearsal-report.md` — found, 16 PASS occurrences (≥8 required), `## Filter-repo invocation flags` section present, `## Trailer-strip smoke validation` section present, `Rollback` section present
- Sacrificial clone `/tmp/beava-rehearsal-bare.git` confirmed absent
- Dev tree HEAD unchanged from pre-rehearsal state
