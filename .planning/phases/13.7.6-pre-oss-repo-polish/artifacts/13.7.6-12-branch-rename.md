# Phase 13.7.6-12 — Branch rename + branch prune report

**Plan:** 13.7.6-12 (Workstream-D, Wave 4 — branch hygiene on rewritten bare clone)
**Run date:** 2026-05-06
**Operator:** Hoang Phan
**Result:** **PASS.** Rewritten bare clone now has exactly one branch (`main`); HEAD ref = `refs/heads/main`. Architectural-invariant tests verified present.

---

## HEAD ref state

```
$ git -C /tmp/beava-public-bare.git symbolic-ref HEAD
refs/heads/main

$ git -C /tmp/beava-public-bare.git log -1 --format='%h %s'
bef1725 docs(13.8): PyPI deferred to v0.0.x — Python SDK ships via git-install for v0.0.0
```

The post-rewrite commit hash `bef1725` corresponds to what was `35069986` in the dev tree (filter-repo recomputed all commit hashes due to mailmap rewrite). Same tree content; different SHA.

## Branch operations

| Step | Action | Result |
|------|--------|--------|
| 1 | `git branch -D main` (delete stale pre-v2 main; was `5cde1bc fix(bench): run_bench.sh honors OUTPUT_DIR env var`) | OK |
| 2 | `git branch -m v2/greenfield main` (rename) | OK |
| 3 | `git symbolic-ref HEAD refs/heads/main` (update HEAD) | OK |
| 4 | Delete all other branches (12 topic + 55 worktree-agent) | OK — 67 branches deleted |

## Final branch state

```
$ git -C /tmp/beava-public-bare.git branch
* main
```

Exactly one branch. No stale topic branches, no orphaned worktree-agent-* branches.

## Architectural-invariant tests — presence check

The filter-repo path-strip removed `.planning/` + `CLAUDE.md` + `.claude/` but MUST NOT have touched the architectural-test tripwires. Verified by `git log` against the rewritten tree:

| Test file | Latest commit (post-rewrite) | Status |
|-----------|------------------------------|:------:|
| `crates/beava-server/tests/phase12_6_mio_only_dataplane.rs` | `061ba7a06e6dd92a82cc7dfe303aa1f913eddd55` | ✓ PRESENT |
| `crates/beava-server/tests/phase12_7_no_table_surface.rs` | `9e883e6db95b4c33486fe74f91aed1154ce01a11` | ✓ PRESENT |
| `crates/beava-server/tests/phase12_8_lifetime_ops_have_bounds.rs` | `fe7ac5272e54c630a3204fd437e576bbe1480a77` | ✓ PRESENT |
| `crates/beava-core/tests/per_entity_size_dump.rs` (AggOp 80B-cap) | `1ac9fd6fe117ca8044d744bf0ec367b64d6c244f` | ✓ PRESENT |

All 4 architectural-invariant tests survived the rewrite. The mio-only / events-only / memory-governance / AggOp-size-cap CI tripwires are intact in the public history.

## Closes

- D-08 Workstream-D-13: branch rename to `main` completed
- HEAD ref now points to `main` (matches GitHub default-branch expectation)
- Branch surface ready for force-push: exactly one branch, sole author/committer = `hoang@beava.dev`
