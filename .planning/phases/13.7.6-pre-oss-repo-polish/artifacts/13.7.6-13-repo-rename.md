# Phase 13.7.6-13 — Repo rename + force-push report

**Plan:** 13.7.6-13 (Workstream-D, Wave 5 — destructive force-push to public repo)
**Run date:** 2026-05-06
**Operator:** Hoang Phan
**Result:** **PASS.** Rewritten history force-pushed to `github.com/beava-dev/beava`. Default branch = `main`. Sole author/committer = `hoang@beava.dev`.

---

## User-precondition checkpoint

| Precondition | Status |
|--------------|:------:|
| `beava-dev` GitHub org exists | ✅ verified via `gh api "orgs/beava-dev"` |
| `hoang@beava.dev` email verified on user's GitHub account | ✅ user confirmed before plan dispatch |
| `beava-dev/beava` repo created on GitHub | ✅ existed pre-push (1 initial empty commit, 5 KB) |
| Default branch already configured to `main` | ✅ via `gh api "repos/beava-dev/beava"` |

## Pre-receive hook surprise (W2-warning fix)

First force-push attempt **rejected by GitHub pre-receive hook**:

```
remote: error: File scripts/demo-sendo/events.jsonl is 125.07 MB; this exceeds GitHub's file size limit of 100.00 MB
remote: error: GH001: Large files detected. You may want to try Git Large File Storage
 ! [remote rejected] main -> main (pre-receive hook declined)
```

This was NOT caught by Plan 09 rehearsal (rehearsal only ran filter-repo + 7 verification greps; didn't actually push to GitHub). The 125 MB `events.jsonl` blob lived in 2 history commits (`78a3a24a` greenfield reset added it; `6a222163` "remove big" removed it from working tree but it stayed in history).

**Fix applied:** ran `git filter-repo --path scripts/demo-sendo/events.jsonl --invert-paths --force` on the bare clone. This stripped just that one file across all of history, recomputing all commit hashes. Spot-check verification rows still PASS (Co-Authored-By trailers = 0; sole author = `hoang@beava.dev`; .planning/ commits = 0).

| Metric | Before second filter | After second filter |
|--------|---------------------:|---------------------:|
| Total commits | 1,577 | 1,502 (filter pruned 75 commits whose entire content was events.jsonl mutations) |
| Size | 32 MB | 18 MB |
| Tip commit | `bef1725` | `da1d6c7` (recomputed) |

**v0.0.x backlog:** consider stripping other dev-only artifacts that lived in earlier history but aren't part of v0 codebase (`tally.snapshot.base.*`, `benchmark/tally-throughput/tally.snapshot.base.*`). All under 2 MB so non-blocking.

## Force-push procedure

```bash
PUBLIC_BARE="/tmp/beava-public-bare.git"
cd "$PUBLIC_BARE"
git remote add origin "https://github.com/beava-dev/beava.git"
git push --force origin main:main
```

**Result:**

```
To https://github.com/beava-dev/beava.git
 + 5ea2135...da1d6c7 main -> main (forced update)
```

The previous tip `5ea2135` (the initial empty commit GitHub auto-created when the repo was claimed) was overwritten by `da1d6c7` (the post-second-filter HEAD). Plus 1,501 ancestor commits.

## Remote-URL verification

```
$ gh api "repos/beava-dev/beava"
  full_name:  beava-dev/beava
  default:    main
  private:    true
  size_kb:    5  (lazy; will sync to ~18 MB)
  pushed_at:  2026-05-06T16:31:15Z

$ gh api "repos/beava-dev/beava/branches"
  main  (only branch)

$ gh api "repos/beava-dev/beava/commits?per_page=5" — author emails:
  da1d6c7f | hoang@beava.dev | docs(13.8): PyPI deferred to v0.0.x ...
  f90acd73 | hoang@beava.dev | docs(13.7.6-15): rewrite README to match v0 locked surface
  a7199385 | hoang@beava.dev | chore(13.7.6-01): audit allow-attrs in beava-server
  5bc47a90 | hoang@beava.dev | chore(13.7.6-01): tighten beava-core expr.rs reason comments
  02c3bc41 | hoang@beava.dev | chore(13.7.6-01): audit allow-attrs in beava-core
```

All authored by `hoang@beava.dev` — mailmap rewrite confirmed on the GitHub side.

## Repo visibility

The remote is currently **private**. v0.0.0 OSS launch requires flipping to **public** before any external announcement. Recommended sequence:

1. (now) Phase 13.7.6 closes; Phase 13.8 packaging starts
2. (Phase 13.8 mid-execution) test docker / brew / curl install paths against the private repo (GitHub Actions on private repos still works for free with public repo's worth of minutes)
3. (Phase 13.8 closure / v0.0.0 tag time) flip repo to public via `gh repo edit beava-dev/beava --visibility public`
4. Soft-launch announcement at T+7-14d after v0.0.0 tag

## Closes

- D-08 Workstream-D-14: force-push completed
- D-03: rewritten public history at `github.com/beava-dev/beava`
- Plan 11 backup bundle (`/tmp/beava-pre-rewrite-bundle.bundle`) preserved as recovery option
- Plan 12 single-branch state pushed to remote: only `main` exists on GitHub
- Phase 13.8 (packaging + GA tag) UNBLOCKED

## State after Plan 13

- **Public repo:** `https://github.com/beava-dev/beava` (private until announcement; default branch `main`; tip `da1d6c7f`)
- **Local rewritten bare clone:** `/tmp/beava-public-bare.git` (18 MB, 1,502 commits) — kept for forensic reference; safe to delete after Phase 13.8 closure
- **Backup bundle:** `/tmp/beava-pre-rewrite-bundle.bundle` (45 MB) — keep until v0.0.0 tag is cut and verified
- **Dev tree:** `/Users/petrpan26/work/tally` — UNCHANGED on `v2/greenfield`; HEAD `35069986`; private workflow continues unaffected

## Pre-launch follow-ups (operator action)

- [ ] (one-time) Set `beava-dev/beava` repo description on GitHub
- [ ] (one-time) Set `beava-dev/beava` repo topics: `feature-store`, `streaming`, `real-time`, `rust`, `python-sdk`, `redis`, `fraud-detection`
- [ ] (one-time) Set `beava-dev/beava` repo homepage URL: `https://beava.dev`
- [ ] (one-time) Configure branch protection on `main` (require PR review + CI green for non-maintainer pushes)
- [ ] (Phase 13.8) flip repo to public visibility before v0.0.0 announcement
