# 13.7.6-19 — CHANGELOG.md Synthesis Worksheet

Internal-only working document. Maps `.planning/`-tracked phase deliverables to the
single user-visible bullets that land in the public `CHANGELOG.md` v0.0.0 entry.
Not committed to public history per Phase 13.7.6 D-01 (path strip).

Per CONTEXT D-08-Workstream-E-20 + Plan 13.7.6-19 §Task 1.

| Category | CHANGELOG line (terse, user-facing) | Source phases (internal — DO NOT publish) |
|----------|--------------------------------------|--------------------------------------------|
| Added | Single-binary real-time feature server with HTTP/1.1 + framed-TCP wire transports; in-memory state, WAL + periodic snapshot durability, sub-millisecond batch-get. | Phases 1, 2, 2.5, 6, 6.1, 7, 12, 12.6 |
| Added | 54 aggregation operators across 7 families (core / sketch / point-ordinal / recency / decay / velocity / buffer-geo). | Phases 5, 8, 9, 10, 11 |
| Added | Python SDK with declarative `@bv.event` decorator API and pipeline DSL — `events.with_columns(...).group_by(K).agg(...)`. | Phases 3, 4, 13.5, 13.5.2 |
| Added | `@bv.table` derivation outputs (aggregation-output only; no upsert/delete/retract). | Phase 13.0 ADR-001, 13.4, 13.5.2 |
| Added | TypeScript SDK (`@beava/sdk`) — communicate-only (push + register + get + batch-get). | Phase 13.6 |
| Added | Go SDK (`github.com/beava-dev/beava/sdk/go`) — communicate-only. | Phase 13.6 |
| Added | Schema evolution flags: `force=True` (destructive register with structured diff) + `dry_run=True` (preview without applying). | Phase 13.4 D-01 |
| Added | Three demo workloads bundled with the Python SDK: `bv.demo("adtech")`, `bv.demo("fraud")`, `bv.demo("ecommerce")`. | Phase 13.5 Plans 06, 09 |
| Added | `beava bench` CLI with four modes: throughput / mixed / memory / fsync. | Phase 13.5 Plan 08 |
| Added | Global aggregations (no `key=` form) and `bv.lit(value)` literal expression. | Phase 13.0 ADR-003 |
| Added | Cold-entity TTL — opt-in lazy eviction via `@bv.event(cold_after='<duration>')`. | Phase 12.8 D-01 |
| Added | Memory governance — every lifetime aggregation operator declares a finite per-entity ceiling at register time; unbounded ops rejected at the JSON-prelude shim. | Phase 12.8 D-02 |
| Added | In-memory persistence backend (`Persistence::Memory`) for ephemeral / test workloads. | Phase 13.4 D-02 |
| Added | `OP_RESET` (0x0040) opcode + `POST /reset` route, gated on test-mode (env / config / SDK kwarg). | Phase 13.4 D-03 |
| Added | Documentation site at `beava.dev` — quickstart, operator catalogue, wire spec, Python/TypeScript/Go SDK API reference, ADR archive. | Phase 13.7 |
| Architecture | Hand-rolled mio-only single-threaded data plane (Redis-shaped); horizontal scaling via multi-instance entity-key sharding. | Phase 12.6, 18, locked memory `project_no_sharded_apply` |
| Architecture | Apache-2.0 OSS license. | LICENSE |
| Removed | Tables: upsert / delete / retract API, MVCC `TemporalStore`, joins, unions, session windows, event-time / watermark semantics — descoped to v0.1+. | Phase 12.6, 12.7 |
| Removed | `event_time_ms` field on the wire — processing-time only. | Phase 12.6 D-03 |
| Security | OWASP Top-10 + LLM Top-10 review against the v0 attack surface. | Phase 13.7.6 Plan 06 |
| Security | ASVS L1 threat model — single-tenant; operator owns network isolation; no auth in v0. | Phase 13.7.6 Plan 07 |
| Security | `cargo audit` + `cargo deny` policy enforced at workspace root. | Phase 13.7.6 Plan 02 |
| Performance | ≥3M events/sec/core sustained on representative fraud workloads (single-thread mio data plane). | Phase 19.4 |
| Performance | P99 batch-get latency under 10 ms warm. | Phase 12 Plan 12-07 |
| Performance | Per-entity memory budget under 7 KB for a typical 30-feature pack (post AggOp boxing). | Phase 12.9 |

## Notes

- Date placeholder `<YYYY-MM-DD>` is intentional. Phase 13.8 closure plan replaces it with the actual GA tag-cut date.
- Cross-link to internal phase numbers / `.planning/` paths is FORBIDDEN in the published `CHANGELOG.md` (D-01 history strip + threat model T-13.7.6-19-01).
- Compare-URLs at the bottom of the public file follow the Keep-a-Changelog convention.
