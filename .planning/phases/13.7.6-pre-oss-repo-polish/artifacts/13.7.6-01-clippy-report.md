## clippy sweep summary

Phase 13.7.6 Plan 01 — Workstream-C-1 clippy `-D warnings` sweep + `#[allow(...)]` audit.
Executed against `v2/greenfield` HEAD `34244a92` on 2026-05-06.

## Pre-fix diagnostic count

`cargo clippy --workspace --all-targets --all-features -- -D warnings` exited 0 with **0 diagnostics** at the start of this plan. The pre-flight session (post Plan 13.7.6-32 verification, commit `bd7f3885`) had already brought the workspace to clippy-clean. The audit value of this plan is therefore the allow-attribute table below, not the lint count.

`cargo fmt --all --check` was also green at start.

## Post-fix diagnostic count

Same: **0 diagnostics**. The audit is doc-comment-only on the surviving `#[allow(...)]` attributes plus a single attempted-removal that clippy rejected (see disposition table). No behavioural code changed.

## Allow-attribute totals

| metric | pre-fix | post-fix |
|--------|--------:|---------:|
| `#[allow(...)]` sites under `crates/` | 61 | 61 |
| sites with adjacent `// reason: …` comment | 0* | 61 |
| sites without justification | 61 | 0 |

*A handful of sites carried multi-line doc-comments above them that explained the suppression in prose, but none had the grep-discoverable `// reason: …` marker.

## Allow-attribute disposition table

| file:line | attribute | category | reason summary |
|-----------|-----------|----------|----------------|
| `crates/beava-runtime-core/src/work_ring.rs:26` | `clippy::large_enum_variant` | KEEP | RingItem::Request is intentionally ~533 B; doc-comment above explains throughput-regime rationale. |
| `crates/beava-runtime-core/src/wal_buffer.rs:259` | `dead_code` (seal_threshold) | KEEP | Stored at construction as canonical record of high-water threshold; clippy errored on attempted removal. |
| `crates/beava-bench/src/workloads/mod.rs:59` | `dead_code` (PipelineConfig.name) | KEEP | Serde-shape parity with bench config files. |
| `crates/beava-bench/src/workloads/mod.rs:62` | `dead_code` (PipelineConfig.description) | KEEP | Serde-shape parity. |
| `crates/beava-bench/src/workloads/mod.rs:67` | `dead_code` (PipelineConfig.features) | KEEP | Serde-shape parity. |
| `crates/beava-bench/src/blast_shape.rs:42` | `dead_code` (PipelineConfig.description) | KEEP | Serde-shape parity. |
| `crates/beava-bench/src/harness/production.rs:841` | `clippy::too_many_arguments` (run_push_worker) | KEEP | Independent worker parameters; struct-bag refactor would obscure call site. |
| `crates/beava-bench/src/harness/production.rs:1032` | `clippy::too_many_arguments` (run_tcp_burst_push_worker) | KEEP | Same as run_push_worker. |
| `crates/beava-bench/src/harness/production.rs:1229` | `clippy::too_many_arguments` (run_tcp_continuous_push_worker) | KEEP | Same as run_push_worker. |
| `crates/beava-core/tests/entity_key_hybrid.rs:12` | `unused_imports` | KEEP | PayloadNode glob retained for symmetry with green-phase scenarios. |
| `crates/beava-core/benches/phase5_agg.rs:311` | `dead_code` (phase5_agg_benches mod) | KEEP | Red-commit contract constant referenced only by unit-test below. |
| `crates/beava-core/benches/phase4_expr.rs:202` | `dead_code` (phase4_expr_benches mod) | KEEP | Red-commit contract constant referenced only by unit-test below. |
| `crates/beava-core/src/agg_op.rs:177` | `clippy::derivable_impls` | KEEP | Explicit Default impl makes Count discoverable; #[derive(Default)] would shift on enum reordering. |
| `crates/beava-core/src/agg_op.rs:705` | `dead_code` (AggKind::is_core) | KEEP | Crate-public for serialization/debugging helpers per doc-comment. |
| `crates/beava-core/src/agg_op.rs:864` | `clippy::too_many_arguments` (update_with_extracted) | KEEP | Hot-path pre-extraction protocol; struct-bag would alloc per event. |
| `crates/beava-core/src/agg_op.rs:907` | `clippy::too_many_arguments` (update_with_extracted_no_where) | KEEP | Pre-where-evaluated sibling of update_with_extracted. |
| `crates/beava-core/src/agg_windowed.rs:233` | `clippy::too_many_arguments` (WindowedOp::update_at) | KEEP | Per-bucket hot-path dispatch; same rationale as agg_op.rs. |
| `crates/beava-core/src/agg_compile.rs:38` | `clippy::result_unit_err` (parse_duration_to_ms) | KEEP | `Result<_, ()>` is the SDK-AGG-06 contract — error variant carries no information by design. |
| `crates/beava-core/src/register_validate.rs:46` | `dead_code` (EventTimeFieldMissing) | KEEP | Wire-codec stability per Phase 12.7 events-only pivot. |
| `crates/beava-core/src/register_validate.rs:51` | `dead_code` (EventTimeFieldWrongType) | KEEP | Same as EventTimeFieldMissing. |
| `crates/beava-core/src/register_validate.rs:79` | `dead_code` (UnsupportedOpInPhase4) | KEEP | Variant reserved for future Rule 10 strictening. |
| `crates/beava-core/src/register_validate.rs:96` | `dead_code` (AggregationDuplicateFeatureName) | KEEP | Reserved for future Vec-based deserialization. |
| `crates/beava-core/src/register_validate.rs:678` | `clippy::type_complexity` (into_parts) | KEEP | 4-tuple is the documented decomposition shape; wrapper struct would obscure call-site destructure pattern. |
| `crates/beava-core/src/registry_diff.rs:200` | `dead_code` (EventTimeFieldMismatch) | KEEP | Wire-codec stability per Phase 12.7 events-only pivot. |
| `crates/beava-core/src/expr.rs:1100` | `dead_code` (field) | KEEP | AST-construction scaffolding for future red-phase tests. |
| `crates/beava-core/src/expr.rs:1108` | `dead_code` (lit_int) | KEEP | Same as field. |
| `crates/beava-core/src/expr.rs:1114` | `dead_code` (lit_float) | KEEP | Same as field. |
| `crates/beava-core/src/expr.rs:1120` | `dead_code` (lit_null) | KEEP | Same as field. |
| `crates/beava-core/src/expr.rs:1126` | `dead_code` (lit_bool) | KEEP | Same as field. |
| `crates/beava-core/src/expr.rs:1132` | `dead_code` (lit_str) | KEEP | Same as field. |
| `crates/beava-core/src/expr.rs:1138` | `dead_code` (binop) | KEEP | Same as field. |
| `crates/beava-core/src/expr.rs:1148` | `dead_code` (unaryop) | KEEP | Same as field. |
| `crates/beava-core/src/expr.rs:1158` | `dead_code` (call) | KEEP | Same as field. |
| `crates/beava-core/src/agg_apply.rs:64` | `dead_code` (extracted_build_count_load) | KEEP | Test-only counter accessor on `cfg(test)`. |
| `crates/beava-core/src/agg_apply.rs:71` | `dead_code` (extracted_build_count_store) | KEEP | Test-only counter accessor on `cfg(test)`. |
| `crates/beava-core/src/sketches/count_distinct.rs:62` | `private_interfaces` | KEEP | NoOpHasher intentionally module-private but reachable via HashSetU64 type alias. |
| `crates/beava-server/tests/phase18_05_continuous_workers_test.rs:24` | `clippy::type_complexity` (spawn_n_workers_with_write) | KEEP | Test helper; 3-tuple mirrors production callers. |
| `crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs:215` | `clippy::type_complexity` (ACTIVE_SINK) | KEEP | Test-only nested-Mutex sink; layered Option<Arc<Mutex<…>>> shape lets each test install/clear its buffer. |
| `crates/beava-server/benches/phase25_wire.rs:109` | `dead_code` (phase25_wire_benches mod) | KEEP | Red-commit contract constant. |
| `crates/beava-server/benches/phase25_wire.rs:118` | `unused_imports` | KEEP | Glob import covers symbols referenced via fully-qualified paths inside individual tests. |
| `crates/beava-server/src/recovery.rs:121` | `dead_code` (WalEventPayload.v) | KEEP | Serde-shape parity with WAL Event record format. |
| `crates/beava-server/src/recovery.rs:124` | `dead_code` (WalEventPayload.rv) | KEEP | Serde-shape parity. |
| `crates/beava-server/src/recovery.rs:135` | `dead_code` (V2Record.rv) | KEEP | Parsed from v=2 record header for completeness. |
| `crates/beava-server/src/register.rs:181` | `dead_code` (ValidationFailed.first_error_code) | KEEP | Recorded for future logging/metrics; v0 wire response is first-error-wins. |
| `crates/beava-server/src/register.rs:187` | `dead_code` (ValidationFailed.all_errors_count) | KEEP | Same as first_error_code. |
| `crates/beava-server/src/apply_shard.rs:32` | `dead_code` (wal_lsn) | KEEP | WalLsn handle retained for future WAL-coordinated dispatches. |
| `crates/beava-server/src/server.rs:443` | `clippy::too_many_arguments` (bind_with_state) | KEEP | TestServer construction surface; independently-meaningful production knobs. |
| `crates/beava-server/src/server.rs:479` | `clippy::too_many_arguments` (bind_with_state_and_overrides) | KEEP | Same as bind_with_state. |
| `crates/beava-server/src/server.rs:697` | `clippy::too_many_arguments` (build_runtime_state_with_persistence) | KEEP | Persistence-aware constructor; each parameter is an independently-meaningful subsystem knob. |
| `crates/beava-server/src/server.rs:1101` | `dead_code` (TOKEN_CLIENT_BASE) | KEEP | Legacy IoPool scaffolding cluster head. |
| `crates/beava-server/src/server.rs:1107` | `dead_code` (MAX_CONCURRENT_CLIENTS) | KEEP | Legacy IoPool scaffolding. |
| `crates/beava-server/src/server.rs:1112` | `dead_code` (MioClient) | KEEP | Legacy IoPool scaffolding. |
| `crates/beava-server/src/server.rs:1137` | `dead_code` (MioProto) | KEEP | Legacy IoPool scaffolding. |
| `crates/beava-server/src/server.rs:1188` | `dead_code` (iopool_observer::record_parse) | KEEP | Optional re-instrumentation point. |
| `crates/beava-server/src/server.rs:1948` | `dead_code` (ClientsPtr) | KEEP | Legacy IoPool scaffolding. |
| `crates/beava-server/src/server.rs:1957` | `dead_code` (ClientsPtr impl) | KEEP | Legacy IoPool scaffolding. |
| `crates/beava-server/src/server.rs:1971` | `dead_code` (MioClient impl) | KEEP | Legacy IoPool scaffolding. |
| `crates/beava-server/src/server.rs:1984` | `dead_code` (accept_clients) | KEEP | Legacy IoPool scaffolding. |
| `crates/beava-server/src/server.rs:2066` | `dead_code` (read_and_parse_client_to_channel) | KEEP | Legacy IoPool scaffolding. |
| `crates/beava-server/src/server.rs:2208` | `dead_code` (drain_channel_until_workers_done) | KEEP | Legacy IoPool scaffolding. |
| `crates/beava-server/src/server.rs:2307` | `dead_code` (serialize_and_write_client) | KEEP | Legacy IoPool scaffolding. |

**Totals:**

| disposition | count |
|-------------|------:|
| KEEP + add reason | 61 |
| REMOVE (stale) | 0 |
| ADD tracking-issue link | 0 |
| **TOTAL** | **61** |

## Removal attempts

One attempted removal during the audit:

- **`crates/beava-runtime-core/src/wal_buffer.rs:255` `#[allow(dead_code)]` on `seal_threshold`** — removed first to test pitfall #3 (stale post-refactor). `cargo clippy -p beava-runtime-core --all-targets --all-features -- -D warnings` reported `error: field is never used: seal_threshold`. Restored attribute and added reason comment.
- **`crates/beava-core/src/expr.rs` 9 test-helper attrs** (field/lit_int/lit_float/lit_null/lit_bool/lit_str/binop/unaryop/call) — removed all 9 to test the same pitfall. Clippy reported all 9 as genuinely unused. Restored as scaffolding for future red-phase tests in this module per the doc-comment above the helpers.

No allow attributes were found stale; all 61 are load-bearing.

## Architectural-tripwire allows

None of the 61 sites are inside the architectural-tripwire test files (`phase12_6_*`, `phase12_7_*`, `phase12_8_*`). Pitfall #6 (load-bearing for invariant grep walks) is non-applicable for this audit.

## Workspace gate verdicts (post-audit)

- `cargo clippy --workspace --all-targets --all-features -- -D warnings` — **GREEN** (exit 0, 0 diagnostics)
- `cargo fmt --all --check` — **GREEN** (exit 0)
- `cargo test --workspace --features testing --no-run` — **GREEN** (compiles)
- per-file `// reason:` adjacency check via awk (5-line lookback window) — **GREEN** (0 sites missing)

## Commits landed

| commit | scope |
|--------|-------|
| `b8dfa766` | beava-runtime-core (2 attrs) |
| `901c5cb4` | beava-bench (5 attrs) |
| `4ad8c3a5` | beava-core (22 attrs) |
| `d328b870` | beava-core expr.rs reason tighten-up |
| `27e66747` | beava-server (25 attrs) |
