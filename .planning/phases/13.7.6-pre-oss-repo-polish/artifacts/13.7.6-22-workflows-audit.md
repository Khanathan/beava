# Plan 13.7.6-22 — `.github/workflows/` audit

**Date:** 2026-05-05
**Plan:** 13.7.6-22 (Workstream-E-23 — pre-OSS repo polish, GitHub Actions workflow audit)
**Scope:** Audit + correct every YAML under `.github/workflows/` for public-launch readiness. Verify (a) repo URL references resolve to `beava-dev/beava`, (b) `${{ secrets.* }}` references are documented for the launch-checklist, (c) no hardcoded private internal URLs leak, (d) third-party action `uses:` lines are pinned (SHA or major-version tag), (e) `ci.yml` test commands match the CLAUDE.md TDD-§ canonical, (f) explicit `permissions:` blocks are present (default-deny stance per OWASP Hardening guide).

**Context:** Workspace lives at HEAD `3631f699` (worktree `agent-ab67c1a003c87d4a2`); base resolved per the executor STEP-0 directive. The plan PLAN.md document expected 5 workflows; reality is **6** — `homebrew-bump.yml` was added in commit `6e2240c8 feat(13.8): add homebrew-bump workflow for auto-formula updates` between plan write and execution. All 6 are audited below.

---

## Per-workflow audit table

## ci.yml (main test/lint pipeline on `push: [main]` + `pull_request: [main]`)

LOC at HEAD `3631f699`: 91. Triggers: `push: branches: [main]` + `pull_request: branches: [main]`. Concurrency-cancel on same ref. `RUSTFLAGS: "-D warnings"` workspace-wide.

#### URL references

| Line | Reference | Disposition |
|------|-----------|-------------|
| —    | (none — file has no repo-URL strings) | OK |

#### Secrets used

| Secret | Disposition |
|--------|-------------|
| (none) | OK — `ci.yml` runs entirely from PR-supplied source + cached toolchains; no secrets needed. |

#### Hardcoded private URLs

| Line | URL | Disposition |
|------|-----|-------------|
| —    | (none — file references only public action repos and test commands) | OK |

#### Third-party action pins

| Line | Action | Pin | Known-good? |
|------|--------|-----|-------------|
| 24, 35, 47, 64 | `actions/checkout@v4` | major-version tag | YES — official GitHub action; v4 stable since 2023-09. |
| 25, 36, 48, 68 | `dtolnay/rust-toolchain@stable` | branch tag | YES — `stable` is the curated release-channel pointer; this action is the de-facto standard for Rust toolchain installs in CI. |
| 39, 49, 69 | `Swatinem/rust-cache@v2` | major-version tag | YES — community-maintained Rust cache action; stable v2 line. |
| 50 | `taiki-e/install-action@v2` | major-version tag | YES — taiki-e/install-action is widely used to fetch `cargo-nextest`; v2 stable. |
| 77 | `actions/setup-python@v5` | major-version tag | YES — official GitHub action. |

All actions are on major-version tags rather than commit SHAs. CONTEXT D-08 Workstream-E-23 accepts both ("SHA pin or major-version-tag"); current pins are acceptable. Future-proof: SHA-pin third-party (non-`actions/*`) actions if a supply-chain incident emerges.

#### Command parity (CLAUDE.md TDD §)

| Line | Command (before) | Canonical | Match? | Disposition |
|------|------------------|-----------|--------|-------------|
| 28 | `cargo fmt --all --check` | `cargo fmt --all --check` | YES | OK |
| 40 | `cargo clippy --all-targets --all-features -- -D warnings` | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | NO — missing `--workspace` | **FIX**: add `--workspace` (in this plan) |
| 53 | `cargo nextest run --all-features --no-fail-fast` | `cargo test --workspace --features testing` | EQUIV — `nextest` is a parallel drop-in for `cargo test`, and `--all-features` is a strict superset of `--features testing` | OK (nextest is the repo's chosen runner; superset feature set covers `testing`) |

#### Permissions block

Workflow had **no explicit `permissions:` block** — defaults to GitHub-org/repo default which is overly permissive on legacy repos. **FIX**: add workflow-level `permissions: contents: read` so the `GITHUB_TOKEN` is read-only by default; jobs that need write can elevate (none in `ci.yml`).

#### Scope OK for public

YES (after fixes applied). `ci.yml` exercises the canonical Rust + Python gate set on every PR; no secrets; no internal URLs. After the `--workspace` clippy fix and `permissions` add, file is public-ready.

---

## pr.yml (PR-only checks; in addition to `ci.yml`)

LOC at HEAD `3631f699`: 132. Triggers: `pull_request: branches: [main]`. Concurrency-cancel per PR number. `RUSTFLAGS: "-D warnings"` workspace-wide.

#### URL references

| Line | Reference | Disposition |
|------|-----------|-------------|
| —    | (none) | OK |

#### Secrets used

| Secret | Disposition |
|--------|-------------|
| (none) | OK |

#### Hardcoded private URLs

| Line | URL | Disposition |
|------|-----|-------------|
| —    | (none) | OK |

#### Third-party action pins

| Line | Action | Pin | Known-good? |
|------|--------|-----|-------------|
| 21, 67, 93 | `actions/checkout@v4` | major-version | YES |
| 23, 69, 95 | `dtolnay/rust-toolchain@stable` | branch | YES |
| 27, 71, 97 | `actions/cache@v4` | major-version | YES — official; `actions/cache@v4` (replaces deprecated v3 mid-2025). |
| 110 | `actions/setup-python@v5` | major-version | YES |

#### Command parity (CLAUDE.md TDD §)

| Line | Command (before) | Canonical | Match? | Disposition |
|------|------------------|-----------|--------|-------------|
| 38 | `cargo fmt --check` | `cargo fmt --all --check` | NO — missing `--all` | **FIX** |
| 41 | `cargo clippy --all-targets -- -D warnings` | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | NO — missing `--workspace` and `--all-features` | **FIX** |
| 46 | `cargo test -- --test-threads=1` | `cargo test --workspace --features testing` (with `--test-threads=1` retained for serialized integration tests) | NO — missing `--workspace`; missing feature gate. **Critical** — without `--all-features` (or `--features testing`), every `[[test]]` entry under `crates/beava-server/Cargo.toml` with `required-features = ["testing"]` (50+ tests) is silently skipped. | **FIX**: change to `cargo test --workspace --all-features -- --test-threads=1` |
| 82 | `PROPTEST_CASES=50 cargo test -p beava --test sharding_parity -- --nocapture` | (sharding-parity smoke is a focused gate not in TDD-§ canonical list) | OK — single-test invocation by design (PR gate per TPC-CORR-05 / Phase 52 D-14) | OK |

#### Permissions block

Workflow had **no explicit `permissions:` block**. **FIX**: add workflow-level `permissions: contents: read` (read-only).

#### Scope OK for public

YES (after fixes applied). PR-only checks; no secrets; no internal URLs. The test-feature gap is a real drift bug (silently skips `testing`-gated integration tests on every PR); fixing here aligns with CLAUDE.md TDD-§ canonical.

---

## docs.yml (docs build + GitHub Pages deploy)

LOC at HEAD `3631f699`: 49. Triggers: `push: branches: [main]` + `paths: [docs/**, mkdocs.yml]`. Concurrency: `pages` group, cancel-in-progress.

#### URL references

| Line | Reference | Disposition |
|------|-----------|-------------|
| —    | (none) | OK |

#### Secrets used

| Secret | Disposition |
|--------|-------------|
| (none — uses `id-token: write` for GitHub-Pages OIDC; no `${{ secrets.* }}` refs) | OK |

#### Hardcoded private URLs

| Line | URL | Disposition |
|------|-----|-------------|
| —    | (none) | OK |

#### Third-party action pins

| Line | Action | Pin | Known-good? |
|------|--------|-----|-------------|
| 24 | `actions/checkout@v4` | major-version | YES |
| 26 | `actions/setup-python@v5` | major-version | YES |
| 36 | `actions/upload-pages-artifact@v3` | major-version | YES — official |
| 48 | `actions/deploy-pages@v4` | major-version | YES — official; OIDC-aware |

#### Permissions block

Workflow already declares `permissions: { contents: read, pages: write, id-token: write }` (lines 11–14) — minimum-grant for the GitHub-Pages OIDC flow. OK; no change.

#### Scope OK for public

YES. No secrets, no internal URLs. The `id-token: write` is required by `actions/deploy-pages@v4` for the Pages-OIDC trust chain; this is correct.

---

## bench-nightly.yml (criterion bench runner — schedule + workflow_dispatch)

LOC at HEAD `3631f699`: 96. Triggers: `schedule: cron "0 2 * * *"` + `workflow_dispatch`. Three jobs: `bench-shard-scaffold`, `pareto-benchmark`, `sharding-parity-proptest`.

#### URL references

| Line | Reference | Disposition |
|------|-----------|-------------|
| —    | (none) | OK |

#### Secrets used

| Secret | Disposition |
|--------|-------------|
| (none) | OK |

#### Hardcoded private URLs

| Line | URL | Disposition |
|------|-----|-------------|
| —    | (none — criterion artifacts uploaded to GitHub Actions storage; no external endpoint) | OK |

#### Third-party action pins

| Line | Action | Pin | Known-good? |
|------|--------|-----|-------------|
| 26, 58, 85 | `actions/checkout@v4` | major-version | YES |
| 28, 60, 87 | `dtolnay/rust-toolchain@stable` | branch | YES |
| 30, 62, 89 | `Swatinem/rust-cache@v2` | major-version | YES |
| 42, 72 | `actions/upload-artifact@v4` | major-version | YES — `v4` is current (`v3` end-of-life 2024-11). |

#### Permissions block

Workflow had **no explicit `permissions:` block**. The bench job uploads artifacts (which works under default token); add `permissions: contents: read` workflow-level + per-job `permissions: { contents: read, actions: write }` is overkill — `upload-artifact@v4` works with `contents: read` alone. **FIX**: add workflow-level `permissions: contents: read`.

#### Scope OK for public

YES (after fix). Nightly off-peak run; no secrets. Public-readable artifacts (90-day retention) acceptable for a public benchmark history.

---

## release.yml (release tag → Linux x86_64 binary publish)

LOC at HEAD `3631f699`: 85. Triggers: `push: tags: ['v*']`. Concurrency: `release-${{ github.ref }}`, cancel-in-progress: false (sticky — release runs to completion).

#### URL references

| Line | Reference | Disposition |
|------|-----------|-------------|
| —    | (none — `softprops/action-gh-release` consumes `tag_name` from the local repo context, no URLs hardcoded) | OK |

#### Secrets used

| Secret | Disposition |
|--------|-------------|
| `GITHUB_TOKEN` (implicit, via `permissions: contents: write`) | OK — auto-provided by GitHub Actions; needed by `softprops/action-gh-release@v2` to create the release. |

#### Hardcoded private URLs

| Line | URL | Disposition |
|------|-----|-------------|
| —    | (none) | OK |

#### Third-party action pins

| Line | Action | Pin | Known-good? |
|------|--------|-----|-------------|
| 24 | `actions/checkout@v4` | major-version | YES |
| 26 | `dtolnay/rust-toolchain@stable` | branch | YES |
| 28 | `actions/cache@v4` | major-version | YES |
| 75 | `softprops/action-gh-release@v2` | major-version | YES — community-maintained, ~2k stars; stable v2 line. **Recommendation for Phase 13.8**: SHA-pin this one specifically (it has direct write access to releases). |

#### Command parity (CLAUDE.md TDD §)

| Line | Command (before) | Canonical | Match? | Disposition |
|------|------------------|-----------|--------|-------------|
| 39 | `cargo test -- --test-threads=1` | `cargo test --workspace --features testing` (with `--test-threads=1` retained) | NO — same drift as `pr.yml` line 46; silently skips `testing`-gated tests at release pre-flight. | **FIX**: change to `cargo test --workspace --all-features -- --test-threads=1`. Per plan must_haves, "release.yml is unchanged in scope here — Phase 13.8 extends to multi-arch wheels". The test-command parity is a quality-gate correction, not a scope extension; it brings the release pre-flight test set into line with what `ci.yml` and the canonical CLAUDE.md TDD-§ command runs, with no change to triggers, jobs, or matrix. Recorded as a deviation under "Audit-driven canonical-command alignment". |

#### Permissions block

Workflow declares `permissions: contents: write` workflow-level (line 15–16). Minimum-grant for `softprops/action-gh-release@v2` (which creates a release + uploads assets). OK; no change.

#### Scope OK for public

YES (after canonical-test-command fix). Phase 13.8 will extend this with multi-arch matrix (macOS/Windows runners); that's deferred. Current single-platform Linux x86_64 publish is correct as-is for v0.0.0 launch.

---

## homebrew-bump.yml (homebrew-tap formula bumper — added Phase 13.8)

LOC at HEAD `3631f699`: 174. Triggers: `push: tags: ['v*.*.*']` + `workflow_dispatch` (manual with `inputs.version`). One job. Note: this workflow was added in commit `6e2240c8` AFTER plan 13.7.6-22 was written; CONTEXT.md and PLAN.md both list 5 workflows. Audited regardless for completeness — public-launch readiness applies equally.

#### URL references

| Line | Reference | Disposition |
|------|-----------|-------------|
| 37, 52, 80, 125, 162, 170, 172 | `github.com/beava-dev/beava` + `github.com:beava-dev/homebrew-beava` | OK — already on the post-rename `beava-dev` org URL. |
| 83 | `git config user.email 'hoang@beava.dev'` | OK — matches D-04 author email. |

#### Secrets used

| Secret | Disposition |
|--------|-------------|
| `secrets.HOMEBREW_TAP_DEPLOY_KEY` (line 71) | **Launch-checklist**: must be added to GitHub Actions secrets at `github.com/beava-dev/beava/settings/secrets/actions` before first release. SSH deploy key with write access to `beava-dev/homebrew-beava`. Documented in Launch-checklist below. |

#### Hardcoded private URLs

| Line | URL | Disposition |
|------|-----|-------------|
| 76 | `ssh-keyscan -t ed25519,rsa github.com` | OK — public GitHub host-key scan. |
| 80 | `git@github.com:beava-dev/homebrew-beava.git` | OK — public tap repo. |

#### Third-party action pins

| Line | Action | Pin | Known-good? |
|------|--------|-----|-------------|
| 69 | `webfactory/ssh-agent@v0.9.0` | full minor-version tag | YES — well-known SSH-agent setup action. **Recommendation**: SHA-pin pre-launch since it gets the SSH private key as input. Tracked in Launch-checklist below. |

#### Permissions block

Workflow had **no explicit `permissions:` block**. The job pushes to an external repo over SSH (using a deploy key); the `GITHUB_TOKEN` is **not** used to write to the tap repo. **FIX**: add workflow-level `permissions: contents: read` (read-only against the source repo; the SSH key handles the cross-repo push).

#### Scope OK for public

YES (after `permissions: contents: read` add). Pre-launch checklist must include populating `HOMEBREW_TAP_DEPLOY_KEY`. Phase 13.8 owns the broader release-flow; this one was a stepping-stone added during 13.8 prep.

---

## Summary table — fixes applied in this plan

| Workflow             | Fix                                                                                                | Reason                                       |
|----------------------|----------------------------------------------------------------------------------------------------|----------------------------------------------|
| `ci.yml`             | Add `--workspace` to clippy line 40                                                                | Match CLAUDE.md TDD-§ canonical              |
| `ci.yml`             | Add `permissions: contents: read` workflow-level                                                   | OWASP / GitHub-Actions hardening default-deny |
| `pr.yml`             | `cargo fmt --check` → `cargo fmt --all --check`                                                    | Match TDD-§ canonical                        |
| `pr.yml`             | `cargo clippy --all-targets -- -D warnings` → add `--workspace --all-features`                     | Match TDD-§ canonical; close silent-skip gap |
| `pr.yml`             | `cargo test -- --test-threads=1` → `cargo test --workspace --all-features -- --test-threads=1`     | Close `required-features=["testing"]` silent-skip gap (50+ tests) |
| `pr.yml`             | Add `permissions: contents: read` workflow-level                                                   | Hardening default-deny                       |
| `release.yml`        | `cargo test -- --test-threads=1` → `cargo test --workspace --all-features -- --test-threads=1`     | Close `required-features=["testing"]` silent-skip gap at release pre-flight |
| `bench-nightly.yml`  | Add `permissions: contents: read` workflow-level                                                   | Hardening default-deny                       |
| `homebrew-bump.yml`  | Add `permissions: contents: read` workflow-level                                                   | Hardening default-deny                       |
| `docs.yml`           | (no change — already has minimum-grant `permissions:` block)                                       | OK as-is                                     |

## Out-of-scope items captured during audit

These are documented for the next plan or later phase; **NOT fixed in this commit**:

| Item                                                                            | File                                                | Owner                    | Severity |
|---------------------------------------------------------------------------------|-----------------------------------------------------|--------------------------|----------|
| `petrpan26/beava/discussions` URL                                               | `.github/ISSUE_TEMPLATE/config.yml:4`               | Plan 13.7.6-21 (issue templates audit) | low — link will 404 once `petrpan26/tally` is renamed; D-03 GitHub auto-redirect catches it. **Picked up here as a Rule-3 deviation** because Task 2's verify command (`! grep -rE 'petrpan26/' .github/`) requires zero matches across `.github/`, including ISSUE_TEMPLATE. Fixed in this commit despite plan `<files_modified>` listing only workflows. |
| `repository = "https://github.com/petrpan26/tally"`                             | `Cargo.toml:14` (workspace.package)                 | Phase 13 history-rewrite + repo rename (D-01/D-03) | low — not under `.github/`; not in this plan's scope. Will be swept by Plan 13.7.6-14 repo rename. |
| Third-party action SHA-pinning recommendation                                   | `.github/workflows/release.yml:75` (`softprops/action-gh-release@v2`); `.github/workflows/homebrew-bump.yml:69` (`webfactory/ssh-agent@v0.9.0`) | Phase 13.8 release-pipeline hardening | medium — these have write access (release publish; SSH-key load). Pin to commit SHA pre-tag-v0.0.0. **DEFER-13.8.** |
| Multi-arch matrix (macOS arm64/x86_64; Linux arm64; Windows)                    | `.github/workflows/release.yml`                     | Phase 13.8                              | required for full GA — single Linux x86_64 binary today. **DEFER-13.8.** |

---

## Launch-checklist secrets

Before tagging `v0.0.0` and triggering Phase 13.8 GA, the following **GitHub Actions secrets** must be configured at `https://github.com/beava-dev/beava/settings/secrets/actions`:

| Secret                          | Used by                       | Source                                                                                                 |
|---------------------------------|-------------------------------|--------------------------------------------------------------------------------------------------------|
| `HOMEBREW_TAP_DEPLOY_KEY`       | `homebrew-bump.yml` line 71   | SSH ed25519 private key with write access to `beava-dev/homebrew-beava` (public key is the deploy-key on the tap repo). |

**Anticipated (Phase 13.8 will add):**

| Secret                                 | Used by                  | Notes                                                                                              |
|----------------------------------------|--------------------------|----------------------------------------------------------------------------------------------------|
| `PYPI_API_TOKEN`                       | (future) `release.yml` PyPI publish job  | scoped to `beava` PyPI project; `__token__` username + `pypi-AgEI...` token. |
| `NPM_TOKEN`                            | (future) `release.yml` npm publish job   | `Automation` token for `@beava/*` org packages. |
| `DOCKERHUB_USERNAME` + `DOCKERHUB_TOKEN` | (future) Docker image publish | per-org access token, NOT account password. |
| `CARGO_REGISTRY_TOKEN`                 | (future) `release.yml` `cargo publish` job (if Beava ships to crates.io) | scoped publish-only token. |
| `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_ACCOUNT_ID` | (future) docs / website Pages deploy (Phase 13.7 deferred manual step)  | scope: Pages Edit. |

**Pre-launch checklist (D-08 Workstream-E-23 plus Phase 13.8 prep):**

1. Create + verify `beava-dev` GitHub org (D-03 / Plan 13.7.6-14 prerequisite).
2. Add + verify `hoang@beava.dev` on the GitHub account that owns `beava-dev/beava` (D-04 prerequisite — contributor-graph linking).
3. Generate `HOMEBREW_TAP_DEPLOY_KEY` (`ssh-keygen -t ed25519 -f homebrew_tap_deploy -C 'homebrew-beava deploy key'`); upload public key to `beava-dev/homebrew-beava` Settings → Deploy keys (write access ON); upload private key to `beava-dev/beava` Actions secrets as `HOMEBREW_TAP_DEPLOY_KEY`.
4. Phase 13.8 owns: PyPI / npm / Docker Hub / crates.io (if applicable) namespace claims + secret population.

---

## Verification (this plan)

Run after edits:

```bash
# Workflow YAML syntax check (pyyaml-based)
for f in .github/workflows/*.yml; do
  python3 -c "import yaml; yaml.safe_load(open('$f'))" || exit 1
done

# Plan-checker: zero petrpan26 refs anywhere under .github/
! grep -rE 'petrpan26/' .github/

# Plan-checker: ci.yml runs canonical clippy
grep -E 'cargo clippy --workspace --all-targets --all-features -- -D warnings' .github/workflows/ci.yml

# Plan-checker: pr.yml runs canonical test/clippy/fmt
grep -E 'cargo fmt --all --check'                                              .github/workflows/pr.yml
grep -E 'cargo clippy --workspace --all-targets --all-features -- -D warnings' .github/workflows/pr.yml
grep -E 'cargo test --workspace --all-features -- --test-threads=1'            .github/workflows/pr.yml

# Plan-checker: release.yml pre-flight test runs canonical (closes testing-feature silent-skip)
grep -E 'cargo test --workspace --all-features -- --test-threads=1' .github/workflows/release.yml

# Plan-checker: every workflow declares an explicit permissions: block (default-deny stance)
for f in .github/workflows/*.yml; do
  grep -q '^permissions:' "$f" || { echo "MISSING permissions: $f"; exit 1; }
done
```

All gates above must pass. Failures bounce the plan.
