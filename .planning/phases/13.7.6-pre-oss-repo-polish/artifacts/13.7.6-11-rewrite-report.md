# Phase 13.7.6-11 — `git filter-repo` apply report

**Plan:** 13.7.6-11 (Workstream-D, Wave 3 — destructive apply)
**Run date:** 2026-05-06
**Operator:** Hoang Phan
**Result:** **PASS.** All 7 verification rows GREEN. Rewritten bare clone ready at `/tmp/beava-public-bare.git`. Dev tree at `/Users/petrpan26/work/tally` UNTOUCHED.

---

## Inputs (carried verbatim from Plan 09 rehearsal)

| Artifact | Path | SHA-256 | Status |
|----------|------|---------|--------|
| Mailmap | `.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-09-mailmap.txt` | `19b1a47d7e93b5a2547e5bf9f339a2bf2b9fba7007b2a8cde69d2ae6d822475c` | Single-commit history (`6a18f42e` Plan 09 commit); no drift since rehearsal — anchor check PASS by construction |
| Trailer-strip | `.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-09-trailer-strip.py` | `7252b85517d7ee84880f575a06bb83c97f4024954fbfc593213e99bfdc8d9bbd` | Single-commit history (`6a18f42e`); no drift — anchor check PASS |
| filter-repo binary | `/opt/homebrew/bin/git-filter-repo` | (version `a40bce548d2c`) | Same version as rehearsal — anchor check PASS |

## Backup snapshot

`git bundle create --all` of dev tree captured at `/tmp/beava-pre-rewrite-bundle.bundle` (45 MB). All refs preserved. Recovery path: `git clone /tmp/beava-pre-rewrite-bundle.bundle <recovery-dir>`.

## Pre-rewrite metrics (sacrificial bare clone)

| Metric | Value | Vs rehearsal (2026-05-05) |
|--------|------:|---------------------------|
| Total commits | 2,644 | +45 (today's session: Plan 32, Tier 1 website, Plan 01 clippy audit, Plan 15 README, Phase 13.8 scope amendments, STATE/ROADMAP updates) |
| On-disk size (`du -sh`) | 126 MB | +3 MB (today's commits) |
| Distinct author emails | 2 (`hoang.phan@viggle.ai`, `phan.minhhoang2606@gmail.com`) | unchanged |
| Distinct committer emails | 2 (same) | unchanged |
| `Co-Authored-By: Claude` line count | 876 | +2 (likely from worktree-agent merge commits with embedded trailers) |
| `🤖 Generated with` line count | 1 | +1 |
| Commits touching `.planning/` | 1,216 | +18 |
| Commits touching `CLAUDE.md` | 11 | +1 |
| Commits touching `.claude/` | 3 | +1 |

## Post-rewrite metrics (after filter-repo)

| Metric | Value | Note |
|--------|------:|------|
| Total commits | 1,577 | Drop of 1,067 (40.4%) — filter-repo prunes commits whose entire tree was nothing-but-stripped paths. Matches rehearsal drop ratio (40.5%). |
| On-disk size (`du -sh`) | 32 MB | 75% reduction. Matches rehearsal (31 MB). |
| filter-repo exit code | 0 | Wall-clock 1.13s on Apple-M4 (parsed 2,644 commits in 0.34s, repacked in ~0.79s). |

## Procedure (verbatim — executed exactly per Plan 09 rehearsal)

```bash
REPO_ROOT="/Users/petrpan26/work/tally"
PUBLIC_BARE="/tmp/beava-public-bare.git"
MAILMAP_SRC="$REPO_ROOT/.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-09-mailmap.txt"
TRAILER_STRIP_SRC="$REPO_ROOT/.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-09-trailer-strip.py"

# Step 0 — backup
git -C "$REPO_ROOT" bundle create /tmp/beava-pre-rewrite-bundle.bundle --all

# Step 1 — fresh bare clone (sacrificial; not the dev tree)
rm -rf "$PUBLIC_BARE"
git clone --bare "$REPO_ROOT" "$PUBLIC_BARE"

# Step 2 — copy artifacts into the bare clone dir
cp "$MAILMAP_SRC"        "$PUBLIC_BARE/13.7.6-09-mailmap.txt"
cp "$TRAILER_STRIP_SRC"  "$PUBLIC_BARE/13.7.6-09-trailer-strip.py"

# Step 3 — run filter-repo
cd "$PUBLIC_BARE"
git filter-repo \
    --invert-paths \
    --path .planning/ \
    --path CLAUDE.md \
    --path .claude/ \
    --mailmap 13.7.6-09-mailmap.txt \
    --commit-callback "$(cat 13.7.6-09-trailer-strip.py)" \
    --force
```

## Rewrite verification

| Row | Check | Expected | Actual | Verdict |
|----:|-------|---------:|-------:|:-------:|
| a | No `.planning/` in any commit tree | 0 | 0 | **PASS** |
| b | No `CLAUDE.md` in any commit tree | 0 | 0 | **PASS** |
| c | No `.claude/` in any commit tree | 0 | 0 | **PASS** |
| d | No `Co-Authored-By: Claude` trailers | 0 | 0 | **PASS** |
| e | No `🤖 Generated with` markers | 0 | 0 | **PASS** |
| f | Every author email = `hoang@beava.dev` | `hoang@beava.dev` | `hoang@beava.dev` | **PASS** |
| g | Every committer email = `hoang@beava.dev` | `hoang@beava.dev` | `hoang@beava.dev` | **PASS** |

**Verdict: PASS — all 7 verification rows GREEN. Rewritten bare clone is ready for Plan 12 (branch rename) + Plan 13 (force-push).**

## State

- **Rewritten bare clone:** `/tmp/beava-public-bare.git` (32 MB, 1,577 commits, sole author/committer = `hoang@beava.dev`)
- **Backup bundle:** `/tmp/beava-pre-rewrite-bundle.bundle` (45 MB, all refs preserved)
- **Dev tree:** `/Users/petrpan26/work/tally` — UNTOUCHED; current HEAD `35069986` on `v2/greenfield`

## Recovery

If anything downstream goes wrong:
1. `rm -rf /tmp/beava-public-bare.git` — discard the rewrite
2. `git clone /tmp/beava-pre-rewrite-bundle.bundle <recovery-dir>` — restore from bundle (only needed if dev tree itself is damaged; it isn't)
3. Re-run from `Step 1` above

## Closes

- D-08 Workstream-D-10: filter-repo applied
- D-08 Workstream-D-11: 7 verification greps PASS
- D-04: author/committer rewrite to `hoang@beava.dev`
- D-02: trailer-strip applied (Co-Authored-By + 🤖 markers gone)
- D-01: path-strip applied (.planning + CLAUDE.md + .claude gone)
- Plan 09 rehearsal verdict (GO) honored verbatim
