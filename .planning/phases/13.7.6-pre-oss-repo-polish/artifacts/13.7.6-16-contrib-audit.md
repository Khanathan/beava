# 13.7.6-16 — CONTRIBUTING.md audit

Captured: 2026-05-05. Inputs: existing `CONTRIBUTING.md` (106 LOC), `.github/workflows/{ci,pr,release}.yml`, `README.md`, `crates/beava-server/src/{cli,config}.rs`, `python/tests/` layout.

## Current vs canonical commands

| Existing CONTRIBUTING.md line | Actual CI invocation | Match? |
|---|---|---|
| `cargo test -- --test-threads=1` (Rust tests) | `ci.yml`: `cargo nextest run --all-features --no-fail-fast`; `pr.yml` + `release.yml`: `cargo test -- --test-threads=1` | partial — canonical per CLAUDE.md §TDD is `cargo test --workspace --features testing`; rewrite to that and drop the contradictory `--test-threads=1` knob |
| `cargo clippy --all-targets -- -D warnings` | `ci.yml`: `cargo clippy --all-targets --all-features -- -D warnings` | drift — missing `--all-features` and not `--workspace`; canonical per CLAUDE.md is `cargo clippy --workspace --all-targets --all-features -- -D warnings` |
| `cargo fmt --check` | `ci.yml`: `cargo fmt --all --check` | drift — missing `--all` |
| `python -m pytest tests/ -q` | `ci.yml`: `python -m pytest tests/ -q --timeout=60 --no-header` | matches structurally; we recommend `pytest tests/v0` per the v0-only events scope (12.7 lock) |
| `cargo build` | `ci.yml`: `cargo build` | matches |

## Default ports (HTTP / TCP / admin)

| Listener | CONTRIBUTING.md (stale) | Actual default (per `README.md` + `crates/beava-server/src/{cli,config}.rs`) |
|---|---|---|
| HTTP / JSON  | `6401` | **`127.0.0.1:7379`** |
| TCP framed   | `6400` | **`127.0.0.1:7380`** |
| Admin sidecar (axum: `/health`, `/ready`, `/metrics`, `/registry`) | n/a | separate port via `cfg.admin_addr` (no documented public default; ephemeral or operator-supplied) |

CONTRIBUTING.md must drop the `6400` / `6401` claim. Per D-06 we keep the contributor doc lean and avoid documenting the admin sidecar there (README + SECURITY.md cover ops surface).

## Stale URLs

Two `petrpan26/beava` references in CONTRIBUTING.md:

- L18: `git clone https://github.com/petrpan26/beava.git`
- L99: `[GitHub Issues](https://github.com/petrpan26/beava/issues)`

Both must become `beava-dev/beava` per CONTEXT D-03 (repo rename pre-launch).

## Obsolete content

- **`--test-threads=1`** — was needed pre-Phase 12 because integration tests bound fixed listener ports. Post-Phase 12 (single-threaded data plane via `apply_shard.rs`), CI uses `cargo nextest run` in parallel without serial flag. The lingering `pr.yml` / `release.yml` `cargo test -- --test-threads=1` lines are themselves stale (Workstream-C-23 territory). For CONTRIBUTING.md purposes: drop the `--test-threads=1` knob; recommend `cargo test --workspace --features testing` per CLAUDE.md TDD §item 5.
- **`cargo run` listens on `6400/6401`** — false; current code defaults to `7379`/`7380`.
- Architectural-invariant explanations (mio-only, events-only, memory governance, TDD red→green discipline, perf gates, throughput baselines) — out of scope per **D-06 USER OVERRIDE**: CONTRIBUTING.md is test/lint/PR-workflow basics only; tripwire CI tests educate breakers organically.

## Action plan (Task 2)

1. Replace stale ports `6400/6401` with `7379` (HTTP) and `7380` (TCP), or just point readers to `cargo run --help` / `README.md` for current defaults.
2. Replace `petrpan26/beava` → `beava-dev/beava` everywhere.
3. Canonical Rust test command: `cargo test --workspace --features testing` (matches CLAUDE.md TDD §item 5; CI gate equivalent).
4. Canonical clippy: `cargo clippy --workspace --all-targets --all-features -- -D warnings`.
5. Canonical fmt: `cargo fmt --all --check`.
6. Canonical Python tests: `cd python && python -m pytest tests/v0 -q` (v0 surface).
7. Add `mypy --strict python/beava` and `ruff check python/beava` per the executor prompt.
8. SECURITY.md is the canonical disclosure channel — short cross-link only (no duplication).
9. Drop the architectural-invariant section per **D-06 USER OVERRIDE**.
10. Single `chore(13.7.6-16):` commit per CLAUDE.md TDD §item #7 doc-only-plan exemption.
