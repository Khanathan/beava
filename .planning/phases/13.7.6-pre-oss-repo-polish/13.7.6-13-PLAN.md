---
phase: 13.7.6
plan: 13
type: checkpoint
wave: 5
depends_on: ["13.7.6-12"]
files_modified:
  - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-13-repo-rename.md
autonomous: false
requirements: [D-08-Workstream-D-14, D-03]
must_haves:
  truths:
    - "User-precondition checkpoint confirms `beava-dev` GitHub org exists AND `hoang@beava.dev` is verified on the GitHub account"
    - "Force-push rewritten public bare clone (Plan 11/12 output at /tmp/beava-public-bare.git) to fresh `git@github.com:beava-dev/beava.git`"
    - "Default branch on the GitHub remote = `main`"
    - "Existing `petrpan26/tally` GitHub repo (if present) is renamed to `beava-dev/beava` via GitHub admin OR is documented as 'separate fresh repo, old `petrpan26/tally` archived/private' (planner picks based on user account state, surfaces in checkpoint)"
    - "Old README / CONTRIBUTING / SECURITY / docs links pointing to `petrpan26/tally` are updated to `beava-dev/beava` (Plan 14 onward already write the new files; this plan verifies any pre-rewrite refs in `.github/workflows/*.yml`, `python/pyproject.toml`, etc. land in the rewritten tree pointing to the new URL)"
  artifacts:
    - path: ".planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-13-repo-rename.md"
      provides: "Repo-rename procedure log + force-push transcript + remote-URL verification"
      contains: "## Pre-launch user-action verification"
  key_links:
    - from: "/tmp/beava-public-bare.git"
      to: "git@github.com:beava-dev/beava.git"
      via: "git push --force --all + git push --force --tags"
      pattern: "force-push complete"
---

<objective>
Force-push the rewritten public bare clone to `git@github.com:beava-dev/beava.git` (the canonical public repo), establishing `main` as the default branch on GitHub. This plan is the LAST destructive operation in Workstream D — once the force-push completes, the public history is canonical.

The plan is **autonomous: false** with TWO checkpoints: (a) a precondition-verification checkpoint confirming the `beava-dev` GitHub org exists + `hoang@beava.dev` is verified on the account, and (b) a destructive-go-ahead checkpoint before the force-push.

Per CONTEXT.md D-03 + D-08 Workstream-D-14 + CONTEXT.md "Pre-launch checklist".

Purpose: Workstream-D-14 — repo rename / fresh-repo creation.

Output: Public repo at `github.com/beava-dev/beava` with rewritten history; force-push log captured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-CONTEXT.md
@.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-11-rewrite-report.md
@.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-12-branch-rename.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Verify pre-launch user-actions complete (org + email)</name>
  <what-built>
    Workstream D's history rewrite is complete on a local bare clone. Two GitHub-side prerequisites must be satisfied by the user before force-push can proceed (these CANNOT be done by Claude via API without the user's GitHub auth context):
    1. The `beava-dev` GitHub organization exists (https://github.com/organizations/new if not — choose Free plan).
    2. `hoang@beava.dev` is added AND verified on the GitHub account that owns `beava-dev` (Settings → Emails → "Add email" → verify via inbox link).
    3. (If old `petrpan26/tally` repo exists publicly) decide: rename to `beava-dev/beava` via Settings → Transfer ownership, OR archive/private and create fresh `beava-dev/beava`. Planner discretion: fresh repo is cleaner since the rewritten history is genuinely new SHAs anyway.
  </what-built>
  <how-to-verify>
    User confirms:
      1. `beava-dev` org exists: https://github.com/beava-dev (200, even if no public repos yet)
      2. `hoang@beava.dev` verified: GitHub Settings → Emails shows green checkmark
      3. Either:
         (a) `petrpan26/tally` was transferred to `beava-dev/beava` (URL: https://github.com/beava-dev/beava resolves; old URL https://github.com/petrpan26/tally redirects automatically)
         OR
         (b) `petrpan26/tally` was archived/made-private and `beava-dev/beava` was created fresh as an empty repo (https://github.com/beava-dev/beava resolves with "empty repo" placeholder)
  </how-to-verify>
  <resume-signal>Reply "verified" with which path was taken (transfer vs fresh) or describe the issue</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Final go-ahead checkpoint — force-push rewritten history to public</name>
  <decision>Approve force-push of rewritten history to git@github.com:beava-dev/beava.git?</decision>
  <context>
    Plan 11 produced `/tmp/beava-public-bare.git` with rewritten history. Plan 12 verified branch state. Plan 11 backup bundle still exists at `/tmp/beava-rewrite-backups/`. Force-push will OVERWRITE whatever currently lives at `git@github.com:beava-dev/beava.git`. If that's a fresh empty repo (Task 1 path b), no overwrite concern. If that's a transferred ex-`petrpan26/tally` (Task 1 path a), the existing `v2/greenfield`-history-still-with-`.planning/`-and-CLAUDE.md is what gets overwritten — which is the entire point.
  </context>
  <options>
    <option id="approve">
      <name>Approve — force-push</name>
      <pros>Public repo becomes canonical; Phase 13.8 GA path unblocked</pros>
      <cons>The pre-rewrite SHAs are no longer recoverable from the GitHub remote (recoverable from /tmp bundle only)</cons>
    </option>
    <option id="halt">
      <name>Halt — re-verify Plan 11/12 reports first</name>
      <pros>Catches any final concern</pros>
      <cons>Delays GA</cons>
    </option>
  </options>
  <resume-signal>Type "approved" to force-push, or describe the concern</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Force-push rewritten bare clone + verify default branch on remote</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-13-repo-rename.md</files>
  <read_first>
    - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-12-branch-rename.md (verifies bare clone state)
  </read_first>
  <action>
    From the bare clone, force-push to the public remote:
      Step 1 — set the public remote inside the bare clone: `cd /tmp/beava-public-bare.git && git remote add public git@github.com:beava-dev/beava.git`.
      Step 2 — force-push refs + tags: `git push --force public main:main` then `git push --force --tags public`.
      Step 3 — set default branch on the GitHub remote (if not already main from a fresh repo): use `gh api -X PATCH /repos/beava-dev/beava -f default_branch=main` (requires `gh auth login` which is a user prerequisite — if `gh` not authed, surface as an additional checkpoint message and abort).
      Step 4 — verify with `gh repo view beava-dev/beava --json defaultBranchRef,visibility,name | jq` that `defaultBranchRef.name == "main"` and the repo is correctly named.

    Capture full transcript into artifact `13.7.6-13-repo-rename.md`:
      - `## Pre-launch user-action verification` (path taken: transfer vs fresh)
      - `## Public-remote configuration`
      - `## Force-push transcript` (stdout/stderr)
      - `## Default-branch state on GitHub` (from `gh repo view --json`)
      - `## Verification`:
        - `gh repo view beava-dev/beava --json defaultBranchRef,name,visibility,isPrivate` → defaultBranchRef.name = main; name = beava
        - `git -c protocol.version=2 ls-remote git@github.com:beava-dev/beava.git refs/heads/main` returns the SHA matching `cd /tmp/beava-public-bare.git && git rev-parse main`
  </action>
  <verify>
    <automated>(cd /tmp/beava-public-bare.git &amp;&amp; gh repo view beava-dev/beava --json defaultBranchRef --jq '.defaultBranchRef.name' | grep -E '^main$') &amp;&amp; (cd /tmp/beava-public-bare.git &amp;&amp; LOCAL=$(git rev-parse main); REMOTE=$(git ls-remote git@github.com:beava-dev/beava.git refs/heads/main | awk '{print $1}'); test "$LOCAL" = "$REMOTE")</automated>
  </verify>
  <done>Remote default branch = main; remote main SHA matches local bare-clone main SHA; transcript captured in artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local bare clone → public GitHub remote | Force-push is the canonical-history-establishing event; once complete, the rewritten SHAs are the public truth |
| GitHub auth context (gh CLI / SSH key) | The user's GitHub auth is required; Claude cannot fabricate auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.7.6-13-01 | T (Tampering) | force-push to wrong remote | mitigate | explicit `git remote add public git@github.com:beava-dev/beava.git`; double-checkpoint before force-push |
| T-13.7.6-13-02 | A (Availability) | force-push fails on unauthed gh CLI | mitigate | Task 3 surfaces auth-prereq if `gh` not logged in; user resolves before re-run |
| T-13.7.6-13-03 | R (Repudiation) | unclear which user did the force-push | accept | Single-maintainer project; single-account expected per D-04 |
</threat_model>

<verification>
- Default branch on GitHub remote = `main`
- Remote main SHA matches local bare-clone main SHA
- Force-push transcript captured
</verification>

<success_criteria>
`github.com/beava-dev/beava` exists with rewritten public history and `main` as default branch.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-13-SUMMARY.md`
</output>
