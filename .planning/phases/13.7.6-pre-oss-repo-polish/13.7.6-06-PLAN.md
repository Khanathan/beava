---
phase: 13.7.6
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-06-owasp-review.md
  - "(crates/beava-runtime-core/src/wire_request.rs OR crates/beava-server/src/apply_shard.rs ONLY if a HIGH-severity finding requires immediate code change before launch; concrete files enumerated post-review)"
autonomous: true
requirements: [D-08-Workstream-C-6]
must_haves:
  truths:
    - "`/cso` skill (or equivalent OWASP review checklist) executed against the v0 attack surface"
    - "OWASP Top-10 (2021) categories A01-A10 each have a finding row: APPLICABLE | NOT-APPLICABLE | MITIGATED with rationale"
    - "OWASP LLM Top-10 reviewed for predicate-string-injection (T-03-02-01) + register-payload sanitisation"
    - "Every HIGH-severity finding has either a same-phase fix or an explicit DEFERRED-WITH-REASON entry"
  artifacts:
    - path: ".planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-06-owasp-review.md"
      provides: "OWASP Top-10 + LLM Top-10 review against v0 attack surface; finding table; disposition"
      contains: "## OWASP Top-10 (2021) review"
  key_links:
    - from: "crates/beava-runtime-core/src/wire_request.rs"
      to: "predicate-string injection T-03-02-01"
      via: "register-payload predicate parsing"
      pattern: "JSON-prelude shim validates input"
    - from: "crates/beava-server/src/apply_shard.rs"
      to: "OP_RESET test_mode gate"
      via: "destructive register force=True path"
      pattern: "test_mode-only"
---

<objective>
Run an OWASP Top-10 (2021) + OWASP LLM Top-10 review of the v0 attack surface using the `/cso` skill (or equivalent rubric). Document findings against each category. Specifically audit the four CONTEXT.md-flagged surfaces:
1. Predicate-string injection at register time (T-03-02-01) in `crates/beava-runtime-core/src/wire_request.rs` and the JSON-prelude shim chain
2. `force=True` destructive register path
3. `OP_RESET` opcode test_mode gate
4. HTTP + TCP listener input handling (frame size limits, JSON parser hardening, content-type validation)

Every HIGH-severity finding ships a same-phase fix; MEDIUM/LOW findings can defer with a documented reason. Output is a single artifact `13.7.6-06-owasp-review.md` containing the review.

Purpose: Workstream-C requirement per CONTEXT.md D-08 Workstream-C-6. v0 OSS launch must declare its security posture.

Output: OWASP review artifact; any HIGH-severity findings fixed in source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-CONTEXT.md
@crates/beava-runtime-core/src/wire_request.rs
@crates/beava-server/src/apply_shard.rs
@crates/beava-server/src/http_admin.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run /cso skill / OWASP Top-10 (2021) review against v0 surface</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-06-owasp-review.md</files>
  <read_first>
    - .agents/skills/cso/SKILL.md if it exists (the `/cso` skill itself); fall back to https://owasp.org/Top10/ for the 2021 list
    - crates/beava-runtime-core/src/wire_request.rs (predicate parsing entry point)
    - crates/beava-server/src/apply_shard.rs (dispatch_*_sync — the only data-plane entry per mio-only invariant)
    - crates/beava-server/src/http_admin.rs (admin sidecar — only legitimate axum surface per CLAUDE.md mio-only invariant)
    - 13.7.6-CONTEXT.md D-08 Workstream-C-6
  </read_first>
  <action>
    Probe for `/cso` skill: `ls .agents/skills/cso/SKILL.md 2>/dev/null` or scan `~/.claude/skills/cso/`. If skill exists, follow its rubric. If absent, use OWASP Top-10 2021 categories directly: A01 Broken Access Control / A02 Cryptographic Failures / A03 Injection / A04 Insecure Design / A05 Security Misconfiguration / A06 Vulnerable Components / A07 Auth Failures / A08 Software & Data Integrity / A09 Logging Failures / A10 SSRF.

    For each category, write a row in the artifact's `## OWASP Top-10 (2021) review` table:
      - **Category**: A01 / A02 / ...
      - **Applicable to v0?**: YES / NO
      - **Surface**: which file/endpoint/symbol
      - **Finding**: APPLICABLE-MITIGATED / APPLICABLE-OPEN / NOT-APPLICABLE
      - **Severity**: HIGH / MEDIUM / LOW / N/A
      - **Disposition**: fix-this-phase | defer-with-reason | not-applicable

    Specifically address:
      - **A03 Injection** → predicate-string injection at register time. Verify JSON-prelude shim chain validates structure before parsing predicates. Cross-ref T-03-02-01 history.
      - **A05 Misconfig** → force=True destructive register; OP_RESET gate. Verify both are gated (force=True requires explicit kwarg; OP_RESET requires test_mode flag).
      - **A07 Auth Failures** → v0 has no auth (single-tenant; documented in SECURITY.md). Confirm SECURITY.md scope-doc is consistent with this finding.
      - **A06 Vulnerable Components** → cross-ref Plan 02 cargo audit / cargo deny output (link to artifact, do not duplicate work).
      - **A09 Logging** → confirm no PII leaks in WARN/ERROR log lines (grep `tracing::warn!` / `error!` for `format!` interpolation of user-supplied identifiers).

    For HIGH-severity findings: implement the fix in source THIS plan, verify with a test if a code change is made, document fix-commit SHA in artifact. For MEDIUM/LOW: defer with reason (open issue link, target phase).
  </action>
  <verify>
    <automated>test -f .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-06-owasp-review.md &amp;&amp; grep -c -E '^\| (A0[1-9]|A10) ' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-06-owasp-review.md | awk '{ if ($1 &gt;= 10) exit 0; else exit 1 }'</automated>
  </verify>
  <done>Artifact exists with one row per OWASP A01-A10 category; predicate-injection + force=True + OP_RESET + listener-input findings recorded; HIGH findings either fixed (with cargo test re-run as evidence) or deferred-with-reason; cargo test --workspace exits 0 if any code change landed.</done>
</task>

<task type="auto">
  <name>Task 2: OWASP LLM Top-10 review (predicate strings + register payloads)</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-06-owasp-review.md</files>
  <read_first>
    - https://owasp.org/www-project-top-10-for-large-language-model-applications/ (2025 Top-10 list)
    - 13.7.6-CONTEXT.md (the `/cso` review item explicitly lists "LLM-prompt review")
  </read_first>
  <action>
    Append `## OWASP LLM Top-10 review` section to the artifact. Categories that could apply to Beava (v0 has no LLM in the runtime path, so most do NOT apply, but the predicate-string parser is structurally similar to a prompt-injection surface):
      - **LLM01 Prompt Injection** → MAP TO predicate-string injection at register time. Document mitigation: JSON-prelude shim validates structure; predicate AST is parsed not eval'd.
      - **LLM02 Insecure Output Handling** → N/A (no LLM output in v0 runtime).
      - **LLM03 Training Data Poisoning** → N/A.
      - **LLM04 Model Denial of Service** → MAP TO frame-size DoS. Document mitigation: 4 MiB max frame default; oversized frames close connection.
      - **LLM05 Supply Chain** → cross-ref cargo deny output.
      - **LLM06-10** → N/A in v0 (no LLM in runtime).

    Each row gets the same shape as Task 1: applicable, surface, finding, severity, disposition.
  </action>
  <verify>
    <automated>grep -c '## OWASP LLM Top-10 review' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-06-owasp-review.md</automated>
  </verify>
  <done>LLM Top-10 section exists; predicate-injection + frame-DoS rows present with mitigation references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network → server (HTTP+TCP listener) | Untrusted bytes hit JSON parser + predicate parser; oversized frames hit frame codec |
| Network → server (admin sidecar) | http_admin.rs binds separate port for /health, /ready, /metrics, /registry |
| Client → server (force=True / OP_RESET) | Destructive operations need explicit gating |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.7.6-06-01 | T (Tampering) | predicate-string parser at /register | mitigate | JSON-prelude shim validates structure; AST parser not eval; finding recorded in OWASP A03 row |
| T-13.7.6-06-02 | D (DoS) | TCP frame codec oversize | mitigate | 4 MiB default `tcp.max_frame_bytes`; oversized → frame_too_large + connection close |
| T-13.7.6-06-03 | E (Elevation) | force=True destructive register | mitigate | requires explicit `force=true` kwarg; documented in SECURITY.md scope |
| T-13.7.6-06-04 | E (Elevation) | OP_RESET opcode | mitigate | gated behind `test_mode` flag; refuses outside test runs |
| T-13.7.6-06-05 | R (Repudiation) | request logging | accept | v0 has no audit log requirement (single-tenant); documented in SECURITY.md |
| T-13.7.6-06-06 | I (Info Disclosure) | log lines may interpolate user-supplied identifiers | mitigate | grep audit Task 1; if PII pattern found, redact via `tracing::field::display` or skip |
</threat_model>

<verification>
- Artifact has both `## OWASP Top-10 (2021) review` + `## OWASP LLM Top-10 review` sections
- All 10 OWASP Top-10 categories rowed with disposition
- HIGH-severity findings either fixed (cargo test --workspace passes if code changed) or deferred-with-reason
- Predicate-injection surface explicitly addressed
</verification>

<success_criteria>
v0 OSS attack surface has been systematically reviewed; HIGH-severity findings closed; MEDIUM/LOW findings tracked.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-06-SUMMARY.md`
</output>
