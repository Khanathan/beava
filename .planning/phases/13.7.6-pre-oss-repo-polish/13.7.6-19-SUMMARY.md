---
phase: 13.7.6
plan: 19
subsystem: oss-polish
tags: [changelog, keep-a-changelog, oss-polish, workstream-e]
requires:
  - .planning/STATE.md (shipped-feature provenance)
  - .planning/ROADMAP.md (phase milestones)
  - 13.7.6-CONTEXT.md (D-08 Workstream-E mapping)
provides:
  - CHANGELOG.md (Keep-a-Changelog 1.1.0; v0.0.0 entry curated; Unreleased placeholder; Compare-URLs)
  - synthesis worksheet cross-referencing internal phase deliverables → public bullets
affects:
  - public-facing repo polish (Workstream E)
tech-stack:
  added: []
  patterns: [keep-a-changelog-1.1.0]
key-files:
  created:
    - CHANGELOG.md
    - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-19-changelog-synthesis.md
    - .planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-19-SUMMARY.md
  modified: []
decisions:
  - "v0.0.0 release date kept as `<YYYY-MM-DD>` placeholder per PLAN.md §Task 2 — Phase 13.8 closure replaces with the actual GA tag-cut date."
  - "Categories used: Added / Architecture / Removed / Security / Performance. Keep-a-Changelog `Changed`/`Deprecated`/`Fixed` sections omitted because v0.0.0 is the initial release (no prior baseline to diff against)."
  - "No internal phase numbers / `.planning/` paths leak into public-facing CHANGELOG (T-13.7.6-19-01 mitigation)."
metrics:
  duration_minutes: ~10
  tasks_completed: 2
  files_changed: 3
  completed_date: 2026-05-05
---

# Phase 13.7.6 Plan 19: CHANGELOG.md create Summary

Public `CHANGELOG.md` authored at repo root in Keep-a-Changelog 1.1.0 format with a
curated v0.0.0 launch entry synthesised from the internal v0 ship-pitch surface.
Initial state is "v0 has not yet shipped" — Phase 13.8 closure flips the date
placeholder when it cuts the GA tag.

## What Landed

### CHANGELOG.md (72 lines)

- `# Changelog` H1 + Keep-a-Changelog 1.1.0 + SemVer preamble.
- `## [Unreleased]` placeholder section (empty — ready for post-v0.0.0 commits).
- `## [0.0.0] - <YYYY-MM-DD>` curated entry with five sections:
  - **Added** (16 bullets): single-binary server, 54-op catalogue, Python SDK +
    pipeline DSL, `@bv.table` derivation outputs, TypeScript / Go SDKs (communicate-only),
    `force` / `dry_run` register flags, three demo workloads, `beava bench` CLI,
    global aggregations + `bv.lit`, cold-entity TTL, memory governance, in-memory
    persistence backend, `OP_RESET`, docs site at beava.dev.
  - **Architecture** (2 bullets): mio-only single-thread data plane (Redis-shaped),
    Apache-2.0 license.
  - **Removed** (2 bullets): tables / joins / unions / session windows /
    event-time semantics descoped to v0.1+; `event_time_ms` removed from wire.
  - **Security** (3 bullets): OWASP Top-10 + LLM Top-10 review, ASVS L1 threat
    model, `cargo audit` + `cargo deny`.
  - **Performance** (3 bullets): ≥3M EPS/core, P99 batch-get < 10 ms, per-entity
    memory < 7 KB.
- Compare-URLs at the bottom: `[Unreleased]` and `[0.0.0]` linked to the
  `beava-dev/beava` GitHub repo.

### Synthesis worksheet (private, internal-only)

`.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-19-changelog-synthesis.md`
maps every public CHANGELOG bullet to the internal phase(s) that shipped the work.
Stays inside `.planning/` (stripped from public history per Phase 13.7.6 D-01).

## Verification (full block from PLAN + user prompt)

```text
exists OK
title OK
kac-link OK
semver-link OK
unreleased OK
added OK
v0.0.0 OK
no-phase-N OK
no-planning-paths OK
lines>=30 OK (72 lines)
```

All ten checks pass. No internal phase numbers (`Phase 13.x`), no `.planning/`
paths, no emoji, no AI-attribution markers in the public file.

## Deviations from Plan

None. PLAN.md §Task 1 produced the synthesis worksheet; §Task 2 produced
CHANGELOG.md following the worksheet exactly. Verification block matched the
plan's `<verify><automated>` predicate plus the user's prompt-level grep set.

The user prompt offered a simpler default structure ("if the plan body specifies
a different format, follow that"); the plan body's richer Added / Removed /
Security / Performance synthesis was followed verbatim.

## Threat Model Compliance

- **T-13.7.6-19-01** (info-disclosure: internal phase numbers in CHANGELOG) —
  mitigated. Negative grep `Phase 1\d\.` against `CHANGELOG.md` returns 0
  matches. Negative grep `\.planning/` against `CHANGELOG.md` returns 0 matches.

## Self-Check: PASSED

- `CHANGELOG.md` exists at repo root.
- `.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-19-changelog-synthesis.md` exists.
- `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-19-SUMMARY.md` exists (this file).
- Single `chore(13.7.6-19): ...` commit will be created in the next step
  (Hoang Phan author/committer; no `Co-Authored-By: Claude` trailer per
  `feedback_no_ai_attribution_in_commits`).
