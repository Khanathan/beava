---
phase: 13.7.6
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-05-go-lint.md
  - "(sdk/go/*.go touched only if go vet or staticcheck surface a regression; concrete files enumerated post-run)"
autonomous: true
requirements: [D-08-Workstream-C-5]
must_haves:
  truths:
    - "`go vet ./...` exits 0 from the Go SDK directory"
    - "`staticcheck ./...` exits 0 from the Go SDK directory if installed; otherwise documented as 'optional, vet-only is sufficient'"
  artifacts:
    - path: ".planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-05-go-lint.md"
      provides: "go vet + staticcheck re-verify report"
      contains: "## go vet output"
  key_links:
    - from: "sdk/go/*.go"
      to: "Go vet analyzer suite"
      via: "go vet"
      pattern: "no findings"
---

<objective>
Re-verify the Go SDK against `go vet ./...` (mandatory) and `staticcheck ./...` (optional) after Phase 13.6 / 13.7.5 land. Capture output to an artifact and surface/fix any drift.

Purpose: Workstream-C requirement per CONTEXT.md D-08 Workstream-C-5.

Output: Go SDK passes go vet (+ optional staticcheck); artifact captures the run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-CONTEXT.md
@sdk/go/go.mod
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locate Go SDK + run go vet ./...</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-05-go-lint.md</files>
  <read_first>
    - sdk/go/go.mod (or wherever Phase 13.6 placed it)
    - 13.7.6-CONTEXT.md D-08 Workstream-C-5
  </read_first>
  <action>
    Locate Go SDK: try `sdk/go/`, fall back to `find . -name 'go.mod' -not -path '*/node_modules/*'`. If absent, record "Go SDK not present" in artifact + exit. From the SDK directory: `go vet ./... 2>&1 | tee /tmp/govet.txt`. Build artifact at `13.7.6-05-go-lint.md` with `## SDK path`, `## go vet output` (full capture). Fix issues at source; re-run until exit 0. Then run `go build ./...` to ensure the package compiles cleanly (vet passes don't imply build passes).
  </action>
  <verify>
    <automated>(cd sdk/go 2&gt;/dev/null &amp;&amp; go vet ./... 2&gt;&amp;1 | tail -3 &amp;&amp; go build ./... 2&gt;&amp;1 | tail -3) || grep -q 'Go SDK not present' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-05-go-lint.md</automated>
  </verify>
  <done>go vet + go build both exit 0 from Go SDK directory (or artifact records absence); artifact has SDK path + vet output sections.</done>
</task>

<task type="auto">
  <name>Task 2: Run staticcheck if installed + capture output</name>
  <files>.planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-05-go-lint.md</files>
  <read_first>
    - 13.7.6-CONTEXT.md (D-08 says "optional staticcheck")
  </read_first>
  <action>
    Probe for staticcheck: `command -v staticcheck`. If installed: from Go SDK dir run `staticcheck ./... 2>&1 | tee /tmp/staticcheck.txt`; fix until exit 0; append `## staticcheck output` section. If NOT installed: install via `go install honnef.co/go/tools/cmd/staticcheck@latest` then run, OR if install fails (network / sandbox), append `## staticcheck status` section recording "staticcheck not installed; vet-only sufficient per CONTEXT D-08". Do NOT silently skip.
  </action>
  <verify>
    <automated>grep -qE '## staticcheck (output|status)' .planning/phases/13.7.6-pre-oss-repo-polish/artifacts/13.7.6-05-go-lint.md</automated>
  </verify>
  <done>Artifact has staticcheck section (output or absence-rationale).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go SDK → public Go module proxy | go vet findings ship to all `github.com/beava-dev/...` consumers; race-condition / nil-deref bugs surface as panics in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.7.6-05-01 | T (Tampering) | Go SDK correctness | mitigate | go vet gate; staticcheck where available |
</threat_model>

<verification>
- `go vet ./...` exits 0 from Go SDK
- `go build ./...` exits 0
- staticcheck output captured (or absence documented)
- Artifact has all sections
</verification>

<success_criteria>
Go SDK passes go vet + go build cleanly; v0 OSS Go module publish gate.
</success_criteria>

<output>
After completion, create `.planning/phases/13.7.6-pre-oss-repo-polish/13.7.6-05-SUMMARY.md`
</output>
