---
phase: 07-snapshot-recovery-schema-evolution
plan: 04
type: execute
wave: 4
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - crates/beava-persistence/benches/snapshot.rs
  - crates/beava-persistence/Cargo.toml
  - crates/beava-server/benches/recovery.rs
  - crates/beava-server/Cargo.toml
  - crates/beava-server/tests/phase7_smoke.rs
  - crates/beava-server/tests/phase7_crash.rs
  - .planning/perf-baselines.md
  - .planning/phases/07-snapshot-recovery-schema-evolution/07-SUMMARY.md
  - .planning/phases/07-snapshot-recovery-schema-evolution/07-VERIFICATION.md
autonomous: true
requirements:
  - SRV-RECOV-01
  - SRV-RECOV-02
  - SRV-RECOV-03
  - SRV-RECOV-04
  - SRV-RECOV-05
  - SRV-REG-04
must_haves:
  truths:
    - "At least 3 criterion benches: snapshot write (body serialize + SnapshotWriter::write), snapshot read (SnapshotReader::open + SnapshotBody::decode), WAL replay hot loop (replay_wal_from_lsn with N pre-written events)"
    - "phase7_smoke.rs exercises all 5 success criteria end-to-end: snapshot atomicity, WAL truncate after snapshot, register+restart preserves schema, corrupt snapshot detected + recovers, /ready 503→200 gate"
    - "phase7_crash.rs exercises SC1/SC4: kill server mid-run (pre-snapshot, post-snapshot, mid-snapshot if reachable); restart; verify state correctness"
    - "Baselines file .planning/perf-baselines.md gains Phase 7 rows — apple-m4/darwin hw-class"
    - "07-SUMMARY.md follows the Phase 6 template (decisions, test count delta, files changed, REQ-IDs closed, follow-ups)"
    - "07-VERIFICATION.md follows the Phase 6 template (gate table + per-SC evidence + open warnings)"
  artifacts:
    - path: "crates/beava-persistence/benches/snapshot.rs"
      provides: "criterion benches: snapshot_write, snapshot_read"
      min_lines: 120
    - path: "crates/beava-server/benches/recovery.rs"
      provides: "criterion bench: wal_replay hot loop"
      min_lines: 100
    - path: "crates/beava-server/tests/phase7_smoke.rs"
      provides: "5 end-to-end smoke tests mapping 1:1 to success criteria"
      min_lines: 250
    - path: "crates/beava-server/tests/phase7_crash.rs"
      provides: "subprocess crash probes for SC1/SC4"
      min_lines: 180
  key_links:
    - from: ".planning/perf-baselines.md"
      to: "crates/beava-persistence/benches/snapshot.rs"
      via: "baseline-keyed rows"
      pattern: "snapshot/write|snapshot/read|recovery/wal_replay"
---

<objective>
Close Phase 7 with perf-discipline baselines + end-to-end smoke + crash probes + written phase summaries.

Purpose: Satisfies CLAUDE.md §Performance Discipline (Phase 6+ requires ≥1 criterion bench on hot path; we ship 3). Provides the final ship-readiness evidence: each of the 5 Phase 7 success criteria gets a dedicated smoke test that either exits 0 or fails the phase gate. Subprocess crash probes confirm apply-AFTER-fsync survives restart.

Output:
- 3 criterion benches (snapshot write, snapshot read, WAL replay).
- `phase7_smoke.rs` — 5 tests, one per SC.
- `phase7_crash.rs` — 2 subprocess probes modeled on `phase6_crash.rs`.
- Updated `.planning/perf-baselines.md` with 3 new rows under Phase 7.
- `07-SUMMARY.md` + `07-VERIFICATION.md` produced at close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-snapshot-recovery-schema-evolution/07-CONTEXT.md
@.planning/phases/06-wal-idempotency/06-SUMMARY.md
@.planning/phases/06-wal-idempotency/06-VERIFICATION.md
@.planning/perf-baselines.md
@CLAUDE.md

@crates/beava-persistence/benches/
@crates/beava-server/tests/phase6_crash.rs
@crates/beava-server/tests/phase6_smoke.rs

<tdd_policy>
Smoke tests follow the "one SC per test" pattern from Phase 6. Criterion benches don't need a red-commit (they're measurement, not contract); commit them with the summary docs in one feat commit at close.
</tdd_policy>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 4a (red): smoke tests mapping 1:1 to the 5 success criteria</name>
  <files>crates/beava-server/tests/phase7_smoke.rs</files>
  <read_first>crates/beava-server/tests/phase6_smoke.rs, crates/beava-server/src/testing.rs</read_first>
  <behavior>
  Tests fail to compile/run until the hooks + wiring land in 07-03 (already in place by this plan). Each test asserts ONE success criterion:

  - `sc1_snapshot_atomicity_and_replay`: register + push N=200 events; force snapshot; push 50 more; shutdown; restart; assert all 250 events visible via /get.
  - `sc2_crash_mid_run_preserves_committed_events`: register; push 100 events; shutdown WITHOUT snapshot; restart; assert 100 events recoverable via WAL replay alone.
  - `sc3_truncate_releases_wal_past_snapshot`: register; push 100 events; force snapshot; capture the set of WAL segment files; shortly after (sleep 50ms or watch-channel await), list WAL segment files → confirm at least one old segment deleted OR total WAL bytes shrunk.
  - `sc4_schema_evolution_survives_restart`: register v1 with EventA; push events; register v2 adding EventB; push events against both; shutdown; restart; GET /registry → version=2; both events present; query features referencing each event return correct values.
  - `sc5_readiness_probe_503_then_200`: pre-seed wal_dir with 500+ records (via a helper that writes directly through WalSink); spawn TestServer; within 1ms call /ready → 503; wait ≤ 5s; /ready → 200.

  Commit: `test(07-04): phase 7 end-to-end smoke — 5 SCs (red)`
  </behavior>
  <verification_policy>
  Manual: `cargo build --tests --package beava-server --features testing 2>&1 | tail -20` — may compile (if 07-03 hooks exist) but tests FAIL until 07-03 is fully landed. Alternative: if 07-03 is already green, these tests should pass on first run — that's acceptable as additive assurance per CLAUDE.md (smoke tests are allowed after sub-features land).
  </verification_policy>
</task>

<task type="auto" tdd="true">
  <name>Task 4b (red): crash probes — kill mid-run + kill between snapshot.tmp rename and WAL truncate</name>
  <files>crates/beava-server/tests/phase7_crash.rs, crates/beava-server/src/bin/phase7_crash_probe.rs</files>
  <read_first>crates/beava-server/tests/phase6_crash.rs, crates/beava-server/src/bin/phase6_crash_probe.rs</read_first>
  <behavior>
  Pattern follows phase6_crash.rs: test spawns a subprocess binary that does something specific then SIGKILLs itself at a known instant; the parent test then inspects artifacts on disk.

  Tests:
  - `snapshot_kill_before_rename_leaves_no_partial_bvs`: probe writes a snapshot but SIGKILLs between fsync and rename; parent asserts no `.bvs` file with that LSN exists; only `.tmp`.
  - `snapshot_kill_after_rename_before_truncate_preserves_wal_past_covered_lsn`: probe writes snapshot successfully, SIGKILLs before `truncate_up_to`; parent restarts a TestServer pointing at the same dirs; asserts both:
      (a) the new server boots successfully from the snapshot,
      (b) any WAL records past snapshot_lsn (there should be none here, but extra old segments are OK) are NOT lost — restart state matches.

  Probe binary `phase7_crash_probe.rs`:
  - args: `--wal-dir`, `--snapshot-dir`, `--mode=before-rename|after-rename-before-truncate`, `--events-before=N`.
  - Modes: register a simple feature, push N events, enter the snapshot path, abort at the specified point using a `BEAVA_CRASH_AT` env var or equivalent.

  Commit: `test(07-04): phase 7 crash probes — snapshot atomicity + truncate ordering (red)`
  </behavior>
  <verification_policy>
  Manual: may require small hooks in snapshot_task.rs to respect `BEAVA_CRASH_AT`. Land those hooks in 4c (green).
  </verification_policy>
</task>

<task type="auto" tdd="true">
  <name>Task 4c (green): crash-injection hook + pass all smoke + crash tests</name>
  <files>crates/beava-server/src/snapshot_task.rs, crates/beava-server/src/bin/phase7_crash_probe.rs</files>
  <read_first>crates/beava-server/tests/phase7_crash.rs, crates/beava-server/tests/phase7_smoke.rs</read_first>
  <behavior>
  Add a test-only crash point inside snapshot_task::do_snapshot (gated `#[cfg(any(feature = "testing", test))]`) that reads `BEAVA_CRASH_AT` and `std::process::abort`s if it equals one of: `before-rename`, `after-rename-before-truncate`. Implement the probe binary.

  Run `cargo test --workspace --features beava-server/testing -- --test-threads=1` — all 11 Phase 7 tests (6 recovery + 5 smoke + 2 crash) pass. (Total new tests in phase: 11 integration + 11 persistence + 16 core ≈ 38).

  Commit: `feat(07-04): crash-injection hook + probe binary`
  </behavior>
  <verification_policy>
  Auto: `cargo test --workspace --features beava-server/testing 2>&1 | tail -5` — no regression.
  Auto: all phase7_* tests pass.
  </verification_policy>
</task>

<task type="auto" tdd="false">
  <name>Task 4d: criterion benches — snapshot write, snapshot read, WAL replay</name>
  <files>crates/beava-persistence/benches/snapshot.rs, crates/beava-persistence/Cargo.toml, crates/beava-server/benches/recovery.rs, crates/beava-server/Cargo.toml, .planning/perf-baselines.md</files>
  <read_first>crates/beava-persistence/benches, .planning/perf-baselines.md</read_first>
  <behavior>
  Add 3 criterion benches:
  - `snapshot_write_10k_entities_30agg`: build a SnapshotBody with 10_000 entities × 3 aggregations × 1 window variant; measure SnapshotWriter::write end-to-end (serialize + fsync).
  - `snapshot_read_10k_entities_30agg`: pre-write once; measure SnapshotReader::open + SnapshotBody::decode.
  - `wal_replay_1k_events`: pre-populate a WAL dir with 1_000 Event records; measure `replay_wal_from_lsn(0)`. (Use an isolated smaller scenario — the hot loop is agg_apply, which Phase 5 already baselined.)

  `cargo.toml`: declare the benches.

  Capture baseline numbers on apple-m4/darwin; append rows to `.planning/perf-baselines.md` under a new `## Phase 7 — Snapshot + recovery` section with format matching existing rows.

  Commit: `perf(07-04): criterion benches for snapshot + recovery hot paths`
  </behavior>
  <verification_policy>
  Manual: `cargo bench --package beava-persistence --bench snapshot -- --quick` returns numbers (take ~10s with --quick).
  Manual: `cargo bench --package beava-server --bench recovery -- --quick` returns numbers.
  Manual: baselines-file contains 3 new rows.
  </verification_policy>
</task>

<task type="auto" tdd="false">
  <name>Task 4e: phase summary + verification docs</name>
  <files>.planning/phases/07-snapshot-recovery-schema-evolution/07-SUMMARY.md, .planning/phases/07-snapshot-recovery-schema-evolution/07-VERIFICATION.md</files>
  <read_first>.planning/phases/06-wal-idempotency/06-SUMMARY.md, .planning/phases/06-wal-idempotency/06-VERIFICATION.md</read_first>
  <behavior>
  `07-SUMMARY.md`: follows Phase 6 template — decisions made, REQ-IDs closed (SRV-REG-04, SRV-RECOV-01..05), files changed, test count delta (590 → N), follow-ups for Phase 8+.
  `07-VERIFICATION.md`: follows Phase 6 template — gate table (test/clippy/fmt), per-SC evidence (SC1–SC5), bench numbers vs Phase 6 baselines (note fsync hw-class WARNING applies here too for macOS), open warnings, gaps.

  Commit: `docs(07): phase summary + verification — all SCs closed`
  </behavior>
  <verification_policy>
  Manual: both files exist and follow the Phase 6 template.
  </verification_policy>
</task>

</tasks>

<success_criteria>
- All 5 smoke tests (one per SC) pass
- Both crash probes pass
- All 3 criterion benches emit numbers; baselines file updated
- Final workspace test count ≥ 590 + 38 ≈ 628
- 07-SUMMARY.md + 07-VERIFICATION.md written
- No regression in existing 590 tests
- `cargo clippy -- -D warnings` + `cargo fmt --all --check` clean
- Commit subjects: `test(07-04):` (2) + `feat(07-04):` (1) + `perf(07-04):` (1) + `docs(07):` (1)
</success_criteria>
</content>
</invoke>