---
phase: 14-streaming-correctness
plan: 04
type: execute
wave: 4
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - crates/beava-server/benches/drop_check.rs
  - .planning/perf-baselines.md
  - .planning/throughput-baselines.md
  - .planning/phases/14-streaming-correctness/14-SUMMARY.md
  - .planning/phases/14-streaming-correctness/14-VERIFICATION.md
autonomous: true
requirements:
  - D-09 (drop-check < 20ns/event)
  - D-10 (< 5% end-to-end regression on simple-fraud)
  - CLAUDE.md §Performance Discipline (criterion microbench)
  - CLAUDE.md §End-to-end throughput (throughput-run row)

must_haves:
  truths:
    - "Drop-check cost per event is measured and committed to perf-baselines.md with hw-class tagging"
    - "Drop-check cost is < 20ns per event on Apple-M4"
    - "beava-bench simple-fraud (small, HTTP, BATCH_MS=0) regression is < 5% vs pre-14 v2/greenfield baseline"
    - "Phase 14 SUMMARY + VERIFICATION documents SC1–SC6 coverage with file:line citations"
  artifacts:
    - path: "crates/beava-server/benches/drop_check.rs"
      provides: "criterion microbench for is_late + observe path"
      contains: "fn bench_drop_check"
    - path: ".planning/perf-baselines.md"
      provides: "Phase 14 drop-check baseline row"
      contains: "phase: 14"
    - path: ".planning/throughput-baselines.md"
      provides: "Phase 14 throughput-run row (simple-fraud × HTTP × BATCH_MS=0)"
      contains: "phase: 14"
    - path: ".planning/phases/14-streaming-correctness/14-SUMMARY.md"
      provides: "Phase summary with SC coverage map"
    - path: ".planning/phases/14-streaming-correctness/14-VERIFICATION.md"
      provides: "Checklist-style verification doc with automated-test citations"
  key_links:
    - from: "criterion bench"
      to: "WatermarkState.is_late + observe"
      via: "direct call in tight loop"
      pattern: "c.bench_function"
---

<objective>
Close out Phase 14 with the performance regression gates (criterion microbench + beava-bench throughput row) and the canonical phase artefacts (SUMMARY + VERIFICATION).

Purpose: Per CLAUDE.md §Performance Discipline and §End-to-end throughput, every phase from 6 onward requires a criterion bench and every phase from 8 onward requires a throughput-run row. Phase 14 satisfies both gates and documents that the drop-check is cheap enough (< 20ns/event) that adding it to the hot path is free.
Output: Bench source file, two baseline-ledger rows, SUMMARY.md, VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-streaming-correctness/14-CONTEXT.md
@.planning/phases/14-streaming-correctness/14-01-PLAN.md
@.planning/phases/14-streaming-correctness/14-02-PLAN.md
@.planning/phases/14-streaming-correctness/14-03-PLAN.md
@.planning/perf-baselines.md
@.planning/throughput-baselines.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criterion microbench — drop-check cost (< 20ns/event)</name>
  <files>crates/beava-server/benches/drop_check.rs, crates/beava-server/Cargo.toml</files>
  <behavior>
    - The bench measures three workloads:
      1. `is_late_miss` — event is NOT late (common path): `WatermarkState` with watermark 10_000 for stream "e", check `is_late("e", 9_999, 5_000)` in a tight loop. Expected: < 10ns (atomic-free BTreeMap read + 2 i64 compares).
      2. `is_late_hit` — event IS late: same state, check `is_late("e", 4_000, 5_000)`. Expected: < 15ns (same path, different branch).
      3. `observe_and_apply_path` — full non-drop branch: `observe("e", event_time)` with increasing event_time + sentinel `apply` no-op. Expected: < 20ns.
    - Report timings in `.planning/perf-baselines.md` with `hw-class: apple-m4` (or whatever the runner is).
    - Gate: if any of the three exceeds 20ns, BLOCK — we have a coding error.
  </behavior>
  <action>
    Create `crates/beava-server/benches/drop_check.rs` using the existing Phase 5.5 criterion bench pattern (see other `benches/*.rs` for template). Register it in `Cargo.toml` under `[[bench]]`.

    The bench calls `WatermarkState` methods directly — no HTTP, no WAL, no apply; this isolates the drop-check cost per D-09.

    Run `cargo bench --bench drop_check -p beava-server` locally; capture the three timings. Append a row to `.planning/perf-baselines.md`:

    ```
    ## Phase 14 — drop-check microbench

    | hw-class | is_late_miss | is_late_hit | observe_and_apply_path | date |
    |---|---|---|---|---|
    | apple-m4 | Xns | Yns | Zns | 2026-04-XX |
    ```

    If numbers exceed 20ns, investigate before proceeding. Likely culprit: unexpected heap allocation or a `format!` in a hot path.

    RED commit: `test(14-04): drop_check criterion bench (compile-fail against WatermarkState pre-impl)` — actually for a bench, "red" means the bench file compiles but the baseline row shows N/A. Use `bench` commit directly.
    GREEN commit: `bench(14-04): drop_check microbench + perf baseline row (< 20ns/event)`
  </action>
  <verify>
    <automated>cargo bench --bench drop_check -p beava-server -- --test</automated>
  </verify>
  <done>Bench compiles + runs; all three variants < 20ns; baseline row committed.</done>
</task>

<task type="auto">
  <name>Task 2: beava-bench throughput run — simple-fraud (small × HTTP × BATCH_MS=0)</name>
  <files>.planning/throughput-baselines.md</files>
  <action>
    Per CLAUDE.md §End-to-end throughput: re-run `beava-bench` with the post-14 binary against the simple-fraud small pipeline (Phase 7.5 baseline shape) over HTTP with BATCH_MS=0, parallel=64 (matches Phase 13.1 measurement shape).

    Command (adapt to actual harness invocation):
    ```
    cargo run --release -p beava-bench -- --pipeline simple-fraud --size small --transport http --batch-ms 0 --parallel 64 --duration 60
    ```

    Append one row to `.planning/throughput-baselines.md` in the existing format:
    ```
    | phase | pipeline | size | transport | batch_ms | parallel | eps | p50_push_ms | p95_push_ms | p99_push_ms | p99_batch_get_ms | rss_mb_peak | hw-class | date | notes |
    | 14 | simple-fraud | small | http | 0 | 64 | ... | ... | ... | ... | ... | ... | apple-m4 | 2026-04-XX | Phase 14 watermark + bucket widening + register validator; drop-check < 20ns; expected ≤ 5% regression vs pre-14 baseline |
    ```

    Gate: compare `eps` to the most recent `v2/greenfield` row for the same shape. If regression > 5%, BLOCK — investigate (likely a hot-path inefficiency in the drop check). If regression is 5–25%, warn + investigate but allow phase to ship with explicit note. If > 25%, hard block per §Conventions.

    Commit: `bench(14-04): throughput baseline — simple-fraud post-watermark (≤5% regression)`
  </action>
  <verify>
    <automated>grep -c "^| 14 | simple-fraud" .planning/throughput-baselines.md | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
  </verify>
  <done>Throughput row committed; EPS regression < 5% vs prior simple-fraud baseline; documented in notes column.</done>
</task>

<task type="auto">
  <name>Task 3: Phase SUMMARY + VERIFICATION docs</name>
  <files>.planning/phases/14-streaming-correctness/14-SUMMARY.md, .planning/phases/14-streaming-correctness/14-VERIFICATION.md</files>
  <action>
    Write `14-SUMMARY.md` following the standard GSD summary template:
    - Scope delivered (watermark + drop + bucket widening + validator + WAL replay + perf gates)
    - Files modified with line counts
    - Test coverage (total new tests, total workspace test count post-phase)
    - SC1–SC6 coverage map with file:line for the asserting test
    - Commit range (first red commit of Plan 14-01 Task 1 → last commit of Plan 14-04 Task 2)
    - Perf snapshot: drop-check timings + simple-fraud EPS before/after
    - Grey areas resolved during execution (from CONTEXT grey-areas list)

    Write `14-VERIFICATION.md` as a checklist per SC with citations:
    - [ ] SC1 — front-door drop correctness: `tests/watermark_drop.rs::test_front_door_drop`
    - [ ] SC2 — counter increment: `tests/watermark_drop.rs::test_counter_increments`
    - [ ] SC3 — rate-limited log: `tests/watermark_drop.rs::test_rate_limit`
    - [ ] SC4 — bucket-reset fixed: `tests/bucket_widening_regression.rs::bucket_reset_does_not_lose_in_tolerance_late_event`
    - [ ] SC5 — register validator: `tests/tolerate_delay_validator.rs`
    - [ ] SC6 — WAL replay: `tests/wal_replay_watermark.rs`
    - [ ] D-09 — drop-check < 20ns: perf-baselines.md row
    - [ ] D-10 — < 5% regression: throughput-baselines.md row

    Each checkbox flipped ✅ with measured timing / row reference.

    Update `.planning/STATE.md` "Shipped & Merged" table (if Phase 14 is merging to `v2/greenfield`) — or leave as on-branch until merge.

    Commit: `docs(14-04): Phase 14 SUMMARY + VERIFICATION`
  </action>
  <verify>
    <automated>test -f .planning/phases/14-streaming-correctness/14-SUMMARY.md && test -f .planning/phases/14-streaming-correctness/14-VERIFICATION.md</automated>
  </verify>
  <done>Both files exist; SC1–SC6 all ✅ with file:line citations; perf + throughput rows linked from VERIFICATION.</done>
</task>

</tasks>

<verification>
1. D-09 (drop-check < 20ns) — Task 1.
2. D-10 (< 5% end-to-end regression) — Task 2.
3. SUMMARY + VERIFICATION consolidate SC1–SC6 — Task 3.
4. CLAUDE.md §Performance Discipline satisfied — Task 1 adds criterion bench under `crates/*/benches/`; Task 2 appends row to `.planning/throughput-baselines.md`.
</verification>

<success_criteria>
- `crates/beava-server/benches/drop_check.rs` exists and shows < 20ns/event for all three variants on the runner's hw-class.
- `.planning/perf-baselines.md` has a Phase 14 row.
- `.planning/throughput-baselines.md` has a Phase 14 row with ≤ 5% regression vs. the most recent prior `v2/greenfield` simple-fraud baseline.
- `14-SUMMARY.md` + `14-VERIFICATION.md` cite SC1–SC6 with file:line references.
</success_criteria>

<output>
After completion, Phase 14 is ready to merge into `v2/greenfield`. STATE.md update happens at merge, not here.
</output>
