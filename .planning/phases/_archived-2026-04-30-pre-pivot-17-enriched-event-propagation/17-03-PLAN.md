---
phase: 17-enriched-event-propagation
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - tests/test_pipeline.rs
  - tests/test_concurrent.rs
autonomous: true
requirements:
  - ENG-01

must_haves:
  truths:
    - "Upstream derive result is visible to downstream operator (sum of upstream derived field returns correct value)"
    - "Multi-level cascade enrichment works (3+ hops)"
    - "Enrichment works correctly with read_features=false (async/batch path)"
    - "8-concurrent-client enrichment produces correct, non-contaminated results (C-5)"
    - "Where-clause can reference enriched upstream fields"
    - "Qualified and unqualified field resolution both work in enrichment"
    - "Benchmark gate passes: <5% regression from 1.1M eps baseline (C-1)"
  artifacts:
    - path: "tests/test_pipeline.rs"
      provides: "Integration tests for enriched cascade: derive-to-downstream, multi-hop, async mode, where-clause, field resolution"
      contains: "test_enriched"
    - path: "tests/test_concurrent.rs"
      provides: "Concurrent enrichment correctness test with 8 clients"
      contains: "test_enriched_concurrent"
  key_links:
    - from: "tests/test_pipeline.rs"
      to: "src/engine/pipeline.rs"
      via: "Tests exercise push_with_cascade with enrichment"
      pattern: "push_with_cascade"
    - from: "tests/test_concurrent.rs"
      to: "src/engine/pipeline.rs"
      via: "Concurrent test exercises enrichment under multi-thread"
      pattern: "push_with_cascade"
---

<objective>
Verify enriched event propagation with integration tests covering all ENG-01 success criteria: derive-to-downstream aggregation, multi-hop cascade, async mode enrichment, concurrent client correctness, and benchmark regression gate.

Purpose: Prove that multi-stage computed features work correctly and that the enrichment accumulator satisfies both the C-1 performance gate and C-5 concurrency safety requirement.

Output: Integration tests in test_pipeline.rs and test_concurrent.rs, benchmark run verifying <5% regression.
</objective>

<execution_context>
@/data/home/tally/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/tally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-enriched-event-propagation/17-RESEARCH.md
@.planning/phases/17-enriched-event-propagation/17-02-SUMMARY.md

@tests/test_pipeline.rs
@tests/test_concurrent.rs
@src/engine/pipeline.rs
</context>

<interfaces>
<!-- From Plan 02 (will exist after execution) -->
From src/engine/pipeline.rs:
```rust
// push_with_cascade_internal now builds enrichment accumulator
// push_internal accepts enrichment_json + enrichment_fv params
// Public API unchanged: push_with_cascade(stream, event, store, now) -> FeatureMap
```

From src/types.rs:
```rust
pub type FeatureMap = ahash::AHashMap<String, FeatureValue>;
pub enum FeatureValue { Float(f64), Int(i64), String(String), Missing }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrichment integration tests in test_pipeline.rs</name>
  <files>tests/test_pipeline.rs</files>
  <read_first>tests/test_pipeline.rs, src/engine/pipeline.rs</read_first>
  <behavior>
    - test_enriched_derive_to_downstream_sum: Register 3-stream pipeline (RawTxns keyless -> CurrencyNorm keyed with derive "amount_usd = _event.amount * _event.exchange_rate" -> UserStats keyed with sum("CurrencyNorm.amount_usd", window="1h")). Push event with amount=100, exchange_rate=1.2. Assert UserStats.total_usd_1h == 120.0.
    - test_enriched_multi_hop_cascade: Register 4-stream pipeline (A -> B -> C -> D) where each stage derives a field the next stage aggregates. Push one event, verify D sees values from A, B, and C.
    - test_enriched_cascade_async_mode: Same 3-stream pipeline as above, push via push_with_cascade_no_features (async path). Then push via push_with_cascade (sync). Verify sync push returns correct enriched downstream result (async push updated operators, sync push reads them).
    - test_enriched_where_clause: Downstream operator has where="CurrencyNorm.amount_usd > 50". Push event with amount=100, exchange_rate=1.2 (amount_usd=120 > 50). Verify count increments. Push event with amount=10, exchange_rate=1.2 (amount_usd=12 < 50). Verify count does NOT increment.
    - test_enriched_field_resolution_qualified: Downstream uses sum("CurrencyNorm.amount_usd"). Verify correct resolution via qualified name.
    - test_enriched_field_resolution_unqualified: Downstream uses sum("amount_usd") (unqualified). Verify it resolves from enrichment (last-writer-wins).
    - test_enriched_no_cascade_unchanged: Single stream (no downstream). Push event. Verify behavior identical to pre-enrichment (no regression).
  </behavior>
  <action>
Add the enrichment integration tests to tests/test_pipeline.rs. Follow the existing test patterns in that file for setting up PipelineEngine, registering streams, creating StateStore, and calling push_with_cascade.

For each test:
1. Build a PipelineEngine with appropriate stream definitions using StreamDefinition and FeatureDef.
2. Create a StateStore.
3. Push events via engine.push_with_cascade() (sync) or engine.push_with_cascade_no_features() (async).
4. Assert feature values in the returned FeatureMap or via engine.push_with_cascade() after async push.

Key implementation details:
- Use `FeatureDef::Derive { expr: ... }` for upstream derive expressions. The expression must be parsed at registration (use `parse_expression` from expression.rs).
- Use `FeatureDef::Sum { field: "CurrencyNorm.amount_usd".to_string(), ... }` for downstream aggregation of upstream field (qualified name).
- Set `depends_on` correctly in downstream StreamDefinition.
- After pushing, read features via a subsequent GET-style push or directly from store.

For the multi-hop test (A -> B -> C -> D):
- A: keyless source
- B: keyed, depends_on=[A], has derive "computed_b = _event.raw_value * 2"
- C: keyed, depends_on=[B], has sum("B.computed_b", window="1h") and derive "computed_c = B.computed_b + 10"
- D: keyed, depends_on=[C], has sum("C.computed_c", window="1h")
- Push event with raw_value=5. B.computed_b=10, C.sum=10, C.computed_c=20, D.sum=20.
  </action>
  <verify>
    <automated>cd /data/home/tally && cargo test test_enriched -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn test_enriched" tests/test_pipeline.rs` returns >= 7
    - `cargo test test_enriched` shows all enrichment tests passing
    - `cargo test test_enriched_derive_to_downstream_sum` passes and asserts 120.0
    - `cargo test test_enriched_multi_hop_cascade` passes with correct 4-hop values
    - `cargo test test_enriched_where_clause` passes with conditional count
  </acceptance_criteria>
  <done>All 7 enrichment integration tests pass. Derive-to-downstream aggregation returns correct values. Multi-hop cascade propagates enrichment through 4 stages. Async mode enrichment works. Where-clause references enriched fields. Both qualified and unqualified field resolution work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Concurrent enrichment correctness test + benchmark gate</name>
  <files>tests/test_concurrent.rs</files>
  <read_first>tests/test_concurrent.rs</read_first>
  <behavior>
    - test_enriched_concurrent_clients: 8 concurrent tokio tasks, each pushing events with unique user_id to a 3-stage cascade pipeline. Each task pushes 100 events. After all complete, verify each user's downstream aggregation is correct (no cross-contamination between concurrent pushes). This proves C-5: enrichment accumulator is per-push, not shared.
    - Benchmark gate: Run `python3 benchmark/tally-throughput/bench.py` (or equivalent) to verify <5% regression from 1.1M eps baseline. If benchmark infrastructure doesn't exist in the test file, add a note about running it manually and focus the automated test on concurrent correctness.
  </behavior>
  <action>
1. **Add test_enriched_concurrent_clients to tests/test_concurrent.rs**:

   Read the existing test_concurrent.rs to understand the testing pattern (likely uses tokio::test, creates AppState, spawns tasks). Follow the same pattern.

   Setup:
   - Register a 3-stream pipeline: Source (keyless) -> Converter (keyed on "user_id", derive "amount_usd = _event.amount * _event.rate") -> Aggregator (keyed on "user_id", sum("Converter.amount_usd", window="1h")).
   - Spawn 8 tokio tasks. Each task:
     - Has a unique user_id prefix (e.g., "user_0", "user_1", ..., "user_7")
     - Pushes 100 events with amount=10.0, rate=1.5 (so amount_usd=15.0 each)
     - Uses push_with_cascade (sync mode to verify results)
   - After all tasks complete, for each user, call GET and verify:
     - Aggregator.total_usd_1h == 100 * 15.0 = 1500.0 (exact, no cross-contamination)
   
   This test MUST use the concurrent ConcurrentAppState/DashMap path (not single-threaded test helper) to exercise real concurrent enrichment.

2. **Benchmark verification**:
   
   Check if `benchmark/tally-throughput/bench.py` exists. If it does, document in the test file comments that the benchmark gate should be run:
   ```
   // Benchmark gate (run manually):
   // python3 benchmark/tally-throughput/bench.py --matrix --clients 8
   // Must pass within -5% of 1.1M eps baseline (C-1 gate)
   ```
   
   If a Rust benchmark exists in `benches/`, check it compiles and note the command. The benchmark gate is a manual verification step since it requires a running server + Python client.

3. **Run full test suite** including existing concurrent tests to verify no regression.
  </action>
  <verify>
    <automated>cd /data/home/tally && cargo test test_enriched_concurrent -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "test_enriched_concurrent" tests/test_concurrent.rs` returns a match
    - `cargo test test_enriched_concurrent` passes
    - `cargo test` (full suite) passes with zero failures
    - The test uses 8 concurrent tasks with unique user_ids and verifies no cross-contamination
  </acceptance_criteria>
  <done>Concurrent enrichment test passes with 8 clients, each getting correct isolated results (no cross-contamination, proving C-5). Full test suite passes. Benchmark gate documented for manual execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced by tests.

| Boundary | Description |
|----------|-------------|
| (none) | Test-only changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | n/a | n/a | n/a | Test-only plan, no production security changes |
</threat_model>

<verification>
- `cargo test test_enriched` passes all 7+ enrichment tests
- `cargo test test_enriched_concurrent` passes concurrent correctness test
- `cargo test` full suite passes with zero failures
- Benchmark gate: `python3 benchmark/tally-throughput/bench.py --matrix --clients 8` passes within -5% of 1.1M eps (manual verification)
</verification>

<success_criteria>
All enrichment integration tests pass, proving: (1) upstream derives visible to downstream operators, (2) multi-hop cascade works, (3) async mode enrichment works, (4) where-clause references enriched fields, (5) qualified and unqualified resolution both work, (6) 8-concurrent-client enrichment produces correct isolated results, (7) no regression in existing tests. Benchmark gate documented and ready for manual execution.
</success_criteria>

<output>
After completion, create `.planning/phases/17-enriched-event-propagation/17-03-SUMMARY.md`
</output>
