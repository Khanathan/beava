---
phase: 13.3-lockless-apply
plan: 04
type: execute
wave: 4
depends_on: [13.3-03]
files_modified:
  - crates/beava-server/benches/phase13_3_apply.rs
  - crates/beava-server/Cargo.toml
  - .planning/perf-baselines.md
  - .planning/throughput-baselines.md
  - .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md
  - .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md
autonomous: false
requirements:
  - PERF-APPLY-LOCKLESS-10  # criterion microbench for per-event apply cost
  - PERF-APPLY-LOCKLESS-11  # throughput-run regression row
  - PERF-APPLY-LOCKLESS-12  # phase SUMMARY + VERIFICATION

must_haves:
  truths:
    - "Criterion microbench `phase13_3_apply` exists under crates/beava-server/benches/, measures per-event apply cost for (count, sum, ewma, count_distinct/HLL) shapes across (actor-roundtrip, actor-internal-only) modes."
    - "Per-event apply cost (actor-internal, count op, Apple-M4) ≤ 80 ns; target 60 ns."
    - "Throughput-run rows appended to `.planning/throughput-baselines.md` for (small, medium, large) × (http, tcp) at parallel=64 AND a BATCH_MS sweep (0, 1, 5, 20) on small-http."
    - "Phase 13.1 small-http post-merge baseline (17k EPS at parallel=64) is demonstrably lifted by ≥ 5× on at least one of the six cells; BLOCKER if no cell hits 5× lift (downgrade the 60 ns target but keep the ≥ 5× throughput target as the ship floor)."
    - "13.3-SUMMARY.md exists with the ledger delta table, decisions made during execution, and a forward-plan sentence for Phase 13's ship gate."
    - "13.3-VERIFICATION.md exists with status=passed if all 6 CONTEXT success criteria are green; status=partial otherwise (with BLOCKER/WARNING rows for failing SCs)."
  artifacts:
    - path: crates/beava-server/benches/phase13_3_apply.rs
      provides: "Criterion bench group `phase13_3_apply` with per-op-family measurements"
    - path: .planning/perf-baselines.md
      provides: "Per-phase section for 13.3 with hw-class-tagged bench medians"
    - path: .planning/throughput-baselines.md
      provides: "Per-phase section for 13.3 with all six (size × transport) rows at parallel=64, plus BATCH_MS sweep rows"
    - path: .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md
      provides: "Phase SUMMARY per GSD template"
    - path: .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md
      provides: "Phase VERIFICATION against the 6 success criteria in CONTEXT"
  key_links:
    - from: crates/beava-server/benches/phase13_3_apply.rs
      to: crates/beava-server/src/apply/actor.rs
      via: "criterion bench directly exercises ApplyActor + ApplyHandle"
      pattern: "ApplyActor::spawn_local"
    - from: .planning/throughput-baselines.md
      to: crates/beava-bench
      via: "harness CLI invocation documented in the plan's execution"
      pattern: "beava-bench"
---

<objective>
Measure. Ledger. Close the phase.

Produce the numbers that prove Phase 13.3 achieved its goal (≥ 5× throughput lift; ≤ 80 ns/event apply cost). Append rows to the two baseline ledgers (`.planning/perf-baselines.md` and `.planning/throughput-baselines.md`) per CLAUDE.md §Performance Discipline. Write 13.3-SUMMARY and 13.3-VERIFICATION.

Includes a HUMAN-VERIFY checkpoint because the throughput run requires running on the right hardware (Apple-M4 / Darwin) and the numbers are only meaningful when captured on a quiescent machine.

Output: a criterion microbench + 6 throughput ledger rows + BATCH_MS sweep + SUMMARY + VERIFICATION.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/13.3-lockless-apply/13.3-CONTEXT.md
@.planning/phases/13.3-lockless-apply/13.3-01-SUMMARY.md
@.planning/phases/13.3-lockless-apply/13.3-02-SUMMARY.md
@.planning/phases/13.3-lockless-apply/13.3-03-SUMMARY.md
@.planning/perf-baselines.md
@.planning/throughput-baselines.md

@crates/beava-server/benches/phase25_wire.rs  # reference pattern for criterion bench structure
@crates/beava-bench/Cargo.toml                  # reference for invoking the end-to-end harness
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criterion microbench for per-event apply cost</name>
  <files>crates/beava-server/benches/phase13_3_apply.rs, crates/beava-server/Cargo.toml</files>
  <behavior>
    - Bench group `phase13_3_apply` registers four benches:
      * `apply_internal/count` — direct `ApplyState::apply_event` call with a count-only aggregation; measures the hot-path without actor/channel overhead. Target: ≤ 80 ns; ideal 60 ns.
      * `apply_internal/sum` — same shape with a sum op. Target ≤ 80 ns.
      * `apply_internal/ewma` — decay op; allocation-heavier. Target ≤ 200 ns.
      * `apply_via_actor/count` — full roundtrip (send cmd → actor apply → oneshot reply). Includes channel cost. Target ≤ 1 µs (mpsc + oneshot overhead dominates).
    - All benches use `Throughput::Elements(1)` so criterion reports elements/sec.
    - Fixtures: `criterion_benches/fixtures/simple_fraud_registry.json` generated deterministically from a function.
  </behavior>
  <subtasks>

    <subtask id="1.a" phase="red">
      <files>crates/beava-server/benches/phase13_3_apply.rs, crates/beava-server/Cargo.toml</files>
      <action>
        Create the bench file with `criterion_group!` + `criterion_main!` scaffolding. Each bench body calls a not-yet-implemented helper `build_apply_state_with_count_agg()` → compile error.

        Add `[[bench]]` entry to Cargo.toml matching the pattern of `phase25_wire` bench.

        Commit: `test(13.3-04): red — phase13_3_apply criterion bench scaffold`.
      </action>
      <verify>
        <automated>cargo bench -p beava-server --bench phase13_3_apply --no-run 2>&1 | grep -E "(error\[|warning:)" | head -10</automated>
      </verify>
      <done>Build fails with clear "function not found" errors for the helpers.</done>
    </subtask>

    <subtask id="1.b" phase="green">
      <files>crates/beava-server/benches/phase13_3_apply.rs</files>
      <action>
        Implement:
        - `build_apply_state_with_count_agg()` / `_sum` / `_ewma` — build a Registry with one event + one aggregation of the target op; construct `ApplyState::new(registry)`; return `(ApplyState, sample_row)`.
        - For `apply_internal/*`: `bench.iter(|| { state.apply_event(event_name, row.clone(), event_time_ms, event_id); event_id += 1; });` — pure synchronous state mutation. No actor, no channel.
        - For `apply_via_actor/count`: set up `#[tokio::main(flavor = "current_thread")]`-style harness via `LocalSet::block_on`, spawn actor, bench iterates `handle.apply_event(...).await`. Use `tokio::runtime::Builder::new_current_thread().enable_all().build()` + `rt.block_on(local.run_until(async { bench body }))`.
        - All benches use `group.throughput(Throughput::Elements(1))` so output is in elements/sec too.
        - Record results; print them at end of run via `criterion` default output.

        Commit: `feat(13.3-04): green — phase13_3_apply per-event cost bench`.
      </action>
      <verify>
        <automated>cargo bench -p beava-server --bench phase13_3_apply 2>&1 | tail -30</automated>
      </verify>
      <done>Four benches execute; median numbers printed; apply_internal/count ≤ 80 ns on Apple-M4 (note: will be re-measured in Task 3 checkpoint).</done>
    </subtask>

  </subtasks>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append criterion baseline rows to .planning/perf-baselines.md</name>
  <files>.planning/perf-baselines.md</files>
  <behavior>
    - Run the bench with `cargo bench -p beava-server --bench phase13_3_apply` on Apple-M4; capture the four medians.
    - Append a new section "### Phase 13.3 — lockless apply (RefCell + LocalSet)" to `.planning/perf-baselines.md` under the Apple-M4 hw-class.
    - Each row: `| bench_id | median | date | phase | notes |`.
    - If any of the four apply_internal medians exceeds 80 ns, mark with ⚠ and flag in VERIFICATION as a WARNING (NOT a BLOCKER per CONTEXT grey-area for sketches).
  </behavior>
  <subtasks>

    <subtask id="2.a" phase="red">
      <files>.planning/perf-baselines.md</files>
      <action>
        Write the "Phase 13.3" section header WITH PLACEHOLDER numbers (e.g., `TBD`). Fail the verify (a grep check: "TBD should not appear in a final row").

        Commit: `test(13.3-04): red — perf-baselines.md Phase 13.3 section skeleton`.
      </action>
      <verify>
        <automated>grep -c "TBD" .planning/perf-baselines.md</automated>
      </verify>
      <done>grep counts ≥ 4 TBDs. Section header exists.</done>
    </subtask>

    <subtask id="2.b" phase="green">
      <files>.planning/perf-baselines.md</files>
      <action>
        Run `cargo bench -p beava-server --bench phase13_3_apply` on Apple-M4 / Darwin 24.3.0 / 10 cores. Capture:
        - apply_internal/count median
        - apply_internal/sum median
        - apply_internal/ewma median
        - apply_via_actor/count median

        Replace TBDs with real numbers. Format: `| apply_internal/count | XX.X ns | 2026-04-24 | 13.3 | target ≤ 80 ns; actual XX.X ns; within target ✓ / above target ⚠ |`.

        If a result exceeds targets, add a bullet under the section calling it out and referencing VERIFICATION.md.

        Commit: `feat(13.3-04): green — perf-baselines.md Phase 13.3 rows captured`.
      </action>
      <verify>
        <automated>grep -c "TBD" .planning/perf-baselines.md && grep "Phase 13.3" .planning/perf-baselines.md | head -5</automated>
      </verify>
      <done>Zero TBDs in the Phase 13.3 section; all four rows have real numbers.</done>
    </subtask>

  </subtasks>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end throughput-run on quiescent Apple-M4 box</name>
  <what-built>
    - `cargo bench -p beava-server --bench phase13_3_apply` completed with rows captured in perf-baselines.md.
    - All Plan 01–03 code merged. `cargo test --workspace` green.
    - No other agent / heavy process running on the Apple-M4 box (required for clean throughput numbers per CLAUDE.md §Performance Discipline).
  </what-built>
  <how-to-verify>
    **Human runs the following by hand on Apple-M4 / Darwin 24.3.0 / 10 cores (quiescent):**

    ```bash
    cd /Users/petrpan26/work/tally
    cargo build --release -p beava-bench -p beava
    HW_CLASS="$(sysctl -n machdep.cpu.brand_string | tr ' ' '-') / $(uname -sr | tr ' ' '-') / $(sysctl -n hw.ncpu) cores"
    echo "hw-class: $HW_CLASS"

    # Six throughput rows at parallel=64:
    for size in small medium large; do
      for transport in http tcp; do
        ./target/release/beava-bench \
          --pipeline $size \
          --transport $transport \
          --duration-secs 60 \
          --parallel 64
        # stdout: markdown row — copy to throughput-baselines.md
      done
    done

    # BATCH_MS sweep on small/http:
    for batch_ms in 0 1 5 20; do
      BEAVA_WAL_FSYNC_INTERVAL_MS=$batch_ms ./target/release/beava-bench \
        --pipeline small --transport http --duration-secs 60 --parallel 64
    done
    ```

    Expected pattern per CONTEXT success criterion #3: at least one of the six cells shows ≥ 5× lift vs the post-merge baseline (small/http at 17k EPS from `.planning/throughput-baselines.md` "POST-MERGE quiescent rerun" section — note that was `--parallel 8`; the Phase 13.1 ceiling at `--parallel 64` is also ~17k per STATE.md).

    **Pasted rows** go into `.planning/throughput-baselines.md` under a new section "### Phase 13.3 — lockless apply (RefCell + LocalSet, parallel=64)" and "### Phase 13.3 — BATCH_MS sweep (small/http, parallel=64)".

    **User confirms:**
    1. All 10 rows captured (6 size×transport + 4 batch_ms).
    2. At least one cell shows ≥ 5× lift vs Phase 13.1 baseline. (If not, see contingency in CONTEXT.)
    3. No run returned errors / timeouts / crash.
    4. Peak RSS is reasonable (linear in feature count, same shape as prior phases).
  </how-to-verify>
  <resume-signal>Reply with "approved" + paste the 10 markdown rows, OR describe the numbers and Claude will assemble and write them.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Append throughput ledger rows + write SUMMARY + VERIFICATION</name>
  <files>.planning/throughput-baselines.md, .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md, .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md</files>
  <behavior>
    - 10 rows appended to throughput-baselines.md in a new Phase 13.3 section.
    - 13.3-SUMMARY.md written per GSD phase-summary template: objective recap, plan breakdown, artifacts created, key decisions during execution, ledger delta table comparing Phase 13.1 vs Phase 13.3, forward-look sentence for Phase 13 ship gate.
    - 13.3-VERIFICATION.md written: row per CONTEXT success criterion (6 rows), status (passed/partial/failed), evidence link.
  </behavior>
  <subtasks>

    <subtask id="4.a" phase="red">
      <files>.planning/throughput-baselines.md, .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md, .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md</files>
      <action>
        Write the three files with explicit TBD placeholders in every row/section that depends on Task 3's numbers. Include the table structure (columns per existing ledger format in throughput-baselines.md) and the 6 success-criterion rows (SC1–SC6 from CONTEXT) in VERIFICATION.md.

        SUMMARY includes a ledger-delta-table template:

        | Cell | Phase 13.1 EPS | Phase 13.3 EPS | Lift |
        |---|---:|---:|---:|
        | small/http | TBD | TBD | TBD× |
        | ... (6 cells) | | | |

        Commit: `test(13.3-04): red — throughput ledger + SUMMARY + VERIFICATION skeletons`.
      </action>
      <verify>
        <automated>ls .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md && grep -c "TBD" .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md .planning/throughput-baselines.md</automated>
      </verify>
      <done>Three files exist; each has ≥ 6 TBDs.</done>
    </subtask>

    <subtask id="4.b" phase="green">
      <files>.planning/throughput-baselines.md, .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md, .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md</files>
      <action>
        After Task 3 human-verify returns approved with the 10 rows:
        1. Replace TBDs in throughput-baselines.md with real rows under `## hw-class: Apple-M4 / Darwin-24.3.0 / 10 cores` → new section `### Phase 13.3 — lockless apply (RefCell + LocalSet, parallel=64)` (6 rows) + `### Phase 13.3 — BATCH_MS sweep (small/http, parallel=64)` (4 rows).
        2. Populate SUMMARY ledger-delta table from throughput-baselines.md (Phase 13.1 baseline rows already exist; cross-reference).
        3. Populate VERIFICATION.md rows — each SC gets status:
           - SC1 (grep check) → evidence line with the grep command output (0 hits confirmed).
           - SC2 (≤ 80 ns apply_internal/count) → evidence line with actual ns from perf-baselines.md.
           - SC3 (≥ 5× throughput lift on at least one cell) → evidence line with which cell + the lift ratio.
           - SC4 (RYW preserved) → evidence link to phase13_3_push_cutover_test::push_ryw_preserved_via_actor (commit SHA).
           - SC5 (workspace tests green, clippy clean, fmt clean) → evidence command + output.
           - SC6 (regression rows appended) → evidence: the ledger section exists; lists 10 rows.
        4. Set overall `status: passed` if all 6 are passed; `status: partial` if any WARNING; `status: failed` if any BLOCKER.
        5. Forward-look sentence in SUMMARY: "Phase 13.3 completes the apply-Mutex removal. Next: Phase 13 ship gate re-runs the three-shape perf benchmark on this foundation; if single-actor throughput proves inadequate for ≥ 3M EPS/core, Phase 13.4 introduces atomic-slot specialization (Option A) or sharded actors (Option B) per the original idea doc."

        Commit: `docs(13.3-04): green — throughput-baselines + SUMMARY + VERIFICATION finalized`.
      </action>
      <verify>
        <automated>grep -c "TBD" .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md .planning/throughput-baselines.md && grep "status:" .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md</automated>
      </verify>
      <done>Zero TBDs; status line present.</done>
    </subtask>

  </subtasks>
</task>

</tasks>

<verification>
Phase-closing verification:

1. `cargo bench -p beava-server --bench phase13_3_apply` runs clean; results match perf-baselines.md.
2. `.planning/perf-baselines.md` has 4 new Phase 13.3 rows under Apple-M4.
3. `.planning/throughput-baselines.md` has 10 new rows under Phase 13.3.
4. `.planning/phases/13.3-lockless-apply/13.3-SUMMARY.md` exists, ≤ 200 lines, includes ledger delta table.
5. `.planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md` exists, all 6 SCs addressed, status line set.
6. No TBDs remaining in any committed file.
7. `cargo test --workspace` green, `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean, `cargo fmt --all --check` clean.
8. `grep -rn "\.lock()" crates/beava-server/src/ --include='*.rs' | grep -v "idem_cache\|register\.rs\|test\|//"` → 0 lines.
</verification>

<success_criteria>
- Criterion microbench measures per-event apply cost; rows in perf-baselines.md.
- 10 throughput rows in throughput-baselines.md (6 size×transport + 4 BATCH_MS).
- At least one cell shows ≥ 5× lift vs Phase 13.1 baseline (CONTEXT SC3).
- Per-event apply_internal/count ≤ 80 ns (CONTEXT SC2).
- SUMMARY + VERIFICATION written.
- Phase 13.3 ledger-complete and ready for Phase 13 ship-gate to consume.
</success_criteria>

<output>
Final artifacts in `.planning/phases/13.3-lockless-apply/`:
- 13.3-CONTEXT.md (written at plan-time)
- 13.3-01-PLAN.md, 13.3-02-PLAN.md, 13.3-03-PLAN.md, 13.3-04-PLAN.md (plans)
- 13.3-01-SUMMARY.md, 13.3-02-SUMMARY.md, 13.3-03-SUMMARY.md (per-plan summaries)
- 13.3-SUMMARY.md (phase summary)
- 13.3-VERIFICATION.md (phase verification)

Plus ledger updates at `.planning/perf-baselines.md` and `.planning/throughput-baselines.md`.

The phase is ready for merge + STATE.md update when VERIFICATION status = passed.
</output>
