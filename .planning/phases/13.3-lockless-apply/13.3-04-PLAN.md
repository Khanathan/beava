---
phase: 13.3-lockless-apply
plan: 04
type: execute
wave: 4
depends_on: [13.3-03]
files_modified:
  - crates/beava-server/benches/phase13_3_apply.rs
  - crates/beava-server/Cargo.toml
  - .planning/perf-baselines.md
  - .planning/throughput-baselines.md
  - .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md
  - .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md
autonomous: false
requirements:
  - PERF-APPLY-LOCKLESS-11  # criterion microbench for per-event apply cost
  - PERF-APPLY-LOCKLESS-12  # throughput-run regression rows
  - PERF-APPLY-LOCKLESS-13  # phase SUMMARY + VERIFICATION

must_haves:
  truths:
    - "Criterion microbench `phase13_3_apply` exists at `crates/beava-server/benches/phase13_3_apply.rs`, measuring per-event `state.borrow_mut().apply_event(...)` cost for 5 op families (count, sum, avg, ewma, hll)."
    - "On Apple-M4 / Darwin 24.3.0, scalar-op medians (count, sum, avg) are ≤ 80 ns — BLOCKER if missed."
    - "Decay-op median (ewma) is ≤ 200 ns; sketch-op median (hll) is ≤ 400 ns — WARNING if missed, not BLOCKER."
    - "Throughput-run rows appended to `.planning/throughput-baselines.md` for (small, medium, large) × (http, tcp) at parallel=64 AND a BATCH_MS sweep (0, 1, 5, 20) on small/http — 10 rows total."
    - "At least one throughput cell shows ≥ 5× lift vs Phase 13.1 post-merge baseline (17k EPS at parallel=64 on small/http) — BLOCKER if no cell hits 5×."
    - "`13.3-SUMMARY.md` exists with the ledger-delta table, decisions during execution, fairness review checklist (if Plan 03 fell back), and a forward-plan sentence for Phase 13 ship gate."
    - "`13.3-VERIFICATION.md` exists with one row per CONTEXT SC1–SC6, status (passed/partial/failed) plus evidence link per row; overall `status: passed` if all 6 pass."
  artifacts:
    - path: crates/beava-server/benches/phase13_3_apply.rs
      provides: "Criterion bench group `phase13_3_apply` with per-op-family measurements; direct `state.borrow_mut().apply_event(...)` hot path — zero channel, zero actor"
    - path: .planning/perf-baselines.md
      provides: "Phase 13.3 section under Apple-M4 hw-class with 5 op-family medians"
    - path: .planning/throughput-baselines.md
      provides: "Phase 13.3 section with 6 (size × transport) rows + 4 BATCH_MS sweep rows"
    - path: .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md
      provides: "Phase SUMMARY per GSD template"
    - path: .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md
      provides: "Phase VERIFICATION against SC1–SC6"
  key_links:
    - from: crates/beava-server/benches/phase13_3_apply.rs
      to: crates/beava-server/src/app_state.rs
      via: "bench body: state.borrow_mut().apply_event(event_name, row, event_time_ms, event_id)"
      pattern: "borrow_mut\\(\\)\\.apply_event"
    - from: .planning/throughput-baselines.md
      to: crates/beava-bench
      via: "harness CLI invocation documented in the human-verify checkpoint"
      pattern: "beava-bench"
---

<objective>
Measure. Ledger. Close the phase.

Produce the numbers that prove Phase 13.3 achieved its goal (≤ 80 ns scalar
apply cost; ≥ 5× throughput lift on at least one cell). Append rows to
`.planning/perf-baselines.md` and `.planning/throughput-baselines.md` per
CLAUDE.md §Performance Discipline. Write `13.3-SUMMARY.md` + `13.3-VERIFICATION.md`.

Task 3 is a HUMAN-VERIFY checkpoint because throughput numbers require a
quiescent Apple-M4 box and are not meaningful when captured mid-agent-load.

Output: criterion bench + 10 throughput rows + SUMMARY + VERIFICATION.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/13.3-lockless-apply/13.3-CONTEXT.md
@.planning/phases/13.3-lockless-apply/13.3-01-SUMMARY.md
@.planning/phases/13.3-lockless-apply/13.3-02-SUMMARY.md
@.planning/phases/13.3-lockless-apply/13.3-03-SUMMARY.md
@.planning/perf-baselines.md
@.planning/throughput-baselines.md

@crates/beava-server/benches/phase25_wire.rs  # reference pattern for criterion bench structure
@crates/beava-bench/Cargo.toml                  # reference for end-to-end harness invocation
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criterion microbench — direct borrow_mut apply cost per op family</name>
  <files>crates/beava-server/benches/phase13_3_apply.rs, crates/beava-server/Cargo.toml</files>
  <behavior>
    - Bench group `phase13_3_apply` with 5 benches:
      * `apply_direct/count` — single event into a count-only agg; target ≤ 80 ns.
      * `apply_direct/sum` — target ≤ 80 ns.
      * `apply_direct/avg` — target ≤ 80 ns.
      * `apply_direct/ewma` — target ≤ 200 ns.
      * `apply_direct/hll` — target ≤ 400 ns (WARNING if missed).
    - Each bench body is literally:
      ```rust
      b.iter(|| {
          let mut s = state.borrow_mut();
          s.apply_event(event_name, row.clone(), event_time_ms, event_id);
          event_id += 1;
      });
      ```
      — NO channel, NO actor, NO roundtrip. Pure `Rc<RefCell<AppState>>` borrow + apply.
    - All benches use `Throughput::Elements(1)` so Criterion reports elements/sec.
    - Fixtures built via deterministic helper functions; `registry` is constructed in-bench via `beava-core` APIs (no JSON parsing in the hot loop).
  </behavior>
  <subtasks>

    <subtask id="1.a" phase="red">
      <files>crates/beava-server/benches/phase13_3_apply.rs, crates/beava-server/Cargo.toml</files>
      <action>
        Create the bench file with `criterion_group!` + `criterion_main!` scaffolding, referencing helpers `build_state_with_count_agg` / `_sum` / `_avg` / `_ewma` / `_hll` that do NOT exist → compile error.

        Add a `[[bench]] name = "phase13_3_apply" harness = false` entry to `beava-server/Cargo.toml`.

        Commit: `test(13.3-04): red — phase13_3_apply criterion bench scaffold`.
      </action>
      <verify>
        <automated>cargo bench -p beava-server --bench phase13_3_apply --no-run 2>&1 | grep -E "(error\[|cannot find)" | head -10</automated>
      </verify>
      <done>Build fails with clear "function not found" errors for the helpers.</done>
    </subtask>

    <subtask id="1.b" phase="green">
      <files>crates/beava-server/benches/phase13_3_apply.rs</files>
      <action>
        Implement:
        - Five helper functions that each build a `Rc<RefCell<AppState>>` with a single-op registry (count / sum / avg / ewma / hll) and return `(Rc<RefCell<AppState>>, event_name: &'static str, sample_row: Row<Value>)`.
        - Each bench creates the state once (outside `b.iter`), then runs the body with `b.iter(|| { let mut s = state.borrow_mut(); s.apply_event(...); event_id += 1; })`.
        - `group.throughput(Throughput::Elements(1))` — Criterion reports elements/sec too.
        - NO actor, NO channel, NO LocalSet — this bench is measuring the pure synchronous hot path as called from a handler. (The tokio runtime cost is separate; it's not part of the 80 ns budget.)

        Commit: `feat(13.3-04): green — phase13_3_apply measures direct borrow_mut apply cost`.
      </action>
      <verify>
        <automated>cargo bench -p beava-server --bench phase13_3_apply 2>&1 | tail -40</automated>
      </verify>
      <done>All 5 benches execute; medians printed. Task 2 will ledger them.</done>
    </subtask>

  </subtasks>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append criterion baseline rows to .planning/perf-baselines.md</name>
  <files>.planning/perf-baselines.md</files>
  <behavior>
    - Run the bench on Apple-M4 / Darwin 24.3.0 / 10 cores; capture the 5 medians.
    - Append a new section "### Phase 13.3 — lockless apply (Rc<RefCell<AppState>>, direct borrow)" under the Apple-M4 hw-class.
    - Row format: `| bench_id | median | target | status | date | notes |`.
    - Scalar miss (count/sum/avg > 80 ns) → mark ⛔ BLOCKER and flag in VERIFICATION as SC2 failure.
    - Decay miss (ewma > 200 ns) → ⚠ WARNING.
    - Sketch miss (hll > 400 ns) → ⚠ WARNING per CONTEXT grey-area.
  </behavior>
  <subtasks>

    <subtask id="2.a" phase="red">
      <files>.planning/perf-baselines.md</files>
      <action>
        Add the Phase 13.3 section header + 5-row table with `TBD` in every median cell.

        Commit: `test(13.3-04): red — perf-baselines.md Phase 13.3 section skeleton`.
      </action>
      <verify>
        <automated>grep -c "TBD" .planning/perf-baselines.md</automated>
      </verify>
      <done>Grep counts ≥ 5 TBDs; section header exists.</done>
    </subtask>

    <subtask id="2.b" phase="green">
      <files>.planning/perf-baselines.md</files>
      <action>
        Run `cargo bench -p beava-server --bench phase13_3_apply` on Apple-M4. Capture medians and replace every TBD with a real number. Include the "target" column and mark status per above:
        ```
        | apply_direct/count | XX.X ns | ≤ 80 ns  | ✓ |
        | apply_direct/sum   | XX.X ns | ≤ 80 ns  | ✓ |
        | apply_direct/avg   | XX.X ns | ≤ 80 ns  | ✓ |
        | apply_direct/ewma  | XX.X ns | ≤ 200 ns | ✓ |
        | apply_direct/hll   | XX.X ns | ≤ 400 ns | ✓ |
        ```
        If a scalar row is above target, mark ⛔ and write a bullet below pointing to VERIFICATION.md SC2 failure.

        Commit: `feat(13.3-04): green — perf-baselines.md Phase 13.3 medians captured`.
      </action>
      <verify>
        <automated>grep -c "TBD" .planning/perf-baselines.md</automated>
      </verify>
      <done>Zero TBDs in the Phase 13.3 section.</done>
    </subtask>

  </subtasks>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end throughput run on quiescent Apple-M4 box</name>
  <what-built>
    - Criterion microbench `phase13_3_apply` medians captured and ledgered in `.planning/perf-baselines.md`.
    - All Plan 01–03 code merged. `cargo test --workspace` green. `cargo clippy` + `fmt` clean.
    - No other agent / heavy process running on the Apple-M4 box (required for clean throughput numbers per CLAUDE.md §Performance Discipline).
  </what-built>
  <how-to-verify>
    **Human runs the following by hand on Apple-M4 / Darwin 24.3.0 / 10 cores (quiescent):**

    ```bash
    cd /Users/petrpan26/work/tally
    cargo build --release -p beava-bench -p beava

    HW_CLASS="$(sysctl -n machdep.cpu.brand_string | tr ' ' '-') / $(uname -sr | tr ' ' '-') / $(sysctl -n hw.ncpu) cores"
    echo "hw-class: $HW_CLASS"

    # Six throughput rows at parallel=64:
    for size in small medium large; do
      for transport in http tcp; do
        ./target/release/beava-bench \
          --pipeline $size \
          --transport $transport \
          --duration-secs 60 \
          --parallel 64
        # stdout: markdown row — capture for throughput-baselines.md
      done
    done

    # BATCH_MS sweep on small/http:
    for batch_ms in 0 1 5 20; do
      BEAVA_WAL_FSYNC_INTERVAL_MS=$batch_ms ./target/release/beava-bench \
        --pipeline small --transport http --duration-secs 60 --parallel 64
    done
    ```

    **Expected per CONTEXT SC3:** at least ONE of the six (size × transport) cells shows
    ≥ 5× lift vs Phase 13.1 post-merge baseline (small/http at 17k EPS, parallel=64,
    per `.planning/throughput-baselines.md` / STATE.md).

    **User confirms:**
    1. All 10 rows captured (6 size×transport + 4 batch_ms).
    2. At least one cell shows ≥ 5× lift vs Phase 13.1 baseline (CONTEXT SC3). If not,
       STOP and discuss contingency — the Mutex was not the only bottleneck.
    3. No run returned errors / timeouts / crashes.
    4. Peak RSS is reasonable (linear in feature count, same shape as prior phases).
    5. No evidence of single-handler starvation (e.g., batch run completed without
       interrupting `/metrics` or `/health` polls).
  </how-to-verify>
  <resume-signal>Reply with "approved" + paste the 10 markdown rows, OR describe the numbers and Claude will assemble and write them.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Append throughput rows + write SUMMARY + VERIFICATION</name>
  <files>.planning/throughput-baselines.md, .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md, .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md</files>
  <behavior>
    - 10 rows appended to `.planning/throughput-baselines.md` under a new Phase 13.3 section.
    - `13.3-SUMMARY.md` per GSD phase-summary template: objective recap, plan breakdown, artifacts created, key decisions during execution (especially D-03 axum-on-LocalSet outcome and any Plan 03 lint-fallback), ledger delta table comparing Phase 13.1 vs Phase 13.3, fairness review checklist (carried over from Plan 03 if applicable), forward-look sentence for Phase 13 ship gate.
    - `13.3-VERIFICATION.md` written: one row per CONTEXT SC1–SC6, status, evidence.
  </behavior>
  <subtasks>

    <subtask id="4.a" phase="red">
      <files>.planning/throughput-baselines.md, .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md, .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md</files>
      <action>
        Write the three files with explicit TBD placeholders in every row/section that depends on Task 3 numbers. SUMMARY includes the ledger delta template:

        | Cell | Phase 13.1 EPS | Phase 13.3 EPS | Lift |
        |---|---:|---:|---:|
        | small/http  parallel=64 | TBD | TBD | TBD× |
        | small/tcp   parallel=64 | TBD | TBD | TBD× |
        | medium/http parallel=64 | TBD | TBD | TBD× |
        | medium/tcp  parallel=64 | TBD | TBD | TBD× |
        | large/http  parallel=64 | TBD | TBD | TBD× |
        | large/tcp   parallel=64 | TBD | TBD | TBD× |

        And the BATCH_MS sweep sub-table.

        VERIFICATION.md: SC1–SC6 rows with `status: TBD` and evidence placeholders.

        Commit: `test(13.3-04): red — throughput ledger + SUMMARY + VERIFICATION skeletons`.
      </action>
      <verify>
        <automated>ls .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md && grep -c "TBD" .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md .planning/throughput-baselines.md</automated>
      </verify>
      <done>Three files exist with ≥ 10 TBDs total.</done>
    </subtask>

    <subtask id="4.b" phase="green">
      <files>.planning/throughput-baselines.md, .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md, .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md</files>
      <action>
        After Task 3 human-verify returns "approved" with 10 rows:

        1. `throughput-baselines.md`: replace TBDs under `## hw-class: Apple-M4 / Darwin-24.3.0 / 10 cores` with two new sub-sections:
           - `### Phase 13.3 — lockless apply (Rc<RefCell<AppState>>, parallel=64)` (6 rows)
           - `### Phase 13.3 — BATCH_MS sweep (small/http, parallel=64)` (4 rows)

        2. `13.3-SUMMARY.md`: populate ledger delta table from the numbers; compute lift per cell; include:
           - fairness review checklist (if Plan 03 used the fallback)
           - "Decisions during execution" section with (a) D-03 axum-on-LocalSet outcome, (b) any surprise `.await` sites restructured during Plan 02, (c) Plan 03 lint vs checklist decision.
           - Forward-look: "Phase 13.3 completes the Mutex removal via direct Rc<RefCell<AppState>> + LocalSet. Next: Phase 13 ship gate re-runs the three-shape perf benchmark on this foundation. If single-thread throughput proves inadequate for ≥ 3M EPS/core Linux target, v0.1 considers atomic-slot specialization for commutative ops (HLL / count / sum)."

        3. `13.3-VERIFICATION.md`: fill SC1–SC6 evidence:
           - SC1 (no Mutex/RwLock/mpsc on apply path): grep command + output (0 hits).
           - SC2 (scalar apply ≤ 80 ns): perf-baselines.md row refs.
           - SC3 (≥ 5× throughput lift on ≥ 1 cell): ledger delta table refs.
           - SC4 (RYW preserved): test name + commit SHA.
           - SC5 (workspace tests green, clippy clean, fmt clean): command + output summary.
           - SC6 (regression rows appended): pointer to perf-baselines Phase 13.3 section + throughput-baselines Phase 13.3 section.
           - Overall `status: passed` / `partial` / `failed`.

        Commit: `docs(13.3-04): green — throughput ledger + SUMMARY + VERIFICATION finalized`.
      </action>
      <verify>
        <automated>grep -c "TBD" .planning/phases/13.3-lockless-apply/13.3-SUMMARY.md .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md .planning/throughput-baselines.md && grep "status:" .planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md</automated>
      </verify>
      <done>Zero TBDs in the three files; VERIFICATION status line present.</done>
    </subtask>

  </subtasks>
</task>

</tasks>

<verification>
Phase-closing verification:

1. `cargo bench -p beava-server --bench phase13_3_apply` runs clean; results match perf-baselines.md Phase 13.3 section.
2. `.planning/perf-baselines.md` has 5 new Phase 13.3 rows under Apple-M4.
3. `.planning/throughput-baselines.md` has 10 new rows under Phase 13.3.
4. `.planning/phases/13.3-lockless-apply/13.3-SUMMARY.md` exists, ≤ 250 lines, includes ledger delta table + fairness checklist.
5. `.planning/phases/13.3-lockless-apply/13.3-VERIFICATION.md` exists, SC1–SC6 addressed, overall status line set.
6. No TBDs remaining in any committed file.
7. `cargo test --workspace` green; `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean; `cargo fmt --all --check` clean.
8. `grep -rn "\.lock()" crates/beava-server/src/ --include='*.rs' | grep -v "idem_cache\|register\.rs\|test\|//"` → 0 production-path state locks.
</verification>

<success_criteria>
- Criterion microbench measures 5 op-family per-event apply cost; rows in perf-baselines.md.
- 10 throughput rows in throughput-baselines.md (6 size×transport + 4 BATCH_MS).
- At least one cell shows ≥ 5× lift vs Phase 13.1 baseline (CONTEXT SC3).
- Scalar apply medians ≤ 80 ns (CONTEXT SC2).
- SUMMARY + VERIFICATION written.
- Phase 13.3 ledger-complete; Phase 13 ship gate unblocked.
</success_criteria>

<output>
Final artifacts under `.planning/phases/13.3-lockless-apply/`:
- 13.3-CONTEXT.md (revised this session)
- 13.3-01-PLAN.md, 13.3-02-PLAN.md, 13.3-03-PLAN.md, 13.3-04-PLAN.md (revised this session)
- 13.3-01-SUMMARY.md, 13.3-02-SUMMARY.md, 13.3-03-SUMMARY.md (per-plan, created at execute-time)
- 13.3-SUMMARY.md (phase summary)
- 13.3-VERIFICATION.md (phase verification)

Plus ledger updates at `.planning/perf-baselines.md` and `.planning/throughput-baselines.md`.

The phase is ready for merge + STATE.md update when VERIFICATION status = passed.
</output>
