---
phase: 12.6-v0-surface-reduction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12.6-v0-surface-reduction/12.6-DEADCODE-REPORT.md
  - .cargo/config.toml         # only if needed to install rustdoc unused-warnings; planner discretion
autonomous: true
# requirements is INTENTIONALLY EMPTY: Plan 02 is a baselining-only doc plan that maps to a
# ROADMAP success criterion (#4 — cargo-deadcode threshold), NOT a REQUIREMENTS.md REQ-ID.
# The success criterion lives in ROADMAP §Phase 12.6 success criterion #4. See `addresses_decisions`
# below for the canonical citation. Frontmatter validators that reject empty `requirements: []`
# should treat this plan as a doc-only plan whose success-criterion linkage is via
# `addresses_decisions` (one of the canonical citation fields).
requirements: []
addresses_decisions:
  - "ROADMAP §Phase 12.6 success criterion #4: cargo-deadcode reports < N% dead code (set N during plan-phase based on baseline scan). This plan produces the baseline + threshold report consumed by Plan 13's VERIFICATION.md."
  - D-05           # Code-only scope — no docs sweep here; report covers code surface only
  - "Discretion: Dead-code threshold — planner picks `cargo-deadcode` % target during plan-phase based on baseline scan"
must_haves:
  truths:
    - "A reproducible deadcode scan produces a baseline reading for the workspace today"
    - "The report identifies which dead-code is expected to be cleaned by Plans 04–10 (axum kill, event-time strip, join/union removal) vs orthogonal noise"
    - "The report sets a numerical threshold target (post-12.6) that Plan 13's VERIFICATION.md can mechanically check"
    - "No actual code is deleted in this plan — it is read-only baselining"
  artifacts:
    - path: ".planning/phases/12.6-v0-surface-reduction/12.6-DEADCODE-REPORT.md"
      provides: "Pre-12.6 deadcode baseline + post-12.6 target threshold + which Plans cover which dead symbols"
      min_lines: 40
  key_links:
    - from: ".planning/phases/12.6-v0-surface-reduction/12.6-DEADCODE-REPORT.md"
      to: ".planning/phases/12.6-v0-surface-reduction/12.6-13-PLAN.md (VERIFICATION)"
      via: "Plan 13 reruns the same scan + asserts count ≤ target"
      pattern: "DEADCODE-REPORT"
---

<objective>
Produce the Phase 12.6 dead-code baseline report that Plan 13 (SUMMARY+VERIFICATION) will mechanically check against. This is a baselining-only plan: no code deletion. The output is a single Markdown report at `.planning/phases/12.6-v0-surface-reduction/12.6-DEADCODE-REPORT.md` containing:

1. Today's `cargo --warn` + clippy + (optional) `cargo-machete` / `cargo-udeps` raw output across the workspace.
2. A categorized table mapping each dead symbol to the Phase 12.6 plan that will remove it (or marking it "orthogonal" if it lives outside 12.6 scope).
3. A numeric threshold (% or count) that the post-12.6 rerun must clear.

Purpose: ROADMAP success criterion #4 says "cargo-deadcode reports < N% dead code (set N during plan-phase based on baseline scan)". The CONTEXT discretion clause says "Dead-code threshold — planner picks during plan-phase based on baseline scan." This plan does that picking and freezes the threshold so Plan 13's verification is mechanical, not subjective.

**Why `requirements: []`:** Plan 02 is a doc-only plan that produces the input to a verification gate. It maps to a ROADMAP success criterion (a phase-scope acceptance bar), not a REQUIREMENTS.md REQ-ID (a product-feature spec). The empty list is intentional; the canonical linkage citation lives in the `addresses_decisions` frontmatter field.

Output:
- `.planning/phases/12.6-v0-surface-reduction/12.6-DEADCODE-REPORT.md` (committed; consumed by Plan 13).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.6-v0-surface-reduction/12.6-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (red): Failing assertion that 12.6-DEADCODE-REPORT.md exists</name>
  <files>.planning/phases/12.6-v0-surface-reduction/12.6-DEADCODE-REPORT.md (must NOT yet exist when this task runs)</files>
  <read_first>
    - .planning/phases/12.6-v0-surface-reduction/12.6-CONTEXT.md (Discretion clause on dead-code threshold)
    - .planning/ROADMAP.md lines 469 (success criterion #4)
  </read_first>
  <behavior>
    - Test target: a CLI-shaped assertion run from the project root: `[ ! -e .planning/phases/12.6-v0-surface-reduction/12.6-DEADCODE-REPORT.md ] && echo "RED: report missing"`. Treat the bash check as the red-form for a doc-output plan.
  </behavior>
  <action>
    Bash one-liner to confirm the report does not yet exist BEFORE Task 1.b runs. This is the doc-equivalent of a failing test. No commit — this is just the start-of-plan invariant.

    Run: `ls .planning/phases/12.6-v0-surface-reduction/12.6-DEADCODE-REPORT.md 2>&1 | grep -E "No such file|cannot access"` and confirm output. (If the file exists from a prior run, delete it via `rm` — RED requires the file absent.)
  </action>
  <verify>
    <automated>! ls .planning/phases/12.6-v0-surface-reduction/12.6-DEADCODE-REPORT.md 2>/dev/null</automated>
  </verify>
  <done>Report file does not exist (verified by negation `!` of `ls`). Proceed to Task 1.b.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (green): Run baseline deadcode scan + write threshold-setting report</name>
  <files>.planning/phases/12.6-v0-surface-reduction/12.6-DEADCODE-REPORT.md</files>
  <read_first>
    - .planning/phases/12.6-v0-surface-reduction/12.6-CONTEXT.md (the `<domain>` block — Bucket 7 dead-code-and-redundancy sweep — for what to scope in/out of this baseline)
    - .planning/STATE.md (lines 195–215 — known dead worktrees, abandoned phase-13.2 spike)
    - .planning/ROADMAP.md (success criterion #4 phrasing)
  </read_first>
  <action>
    1. Run the workspace-level dead-code scan with the existing toolchain (no new dev-deps required):
       ```bash
       cargo clippy --workspace --all-targets --all-features -- -W dead_code -W unused_imports -W unused_variables -W unused_mut 2>&1 | tee /tmp/12.6-deadcode-baseline.txt
       grep -E "warning:.*dead_code|warning:.*unused" /tmp/12.6-deadcode-baseline.txt | wc -l
       grep -E "warning:.*dead_code|warning:.*unused" /tmp/12.6-deadcode-baseline.txt > /tmp/12.6-deadcode-warnings.txt
       ```
       Use the warning count as the baseline metric. Optionally run `cargo machete` if installed (skip if not — discretion).

    2. Categorize each warning into one of:
       - **Will be killed by Plan 12.6-04 (joins/unions):** symbols mentioning `OpNode::Join`, `OpNode::Union`, `JoinType`, `join_type`, `within_ms`, schema_propagate.rs join/union branches.
       - **Will be killed by Plan 12.6-05 (Path X — windowed-op time-source swap):** symbols mentioning `_event_time_ms` parameters that go away when the parameter is renamed/removed.
       - **Will be killed by Plan 12.6-06 (event-time wire schema strip):** `event_time_field`, `tolerate_delay_ms`, `DEFAULT_TOLERATE_DELAY_MS`, `max_event_time_ms`, `event_time_ms` on push wire.
       - **Will be killed by Plan 12.6-07 (legacy axum kill):** symbols in `push.rs`, `http.rs`, `http_admin.rs`, `runtime_core_glue::dispatch_wire_request` async path, `BEAVA_DEV_ENDPOINTS`, `dev_endpoints` flags, legacy `Server` struct + impl.
       - **Will be killed by Plan 12.6-08 (Python SDK strip):** any Rust-side warnings cross-referencing Python decorator parameters.
       - **Will be killed by Plan 12.6-10 (mio-only enforcement):** any leftover dual-path symbols.
       - **Orthogonal — survives 12.6:** dead code unrelated to the buckets above.

    3. Write `.planning/phases/12.6-v0-surface-reduction/12.6-DEADCODE-REPORT.md` with the following structure:

       ```markdown
       # Phase 12.6 Dead-Code Baseline Report

       **Generated:** YYYY-MM-DD
       **Scan tool:** `cargo clippy --workspace --all-targets --all-features -- -W dead_code -W unused_imports -W unused_variables -W unused_mut`
       **HEAD at scan:** `git rev-parse HEAD` → <hash>

       ## Headline numbers

       - Total warnings (dead_code + unused): N (BASELINE)
       - Of which slated for removal in Phase 12.6: M (= N - K_orthogonal)
       - Orthogonal warnings (survive 12.6): K
       - **Post-12.6 target threshold: ≤ K + 5 warnings** (small slack for new noise during impl)

       ## Per-plan removal map

       | Warning category | Count | Closing plan |
       |---|---|---|
       | OpNode::Join / OpNode::Union / JoinType | <count> | Plan 12.6-04 |
       | Path X _event_time_ms unused params | <count> | Plan 12.6-05 |
       | event_time_field / tolerate_delay_ms / max_event_time_ms | <count> | Plan 12.6-06 |
       | legacy axum (push.rs/http.rs/http_admin.rs/dispatch_wire_request async/BEAVA_DEV_ENDPOINTS) | <count> | Plan 12.6-07 |
       | Python SDK Rust-side echoes | <count> | Plan 12.6-08 |
       | mio-only architectural cleanup | <count> | Plan 12.6-10 |
       | Orthogonal (NOT closed by 12.6) | <count> | n/a — file as v0.1+ followups in STATE.md |

       ## Raw warning list (full output)

       <embed `/tmp/12.6-deadcode-warnings.txt` verbatim, code-fenced>

       ## Threshold rationale

       - Pre-12.6 baseline: N warnings.
       - Post-12.6 target: ≤ K + 5 warnings (orthogonal floor + 5-warning slack for impl noise).
       - Plan 12.6-13 (SUMMARY+VERIFICATION) re-runs the same `cargo clippy` invocation and asserts the count ≤ target.
       - If post-12.6 count > target: Plan 13 verification BLOCKS. If orthogonal warnings have grown organically, Plan 13 may amend K with explicit justification — but no silent slack.

       ## Orthogonal items (not closed by 12.6)

       <list each surviving warning with the file:line and a one-line note explaining why it's orthogonal — e.g., "phase-13.1-perf-fix import that's used in tests only"; flag any that look suspicious for a v0.1+ followup>
       ```

    4. Commit `chore(12.6-02): baseline dead-code scan + threshold for Phase 12.6 verification`.
  </action>
  <verify>
    <automated>test -f .planning/phases/12.6-v0-surface-reduction/12.6-DEADCODE-REPORT.md && grep -E "Post-12.6 target threshold:|Headline numbers|Per-plan removal map" .planning/phases/12.6-v0-surface-reduction/12.6-DEADCODE-REPORT.md | wc -l | grep -E "^[3-9]|^[1-9][0-9]"</automated>
  </verify>
  <done>Report file exists; contains the three required sections (Headline numbers / Per-plan removal map / Threshold rationale); the threshold number is recorded as a concrete integer; commit `chore(12.6-02): ...` lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Report → Plan 13 verification | Threshold is a hardcoded number that downstream plans depend on for pass/fail; if the report is wrong, verification is wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.6.02-01 | Tampering | Threshold gamed by under-counting orthogonal warnings | mitigate | Threshold derivation is shown in the report (formula: K + 5); reviewers can re-derive from the raw warning list embedded |
| T-12.6.02-02 | Repudiation | Baseline taken at HEAD that drifts before Plan 13 | mitigate | Report records exact HEAD hash at scan; Plan 13 re-runs from current HEAD and notes any orthogonal drift |
</threat_model>

<verification>
1. `.planning/phases/12.6-v0-surface-reduction/12.6-DEADCODE-REPORT.md` exists at HEAD.
2. The report records: baseline count N, target threshold K+5, per-plan removal map, raw warning list, threshold rationale.
3. `git log -1 --oneline -- .planning/phases/12.6-v0-surface-reduction/12.6-DEADCODE-REPORT.md` shows the `chore(12.6-02): ...` commit.
4. The report's per-plan removal map covers Plans 04, 05, 06, 07, 08, 10 (= every plan in 12.6 that deletes code).
</verification>

<success_criteria>
1. Report exists with all three required sections.
2. Threshold is a concrete integer (e.g., "≤ 18 warnings"), not a placeholder.
3. Per-plan removal map accounts for every dead_code/unused warning either as a Phase 12.6 plan target or as an orthogonal item.
4. Commit `chore(12.6-02): ...` lands.
</success_criteria>

<output>
After completion, create `.planning/phases/12.6-v0-surface-reduction/12.6-02-SUMMARY.md` covering:
- Baseline numbers (count and snapshot HEAD)
- Threshold target chosen + rationale
- Any surprises (warnings that didn't fit any plan's removal scope)
</output>
</content>
</invoke>