---
phase: 12.6-v0-surface-reduction
plan: 12
type: execute
wave: 7
depends_on: [11]
files_modified:
  - .planning/throughput-baselines.md
autonomous: true
requirements:
  # CLAUDE.md §Conventions §Performance Discipline §End-to-end throughput regression contract:
  # every phase from Phase 8 onward MUST include a "throughput run" task whose files_modified
  # includes .planning/throughput-baselines.md
addresses_decisions:
  - "Project conventions §Performance Discipline §End-to-end throughput regression contract (CLAUDE.md): Phase 8+ must run beava-bench against small/medium/large pipelines + fraud-team.json zipfian"
must_haves:
  truths:
    - "beava-bench runs successfully against small/medium/large pipelines + fraud-team.json zipfian shape (post-12.6 codebase)"
    - "Each (pipeline-shape, transport) tuple produces an EPS measurement appended to .planning/throughput-baselines.md"
    - "Comparison vs the most recent prior baseline (post-Plan-12-09) on the simple-fraud (small) shape is computed and logged with a Verdict (PASS / WARN / BLOCK)"
    - "10% slower than prior simple-fraud baseline = WARN; 25% slower = BLOCK (Phase 12.6 verification fails)"
  artifacts:
    - path: ".planning/throughput-baselines.md"
      provides: "Phase 12.6 throughput row appended for each (pipeline-shape, transport) tuple"
      contains: "Phase 12.6"
  key_links:
    - from: ".planning/throughput-baselines.md (Phase 12.6 section)"
      to: ".planning/throughput-baselines.md (post-Plan-12-09 section)"
      via: "comparison rows for the same hw-class cite both"
      pattern: "Phase 12.6|post-Plan-12-09"
---

<objective>
Run the end-to-end throughput harness (beava-bench) against the post-12.6 codebase. This satisfies CLAUDE.md §Conventions §Performance Discipline §End-to-end throughput regression contract: every Phase 8+ ships a throughput run + appends to `.planning/throughput-baselines.md`.

The bench matrix (canonical Phase 8+ shape):
- **small** = simple-fraud (1 feature, 1 entity, 1 window) — the **regression-gate cell**
- **medium** = 5 features
- **large** = 15 features
- **fraud-team.json zipfian** = primary tuning benchmark per `project_fraud_team_primary_bench` (locked 2026-04-27)
- **transports:** HTTP, TCP

Each cell produces (EPS, P50/P95/P99 push latency, P99 batch-get latency, RSS at peak) per the harness's structured output (per THROUGHPUT-HARNESS-01 in REQUIREMENTS.md).

Comparison: simple-fraud TCP from the most recent prior baseline (post-Plan-12-09 per STATE.md line 116; or post-Plan-12-08 if 12-09's row got rebaselined into 19.4). Look in `.planning/throughput-baselines.md` for the most recent "simple-fraud TCP" row labeled with a Phase tag.

Wave 7 placement: depends on Plan 11 (microbench landed first; throughput is the system-level signal that confirms the microbench's measurement isn't drowned by orthogonal regressions). Plan 12 runs LAST in code-impact terms; Plan 13 (SUMMARY+VERIFICATION) consumes both Plan 11's perf-baselines row and Plan 12's throughput-baselines row.

Output:
- `.planning/throughput-baselines.md` — appended `## Phase 12.6 — Post-axum-kill throughput rebaseline` section with rows for each (pipeline-shape, transport) tuple
- Verdict per cell against the simple-fraud TCP regression gate (PASS / WARN / BLOCK)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.6-v0-surface-reduction/12.6-CONTEXT.md
@.planning/phases/12.6-v0-surface-reduction/12.6-11-SUMMARY.md
@CLAUDE.md
@.planning/throughput-baselines.md
@crates/beava-bench/configs                       # ls + read each .json shape
@crates/beava-bench/scripts                       # entry-point invocation reference
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (red): Verify beava-bench is functional + the prior baseline row exists for comparison</name>
  <files>scratch — pre-flight check</files>
  <read_first>
    - .planning/throughput-baselines.md (find the most recent simple-fraud TCP row + its Phase tag)
    - crates/beava-bench/configs/ (ls)
    - crates/beava-bench/Cargo.toml (find the bin entry; the harness binary name)
  </read_first>
  <behavior>
    - Pre-flight: assert the harness binary runs and the prior baseline row exists for comparison.
  </behavior>
  <action>
    Step 1 — Confirm harness builds:
      cargo build -p beava-bench --release 2>&1 | tail -5

    Step 2 — Confirm prior baseline row exists. From STATE.md and CLAUDE.md context, the most recent prior baseline is post-Plan-12-09 OR post-Plan-12-07 (per STATE.md line 116: "small/tcp 694,144 EPS post-12-07 vs 642,760 EPS post-19.4 = +8.0% (PASS)").
      grep -E "^.* [0-9,]+ EPS .* simple-fraud .* tcp" .planning/throughput-baselines.md | tail -5
    Capture the most recent number to /tmp/12.6-12-prior-baseline.txt.

    Step 3 — Confirm fraud-team.json config exists:
      ls crates/beava-bench/configs/fraud-team.json

    No commit — pre-flight only.
  </action>
  <verify>
    <automated>cargo build -p beava-bench --release 2>&1 | tail -3 ; ls crates/beava-bench/configs/fraud-team.json crates/beava-bench/configs/*.json 2>/dev/null</automated>
  </verify>
  <done>beava-bench builds; fraud-team.json exists; prior baseline row identified. Proceed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (green): Run beava-bench across the matrix; append Phase 12.6 rows to throughput-baselines.md</name>
  <files>.planning/throughput-baselines.md</files>
  <read_first>
    - crates/beava-bench/scripts/ (find the canonical invocation script — e.g., run-bench.sh or similar)
    - .planning/throughput-baselines.md last 50 lines (existing row format reference)
  </read_first>
  <action>
    Step 1 — Build the release binary:
      cargo build --release -p beava-server -p beava-bench

    Step 2 — Run the matrix. Per STATE.md line 116 and CLAUDE.md §Performance Discipline, the canonical shape is:
      ./target/release/beava-bench \
          --pipeline crates/beava-bench/configs/<config>.json \
          --transport <http|tcp> \
          --total-events 1_000_000 \
          --parallel 16 \
          --pipeline-depth 256 \
          --duration-secs 30 \
          --continuous-pipeline=true

    For each cell, capture: EPS, P50, P95, P99 push, P99 batch-get, RSS at peak.

    Cells:
    - simple-fraud × http
    - simple-fraud × tcp
    - medium × http
    - medium × tcp
    - large × http
    - large × tcp
    - fraud-team-json-zipfian × http
    - fraud-team-json-zipfian × tcp

    Save raw output to /tmp/12.6-12-matrix.txt.

    Step 3 — Append to .planning/throughput-baselines.md under a new section header:

    ```markdown
    ## Phase 12.6 — Post-axum-kill throughput rebaseline (Apple-M4 / 16 GiB / continuous pipeline / p=16/pd=256)

    **HEAD at measurement:** `<git rev-parse HEAD>`
    **Date:** YYYY-MM-DD
    **Beava binary:** target/release/beava (ServerV18, mio data plane only)

    | pipeline-shape | transport | EPS | P50 push | P99 push | P99 batch-get | RSS peak | Prior simple-fraud baseline (post-Plan-12-09) | Verdict |
    |---|---|---|---|---|---|---|---|---|
    | simple-fraud | tcp | <eps> | <p50> | <p99> | <p99-get> | <rss> | <prior-eps> | <PASS/WARN/BLOCK> |
    | simple-fraud | http | ... | ... | ... | ... | ... | n/a | n/a |
    | medium | tcp | ... | ... | ... | ... | ... | n/a | n/a |
    | medium | http | ... | ... | ... | ... | ... | n/a | n/a |
    | large | tcp | ... | ... | ... | ... | ... | n/a | n/a |
    | large | http | ... | ... | ... | ... | ... | n/a | n/a |
    | fraud-team-zipfian | tcp | ... | ... | ... | ... | ... | n/a | n/a |
    | fraud-team-zipfian | http | ... | ... | ... | ... | ... | n/a | n/a |

    **Verdict thresholds (CLAUDE.md §Performance Discipline §End-to-end throughput regression contract):**
    - simple-fraud TCP <90% of prior baseline → WARN (must investigate before Phase 13)
    - simple-fraud TCP <75% of prior baseline → BLOCK (phase verification fails)
    - All other cells: informational; no block threshold (per the contract: only the simple-fraud (small) shape's TCP cell is the gate)

    **Notes:**
    - Phase 12.6 net code change vs post-Plan-12-09: legacy axum data plane deleted, event-time field stripped from wire, windowed-op time source switched to server now_ms() (Path X), joins/unions DAG nodes deleted, ~3500 LOC removed.
    - Expected: small lift on simple-fraud TCP from removed legacy code (no longer compiled in the binary, fewer instructions in icache); possible +0–5% EPS. SystemTime::now() syscall on every push could add ~10-30 ns; with apply at ~500 ns mean, that's ~2-6% headwind. Net: roughly neutral, with high run-to-run variance band per `project_phase19_bench_wallclock_fix`.
    ```

    Step 4 — If simple-fraud TCP shows >25% regression: STOP, surface to user, do NOT proceed to Plan 13. Diagnose first.

    Step 5 — Commit `feat(12.6-12): Phase 12.6 throughput rebaseline — small/medium/large + fraud-team-zipfian × http+tcp`.
  </action>
  <verify>
    <automated>grep -c "## Phase 12.6 — Post-axum-kill throughput rebaseline" .planning/throughput-baselines.md | grep -E "^[1-9]" ; grep -E "simple-fraud.*tcp" .planning/throughput-baselines.md | tail -3</automated>
  </verify>
  <done>throughput-baselines.md has the new Phase 12.6 section with all 8 (shape, transport) cells populated. The simple-fraud TCP cell has a Verdict (PASS/WARN/BLOCK). If WARN/BLOCK, the SUMMARY documents the diagnosis. Commit lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Throughput numbers → Plan 13 verification | Plan 13's VERIFICATION.md re-reads this row and asserts the verdict |
| Bench harness driving production binary → real apply path | The bench measures the actual ServerV18 production binary, not a mocked/stubbed path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.6.12-01 | Tampering | Bench run on noisy box (background load) → false WARN | mitigate | SUMMARY notes system-load context per Plan 12-09's existing convention; if WARN suspected to be load-noise, executor reruns 3× and uses median |
| T-12.6.12-02 | Repudiation | Multiple Phase-12.6 throughput runs land out-of-order, prior baseline pointer lost | accept | Each row records HEAD hash at measurement; baseline.md is append-only (per THROUGHPUT-HARNESS-02) |
</threat_model>

<verification>
1. `.planning/throughput-baselines.md` has the "## Phase 12.6 — Post-axum-kill throughput rebaseline" section with 8 rows.
2. Simple-fraud TCP row has a Verdict (PASS / WARN / BLOCK).
3. If BLOCK: Phase 12.6 verification fails; the executor stops, surfaces, and does NOT advance to Plan 13.
4. fraud-team-zipfian row exists for both http and tcp (per `project_fraud_team_primary_bench` 2026-04-27 lock).
5. One commit lands: feat(12.6-12) throughput rebaseline.
</verification>

<success_criteria>
1. Throughput rebaseline matrix complete (8 cells) and appended to throughput-baselines.md.
2. Simple-fraud TCP regression gate satisfied (PASS or documented WARN with diagnosis).
3. CLAUDE.md §Conventions §End-to-end throughput regression contract satisfied (Phase 12.6 has a throughput-run task touching .planning/throughput-baselines.md).
4. fraud-team.json zipfian cells captured (primary tuning bench per the locked memory).
</success_criteria>

<output>
After completion, create `.planning/phases/12.6-v0-surface-reduction/12.6-12-SUMMARY.md` covering:
- All 8 cell results (verbatim from throughput-baselines.md row)
- Simple-fraud TCP Verdict + rationale
- Any cells outside the regression-gate (medium/large/fraud-team) that look anomalous → flag for Phase 13 investigation
- System-load context (uptime / load avg at run time) — replicates the Plan 12-09 honesty pattern about quiescent-vs-noisy boxes
</output>
