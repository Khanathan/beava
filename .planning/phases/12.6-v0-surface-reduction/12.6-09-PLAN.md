---
phase: 12.6-v0-surface-reduction
plan: 09
type: execute
wave: 4
depends_on: [04, 07]
files_modified:
  - .planning/phases/12.5-push-and-get/12.5-CONTEXT.md           # add SUPERSEDED-AND-DEFERRED banner
  - .planning/phases/12.5-push-and-get/12.5-01-PLAN.md           # add SUPERSEDED banner
  - .planning/phases/12.5-push-and-get/12.5-02-PLAN.md
  - .planning/phases/12.5-push-and-get/12.5-03-PLAN.md
  # SECTION OWNERSHIP for shared docs (BLOCKER #2 collision avoidance with Plan 13):
  # Plan 09 owns ONLY: Phase 12.5 entry header (rewrite to ARCHIVED-AND-DEFERRED) + worktree status block (Phase 13.3 archived + phase-12-joins ABANDONED + phase-13.2-followup ABANDONED + phase-15 ARCHIVED + phase-16 deferred)
  # Plan 09 does NOT touch: Phase 12.6 entry header, v0 critical-path block, Phase 13.3 worktree map (already documented per STATE.md line 213)
  - .planning/STATE.md                                            # SECTION: worktree status block ONLY — see ownership note above
  - .planning/ROADMAP.md                                          # SECTION: Phase 12.5 entry ONLY — see ownership note above
autonomous: true
requirements:
  # No code REQ-IDs; this plan handles archive logistics that the doc-state requires
addresses_decisions:
  - D-04                  # Plan 12-10 push-and-get stays deferred entirely; Phase 12.5 dir gets SUPERSEDED-AND-DEFERRED banner
  - "Discretion: phase-12-joins branch handling — abandon vs cherry-pick survivors"
  - "Discretion: dead worktree archive paths — phase-13.3-lockless-apply rename target"
must_haves:
  truths:
    - "Phase 12.5 directory carries a SUPERSEDED-AND-DEFERRED banner at the top of its CONTEXT and PLAN files"
    - "STATE.md reflects the post-12.6 worktree map (which worktrees still active, which archived, which abandoned) — Plan 09 owns the worktree status block; Plan 13 owns the phase-progress block (no overlap)"
    - "ROADMAP.md Phase 12.5 entry is marked ARCHIVED-AND-DEFERRED with a pointer to Plan 12-10's deferred state — Plan 09 owns the Phase 12.5 entry; Plan 13 owns the Phase 12.6 entry + critical-path block (no overlap)"
    - "phase-13.3-lockless-apply worktree status is documented (already deleted per STATE.md line 213)"
    - "phase-12-joins branch fate is documented (planner decision: abandon — joins are dead architecture)"
    - "Dead phase-followup worktrees that have either merged or been abandoned are flagged in STATE.md worktree-status block"
  artifacts:
    - path: ".planning/phases/12.5-push-and-get/12.5-CONTEXT.md"
      provides: "SUPERSEDED-AND-DEFERRED banner; clear pointer to Plan 12-10 (deferred per D-04)"
      contains: "SUPERSEDED-AND-DEFERRED"
    - path: ".planning/STATE.md"
      provides: "Post-12.6 worktree status block (BOUNDED EDIT — only the worktree-status section, NOT the phase-progress block)"
      contains: "phase-12-joins"
  key_links:
    - from: ".planning/phases/12.5-push-and-get/12.5-CONTEXT.md"
      to: ".planning/phases/12-server-side-async-push-coalescing/12-10-PLAN.md"
      via: "Banner cites Plan 12-10's deferred-from-v0 state per D-04"
      pattern: "12-10-PLAN"
---

<objective>
Archive housekeeping: per CONTEXT D-04 and the discretion clauses, this plan handles the "Sweep `phase-13.3-lockless-apply` worktree archival" + "Sweep dead `phase-{N}-followup` worktrees" + "Phase 12.5 retirement" work. No code changes; only planning-doc and worktree-metadata updates.

**Section ownership for shared docs (BLOCKER #2 — collision avoidance with Plan 13):**

Plan 09 (Wave 4) and Plan 13 (Wave 8) both edit `.planning/STATE.md` and `.planning/ROADMAP.md`. To prevent collisions:

| Doc | Plan 09 owns (this plan) | Plan 13 owns (later wave) |
|---|---|---|
| `.planning/ROADMAP.md` | **Phase 12.5 entry header ONLY** — rewrite to "ARCHIVED-AND-DEFERRED 2026-04-30 …" | **Phase 12.6 entry header + v0 critical-path block ONLY** — mark Phase 12.6 ✅ COMPLETED + advance critical path from 12.6 → Phase 13 |
| `.planning/STATE.md` | **Worktree-status block ONLY** — phase-12-joins ABANDONED, phase-13.2-followup ABANDONED, phase-15 ARCHIVED, phase-16 deferred-to-Phase-13, phase-13.3 ARCHIVED-REJECTED (already noted line 213) | **Phase-progress block + "Current Focus" line ONLY** — Phase 12.6 marked COMPLETE, completed_phases incremented, last_updated bumped |

Plan 09 MUST NOT edit:
- ROADMAP.md Phase 12.6 entry (Plan 13's territory)
- ROADMAP.md v0 critical-path block (Plan 13's territory)
- STATE.md phase-progress block (Plan 13's territory)
- STATE.md "Current Focus" line (Plan 13's territory)
- STATE.md Phase 13.3 worktree map (already finalized line 213; do not re-touch)

Plan 13 MUST NOT edit:
- ROADMAP.md Phase 12.5 entry (Plan 09's territory; locked by Plan 09 commit)
- STATE.md worktree-status block (Plan 09's territory)
- Phase 12.5 dir banners (Plan 09's territory)

If a future revision needs to touch BOTH territories in one commit, it requires explicit user sign-off; this plan + Plan 13 do not.

Three sweeps:

1. **Phase 12.5 retirement (D-04 explicit):** Add a SUPERSEDED-AND-DEFERRED banner to the top of every file in `.planning/phases/12.5-push-and-get/`. The banner explains: Phase 12.5 was the original push-and-get scope; it was superseded by Plan 12-10 (in `.planning/phases/12-server-side-async-push-coalescing/12-10-PLAN.md`) which itself is now deferred from v0 per D-04. Users searching for push-and-get in v0 should know it's NOT shipping.

2. **phase-12-joins branch fate (planner discretion):** Per CONTEXT, the discretion clause says: "Planner decides during plan-phase whether to merge non-join plans, cherry-pick the survivors, or abandon the entire worktree." Decision: **ABANDON**. Rationale:
   - Plan 04 of Phase 12.6 already deleted joins as architecture.
   - The phase-12-joins branch contains "Plan 12-02 (WAL replay for TableUpsert/Delete/Retract)" which is non-join work — but per STATE.md line 182 that work is gated behind the joins plumbing on the same branch, which is now dead.
   - The non-join plans 12-01/03/04/05/06 (push_sync, push_many, push_table, delete_table, set, mset, mget, get_multi) are tracked separately on `.claude/worktrees/phase-12-followup` (which is OFF phase-12-joins per STATE.md line 205). Plan 09 documents that the followup worktree is the future of those plans, NOT phase-12-joins.
   - Conclusion: phase-12-joins branch is fully abandoned; do NOT merge to v2/greenfield. STATE.md updates to mark it ABANDONED. The followup branch is the survivor.

3. **phase-13.3-lockless-apply worktree (planner discretion):** Per STATE.md line 213, the worktree was deleted on 2026-04-26 during repo cleanup. Plans 13.3-01..04 retained at `.planning/phases/13.3-lockless-apply/` for historical reference. Plan 09 confirms this status in STATE.md and adds an ARCHIVED-REJECTED banner to the planning files there (cite the architectural rejection 2026-04-26).

4. **Other dead phase-followup worktrees:** Audit `.claude/worktrees/`:
   - `phase-12-followup` (off phase-12-joins) — KEEP, but rebase off v2/greenfield post-12.6 (joins on the base branch are dead).
   - `phase-13-followup` (off phase-13-ship) — KEEP per STATE.md "Phase 13 follow-up" tracking.
   - `phase-13.2-followup` — STATE.md line 261 marks ABANDONED. Add banner.
   - `phase-13.1-perf-fix`, `phase-13-ship`, `phase-15-event-time-pit` — verify status; phase-15 is dead per archived 2026-04-30.
   - `phase-16-sdk-source-annotation` — STATE.md line 102 says Phase 16 reworked; verify worktree status.

Wave 4 placement: depends on Plans 04 (joins removal — needed for phase-12-joins ABANDON rationale) and 07 (axum kill — Phase 12.5 superseded posture is reinforced once legacy push_and_get.rs is gone). Independent of Plans 05, 06, 08, 10 — they touch different files.

Output:
- `.planning/phases/12.5-push-and-get/12.5-CONTEXT.md` + 12.5-01..03-PLAN.md prefixed with SUPERSEDED-AND-DEFERRED banner
- `.planning/phases/13.3-lockless-apply/` (if exists) prefixed with ARCHIVED-REJECTED banner
- `.planning/STATE.md` worktree status block updated (BOUNDED to that block; phase-progress block left alone for Plan 13)
- `.planning/ROADMAP.md` Phase 12.5 entry marked ARCHIVED-AND-DEFERRED (BOUNDED to Phase 12.5 entry; Phase 12.6 entry left alone for Plan 13)
- No code changes; no `git rm` of branches (left for end-of-Phase-13 lifecycle cleanup)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.6-v0-surface-reduction/12.6-CONTEXT.md
@.planning/phases/12.6-v0-surface-reduction/12.6-04-SUMMARY.md
@.planning/phases/12.6-v0-surface-reduction/12.6-07-SUMMARY.md
@.planning/phases/12.5-push-and-get/12.5-CONTEXT.md
@.planning/phases/12-server-side-async-push-coalescing/12-10-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (red): Audit assertion that Phase 12.5 dir lacks SUPERSEDED banner</name>
  <files>scratch — verification audit only</files>
  <read_first>
    - .planning/phases/12.5-push-and-get/12.5-CONTEXT.md
    - .planning/phases/12.5-push-and-get/12.5-01-PLAN.md (and 02, 03)
  </read_first>
  <behavior>
    - Audit assertion: every file in `.planning/phases/12.5-push-and-get/*.md` MUST contain `SUPERSEDED-AND-DEFERRED` near the top (within first 30 lines). Currently zero of them do.
  </behavior>
  <action>
    Run:
      grep -L "SUPERSEDED-AND-DEFERRED" .planning/phases/12.5-push-and-get/*.md > /tmp/12.6-09-missing-banner.txt
      cat /tmp/12.6-09-missing-banner.txt
    Expected: all 4 files (12.5-CONTEXT.md, 12.5-01-PLAN.md, 12.5-02-PLAN.md, 12.5-03-PLAN.md) listed as missing the banner. RED.

    Commit `test(12.6-09): RED — Phase 12.5 dir lacks SUPERSEDED-AND-DEFERRED banner`.
  </action>
  <verify>
    <automated>test -s /tmp/12.6-09-missing-banner.txt</automated>
  </verify>
  <done>The /tmp file is non-empty, listing the 4 missing-banner files. Commit lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (green): Add SUPERSEDED-AND-DEFERRED banners; update STATE.md (worktree-status block ONLY) and ROADMAP.md (Phase 12.5 entry ONLY)</name>
  <files>.planning/phases/12.5-push-and-get/12.5-CONTEXT.md, .planning/phases/12.5-push-and-get/12.5-01-PLAN.md, .planning/phases/12.5-push-and-get/12.5-02-PLAN.md, .planning/phases/12.5-push-and-get/12.5-03-PLAN.md, .planning/STATE.md (worktree-status block ONLY), .planning/ROADMAP.md (Phase 12.5 entry ONLY), .planning/phases/13.3-lockless-apply/ (if files exist there)</files>
  <read_first>
    - .planning/phases/12.5-push-and-get/12.5-CONTEXT.md
    - .planning/STATE.md (worktree status block; lines 178-220) — IDENTIFY THIS BLOCK PRECISELY before editing; do NOT touch the phase-progress block
    - .planning/ROADMAP.md (Phase 12.5 entry — find via `grep -n "Phase 12.5" .planning/ROADMAP.md`) — IDENTIFY THIS ENTRY PRECISELY before editing; do NOT touch the Phase 12.6 entry or v0 critical-path block
    - .planning/phases/13.3-lockless-apply/ ls (if directory exists)
  </read_first>
  <action>
    **Section-ownership reminder (BLOCKER #2 contract):** Plan 09 edits ONLY:
    - ROADMAP.md Phase 12.5 entry header (rewrite to ARCHIVED-AND-DEFERRED)
    - STATE.md worktree-status block (the lines 178-220 worktree map)

    Plan 09 MUST NOT edit:
    - ROADMAP.md Phase 12.6 entry (Plan 13's territory)
    - ROADMAP.md v0 critical-path block (Plan 13's territory)
    - STATE.md phase-progress block (Plan 13's territory)
    - STATE.md "Current Focus" line (Plan 13's territory)

    If your edit would touch any of those, STOP and surface to user.

    Step 1 — Banner template (use as the FIRST 8 lines of each Phase 12.5 file):

    ```markdown
    > **STATUS: SUPERSEDED-AND-DEFERRED (2026-04-30 / Phase 12.6)**
    >
    > Phase 12.5 (original push-and-get scope) was superseded by Plan 12-10
    > (`.planning/phases/12-server-side-async-push-coalescing/12-10-PLAN.md`).
    > Plan 12-10 itself is **DEFERRED entirely from v0** per Phase 12.6 CONTEXT D-04.
    > v0 ships without push-and-get. Users do 2 RTs (push then get).
    > Files in this directory are retained for historical reference only — do NOT execute.
    >
    > See `.planning/phases/12.6-v0-surface-reduction/12.6-CONTEXT.md` for the architectural decision.

    ---

    ```

    Insert this block at the top of each of the four files:
    - 12.5-CONTEXT.md
    - 12.5-01-PLAN.md
    - 12.5-02-PLAN.md
    - 12.5-03-PLAN.md

    Step 2 — STATE.md WORKTREE-STATUS-BLOCK update (BOUNDED EDIT). Find the "Pre-created worktrees" block (around line 257) and the "Shipped Partial — Awaiting Merge + Follow-up" table (around line 178). These are the worktree-status block.

    **Edit ONLY within the worktree-status block.** Do NOT touch the phase-progress block (the Phase 12.6 → 12.7 advance is Plan 13's responsibility).

    Append/edit within the worktree-status block:

    Worktree map post-12.6:
    - `phase-12-joins` (HEAD d541971) — **ABANDONED 2026-04-30 per Phase 12.6-09**. Joins removed permanently per `project_redis_shaped_no_event_time_ever`. Plan 12-02 (WAL replay for TableUpsert/Delete/Retract) is non-join work; if revived, it must be cherry-picked onto `v2/greenfield` directly, NOT merged from this branch.
    - `phase-12-followup` (off phase-12-joins) — **REBASE PENDING**. Off-branch dependency on `phase-12-joins` is now stale. Either rebase onto v2/greenfield (preferred) or abandon and recreate.
    - `phase-13.3-lockless-apply` — **ARCHIVED-REJECTED 2026-04-26** (already noted in STATE.md line 213). Phase 12.6-09 confirms.
    - `phase-13.2-followup` — **ABANDONED** per STATE.md line 261; banner added in Phase 12.6-09.
    - `phase-15-event-time-pit` — **ARCHIVED 2026-04-30** per no-event-time pivot. Worktree may stay on disk for historical reference; do not check out for new work.
    - `phase-16-sdk-source-annotation` — **REWORKED 2026-04-30** per pivot; worktree status is "needs reassessment" — defer to Phase 13.

    Step 3 — ROADMAP.md PHASE-12.5-ENTRY update (BOUNDED EDIT). Find via `grep -n "Phase 12.5" .planning/ROADMAP.md`.

    **Edit ONLY the Phase 12.5 entry header.** Do NOT touch the Phase 12.6 entry, the Phase 13 entry, or the v0 critical-path block (those are Plan 13's territory).

    Update the Phase 12.5 entry header to:

    ```
    ### Phase 12.5: push-and-get — ARCHIVED-AND-DEFERRED 2026-04-30

    > **Status:** ARCHIVED-AND-DEFERRED 2026-04-30 (Phase 12.6 retired this directory). Replaced by Plan 12-10 (`.planning/phases/12-server-side-async-push-coalescing/12-10-PLAN.md`) which is itself DEFERRED entirely from v0 per Phase 12.6 CONTEXT D-04. v0 ships without push-and-get.

    [original Phase 12.5 description preserved below for historical reference; do NOT execute]
    ```

    Step 4 — `.planning/phases/13.3-lockless-apply/` banners (if directory exists):
    Per STATE.md line 213, Phase 13.3 plans are retained for historical reference. Add a banner at the top of each .md file:

    ```markdown
    > **STATUS: ARCHIVED-REJECTED (2026-04-26)**
    >
    > Phase 13.3 (in-process apply sharding via lockless RefCell + LocalSet) is REJECTED.
    > Beava commits to single-threaded data plane forever per `project_no_sharded_apply`.
    > For aggregate throughput beyond per-instance ceiling, users run multiple Beava instances
    > sharded at entity-key level (Redis-cluster pattern).
    > Files retained for historical reference; do NOT execute.

    ---

    ```

    If the directory does not exist, skip step 4 (worktree was already deleted; no plans to banner).

    Step 5 — Verify section-ownership boundary held: re-read the Phase 12.6 entry in ROADMAP.md (must be unchanged) and the phase-progress block in STATE.md (must be unchanged). If you accidentally touched either, REVERT those edits before commit.

    Step 6 — Commit `docs(12.6-09): SUPERSEDED-AND-DEFERRED banners — Phase 12.5 + 13.3 archive sweep + STATE.md worktree map (Phase 12.6 entry + phase-progress untouched, owned by Plan 13)`.
  </action>
  <verify>
    <automated>! grep -L "SUPERSEDED-AND-DEFERRED" .planning/phases/12.5-push-and-get/*.md ; grep -c "ABANDONED 2026-04-30 per Phase 12.6-09" .planning/STATE.md | grep -E "^[1-9]"</automated>
  </verify>
  <done>All four Phase 12.5 files contain the SUPERSEDED banner; STATE.md has the post-12.6 worktree status block; ROADMAP.md Phase 12.5 entry is marked ARCHIVED-AND-DEFERRED. Phase 12.6 entry in ROADMAP.md is UNCHANGED (still says PLANNED — Plan 13 will mark it COMPLETED). STATE.md phase-progress block is UNCHANGED. Commit lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Old planning docs → future Claude executors who might re-execute Phase 12.5 plans | SUPERSEDED banner prevents accidental re-execution |
| STATE.md → /gsd-resume-work | Worktree map drives "what's next"; stale entries confuse the orchestrator |
| Plan 09 ↔ Plan 13 shared-doc edits | Section-ownership contract prevents merge collision when Plan 13 lands at Wave 8 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.6.09-01 | Tampering | A future agent re-executes Phase 12.5 plans without seeing the banner | mitigate | Banner is the first 8 lines of every file; orchestrator's mandatory_initial_read includes it |
| T-12.6.09-02 | Repudiation | Worktree fate decisions undocumented → confusion in 6 months | mitigate | STATE.md worktree map records each branch's status with a date and rationale |
| T-12.6.09-03 | Tampering | Plan 09 accidentally rewrites Phase 12.6 entry that Plan 13 expects pristine | mitigate | Section-ownership contract written into the plan; Step 5 explicitly re-reads boundaries before commit |
</threat_model>

<verification>
1. `.planning/phases/12.5-push-and-get/12.5-CONTEXT.md` first 10 lines contain "SUPERSEDED-AND-DEFERRED" + cite to Plan 12-10 + cite to Phase 12.6 D-04.
2. Same for 12.5-01-PLAN.md, 12.5-02-PLAN.md, 12.5-03-PLAN.md.
3. `.planning/STATE.md` contains the worktree status update with phase-12-joins ABANDONED + phase-13.2-followup ABANDONED.
4. `.planning/ROADMAP.md` Phase 12.5 entry header is updated to ARCHIVED-AND-DEFERRED.
5. `.planning/phases/13.3-lockless-apply/*.md` (if exists) contains ARCHIVED-REJECTED banner.
6. **Section-ownership invariant:** `.planning/ROADMAP.md` Phase 12.6 entry header is UNCHANGED (Plan 13 will edit it later); `.planning/STATE.md` phase-progress block is UNCHANGED (Plan 13 territory).
7. Two commits land in plan order: test(12.6-09) RED audit, docs(12.6-09) GREEN sweep.
</verification>

<success_criteria>
1. SUPERSEDED-AND-DEFERRED banner on every Phase 12.5 file (4 files).
2. STATE.md worktree map post-12.6: phase-12-joins ABANDONED, phase-13.2-followup ABANDONED, phase-15 ARCHIVED, phase-13.3 ARCHIVED-REJECTED (already), phase-16 deferred-to-Phase-13.
3. ROADMAP.md Phase 12.5 entry marked ARCHIVED-AND-DEFERRED. **Phase 12.6 entry untouched (Plan 13 territory).**
4. STATE.md worktree-status block edited; phase-progress block UNCHANGED (Plan 13 territory).
5. No code changes; no `git rm` of branches (lifecycle cleanup is end-of-milestone work).
</success_criteria>

<output>
After completion, create `.planning/phases/12.6-v0-surface-reduction/12.6-09-SUMMARY.md` covering:
- Each banner added (file + first line of banner) for verification
- The phase-12-joins ABANDON rationale paragraph (verbatim)
- Each worktree's status post-12.6 (in tabular form)
- Confirmation that section-ownership boundary held: Phase 12.6 ROADMAP entry untouched, STATE.md phase-progress block untouched (Plan 13 territory preserved)
- Any worktrees flagged for follow-up cleanup in Phase 13 (e.g., `git branch -D phase-12-joins` once everyone's confident)
</output>
</content>
</invoke>