---
phase: 12.6-v0-surface-reduction
plan: 15
type: execute
wave: 1.5
gap_closure: true
depends_on: [01]
files_modified:
  - crates/beava-server/tests/phase4_smoke.rs
  - crates/beava-server/tests/phase5_smoke.rs
  - crates/beava-server/tests/phase9_smoke.rs
  - crates/beava-server/tests/phase6_smoke.rs
  - crates/beava-server/tests/phase6_push.rs
  - crates/beava-server/tests/phase7_restart_cycle.rs
  - crates/beava-server/tests/phase8_tcp_push.rs
  - crates/beava-server/tests/wal_env_var_tunables.rs
  - crates/beava-server/tests/phase18_04_6_integration_test.rs
  # Plan 15 deviation — server-side functionality additions surfaced as Bucket A/C residuals
  # required modifying src/* files (auto-applied per Rule 1+2 in the executor prompt).
  - crates/beava-server/src/server.rs
  - crates/beava-server/src/runtime_core_glue.rs
  - crates/beava-server/src/apply_shard.rs
  - crates/beava-server/src/idem_cache.rs
  - crates/beava-server/src/testing.rs
  - crates/beava-runtime-core/src/tcp_listener.rs
  - crates/beava-runtime-core/src/work_ring.rs
  - crates/beava-runtime-core/src/io_thread_worker.rs
  # Test bins deleted (D-04 deferred / Plan 18-05/06 obsolete):
  # - crates/beava-server/tests/phase18_07_push_and_get_test.rs (deleted, D-04)
  # - crates/beava-server/tests/phase18_04_7_iopool_test.rs (deleted, observer counters dead post-Plan-18-05/06)
  # Cargo.toml updated to drop deleted test bin entries.
  - crates/beava-server/Cargo.toml
autonomous: true
requirements: []
# Plan 15 is a Wave-1 gap-closure plan; it satisfies plan 01's "All ~20 server tests using
# TestServer compile and pass" must-have via test-side migrations and per-test fixture
# adjustments. No REQUIREMENTS.md REQ-IDs — citation is via addresses_decisions.
addresses_decisions:
  - "Wave-1 gap closure: Plan 01 surfaced 18 broken test binaries on mio data plane after TestServer→ServerV18 swap. Plan 15 fixes the test-side residue (migrations to dispatch_*_sync white-box accessors, pattern-match updates, per-test fixture adjustments)."
  - "Per Plan 01 deferred-issues §A: tests hitting /dev/apply_events or /dev/apply_ops migrate to apply_shard::dispatch_wire_request_sync (no mio routes added — preserves the architectural commitment that dev endpoints are test-only)"
  - "Architectural commitment: project_phase18_no_dual_runtime — do NOT add a second hot-path entry; do NOT add /dev paths to mio production routing"
must_haves:
  truths:
    - "Tests using /dev/apply_events / /dev/apply_ops are migrated to call apply_shard::dispatch_wire_request_sync (or dispatch_wire_request_with_row) directly via the AppState handle"
    - "Tests asserting on Plan 01's collapsed GlueResponse::Register variant are updated to match the unified Register { http_status, body, tcp_op } shape"
    - "Per-test fixture adjustments fix wiring for: phase18_04_6 (instrumentation hook), phase18_04_7 (iopool wiring), phase18_07_push_and_get (mio dispatch), phase8_tcp_push (TCP wire shape), phase7_restart_cycle, phase6_smoke/push (content-type fixtures), phase7_5_test_server_reproducer (TestServer-specific snapshot timing), wal_env_var_tunables (env-var WAL config), bench_v18_blast_smoke (TestServer wiring)"
    - "After Plan 15, cargo test --workspace --all-features --no-fail-fast is GREEN (excluding any tests Plan 14 owns; Plans 14 + 15 together return the workspace to ≥1132 passed / 0 failed)"
  artifacts:
    - path: "crates/beava-server/tests/*"
      provides: "Test files updated to dispatch via white-box accessors or to match Plan 01's unified GlueResponse::Register variant; per-test fixture fixes per residual bucket"
key_links:
  - from: "crates/beava-server/tests/phase4_smoke.rs (post-migration)"
    to: "crates/beava-server/src/apply_shard.rs::dispatch_wire_request_sync"
    via: "white-box test calls dispatch_*_sync directly, bypassing /dev/apply_ops HTTP route"
    pattern: "dispatch_wire_request_sync"
---

<objective>
Test-side gap closure for Plan 01's surfaced failures. Two patterns:

**Pattern A — Migrate /dev/apply_events and /dev/apply_ops tests to white-box accessors.**

Replace HTTP POSTs to `/dev/apply_events` / `/dev/apply_ops` with direct calls to `apply_shard::dispatch_wire_request_sync` (or its `dispatch_wire_request_with_row` sibling). This is the architecturally-clean fix per CONTEXT note "operator regression is a separate /gsd-debug cycle" and per the project_phase18_no_dual_runtime commitment that mio is the only data-plane runtime — dev endpoints stay test-only.

Files:
- `crates/beava-server/tests/phase4_smoke.rs` (uses /dev/apply_ops)
- `crates/beava-server/tests/phase5_smoke.rs` (uses /dev/apply_events)
- `crates/beava-server/tests/phase9_smoke.rs` (uses /dev/apply_events)
- `crates/beava-server/tests/phase11_smoke.rs` (mixed; the type_mix test is already fixed by Plan 01, the residue is the /dev calls)
- `crates/beava-server/tests/phase10_sketch_recovery.rs` (likely uses /dev paths)

Reference shape (from Plan 01 plan body):
```rust
let app_state = ts.app_state();
let req = WireRequest::HttpPush { feature: "...".into(), body: serde_json::to_vec(&events).unwrap().into() };
let responses = app_state.dispatch_wire_request_sync(req);
```

**Pattern B — Per-test residual fixes.**

Inspect each failing test and apply the minimum fix consistent with the post-Plan-01 architecture:
- `phase18_01_glue.rs` — likely needs Plan 01's unified `GlueResponse::Register { http_status, body, tcp_op }` pattern-match update (Plan 01 already updated 2 of these test files; this is a residual)
- `phase18_04_6_integration_test.rs` — apply-thread instrumentation hook adjustment
- `phase18_04_7_iopool_test.rs` — iopool wiring after ServerV18 swap
- `phase18_07_push_and_get_test.rs` — push-and-get dispatch shape on mio
- `phase6_smoke.rs` / `phase6_push.rs` / `phase7_restart_cycle.rs` — likely Content-Type fixture issues; if Plan 14 lands content-type validation, these may pass without changes — re-run after Plan 14 lands and only patch what remains
- `phase7_5_test_server_reproducer.rs` — TestServer-specific snapshot timing reproducer
- `phase8_tcp_push.rs` — TCP wire shape (Plan 01 fixed RegisterOk OP code; check if other TCP frames need fixes)
- `wal_env_var_tunables.rs` — env-var driven WAL config; check if Plan 01's bind_with_state honors env vars
- `bench_v18_blast_smoke.rs` (in beava-bench) — TestServer-style wiring; depends on Plan 01's testing.rs

**TDD note:** This plan is mostly test-side migrations of existing tests. The "red" is the existing test failing under mio. The "green" is the migrated test passing. Single commit per logical migration is acceptable since the test contract was already authored. Per CLAUDE.md §TDD Discipline §Note 4, smoke/acceptance tests can be edited atomically when the per-feature unit-test red-green is preserved at the implementation level (which Plan 01 + Plan 14 own).

**Order of execution:**
1. Wait until Plan 14 lands (it adds /upsert /delete /retract + dev_endpoints gating + content-type to mio) — many residual failures may auto-resolve.
2. Re-run `cargo test --workspace --all-features --no-fail-fast`.
3. For the remaining failures, apply per-test fixes per Pattern A/B.

If running this plan in PARALLEL with Plan 14 (preferred for wall-clock): focus on Pattern A migrations first (those are not affected by Plan 14's work), then return to Pattern B residuals after Plan 14 has merged.

**Out of scope:**
- Server-side mio additions (Plan 14 owns)
- Modifying `crates/beava-server/src/` or `crates/beava-runtime-core/src/` — those are Plan 14's territory
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12.6-v0-surface-reduction/12.6-CONTEXT.md
@.planning/phases/12.6-v0-surface-reduction/12.6-01-SUMMARY.md   # critical: explains the unified GlueResponse::Register variant + the deferred buckets
@CLAUDE.md
@crates/beava-server/src/apply_shard.rs                          # white-box accessors live here
@crates/beava-server/src/testing.rs                              # TestServer post-Plan-01
</context>

<tasks>
<task id="1" type="single-commit">
**Pattern A migration:** `crates/beava-server/tests/phase4_smoke.rs` — replace /dev/apply_ops HTTP calls with `app_state.dispatch_wire_request_sync(...)`. Verify test passes. Commit `test(12.6-15): migrate phase4_smoke from /dev/apply_ops to white-box accessor`.
</task>

<task id="2" type="single-commit">
**Pattern A migration:** `crates/beava-server/tests/phase5_smoke.rs` — same as Task 1 for /dev/apply_events. Commit.
</task>

<task id="3" type="single-commit">
**Pattern A migration:** `crates/beava-server/tests/phase9_smoke.rs` — same. Commit.
</task>

<task id="4" type="single-commit">
**Pattern A migration:** `crates/beava-server/tests/phase11_smoke.rs` (residual /dev calls only — type_mix fix from Plan 01 already in place). Commit.
</task>

<task id="5" type="single-commit">
**Pattern A or B:** `crates/beava-server/tests/phase10_sketch_recovery.rs` — investigate failure, apply minimum fix. Commit.
</task>

<task id="6" type="single-commit">
**Pattern B:** `crates/beava-server/tests/phase18_01_glue.rs` — pattern-match update to Plan 01's unified `GlueResponse::Register { http_status, body, tcp_op }` (Plan 01 fixed 2 of these tests; this is the residual). Commit.
</task>

<task id="7" type="single-commit">
**Pattern B:** Per-test fix for one of: phase18_04_6, phase18_04_7, phase18_07_push_and_get, phase8_tcp_push, phase7_restart_cycle, phase6_smoke, phase6_push, phase7_5_test_server_reproducer, wal_env_var_tunables, bench_v18_blast_smoke. Commit each separately. Investigate failure mode, apply minimum fix.
</task>

<task id="8">
**Verification:** Run `cargo test --workspace --all-features --no-fail-fast` and confirm 0 failures (combined with Plan 14's mio additions). Run `cargo clippy --workspace --all-targets --all-features -- -D warnings` — must pass. Run `cargo fmt --all --check` — must pass.
</task>

<task id="9">
Create `.planning/phases/12.6-v0-surface-reduction/12.6-15-SUMMARY.md`. Commit.
</task>
</tasks>

<success_criteria>
- [ ] All Pattern A migrations complete (phase4/5/9/11_smoke + phase10_sketch_recovery moved to white-box dispatch)
- [ ] All Pattern B residuals fixed (phase18_01_glue, phase18_04_6, phase18_04_7, phase18_07_push_and_get, phase6_*/phase7_*, phase8_tcp_push, phase7_5_test_server_reproducer, wal_env_var_tunables, bench_v18_blast_smoke)
- [ ] cargo test --workspace --all-features --no-fail-fast: 0 failures (combined with Plan 14)
- [ ] cargo clippy + cargo fmt pass
- [ ] No modifications to crates/beava-server/src/* or crates/beava-runtime-core/src/* (Plan 14's territory)
- [ ] SUMMARY.md committed
- [ ] No modifications to .planning/STATE.md or .planning/ROADMAP.md
</success_criteria>
