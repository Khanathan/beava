---
phase: 12.6-v0-surface-reduction
plan: 03
subsystem: docs
tags: [requirements, project-doc, no-event-time-pivot, surgical-sweep]

# Dependency graph
requires:
  - phase: 12.6-v0-surface-reduction
    provides: "Phase context (CONTEXT.md) decisions D-01..D-05; doc-only commit 2d58d80 (2026-04-30) that landed most of the post-pivot wording"
provides:
  - "REQUIREMENTS.md fully aligned with no-event-time / no-joins / no-unions / no-PIT pivot — every event_time/tolerate_delay/join/union/as_of/watermark mention is REMOVED/DEFERRED/REJECTED-cited or describes the post-pivot architecture"
  - "Audit-classification artifact (12.6-03-audit-classification.md) — evidence trail for the surgical sweep; durable record under .planning/phases/ that survives /tmp cleanup"
  - "Verify-filter contract: grep -E plain-hits filtered by REMOVED/DEFERRED/REJECTED/now_ms/recv_ts_ms/processing-time/no-event-time pivot/server-side/out of scope returns 0 unexplained occurrences"
  - "Confirmation that PROJECT.md §Out of Scope is comprehensive (joins, unions, event-time, watermarks, PIT, event_time_ms, tolerate_delay_ms, event_time_field, as_of= all listed) — no PROJECT.md edits required this plan"
affects: [12.6-04 join+union removal, 12.6-05 path-x time-source swap, 12.6-06 event-time hard rip, 12.6-07 axum kill, 12.6-08 python sdk strip, phase-13 website-and-docs sweep]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Surgical sweep verification — case-sensitive verify regex on REMOVED/DEFERRED/REJECTED literals catches drift the doc-only commit (2d58d80) missed"
    - "Audit classification as RED gate — when a sweep is doc-only, the classification artifact IS the failing test; surgical edits make the verify regex green"
    - "Persisted audit trail under .planning/phases/ — /tmp/ classification copies are ephemeral; canonical artifacts must live in-tree"

key-files:
  created:
    - ".planning/phases/12.6-v0-surface-reduction/12.6-03-audit-classification.md (audit evidence)"
    - ".planning/phases/12.6-v0-surface-reduction/12.6-03-SUMMARY.md (this file)"
  modified:
    - ".planning/REQUIREMENTS.md (2 surgical edits — line 15 SDK-DEC-02 split note, line 144 SDK-JOIN preamble)"

key-decisions:
  - "Sweep depth = surgical (not comprehensive). Plan estimated 11 hits; actual was 9 plain / 16 broader; classification showed only 2 lines required edits — both case-sensitivity / phrasing fixes, not architectural drift. Comprehensive sweep was unnecessary because doc-only commit 2d58d80 (2026-04-30) had already done the heavy lift."
  - "PROJECT.md §Out of Scope ratified without edits. The block already names every excluded item (joins event↔event/table↔table/stream↔table, bv.union, event-time/watermarks/PIT, event_time_ms, tolerate_delay_ms, event_time_field, as_of=). Adding more would be padding."
  - "Verify-filter literal-string contract is the canonical sweep gate. The pre-existing doc cited the no-event-time pivot via memory-file path (`project_redis_shaped_no_event_time_ever`); the verify filter wants the literal phrase 'no-event-time pivot' OR the upper-case status word REMOVED/DEFERRED/REJECTED. Surgical edits added both."

patterns-established:
  - "Doc-edit plans with TDD framing: the audit classification IS the RED gate (failing-assertion artifact); the surgical sweep is the GREEN gate (makes verify regex pass). Two atomic commits — test(scope) followed by docs(scope) — preserve the convention without inventing executable tests for prose."
  - "When the classification reports 0 NEEDS FIX but the verify regex still returns >0, the regex is the contract — re-classify using the regex's literal-string criteria, not the human-judgment criteria."

requirements-completed:
  - "JOIN-EXTRA-01"          # confirmed REMOVED 2026-04-30 (line 307); strikethrough + REMOVED + citation
  - "UNION-RECONCILE-01"     # confirmed REMOVED 2026-04-30 (line 308); strikethrough + REMOVED + citation
  - "EVENT-TIME-RESTORE-01"  # confirmed REJECTED permanently 2026-04-30 (line 309); cites pivot memory + names override conditions

# Metrics
duration: ~25min
completed: 2026-04-30
---

# Phase 12.6 Plan 03: REQUIREMENTS.md + PROJECT.md no-event-time-pivot sweep — Summary

**Surgical sweep ratified the prior 2026-04-30 doc-only pass; closed 2 case-sensitivity gaps in REQUIREMENTS.md (SDK-DEC-02 split note + SDK-JOIN preamble) so the verify-filter contract returns 0 unexplained occurrences. PROJECT.md §Out of Scope was already comprehensive — no edits.**

## Performance

- **Duration:** ~25 min (plan-load → audit → classify → surgical edits → verify → SUMMARY)
- **Started:** 2026-04-30T16:08Z (approximate; agent invocation)
- **Completed:** 2026-04-30T16:33:31Z
- **Tasks:** 2 (Task 1.a RED audit, Task 1.b GREEN sweep)
- **Files modified:** 1 (.planning/REQUIREMENTS.md, 2 surgical edits) + 1 created (.planning/phases/12.6-v0-surface-reduction/12.6-03-audit-classification.md)

## Pre-Sweep / Post-Sweep Counts

| Metric | Pre-Sweep | Post-Sweep | Notes |
|--------|-----------|------------|-------|
| Plain regex hits (`grep -cE "event_time\|tolerate_delay\|\bjoin\b\|\bunion\b\|as_of\|watermark"`) | 9 | 9 | Surgical edits replaced lower-case "removed" with upper-case "REMOVED"; added "no-event-time pivot" phrase. No new occurrences introduced. |
| Broader regex hits (`grep -nEi "event_time\|tolerate_delay\|join\|union\|as_of\|watermark"`) | 16 | 16 | Same — surgical edits don't alter aggregate count. |
| Verify-filter unexplained occurrences (plain hits filtered by `REMOVED\|DEFERRED\|REJECTED\|now_ms\|recv_ts_ms\|processing-time\|no-event-time pivot\|server-side\|out of scope`) | 2 (lines 15, 144) | 0 | Task 1.b GREEN gate condition met. |

## Sweep-Depth Choice + Rationale

**Choice:** Surgical (planner discretion per CONTEXT D-05 + Discretion clause).

**Rationale:**

1. **Pre-existing doc-only commit `2d58d80` (2026-04-30) carried most of the work.** That commit (which landed before phase 12.6 planning) already converted ~13 occurrences to strikethrough-with-citation form. The classification per line confirmed 11 OK-removed + 3 OK-post-pivot pre-sweep — only 2 needed touch-ups.
2. **Comprehensive sweep would be padding.** Re-narrating sections that already cite the pivot accurately would reduce signal density without changing meaning.
3. **Verify-filter as objective gate.** The plan's `<verify>` regex is case-sensitive on REMOVED/DEFERRED/REJECTED; running it pre-sweep returned 2 lines, post-sweep returned 0. That binary signal is more reliable than a fuzzy human "looks done" judgement.

**What the surgical sweep actually did:**

- **Line 15** (SDK-DEC-02 split note): replaced `removed` → `REMOVED` (2 instances) and added `per the no-event-time pivot` so the line carries the canonical citation.
- **Line 144** (SDK-JOIN preamble): replaced `removed permanently per ...` → `REMOVED 2026-04-30 per the no-event-time pivot (see ...)`; replaced `deferred with joins` → `DEFERRED to v0.1+ alongside joins`.

Both edits are case + phrasing fixes; no semantic change.

## Items Deferred to Phase 13

Per CONTEXT D-05 (code-only-scope clarification), this plan touches ONLY `.planning/REQUIREMENTS.md` and `.planning/PROJECT.md`. Out-of-tree event-time/join references — listed for Phase 13 launch-polish work — include:

- `beava-website/` event-time and join examples in guide chapters, recipe pages, home-page Python snippets
- `beava-design-system/` UI kit code examples (the project root `beava-design-system/project/ui_kits/learn/` is dirty per `git status` but unrelated to phase 12.6 scope)
- `docs/` directory operator catalog, concepts.md, http-api.md (Phase 13 picks up DOC-01..06)
- `README.md` 3-command smoke demo if it references event-time
- Recipes under `beava-website/project/guide/recipes/` (anomaly, attribution, fraud, geospatial, leaderboard, personalization, rate-limiting, usage-metering)

Phase 13 owns this sweep; phase 12.6 is architectural-cleanup only.

## Task Commits

In strict red-then-green order per CLAUDE.md TDD discipline:

1. **Task 1.a (RED) — Audit classification artifact** — `40c6d2a` (test)
   - Title: `test(12.6-03): audit REQUIREMENTS.md event-time/join/union references — classification results`
   - File: `.planning/phases/12.6-v0-surface-reduction/12.6-03-audit-classification.md` (created)
   - Records 9 plain / 16 broader hits, classification per line, 2 NEEDS FIX entries (the failing assertions).

2. **Task 1.b (GREEN) — Surgical sweep** — `5d625c3` (docs)
   - Title: `docs(12.6-03): surgical sweep — REQUIREMENTS.md aligns with no-event-time pivot`
   - File: `.planning/REQUIREMENTS.md` (modified: lines 15, 144)
   - Closes the 2 NEEDS-FIX entries from the audit. Verify-filter returns 0.

(SUMMARY commit will follow this file's creation; orchestrator owns the final-metadata commit.)

## Decisions Made

- **Surgical sweep depth, not comprehensive.** See "Sweep-Depth Choice + Rationale" above.
- **PROJECT.md §Out of Scope ratified as-is, no edits.** Grep across PROJECT.md for the audit terms returned 10 lines, all in §What This Is / §Active / §Out of Scope / §Context (historical) / §Constraints / §Key Decisions — every one already cites the post-pivot architecture or the 2026-04-30 lock. The §Out of Scope block specifically (lines 100-102) names joins, unions, event-time/watermarks/PIT, `event_time_ms`, `tolerate_delay_ms`, `event_time_field`, `as_of=`. Adding more would be padding.
- **Audit classification persisted under `.planning/phases/12.6-v0-surface-reduction/`, not `/tmp/`.** The plan suggested `/tmp/12.6-03-audit-classification.md` for the audit, but ephemeral storage was inappropriate for an evidence trail downstream plans cite. Persisted to phase directory.
- **Verify regex is the contract, not human judgement.** Pre-edit, my by-eye classification said "0 NEEDS FIX." Running the plan's literal-string verify filter showed 2. The regex was correct — the human classification was lenient. Always trust the regex.

## Deviations from Plan

None substantive. Two minor procedural notes:

- **Audit classification path:** plan suggested `/tmp/12.6-03-audit-classification.md`; persisted instead to `.planning/phases/12.6-v0-surface-reduction/12.6-03-audit-classification.md` so the evidence trail survives `/tmp` cleanup. (Logistics decision; per `feedback_logistics_autonomy` no user prompt needed.)
- **Pre-edit classification said 0 NEEDS FIX; post-regex it was 2.** Discovered when running the plan's verify regex. The two failing lines (15 and 144) used lower-case "removed" / "deferred" without the canonical phrase — substantively correct but failing the literal-string filter. Surgical edits closed the gap. The audit-classification file was rewritten to reflect the regex's stricter view before commit.

## Issues Encountered

None — straightforward doc-edit plan; no test infrastructure or CI gates to navigate.

## User Setup Required

None — pure planning-doc edits.

## Next Phase Readiness

**Wave 1 of phase 12.6 plans 12.6-04..08 are now safe to dispatch:**

- Plan 12.6-04 (join + union removal) can cite REQ JOIN-EXTRA-01 + UNION-RECONCILE-01 in its `requirements_addressed` field with confidence the REQUIREMENTS.md entries match the pre-pivot reality.
- Plan 12.6-05 (Path X time-source swap) can cite SRV-APPLY-03 + SRV-DUR-03 (lines 213, 224) as the canonical post-pivot time-source spec.
- Plan 12.6-06 (event-time hard rip) can cite SDK-DEC-02b + SDK-DEC-08 + SDK-DEC-09 (deduplication retained) without the doc lying about what's still in v0.
- Plan 12.6-08 (Python SDK strip) can reference the SDK-JOIN section (line 142+) preamble for the architectural rationale to surface in error messages.

Phase 13 picks up the website / docs / recipes / README sweep for the same architectural commitment — see "Items Deferred to Phase 13" above.

## Self-Check: PASSED

Verifications run after completing the work:

- `grep -nE "event_time|tolerate_delay|\bjoin\b|\bunion\b|as_of|watermark" .planning/REQUIREMENTS.md | grep -vE "REMOVED|DEFERRED|REJECTED|now_ms|recv_ts_ms|processing-time|no-event-time pivot|server-side|out of scope" | wc -l` → **0** (PASS)
- `grep -cE "event_time|tolerate_delay|\bjoin\b|\bunion\b|as_of|watermark" .planning/REQUIREMENTS.md` → **9** (preserved; no occurrences removed)
- `git log --oneline | head -3` shows commits in strict red-then-green order: `5d625c3 docs(12.6-03)` after `40c6d2a test(12.6-03)`
- `[ -f .planning/phases/12.6-v0-surface-reduction/12.6-03-audit-classification.md ]` → FOUND
- `[ -f .planning/phases/12.6-v0-surface-reduction/12.6-03-SUMMARY.md ]` → FOUND (this file)
- No code files modified outside `.planning/` (D-05 preserved); `git diff --name-only HEAD~2 HEAD` lists only `.planning/...` paths
- STATE.md / ROADMAP.md untouched (orchestrator-owned)

---

*Phase: 12.6-v0-surface-reduction*
*Plan: 03*
*Completed: 2026-04-30*
