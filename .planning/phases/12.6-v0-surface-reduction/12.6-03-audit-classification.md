# Plan 12.6-03 Audit Classification

**Date:** 2026-04-30
**Target:** `.planning/REQUIREMENTS.md`
**Audit regex:** `event_time|tolerate_delay|\bjoin\b|\bunion\b|as_of|watermark`
**Plain regex hits (`-E`):** 9 (pre-sweep) / 9 (post-sweep)
**Broader hits (`-Ei` no word boundaries, includes "PIT" and matches like `joins~`):** 16

The plan estimated 11 hits; actual is 9 (plain) / 16 (broader). The drift is cosmetic — most lines were
already corrected by the prior doc-only commit `2d58d80` on 2026-04-30. The classification below uses
the broader hit set for thoroughness.

## Classification per Line (Pre-Sweep)

Each line is one of:
- **OK-removed:** strikethrough + REMOVED/DEFERRED/REJECTED label + cites the no-event-time pivot
- **OK-post-pivot:** describes the post-pivot architecture (`now_ms()`, `recv_ts_ms`, processing-time, server-side arrival, "NOT covered"), so the mention is explanatory, not a residual feature
- **NEEDS FIX (verify-filter):** substantively correct but case-sensitive verify filter (`grep -E "REMOVED|DEFERRED|REJECTED|now_ms|recv_ts_ms|processing-time|no-event-time pivot|server-side|out of scope"`) misses lower-case "removed" / phrases that don't include the canonical pivot string

| Line | REQ-ID / Section | Class | Justification |
|------|-------------------|-------|---------------|
| 14 | SDK-DEC-02b (tolerate_delay) | OK-removed | strikethrough + "REMOVED 2026-04-30 per no-event-time pivot" + cites memory file |
| 15 | SDK-DEC-02 split note | NEEDS FIX (verify-filter) | meta-note used lower-case "removed" in 2 places; verify-grep is case-sensitive on REMOVED so the line failed the filter even though the content is correct |
| 21 | SDK-DEC-08 (event_time field) | OK-removed | strikethrough + "REMOVED 2026-04-30 per no-event-time pivot" |
| 142 | `### SDK-JOIN — REMOVED 2026-04-30` (section header) | OK-removed | section title literally says REMOVED with date stamp |
| 144 | SDK-JOIN preamble paragraph | NEEDS FIX (verify-filter) | content was correct ("removed permanently per `project_redis_shaped_no_event_time_ever`") but used lower-case "removed" + "deferred"; the canonical phrase "no-event-time pivot" was implicit (cited via memory-file path) rather than literal |
| 146 | `~~SDK-JOIN-01..04: joins~~ — REMOVED` | OK-removed | strikethrough + REMOVED |
| 147 | `~~SDK-JOIN-05: bv.union(*events)~~ — DEFERRED v0.1+` | OK-removed | strikethrough + DEFERRED |
| 213 | SRV-APPLY-03 windowed wrapper | OK-post-pivot | describes `now_ms()` semantics explicitly; phrase `NOT event_time per 2026-04-30 pivot` makes the historical reference explicit |
| 218 | `~~SRV-APPLY-08: Join support~~` | OK-removed | strikethrough + REMOVED |
| 224 | SRV-DUR-03 WAL format | OK-post-pivot | describes `recv_ts_ms` semantics; parenthetical clarifies `event_time` field DROPPED in 12.6 |
| 267 | `~~TEST-07: Join tests~~` | OK-removed | strikethrough + REMOVED |
| 273 | DOC-03 concepts.md description | OK-post-pivot | "joins/unions/event-time NOT covered — removed permanently per 2026-04-30 architectural pivot" |
| 307 | `~~JOIN-EXTRA-01~~` | OK-removed | strikethrough + REMOVED |
| 308 | `~~UNION-RECONCILE-01~~` | OK-removed | strikethrough + REMOVED |
| 309 | EVENT-TIME-RESTORE-01 | OK-removed | "REJECTED permanently 2026-04-30" + cites pivot memory + names override conditions |

## Pre-Sweep Result

**OK-removed:** 11
**OK-post-pivot:** 3 (SRV-APPLY-03 line 213, SRV-DUR-03 line 224, DOC-03 line 273)
**NEEDS FIX (verify-filter):** 2 (line 15 SDK-DEC-02 split note, line 144 SDK-JOIN preamble)

## Verify-Filter Failure Detail

The plan's Task 1.b verify regex is:

```
grep -nE "event_time|tolerate_delay|\\bjoin\\b|\\bunion\\b|as_of|watermark" .planning/REQUIREMENTS.md \
  | grep -vE "REMOVED|DEFERRED|REJECTED|now_ms|recv_ts_ms|processing-time|no-event-time pivot|server-side|out of scope" \
  | wc -l
```

The negative-filter `grep -vE` is case-sensitive. Pre-sweep, 2 lines used lower-case "removed" or
"deferred" and didn't carry the literal phrase "no-event-time pivot" — so the filter returned 2,
not 0. Substantively the lines were correct (they cited the architectural commitment via memory
file path); the filter was a stricter literal-string check.

## Surgical Fixes Applied (Task 1.b GREEN)

**Line 15** (SDK-DEC-02 split note):
- BEFORE: "to make the active vs removed surfaces explicit. The original SDK-DEC-02 conflated
  `keep_events_for` (kept) with `tolerate_delay` (removed)"
- AFTER: "to make the active vs REMOVED surfaces explicit per the no-event-time pivot. The
  original SDK-DEC-02 conflated `keep_events_for` (kept) with `tolerate_delay` (REMOVED)"
- Diff: lower-case "removed" → upper-case "REMOVED" (2 instances), added explicit "per the
  no-event-time pivot" phrase.

**Line 144** (SDK-JOIN preamble):
- BEFORE: "All join + union requirements removed permanently per
  `project_redis_shaped_no_event_time_ever`. ... `bv.union(*events)` deferred with joins to v0.1+."
- AFTER: "All join + union requirements REMOVED 2026-04-30 per the no-event-time pivot (see
  `project_redis_shaped_no_event_time_ever`). ... `bv.union(*events)` DEFERRED to v0.1+ alongside
  joins."
- Diff: lower-case "removed permanently" → upper-case "REMOVED 2026-04-30 per the no-event-time
  pivot"; lower-case "deferred" → upper-case "DEFERRED to v0.1+".

## Post-Sweep Verification

After surgical edits:

```bash
$ grep -nE "event_time|tolerate_delay|\\bjoin\\b|\\bunion\\b|as_of|watermark" .planning/REQUIREMENTS.md \
    | grep -vE "REMOVED|DEFERRED|REJECTED|now_ms|recv_ts_ms|processing-time|no-event-time pivot|server-side|out of scope" \
    | wc -l
0
```

Verify filter PASSES. All 9 (plain) / 16 (broader) hits in REQUIREMENTS.md now meet the
post-pivot citation contract.

## PROJECT.md Cross-Check

`grep -nEi "event_time|tolerate_delay|join|union|as_of|watermark|PIT" .planning/PROJECT.md` returned
10 lines (5, 27, 100, 101, 102, 111, 140, 153, 154, 166). All ten are either:

- §"What This Is" (line 5): describes the architectural commitment ("processing-time semantics
  only — no event-time / no watermarks / no joins")
- §Requirements / Active (line 27): "Joins + bv.union deferred to v0.1+ alongside event-time
  work; not in v0 scope" — explicit deferral
- §Out of Scope (lines 100, 101, 102): explicit exclusions for joins, unions, and
  event-time/watermarks/PIT with full citation to `project_redis_shaped_no_event_time_ever`
- §Context (line 111): historical description of what v1 shipped — not a v0 promise
- §Constraints (line 140): "API compatibility" lists the breaking changes from v1
- §Key Decisions (lines 153, 154, 166): locked decisions citing the 2026-04-30 pivot

The §Out of Scope block is **already comprehensive** — joins, unions, event-time, watermarks,
PIT, `event_time_ms`, `tolerate_delay_ms`, `event_time_field`, `as_of=` are all listed. No fix
required.

## Plan 12.6-03 Disposition

Surgical sweep depth chosen at planning time (11 estimated hits → low-N regime). Audit confirms
the prior 2026-04-30 doc-only commit `2d58d80` carried the load; Plan 03's surgical sweep is the
final ratification. Two lines (15, 144) needed cosmetic case fixes to satisfy the verify filter
without changing meaning. PROJECT.md §Out of Scope needed no edits.

This is the canonical outcome of a surgical sweep when the doc is already substantively sound:
the plan's role is to verify, fix any literal-string drift the verify filter catches, and commit
the audit trail.
