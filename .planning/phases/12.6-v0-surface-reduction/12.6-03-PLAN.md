---
phase: 12.6-v0-surface-reduction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - JOIN-EXTRA-01           # confirm marked REMOVED, no code/doc orphan
  - UNION-RECONCILE-01      # confirm marked REMOVED, no code/doc orphan
  - EVENT-TIME-RESTORE-01   # confirm marked REJECTED, no code/doc orphan
addresses_decisions:
  - D-05           # Code-only-scope clarification: this plan touches REQUIREMENTS.md (planning doc) only — NOT website/docs/recipes
  - "Discretion: REQUIREMENTS.md sweep depth — surgical vs comprehensive — planner decides based on grep audit"
must_haves:
  truths:
    - "Every event_time / tolerate_delay / join / union / as_of / watermark reference in REQUIREMENTS.md is either documented as REMOVED/DEFERRED/REJECTED or describes the post-pivot architecture (`now_ms()`, `recv_ts_ms`, processing-time)"
    - "PROJECT.md §Out of Scope reflects the no-event-time / no-joins / no-unions architectural commitment"
    - "No REQUIREMENTS.md item describes a feature that will silently disappear when Plans 04–08 land"
    - "Sweep depth choice is justified by the grep occurrence count surveyed at planning time (surgical for low-N, comprehensive for high-N)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Authoritative requirement list aligned with post-pivot architecture (no event-time, no joins, no unions, no PIT)"
      contains: "REMOVED 2026-04-30 per no-event-time pivot"
    - path: ".planning/PROJECT.md"
      provides: "Out-of-Scope section confirms event-time / joins / unions / watermarks / PIT are permanently excluded"
      contains: "Out of Scope"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "/Users/petrpan26/.claude/projects/-Users-petrpan26-work-tally/memory/project_redis_shaped_no_event_time_ever.md"
      via: "every REMOVED item cites this commitment"
      pattern: "no-event-time pivot"
---

<objective>
Sweep `.planning/REQUIREMENTS.md` and `.planning/PROJECT.md` so the requirement-level documentation matches the architectural reality after Plans 04–08 land. The sweep is **doc-only inside the .planning tree** — per D-05, this plan does NOT touch beava-website / docs / recipes / README (those go to Phase 13).

Why now (Wave 1): Subsequent plans (04 join/union removal, 05 Path X, 06 event-time strip, 07 axum kill, 08 Python SDK strip) will leave the codebase in a state where REQUIREMENTS.md is the authoritative reference for "what v0 ships" — those plans need the doc to already be aligned so their `requirements_addressed` field matches reality. Doing this in Wave 1 also lets Plan 02's deadcode threshold rationale cite a clean REQUIREMENTS.md.

Sweep depth (planner discretion per CONTEXT): The grep audit at planning time showed **11 remaining occurrences** of `event_time|tolerate_delay|join|union|as_of|watermark` in REQUIREMENTS.md. Of those:
- 7 are already correct (marked REMOVED / DEFERRED / REJECTED with citations to the no-event-time pivot)
- 4 are **descriptive** mentions that need ratification (e.g., SDK-DEC-02, SRV-APPLY-03, SRV-DUR-03, DOC-03 — they describe the post-pivot shape correctly but warrant a final pass for consistency).

That low remaining count suggests the **surgical** depth: read every occurrence, verify each is correct, fix any drift. No mass rewrites. Comprehensive sweep is unnecessary because the doc-only commit `2d58d80` already did the heavy lift on 2026-04-30.

Output:
- Updated `.planning/REQUIREMENTS.md` (surgical edits, no structural change)
- Updated `.planning/PROJECT.md` §Out of Scope (confirm wording matches architectural commitment)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.6-v0-surface-reduction/12.6-CONTEXT.md
@/Users/petrpan26/.claude/projects/-Users-petrpan26-work-tally/memory/project_redis_shaped_no_event_time_ever.md
@/Users/petrpan26/.claude/projects/-Users-petrpan26-work-tally/memory/project_stateful_architecture.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (red): Failing assertion that all event-time/join/union references are correctly marked</name>
  <files>.planning/REQUIREMENTS.md (audit only)</files>
  <read_first>
    - .planning/REQUIREMENTS.md (all 339 lines)
    - .planning/phases/12.6-v0-surface-reduction/12.6-CONTEXT.md (Discretion clause on sweep depth)
    - /Users/petrpan26/.claude/projects/-Users-petrpan26-work-tally/memory/project_redis_shaped_no_event_time_ever.md (architectural commitment)
  </read_first>
  <behavior>
    - Sweep audit assertion: every occurrence of `event_time|tolerate_delay|join|union|as_of|watermark` in `.planning/REQUIREMENTS.md` MUST either:
      (a) describe an item already labelled REMOVED/DEFERRED/REJECTED (with `~~strikethrough~~` + citation to the no-event-time pivot), or
      (b) describe the post-pivot architecture (mentions of `recv_ts_ms`, `now_ms()`, processing-time, server-side arrival).
    - The current state of REQUIREMENTS.md has 11 occurrences (counted via `grep -c "event_time\|tolerate_delay\|join\|union\|as_of\|watermark" .planning/REQUIREMENTS.md`). The audit checks each line.
  </behavior>
  <action>
    Run the audit:
    ```bash
    grep -nE "event_time|tolerate_delay|\\bjoin\\b|\\bunion\\b|as_of|watermark" .planning/REQUIREMENTS.md > /tmp/12.6-03-audit.txt
    cat /tmp/12.6-03-audit.txt
    ```

    For each line in the output, classify it as one of:
    - **OK-removed:** `~~strikethrough~~` + cites no-event-time pivot
    - **OK-post-pivot:** describes the new architecture (e.g., `now_ms()`, `recv_ts_ms`, processing-time)
    - **NEEDS FIX:** describes the old architecture or lacks the pivot citation

    Write the classification into a Markdown comment block at the top of `/tmp/12.6-03-audit-classification.md`. If ANY line is **NEEDS FIX**, the audit FAILS — that's the RED state. If ALL are OK, the audit passes (no GREEN task needed; commit a confirmation note).

    Commit `test(12.6-03): audit REQUIREMENTS.md event-time/join/union references — classification results`.
  </action>
  <verify>
    <automated>grep -cE "event_time|tolerate_delay|\\bjoin\\b|\\bunion\\b|as_of|watermark" .planning/REQUIREMENTS.md && test -f /tmp/12.6-03-audit-classification.md</automated>
  </verify>
  <done>Classification file exists; the count is recorded; each line is labelled OK-removed / OK-post-pivot / NEEDS FIX. Commit `test(12.6-03): ...` lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (green): Apply surgical fixes for any NEEDS FIX entries; ratify PROJECT.md §Out of Scope</name>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <read_first>
    - /tmp/12.6-03-audit-classification.md (output of Task 1.a)
    - .planning/PROJECT.md §Out of Scope section (find with `grep -n "Out of Scope" .planning/PROJECT.md`)
  </read_first>
  <action>
    For each line classified **NEEDS FIX** in Task 1.a:

    - If the line describes a feature still planned for v0: leave it alone — that's not a 12.6 issue.
    - If the line describes event-time / joins / unions / watermarks / PIT / `as_of=` AS IF still present: rewrite to match the no-event-time pivot. Use these template wordings (copy verbatim where possible):
      - For event-time: `~~**REQ-ID**: <original>~~ — REMOVED 2026-04-30 per no-event-time pivot. See PROJECT.md §Out of Scope.`
      - For joins: `~~**REQ-ID**: <original>~~ — REMOVED 2026-04-30 per no-event-time pivot. Joins of any shape (event↔event, event↔table, table↔table) are not part of v0+.`
      - For unions: `~~**REQ-ID**: <original>~~ — DEFERRED v0.1+ with joins. Pending architectural review post-v0.`
      - For watermarks: `~~**REQ-ID**: <original>~~ — REMOVED 2026-04-30 per no-event-time pivot. Beava is processing-time only; watermarks have no role.`
      - For PIT / `as_of=`: `~~**REQ-ID**: <original>~~ — REMOVED 2026-04-30 per no-event-time pivot. PIT temporal reads superseded by explicit \`app.retract(event_id)\`.`

    Then ratify `.planning/PROJECT.md` §Out of Scope: confirm it lists `event_time_ms`, joins, unions, watermarks, PIT, `as_of=`, multi-thread apply (already locked per `project_no_sharded_apply`). If any line is missing, append it with citation to the no-event-time pivot. Do NOT touch sections outside §Out of Scope (per D-05 — website/docs sweep is Phase 13).

    Re-run the audit grep:
    ```bash
    grep -nE "event_time|tolerate_delay|\\bjoin\\b|\\bunion\\b|as_of|watermark" .planning/REQUIREMENTS.md | wc -l
    ```
    The count may increase (because of the new strikethrough lines) — that's fine. The check is qualitative: every line is now OK-removed, OK-post-pivot, or describes a v0-active item.

    Commit `docs(12.6-03): surgical sweep — REQUIREMENTS.md + PROJECT.md align with no-event-time pivot`.
  </action>
  <verify>
    <automated>grep -nE "event_time|tolerate_delay|\\bjoin\\b|\\bunion\\b|as_of|watermark" .planning/REQUIREMENTS.md | grep -vE "REMOVED|DEFERRED|REJECTED|now_ms|recv_ts_ms|processing-time|no-event-time pivot|server-side|out of scope" | wc -l | grep -E "^0$"</automated>
  </verify>
  <done>The `wc -l` of the filtered grep returns 0 (every event-time/join/union mention is justified by REMOVED / DEFERRED / REJECTED / post-pivot description / out-of-scope citation). Commit `docs(12.6-03): ...` lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REQUIREMENTS.md → downstream plan `requirements_addressed` fields | Plans 04–08 cite REQ-IDs as evidence for what they remove; if REQUIREMENTS.md describes them wrong, executors get confused |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.6.03-01 | Tampering | Sweep depth gamed (surgical declared but real fixes skipped) | mitigate | Task 1.a writes the classification file as evidence; Task 1.b's verify regex catches uncited references; Plan 13 re-runs the same audit |
| T-12.6.03-02 | Information Disclosure | Removing references to architectural decisions loses provenance | accept | Strikethrough preserves history; citations to memory file pin provenance |
</threat_model>

<verification>
1. `grep -cE "event_time|tolerate_delay|join|union|as_of|watermark" .planning/REQUIREMENTS.md` returns N ≥ 11 (sweep ADDS strikethrough citations, doesn't subtract).
2. Filtered grep (excluding lines that cite REMOVED / DEFERRED / REJECTED / now_ms / recv_ts_ms / processing-time / no-event-time pivot / server-side / out-of-scope) returns 0.
3. `.planning/PROJECT.md` §Out of Scope contains lines mentioning event-time / joins / unions / watermarks / PIT.
4. Both commits land in plan order: `test(12.6-03)` then `docs(12.6-03)`.
</verification>

<success_criteria>
1. REQUIREMENTS.md surgical sweep complete; no orphan event-time/join/union descriptive mentions without proper REMOVED/DEFERRED/REJECTED tagging.
2. PROJECT.md §Out of Scope ratified.
3. The surgical-vs-comprehensive depth choice is documented in the SUMMARY (with the original 11-occurrence count and the post-sweep count).
4. No website / docs / recipes / README files are touched (D-05 preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/12.6-v0-surface-reduction/12.6-03-SUMMARY.md` covering:
- Pre-sweep / post-sweep occurrence counts
- The sweep-depth choice (surgical) and rationale
- Any items deferred to Phase 13's website/docs sweep
</output>
