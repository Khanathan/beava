---
phase: 12.6-v0-surface-reduction
plan: 07
type: execute
wave: 3
depends_on: [01, 04, 05, 06]
files_modified:
  - crates/beava-server/src/server.rs
  - crates/beava-server/src/lib.rs
  - crates/beava-server/src/main.rs
  - crates/beava-server/src/runtime_core_glue.rs
  - crates/beava-server/src/testing.rs                        # delete the dev_endpoints no-op holdover from Plan 01
  - crates/beava-server/src/push.rs                           # FULL DELETE
  - crates/beava-server/src/http.rs                           # FULL DELETE
  - crates/beava-server/src/http_admin.rs                     # FULL DELETE
  - crates/beava-server/src/push_and_get.rs                   # FULL DELETE per D-04 (Plan 12-10 stays deferred; mio-side absent)
  - crates/beava-server/src/temporal_http.rs                  # MIXED: delete axum-handler functions (upsert_handler, delete_handler, retract_handler, table_get_handler, temporal_router); keep helpers used by ServerV18 (entity_key_from_body, entity_key_from_str, json_object_to_row, row_to_json, parse_query_pairs, now_ms, error_response, ok_response) — see <interfaces> for the resolved decision
  - crates/beava-server/src/feature_query.rs                  # legacy axum /get — branch surgery; keep query helpers used by mio
  - crates/beava-server/src/register.rs                       # legacy axum /register handler — branch surgery; keep core register fns used by mio glue
  - crates/beava-server/src/registry_debug.rs                 # delete BEAVA_DEV_ENDPOINTS gating + axum /registry handler
  - crates/beava-server/src/shutdown.rs                       # KEEP — already consumed by ServerV18 via main.rs:82; only doc-comment update (axum::serve → ServerV18::serve_with_dirs) — see <interfaces> for the resolved decision
autonomous: true
requirements:
  # Plan 07 sweeps the entire legacy axum data plane that supports the now-deleted features
  # from Plans 04 (joins/unions DAG) and 06 (event-time wire). It carries no REQ-IDs of its own;
  # all REQ-IDs are addressed via the upstream plans. This plan implements ROADMAP success criteria 1+3.
addresses_decisions:
  - "Bucket 1 (CONTEXT): Legacy axum kill (~3500 LOC); ServerV18 stays at server.rs:370+"
  - "Architectural commitment: project_phase18_no_dual_runtime — mio is the only data-plane runtime; admin sidecar stays on tokio"
  - D-01                  # TestServer drop-in rewrite is the precondition (Plan 01); Plan 07 removes the legacy server it preserved
  - D-04                  # Plan 12-10 push-and-get stays deferred; legacy push_and_get.rs deletes; mio-side absent
must_haves:
  truths:
    - "crates/beava-server/src/push.rs is gone"
    - "crates/beava-server/src/http.rs is gone"
    - "crates/beava-server/src/http_admin.rs is gone"
    - "crates/beava-server/src/push_and_get.rs is gone"
    - "Legacy `pub struct Server` in server.rs and its impl block (lines 54–369) are gone"
    - "ServerV18 (server.rs:370+) is preserved and remains the production binary entry"
    - "runtime_core_glue.rs::dispatch_wire_request async function is gone; sync helpers (dispatch_get_batch_sync, dispatch_get_single_sync) are preserved"
    - "BEAVA_DEV_ENDPOINTS env-var paths are gone; dev_endpoints flag is gone from TestServer (Plan 01 left it as a no-op shim — now fully removed)"
    - "main.rs boots ServerV18 only — no conditional path for legacy Server"
    - "shutdown.rs::shutdown_signal() is preserved and continues to be consumed by ServerV18 via main.rs (already wired); only the stale axum doc-comment is updated"
    - "temporal_http.rs has had its four axum handler functions (upsert_handler, delete_handler, retract_handler, table_get_handler) AND temporal_router deleted; non-axum helper functions (entity_key_from_body, entity_key_from_str, json_object_to_row, row_to_json, parse_query_pairs, now_ms, error_response, ok_response) are preserved for ServerV18 reuse"
    - "Admin sidecar (tokio /metrics, /health, /ready, /registry) on a SEPARATE PORT is preserved per project_phase18_no_dual_runtime"
    - "All workspace tests pass (excluding any tests that exercised LEGACY-ONLY axum routes; those are migrated to ServerV18 or deleted with coverage replicated elsewhere)"
  artifacts:
    - path: "crates/beava-server/src/server.rs"
      provides: "ServerV18-only contents; legacy `Server` struct + impl deleted"
      min_lines: 1500          # post-delete, ~2540 - 1100 (legacy Server impl) = ~1440 lines
    - path: "crates/beava-server/src/main.rs"
      provides: "Boots ServerV18; no env-var gates for legacy paths; still imports shutdown::shutdown_signal"
      contains: "ServerV18"
    - path: "crates/beava-server/src/runtime_core_glue.rs"
      provides: "Sync glue helpers only; no async dispatch_wire_request"
      min_lines: 200
    - path: "crates/beava-server/src/shutdown.rs"
      provides: "shutdown_signal() future preserved; doc-comment updated to cite ServerV18 instead of axum::serve"
      contains: "shutdown_signal"
    - path: "crates/beava-server/src/temporal_http.rs"
      provides: "Helper functions only (entity_key_from_body, json_object_to_row, etc.); axum handlers + temporal_router deleted"
      contains: "json_object_to_row"
  key_links:
    - from: "crates/beava-server/src/main.rs"
      to: "crates/beava-server/src/server.rs::ServerV18"
      via: "main.rs invokes ServerV18::bind() / serve_with_dirs() directly with shutdown_signal future"
      pattern: "ServerV18"
    - from: "crates/beava-server/src/main.rs"
      to: "crates/beava-server/src/shutdown.rs::shutdown_signal"
      via: "main.rs:82 calls shutdown_signal() and threads the future into ServerV18::serve_with_dirs(..., shutdown)"
      pattern: "shutdown_signal"
---

<objective>
Delete the legacy axum data plane. After this plan lands, every push, get, upsert, delete, retract, and register on the data plane goes through ServerV18 (mio) — there is no axum data-plane code path remaining. The tokio admin sidecar (port admin_addr; /metrics, /health, /ready, /registry) is preserved per project_phase18_no_dual_runtime.

This is the largest deletion plan in Phase 12.6 — ~3500 LOC removed across:
- crates/beava-server/src/push.rs (414 LOC, full delete)
- crates/beava-server/src/http.rs (367 LOC, full delete)
- crates/beava-server/src/http_admin.rs (179 LOC, full delete)
- crates/beava-server/src/push_and_get.rs (~250 LOC, full delete per D-04 — Plan 12-10 stays deferred; mio-side absent)
- crates/beava-server/src/server.rs lines 54–369 (legacy `Server` struct + impl, ~1100 LOC)
- crates/beava-server/src/runtime_core_glue.rs lines 85–352 (async dispatch_wire_request, ~270 LOC)
- BEAVA_DEV_ENDPOINTS env-var infrastructure across server.rs / main.rs / register.rs / registry_debug.rs (~50 LOC scattered)
- TestServer's dev_endpoints holdover from Plan 01 (~10 LOC)
- temporal_http.rs axum handlers (upsert_handler, delete_handler, retract_handler, table_get_handler) + temporal_router (~600 LOC)
- feature_query.rs / register.rs branch surgery for axum routes (~200 LOC keeping mio-side helpers)
- shutdown.rs preserved as-is (consumed by ServerV18 via main.rs:82); only doc-comment updated (~5 LOC of doc)

Wave 3 placement: depends on Plans 01 (TestServer rewrite — must already be ServerV18-backed so deleting `Server` doesn't break tests), 04 (joins/unions deletion — register-time validator changes already landed, so the legacy register path's join/union handling is already a no-op), 05 (Path X — the legacy push.rs::apply_event_to_aggregations call site at line 302 is dead code by the time we delete the file), and 06 (event-time strip — the legacy push.rs read of event_time_field is dead by Plan 06). All four deps must be green before starting Plan 07.

Output:
- ~3500 LOC deleted from crates/beava-server/src/
- main.rs, lib.rs cleaned of legacy `Server` references
- ServerV18 is the only binary entry path
- Admin sidecar (tokio) preserved on its own port
- shutdown.rs preserved as-is (already consumed by ServerV18 via main.rs:82); doc-comment updated to cite ServerV18 instead of axum::serve
- temporal_http.rs has axum handlers + temporal_router fully deleted; helpers preserved
- All workspace tests pass (cargo test --workspace --all-features green)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.6-v0-surface-reduction/12.6-CONTEXT.md
@.planning/phases/12.6-v0-surface-reduction/12.6-01-SUMMARY.md
@.planning/phases/12.6-v0-surface-reduction/12.6-04-SUMMARY.md
@.planning/phases/12.6-v0-surface-reduction/12.6-05-SUMMARY.md
@.planning/phases/12.6-v0-surface-reduction/12.6-06-SUMMARY.md
@CLAUDE.md
@/Users/petrpan26/.claude/projects/-Users-petrpan26-work-tally/memory/project_phase18_no_dual_runtime.md
@crates/beava-server/src/server.rs
@crates/beava-server/src/main.rs
@crates/beava-server/src/lib.rs
@crates/beava-server/src/runtime_core_glue.rs
@crates/beava-server/src/push.rs
@crates/beava-server/src/http.rs
@crates/beava-server/src/http_admin.rs
@crates/beava-server/src/push_and_get.rs
@crates/beava-server/src/temporal_http.rs
@crates/beava-server/src/feature_query.rs
@crates/beava-server/src/register.rs
@crates/beava-server/src/shutdown.rs

<interfaces>
Reference points (read these before deleting anything to confirm the surgical boundary):

server.rs structure:
- Lines 54–369: `pub struct Server` + `impl Server` — DELETE.
- Lines 370+: `pub struct ServerV18` + `impl ServerV18` — KEEP. ServerV18::bind / serve / serve_with_dirs / shutdown stay.
- Line 2257+: #[cfg(test)] mod tests — read carefully; some tests target the legacy Server, some target ServerV18. Keep ServerV18 tests; delete legacy Server tests OR migrate to ServerV18.

push.rs (414 LOC, full delete):
- Apply call at line 302: `apply_event_to_aggregations(...)` — dead code post-Plan-05 (mio path computes this directly via apply_shard.rs::dispatch_push_sync). Confirmed by `grep -n "apply_event_to_aggregations" crates/beava-server/src/push.rs` showing the only invocation is from this legacy file.
- Lines 248–326 read event_time_ms + write to dev_agg.max_event_time_ms — already dead post-Plan-06 (max_event_time_ms field deleted).
- Full file deletes safely.

http.rs (367 LOC, full delete):
- Sets up the axum router at lines 70–130. Routes mounted: /push, /push-sync, /push-batch, /push-table, /delete-table, /retract, /push-and-get, /push-sync-and-get, /get, /set, /mset, /register, /validate, /health, /ready, /metrics, /registry.
- All these routes are now served by ServerV18 (per Plan 12-07 SUMMARY) on the mio data plane (data routes) + the tokio admin sidecar (admin routes /metrics, /health, /ready, /registry).
- BEAVA_DEV_ENDPOINTS gating at lines 80, 98, 120 — delete the gating; the dev /registry endpoint goes to the admin sidecar (which has its own auth posture per the existing code).

http_admin.rs (179 LOC, full delete):
- This is the legacy admin handler set. ServerV18's tokio sidecar (line 488+, serve()) has its own admin routes. Confirm via `grep -n "fn admin\\|fn serve\\b\\|axum::Router" crates/beava-server/src/server.rs` — the ServerV18 admin sidecar already implements /metrics, /health, /ready, /registry. http_admin.rs is fully duplicate.

push_and_get.rs (~250 LOC, full delete per D-04):
- Legacy axum push-and-get handler. Plan 12-10 (mio-side push-and-get) stays deferred per D-04. After deletion, the route does not exist anywhere; v0 ships without it. Users do 2 RTs (push then get).

runtime_core_glue.rs:
- Lines 85–352: pub async fn dispatch_wire_request — DELETE entire async function. Used only by legacy axum dispatchers.
- Lines 353–631: sync helpers (dispatch_get_single_sync, dispatch_get_batch_sync, etc.) — KEEP. apply_shard.rs uses them.

main.rs (full file ~120 LOC):
- Currently boots ServerV18 by default. May still have a fallback path for env-var to boot legacy Server (check via grep). Delete any legacy fallback; ServerV18 is unconditional.
- main.rs:24 imports `shutdown::shutdown_signal` and main.rs:82 wires it into ServerV18's serve loop. Both KEEP — `shutdown_signal` is the canonical shutdown future for ServerV18, NOT a legacy-Server holdover.

lib.rs:
- Line 17: `pub mod push;` — DELETE
- Line 13: `pub mod http;` — DELETE
- Line 14: `pub mod http_admin;` — DELETE
- Line 18: `pub mod push_and_get;` — DELETE
- `pub mod shutdown;` — KEEP (consumed by main.rs and ServerV18)
- Other modules (apply_shard, runtime_core_glue, server, testing, temporal_http, feature_query, register, registry_debug, recovery, snapshot_task, etc.) — KEEP (reference: lines 10–28).

**temporal_http.rs — RESOLVED DECISION (Mixed: keep helpers; delete axum handlers + temporal_router):**

Verified contents (per `grep -n "fn\\|pub\\|axum" crates/beava-server/src/temporal_http.rs`):

DELETE — axum handler signatures and the router (axum-coupled):
- Line 183: `async fn upsert_handler(State(...), body) -> Response` — full delete
- Line 323: `async fn delete_handler(State(...), body) -> Response` — full delete
- Line 480: `async fn retract_handler(State(app): State<Arc<AppState>>, body_bytes: Bytes) -> Response` — full delete
- Line 632: `async fn table_get_handler(...)` — full delete
- Line 724: `pub fn temporal_router(state: Arc<AppState>) -> Router` — full delete (the router that mounted the four handlers)
- Line 34 axum imports + line 82/91 `axum::body::Body::from(bytes)` calls inside `error_response` / `ok_response` — REWRITE to return `(StatusCode, Bytes)` tuples or use a non-axum response type that ServerV18's mio HTTP layer accepts. ServerV18 has its own response-encoding shape; verify by reading the mio HTTP response writer in apply_shard.rs / server.rs.

KEEP — non-axum helpers consumed by ServerV18 mio path (cite specific functions):
- Line 55: `fn parse_query_pairs(q: &str) -> HashMap<String, String>` — pure utility
- Line 70: `fn now_ms() -> u64` — pure utility
- Line 97: `fn entity_key_from_body(body, primary_key, primary_key_len) -> Option<Vec<u8>>` — pure logic
- Line 121: `fn entity_key_from_str(value: &str, primary_key_len: usize) -> Option<Vec<u8>>` — pure logic
- Line 131: `fn json_object_to_row(body: &serde_json::Map<String, JsonValue>) -> Row` — pure logic
- Line 154: `fn row_to_json(row: &Row) -> serde_json::Value` — pure logic
- Line 77 `fn error_response`, line 86 `fn ok_response` — REWRITE without axum's `Response`/`Body` types (return Bytes + StatusCode; ServerV18 mio HTTP serializer wraps it)

This is a Mixed-delete (NOT a full file delete). The acceptance criterion grep below confirms zero `axum::` references in temporal_http.rs post-edit.

**shutdown.rs — RESOLVED DECISION (KEEP, already wired into ServerV18):**

Verified contents (per direct read of `crates/beava-server/src/shutdown.rs`):
- `pub async fn shutdown_signal()` — listens for SIGTERM/SIGINT via `tokio::signal::unix::signal`. The implementation is tokio-only (which is fine — admin sidecar runs on tokio, and main.rs orchestrates the whole boot via tokio).
- main.rs:24 imports it: `use beava_server::{banner, cli::Cli, logging, shutdown::shutdown_signal, ServerV18};`
- main.rs:82 invokes it inside the ServerV18 boot path: `... shutdown_signal(), ...`
- http_admin.rs:159–164 ALSO uses it (for the legacy axum admin server's `with_graceful_shutdown(shutdown_signal)`). This usage goes away when http_admin.rs is deleted (Task 1.b).

Conclusion: `shutdown.rs::shutdown_signal()` is the canonical graceful-shutdown future for ServerV18 + the tokio admin sidecar. KEEP the file as-is (the function body is correct). Only update the doc-comment at lines 1–5: replace the "Passed to `axum::serve(...).with_graceful_shutdown(...)`" wording with "Passed to `ServerV18::serve_with_dirs(...)` and the tokio admin-sidecar router (which uses `axum::serve` internally on the tokio admin port)." This single doc-comment edit is the only modification.

NO new wiring is needed: shutdown_signal is ALREADY consumed by ServerV18's main.rs callsite. Plan 07 just deletes the legacy http_admin.rs callsite (which goes away with the file delete) and updates the doc-comment for accuracy.

feature_query.rs:
- Mixed: query helper functions (e.g., feature_query_engine, lookup helpers) used by both legacy axum /get and mio path — KEEP. The ones taking axum::extract are legacy — verify via signature inspection.

register.rs:
- The HTTP /register axum handler (around line 1187 — `dev_endpoints=true so GET /registry works`). The async register helper (`register_payload_to_outcome` or whatever the core function is) is reused by ServerV18. Delete the axum-specific wrapper; keep the inner function.

registry_debug.rs:
- BEAVA_DEV_ENDPOINTS-gated `/dev/apply_ops` and `GET /registry` axum handlers (lines 134, 339, 353) — DELETE the axum handlers. The /registry endpoint moves to the admin sidecar (which has its own gating posture).

BEAVA_DEV_ENDPOINTS env-var:
- Read sites: server.rs:108, main.rs (probably), registry_debug.rs:3, http.rs:80/161, register.rs:1449. Delete all reads. Delete the env-var documentation comment in main.rs:16.

TestServer.dev_endpoints (Plan 01 left as no-op shim):
- testing.rs has `.dev_endpoints(...)` as a no-op builder method. Delete it now (post-Plan-07, no caller cares).

Tests that need migration or deletion:
- crates/beava-server/tests/cli_smoke.rs, foundation_smoke.rs, logging_smoke.rs, debug_op_cost_endpoint.rs — verify each. cli_smoke and foundation_smoke probably test ServerV18 already (post-Plan-01); debug_op_cost_endpoint may test BEAVA_DEV_ENDPOINTS gating (find via grep) — if so, migrate to admin-sidecar gating or delete.
- phase18_07_no_tokio_dataplane_test.rs — already asserts no tokio data plane; should pass after this plan strengthens it.
- phase6_crash_probe — referenced in CONTEXT as needing migration; check current state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (red): Architectural test asserts legacy Server struct does not exist + push.rs/http.rs/http_admin.rs files do not exist + temporal_http.rs has zero axum:: references</name>
  <files>crates/beava-server/tests/phase12_6_legacy_axum_killed.rs (new)</files>
  <read_first>
    - crates/beava-server/src/server.rs (current legacy Server struct location)
    - crates/beava-server/src/push.rs (full)
    - crates/beava-server/src/temporal_http.rs (full — verify the four axum handlers exist today)
    - crates/beava-server/tests/phase18_07_no_tokio_dataplane_test.rs (existing pattern — uses include_str on Cargo.toml or the source root)
  </read_first>
  <behavior>
    - Test 1: legacy_axum_files_deleted. Asserts `std::path::Path::new("crates/beava-server/src/push.rs").exists() == false`, same for http.rs / http_admin.rs / push_and_get.rs. Run from the workspace root via `env!("CARGO_MANIFEST_DIR")` on `beava-server` then walk up two levels.
    - Test 2: legacy_server_struct_deleted. Reads crates/beava-server/src/server.rs via include_str! and asserts it does NOT contain `pub struct Server {` (only `pub struct ServerV18 {`).
    - Test 3: dispatch_wire_request_async_deleted. Reads crates/beava-server/src/runtime_core_glue.rs via include_str! and asserts no occurrence of `pub async fn dispatch_wire_request(` (only the `_sync` variants survive).
    - Test 4: beava_dev_endpoints_env_var_deleted. greps the workspace src/ for `BEAVA_DEV_ENDPOINTS` and asserts zero occurrences.
    - Test 5 (NEW per BLOCKER #3 fix): temporal_http_axum_handlers_deleted. Reads crates/beava-server/src/temporal_http.rs via include_str! and asserts ZERO occurrences of `axum::`, `Router::new`, `upsert_handler`, `delete_handler`, `retract_handler`, `table_get_handler`, `temporal_router`. Also asserts the helpers ARE preserved: at least one occurrence of `fn json_object_to_row`, `fn entity_key_from_body`, `fn row_to_json`. (Confirms Mixed-delete landed correctly.)
    - Test 6 (NEW per BLOCKER #3 fix): shutdown_signal_preserved_and_used_by_serverv18. Reads crates/beava-server/src/shutdown.rs via include_str! and asserts `pub async fn shutdown_signal(` exists. Reads main.rs via include_str! and asserts `shutdown_signal` is invoked. Asserts the doc-comment in shutdown.rs no longer cites `axum::serve` (post-update mentions ServerV18).
  </behavior>
  <action>
    Create crates/beava-server/tests/phase12_6_legacy_axum_killed.rs with the six test functions. Use:

      const PUSH_RS: &str = include_str!("../src/push.rs"); // RED: file still exists, include_str will compile but file is not yet deleted

    Wait — include_str! REQUIRES the file to exist at compile time. So a "file deleted" test cannot include_str its absence. The cleanest red form is a runtime path-existence check:

      let workspace_root = std::path::Path::new(env!("CARGO_MANIFEST_DIR")).parent().unwrap().parent().unwrap();
      let push_rs = workspace_root.join("crates/beava-server/src/push.rs");
      assert!(!push_rs.exists(), "push.rs must be deleted post-Plan-12.6-07");

    Currently this fails (RED) because the file exists.

    For the dispatch_wire_request async check, since runtime_core_glue.rs continues to exist (sync helpers stay), use include_str! to read its current contents and assert no `pub async fn dispatch_wire_request(`.

    For the legacy Server struct check, since server.rs continues to exist (ServerV18 stays), use include_str! and assert no `pub struct Server {` (with the trailing space + brace to disambiguate from `pub struct ServerV18 {`).

    For BEAVA_DEV_ENDPOINTS, walk the entire src/ tree at runtime and grep:

      let server_src = workspace_root.join("crates/beava-server/src");
      let count = walkdir::WalkDir::new(&server_src).into_iter()
          .filter_map(Result::ok)
          .filter(|e| e.path().extension().map_or(false, |ext| ext == "rs"))
          .map(|e| std::fs::read_to_string(e.path()).unwrap_or_default())
          .filter(|s| s.contains("BEAVA_DEV_ENDPOINTS"))
          .count();
      assert_eq!(count, 0, "BEAVA_DEV_ENDPOINTS must be removed from all server src files");

    (`walkdir` is a common dev-dep; if unavailable, glob via std::fs::read_dir manually.)

    For temporal_http.rs Mixed-delete (Test 5), use a runtime path-read since temporal_http.rs continues to exist (helpers preserved):

      let temporal = std::fs::read_to_string(workspace_root.join("crates/beava-server/src/temporal_http.rs")).expect("temporal_http.rs exists");
      assert!(!temporal.contains("axum::"), "temporal_http.rs must have zero axum:: references post-Plan-12.6-07");
      assert!(!temporal.contains("upsert_handler"), "axum upsert_handler must be deleted");
      assert!(!temporal.contains("delete_handler"), "axum delete_handler must be deleted");
      assert!(!temporal.contains("retract_handler"), "axum retract_handler must be deleted");
      assert!(!temporal.contains("table_get_handler"), "axum table_get_handler must be deleted");
      assert!(!temporal.contains("temporal_router"), "axum temporal_router must be deleted");
      // Helpers preserved
      assert!(temporal.contains("fn json_object_to_row"), "json_object_to_row helper must be preserved");
      assert!(temporal.contains("fn entity_key_from_body"), "entity_key_from_body helper must be preserved");
      assert!(temporal.contains("fn row_to_json"), "row_to_json helper must be preserved");

    For shutdown.rs (Test 6):

      let shutdown = std::fs::read_to_string(workspace_root.join("crates/beava-server/src/shutdown.rs")).expect("shutdown.rs exists");
      assert!(shutdown.contains("pub async fn shutdown_signal"), "shutdown_signal preserved");
      assert!(!shutdown.contains("axum::serve"), "stale axum::serve doc reference must be updated to ServerV18");
      let main_rs = std::fs::read_to_string(workspace_root.join("crates/beava-server/src/main.rs")).expect("main.rs exists");
      assert!(main_rs.contains("shutdown_signal"), "main.rs continues to invoke shutdown_signal");

    All six tests FAIL today. Commit `test(12.6-07): RED — legacy axum data plane still present (per D-01 drop-in TestServer + project_phase18_no_dual_runtime mio-only data plane)`.
  </action>
  <verify>
    <automated>cargo test -p beava-server --test phase12_6_legacy_axum_killed 2>&1 | grep -E "FAILED|test result"</automated>
  </verify>
  <done>Test file exists with six tests; all six fail. Commit lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (green): Delete push.rs / http.rs / http_admin.rs / push_and_get.rs; remove from lib.rs; chase compile errors</name>
  <files>crates/beava-server/src/push.rs (DELETE), crates/beava-server/src/http.rs (DELETE), crates/beava-server/src/http_admin.rs (DELETE), crates/beava-server/src/push_and_get.rs (DELETE), crates/beava-server/src/lib.rs</files>
  <read_first>
    - crates/beava-server/src/lib.rs (full)
    - All four files about to be deleted (full — confirm no orthogonal helpers used elsewhere)
  </read_first>
  <action>
    **per D-01 (drop-in TestServer rewrite — Plan 01 already migrated TestServer onto ServerV18) + `project_phase18_no_dual_runtime` (mio-only data plane)**

    Step 1 — Confirm no external callers of items in the four target files. Run:
      grep -rn "use beava_server::push::" crates/ python/
      grep -rn "use beava_server::http::" crates/
      grep -rn "use beava_server::http_admin::" crates/
      grep -rn "use beava_server::push_and_get::" crates/
    Expected: zero matches outside the four files themselves. If any external caller exists, they must be migrated to ServerV18 first — surface to the user. Default expectation per planning is zero external callers (post-Plan-12-07 confirmed migration).

    Step 2 — Delete the four files:
      git rm crates/beava-server/src/push.rs
      git rm crates/beava-server/src/http.rs
      git rm crates/beava-server/src/http_admin.rs
      git rm crates/beava-server/src/push_and_get.rs

    Step 3 — Remove their `pub mod` declarations from crates/beava-server/src/lib.rs:
      Delete lines 13 (pub mod http;), 14 (pub mod http_admin;), 17 (pub mod push;), 18 (pub mod push_and_get;).
      KEEP `pub mod shutdown;` — it's consumed by main.rs and ServerV18.

    Step 4 — Compile:
      cargo build --workspace 2>&1 | tail -80
    Errors will surface from any remaining references. Common offenders:
      - server.rs's legacy Server::serve() at line 276 calls into http::router(...) → these blocks delete in Task 2.
      - register.rs's router builder calls — these are part of the legacy Server::bind() path; delete in Task 2.
      - testing.rs's old Server::bind path — already gone post-Plan-01; verify with grep.
      - http_admin.rs's `shutdown_signal` import in its with_graceful_shutdown call — this disappears with the http_admin.rs file delete; shutdown.rs itself is preserved.
      - Any test file: ones that imported beava_server::push::... must be migrated to apply_shard.rs::dispatch_*_sync or deleted.

    Step 5 — For each compile error, decide:
      - If the error is from server.rs's legacy `Server` impl → leave for Task 2 (don't try to fix piecemeal).
      - If the error is from a test or helper outside the legacy Server impl → fix it now (migrate to ServerV18 or delete the test).

    Step 6 — Commit `refactor(12.6-07): delete legacy push.rs/http.rs/http_admin.rs/push_and_get.rs; remove module declarations`. The full workspace will NOT compile yet; the next task fixes server.rs.

    NOTE on staged commits: it's idiomatic in TDD to commit a non-compiling state for one logical step when the next task fixes the cascade. The pre-commit hook (`cargo test --workspace`) will fail. Per CLAUDE.md: TDD discipline mandates the test-first rule, and the verify gate runs full workspace tests at the END of each plan, not after every commit. The CI gate runs against the post-Task-2 state. If the project's pre-commit hook BLOCKS the non-compiling commit, instead bundle Task 1.b + Task 2.b into a single commit (smaller scope-spread but still TDD-aligned: Task 1.a's RED is what the GREEN merges to GREEN).

    Bundle decision: per CLAUDE.md TDD §1, every plan task produces ≥2 atomic commits — the red commit lands FIRST. Task 1.a is the red. Task 1.b's incomplete-compile state is acceptable IF the project hooks tolerate it; otherwise Task 1.b and Task 2.b merge into one green commit "feat(12.6-07): delete entire legacy axum data plane (push.rs + http.rs + http_admin.rs + push_and_get.rs + Server struct + dispatch_wire_request async + temporal_http axum handlers)".

    Default behavior: try Task 1.b commit first. If hook blocks it, merge with 2.b.
  </action>
  <verify>
    <automated>! ls crates/beava-server/src/push.rs crates/beava-server/src/http.rs crates/beava-server/src/http_admin.rs crates/beava-server/src/push_and_get.rs 2>/dev/null && ! grep -E "pub mod (push|http|http_admin|push_and_get);" crates/beava-server/src/lib.rs</automated>
  </verify>
  <done>The four files are deleted from disk; lib.rs no longer declares their modules. shutdown.rs continues to exist; lib.rs continues to declare `pub mod shutdown;`. Commit lands (or bundle is queued for Task 2.b).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.b (green): Delete legacy `Server` struct + impl from server.rs; delete dispatch_wire_request async; delete BEAVA_DEV_ENDPOINTS infrastructure; mixed-delete temporal_http.rs axum handlers + temporal_router (keep helpers); update shutdown.rs doc-comment; chase final compile errors; full workspace green</name>
  <files>crates/beava-server/src/server.rs, crates/beava-server/src/main.rs, crates/beava-server/src/runtime_core_glue.rs, crates/beava-server/src/register.rs, crates/beava-server/src/registry_debug.rs, crates/beava-server/src/temporal_http.rs, crates/beava-server/src/feature_query.rs, crates/beava-server/src/testing.rs, crates/beava-server/src/shutdown.rs</files>
  <read_first>
    - crates/beava-server/src/server.rs (lines 54–370 — the entire legacy `Server` impl block)
    - crates/beava-server/src/runtime_core_glue.rs (lines 85–352 — the async `dispatch_wire_request`)
    - crates/beava-server/src/main.rs (full)
    - crates/beava-server/src/register.rs lines 1180–1450 (legacy axum register handler — confirm boundary with the inner register fn used by mio glue)
    - crates/beava-server/src/registry_debug.rs (BEAVA_DEV_ENDPOINTS-gated routes)
    - crates/beava-server/src/temporal_http.rs (full — confirm the resolved Mixed-delete decision in <interfaces>: delete upsert_handler/delete_handler/retract_handler/table_get_handler/temporal_router; keep entity_key_from_body / json_object_to_row / row_to_json / parse_query_pairs / now_ms / error_response / ok_response after rewriting the latter two to drop axum types)
    - crates/beava-server/src/feature_query.rs (axum branches vs mio-side helpers)
    - crates/beava-server/src/testing.rs (Plan 01 dev_endpoints no-op shim)
    - crates/beava-server/src/shutdown.rs (full — only doc-comment lines 1-5 need updating)
  </read_first>
  <action>
    **per D-01 (drop-in TestServer rewrite) + `project_phase18_no_dual_runtime` (mio-only data plane) — these decisions establish that ServerV18 is the only data-plane runtime; Plan 07 finishes the kill of the legacy Server's surface area.**

    Step 1 — server.rs surgery:
      a. Delete lines 54–369 (legacy `Server` struct + impl block; verify the boundary by finding the line where `pub struct ServerV18 {` starts — should be ~370). The deleted range includes:
         - `pub struct Server { ... }` declaration
         - `impl Server { pub async fn bind(...) -> Result<Self, ServerError> { ... } pub async fn serve<F>(...) -> Result<(), ServerError> { ... } ... }`
         - All other helpers exclusively for legacy Server.
      b. Delete `dev_endpoints: bool` field on legacy Server (already inside the deleted block) — confirm.
      c. Delete the test mod entries at server.rs:2257+ that test the legacy Server (keep ServerV18-targeted tests). Verify each test's setup: if it builds `Server::bind(...)`, delete; if it builds `ServerV18::bind(...)`, keep.

    Step 2 — runtime_core_glue.rs:
      a. Delete lines 85–352: `pub async fn dispatch_wire_request(app: &Arc<AppState>, req: WireRequest) -> GlueResponse { ... }` and any helper async fns (dispatch_push_async, dispatch_get_async, etc.) that exist exclusively for the legacy axum path.
      b. Keep the sync helpers (dispatch_get_single_sync at line 353, dispatch_get_batch_sync at line 365, and any others in the 353–631 range). These are used by apply_shard.rs and stay.

    Step 3 — main.rs:
      a. Delete any environment variable check that boots legacy Server vs ServerV18 (per Plan 12-07 the production binary already boots ServerV18; this is a final cleanup if any conditional remains).
      b. Delete the BEAVA_DEV_ENDPOINTS comment at line 16 (the env-var infrastructure is going away).
      c. KEEP the `use beava_server::shutdown::shutdown_signal` import at line 24 (it's the canonical shutdown future for ServerV18).
      d. KEEP the `shutdown_signal()` invocation at line 82 (already wired into ServerV18).
      e. Verify main.rs still compiles by booting ServerV18 with default config.

    Step 4 — register.rs:
      a. Find the axum-handler for /register (around line 1187 per CONTEXT). Delete the handler function (it's a `pub async fn register_handler(State(app), ...) -> impl IntoResponse` shape). Keep the inner `register_payload_to_outcome` (or equivalent — find via grep) which the mio-side glue uses.
      b. Delete `dev_endpoints: bool` parameters from helper functions (line 1449 `.dev_endpoints(false)` reference) — these are no longer meaningful.

    Step 5 — registry_debug.rs:
      a. Delete the axum-handler for `/registry` (lines 134-ish where it returns `axum::Json(...)`). The /registry endpoint moves to the admin sidecar (it's part of ServerV18's tokio admin sidecar already; verify).
      b. Delete the BEAVA_DEV_ENDPOINTS gating (lines 3, 134, 339, 353 doc-comments + runtime checks).
      c. Delete the test at line 792-813 that asserts gating with dev_endpoints=false (the gating no longer exists).
      d. Keep the core registry-debug helper functions (`format_registry_state`, etc.) — used by mio-side admin endpoint.

    Step 6 — temporal_http.rs Mixed-delete (per the resolved <interfaces> decision):
      a. Delete the `axum::` use statement at line 34 entirely.
      b. Delete `async fn upsert_handler` (line 183 onward, full function body).
      c. Delete `async fn delete_handler` (line 323 onward, full function body).
      d. Delete `async fn retract_handler` (line 480 onward, full function body).
      e. Delete `async fn table_get_handler` (line 632 onward, full function body).
      f. Delete `pub fn temporal_router` (line 724 onward, full function body).
      g. Rewrite `fn error_response` (line 77) and `fn ok_response` (line 86): change return type from `Response` (axum) to a tuple `(StatusCode, Bytes)` or to ServerV18's mio HTTP response shape. Find the equivalent shape via `grep -n "fn .*_response\\|http_response\\|HttpResponse" crates/beava-server/src/apply_shard.rs crates/beava-server/src/server.rs` — use the canonical mio response shape.
      h. KEEP `fn parse_query_pairs`, `fn now_ms`, `fn entity_key_from_body`, `fn entity_key_from_str`, `fn json_object_to_row`, `fn row_to_json` — these are pure utilities consumed by ServerV18 mio routes.
      i. Verify post-edit grep: `grep -c "axum::" crates/beava-server/src/temporal_http.rs` returns 0; `grep -c "fn json_object_to_row\\|fn entity_key_from_body" crates/beava-server/src/temporal_http.rs` returns 2.

    Step 7 — feature_query.rs:
      a. Delete axum handler signatures (same pattern).
      b. Keep query helpers (FeatureQueryEngine, etc.) — used by mio /get path.

    Step 8 — testing.rs:
      a. Delete the `dev_endpoints: bool` field on TestServerBuilder (Plan 01 left it as a no-op shim).
      b. Delete the `.dev_endpoints(...)` builder method.
      c. Any test that calls `.dev_endpoints(true)` will now fail to compile — fix by deleting the call (the gated /registry endpoint is now on the admin sidecar always-on).

    Step 9 — shutdown.rs doc-comment update:
      a. Open crates/beava-server/src/shutdown.rs.
      b. Lines 1–5 currently read:
         ```
         //! Graceful shutdown signal listener.
         //!
         //! Returns a future that completes when SIGTERM or SIGINT is received. Passed to
         //! `axum::serve(...).with_graceful_shutdown(...)` so in-flight requests drain before
         //! the listener closes.
         ```
      c. Replace with:
         ```
         //! Graceful shutdown signal listener.
         //!
         //! Returns a future that completes when SIGTERM or SIGINT is received. Passed to
         //! `ServerV18::serve_with_dirs(...)` and the tokio admin-sidecar router (the latter
         //! uses `axum::serve` internally on the tokio admin port). The mio data plane reads
         //! the same future to gate its accept loop.
         ```
      d. Function body and signature unchanged — only the doc comment.

    Step 10 — Compile + chase:
      cargo build --workspace 2>&1 | tail -100
      Fix every error. Common patterns:
        - "no method named `dev_endpoints`" → delete the .dev_endpoints(...) call
        - "no struct named `Server`" → migrate test to ServerV18 (use TestServer)
        - "no function `dispatch_wire_request`" → migrate to dispatch_wire_request_sync via ApplyShard
        - "unresolved import `axum`" inside temporal_http.rs → re-check Step 6 for missed axum-coupled paths

    Step 11 — Run full test suite:
      cargo test --workspace --all-features
    All green. The Task 1.a tests (legacy_axum_files_deleted, legacy_server_struct_deleted, dispatch_wire_request_async_deleted, beava_dev_endpoints_env_var_deleted, temporal_http_axum_handlers_deleted, shutdown_signal_preserved_and_used_by_serverv18) all pass.

    Step 12 — Commit `feat(12.6-07): delete legacy `Server` struct + dispatch_wire_request async + BEAVA_DEV_ENDPOINTS + temporal_http axum handlers; preserve shutdown.rs (consumed by ServerV18)`.
  </action>
  <verify>
    <automated>cargo test --workspace --all-features 2>&1 | grep -E "test result.*ok|FAILED" | tail -10 ; cargo test -p beava-server --test phase12_6_legacy_axum_killed 2>&1 | grep -E "test result.*ok" ; grep -c "axum::" crates/beava-server/src/temporal_http.rs | grep -E "^0$" ; grep -c "fn json_object_to_row" crates/beava-server/src/temporal_http.rs | grep -E "^[1-9]" ; grep -c "ServerV18::serve_with_dirs" crates/beava-server/src/shutdown.rs | grep -E "^[1-9]"</automated>
  </verify>
  <done>cargo test --workspace --all-features fully green. The six Task 1.a architectural tests all pass. Legacy push.rs / http.rs / http_admin.rs / push_and_get.rs files are gone. Legacy `Server` struct gone from server.rs. dispatch_wire_request async gone from runtime_core_glue.rs. BEAVA_DEV_ENDPOINTS gone everywhere. temporal_http.rs has zero `axum::` references; helpers preserved. shutdown.rs preserved with updated doc-comment citing ServerV18 instead of axum::serve. Commit lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inbound HTTP (data plane) | Single-path through ServerV18 mio HTTP listener — eliminates the second axum entry point |
| Inbound TCP (data plane) | Single-path through ServerV18 mio TCP listener |
| Inbound HTTP (admin) | Tokio sidecar on separate port — preserved per project_phase18_no_dual_runtime |
| Process shutdown | shutdown_signal() future drives both ServerV18 mio loop AND tokio admin sidecar's graceful drain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.6.07-01 | Tampering | Stale client tries to hit legacy /push-and-get URL | accept | Server returns 404 (no route registered) — D-04 keeps the feature deferred; documented in Phase 13 docs |
| T-12.6.07-02 | Information Disclosure | BEAVA_DEV_ENDPOINTS leaked /registry data — now no env-var path | mitigate | The env-var infrastructure is deleted; admin /registry has its own gating (existing posture preserved on the tokio sidecar) |
| T-12.6.07-03 | DoS | mio data plane is the only entry point — single-thread saturation risk | accept | Per project_no_sharded_apply: single apply thread is the v0 commitment; users scale via multi-instance Redis-cluster pattern |
| T-12.6.07-04 | Repudiation | Test deletions hide intent | mitigate | Each deleted test that exercised legacy axum-only behavior is replaced by an equivalent ServerV18 test OR explicitly noted in 12.6-07-SUMMARY.md as "coverage replicated by phase12_6_*" |
| T-12.6.07-05 | DoS | shutdown_signal future never resolves on signal racing with worker drain | accept | tokio::select! over SIGTERM/SIGINT is well-tested; v0 acceptable risk |
</threat_model>

<verification>
1. cargo test --workspace --all-features fully green.
2. cargo clippy --workspace --all-targets --all-features -- -D warnings clean.
3. cargo fmt --all --check clean.
4. `! ls crates/beava-server/src/push.rs crates/beava-server/src/http.rs crates/beava-server/src/http_admin.rs crates/beava-server/src/push_and_get.rs 2>/dev/null` returns success (files don't exist).
5. `grep -c "pub struct Server {" crates/beava-server/src/server.rs` returns 0 (only `pub struct ServerV18 {` remains).
6. `grep -cE "pub async fn dispatch_wire_request\\b" crates/beava-server/src/runtime_core_glue.rs` returns 0.
7. `grep -rn "BEAVA_DEV_ENDPOINTS" crates/beava-server/src/` returns 0 lines.
8. `grep -c "dev_endpoints" crates/beava-server/src/testing.rs` returns 0.
9. `grep -rn "axum::" crates/beava-server/src/` returns ONLY references inside the ServerV18 admin sidecar block in server.rs (which is OK — admin stays on tokio per the architectural commitment). `grep -c "axum::" crates/beava-server/src/temporal_http.rs` returns 0.
10. crates/beava-server/tests/phase12_6_legacy_axum_killed.rs has all six tests green (added two: temporal_http_axum_handlers_deleted, shutdown_signal_preserved_and_used_by_serverv18).
11. main.rs boots ServerV18 unconditionally — `grep -c "Server::bind" crates/beava-server/src/main.rs` returns 0.
12. shutdown.rs preserved — `grep -c "pub async fn shutdown_signal" crates/beava-server/src/shutdown.rs` returns 1; `grep -c "axum::serve" crates/beava-server/src/shutdown.rs` returns 0 (doc-comment updated); `grep -c "ServerV18::serve_with_dirs" crates/beava-server/src/shutdown.rs` returns 1 (new doc-comment).
13. Three commits land: test(12.6-07) RED, refactor(12.6-07) file deletes (or bundled), feat(12.6-07) struct + async dispatch + env-var deletes + temporal_http Mixed-delete + shutdown.rs doc-comment update.
</verification>

<success_criteria>
1. ~3500 LOC of legacy axum code deleted across push.rs, http.rs, http_admin.rs, push_and_get.rs, server.rs (legacy Server impl), runtime_core_glue.rs (async dispatch_wire_request), temporal_http.rs (axum handlers + temporal_router), and BEAVA_DEV_ENDPOINTS infrastructure.
2. ServerV18 is preserved (server.rs:370+) and remains the production binary entry.
3. Tokio admin sidecar (separate port) is preserved per project_phase18_no_dual_runtime.
4. **shutdown.rs preserved** — already consumed by ServerV18 via main.rs:82; only the stale axum::serve doc-comment is updated to cite ServerV18::serve_with_dirs.
5. **temporal_http.rs Mixed-delete:** axum handlers (upsert_handler, delete_handler, retract_handler, table_get_handler) + temporal_router DELETED; helpers (entity_key_from_body, json_object_to_row, row_to_json, parse_query_pairs, now_ms, entity_key_from_str) PRESERVED for ServerV18 mio reuse.
6. Workspace tests fully green; the six architectural tests in phase12_6_legacy_axum_killed.rs all pass.
7. ROADMAP success criteria 1, 3 satisfied: cargo test green post-deletes; grep -rn "axum::" returns matches only on the ServerV18 admin sidecar block.
</success_criteria>

<output>
After completion, create `.planning/phases/12.6-v0-surface-reduction/12.6-07-SUMMARY.md` covering:
- Total LOC deleted (per file: push.rs, http.rs, http_admin.rs, push_and_get.rs, server.rs legacy block, runtime_core_glue.rs async block, temporal_http.rs axum handlers + temporal_router, BEAVA_DEV_ENDPOINTS scattered)
- The exact diff of `pub mod` declarations removed from lib.rs (note `pub mod shutdown;` PRESERVED)
- The temporal_http.rs Mixed-delete inventory: which functions were deleted (handlers + router) vs preserved (helpers); cite line numbers from the pre-edit file
- The shutdown.rs doc-comment update (verbatim before/after) and confirmation that ServerV18 already consumes shutdown_signal via main.rs:82
- Test files that needed migration vs deletion (with one-line rationale per test)
- Confirmation that admin sidecar (tokio) is on a separate port from the mio data plane
- Any axum:: imports that survived (admin sidecar only, in server.rs ServerV18 block) and the file:line where they live
</output>
</content>
</invoke>