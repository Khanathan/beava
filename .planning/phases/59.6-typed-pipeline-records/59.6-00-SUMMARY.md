---
phase: 59.6-typed-pipeline-records
plan: 00
subsystem: testing
tags: [red-scaffolding, typed-rows, parity-harness, criterion, grep-gate, schema-registry]

# Dependency graph
requires:
  - phase: 59-binary-wire-format-for-push
    provides: binary OP_PUSH_BATCH frame + ConcurrentAppState counter pre-seed convention (Phase 59 D-C3/D-A3)
  - phase: 59.5-shared-source-tables-for-enrich
    provides: beava_shard_push_phase_seconds histograms (59.5-W3.5 8.5us baseline)
provides:
  - 11 RED integration test files (21 ignored tests) tagged 59.6-W[1-6]
  - tests/typed_row_parity.rs parity-harness stub
  - scripts/verify-typed-path.sh grep-gate (exit 1 pre-Wave-1, exit 0 at Wave 7 close)
  - benches/typed_pipeline_phase_latency.rs Criterion bench stub
  - typed_row_path_total + value_fallback_path_total AtomicU64 pre-seeds on ConcurrentAppState
  - TPC-PERF-11 requirement row + TPC-CORR-07 Phase-59.6 extension
affects:
  - 59.6-01 (schema runtime + registry; Wave 1)
  - 59.6-02 (wire codec schema_id prefix; Wave 2)
  - 59.6-03 through 59.6-06 (operators)
  - 59.6-07 (perf-gate ship wave)

# Tech tracking
tech-stack:
  added: []  # Wave 0 adds no new dependencies
  patterns:
    - "RED-scaffolding Wave 0 pattern: ignored tests + grep-gate + bench stub + counter pre-seed; mirrors Phase 57-W0 / 58-W0 / 59-W0 cadence"
    - "AtomicU64 counter pre-seed on ConcurrentAppState — hot-path struct layout locked before Wave 1+ wiring"
    - "Plan-owned deferred-items.md for pre-existing out-of-scope blockers"

key-files:
  created:
    - tests/typed_row_parity.rs (SC-4 smoke + parity-harness stub)
    - tests/register_typed_schema.rs (SC-1)
    - tests/typed_push_ingest.rs (SC-2)
    - tests/typed_enrich_from_table.rs (SC-3)
    - tests/typed_aggregation_parity.rs (SC-4)
    - tests/typed_state_store_roundtrip.rs (SC-8)
    - tests/typed_snapshot_v11_migration.rs (SC-10)
    - tests/typed_mixed_mode_coexistence.rs (SC-7)
    - tests/typed_value_fallback_unregressed.rs (SC-6)
    - tests/typed_ssj_crossshard_parity.rs (SC-9)
    - tests/typed_python_sdk_handshake.rs (SC-6)
    - scripts/verify-typed-path.sh
    - benches/typed_pipeline_phase_latency.rs
    - .planning/phases/59.6-typed-pipeline-records/deferred-items.md
  modified:
    - .planning/REQUIREMENTS.md (TPC-PERF-11 row + TPC-CORR-07 extension + Traceability 40/40)
    - src/server/tcp.rs (typed_row_path_total + value_fallback_path_total fields on ConcurrentAppState)
    - Cargo.toml ([[bench]] registration for typed_pipeline_phase_latency)

key-decisions:
  - "Wave 0 bench body is a zero-work noop (not a panic) so cargo bench --no-run gates pass before RegisteredSchema infra lands in Wave 1"
  - "Pre-existing Phase 60 StreamDefinition.salt test-sweep failures are out-of-scope — documented in deferred-items.md, not auto-fixed"
  - "21 RED tests across 11 files (plan target was 14+) — covers SC-1 through SC-10 with W1-W6 staging"

patterns-established:
  - "Pattern 1: Each RED test panics with 'SC-N RED: ...' identifier so Wave-N flips are visible per SC"
  - "Pattern 2: Grep-gate checks 5 invariants (RegisteredSchema, Row repr(C), engine accessors, schema_id prefix, counter increments) with exit 1 until Wave 7 close"
  - "Pattern 3: Force-add .planning/ files with 'git add -f' (.planning/ is gitignored)"

requirements-completed:
  - TPC-PERF-11  # Newly added; Wave 0 plants the scaffolding, Wave 7 closes the gate
  - TPC-CORR-07  # Extension bullet added; Phase-55 row stays [x], extension is a 59.6 sub-bullet

# Metrics
duration: 8min
completed: 2026-04-21
---

# Phase 59.6 Plan 00: Wave 0 RED Scaffolding Summary

**11 RED integration tests + parity harness stub + grep-gate + per-event latency bench + 2 pre-seeded counter fields + TPC-PERF-11/TPC-CORR-07 requirements rows — Wave 0 acceptance surface locked before any production code touches typed rows.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-21T18:19:30Z
- **Completed:** 2026-04-21T18:27:44Z
- **Tasks:** 5 / 5
- **Files created:** 14 (11 tests + 1 script + 1 bench + 1 deferred-items)
- **Files modified:** 3 (REQUIREMENTS.md, src/server/tcp.rs, Cargo.toml)
- **Commits:** 6

## Accomplishments

- REQUIREMENTS.md gains TPC-PERF-11 (Phase 59.6 perf gate: 4,542,285 EPS floor; 2.0μs pipeline avg; down from 8.5μs 59.5-W3.5 baseline) + TPC-CORR-07 Phase 59.6 extension bullet + Traceability row; Coverage updated 39/39 → 40/40.
- `ConcurrentAppState` gains two `AtomicU64` pre-seeds (`typed_row_path_total`, `value_fallback_path_total`), both doc-commented citing D-E2 TPC-PERF-11, both initialized to 0 in `make_concurrent_state_full`. Wave 1+ typed-path wiring bumps them; Wave 7 exposes them as Prometheus counters.
- `scripts/verify-typed-path.sh` grep-gate exits 1 with `GATE FAIL: RegisteredSchema not yet defined` (the expected Wave 0 RED state). Flips to exit 0 at Wave 7 close when all 5 invariants hold.
- 11 test files planted under `tests/` (10 per-SC + 1 parity-harness module), 21 ignored tests tagged `#[ignore = "59.6-W[1-6]"]`. Each RED test panics with `SC-N RED: …` marker for per-SC flip visibility.
- `benches/typed_pipeline_phase_latency.rs` Criterion bench (Wave 0 noop stub) registered in Cargo.toml; `cargo bench --no-run` gate passes.

## Task Commits

| Task | Name | Commit | Type |
|------|------|--------|------|
| 1 | TPC-PERF-11 + TPC-CORR-07 extension in REQUIREMENTS.md | `08fc600` | docs |
| 2 | typed_row_path_total + value_fallback_path_total fields on ConcurrentAppState | `6ef82f6` | feat |
| 3 | scripts/verify-typed-path.sh grep-gate | `08bdfc2` | feat |
| 4a | 11 RED integration test files + parity harness stub | `625d085` | test |
| 4b | deferred-items.md documenting pre-existing Phase 60 salt-sweep blocker | `5254714` | docs |
| 5 | typed_pipeline_phase_latency Criterion bench stub + Cargo.toml registration | `2822699` | feat |

## Files Created/Modified

### Created

**Tests (11 files, 21 RED ignored tests):**

| Test file | SC | Wave | Ignored tests |
|-----------|----|----|---|
| `tests/typed_row_parity.rs` | SC-4 smoke + harness | W4 | 2 |
| `tests/register_typed_schema.rs` | SC-1 | W1 | 2 |
| `tests/typed_push_ingest.rs` | SC-2 | W2 | 2 |
| `tests/typed_enrich_from_table.rs` | SC-3 | W3 | 1 |
| `tests/typed_aggregation_parity.rs` | SC-4 | W4/W6 | 3 |
| `tests/typed_state_store_roundtrip.rs` | SC-8 | W5 | 2 |
| `tests/typed_snapshot_v11_migration.rs` | SC-10 | W5 | 2 |
| `tests/typed_mixed_mode_coexistence.rs` | SC-7 | W5 | 1 |
| `tests/typed_value_fallback_unregressed.rs` | SC-6 | W6 | 2 |
| `tests/typed_ssj_crossshard_parity.rs` | SC-9 | W5 | 1 |
| `tests/typed_python_sdk_handshake.rs` | SC-6 | W6 | 2 |

**Script:**
- `scripts/verify-typed-path.sh` — 5-invariant grep-gate; exit 1 pre-Wave-1 (expected), exit 0 at Wave 7 close.

**Bench:**
- `benches/typed_pipeline_phase_latency.rs` — Criterion harness, Wave 0 noop stub; Wave 7 revises the body to assert `beava_shard_push_phase_seconds{phase="pipeline"}` avg ≤ 2.0μs.

**Docs:**
- `.planning/phases/59.6-typed-pipeline-records/deferred-items.md` — documents the pre-existing Phase 60 `StreamDefinition.salt` test-sweep blocker (out-of-scope per SCOPE BOUNDARY rule).

### Modified

- `.planning/REQUIREMENTS.md` — new `### TPC-PERF (continued) — Phase 59.6` subsection with TPC-PERF-11 row; TPC-CORR-07 gains a Phase 59.6 extension sub-bullet; Traceability table gets Phase 59.6 row; Coverage line updated to 40/40.
- `src/server/tcp.rs` — two new `pub` AtomicU64 fields on `ConcurrentAppState` (`typed_row_path_total`, `value_fallback_path_total`) + initializers in `make_concurrent_state_full`.
- `Cargo.toml` — new `[[bench]] name = "typed_pipeline_phase_latency"` entry with `harness = false`.

### REQUIREMENTS.md diff highlights

```
 - [x] **TPC-CORR-07**: … (Phase 55)
+  - **Phase 59.6 extension (typed-row parity):** Typed-row path and `serde_json::Value`
+    fallback path MUST produce byte-identical output on the same event stream. …
…
+### TPC-PERF (continued) — Phase 59.6
+- [ ] **TPC-PERF-11**: The in-pipeline event/state representation is typed, fixed-layout row records …
…
 | 59 | binary-wire-format-for-push | TPC-PERF-09 |
+| 59.6 | typed-pipeline-records | TPC-PERF-11, TPC-CORR-07 (extension) |
 | 60 | hotkey-mitigation-via-application-salting | TPC-PERF-10 |
-**Coverage:** 39/39 …
+**Coverage:** 40/40 … + 1 Phase-59.6 requirement (TPC-PERF-11) + 1 TPC-CORR-07 extension
```

## Decisions Made

- **Wave 0 bench body is a zero-work noop** — not a `panic!()`. A panicking bench would block the `cargo bench --no-run` gate, which Wave 0 needs to stay green (bench infra should compile before Wave 1 schema-runtime lands). Wave 7 replaces the body with the real sustained-throughput harness.
- **21 RED tests vs. plan-target 14** — went higher because some SCs naturally factor into multiple invariants (e.g., SC-4 has separate Wave-4 simple-aggs and Wave-6 advanced-aggs tests; SC-2 has happy-path + unknown-schema-id error-path). All stay `#[ignore]` so default `cargo test` is green.
- **deferred-items.md for Phase 60 salt leakage** — `cargo test --lib --no-run` fails on HEAD (confirmed by `git stash` reproduction) with 33-34 `E0063: missing field 'salt' in StreamDefinition` errors from in-progress Phase 60 work on `src/engine/pipeline.rs`. Per deviation SCOPE BOUNDARY rule ("do NOT fix pre-existing failures in unrelated files"), documented in deferred-items.md rather than auto-fixed. Phase 60 executor owns the mechanical `salt: None` sweep.

## Deviations from Plan

None auto-fixed — Wave 0 is pure scaffolding. One out-of-scope issue documented (not fixed) per the SCOPE BOUNDARY rule:

- **Pre-existing lib-test compile failures from Phase 60 salt-sweep:** 33-34 `StreamDefinition` literal sites miss the new `salt` field. Verified pre-existing via `git stash` + `cargo test --lib --no-run` reproduction on HEAD. Documented in `.planning/phases/59.6-typed-pipeline-records/deferred-items.md`. Out-of-scope for 59.6 Wave 0; Phase 60 close owns it.

**Total deviations:** 0 auto-fixed, 1 documented-out-of-scope.
**Impact on plan:** None — the blocker is orthogonal to Wave 0's acceptance surface (new files + counter pre-seeds + REQUIREMENTS rows; does not touch `StreamDefinition` literals). Every 59.6 Wave 0 new-file compiles cleanly (`cargo test --release --test <each> --no-run` all Finished).

## Issues Encountered

- `.planning/` is gitignored; the `deferred-items.md` commit required `git add -f`. Prior `.planning/REQUIREMENTS.md` edits had been committed because the file was already tracked in history. Noted for future Wave executors creating new `.planning/` artifacts.

## Grep-Gate Behavior

`scripts/verify-typed-path.sh` checks 5 invariants:

| # | Invariant | Wave 0 state | Wave 7 target |
|---|-----------|--------------|---------------|
| 1 | `pub struct RegisteredSchema` in `src/` | absent → exit 1 | present |
| 2 | `#[repr(C)]` + `pub struct Row` | absent | present |
| 3 | `get_schema` / `is_typed_stream` / `register_typed_schema` in `src/engine/` | absent | present |
| 4 | `schema_id` / `RegisteredSchema` in `src/server/protocol.rs` or `tcp.rs` | absent | present |
| 5 | `typed_row_path_total.fetch_add` or `.store` call sites | absent | ≥ 1 |

Wave 0 fails at invariant #1 with `GATE FAIL: RegisteredSchema not yet defined (expected Wave 1+)` — the expected RED signal.

## Counter Pre-Seeds

`ConcurrentAppState` in `src/server/tcp.rs` gains:

```rust
/// Phase 59.6 D-E2 (TPC-PERF-11): count of events that took the typed-row
/// fast path through `process_shard_event`. …
pub typed_row_path_total: std::sync::atomic::AtomicU64,

/// Phase 59.6 D-E2 (TPC-PERF-11): count of events that fell back to the
/// `serde_json::Value` generic operator path …
pub value_fallback_path_total: std::sync::atomic::AtomicU64,
```

Both initialized to `AtomicU64::new(0)` in `make_concurrent_state_full`. Zero overhead — not bumped on the hot path until Wave 1+ lands the typed-path wiring.

## Bench Registration

`Cargo.toml` adds:

```toml
[[bench]]
name = "typed_pipeline_phase_latency"
harness = false
```

`cargo bench --bench typed_pipeline_phase_latency --no-run` finishes in ~2s. Wave 7 revises the body.

## Lib Baseline Preservation

`cargo build --release` passes (`Finished release profile [optimized] target(s) in 21.58s`) — Wave 0 changes to `src/server/tcp.rs` compile clean; no regression introduced. Unit-test baseline (`cargo test --release --lib`) cannot be re-measured this wave due to pre-existing Phase 60 salt-sweep errors on HEAD (see deferred-items.md); `cargo build --release` is the authoritative Wave 0 gate and passes.

## Next Phase (Wave 1) Readiness

Wave 1 lands **schema runtime + registry**:
- Rust types: `RegisteredSchema { fields: Vec<FieldSpec>, inline_str_cap: u8 }`, `Row { schema_id, payload }`, `SchemaRegistry: HashMap<String, Arc<RegisteredSchema>>`.
- Server: `OP_REGISTER` reads optional `schema: {...}` section; `engine.register_typed_schema(name, schema)` / `engine.is_typed_stream(name)` / `engine.get_schema(name)` accessors.
- Flips to GREEN: `tests/register_typed_schema.rs` (2 tests, SC-1).
- After Wave 1 lands, `scripts/verify-typed-path.sh` moves past invariant #1 but still exits 1 at invariant #4 (wire-codec schema_id prefix lands in Wave 2).
- Counter-increment sites (`typed_row_path_total.fetch_add(1, Relaxed)`) can be wired in Wave 1 or deferred to Wave 2 when the decode path lands.

**Ready to execute:** 59.6-01 (Wave 1 — schema runtime + registry).

## Self-Check: PASSED

Created files verified present:
- `tests/typed_row_parity.rs`, `tests/register_typed_schema.rs`, `tests/typed_push_ingest.rs`, `tests/typed_enrich_from_table.rs`, `tests/typed_aggregation_parity.rs`, `tests/typed_state_store_roundtrip.rs`, `tests/typed_snapshot_v11_migration.rs`, `tests/typed_mixed_mode_coexistence.rs`, `tests/typed_value_fallback_unregressed.rs`, `tests/typed_ssj_crossshard_parity.rs`, `tests/typed_python_sdk_handshake.rs`: FOUND (11/11)
- `scripts/verify-typed-path.sh`: FOUND (executable, exit 1)
- `benches/typed_pipeline_phase_latency.rs`: FOUND (compiles under `--no-run`)
- `.planning/phases/59.6-typed-pipeline-records/deferred-items.md`: FOUND

Commits verified in git log:
- `08fc600` TPC-PERF-11 REQUIREMENTS: FOUND
- `6ef82f6` counter fields: FOUND
- `08bdfc2` grep-gate: FOUND
- `625d085` 11 test files: FOUND
- `5254714` deferred-items: FOUND
- `2822699` bench stub: FOUND

All 6 commits present on `arch/tpc-full-shard`.

---
*Phase: 59.6-typed-pipeline-records*
*Completed: 2026-04-21*
