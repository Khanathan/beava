---
phase: 52-event-log-recovery-ship-gate
plan: 10
type: execute
wave: 4
depends_on:
  - 52-09-PLAN.md
files_modified:
  - src/state/store.rs
  - Cargo.toml
  - tests/ship_gate.rs
autonomous: true
requirements:
  - TPC-CORR-02
  - TPC-CORR-05
  - TPC-CORR-06
  - TPC-PERF-07
  - TPC-DX-03
  - TPC-DX-04
  - TPC-INFRA-06

must_haves:
  truths:
    - "dashmap and arc-swap crates are removed from Cargo.toml dependencies and do not appear in Cargo.lock"
    - "All state operations go through the Shard-backed path — no compat shim code remains"
    - "BEAVA_ENTITIES_SHARDS env handler is removed (deprecated in Phase 50 D-13)"
    - "Full cargo test -p beava passes after deletion"
    - "N=1 vs N=8 parity proptest (PROPTEST_CASES=10000) passes post-deletion"
    - "All 3 ship-gate criteria hold in the final 9-cell + pareto benchmark run"
    - "tests/ship_gate.rs asserts SNAPSHOT_FORMAT_VERSION==8 and dashmap/arc-swap absent from Cargo.lock"
  artifacts:
    - path: "src/state/store.rs"
      provides: "StateStore with no DashMap/ArcSwap imports or compat shim code"
    - path: "Cargo.toml"
      provides: "dashmap and arc-swap removed from [dependencies]"
    - path: "tests/ship_gate.rs"
      provides: "Final assertions: snapshot version, crate absence, cargo audit clean"
  key_links:
    - from: "tests/ship_gate.rs"
      to: "Cargo.lock"
      via: "grep Cargo.lock for dashmap/arc-swap absence"
      pattern: "dashmap|arc-swap"
    - from: "src/state/store.rs"
      to: "src/state/shard.rs"
      via: "all state ops routed through Shard exclusively after shim deletion"
      pattern: "Shard|shard_router"
---

<objective>
Final cleanup: delete DashMap and ArcSwap compat shims, remove deprecated
`BEAVA_ENTITIES_SHARDS` env handler, and run the complete final verification (9-cell +
pareto benchmark, full test suite, N=1↔N=8 parity proptest). Wire a persistent ship-gate
test file `tests/ship_gate.rs` that future CI runs can use to confirm v1.2 invariants
hold. This is the last task of Phase 52 — after this, the branch is ready to merge to main.

Purpose: DashMap and ArcSwap were retained as compat shims throughout Phases 49–51 (per
TPC-PERF-01) to allow incremental migration. All state now flows through the Shard-backed
path. Deletion removes dead code, eliminates the compat layer, and proves that all prior
wave work (52-01 through 52-09) is correct and complete. CONTEXT D-18: run AFTER all tests
green AND ship-gate benchmarks green (enforced by wave-4 depends_on chain).

Output: Clean `src/state/store.rs`, trimmed `Cargo.toml`, committed `tests/ship_gate.rs`
with invariant assertions. v1.2 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-event-log-recovery-ship-gate/52-CONTEXT.md
@src/state/store.rs
@Cargo.toml
@Cargo.lock

<interfaces>
<!-- DashMap/ArcSwap import patterns to remove -->
```rust
// DELETE these from src/state/store.rs:
use dashmap::DashMap;
use arc_swap::ArcSwap;
// Also delete any compat shim type aliases like:
// type EntityStore = DashMap<...>;
// type SharedState = ArcSwap<...>;
```

<!-- BEAVA_ENTITIES_SHARDS handler to remove -->
```rust
// DELETE from env parsing / main.rs / config:
"BEAVA_ENTITIES_SHARDS" => { /* deprecated DashMap tuning knob */ }
```

<!-- ship_gate.rs assertions -->
```rust
#[test]
fn snapshot_format_version_is_v8() {
    assert_eq!(beava::state::store::SNAPSHOT_FORMAT_VERSION, 8);
}

#[test]
fn dashmap_not_in_cargo_lock() {
    let lock = std::fs::read_to_string("Cargo.lock").unwrap();
    assert!(!lock.contains("name = \"dashmap\""),
        "dashmap must not be in Cargo.lock post-v1.2 cleanup");
    assert!(!lock.contains("name = \"arc-swap\""),
        "arc-swap must not be in Cargo.lock post-v1.2 cleanup");
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete DashMap + ArcSwap shims + BEAVA_ENTITIES_SHARDS handler</name>
  <files>
    src/state/store.rs
    Cargo.toml
    tests/ship_gate.rs
  </files>
  <behavior>
    - Test 1 (RED): `cargo build -p beava` succeeds with zero references to `dashmap` or
      `arc_swap` in `src/` (grep test: `grep -r "dashmap\|arc_swap\|ArcSwap\|DashMap" src/`
      returns empty).
    - Test 2 (RED): All entity state operations (get, set, iterate) succeed via the Shard-
      backed path. Verify by running existing state unit tests: `cargo test -p beava state_`.
    - Test 3 (RED): Full test suite green: `cargo test -p beava` passes with no failures.
  </behavior>
  <action>
    1. In `src/state/store.rs`:
       a. Remove `use dashmap::DashMap;` and `use arc_swap::ArcSwap;` imports.
       b. Remove all compat shim type aliases (`EntityStore = DashMap<...>`,
          `SharedState = ArcSwap<...>`, etc.).
       c. Remove any `StateStore` fields typed as `DashMap<...>` or `ArcSwap<...>`.
       d. Remove compat shim method impls that delegated to DashMap/ArcSwap. Replace any
          call sites with direct `shard_router.get_shard(key).state_op(...)` calls.
       e. Fix all resulting compile errors: trace each `DashMap` / `ArcSwap` reference,
          replace with the Shard-backed equivalent established in Phases 49–51.
    2. Remove `BEAVA_ENTITIES_SHARDS` env handler from wherever it is parsed
       (likely `src/main.rs` or a config parsing module). The knob is deprecated (Phase 50
       D-13) and is now meaningless — removing it avoids operator confusion.
    3. In `Cargo.toml` `[dependencies]`: remove `dashmap` and `arc-swap` lines entirely.
    4. Run `cargo update` to remove them from `Cargo.lock` as well.
    5. Create `tests/ship_gate.rs` with the two assertions from the interfaces block:
       - `snapshot_format_version_is_v8`: asserts `SNAPSHOT_FORMAT_VERSION == 8`.
       - `dashmap_not_in_cargo_lock`: reads Cargo.lock and asserts neither `dashmap` nor
         `arc-swap` appears as a `name =` entry.
    6. Expose `pub const SNAPSHOT_FORMAT_VERSION: u8 = 8;` from `src/state/store.rs` if
       not already public.
  </action>
  <verify>
    <automated>cargo build -p beava 2>&1 | grep -E "error|warning.*unused" ; cargo test -p beava ship_gate -- --nocapture 2>&1 | grep -E "ok|FAILED"</automated>
  </verify>
  <done>
    `cargo build -p beava` succeeds with no DashMap/ArcSwap references. `tests/ship_gate.rs`
    both assertions pass. No `dashmap` or `arc-swap` in `Cargo.lock`. BEAVA_ENTITIES_SHARDS
    handler removed. All state ops work through Shard-backed path.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final full verification — test suite + proptest + 9-cell + cargo audit</name>
  <files>
    tests/ship_gate.rs
  </files>
  <action>
    Run the full v1.2 verification sequence in order:

    1. Full test suite:
       ```
       cargo test -p beava 2>&1 | tail -20
       ```
       Must be all-green. Any failure is a blocker.

    2. N=1↔N=8 parity proptest (full nightly run):
       ```
       PROPTEST_CASES=10000 cargo test -p beava --test sharding_parity -- --nocapture 2>&1 | tail -10
       ```
       Must pass. This is the hard pre-merge gate from 52-07.

    3. Final 9-cell + pareto benchmark at N=CPU_COUNT:
       ```
       cargo bench -p beava -- --nocapture 2>&1 | grep -E "cross_shard|EPS|baseline"
       cargo bench -p beava --bench pareto_workload -- --nocapture 2>&1 | grep -E "cross_shard|PASS|FAIL"
       ```
       Confirm all 3 ship-gate criteria hold post-deletion.

    4. Security audit:
       ```
       cargo audit 2>&1 | tail -5
       ```
       No critical advisories. (Informational or low-severity advisories acceptable with
       comment in SUMMARY.md.)

    5. Confirm ship_gate.rs tests pass in isolation:
       ```
       cargo test -p beava --test ship_gate -- --nocapture
       ```

    If all 5 steps are green: v1.2 is complete and ready to merge to main. Document
    results in the SUMMARY.md output section.
  </action>
  <verify>
    <automated>cargo test -p beava 2>&1 | grep -E "^test result" ; PROPTEST_CASES=100 cargo test -p beava --test sharding_parity -- --nocapture 2>&1 | grep -E "ok|FAILED" ; cargo audit 2>&1 | grep -E "error|0 vulnerabilities"</automated>
  </verify>
  <done>
    `cargo test -p beava` all-green. Parity proptest (PROPTEST_CASES=10000 in CI, 100 in
    local smoke) passes. All 3 ship-gate benchmark criteria hold post-deletion. `cargo audit`
    clean (no critical advisories). `tests/ship_gate.rs` both assertions pass.
    v1.2 is ready to merge to main.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo.lock → ship_gate assertion | Cargo.lock is a committed file; assertion reads it at test time — no runtime dep |
| compat shim deletion → callers | Removing shim must not leave callers with undetected silent fallbacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-10-01 | Denial | compat shim deletion breaks callers | mitigate | Compile errors are the safety net — cargo build must succeed before any test runs; all broken call sites fixed in Task 1 |
| T-52-10-02 | Repudiation | dashmap re-introduced by transitive dep | mitigate | ship_gate.rs Cargo.lock assertion detects re-introduction in any future CI run |
| T-52-10-03 | Denial | cargo audit critical advisory | mitigate | Run audit as final step; critical advisories are blockers for merge to main |
</threat_model>

<verification>
- `grep -r "dashmap\|arc_swap\|ArcSwap\|DashMap" src/` returns empty
- `grep "name = \"dashmap\"" Cargo.lock` returns empty
- `grep "name = \"arc-swap\"" Cargo.lock` returns empty
- `cargo test -p beava` all-green
- `PROPTEST_CASES=10000 cargo test -p beava --test sharding_parity` passes
- `cargo bench -p beava --bench pareto_workload` cross_shard_fraction < 0.40 (no panic)
- `cargo audit` no critical advisories
- `cargo test -p beava --test ship_gate` both assertions pass
</verification>

<success_criteria>
DashMap and ArcSwap are fully removed from the codebase and Cargo.lock. All v1.2 invariants
are asserted in `tests/ship_gate.rs` for ongoing CI protection. Full test suite, parity
harness, and benchmark suite are green post-deletion. v1.2 complete. All 7 Phase 52
requirements (TPC-INFRA-06, TPC-CORR-02, TPC-CORR-05, TPC-CORR-06, TPC-PERF-07,
TPC-DX-03, TPC-DX-04) are fully satisfied across plans 52-01 through 52-10.
</success_criteria>

<output>
After completion, create `.planning/phases/52-event-log-recovery-ship-gate/52-10-SUMMARY.md`

This is the final plan of Phase 52. After SUMMARY is written, Phase 52 is complete.
Update `.planning/STATE.md` to reflect Phase 52 done and v1.2 milestone complete.
</output>
