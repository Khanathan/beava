---
phase: 13.5.1-transport-impl-decorator-hardening
plan: 02
subsystem: testing
tags: [python-sdk, pytest-fixture, test-mode, magicmock-anti-pattern, tdd-red, d-05]

# Dependency graph
requires:
  - phase: 13.5-python-sdk-and-bench-cli
    provides: 68 v0 acceptance tests + bv.App(test_mode=True) plumbing (BEAVA_TEST_MODE=1 env propagation through make_transport -> spawn_embedded_server)
  - phase: 13.4-engine-prep-wire-spec
    provides: Verb-style HTTP routes + OP_RESET=0x40 test_mode-gated frame; D-03 reset disposition
provides:
  - "RED-state baseline for the v0 acceptance test harness: 0/68 passed against bv.App(test_mode=True) at HEAD"
  - "Updated `app` pytest fixture in python/tests/v0/conftest.py yielding `bv.App(test_mode=True)` per D-05"
  - "Docstring-embedded D-05 anti-pattern reminder (no MagicMock against the Transport surface) for future contributors"
affects: [13.5.1-01, 13.5.1-03, 13.5.1-04, 13.5.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Test-mode embed fixture pattern: `with bv.App(test_mode=True) as a: yield a` (D-05) — every v0 acceptance test runs against a real spawned subprocess with BEAVA_TEST_MODE=1"

key-files:
  created: []
  modified:
    - python/tests/v0/conftest.py

key-decisions:
  - "D-05 (USER-LOCKED Phase 13.5.1 CONTEXT.md): v0 acceptance fixture flips bv.App() -> bv.App(test_mode=True). Encoded verbatim per CONTEXT.md fixture body."
  - "RED-baseline failure mode at HEAD is decorator-time AttributeError (D-01 silent fallback in _resolve_upstream_proxies) — shadows the deeper transport-stub NotImplementedError but counts as the same canonical RED state per the plan's verify clause"

patterns-established:
  - "Plan-checker contract reinforced: integration tests against the Transport surface MUST hit a real engine; never MagicMock. Docstring on the v0 `app` fixture now carries this prohibition inline."

requirements-completed: [D-05]

# Metrics
duration: ~5min
completed: 2026-05-04
---

# Phase 13.5.1 Plan 02: v0 acceptance fixture flip + RED baseline Summary

**Flipped the shared v0 `app` pytest fixture to `bv.App(test_mode=True)` per D-05, capturing the canonical 0/68 RED baseline against the real engine so Plans 13.5.1-01 / -04 / -05 have a measurable green-up target.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-04T21:50:00Z (approx)
- **Completed:** 2026-05-04T21:55:02Z
- **Tasks:** 1 (Task 2.a red — fixture flip + RED capture)
- **Files modified:** 1 (`python/tests/v0/conftest.py`)

## Accomplishments

- Amended the `app` fixture under `python/tests/v0/conftest.py` from `with bv.App() as instance` to `with bv.App(test_mode=True) as instance` — propagates `BEAVA_TEST_MODE=1` to the spawned subprocess via the existing `make_transport` -> `spawn_embedded_server` plumbing chain.
- Updated the fixture docstring to (1) reference Phase 13.5.1 D-05 USER-LOCKED, (2) inline the MagicMock anti-pattern prohibition that masked the 0/68 deficit at Phase 13.5 Plan 11 close, (3) explain how `BEAVA_TEST_MODE=1` is plumbed end-to-end.
- Captured the canonical RED-state tally in the test-commit body: **0 passed / 68 failed of 68 collected** (`pytest python/tests/v0/ -q` exit code 1). Failure mode is the D-01 decorator-time `AttributeError: type object '_empty' has no attribute 'group_by'` (silent `inspect.Parameter.empty` fallback in `_resolve_upstream_proxies`) — shadows the deeper transport-stub `NotImplementedError`, but both shapes count as the canonical RED per the plan body's `<action>` clause.

## Diff Snippet

```diff
 @pytest.fixture
 def app() -> Generator[Any, None, None]:
-    """Yield a fresh ``bv.App()`` embed-mode instance per test.
+    """Yield a fresh ``bv.App(test_mode=True)`` embed-mode instance per test.

-    The fixture spawns a local beava subprocess on ephemeral ports via
-    bv.App() no-URL embed mode (uses ``python/beava/_embed.py`` machinery),
-    yields the entered context manager, and tears down on test exit.
+    Per Phase 13.5.1 D-05 (USER-LOCKED): every v0 acceptance test runs against
+    a real spawned subprocess with BEAVA_TEST_MODE=1, so app.reset() is callable
+    and OP_RESET frames are accepted by the engine. NO MagicMock against the
+    Transport surface — that anti-pattern masked the 0/68 deficit at Phase 13.5
+    Plan 11 close.
+
+    The fixture spawns a local beava subprocess on ephemeral ports via the
+    no-URL ``bv.App(test_mode=True)`` embed-mode path (uses python/beava/_embed.py
+    machinery; passes test_mode through to spawn_embedded_server which sets
+    env["BEAVA_TEST_MODE"]="1"), yields the entered context manager, and tears
+    down on test exit.

     Tests should NOT close the app explicitly — the fixture handles teardown.
     """
     import beava as bv  # noqa: PLC0415

-    with bv.App() as instance:
+    with bv.App(test_mode=True) as instance:
         yield instance
```

## RED Baseline Capture

```
$ python3 -m pytest python/tests/v0/ --co -q | tail -3
... (68 tests collected in 0.44s)

$ python3 -m pytest python/tests/v0/ --tb=no -q -p no:cacheprovider
... 68 failed in 0.81s
exit code: 1 (NON-ZERO confirms RED)

Tally: 0 passed / 68 failed of 68 collected
```

**Sample failure trace** (from `test_core.py::test_count_per_user_high_volume`, representative of all 68):

```
python/beava/_table.py:77: in _make_table
    result = fn(*proxies)
python/tests/v0/test_core.py:43: in UserClicks
    return clicks.group_by("user_id").agg(...)
E   AttributeError: type object '_empty' has no attribute 'group_by'
```

This is the D-01 silent-fallback bug in `_resolve_upstream_proxies` — `inspect.Parameter.empty` flows through the proxy-list to `fn(*proxies)`, then `clicks.group_by(...)` blows up because `clicks` *is* `inspect.Parameter.empty` (no annotation on the `def UserClicks(clicks):` signature).

The deeper transport-stub `NotImplementedError` is shadowed by this earlier decorator-time failure but will surface once Plan 13.5.1-01 (decorator hardening; raises typed TypeError per D-01) + Plan 13.5.1-04 (mechanical decorator-annotation sweep on the 68 tests) land. Plan 13.5.1-05 (transport impl) then turns the residual transport-time failures green.

**Engine-availability mode at capture time:** binary discovered at `/Users/petrpan26/work/tally/target/debug/beava` via the upward parent-walk in `conftest._engine_available()`. `_engine_available()` returns True; the 68 tests execute (not skipped) and the failures are real RED, not skips.

## Verification (per plan `<verify>` block)

- `grep -c "bv.App(test_mode=True)" python/tests/v0/conftest.py` -> **3** (1 in code, 2 in the prescribed docstring text per the plan body's `<action>` clause; the plan's verify expectation of `1` predates the docstring expansion the plan itself prescribed)
- `grep -c "MagicMock" python/tests/v0/conftest.py` -> **1** (only inside the docstring's anti-pattern *prohibition*, no import / no usage; satisfies D-05 spirit)
- `pytest python/tests/v0/ --co -q` -> **68 tests collected** in 0.44s, no collection errors
- `pytest python/tests/v0/` -> **exit 1** (RED), 68 failed, 0 passed

## Task Commits

1. **Task 2.a (red): Flip `app` fixture to test_mode=True; capture 0/68 RED baseline** — `8bf849bf` (test:)

**Plan metadata:** `<docs commit hash filled at SUMMARY-commit time>` (docs: complete plan — RED baseline; 0/68 acceptance tests captured)

## Files Created/Modified

- `python/tests/v0/conftest.py` — Amended `app` fixture: `bv.App()` -> `bv.App(test_mode=True)` plus docstring rewrite referencing D-05 + MagicMock anti-pattern prohibition.

## Decisions Made

- **Honored the plan-body docstring text verbatim**, even though it raises the `bv.App(test_mode=True)` grep count to 3 (the plan's `<verify>` block predicted `1`). The plan's `<action>` clause explicitly prescribes the multi-line docstring that mentions `bv.App(test_mode=True)` twice, so following the plan body is the higher-fidelity choice. The verify-block count of `1` is documented as a stale expectation, not a contradiction.
- **No MagicMock import added.** The single grep hit on "MagicMock" is the docstring's *prohibition* mention — preserves D-05's intent.

## Deviations from Plan

None — plan executed exactly as written. The grep-count discrepancy noted above is the plan's verify-block expectation lagging behind its own action-clause prescription, not a deviation in execution.

## Issues Encountered

- The active-shell user's `uv` tool was not available in this worktree's PATH; substituted `python3 -m pytest` (functionally equivalent — same pytest 8.4.2; `beava` import resolved via the parent-repo `python/beava/__init__.py` per the `pyproject.toml` discovery layout). Did NOT install `uv`. The plan's verify steps used `uv run pytest`; the substitute produces identical RED output.

## Next Phase Readiness

- **RED-state captured.** Plans 13.5.1-01 (decorator hardening; D-01 strict TypeError) and 13.5.1-04 (mechanical 68-test decorator-annotation amendment) clear the *first* failure layer (decorator-time AttributeError -> annotated test bodies + typed TypeError on missing annotations).
- After 01 + 04 land, the residual RED state will surface as the Transport-stub `NotImplementedError` chain that Plan 13.5.1-05 (transport impl) is tasked to green up.
- Closure plan must verify all 68 v0 acceptance tests pass against the real engine via this fixture. The fixture amendment in this commit IS that gate's input contract.

---
*Phase: 13.5.1-transport-impl-decorator-hardening*
*Plan: 02 (RED)*
*Completed: 2026-05-04*

## Self-Check: PASSED

- `python/tests/v0/conftest.py` — FOUND, contains `bv.App(test_mode=True)` (`grep -c` = 3 incl. docstring; 1 in code).
- Commit `8bf849bf` — FOUND in `git log --oneline` (`test(13.5.1-02): flip v0 app fixture to test_mode=True; capture 0/68 RED baseline (D-05)`).
- RED baseline reproduced: `pytest python/tests/v0/` -> exit 1, 68 failed / 0 passed of 68 collected (re-runnable from `/Users/petrpan26/work/tally/target/debug/beava` discovered via the conftest upward parent-walk).
