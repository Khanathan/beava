---
phase: 13.5.1
plan: 06
type: execute
wave: 4
depends_on: [13.5.1-04, 13.5.1-05]
files_modified:
  - .planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-SUMMARY.md
  - .planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-VERIFICATION.md
  - .planning/throughput-baselines.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
tdd: false
tags:
  - closure
  - documentation
  - throughput-baseline

must_haves:
  truths:
    - "Phase 13.5.1 SUMMARY.md captures the deficit-close narrative + commit list + final test tallies"
    - "Phase 13.5.1 VERIFICATION.md records every grep / pytest / mypy / cargo / ruff gate result with PASS/FAIL"
    - "Throughput-baselines.md has a Phase 13.5.1 row for the small/tcp regression-gate cell using the same hw-class as Phase 13.5"
    - "STATE.md last_updated bumped; status string updated to reflect 13.5.1 close + 13.7.5/13.7.6/13.8 remaining"
    - "ROADMAP.md Phase 13.5.1 entry shows verdict + close date"
    - "Throughput-bench command sourced from .planning/throughput-baselines.md:1990 (Phase 13.5 small/tcp gate-cell harness invocation; combined with --pipeline / --transport prefix as documented in lines 800, 831, 875, 915 from Phase 19.x)"
    - "Doc-only commit shape (single docs:) is permitted per CLAUDE.md §Conventions → TDD Discipline item #4 — Plans 01–05 already shipped per-task red→green pairs; Plan 06 captures phase-level acceptance verification, not new code"
  artifacts:
    - path: ".planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-SUMMARY.md"
      provides: "Phase narrative — deficit closed, 68/68 + 18/18 green, transport surface shipped"
      contains: "Phase 13.5.1"
      min_lines: 80
    - path: ".planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-VERIFICATION.md"
      provides: "Mechanical PASS/FAIL grid for every gate referenced by ROADMAP success criteria + CONTEXT decisions"
      contains: "PASS\\|FAIL"
      min_lines: 60
    - path: ".planning/throughput-baselines.md"
      provides: "Phase 13.5.1 row in the small/tcp regression-gate table (same hw-class as Phase 13.5)"
      contains: "Phase 13.5.1"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-SUMMARY.md"
      via: "phase-close commentary references the summary"
      pattern: "13.5.1-SUMMARY"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-SUMMARY.md"
      via: "Phase entry status flips to CLOSED with link"
      pattern: "Phase 13.5.1"
---

<objective>
Closure plan — single doc-only commit per CLAUDE.md §Conventions → TDD Discipline item #4 (the actual numbered clause, verbatim):

> "Smoke / acceptance tests can be written after all sub-features land, but each sub-feature must have its own unit-test red-green inside the plan. The phase-level smoke test is additive assurance, not a replacement for per-task tests."

Plan 06 modifies docs/state files only (SUMMARY.md, VERIFICATION.md, throughput-baselines.md, STATE.md, ROADMAP.md). Plans 01–05 already shipped per-task red→green pairs (each sub-feature has its own unit-test red-green inside the plan); Plan 06 captures the phase-level acceptance verification (smoke test as additive assurance), not new code. The `docs:` commit is therefore permitted without a paired `test:` because the acceptance commands run during this plan exercise tests that already exist and are already green from prior plans — Plan 06 is recording results, not introducing new code or new contracts.

Five deliverables:
1. **SUMMARY.md** — phase narrative: what shipped, deficit closed, commit list, test tallies, hw-class throughput row, follow-ups (none expected; v0.0.x deferrals are already captured by Plan 13.5.1-05c).
2. **VERIFICATION.md** — mechanical PASS/FAIL grid: every grep / pytest / mypy / cargo / ruff gate from CONTEXT decisions D-01..D-05 + ROADMAP success criteria 1-5 explicitly listed with the command, expected result, and observed result.
3. **Throughput rebaseline** — a Phase 13.5.1 row in `.planning/throughput-baselines.md` covering the small/tcp regression-gate cell. Per CLAUDE.md §Conventions → Performance Discipline: this phase ships zero Rust hot-path changes (Python SDK + decorator only), so a microbench is exempt. The throughput-run contract is unconditional from Phase 8 onward, so we capture one cell against the small/tcp gate.
4. **STATE.md** — bump `last_updated` to today; update `status` string to mark 13.5.1 closed and surface remaining v0 critical path (13.7.5 → 13.7.6 → 13.8). Add a Phase 13.5.1 close comment block following the Phase 13.5 / 13.6 / 13.7 comment-block format.
5. **ROADMAP.md** — flip the Phase 13.5.1 entry status from `📋 PLANNED` to `✅ CLOSED YYYY-MM-DD (PASS)` with verdict line.

Purpose: provide reviewer-friendly, machine-checkable closure that the orchestrator and any downstream-phase planner (13.7.5 / 13.7.6 / 13.8) can read to confirm the GA-blocker is gone.

Output: one `docs(13.5.1):` commit landing all 5 file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-CONTEXT.md
@.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-PATTERNS.md
@.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-01-SUMMARY.md
@.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-02-SUMMARY.md
@.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-03-SUMMARY.md
@.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-04-SUMMARY.md
@.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-05-SUMMARY.md
@.planning/phases/13.5-python-sdk-and-bench-cli/13.5-SUMMARY.md
@.planning/phases/13.5-python-sdk-and-bench-cli/13.5-VERIFICATION.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/throughput-baselines.md
@CLAUDE.md

<interfaces>
<!-- Templates and formats inherited from prior closure plans. -->

# .planning/STATE.md — front-matter `last_updated` field is bumped on every close
# Comment-block convention: <!-- Phase 13.5.1 OFFICIALLY CLOSED YYYY-MM-DD (PASS|PASS-WITH-WARN). ... -->

# .planning/ROADMAP.md Phase 13.5.1 entry — line 846 currently reads
# "### Phase 13.5.1: Transport impl + decorator hardening (Phase 13.5 fix-up) — 📋 PLANNED 2026-05-04"
# Flip to: "### Phase 13.5.1: Transport impl + decorator hardening (Phase 13.5 fix-up) — ✅ CLOSED YYYY-MM-DD (PASS)"

# .planning/throughput-baselines.md format (from Phase 13.5 row precedent):
# Phase 13.5 baseline for small/tcp: 674,108 EPS (Apple-M4 / Darwin-24.3.0 / 10 cores).
# Phase 13.5.1 expectation: within ±10% of Phase 13.5 (no Rust hot-path changes; Python SDK + decorator only).

# .planning/throughput-baselines.md line 1990 documents the EXACT Phase 13.5 small/tcp harness
# invocation (verbatim source for Plan 06 Step 3 below). Other phase rows on lines 800, 831,
# 875, 915 add the leading `--pipeline crates/beava-bench/configs/small.json --transport tcp`
# prefix that the line-1990 row leaves implicit; Plan 06 pins the fully-qualified form.

# CLAUDE.md §End-to-end throughput regression contract — every Phase 8+ MUST add a row.
# Doc-only-phase precedent (13.0 / 13.6 / 13.7) requires a single throughput-run cell against
# small/tcp, not the full 8-cell matrix. Phase 13.5.1 follows that precedent because it ships
# zero Rust hot-path changes.

# CLAUDE.md §Performance Discipline — microbench is for hot-path code only ("Do NOT bench:
# cold-path code, register compile, schema propagation at register-time, I/O paths that will
# change..."). This phase has zero Rust changes ⇒ exempt from microbench. Document the
# exemption explicitly in 13.5.1-VERIFICATION.md citing the verbatim "Do NOT bench: cold-path
# code" clause from CLAUDE.md §Performance Discipline.

# CLAUDE.md §TDD Discipline item #4 (verbatim, line 46): "Smoke / acceptance tests can be
# written after all sub-features land, but each sub-feature must have its own unit-test
# red-green inside the plan. The phase-level smoke test is additive assurance, not a
# replacement for per-task tests." Plan 06's docs-only commit is permitted under this clause.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 6.a (docs): Write SUMMARY + VERIFICATION + run small/tcp throughput cell + bump STATE/ROADMAP</name>
  <files>.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-SUMMARY.md, .planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-VERIFICATION.md, .planning/throughput-baselines.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/13.5-python-sdk-and-bench-cli/13.5-SUMMARY.md (analog format for SUMMARY.md narrative + structure)
    - .planning/phases/13.5-python-sdk-and-bench-cli/13.5-VERIFICATION.md (analog format for VERIFICATION.md PASS/FAIL grid)
    - .planning/throughput-baselines.md lines 1985–2030 (Phase 13.5 row format — bottom of file; Apple-M4 hw-class; canonical command on line 1990)
    - .planning/STATE.md (front-matter format + close comment-block convention)
    - .planning/ROADMAP.md lines 846-877 (Phase 13.5.1 entry to flip)
    - All five plan-summary files: 13.5.1-{01..05}-SUMMARY.md (commit SHAs + per-plan deliverables)
    - CLAUDE.md §Conventions → Performance Discipline + §End-to-end throughput regression contract + §TDD Discipline item #4
    - python/pyproject.toml (confirms Python lint = ruff per the [tool.ruff] section line 62)
  </read_first>
  <action>
    Step 1 — write `.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-SUMMARY.md`. Structure (mirror Phase 13.5's SUMMARY format):

    ```markdown
    # Phase 13.5.1 — Transport impl + decorator hardening (Phase 13.5 fix-up) — SUMMARY

    **Status:** ✅ CLOSED <YYYY-MM-DD> (PASS)
    **Plans:** 6 across 4 waves
    **Commits:** ~7-8 (1 per RED plan + 1 per GREEN plan + 4 inner commits in Plan 05 cluster + this closure commit)
    **Worktree HEAD:** <git rev-parse HEAD>

    ## What shipped

    The 0/68 v0-acceptance-test deficit from Phase 13.5 Plan 11 close is **resolved**.
    All four `Http/Tcp/EmbedTransport.send_*` methods are wired; `@bv.table` raises a strict
    `TypeError` on unannotated parameters; the legacy `*_get_single` helpers are private; and
    the v0 acceptance harness runs against a real `bv.App(test_mode=True)` engine with zero
    `MagicMock` against the Transport surface.

    ### Deliverables by decision

    - **D-01 (strict TypeError):** `_resolve_upstream_proxies` raises clean `TypeError` with
      a helpful message (commit <plan-04 SHA>); 70 v0 test decorators amended with annotations
      (commit <plan-05d SHA>).
    - **D-02 (TCP read-path JSON default):** `TcpTransport.send_get` / `send_batch_get` default
      to CT_JSON; `wire_format` kwarg path preserved on the legacy `_tcp_get_single` helper for
      callers that want msgpack (commit <plan-05b SHA>).
    - **D-03 (`features` filter):** Base-class `Transport.send_get` accepts
      `features: list[str] | None = None`; HTTP and TCP paths plumb it through; `App.get` exposes
      it on the public surface (commit <plan-05a/b SHAs>). `App.batch_get` keeps the
      tuple-of-2 shape for v0 (per-entry features deferred to v0.1+).
    - **D-04 (legacy helpers private):** `tcp_get_single` / `http_get_single` →
      `_tcp_get_single` / `_http_get_single`; all callers updated; v0.0.x removal scheduled in
      `.planning/ideas/v0.1-deferrals.md` (commit <plan-05c SHA>).
    - **D-05 (real-engine harness, no MagicMock):** v0 conftest `app` fixture flipped to
      `bv.App(test_mode=True)` (commit <plan-02 SHA>); MagicMock-using lifecycle tests
      rewritten off MagicMock against the Transport surface (commit <plan-05d SHA>); new
      `test_transport_equivalence.py` with 6-or-7 tests × 3 transports (commit <plan-03 +
      plan-05a/b SHAs>).

    ### Test tallies (final, from VERIFICATION.md)

    | Suite | Result |
    |---|---|
    | `pytest python/tests/v0/` | ≥ 68 passed (collected count >= 68) |
    | `pytest python/tests/v0/test_transport_equivalence.py` | ≥ 6 passed (Plan 03 may have finalized 7 incl. batch_get features test) |
    | `pytest python/tests/internal/test_app_lifecycle.py` | all passed |
    | `pytest python/tests/internal/test_table_decorator_typeerror.py` | all passed |
    | `mypy --strict python/beava` | Success |
    | `cargo test --workspace --features testing` | <count> passed |
    | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | clean |
    | `cargo fmt --all --check` | clean |
    | `ruff check python/` | clean |

    ### Throughput regression-gate cell (small/tcp)

    Captured against the same hw-class as Phase 13.5 (Apple-M4 / Darwin-24.3.0 / 10 cores).
    Phase 13.5.1 ships zero Rust hot-path changes ⇒ expected within ±10% of Phase 13.5
    baseline (674,108 EPS).

    Observed: <EPS> EPS. Delta vs Phase 13.5: <±X>%. Verdict: <PASS|WARN|FAIL>.

    ### Microbench exemption

    Per CLAUDE.md §Conventions → Performance Discipline ("Do NOT bench: cold-path code"), the
    Python SDK transport layer is cross-process / cold-path / SDK-only. Phase 13.5.1 inherits
    the same doc-only-phase precedent used by Phase 13.0 / 13.6 / 13.7. No microbench is added
    to `.planning/perf-baselines.md`; the throughput-baselines small/tcp cell remains the
    regression tripwire (per Phase 8+ throughput-run contract).

    ## Plan-by-plan commit list

    - Plan 01 (Wave 1, RED): test(13.5.1-01) <SHA>
    - Plan 02 (Wave 1, RED): test(13.5.1-02) <SHA>
    - Plan 03 (Wave 1, RED): test(13.5.1-03) <SHA>
    - Plan 04 (Wave 2, GREEN): fix(13.5.1-04) <SHA>
    - Plan 05 (Wave 3, GREEN cluster):
      - 5a feat(13.5.1-05a) <SHA> — HttpTransport impls
      - 5b feat(13.5.1-05b) <SHA> — TcpTransport impls + EmbedTransport delegates
      - 5c refactor(13.5.1-05c) <SHA> — D-04 rename + v0.0.x scheduling
      - 5d chore(13.5.1-05d) <SHA> — 70 decorator annotations + MagicMock replacements
    - Plan 06 (Wave 4, closure): docs(13.5.1) <SHA> ← this commit

    ## Follow-ups for v0.0.x

    All deferreds from CONTEXT.md `<deferred>` section already captured in
    `.planning/ideas/v0.1-deferrals.md`:
    - HTTP-transport throughput regression -24% to -32% (from Phase 13.4)
    - Conformance-harness payload bug (from Phase 13.6)
    - Cross-SDK per-op equivalence harness
    - `_tcp_get_single` / `_http_get_single` removal (added by Plan 05c)
    - Strict-mode push rejection
    - `OP_PUSH_SYNC=0x0011` (`acks=all`)

    ## Resume next

    `/gsd-execute-phase 13.7.5` (CONTEXT already locked 2026-05-04 alongside this phase).
    13.7.5 → 13.7.6 → 13.8 GA on the v0 critical path.
    ```

    Replace `<placeholders>` with real values when writing the file:
    - `<YYYY-MM-DD>` = today
    - `<git rev-parse HEAD>` = current commit
    - `<plan-NN SHA>` = `git log --oneline -1 <commit-grep-pattern>` for each plan's commit prefix
    - `<EPS>` / `<±X>%` / verdict = filled in after the throughput run (Step 3 below)

    Step 2 — write `.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-VERIFICATION.md`. Structure (mirror Phase 13.5's VERIFICATION format — explicit PASS/FAIL grid):

    ```markdown
    # Phase 13.5.1 — VERIFICATION

    **Verdict:** PASS
    **Date:** <YYYY-MM-DD>
    **HEAD:** <git rev-parse HEAD>

    ## ROADMAP success criteria (all 5 from .planning/ROADMAP.md lines 868-873)

    | # | Criterion | Command | Expected | Observed | Result |
    |---|---|---|---|---|---|
    | 1 | 68/68 v0 acceptance tests GREEN against `bv.App(test_mode=True)` | `cd python && pytest tests/v0/ -x` | exit 0; collected ≥ 68; ≥ 68 passed | <observed> | <PASS/FAIL> |
    | 2 | ≥ 6 cross-transport equivalence tests passing (Plan 03 may have finalized 7) | `cd python && pytest tests/v0/test_transport_equivalence.py -x` | exit 0; ≥ 6 passed | <observed> | <PASS/FAIL> |
    | 3a | NO MagicMock against Transport surface in `python/tests/v0/` | `grep -rn "MagicMock" python/tests/v0/ \| wc -l` | 0 | <observed> | <PASS/FAIL> |
    | 3b | NO MagicMock against Transport surface in lifecycle tests | `grep -rn 'MagicMock.*[Tt]ransport\|spec=.*Transport' python/tests/internal/test_app_lifecycle.py \| wc -l` | 0 | <observed> | <PASS/FAIL> |
    | 4a | cargo test workspace green | `cargo test --workspace --features testing` | exit 0 | <observed> | <PASS/FAIL> |
    | 4b | cargo clippy clean | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | exit 0 | <observed> | <PASS/FAIL> |
    | 4c | cargo fmt clean | `cargo fmt --all --check` | exit 0 | <observed> | <PASS/FAIL> |
    | 4d | mypy --strict green | `cd python && mypy --strict beava` | Success | <observed> | <PASS/FAIL> |
    | 4e | ruff check clean (Python lint per python/pyproject.toml [tool.ruff]) | `cd python && ruff check .` | exit 0 | <observed> | <PASS/FAIL> |
    | 5a | Unannotated `@bv.table` raises TypeError | `cd python && pytest tests/internal/test_table_decorator_typeerror.py::test_typeerror_on_missing_annotation -x` | passed | <observed> | <PASS/FAIL> |
    | 5b | Annotated `@bv.table` works | `cd python && pytest tests/internal/test_table_decorator_typeerror.py::test_positive_control_annotated -x` | passed | <observed> | <PASS/FAIL> |

    ## CONTEXT.md decisions

    | Decision | Verification | Result |
    |---|---|---|
    | D-01 strict TypeError | TypeError message matches USER-LOCKED template | <PASS/FAIL> |
    | D-02 TCP read-path JSON default | `grep -c "encode_frame(OP_GET, CT_JSON" python/beava/_transport.py` >= 1 | <PASS/FAIL> |
    | D-03 `features` filter | `grep -c "features: list\[str\] \| None = None" python/beava/_transport.py python/beava/_app.py` >= 3 | <PASS/FAIL> |
    | D-04 `*_get_single` private | `grep -rn "tcp_get_single\|http_get_single" python/ \| grep -v "_tcp_get_single\|_http_get_single" \| wc -l` == 0 | <PASS/FAIL> |
    | D-05 embed-only fixture + no MagicMock | `grep -c "bv.App(test_mode=True)" python/tests/v0/conftest.py` == 1 AND `grep -rn "MagicMock" python/tests/v0/ \| wc -l` == 0 | <PASS/FAIL> |

    ## Throughput regression-gate (small/tcp; Apple-M4 / Darwin-24.3.0 / 10 cores)

    Command (verbatim, sourced from `.planning/throughput-baselines.md:1990` + leading
    `--pipeline crates/beava-bench/configs/small.json --transport tcp` per same-file lines
    800/831/875/915 invocation precedent):

    ```bash
    ./target/release/beava-bench-v18 \
        --pipeline crates/beava-bench/configs/small.json \
        --transport tcp \
        --wire-format msgpack \
        --blast-shape zipfian \
        --cardinality 10000 \
        --total-events 1000000 \
        --parallel 16 \
        --pipeline-depth 1024 \
        --no-ledger
    ```

    | Cell | Phase 13.5.1 | Phase 13.5 baseline | Delta | Verdict |
    |---|---|---|---|---|
    | small/tcp | <EPS> | 674,108 EPS | <±X>% | <PASS\|WARN\|FAIL> |

    Microbench exemption: Phase 13.5.1 ships zero Rust hot-path changes (Python SDK + decorator
    only). Per CLAUDE.md §Conventions → Performance Discipline ("Do NOT bench: cold-path
    code"), inherits the doc-only-phase precedent used by Phases 13.0 / 13.6 / 13.7. The
    throughput regression-gate cell above is the sole tripwire per CLAUDE.md §End-to-end
    throughput regression contract.

    ## Anti-pattern enforcement (from CONTEXT.md)

    - `MagicMock` against Transport surface: forbidden in `python/tests/v0/`. Verified via grep == 0.
    - Per-call `httpx.Client` instantiation: forbidden. Verified by inspection — all four HttpTransport methods reuse `self._client`.
    - Legacy `/push/{event_name}` route in SDK: forbidden. Verified via `grep -c '"/push/"' python/beava/_transport.py` == 0 (only verb-style `"/push"` used).
    - `OP_GET` (not `OP_GET_RESPONSE`) used as response-op check: forbidden. Verified by inspection — both `send_get` and `send_batch_get` check `OP_GET_RESPONSE`.

    ## Verdict

    PASS. All 5 ROADMAP success criteria, all 5 CONTEXT decisions, and all anti-pattern checks
    pass. Phase 13.5.1 is closed. Phase 13.7.5 is unblocked.
    ```

    Step 3 — run the throughput regression-gate cell. The exact harness invocation is pinned
    verbatim from `.planning/throughput-baselines.md:1990` (Phase 13.5 row, "Harness:" line);
    the leading `--pipeline crates/beava-bench/configs/small.json --transport tcp` prefix is
    sourced from the same file's lines 800, 831, 875, and 915 (Phase 19.x small/tcp invocation
    precedent that the line-1990 row leaves implicit because all 8 cells share the same suffix).
    The combined fully-qualified command for the small/tcp gate cell is:

    ```bash
    cd /Users/petrpan26/work/tally
    # Build the bench harness (the binary used at Phase 13.5 close):
    cargo build --release -p beava-bench --bin beava-bench-v18 2>&1 | tail -5

    # Run the small/tcp gate cell — 3 runs, take median (matches Phase 13.5 row's "median of 3"):
    for i in 1 2 3; do
      ./target/release/beava-bench-v18 \
          --pipeline crates/beava-bench/configs/small.json \
          --transport tcp \
          --wire-format msgpack \
          --blast-shape zipfian \
          --cardinality 10000 \
          --total-events 1000000 \
          --parallel 16 \
          --pipeline-depth 1024 \
          --no-ledger 2>&1 | tee /tmp/bench-13.5.1-run-$i.log | tail -10
    done

    # Extract the EPS line from each run's log; compute median
    grep -i "events_per_sec\|EPS\|throughput" /tmp/bench-13.5.1-run-*.log
    ```

    Take the median of the 3 runs. Capture the EPS number. Compute delta vs Phase 13.5 baseline
    (674,108 EPS — sourced from `.planning/throughput-baselines.md:1998`). Verdict per CLAUDE.md
    §Conventions → Performance Discipline: ±10% = PASS, 10–25% slower = WARN, > 25% slower = BLOCK.

    Step 4 — append the Phase 13.5.1 row to `.planning/throughput-baselines.md` as a new
    section appended at end-of-file. Use the same row format as the Phase 13.5 row at lines
    1987–2030. Format:

    ```markdown
    ---

    ## Phase 13.5.1 — Transport impl + decorator hardening (Phase 13.5 fix-up) — small/tcp regression-gate cell only (Apple-M4)

    **Date:** <YYYY-MM-DD>
    **Harness:** `./target/release/beava-bench-v18 --pipeline crates/beava-bench/configs/small.json --transport tcp --wire-format msgpack --blast-shape zipfian --cardinality 10000 --total-events 1000000 --parallel 16 --pipeline-depth 1024 --no-ledger` (small/tcp gate cell — 3 runs, median; verbatim from Phase 13.5 row at line 1990 of this file)
    **Commit:** <git rev-parse HEAD>
    **hw-class:** Darwin-24.3.0 / 10 cores (Apple M4) — same as Phase 13.5

    ### Phase 13.5.1 small/tcp regression-gate cell

    | Cell | Phase 13.5.1 EPS (median of 3) | Phase 13.5 baseline EPS | Δ% vs 13.5 | Verdict |
    |------|-------------------------------:|------------------------:|-----------:|---------|
    | small/tcp (gate) | <EPS> (median of 3: <r1> / <r2> / <r3>) | 674,108 (Phase 13.5) | <±X>% | <PASS\|WARN\|FAIL> |

    ### Doc-only-phase exemption

    Phase 13.5.1 is a Python-SDK fix-up phase with zero Rust hot-path changes (Python SDK
    transport implementations + `@bv.table` decorator hardening + test-fixture flips only).
    Per CLAUDE.md §Conventions → Performance Discipline ("Do NOT bench: cold-path code"),
    only the small/tcp regression-gate cell is captured (precedent: Phases 13.0 / 13.6 / 13.7
    used the same single-cell shape). The full 8-cell matrix is not required because the
    Rust hot path (`apply_event_to_aggregations`, `apply_shard.rs::dispatch_*`,
    `tcp_listener.rs`, `http_admin.rs`) is unchanged from Phase 13.5.

    ### Verdict

    <PASS|WARN|FAIL>. Phase 13.5.1 verdict per CLAUDE.md §End-to-end throughput regression
    contract: <verdict text> at the gate cell (small/tcp, <±X>%).
    ```

    Step 5 — bump `.planning/STATE.md`:

    1. Update front-matter `last_updated` to today's ISO timestamp (e.g. `"<YYYY-MM-DD>T<HH:MM:SS>.000Z"`).
    2. Update `status` field to reflect 13.5.1 close. Replace the existing 13.5.1-related sentence ("v0 OSS critical path remaining: Phase 13.7.5 transport impl + decorator hardening...") with the post-13.5.1 truth: "Phase 13.5.1 OFFICIALLY CLOSED <YYYY-MM-DD> (PASS); 0/68 v0-acceptance-test deficit resolved. v0 OSS critical path remaining: Phase 13.7.5 + 13.7.6 + 13.8 GA tag."
    3. Append a new HTML comment block following the existing 13.4/13.5/13.6/13.7 close-block convention:

       ```html
       <!-- Phase 13.5.1 OFFICIALLY CLOSED <YYYY-MM-DD> (PASS). 6 plans across 4 waves: Wave 1 (Plans 01-03 RED — decorator-hardening test, v0 fixture flip, transport-equivalence smoke); Wave 2 (Plan 04 GREEN — _table.py strict TypeError fix); Wave 3 (Plan 05 GREEN cluster — HttpTransport.send_push/get/batch_get/reset + TcpTransport.send_get/batch_get/reset + EmbedTransport delegates + _*_get_single rename + 70 decorator annotations + MagicMock replacements); Wave 4 (Plan 06 closure docs). 8+ commits total: 3 test: + 1 fix: + 4 feat:/refactor:/chore: in Plan 05 cluster + 1 docs:. All 5 ROADMAP success criteria + all 5 USER-LOCKED CONTEXT decisions D-01..D-05 honored verbatim. Test tallies: ≥ 68/68 v0 acceptance + ≥ 6 transport-equivalence (likely 7 incl. batch_get features test) + lifecycle + decorator-typeerror. Workspace gates GREEN: cargo test --workspace --features testing, cargo clippy, cargo fmt, mypy --strict python/beava, ruff check python/. Throughput regression-gate small/tcp: <±X>% (PASS|WARN). Anti-pattern enforced: zero MagicMock against Transport surface in python/tests/v0/. SUMMARY: .planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-SUMMARY.md. VERIFICATION: .planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-VERIFICATION.md. v0 critical path remaining: 13.7.5 (~2-3 days) → 13.7.6 (~2-3 days) → 13.8 GA (~5-7 days). -->
       ```

    Step 6 — update `.planning/ROADMAP.md` Phase 13.5.1 entry. Find line 846 (`### Phase 13.5.1: ... — 📋 PLANNED 2026-05-04`) and replace with:

    ```markdown
    ### Phase 13.5.1: Transport impl + decorator hardening (Phase 13.5 fix-up) — ✅ CLOSED <YYYY-MM-DD> (PASS)
    ```

    Append a verdict line immediately after the existing **Status** line (line 848):

    ```markdown
    **Verdict:** PASS. ≥ 68/68 v0 acceptance + ≥ 6 transport-equivalence + lifecycle GREEN. All 5 USER-LOCKED CONTEXT decisions D-01..D-05 honored. Throughput small/tcp regression-gate <±X>%. SUMMARY: `.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-SUMMARY.md`. VERIFICATION: `.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-VERIFICATION.md`.
    ```

    Step 7 — single docs commit. Per CLAUDE.md §Conventions → TDD Discipline item #4 (verbatim:
    "Smoke / acceptance tests can be written after all sub-features land, but each sub-feature
    must have its own unit-test red-green inside the plan. The phase-level smoke test is
    additive assurance, not a replacement for per-task tests."), this entire plan is one
    `docs:` commit with no paired `test:` because Plans 01–05 already shipped per-task
    red→green pairs (each sub-feature has its own unit-test red-green); Plan 06 captures only
    the phase-level acceptance verification (smoke as additive assurance), not new code:

    ```bash
    git add .planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-SUMMARY.md \
            .planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-VERIFICATION.md \
            .planning/throughput-baselines.md \
            .planning/STATE.md \
            .planning/ROADMAP.md
    git commit -m "docs(13.5.1-06): close phase — ≥ 68/68 + ≥ 6 transport-equivalence GREEN; 0/68 deficit resolved"
    ```
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && ls .planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-SUMMARY.md .planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-VERIFICATION.md && grep -c "Phase 13.5.1" .planning/throughput-baselines.md && grep -c "Phase 13.5.1 OFFICIALLY CLOSED" .planning/STATE.md && grep -c "✅ CLOSED" .planning/ROADMAP.md</automated>
    <expected_state>
      Output: both file paths print without error; `Phase 13.5.1` count >= 1 in throughput-baselines.md; close-comment count == 1 in STATE.md; `✅ CLOSED` count >= 1 in ROADMAP.md (this phase + any other closed phases).
    </expected_state>
  </verify>
  <done>
    - `.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-SUMMARY.md` exists with all template sections filled in.
    - `.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-VERIFICATION.md` exists with PASS/FAIL grid populated from real command outputs.
    - `.planning/throughput-baselines.md` has a Phase 13.5.1 small/tcp row.
    - `.planning/STATE.md` `last_updated` bumped; status reflects 13.5.1 close; new HTML comment block appended.
    - `.planning/ROADMAP.md` Phase 13.5.1 entry shows `✅ CLOSED <YYYY-MM-DD> (PASS)` with verdict line.
    - Single `docs(13.5.1-06): ...` commit lands all 5 file changes.
  </done>
</task>

</tasks>

<acceptance_criteria>
All five ROADMAP.md success criteria (lines 868-873) MUST be checkpointed in `13.5.1-VERIFICATION.md` with a grep- or pytest-verifiable check, each as a separate row in the PASS/FAIL grid:

1. **68/68 v0 acceptance tests GREEN against `bv.App(test_mode=True)` embed engine.**
   Check: `cd python && pytest tests/v0/ -x` exits 0 with collected ≥ 68 and ≥ 68 passed.

2. **≥ 6 cross-transport equivalence tests passing** (sibling Plan 03 rewrite may finalize 7 — original 6 + 1 batch_get-features test; flexible assertion accepts either count).
   Check: `cd python && pytest tests/v0/test_transport_equivalence.py -x` exits 0 with ≥ 6 passed.

3. **NO `MagicMock` against the Transport surface in any integration test.**
   Check 3a: `grep -rn "MagicMock" python/tests/v0/ | wc -l` == 0.
   Check 3b: `grep -rn 'MagicMock.*[Tt]ransport\|spec=.*Transport' python/tests/internal/test_app_lifecycle.py | wc -l` == 0.

4. **Workspace gates green.**
   Check 4a: `cargo test --workspace --features testing` exit 0.
   Check 4b: `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0.
   Check 4c: `cargo fmt --all --check` exit 0.
   Check 4d: `cd python && mypy --strict beava` exit 0 (Success).
   Check 4e: `cd python && ruff check .` exit 0 (Python lint per `python/pyproject.toml [tool.ruff]` line 62).

5. **`@bv.table(key="...")\ndef Fn(events):` raises `TypeError`; annotated form works.**
   Check 5a: `cd python && pytest tests/internal/test_table_decorator_typeerror.py::test_typeerror_on_missing_annotation -x` passed.
   Check 5b: `cd python && pytest tests/internal/test_table_decorator_typeerror.py::test_positive_control_annotated -x` passed.
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure documentation update; no code or input change) | n/a |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.5.1-10 | I (Information disclosure) | SUMMARY/VERIFICATION docs | accept | Documentation may reference architectural details; consistent with prior phase SUMMARYs (13.4 / 13.5 / 13.6 / 13.7) which are already public-repo; no PII or auth secrets in close docs. |

(Other STRIDE categories N/A — closure plan touches docs only.)
</threat_model>

<verification>
- `.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-SUMMARY.md` exists.
- `.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-VERIFICATION.md` exists with verdict PASS.
- `grep -c "Phase 13.5.1" .planning/throughput-baselines.md` >= 1.
- `grep -c "Phase 13.5.1 OFFICIALLY CLOSED" .planning/STATE.md` >= 1.
- `grep -c "Phase 13.5.1.*CLOSED" .planning/ROADMAP.md` >= 1.
- All five plan-summary files referenced in SUMMARY.md (`13.5.1-{01..05}-SUMMARY.md`) exist.
- Single `docs(13.5.1-06): ...` commit lands all 5 modified files.
</verification>

<success_criteria>
- Phase 13.5.1 has a complete close: SUMMARY + VERIFICATION + STATE/ROADMAP advance + throughput row.
- Verdict line is unambiguous (PASS, with WARN qualifier ONLY if the throughput cell exceeds 10% delta).
- Reviewer can verify every CONTEXT decision via the VERIFICATION.md grep / pytest commands.
- All 5 ROADMAP.md success criteria are checkpointed in `<acceptance_criteria>` and each maps to a separate row in `13.5.1-VERIFICATION.md`'s PASS/FAIL grid.
- Phase 13.7.5 is unblocked (next on v0 critical path).
</success_criteria>

<output>
After completion, this plan IS the closure — no further `*-SUMMARY.md` is needed for Plan 06 itself (the phase-level `13.5.1-SUMMARY.md` is the closure deliverable).
</output>
</content>
</invoke>