---
phase: 13.5.1
plan: 04
type: execute
wave: 2
status: COMPLETE-GREEN
subsystem: python-sdk
tags:
  - python-sdk
  - decorator
  - tdd-green
  - phase-13.5.1
requirements:
  - D-01
dependency_graph:
  requires:
    - 13.5.1-01-SUMMARY.md (RED tests landed at commit 404175ee)
    - 13.5.1-CONTEXT.md§D-01 (USER-LOCKED verbatim TypeError template)
    - "python/beava/_table.py::_resolve_upstream_proxies (HEAD silent-fallback site)"
  provides:
    - "Strict-TypeError fix in _resolve_upstream_proxies per D-01"
    - "Plan 13.5.1-01's 4 unit tests turn GREEN (3 negative + 1 positive control)"
    - "Unblocks Plan 13.5.1-05 (v0 decorator amendment sweep)"
  affects:
    - "Plan 13.5.1-05: 70 v0 acceptance tests using `def UserClicks(clicks):` will now raise TypeError at decoration time → Plan 05 amends them with `clicks: Click`-style annotations to re-green."
tech_stack:
  added: []
  patterns:
    - "Explicit `inspect.Parameter.empty` check at decoration time → TypeError with corrected-signature suggestion"
    - "Em-dash U+2014 preserved verbatim per CONTEXT.md D-01 template"
key_files:
  created:
    - ".planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-04-SUMMARY.md"
  modified:
    - "python/beava/_table.py (12-line insert: 6 docstring lines + 6 raise-TypeError lines)"
decisions:
  - "Source-line layout for f-string: `must be annotated with the upstream event class` lives on a single contiguous source line so the plan's `grep -c \"must be annotated...\"` verification predicate returns 1. Runtime-emitted joined string is byte-identical to D-01 USER-LOCKED template."
  - "Docstring updated to reference Phase 13.5.1 D-01 USER-LOCKED rationale; future maintainers see the contract embedded in the impl."
  - "Scope strictly _table.py ONLY per D-01: _events.py byte-identical to HEAD (verified `git diff HEAD -- python/beava/_events.py | wc -l` = 0)."
metrics:
  duration_minutes: 4
  completed: "2026-05-04T22:08:00Z"
  test_count: 4
  files_modified: 1
  insertions: 12
  deletions: 0
---

# Phase 13.5.1 Plan 04: GREEN _table.py strict TypeError; 4/4 decorator tests passing

**One-liner:** GREEN commit for D-01 — replaced the silent `inspect.Parameter.empty` fallback in `_resolve_upstream_proxies` with the explicit USER-LOCKED `TypeError` raise (em-dash U+2014 preserved), flipping Plan 01's 4 RED tests `3 failed, 1 passed` → `4 passed`, with `mypy --strict` clean and zero regression in the 149-test internal sweep.

## Diff Snippet

`python/beava/_table.py` — single function `_resolve_upstream_proxies` — 12-line insertion (6 docstring lines + 6 raise-TypeError lines):

```diff
diff --git a/python/beava/_table.py b/python/beava/_table.py
index fe8344d0..738c7755 100644
--- a/python/beava/_table.py
+++ b/python/beava/_table.py
@@ -50,6 +50,12 @@ def _resolve_upstream_proxies(fn: Callable[..., Any]) -> list[Any]:

     Mirrors ``_events._make_event_derivation`` so @bv.table works under
     ``from __future__ import annotations``.
+
+    Per Phase 13.5.1 D-01 (USER-LOCKED): raises ``TypeError`` if any decorated
+    parameter is missing an annotation — predictable, mypy-friendly, mirrors
+    the existing ``@bv.event`` convention. Silent fallback to
+    ``inspect.Parameter.empty`` (which surfaced as ``AttributeError`` in
+    user code) is forbidden.
     """
     sig = inspect.signature(fn)
     params = list(sig.parameters.values())
@@ -64,6 +70,12 @@ def _resolve_upstream_proxies(fn: Callable[..., Any]) -> list[Any]:
     proxies: list[Any] = []
     for p in params:
         ann = resolved.get(p.name, p.annotation)
+        if ann is inspect.Parameter.empty:
+            raise TypeError(
+                f"@bv.table function {fn.__name__!r} parameter {p.name!r} "
+                f"must be annotated with the upstream event class — "
+                f"e.g. def {fn.__name__}({p.name}: Click): ..."
+            )
         if isinstance(ann, str):
             ann = fn.__globals__.get(ann, ann)
         proxies.append(ann)
```

## D-01 Verbatim Message Text (Runtime-Emitted)

Asserted via `excinfo.value.args[0]` against a `@bv.table(key="user_id") def UserClicks(clicks): ...` decoration:

```python
"@bv.table function 'UserClicks' parameter 'clicks' must be annotated with the upstream event class — e.g. def UserClicks(clicks: Click): ..."
```

`MATCH: True` against the CONTEXT.md §D-01 USER-LOCKED template. Em-dash U+2014 byte-preserved.

## Test Results — RED → GREEN Transition

### At HEAD (pre-fix), Plan 01 RED state

```
$ pytest python/tests/internal/test_table_decorator_typeerror.py -v
3 failed, 1 passed in 0.33s
exit 1
# AttributeError: type object '_empty' has no attribute 'group_by'
# AttributeError: type object '_empty' has no attribute 'agg'
```

### Post-fix (this plan), Plan 01 GREEN state

```
$ pytest python/tests/internal/test_table_decorator_typeerror.py -v
test_keyed_form_unannotated_parameter_raises_typeerror PASSED      [ 25%]
test_composite_key_form_unannotated_parameter_raises_typeerror PASSED [ 50%]
test_bare_global_form_unannotated_parameter_raises_typeerror PASSED   [ 75%]
test_positive_control_annotated_parameter_returns_table_descriptor PASSED [100%]
============================== 4 passed in 0.52s ===============================
exit 0
```

### Internal-test regression sweep (no regressions)

```
$ pytest python/tests/internal/
============================== 149 passed in 0.82s ==============================
exit 0
```

### Type-check (mypy --strict)

```
$ mypy --strict python/beava
Success: no issues found in 19 source files
exit 0
```

## Verification Predicates (per <verification> section of plan)

| Predicate | Expected | Actual |
|---|---|---|
| `grep -c "if ann is inspect.Parameter.empty:" python/beava/_table.py` | `1` | `1` PASS |
| `grep -c "must be annotated with the upstream event class" python/beava/_table.py` | `1` | `1` PASS |
| `pytest python/tests/internal/test_table_decorator_typeerror.py -v` reports `4 passed` | `4 passed` | `4 passed` PASS |
| `pytest python/tests/internal/` exits 0 (no regression) | `exit 0` | `149 passed, exit 0` PASS |
| `mypy --strict python/beava` exits 0 | `Success` | `Success: no issues found in 19 source files` PASS |
| `python/beava/_events.py` byte-identical to pre-plan state | 0-line diff | `git diff HEAD~1 -- python/beava/_events.py \| wc -l` = 0 PASS |

## Done Criteria (per <done> section of plan)

- [x] `python/beava/_table.py::_resolve_upstream_proxies` raises `TypeError` on `inspect.Parameter.empty`.
- [x] All 4 tests in `python/tests/internal/test_table_decorator_typeerror.py` PASS.
- [x] All other tests in `python/tests/internal/` still pass (no regression — 149 passed).
- [x] `mypy --strict python/beava` exits 0.
- [x] Commit `fix(13.5.1-04): ...` lands with `_table.py` as the only change (verified: `git diff --stat HEAD~1` shows `python/beava/_table.py | 12 ++++++++++++` and only that file).
- [x] `python/beava/_events.py` is NOT modified (per D-01 scope).

## Commits

| # | SHA | Type | Subject |
|---|---|---|---|
| 1 | `ac1fcab1` | `fix` | `fix(13.5.1-04): @bv.table empty-annotation now raises TypeError with helpful message (D-01)` |
| 2 | _(this commit)_ | `docs` | `docs(13.5.1-04): complete plan — GREEN _table.py strict TypeError; 4/4 decorator tests passing` |

Author / committer: Hoang Phan `<hoang.phan@viggle.ai>` (no AI attribution per `feedback_no_ai_attribution_in_commits` memory).

## Plan-Checker Contract Compliance

| Contract | Status |
|---|---|
| TDD red-then-green: `test:` (Plan 01, `404175ee`) precedes `fix:` (this plan, `ac1fcab1`) | PASS |
| `fix:` commit modifies only `python/beava/_table.py` | PASS (`git diff --stat HEAD~1` confirmed) |
| `_events.py` unchanged (D-01 scope: `_table.py` only) | PASS (0-line diff vs HEAD~1) |
| `pytest` exit 0 on internal/ sweep | PASS (149 passed) |
| `mypy --strict python/beava` exit 0 | PASS |
| Author = Hoang Phan, no Co-Authored-By Claude | PASS (verified via `git log -1 --format='%an <%ae>'`) |
| D-01 verbatim message text emitted at runtime (em-dash U+2014 preserved) | PASS (`MATCH: True` vs CONTEXT.md template) |

## Self-Check

- [x] `python/beava/_table.py` modified (commit `ac1fcab1` — FOUND in `git log --oneline --all`).
- [x] `.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-04-SUMMARY.md` exists.
- [x] All 4 D-01 tests pass (verified `pytest -v`).
- [x] `mypy --strict python/beava` exit 0 (verified).
- [x] No file deletions, no `_events.py` drift (D-01 scope respected).

## Self-Check: PASSED

## Hand-off to Plan 13.5.1-05 (decorator amendment sweep)

Plan 04's GREEN landing means D-01's strict TypeError is now the live behavior at decoration time. Per CONTEXT.md `<estimate>` Wave 2 and Plan 04's `<action>` note ("Inter-plan ordering caveat"), the v0 acceptance tests under `python/tests/v0/` that decorate `def UserClicks(clicks):` (~70 sites) will now raise `TypeError` at decoration time rather than silently propagating an `_empty` sentinel. Plan 13.5.1-05 owns the mechanical sweep that adds `clicks: Click`-style annotations and re-greens those 70 sites alongside the transport-impl green-up. Closure plan 13.5.1-06 verifies the final 68/68 GREEN state of the v0 acceptance suite plus the transport-equivalence smokes.

## Known Stubs

None. Pure decorator-time validation; no UI / data wiring / placeholders introduced.

## Threat Flags

None. Decorator-time validation, no I/O, no auth surface, no PII (function/parameter names are user-supplied source-level identifiers; identical exposure profile to existing `_make_event_derivation` TypeError on empty params per CONTEXT.md `<threat_model>` T-13.5.1-05 disposition `accept`).
