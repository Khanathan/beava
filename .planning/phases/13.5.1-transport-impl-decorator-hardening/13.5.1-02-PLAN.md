---
phase: 13.5.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - python/tests/v0/conftest.py
autonomous: true
requirements:
  - D-05
tags:
  - python-sdk
  - test-infrastructure
  - tdd-red
  - magicmock-anti-pattern

must_haves:
  truths:
    - "The shared `app` fixture under `python/tests/v0/conftest.py` yields `bv.App(test_mode=True)` (real engine spawn with BEAVA_TEST_MODE=1)"
    - "Running the existing 68 v0 acceptance tests through this fixture produces a RED state at HEAD because the transport `send_*` stubs raise `NotImplementedError`"
    - "The fixture spawns a fresh subprocess per test (function-scope) — no cross-test state leakage"
  artifacts:
    - path: "python/tests/v0/conftest.py"
      provides: "Updated `app` fixture using test_mode=True per D-05"
      contains: "bv.App(test_mode=True)"
      min_lines: 95
  key_links:
    - from: "python/tests/v0/conftest.py::app"
      to: "python/beava/_app.py::App.__init__"
      via: "test_mode kwarg propagation"
      pattern: "App\\(test_mode=True\\)"
    - from: "python/tests/v0/conftest.py::app"
      to: "python/beava/_embed.py::spawn_embedded_server"
      via: "BEAVA_TEST_MODE=1 env propagation"
      pattern: "BEAVA_TEST_MODE"
---

<objective>
RED scaffold for D-05 (USER-LOCKED CONTEXT decision): flip the existing `app` fixture in `python/tests/v0/conftest.py` from `bv.App()` to `bv.App(test_mode=True)`, then run the 10 v0 acceptance test files through it to confirm the **0/68 deficit reproduces against the real engine**. The current state at HEAD has the fixture using plain `bv.App()` — but no test currently passes because the transport `send_*` stubs raise `NotImplementedError`.

This plan does NOT add new tests; the 68 acceptance tests already exist. It ONLY amends the fixture body and captures the RED-state baseline (0/68 expected) so Plan 13.5.1-05's GREEN commit (transport impl) has a measurable green-up target.

Purpose: encode the test-harness contract before the impl. Per D-05 anti-pattern reminder, integration tests MUST hit the real engine. This is the canonical CLAUDE.md §TDD Discipline red-then-green pattern applied to a fixture amendment.

Output: a one-line fixture amendment + a RED-state log captured in the commit body.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-CONTEXT.md
@.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-PATTERNS.md
@.planning/STATE.md
@CLAUDE.md
@python/tests/v0/conftest.py
@python/tests/internal/test_app_test_mode.py
@python/beava/_app.py
@python/beava/_transport.py
@python/beava/_embed.py

<interfaces>
<!-- Key context the executor needs. Extracted from codebase 2026-05-04. -->

# Current `app` fixture — python/tests/v0/conftest.py:85-98 (D-05 amend site)
@pytest.fixture
def app() -> Generator[Any, None, None]:
    """Yield a fresh ``bv.App()`` embed-mode instance per test.

    The fixture spawns a local beava subprocess on ephemeral ports via
    bv.App() no-URL embed mode (uses ``python/beava/_embed.py`` machinery),
    yields the entered context manager, and tears down on test exit.

    Tests should NOT close the app explicitly — the fixture handles teardown.
    """
    import beava as bv  # noqa: PLC0415

    with bv.App() as instance:    # ← amend to: bv.App(test_mode=True)
        yield instance


# `bv.App(test_mode=True)` plumbing chain (already lands BEAVA_TEST_MODE=1):
# python/beava/_app.py::App.__init__ → make_transport(test_mode=test_mode)
# python/beava/_transport.py::make_transport → spawn_embedded_server(test_mode=...)
# python/beava/_embed.py::spawn_embedded_server → env["BEAVA_TEST_MODE"]="1"
# Spawn boundary verified by python/tests/internal/test_app_test_mode.py:21-50
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.a (red): Flip `app` fixture to test_mode=True; capture 0/68 RED baseline</name>
  <files>python/tests/v0/conftest.py</files>
  <read_first>
    - python/tests/v0/conftest.py (current `app` fixture body, lines 85-98)
    - python/tests/internal/test_app_test_mode.py:21-50 (analog: real spawn-boundary test for test_mode propagation)
    - .planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-CONTEXT.md §D-05 (USER-LOCKED fixture body)
    - .planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-PATTERNS.md §"Target: python/tests/v0/conftest.py"
  </read_first>
  <behavior>
    - Behavior 1: After the amendment, `app` fixture yields a `bv.App(test_mode=True)` context manager (real spawned subprocess, BEAVA_TEST_MODE=1 in its env).
    - Behavior 2: Running `pytest python/tests/v0/ -x --collect-only -q` reports 68 collected tests (no collection errors; no `_engine_available()` skip).
    - Behavior 3: Running the full `pytest python/tests/v0/` produces 0 passed / 68 failed-or-errored at HEAD because `Http/Tcp/EmbedTransport.send_push/get/batch_get/reset` raise `NotImplementedError` at base class.
  </behavior>
  <action>
    Edit ONLY `python/tests/v0/conftest.py`. Change line 97 from:

    ```python
    with bv.App() as instance:
    ```

    to:

    ```python
    with bv.App(test_mode=True) as instance:
    ```

    Update the docstring above (lines 86-94) to mention `test_mode=True`:

    ```python
    """Yield a fresh ``bv.App(test_mode=True)`` embed-mode instance per test.

    Per Phase 13.5.1 D-05 (USER-LOCKED): every v0 acceptance test runs against
    a real spawned subprocess with BEAVA_TEST_MODE=1, so app.reset() is callable
    and OP_RESET frames are accepted by the engine. NO MagicMock against the
    Transport surface — that anti-pattern masked the 0/68 deficit at Phase 13.5
    Plan 11 close.

    The fixture spawns a local beava subprocess on ephemeral ports via the
    no-URL `bv.App(test_mode=True)` embed-mode path (uses python/beava/_embed.py
    machinery; passes test_mode through to spawn_embedded_server which sets
    env["BEAVA_TEST_MODE"]="1"), yields the entered context manager, and tears
    down on test exit.

    Tests should NOT close the app explicitly — the fixture handles teardown.
    """
    ```

    Do NOT modify any of the 10 `python/tests/v0/test_*.py` files (decorator-annotation amendments are Plan 13.5.1-05's responsibility — the test_mode-fixture amendment must NOT cascade decorator edits into this RED commit).

    Confirm the RED state by running the suite. Capture the result (e.g. `0 passed, 68 failed`) and put it in the commit body so reviewers can see the baseline. The expected failure mode is `NotImplementedError` from `Transport.send_register` / `send_push` / etc.

    NOTE: depending on whether `_engine_available()` returns True (binary discoverable) vs False (binary missing), the test count visible in the failure may be 68 collected + 68 errored (if binary present) or 68 collected + 68 skipped (if binary missing). Either RED state is acceptable — the commit body must record which mode was observed.

    Commit with subject: `test(13.5.1-02): flip v0 app fixture to test_mode=True; capture 0/68 RED baseline (D-05)`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && grep -c "bv.App(test_mode=True)" python/tests/v0/conftest.py</automated>
    <expected_state>
      Output is `1` (single fixture amended).
      Then run: `cd /Users/petrpan26/work/tally && uv run pytest python/tests/v0/ --co -q 2>&1 | tail -5` — should show `68 tests collected` (or skip-marker count) without collection errors.
      Then run: `cd /Users/petrpan26/work/tally && uv run pytest python/tests/v0/ -x 2>&1 | tail -10` — exit NON-ZERO at HEAD (RED).
    </expected_state>
  </verify>
  <done>
    - `python/tests/v0/conftest.py` line 97 reads `with bv.App(test_mode=True) as instance:`.
    - The docstring is updated to reference D-05 + the MagicMock anti-pattern reminder.
    - `pytest python/tests/v0/ -x` exits NON-ZERO at HEAD (RED).
    - Commit `test(13.5.1-02): ...` lands with conftest.py as the only change.
    - Commit body records the observed RED baseline (passed / failed / errored / skipped tallies).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → spawned beava subprocess | Subprocess spawned via discovered binary (`BEAVA_BINARY` env or `target/debug/beava`) with `BEAVA_TEST_MODE=1` env. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.5.1-02 | E (EoP via test_mode) | Spawned binary in test_mode | mitigate | `test_mode=True` is gated to embed-mode (no URL); `_embed.spawn_embedded_server` is the only path that sets `BEAVA_TEST_MODE=1`. Existing internal test (`python/tests/internal/test_app_test_mode.py`) already asserts the env-var propagation boundary. No new attack surface. |

(Other STRIDE categories N/A — fixture amendment is a one-liner; spawn boundary already exists.)
</threat_model>

<verification>
- `grep -c "bv.App(test_mode=True)" python/tests/v0/conftest.py` returns `1`.
- `grep -c "MagicMock" python/tests/v0/conftest.py` returns `0` (no mock contamination of the v0 fixture).
- `pytest python/tests/v0/ --co -q` collects without error.
- `pytest python/tests/v0/ -x` exits non-zero at HEAD (RED — transport stubs raise `NotImplementedError`).
- Commit body captures the observed failure tally.
</verification>

<success_criteria>
- RED baseline reproduced and recorded.
- Plan 13.5.1-05 will turn this RED into GREEN by implementing `Http/Tcp/EmbedTransport.send_*` methods.
</success_criteria>

<output>
After completion, create `.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-02-SUMMARY.md` with:
- Diff snippet (1-line fixture amendment + docstring rewrite)
- RED baseline tally (e.g. `0 passed, 68 errored at NotImplementedError`)
- Commit SHA of `test:` commit
</output>
