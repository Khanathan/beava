---
plan: 13.5.1-07a
phase: 13.5.1
wave: 6
depends_on: [13.5.1-07]
files_modified:
  - python/beava/_agg.py
autonomous: true
requirements: [D-01, D-05]
tdd: true
tags:
  - sdk-helpers
  - signature-reconciliation
  - operator-catalogue
status: AUTHORED-2026-05-04
must_haves:
  truths:
    - "Every one of the 21 v0 acceptance Cat-1 tests called out in 13.5.1-07-PLAN.md lines 78-103 has a helper signature that matches its call site verbatim."
    - "The 11 helpers with signature drift (histogram, hour_of_day_histogram, dow_hour_histogram, seasonal_deviation, event_type_mix, reservoir_sample, geo_velocity, geo_distance, geo_spread, distance_from_home, streak, max_streak, negative_streak, first_seen_in_window, has_seen, outlier_count) are reconciled to the contract documented under docs/operators/."
    - "Helpers whose tests already pass at the signature layer (first_n, last_n, time_since, time_since_last_n, value_change_count) are extended with where= for parity with the doc contract — non-breaking additive change."
    - "Per CLAUDE.md §Conventions §TDD Discipline item #1, every family fix lands as a red commit (failing helper-signature unit test) followed by a green commit (helper edit) — the v0 acceptance suite remains the smoke layer per item #4 (it is engine-dependent + binary-gated)."
  artifacts:
    - path: ".planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-07a-PLAN.md"
      provides: "Per-family signature-reconciliation execution plan"
    - path: ".planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-07a-SUMMARY.md"
      provides: "Closure record + per-family commit hashes + GREEN verification of new unit tests"
    - path: "python/beava/_agg.py"
      provides: "Reconciled helper signatures for 11 operator helpers across 5 families"
    - path: "python/tests/internal/test_agg_signatures_07a.py"
      provides: "Unit-test suite that asserts each reconciled helper accepts the call shape used by the v0 acceptance tests AND emits the documented descriptor JSON"
  key_links:
    - from: ".planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-07a-PLAN.md"
      to: ".planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-07-PLAN.md"
      via: "Direct dispatch from triage Category 1 (lines 78-103, 256-266)"
    - from: "python/beava/_agg.py"
      to: "docs/operators/buffer-geo/, docs/operators/recency/, docs/operators/velocity/, docs/operators/point-ordinal/"
      via: "Each helper signature now matches the canonical signature block under docs/operators/<family>/<op>.md"
---

<objective>
Reconcile 16 SDK aggregation-helper signatures in `python/beava/_agg.py` so the 21 Category-1 v0 acceptance tests called out in the 13.5.1-07 triage (lines 78-103) can register without raising `TypeError`. The contract is encoded by both the live test suite (`python/tests/v0/test_{buffer,geo,point_ordinal,recency,velocity}.py`) and the canonical signature blocks under `docs/operators/<family>/<op>.md`. Helper edits are bounded — kwarg-renames, kwarg-additions, kwarg-removals, type changes — no descriptor-construction or wire-shape changes outside `_agg.py`. Five families, one commit pair (red `test:` + green `fix:`/`feat:`) per family, plus a closing `docs:` SUMMARY commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-07-PLAN.md
@.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-CONTEXT.md
@docs/sdk-api/python.md
@CLAUDE.md
</context>

<tdd_discipline>
This is NOT a doc-only plan — `python/beava/_agg.py` is modified. Per CLAUDE.md §Conventions §TDD Discipline item #1, every family fix decomposes into:

- **Task N.a (red)** — extend `python/tests/internal/test_agg_signatures_07a.py` with assertions covering the family's reconciled helpers; run pytest, confirm RED with the expected `TypeError` / wrong-descriptor-shape signature errors. Commit `test(13.5.1-07a): <family> signature contract`.
- **Task N.b (green)** — edit `python/beava/_agg.py` to fix the family's helpers; rerun the unit suite, confirm GREEN. Commit `fix(13.5.1-07a): <family> signature reconciliation` (or `feat:` if the change adds an entirely new kwarg surface like `categories`/`max_categories` on `event_type_mix`).

Item #4 applies for the v0 acceptance integration smoke: this plan closes the helper-sig leg of the deficit. The 21 Cat-1 tests cannot all pass purely from helper edits — most require a working engine binary too — and the engine-side closure tracks separately under 07b/07c/07d. This plan's GREEN gate is the unit-suite added in Task N.a, plus the gate that the previously-passing v0 tests (those whose helper sigs are unchanged) remain GREEN.
</tdd_discipline>

<scope_boundary>
**In scope (this plan):**
- Edit signatures of 16 helpers in `python/beava/_agg.py` — 6 buffer, 4 geo, 2 point/ordinal, 7 recency (including 4 with sig drift + 3 with where= parity-add), 2 velocity (1 sig drift + 1 where= already-present test).
- Add `python/tests/internal/test_agg_signatures_07a.py` covering the reconciled call shapes (the unit RED→GREEN contract).
- Mechanical descriptor-construction follow-through: `lat`/`lon` kwargs replace `lat_field`/`lon_field` positional args at extras-dict construction; `samples` kwarg replaces `k` extras key; `sigma` kwarg replaces `threshold` extras key; `buckets: list[float]` flows through into the extras dict; `categories: list[str] | None` and `max_categories: int = 256` are added on `event_type_mix`.

**Out of scope (defer to other plans):**
- `python/beava/_app.py` register-payload builder (`_descriptor_to_node`, `_FIXED_OP_OUTPUT_TYPE`) — owned by 13.5.1-07b.
- `python/beava/_table.py` (`_resolve_upstream_proxies`) — owned by 13.5.1-07e.
- `python/beava/_embed.py` cleanup hooks — owned by 13.5.1-07e.
- Any Rust crate (`crates/**`) — owned by 13.5.1-07b for the schema-propagation fix.
- The Cat-2/3/4/5 tests — owned by 07b/07c/07d.
- Changing the wire-shape that the descriptor `to_dict()` produces — sketches were already updated in 13.5.1-05 to handle the existing extras dict shape; we keep extras-key naming aligned with what the Rust deserializer accepts (`buckets`, `samples`, `sigma`, `lat_field`, `lon_field`, `categories`, `max_categories`). Where the SDK-facing kwarg name differs from the wire extras-key, the helper translates inside the body.

**Wire-name vs SDK-name translation (deliberate, per docs/sdk-api/python.md "user-facing names" lock):**
- SDK kwarg `lat=` → wire extras key `lat_field` (wire stays stable; user-facing name matches docs).
- SDK kwarg `lon=` → wire extras key `lon_field`.
- SDK kwarg `samples=` → wire extras key `samples` (already matches; just a kwarg rename from `k`).
- SDK kwarg `sigma=` → wire extras key `sigma` (rename from `threshold`).

This translation is local to `_agg.py` helper bodies; it keeps the Rust deserializer untouched (server schema for `geo_*` reads `lat_field`/`lon_field`; server `outlier_count` already accepts `sigma` per CONTEXT D-02 implication and the test passing `sigma=` in 13.5.1-05).
</scope_boundary>

<per_family_table>
## Per-family fix table

| Family | Tests (count) | Helpers touched | Drift kind | Estimated LOC |
|--------|---------------|-----------------|------------|---------------|
| **A — Buffer** | 6 (test_buffer.py: histogram, hour_of_day_histogram, dow_hour_histogram, seasonal_deviation, event_type_mix, reservoir_sample) | `histogram`, `hour_of_day_histogram`, `dow_hour_histogram`, `seasonal_deviation`, `event_type_mix`, `reservoir_sample` | Drop `window=` from 5 ops (lifetime-only per docs); change `histogram.buckets` from `int` → `list[float]`; rename `reservoir_sample.k` → `samples`; add `categories`/`max_categories` to `event_type_mix` | ~40 |
| **B — Geo** | 4 (test_geo.py: geo_velocity, geo_distance, geo_spread, distance_from_home) | `geo_velocity`, `geo_distance`, `geo_spread`, `distance_from_home` | Replace positional `lat_field, lon_field` → kw-only `lat=`, `lon=`; drop `window=` from 3 ops (no `window=` in v0 docs); add `samples=100` default to `distance_from_home` | ~30 |
| **C — Point/ordinal** | 2 (test_point_ordinal.py: first_n, last_n) | `first_n`, `last_n` | Helpers already accept the test call shape; doc adds `where=` parity | ~6 |
| **D — Recency** | 7 (test_recency.py: streak, max_streak, negative_streak, first_seen_in_window, time_since, time_since_last_n, has_seen) | `streak`, `max_streak`, `negative_streak`, `has_seen`, `first_seen_in_window`, `time_since`, `time_since_last_n` | Drop required `field` positional arg from 4 ops (`streak`, `max_streak`, `negative_streak`, `has_seen`, `first_seen_in_window`); add `where=` to all 7 helpers per doc parity | ~35 |
| **E — Velocity** | 2 (test_velocity.py: outlier_count, value_change_count) | `outlier_count`, `value_change_count` | Rename `outlier_count.threshold` → `sigma` (test passes `sigma=`, doc says `sigma`); `value_change_count` already matches — no change | ~6 |

**Total:** 16 helpers across 5 families; ~120 LOC of `_agg.py` edits; ~250 LOC of new unit tests.

</per_family_table>

<tasks>

### Task 1 — Buffer family (test_buffer.py: 6 tests)

**Task 1.a (red)** — Author `python/tests/internal/test_agg_signatures_07a.py::test_buffer_family` covering:
- `bv.histogram("amount", buckets=[10.0, 50.0, 100.0, 500.0])` returns descriptor with `extras={"buckets": [10.0, 50.0, 100.0, 500.0]}`, no `window` field on `to_dict()`.
- `bv.hour_of_day_histogram()` returns descriptor with `op="hour_of_day_histogram"`, no `window` field.
- `bv.dow_hour_histogram()` returns descriptor with `op="dow_hour_histogram"`, no `window` field.
- `bv.seasonal_deviation("amount")` returns descriptor with `op="seasonal_deviation"`, `field="amount"`, no `window`.
- `bv.event_type_mix("kind")` returns descriptor with `op="event_type_mix"`, `field="kind"`, no `window`.
- `bv.event_type_mix("kind", categories=["p2p","card"], max_categories=10)` returns descriptor with `extras={"categories": [...], "max_categories": 10}`.
- `bv.reservoir_sample("amount", samples=100)` returns descriptor with `extras={"samples": 100}` (NOT `k`).
- Run `pytest python/tests/internal/test_agg_signatures_07a.py::test_buffer_family -v`. Expect 7 RED `TypeError`s.
- Commit: `test(13.5.1-07a): buffer family signature contract`.

**Task 1.b (green)** — Edit `python/beava/_agg.py`:
- `histogram(field, *, buckets: list[float], where=None)`: drop `window`, change `buckets` type to `list[float]`, validate `len(buckets) >= 1` + strictly-increasing.
- `hour_of_day_histogram(*, where=None)`: drop required `window`.
- `dow_hour_histogram(*, where=None)`: drop required `window`.
- `seasonal_deviation(field, *, where=None)`: drop required `window`.
- `event_type_mix(field, *, categories=None, max_categories=256, where=None)`: drop `window`, add `categories`/`max_categories`. Add validation `max_categories >= 1`.
- `reservoir_sample(field, *, samples: int, where=None)`: rename `k` → `samples`. Validate `samples >= 1`. Add `where=` for parity with docs.
- Re-run `pytest python/tests/internal/test_agg_signatures_07a.py::test_buffer_family -v`. Expect GREEN.
- Commit: `feat(13.5.1-07a): reconcile buffer family signatures (histogram lifetime + reservoir samples + event_type_mix categories)`.

### Task 2 — Geo family (test_geo.py: 4 tests)

**Task 2.a (red)** — Extend `python/tests/internal/test_agg_signatures_07a.py::test_geo_family`:
- `bv.geo_velocity(lat="latitude", lon="longitude")` returns descriptor with `extras={"lat_field": "latitude", "lon_field": "longitude"}`, no `window` field.
- `bv.geo_distance(lat="lat", lon="lon")` returns descriptor with extras lat/lon, no `window`.
- `bv.geo_spread(lat="lat", lon="lon")` returns descriptor with extras lat/lon, no `window`.
- `bv.distance_from_home(lat="lat", lon="lon", samples=50)` returns descriptor with `extras={"lat_field": "lat", "lon_field": "lon", "samples": 50}`, no `window`.
- Run targeted test. Expect 4 RED.
- Commit: `test(13.5.1-07a): geo family signature contract`.

**Task 2.b (green)** — Edit `python/beava/_agg.py`:
- `geo_velocity(*, lat: str, lon: str, where=None)`: replace positional `lat_field, lon_field` + drop `window`. Body: `extras={"lat_field": lat, "lon_field": lon}`.
- `geo_distance(*, lat: str, lon: str, where=None)`: same shape.
- `geo_spread(*, lat: str, lon: str, where=None)`: same shape; drop `window`.
- `distance_from_home(*, lat: str, lon: str, samples: int = 100, where=None)`: same shape; drop `window`; add `samples=100` default. Validate `samples >= 1`.
- Run targeted test. Expect GREEN.
- Commit: `feat(13.5.1-07a): reconcile geo family signatures (lat/lon kwargs + lifetime + samples)`.

### Task 3 — Point/ordinal family (test_point_ordinal.py: 2 tests)

**Task 3.a (red)** — Extend `python/tests/internal/test_agg_signatures_07a.py::test_point_ordinal_family`:
- `bv.first_n("target", n=5)` already passes — assert descriptor shape unchanged.
- `bv.first_n("target", n=5, where=bv.col("k") == "v")` returns descriptor with `where` set — NEW kwarg.
- `bv.last_n("action", n=5, where=bv.col("k") == "v")` — same.
- Run targeted test. Expect 2 RED on the `where=` cases.
- Commit: `test(13.5.1-07a): point/ordinal family where= parity contract`.

**Task 3.b (green)** — Edit `python/beava/_agg.py`:
- `first_n(field, *, n: int, where=None)`: add `where=`.
- `last_n(field, *, n: int, where=None)`: add `where=`.
- Run targeted test. Expect GREEN.
- Commit: `feat(13.5.1-07a): add where= parity to first_n / last_n helpers`.

### Task 4 — Recency family (test_recency.py: 7 tests)

**Task 4.a (red)** — Extend `python/tests/internal/test_agg_signatures_07a.py::test_recency_family`:
- `bv.streak(where=bv.col("outcome") == "win")` returns descriptor with `op="streak"`, no `field`, `where` populated.
- `bv.max_streak(where=...)` — same.
- `bv.negative_streak(where=...)` — same.
- `bv.first_seen_in_window(window="1h")` returns descriptor with `op="first_seen_in_window"`, `window="1h"`, no `field`.
- `bv.first_seen_in_window(window="1h", where=...)` — also accepts `where=`.
- `bv.time_since(where=...)` — accepts `where=` parity.
- `bv.time_since_last_n(n=3, where=...)` — accepts `where=` parity.
- `bv.has_seen(where=bv.col("status") == "failed")` — accepts `where=`, no required `field`.
- Run targeted test. Expect 8 RED.
- Commit: `test(13.5.1-07a): recency family signature contract`.

**Task 4.b (green)** — Edit `python/beava/_agg.py`:
- `streak(*, where=None)`: drop required `field` positional arg.
- `max_streak(*, where=None)`: same.
- `negative_streak(*, where=None)`: same.
- `has_seen(*, where=None)`: drop required `field`, add `where=`.
- `first_seen_in_window(*, window: str, where=None)`: drop required `field`, add `where=`.
- `time_since(*, where=None)`: add `where=`.
- `time_since_last_n(*, n: int, where=None)`: add `where=`.
- Run targeted test. Expect GREEN.
- Commit: `feat(13.5.1-07a): reconcile recency family signatures (drop field + add where parity)`.

### Task 5 — Velocity family (test_velocity.py: 2 tests)

**Task 5.a (red)** — Extend `python/tests/internal/test_agg_signatures_07a.py::test_velocity_family`:
- `bv.outlier_count("x", window="1h", sigma=3.0)` returns descriptor with `extras={"sigma": 3.0}` (NOT `threshold`).
- `bv.value_change_count("state", window="1h")` already passes — assert descriptor unchanged (regression guard).
- Run targeted test. Expect 1 RED on `sigma`.
- Commit: `test(13.5.1-07a): velocity family signature contract`.

**Task 5.b (green)** — Edit `python/beava/_agg.py`:
- `outlier_count(field, *, window: str, sigma: float = 3.0, where=None)`: rename `threshold` → `sigma`.
- `value_change_count`: no change.
- Run targeted test. Expect GREEN.
- Commit: `fix(13.5.1-07a): rename outlier_count.threshold → sigma (matches docs + test contract)`.

### Task 6 — Closure

**Task 6.a (docs)** — Author `13.5.1-07a-SUMMARY.md` per template; record per-family commit hashes; verify the new unit suite is GREEN end-to-end (`pytest python/tests/internal/test_agg_signatures_07a.py -v`); verify `mypy --strict python/beava` exits 0; verify the previously-passing v0 acceptance tests remain GREEN where the binary is available (or skip cleanly where it is not). Commit: `docs(13.5.1-07a): complete plan — operator-catalogue signature reconciliation 5/5 families`.

</tasks>

<verification>

**Per-family unit GREEN:**
```bash
pytest python/tests/internal/test_agg_signatures_07a.py -v
```
Expected: all assertions pass; ~22 test cases total across 5 family tests.

**SDK-wide regression GREEN:**
```bash
pytest python/tests/internal/ -v
pytest python/tests/v0/ -v --no-header
```
The internal suite must remain fully GREEN. The v0 acceptance suite either:
- Skips cleanly when `_engine_available()` returns False (no binary on PATH); OR
- Runs against the binary; expectation is that the 21 Cat-1 tests previously failing at register-time `TypeError` now reach execution; whether they all pass end-to-end depends on engine-side fixes tracked in 07b/07c/07d (out of scope here).

**Type-check GREEN:**
```bash
mypy --strict python/beava
```
Must exit 0.

**Format / lint:**
```bash
ruff check python/beava
```
Must exit 0.

(No Rust touched in this plan — no `cargo` gates required.)

</verification>

<success_criteria>

1. `python/tests/internal/test_agg_signatures_07a.py` exists and is GREEN.
2. `python/beava/_agg.py` helpers match the documented signatures under `docs/operators/<family>/<op>.md` for all 16 helpers in the per-family table.
3. `mypy --strict python/beava` exits 0.
4. Five `feat:`/`fix:` commits land (one per family) plus their five paired `test:` commits + one closing `docs:` commit. Per CLAUDE.md §Conventions §TDD Discipline item #1, every `feat:`/`fix:` is preceded by its matching `test:` in the git log.
5. `13.5.1-07a-SUMMARY.md` is authored at the standard path with substantive per-family commit hashes and a self-check section.

</success_criteria>

<output>
- `.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-07a-PLAN.md` (this file)
- `.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-07a-SUMMARY.md` (Task 6.a)
- `python/beava/_agg.py` (Tasks 1.b, 2.b, 3.b, 4.b, 5.b — modified in place)
- `python/tests/internal/test_agg_signatures_07a.py` (Tasks 1.a, 2.a, 3.a, 4.a, 5.a — new file)
</output>

---

*Authored 2026-05-04 by the GSD plan executor for follow-through on 13.5.1-07 triage Category 1.*
