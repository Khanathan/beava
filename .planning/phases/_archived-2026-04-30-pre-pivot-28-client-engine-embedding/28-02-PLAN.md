---
phase: 28-client-engine-embedding
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - Cargo.toml
  - src/bin/tally_cli.rs
requirements: [P28-C1, P28-CLI-SKELETON]
autonomous: true

must_haves:
  truths:
    - "Running `cargo run --bin tally_cli -- clone --remote host:6400 --streams Transactions,Logins` exits 0 and prints a `not implemented yet` line naming the clone subcommand."
    - "Running `cargo run --bin tally_cli -- sync --remote host:6400` exits 0 and prints a `not implemented yet` line naming the sync subcommand."
    - "`tally_cli clone --mode streaming ...` exits non-zero with an error message naming Phase 31 as the future home of streaming mode."
    - "`tally_cli clone` with no `--remote` flag exits non-zero with a usage-style error."
    - "The admin token resolves from `TALLY_TOKEN` env var OR `--token` flag, with `--token` taking precedence."
    - "The existing `tally` server binary is unchanged — `cargo run -- ...` still boots the server with the same argv semantics."
  artifacts:
    - path: "src/bin/tally_cli.rs"
      provides: "clap-style subcommand dispatch for `clone` and `sync`, stub handlers, arg parsing for --remote/--streams/--keys/--key-prefix/--mode/--token"
      min_lines: 120
    - path: "Cargo.toml"
      provides: "new `[[bin]] name = \"tally_cli\" path = \"src/bin/tally_cli.rs\"` entry, no new top-level dependency"
      contains: 'name = "tally_cli"'
  key_links:
    - from: "src/bin/tally_cli.rs `main()`"
      to: "subcommand dispatch"
      via: "hand-rolled argv match on args[0] after binary name"
      pattern: 'match\s+subcommand'
    - from: "src/bin/tally_cli.rs token resolution"
      to: "env::var(\"TALLY_TOKEN\") + --token flag"
      via: "flag overrides env"
      pattern: 'TALLY_TOKEN'
---

<objective>
Add a `tally_cli` binary to the main crate (per locked decision C1) with `clone` and `sync` subcommand skeletons. Both subcommands parse the full Phase 29 flag surface (`--remote`, `--streams`, `--keys | --key-prefix`, `--mode`, `--token`) but print "not implemented yet — Phase 29" and exit 0 (success for a stub is correct; they did what they promised). `--mode streaming` is the one rejection path (Phase 31). No network code. No `Session` construction (that's 28-03). The goal is a demo-runnable binary that parses args correctly so Phase 29 can plug in without re-doing argument handling.

Purpose: Implements C1. Gives the user a CLI shape to demo ("look, `tally clone --streams X` parses") and gives Phase 29 a stable arg surface to target.

Output: `cargo build --bin tally_cli` produces an executable that correctly parses every documented flag shape and prints stub output for both subcommands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-client-engine-embedding/28-CONTEXT.md
@Cargo.toml
@src/bin/tally_suggest_config.rs

<interfaces>
<!-- Existing CLI arg-parsing style (hand-rolled, no clap). Match this style — CONTEXT §Specifics
     says "reuse whatever version the main crate already pins. No new top-level dependency unless
     strictly required." The crate has no clap pin; adding one is a new top-level dep. Reject it
     for v0. -->

From src/bin/tally_suggest_config.rs (the exemplar):
```rust
fn main() {
    let args: Vec<String> = env::args().skip(1).collect();
    let mut addr = "http://localhost:6401".to_string();
    let mut token: Option<String> = None;
    let mut i = 0;
    while i < args.len() {
        match args[i].as_str() {
            "--addr" => { i += 1; addr = args[i].clone(); }
            "--token" => { i += 1; token = Some(args[i].clone()); }
            "-h" | "--help" => { print_usage(); return; }
            _ => { eprintln!("unknown arg: {}", args[i]); print_usage(); std::process::exit(2); }
        }
        i += 1;
    }
    // ...
}
```

<!-- Flags required by CONTEXT D-C1 -->
- `--remote <host:port>` — required. E.g. `prod-cluster:6400`.
- `--streams <name>[,name...]` — required for `clone`, optional for `sync`.
- `--keys <key>[,key...]` — mutually exclusive with `--key-prefix`.
- `--key-prefix <prefix>` — mutually exclusive with `--keys`.
- `--mode historical|streaming` — default `historical`. `streaming` rejected in Phase 28 (Phase 31 will enable).
- `--token <token>` — overrides `TALLY_TOKEN` env var.
- `-h`, `--help` — usage text.
</interfaces>

Decision traceability:
- Per D-C1: `src/bin/tally_cli.rs` new bin target; subcommand dispatch; stub handlers.
- Per D-C1 flag list: every flag wired and parsed in this phase, even though handlers don't use them.
- Per CONTEXT §Specifics "reuse what's pinned": hand-rolled arg parsing to avoid a new `clap` dependency.
- Per CONTEXT §Specifics "tally_cli binary name": bin target `tally_cli` (cargo-level name); server binary keeps its `tally` name unchanged. We explicitly take the two-binary path (option b) — it's zero surgery on the server and is fine for demo. Users invoke via `cargo run --bin tally_cli -- clone ...` or the built `./target/debug/tally_cli clone ...`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tally_cli binary target with arg parsing and stub handlers</name>
  <files>src/bin/tally_cli.rs, Cargo.toml</files>
  <behavior>
    - Test 1: `clone --remote foo:6400 --streams Transactions` → exit 0, stdout contains "clone" + "not implemented yet".
    - Test 2: `sync --remote foo:6400` → exit 0, stdout contains "sync" + "not implemented yet".
    - Test 3: `clone --remote foo:6400 --streams Txn --mode streaming` → exit 2, stderr mentions "Phase 31" and "streaming".
    - Test 4: `clone --streams Txn` (no --remote) → exit 2, stderr mentions "--remote" required.
    - Test 5: `clone --remote foo:6400 --streams A --keys k1 --key-prefix pre` → exit 2, stderr mentions "mutually exclusive".
    - Test 6: Token precedence — with `TALLY_TOKEN=envtoken` env and `--token cli` flag, parsed token is "cli". With env only, parsed token is "envtoken". With neither, token is None (that's fine; network needs it, stubs don't).
    - Test 7: `--help` prints usage and exits 0.
    - Test 8: unknown subcommand (e.g., `tally_cli foo`) exits 2.
  </behavior>
  <action>
1. **Cargo.toml**: Add a new `[[bin]]` entry (append to the existing bin list):
   ```toml
   [[bin]]
   name = "tally_cli"
   path = "src/bin/tally_cli.rs"
   ```
   Do NOT add any `required-features` constraint. The tally_cli binary should build under both `server` and `client` features (it references nothing server-specific in this phase). This lets `cargo build --no-default-features --features client --bin tally_cli` succeed in 28-03.

2. **src/bin/tally_cli.rs**: Create with this structure (style matches `tally_suggest_config.rs`):
   - Top docstring: `//! tally_cli — client CLI for scoped local replicas. Subcommands: clone, sync. Phase 28 stubs.`
   - No external deps. Use `std::env`, `std::process`.
   - Define an internal enum:
     ```rust
     enum Subcommand { Clone, Sync }
     ```
   - Define a shared `ParsedArgs` struct with fields: `remote: Option<String>`, `streams: Vec<String>`, `keys: Option<Vec<String>>`, `key_prefix: Option<String>`, `mode: String` (default `"historical"`), `token: Option<String>`.
   - `fn parse_args(argv: &[String]) -> Result<(Subcommand, ParsedArgs), String>` — pure function, unit-testable. Walks argv flag-by-flag; returns structured error string on violation. Enforces:
     - subcommand must be `clone` or `sync` (first positional arg).
     - `--keys` and `--key-prefix` are mutually exclusive (error if both present).
     - `--remote` is required for both subcommands in this phase.
     - `--streams` parses comma-separated names into `Vec<String>` (required for `clone`; optional for `sync`).
     - `--keys` parses comma-separated.
     - `--mode` must be `historical` or `streaming`; `streaming` is accepted by the PARSER but REJECTED by the handler (clearer error messaging that way — parser fails only on invalid values like "foo").
     - Unknown flag or missing value → return `Err`.
   - `fn resolve_token(flag: Option<String>) -> Option<String>` — flag wins; else env `TALLY_TOKEN`; else None.
   - `fn handle_clone(args: ParsedArgs) -> i32` — if `args.mode == "streaming"`, print to stderr `"error: --mode streaming is not supported yet (Phase 31 will enable streaming mode)"` and return 2. Otherwise print to stdout:
     ```
     tally clone: not implemented yet — Phase 29 will wire the session manager + snapshot fetch + log consumer.
     (parsed) remote=<r> streams=[<s1>,<s2>] keys=<...> key_prefix=<...> mode=<m> token_set=<bool>
     ```
     Return 0.
   - `fn handle_sync(args: ParsedArgs) -> i32` — similar shape; print `"tally sync: not implemented yet — Phase 29"` and return 0. (Sync also rejects `--mode streaming` with the same Phase 31 message.)
   - `fn print_usage()` — multi-line usage block covering both subcommands and every flag, shown on `-h`/`--help` or on parse error. Mention the `TALLY_TOKEN` env var.
   - `fn main()`: collect argv, call `parse_args`, on Err → print usage + exit 2, on Ok dispatch to the handler, exit with its returned code.

3. **Tests** — add in the same file under `#[cfg(test)] mod tests { ... }`:
   - Unit tests covering the 8 behaviors in `<behavior>` above. Every test operates on `parse_args(&["sub".into(), ...])` and inspects the returned struct or `Err`. Handler tests (`handle_clone` return code for streaming rejection) run in-process — do NOT shell out to the compiled binary.
   - Token env test: use `std::env::set_var` / `remove_var` with care — serialize env-touching tests with a `parking_lot::Mutex` (already a crate dep) or gate them behind a `#[test]` + `--test-threads=1` note. Simplest: make `resolve_token` take an env closure injected for testability (`fn resolve_token(flag: Option<String>, env_lookup: impl Fn(&str) -> Option<String>) -> Option<String>`). Then test it without touching real env. Prefer this — zero flakiness.

4. Do NOT add a `clap` dependency. Do NOT wire any network code. Do NOT import anything from `tally::server` or `tally::client` (the latter doesn't exist yet; 28-03 creates it). The only `tally::` import acceptable here is if you reuse type names — in this plan, you don't need any.

5. Keep total file under ~250 lines. If it grows past that, flag it as too complex and split the tests into a separate `tests/tally_cli_args.rs` integration test file (but the parse_args function must stay in the bin so it remains testable via `#[cfg(test)]`).
  </action>
  <verify>
    <automated>cd /data/home/tally &amp;&amp; cargo build --bin tally_cli 2>&amp;1 | tail -20 &amp;&amp; cargo test --bin tally_cli 2>&amp;1 | tail -30 &amp;&amp; ./target/debug/tally_cli clone --remote foo:6400 --streams Transactions,Logins &amp;&amp; ./target/debug/tally_cli sync --remote foo:6400 &amp;&amp; (./target/debug/tally_cli clone --remote foo:6400 --streams A --mode streaming; test $? -eq 2)</automated>
  </verify>
  <done>
- `cargo build --bin tally_cli` succeeds.
- `cargo test --bin tally_cli` passes all 8 behavior tests.
- Manual: all 3 invocations in the verify command match their expected exit codes and output.
- `cargo build` (defaults) still green; `cargo test` full suite still green (no regression from Task 1 of 28-01).
- Cargo.toml has exactly one new `[[bin]]` entry for `tally_cli`.
  </done>
</task>

</tasks>

<test_plan>
## Plan-level test plan

**What we're testing:** Argument parsing correctness + subcommand dispatch + environment/flag token precedence + mode validation. Zero network, zero state, zero engine code exercised.

**Test coverage (all in `src/bin/tally_cli.rs#tests`):**

1. **Clone happy path** — `clone --remote foo:6400 --streams A,B` parses cleanly, handler exits 0.
2. **Sync happy path** — `sync --remote foo:6400` parses cleanly, handler exits 0.
3. **Streaming rejection** — both subcommands with `--mode streaming` exit 2 with a Phase 31 message.
4. **Missing --remote** — exits 2 with a usage error.
5. **Mutually-exclusive key selectors** — `--keys X --key-prefix Y` → parse error.
6. **Token precedence (injected env)** — `resolve_token(Some("cli"), |_| Some("env"))` → "cli". `resolve_token(None, |_| Some("env"))` → "env". `resolve_token(None, |_| None)` → None.
7. **--help** — returns 0, prints usage.
8. **Unknown subcommand** — `tally_cli foo` exits 2.

**Integration smoke (in `<verify>` block):** build the binary, run 3 real invocations, check exit codes.

**Out of scope (deferred to later phases):**
- Actual `--remote` connection — Phase 29.
- `--keys` / `--key-prefix` scope semantics validation against server — Phase 29.
- `tally query` / `tally inspect` subcommands — Phase 30.
- Streaming behavior — Phase 31.
- Historical catch-up loop — Phase 29.

**Regression guard:**
- `cargo test` full suite must still pass. Phase 28-01's `tests/phase28_feature_build.rs` must still pass.
- Existing server binary unchanged: `cargo build` produces `./target/debug/tally` with identical behavior (not asserted by automated test in this plan — trust the 28-01 regression suite).
</test_plan>

<verification>
- `cargo build --bin tally_cli` → exits 0.
- `cargo test --bin tally_cli` → 8 tests pass.
- `./target/debug/tally_cli clone --remote foo:6400 --streams A,B` → prints stub message, exits 0.
- `./target/debug/tally_cli sync --remote foo:6400` → prints stub message, exits 0.
- `./target/debug/tally_cli clone --remote foo:6400 --streams A --mode streaming` → exits 2, stderr mentions Phase 31.
- `cargo test` full suite → still green.
</verification>

<success_criteria>
A working `tally_cli` binary with two subcommand stubs, full Phase 29 flag surface parsed and validated, unit-tested argument handling, streaming mode explicitly deferred to Phase 31 via a clean error, and zero new top-level dependencies. The server binary remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/28-client-engine-embedding/28-02-SUMMARY.md` capturing:
- Exact usage string for `tally_cli --help`.
- List of arg-parsing edge cases covered by tests.
- Explicit note: what Phase 29 inherits (parse_args contract, ParsedArgs struct — or whether 29 is expected to replace the arg parser with clap).
- Any decision on streaming-mode parser handling (accept + handler-reject, vs parser-reject).
</output>
