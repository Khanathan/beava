---
phase: 28-client-engine-embedding
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/lib.rs
  - src/client/mod.rs
  - src/engine/pipeline.rs
  - tests/client_engine_roundtrip.rs
requirements: [P28-B1, P28-D1, P28-CLIENT-MODULE]
autonomous: true

must_haves:
  truths:
    - "A new `src/client/mod.rs` compiles under BOTH `--features client` and `--features server`."
    - "A stub `Session` type exists in `tally::client` with fields for the scope-shaped arguments the CLI parses. Fields are plain String/Vec<String>; the real Phase 27 Scope struct is NOT referenced."
    - "`OutOfScopeError` type exists in `tally::client` per CONTEXT §Shared dependencies — Phase 29 imports it."
    - "Engine's `push` path is audited for server-only side effects; any found are cfg-gated. (Expected: zero found — emission lives in server wrappers, not the engine.)"
    - "A client-only round-trip test passes under `--no-default-features --features client` — pushes an event, asserts state mutates, no server imports, no listeners, no network."
  artifacts:
    - path: "src/client/mod.rs"
      provides: "`Session` stub struct + `SessionMode` enum + `OutOfScopeError` type"
      min_lines: 40
    - path: "src/lib.rs"
      provides: "`pub mod client;` (unconditional)"
      contains: "pub mod client"
    - path: "tests/client_engine_roundtrip.rs"
      provides: "integration test passing under both default and client features"
      min_lines: 30
  key_links:
    - from: "src/client/mod.rs"
      to: "src/lib.rs"
      via: "pub mod client"
      pattern: "pub mod client"
    - from: "tests/client_engine_roundtrip.rs"
      to: "tally::engine::pipeline::PipelineEngine + tally::state::store::StateStore"
      via: "direct API — register pipeline, push event, assert state mutated"
      pattern: "PipelineEngine.*push"
---

<objective>
Land the shared `tally::client` module with stub `Session` + `OutOfScopeError` types, audit the engine's `push` path for server-only side effects (cfg-gate any found), and lock the "engine runs in client context" contract with an integration test that exercises it under `--features client`.

Purpose: Implements locked decisions B1 (memory-only client state — just reuse existing `StateStore`, no new disk type), D1 (cfg-gate side effects), and the shared `OutOfScopeError` type. Phase 29 builds the session manager on top of `Session` and swaps the stub scope fields for Phase 27's real `Scope` struct.

Output: `cargo test --no-default-features --features client --test client_engine_roundtrip` passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-client-engine-embedding/28-CONTEXT.md
@src/lib.rs
@src/engine/pipeline.rs
@src/state/store.rs

<interfaces>
Engine entry points (from `src/engine/pipeline.rs`) the client will eventually drive:

```rust
impl PipelineEngine {
    pub fn push(
        &self,
        stream_name: &str,
        event: &serde_json::Value,
        store: &StateStore,
        now: SystemTime,
    ) -> Result<FeatureMap, TallyError>;

    pub fn push_no_features(/* same args */) -> Result<FeatureMap, TallyError>;
}
```

Side-effect inventory (from planning grep):
- `src/engine/pipeline.rs`: NO direct `signals::` / `listener::` / `emit_*` / `SubscriberRegistry` / `SignalRegistry` calls found.
- All metric/signal/listener emission lives in `src/server/tcp.rs`, `src/server/signals.rs`, `src/server/latency.rs` — wrappers AROUND engine.push, not inside it.
- Conclusion: CONTEXT D1's "wrap side-effect branches in #[cfg]" is already satisfied structurally. Task 2 re-verifies this and adds an anti-regression check.

Stub Session shape (this plan creates):
```rust
#[derive(Debug, Clone)]
pub enum SessionMode { Historical /* Streaming added in Phase 31 */ }

#[derive(Debug, Clone)]
pub struct Session {
    pub remote: String,
    pub streams: Vec<String>,
    pub keys: Option<Vec<String>>,
    pub key_prefix: Option<String>,
    pub mode: SessionMode,
    pub token: Option<String>,
}

#[derive(Debug, thiserror::Error)]
#[error("query out of scope: {0}")]
pub struct OutOfScopeError(pub String);
```
</interfaces>

Decision traceability:
- Per D-B1: client state is just the existing in-memory `StateStore`. No new disk-backed variant.
- Per D-D1: audit for in-engine side effects and cfg-gate any found.
- Per CONTEXT §Shared dependencies: `OutOfScopeError` defined here for Phase 29 to import.
- Per user directive: stub `Session` compiles without Phase 27 code — fields are plain `String`/`Vec<String>`, not `Scope`.
- `thiserror = "2.0"` already in Cargo.toml → safe to use the derive.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create `src/client/mod.rs` stubs and wire into lib.rs</name>
  <files>src/client/mod.rs, src/lib.rs</files>
  <behavior>
    - `Session::new("host:6400", vec!["Txn"])` constructs with defaults: Historical mode, no keys/prefix/token.
    - `SessionMode::Streaming` does NOT exist in Phase 28 (commented out with a Phase 31 TODO).
    - `format!("{}", OutOfScopeError::new("foo"))` == `"query out of scope: foo"`.
    - `tally::client::Session` imports cleanly under both `--features server` (default) and `--features client` builds.
    - No `tally::server::*` reference in `src/client/mod.rs`.
  </behavior>
  <action>
1. Create `src/client/mod.rs`:
   - Top docstring: `//! Client-side engine embedding. Phase 28 ships the type surface; Phase 29 wires session manager + log consumer.`
   - Module is UNCONDITIONAL (no feature gate). Server builds compile it too — cheap and keeps things simple. This lets server-side tests reference client types if useful later.
   - `pub enum SessionMode { Historical }` — comment line noting `Streaming` variant is Phase 31.
   - `pub struct Session` with fields shown in `<interfaces>`. All fields `pub`. `#[derive(Debug, Clone)]`.
   - `impl Session { pub fn new(remote: impl Into<String>, streams: Vec<String>) -> Self { /* defaults */ } }`.
   - `pub struct OutOfScopeError(pub String);` with `#[derive(Debug, thiserror::Error)]` and `#[error("query out of scope: {0}")]`.
   - `impl OutOfScopeError { pub fn new(s: impl Into<String>) -> Self { Self(s.into()) } }`.
   - `#[cfg(test)] mod tests` with two tests:
     - `session_new_defaults` — `Session::new("h", vec!["A".into()])` → mode is Historical, keys/prefix/token are None.
     - `out_of_scope_display` — assert formatted string equals `"query out of scope: foo"`.

2. Edit `src/lib.rs` — add `pub mod client;` (unconditional). Final file should read:
   ```rust
   pub mod client;
   pub mod engine;
   pub mod error;
   #[cfg(feature = "server")]
   pub mod server;
   pub mod state;
   pub mod types;
   ```

3. Do NOT:
   - Import or reference `tally::server::*` anywhere in `src/client/mod.rs`.
   - Reference a Phase 27 `Scope` struct (may not exist yet; not needed per user directive).
   - Add async/tokio/axum types.
   - Add lifecycle methods (`connect`, `bootstrap`, `run`) to `Session` — Phase 29's job.
  </action>
  <verify>
    <automated>cd /data/home/tally &amp;&amp; cargo test client:: --lib 2>&amp;1 | tail -20 &amp;&amp; cargo build --no-default-features --features client --lib 2>&amp;1 | tail -10 &amp;&amp; cargo build 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
- `src/client/mod.rs` exists with Session, SessionMode, OutOfScopeError + 2 passing unit tests.
- `src/lib.rs` has `pub mod client;` unconditional.
- `cargo test client::` passes.
- `cargo build --no-default-features --features client --lib` green.
- `cargo build` (defaults) green.
- `grep -rn "crate::server\|tally::server" src/client/mod.rs` returns empty.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit engine push path + land client-mode round-trip integration test</name>
  <files>src/engine/pipeline.rs, tests/client_engine_roundtrip.rs</files>
  <action>
1. **Audit**: run `grep -nE "crate::server|tally::server|signals::|SignalRegistry|SubscriberRegistry|LatencyTracker|ThroughputTracker|emit_[a-z_]+" src/engine/pipeline.rs`.
   - Expected result: zero hits (planning audit already found this).
   - If ANY hit is found, wrap the enclosing statement or block with `#[cfg(feature = "server")]`. Document each gate in the task commit body.
   - If zero hits: no `pipeline.rs` change needed. Record the audit result in the task SUMMARY.

2. **Inspect existing engine integration tests** to crib a minimal-pipeline pattern:
   - `ls tests/ | head -30` — find an existing test that calls `PipelineEngine::push` with a tiny pipeline.
   - Read the smallest one. Note: the API for registering a pipeline (likely `PipelineEngine::register` or via `RegisterRequest`); the structure of a minimal descriptor (one stream, one operator); the imports needed.

3. **Create `tests/client_engine_roundtrip.rs`**:
   - Top docstring: `//! Phase 28-03: prove the engine runs in a client context. Registers a minimal pipeline, pushes one event, asserts state mutated. No server imports; compiles and passes under --no-default-features --features client.`
   - Imports ONLY from `tally::engine::*`, `tally::state::*`, `tally::client::*` (if useful), `tally::types::*`, `tally::error::*`, `serde_json`, `std::time::SystemTime`. No `tally::server::*` — enforce by manual review + the fact that under `--features client` that path doesn't exist.
   - Body:
     1. Build an empty `StateStore`.
     2. Build a `PipelineEngine` and register exactly one pipeline using the same approach the cribbed reference test uses. Prefer the simplest operator — a count or sum over a single numeric field on a single stream.
     3. Call `engine.push("Events", &json!({"entity_key":"u1","amt":1}), &store, SystemTime::now())` (field name adjusted to what the minimal pipeline expects — follow the cribbed test).
     4. Assert the returned `FeatureMap` is non-empty (or whatever assertion the reference test uses). Alternatively, directly query the StateStore for the entity and assert state is present.
     5. Push a second event, assert the aggregated feature value has advanced (proves the engine's operator state actually mutated, not just that `push` returned Ok).
   - Target length: 40–80 lines. If registering a pipeline by hand balloons past 100 lines, that's a signal there's no reusable helper — keep it inline but comment that Phase 29 should factor out a `minimal_client_harness()` helper.

4. **Verify under both feature sets**:
   - `cargo test --no-default-features --features client --test client_engine_roundtrip` → must pass.
   - `cargo test --test client_engine_roundtrip` → must pass (defaults).
   - If the client-features run fails because an existing test helper is only compiled under `server`, either:
     - Inline the necessary setup in this test (preferred — keeps the test self-contained).
     - Promote the helper to a shared location (`src/engine/*` or a `pub(crate)` module) behind both features.
   - Do NOT enable the `server` feature for this test — defeats the purpose.

5. **Regression check**: full suite `cargo test` under defaults must still be green.
  </action>
  <verify>
    <automated>cd /data/home/tally &amp;&amp; cargo test --no-default-features --features client --test client_engine_roundtrip 2>&amp;1 | tail -20 &amp;&amp; cargo test --test client_engine_roundtrip 2>&amp;1 | tail -10 &amp;&amp; cargo test 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
- Audit completed; any found side-effects in engine/pipeline.rs are cfg-gated (or the audit found none and this is documented in the SUMMARY).
- `tests/client_engine_roundtrip.rs` exists with a 2-event round-trip asserting state mutation.
- `cargo test --no-default-features --features client --test client_engine_roundtrip` passes.
- `cargo test --test client_engine_roundtrip` (defaults) passes.
- Full `cargo test` suite green — no regressions.
- The test file imports nothing from `tally::server::*`.
  </done>
</task>

</tasks>

<test_plan>
## Plan-level test plan

**What we're testing:** That the engine is genuinely usable in a no-listener context — the core Phase 28 deliverable.

**Test coverage:**

1. **Unit: client type surface** (`src/client/mod.rs#tests`, 2 tests).
   - Session constructor defaults.
   - OutOfScopeError Display format.

2. **Integration: client-mode engine round-trip** (`tests/client_engine_roundtrip.rs`).
   - Register minimal pipeline.
   - Push event 1 → state mutates.
   - Push event 2 → aggregated state advances.
   - Passes under `--no-default-features --features client` (the core invariant).
   - Also passes under defaults (regression guard).

3. **Audit: no in-engine side effects**.
   - Grep-based audit in Task 2; expected zero hits. If any hit surfaces in future edits, the compile under `--features client` will break (server symbols not in scope) — that's the automated guard.

4. **Regression: full suite**.
   - `cargo test` under defaults — every pre-existing test still green.
   - `cargo test --no-default-features --features client --lib` — library passes its tests (from 28-01 + this plan).

**Out of scope:**
- Multi-stream pipelines, joins, tables — the test is deliberately a single-stream single-operator minimum. Phase 29 adds richer scenarios.
- Network / session manager — Phase 29.
- Scope validation / OutOfScopeError raising path — Phase 29.
- Streaming mode — Phase 31.
- On-disk client state — Phase 32.

**Test design principles honored:**
- Self-contained — no reliance on server-feature helpers.
- Fast — pure in-process, no IO, no sleeps.
- Deterministic — fixed SystemTime value or `::now()` is fine because the minimal operator doesn't depend on time semantics. If the cribbed reference uses a fixed timestamp, copy that.
- Single obvious failure mode — if the test fails, the engine broke under client features; there's no ambiguity.
</test_plan>

<verification>
- `cargo test --no-default-features --features client --test client_engine_roundtrip` → passes.
- `cargo test --test client_engine_roundtrip` → passes.
- `cargo test client::` → 2 tests pass.
- `cargo test` → full suite green.
- `grep -rn "crate::server\|tally::server" src/client/ tests/client_engine_roundtrip.rs` → empty.
- `grep -nE "crate::server|signals::|SignalRegistry|SubscriberRegistry" src/engine/pipeline.rs` → empty (or any hit is cfg-gated).
</verification>

<success_criteria>
The engine provably runs in a client context: types exist (`Session`, `OutOfScopeError`), the engine's push path has no server dependencies (audited + locked in by a client-features integration test), and the full existing test suite is unregressed. Phase 29 can build the session manager against this surface without waiting on Phase 27.
</success_criteria>

<output>
After completion, create `.planning/phases/28-client-engine-embedding/28-03-SUMMARY.md` capturing:
- Audit result for `src/engine/pipeline.rs` (hit count; which blocks gated, if any).
- The cribbed reference test and what was copied vs inlined.
- Any helpers that had to be promoted out of server-only scope.
- Explicit list of what Phase 29 inherits: `Session` field shape, `OutOfScopeError` import path, the round-trip test as a smoke harness.
- Explicit deferral note: Phase 27's `Scope` struct replaces the stub String/Vec<String> fields in Phase 29.
</output>
