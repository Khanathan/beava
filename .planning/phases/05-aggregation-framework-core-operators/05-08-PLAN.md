---
phase: 05-aggregation-framework-core-operators
plan: 08
type: execute
wave: 6
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06", "05-07"]
files_modified:
  - crates/beava-server/tests/phase5_smoke.rs
  - python/tests/test_phase5_smoke.py
files_modified_minor:
  - crates/beava-server/Cargo.toml
autonomous: true
requirements: [SDK-AGG-01, SDK-AGG-02, SDK-AGG-03, SDK-AGG-04, SDK-AGG-05, SDK-AGG-06, AGG-CORE-01, AGG-CORE-02, AGG-CORE-03, AGG-CORE-04, AGG-CORE-05, AGG-CORE-06, AGG-CORE-07, AGG-CORE-08, AGG-CORE-09]
must_haves:
  truths:
    - "SC1: Event.group_by(\"user_id\").agg(cnt=bv.count(window=\"5m\")) registered via SDK produces a Table with cnt feature"
    - "SC2: Push via /dev/apply_events updates the aggregation; GET /get/cnt/alice returns the current value"
    - "SC3: All 8 core operators pass table-driven correctness tests (covered by per-op unit tests in 05-01; SC3 E2E confirms they land end-to-end)"
    - "SC4: Replaying the same event stream produces byte-identical aggregation state"
    - "SC5: Lifetime / windowless mode works for ratio and count (window omitted)"
    - "SC6: Unknown field in op.field rejected at registration (covered by Plan 05-04; SC6 E2E confirms wire shape)"
  artifacts:
    - path: "crates/beava-server/tests/phase5_smoke.rs"
      provides: "Rust end-to-end smoke covering SC1..SC6 via TestServer + HTTP"
    - path: "python/tests/test_phase5_smoke.py"
      provides: "Python end-to-end smoke: SDK register + /dev/apply_events + GET /get"
  key_links:
    - from: "phase5_smoke.rs::sc4_replay_determinism"
      to: "apply_event_to_aggregations applied twice + state byte-identical"
      via: "format!(\"{:?}\", state_tables) comparison"
      pattern: "assert_eq!.*format!"
---

<objective>
Prove Phase 5 ships by exercising the full stack — SDK → register → /dev/apply_events → GET /get — and asserting SC1..SC6 from the ROADMAP.

Two smokes:
1. **Rust smoke** (`crates/beava-server/tests/phase5_smoke.rs`) — spins TestServer, exercises HTTP endpoints directly (no Python dependency), asserts each SC.
2. **Python smoke** (`python/tests/test_phase5_smoke.py`) — uses the SDK from Plan 05-07 to register against a live TestServer binary, pushes events via /dev/apply_events (since app.push is Phase 6), queries via GET /get.

Decision coverage matrix:

| D-XX | Plan | Coverage |
|------|------|----------|
| D-01 (enum dispatch) | 05-01 | per-op unit tests prove enum variants behave; phase5_smoke::sc3 confirms 8 operators land end-to-end |
| D-02 ({value} envelope) | 05-06 | sc1_register_and_get_single asserts response shape |
| D-03 (where predicate) | 05-02, 05-07 | sc2_push_with_where applies stream with where predicate; asserts value |
| D-04 (64-bucket tumbling) | 05-01 | windowed_count tests in 05-01; sc4_replay_determinism smoke |
| D-05 (Table output) | 05-03, 05-04 | sc1 asserts registry shows Table output_kind |
| D-06 (replay determinism) | 05-01, 05-02, 05-05 | sc4_replay_determinism smoke — byte-identical state after replay |
| D-07 (register-time validation) | 05-04 | sc6_unknown_field_rejected + sc6_aggregation_on_table_rejected smokes |
| D-08 (WAL shape) | Phase 6 | Not applicable; surfaced here only as a documented expectation |

Output: two smoke tests, phase verification passes all 6 SCs, phase ready to close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-aggregation-framework-core-operators/05-CONTEXT.md
@.planning/phases/05-aggregation-framework-core-operators/05-01-PLAN.md
@.planning/phases/05-aggregation-framework-core-operators/05-04-PLAN.md
@.planning/phases/05-aggregation-framework-core-operators/05-05-PLAN.md
@.planning/phases/05-aggregation-framework-core-operators/05-06-PLAN.md
@.planning/phases/05-aggregation-framework-core-operators/05-07-PLAN.md
@crates/beava-server/tests/phase2_5_smoke.rs
@crates/beava-server/tests/phase4_smoke.rs
@python/tests/test_phase4_smoke.py

<interfaces>
Builds on all prior plans. No new production APIs introduced; this plan is pure verification.

TestServer harness (Phase 1–2.5):
```rust
let ts = TestServer::spawn_with_dev_endpoints().await;  // sets BEAVA_DEV_ENDPOINTS=1
ts.post_json("/register", &payload).await;
ts.post_json("/dev/apply_events", &event).await;
let body = ts.get_json("/get/cnt/alice").await;
```

Python harness (Phase 3):
```python
with TestServer() as ts:  # spawns Rust binary with dev endpoints
    app = bv.App(ts.http_url)
    app.register(Transaction, TxCount5m)
    # /dev/apply_events via requests/httpx
    app.flush()
    result = httpx.get(f"{ts.http_url}/get/cnt_5m/alice").json()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (red): Failing Rust phase5_smoke covering SC1..SC6</name>
  <files>crates/beava-server/tests/phase5_smoke.rs</files>
  <read_first>
    - .planning/ROADMAP.md Phase 5 section (SC1..SC6 definitions)
    - crates/beava-server/tests/phase2_5_smoke.rs (TestServer spawn pattern)
    - crates/beava-server/tests/phase4_smoke.rs (dev-endpoints setup, register + apply pattern)
    - .planning/phases/05-aggregation-framework-core-operators/05-05-PLAN.md (/dev/apply_events request shape)
    - .planning/phases/05-aggregation-framework-core-operators/05-06-PLAN.md (GET /get + POST /get shapes)
  </read_first>
  <behavior>
    One test per SC plus decision coverage:

    - **sc1_register_groupby_produces_table**: POST /register with Transaction event + derivation `{name: "TxCount5m", upstreams: ["Transaction"], output_kind: "table", ops: [{op: "group_by", keys: ["user_id"], agg: {"cnt": {op: "count", params: {"window": "5m"}}}}], schema: <empty>, table_primary_key: ["user_id"]}` → 200, registry_version bumped. GET /registry shows derivation with output_kind=Table, table_primary_key=["user_id"], server-propagated schema fields contain {user_id: Str, cnt: I64}.

    - **sc2_push_then_get_returns_count**: Continuation of sc1. POST /dev/apply_events 10 times for user_id=alice → GET /get/cnt/alice → `{"value": 10}`.

    - **sc2_push_with_where_filters**: Register TxCount5mOk with where=`(status == "ok")`; push 10 events (7 ok, 3 failed) → GET /get/cnt_ok/alice → `{"value": 7}`.

    - **sc3_all_8_operators_e2e**: Register 8 separate aggregations on the same Transaction event, one per operator. Push 5 fixed events (amount=10,20,30,40,50 status=ok; one with status="bad"). Assert GET returns:
       - count: 5
       - sum: 150.0
       - avg: 30.0
       - min: 10.0
       - max: 50.0
       - variance: sample variance of [10,20,30,40,50] == 250.0 (exact)
       - stddev: sqrt(250.0) ≈ 15.8113883...
       - ratio (where status=="ok"): 5/6 → test with 6 events where 5 ok → 5/6 ≈ 0.833...
       - Use 1e-9 tolerance for float comparisons.

    - **sc4_replay_determinism**: Register count-5m. Push 100 events with deterministic content (user_id="u{i%3}", amount=i, event_time_ms=1000*i for i in 0..100). Snapshot the /dev state via a new dev endpoint `GET /dev/agg_state_snapshot` (which returns `format!("{:?}", state_tables)` as a string) OR make two instances of TestServer with the same event stream and assert their GET /get/cnt/u0 values match. Simpler approach: register against the same TestServer, push the 100 events, GET /get/cnt/u0 and /get/cnt/u1 and /get/cnt/u2. RESET state (new TestServer), push same 100 events, assert GET returns same values byte-identical (JSON string equality).

    - **sc5_lifetime_count_works**: Register `bv.count()` (no window) → AGG-CORE-09. Push 100 events for alice → GET /get/cnt_lifetime/alice → `{"value": 100}`.

    - **sc5_lifetime_ratio_works**: Register `bv.ratio(where=status=="ok")` (no window). Push 10 events (3 ok). GET → `{"value": 0.3}`.

    - **sc6_unknown_field_rejected**: POST /register with `agg: {"bad": {op: "sum", params: {"field": "nonexistent", "window": "5m"}}}` → 400 with `error.code == "aggregation_unknown_field"` and path including "nonexistent".

    - **sc6_aggregation_on_table_rejected**: POST /register with a Table source + group_by derivation → 400 with `error.code == "aggregation_on_table_not_supported"`.

    - **envelope_shape_is_value_only**: GET /get/cnt/alice body has exactly one top-level key, "value". NOT "meta", NOT "updated_at", NOT "value_and_meta". Asserts D-02.

    Write each as `#[tokio::test]` inside phase5_smoke.rs. All tests currently MUST fail because either:
    - the /dev/apply_events endpoint doesn't exist yet (Plan 05-05 hasn't landed) — but since this plan depends on 05-05..07 landing first, the tests will exist and can run
    - OR simply the `phase5_smoke.rs` file doesn't exist yet

    For the RED commit of this task: create the file with all 10 tests; they MAY currently pass if all prior plans are merged. The RED here is a different notion — the RED artifact is the test file stating the contract; the GREEN is confirming they all pass and gating CI. If any test fails at red, Task 1.b fixes whatever integration bug caused the failure.

    **This is an acceptance plan — the red-green cycle is slightly degenerate. Structure it so:**
    - Task 1.a creates the test file and **skips the test bodies initially via `#[ignore]`** with a comment `// UNIGNORE IN 1.b after asserting SC claims`. This produces a clean "no tests run" RED state.
    - Task 1.b removes the `#[ignore]`, lets the tests run, and fixes any integration bug surfaced.
  </behavior>
  <action>
    1. Create `crates/beava-server/tests/phase5_smoke.rs` with:
       - All 10 `#[tokio::test]` stubs
       - Each marked `#[ignore]` with a TODO comment: `// Plan 05-08 Task 1.b: un-ignore after cross-plan integration verified`
       - Test bodies can contain the full assertion logic already (we want to see if they would pass); the `#[ignore]` skips them from `cargo test --workspace` for now
    2. Register this integration-test binary in `crates/beava-server/Cargo.toml` under `[[test]] name = "phase5_smoke"` with `required-features = ["testing"]` (matching phase2_5_smoke.rs / phase4_smoke.rs).
    3. Run `cargo test --workspace -- --ignored phase5_smoke 2>&1` — expected: SOME pass (if all prior plans shipped cleanly), SOME fail (if cross-plan integration gaps exist). Document failures in the commit message.
    4. Run `cargo test --workspace` (without --ignored) — the new phase5_smoke tests should be listed as "ignored" (RED-neutral state).
    5. Commit matching regex `^test\(05-08\): ` (subject: `test(05-08): add phase5_smoke SC1..SC6 acceptance tests (ignored until 1.b)`).
  </action>
  <acceptance_criteria>
    - `ls crates/beava-server/tests/phase5_smoke.rs` exists
    - `grep -c '#\[ignore\]' crates/beava-server/tests/phase5_smoke.rs` >= 10
    - `grep -q 'sc1_register_groupby_produces_table' crates/beava-server/tests/phase5_smoke.rs` returns 0
    - `grep -q 'sc4_replay_determinism' crates/beava-server/tests/phase5_smoke.rs` returns 0
    - `grep -q 'envelope_shape_is_value_only' crates/beava-server/tests/phase5_smoke.rs` returns 0
    - `cargo test --workspace 2>&1 | grep 'phase5_smoke.*ignored'` shows ignored tests
    - `cargo test --workspace -- --ignored phase5_smoke 2>&1 | tail -10` runs tests (some may fail — that's fine for RED)
    - `git log -1 --format='%s'` matches regex `^test\(05-08\): `
  </acceptance_criteria>
  <done>Red commit lands with all 10 SC tests defined but ignored. Task 1.b un-ignores and fixes cross-plan integration gaps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (green): Un-ignore phase5_smoke + Python smoke + fix cross-plan integration gaps</name>
  <files>crates/beava-server/tests/phase5_smoke.rs, python/tests/test_phase5_smoke.py, any integration files identified as gaps</files>
  <read_first>
    - crates/beava-server/tests/phase5_smoke.rs (tests from 1.a)
    - python/tests/test_phase4_smoke.py (reference pattern for Python acceptance)
    - .planning/phases/05-aggregation-framework-core-operators/05-07-PLAN.md (Python SDK surface)
    - All other 05-0N-PLAN.md (if integration bugs surface, trace back to the plan that owns the module)
  </read_first>
  <action>
    1. Remove `#[ignore]` from all 10 tests in phase5_smoke.rs.
    2. Run `cargo test --workspace`. Identify any failures.
    3. For each failure, diagnose the root cause:
       - If it's a cross-plan integration gap (e.g., /dev/apply_events state_tables not actually shared with GET /get because Plan 05-05 and 05-06 constructed separate Arcs): fix in the appropriate plan's module (register.rs, feature_query.rs, registry_debug.rs).
       - If it's a test bug (wrong expected value): correct the test.
       - If it's a regression of a prior plan's unit test: fix the regression; do NOT paper over it.
    4. Create `python/tests/test_phase5_smoke.py` mirroring the Rust smoke:
       - Uses `TestServer()` context manager (from prior phases; spawns binary with BEAVA_DEV_ENDPOINTS=1)
       - Uses `bv.App(ts.http_url)` to register via SDK
       - Uses `httpx` directly to hit `/dev/apply_events` (Phase 5 dev-only; Python SDK.push is Phase 6)
       - Uses `httpx.get(f"{url}/get/cnt/alice")` for queries
       - Covers all 6 SCs (parallel to Rust smoke, but focus on SDK-side correctness: that the SDK's REGISTER JSON is accepted, that the Python surface returns correct values through the wire)
    5. Run:
       - `cargo test --workspace` — all green (200+ tests including the now-un-ignored 10 phase5_smoke)
       - `cargo clippy --workspace --all-targets --all-features -- -D warnings` — clean
       - `cargo fmt --all --check` — clean
       - `cd python && python -m pytest -q` — green (156 prior + ~25 new test_agg.py + ~6 new test_phase5_smoke.py)
       - `cd python && python -m ruff check . && python -m mypy beava/` — clean
    6. Commit matching regex `^feat\(05-08\): ` (subject: `feat(05-08): un-ignore phase5_smoke + Python phase5_smoke; fix cross-plan integration gaps; Phase 5 acceptance green`).

    **If cross-plan integration bugs surface, commit the fixes as separate commits** (`fix(05-NN): ...` for the relevant plan) BEFORE the final `feat(05-08): ...` commit. This keeps the red-green trace clean and lets the reviewer see exactly what glue was missing.
  </action>
  <acceptance_criteria>
    - `grep -c '#\[ignore\]' crates/beava-server/tests/phase5_smoke.rs` == 0
    - `cargo test --workspace` 0 failures, 0 ignored
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean
    - `cargo fmt --all --check` clean
    - `cd python && python -m pytest -q` all green
    - `cd python && python -m ruff check .` clean
    - `cd python && python -m mypy beava/` clean
    - All 10 Rust smokes PASS: sc1_register_groupby_produces_table, sc2_push_then_get_returns_count, sc2_push_with_where_filters, sc3_all_8_operators_e2e, sc4_replay_determinism, sc5_lifetime_count_works, sc5_lifetime_ratio_works, sc6_unknown_field_rejected, sc6_aggregation_on_table_rejected, envelope_shape_is_value_only
    - Python smoke `test_phase5_smoke.py` >= 6 test functions, all green
    - `git log -1 --format='%s'` matches regex `^feat\(05-08\): `
    - All 15 Phase 5 REQ-IDs have at least one smoke or unit test proving them
  </acceptance_criteria>
  <done>GREEN commit lands. Phase 5 acceptance complete. 15/15 REQ-IDs covered. SC1..SC6 from ROADMAP proven by smokes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase5_smoke → TestServer HTTP | Dev-endpoint surface; BEAVA_DEV_ENDPOINTS=1 in test harness; unused in prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Information Disclosure | Dev endpoints in test harness | accept | phase5_smoke sets BEAVA_DEV_ENDPOINTS=1 explicitly; production binary defaults to OFF |
| T-05-08-02 | Tampering | Flaky replay-determinism test | mitigate | sc4 uses a fixed pseudo-random input (deterministic formula based on i), NOT a RNG; byte comparison gives zero-tolerance assertion |
</threat_model>

<verification>
- `cargo test --workspace` all green, no ignored tests
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean
- `cargo fmt --all --check` clean
- `cd python && python -m pytest -q` all green
- `cd python && python -m ruff check . && python -m mypy beava/` clean
- Every ROADMAP SC (SC1..SC6) proven by a named test
- All 15 Phase 5 REQ-IDs traceable to at least one test
</verification>

<success_criteria>
- 10 Rust smoke tests pass covering SC1..SC6 + D-02 envelope + D-06 replay determinism + D-07 wire errors
- 6+ Python smoke tests pass exercising the SDK end-to-end against a live binary
- No regression of prior phases (~373+ Rust, ~156+ Python tests)
- Phase 5 acceptance gate PASSED; ready to close phase + write SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/05-aggregation-framework-core-operators/05-08-SUMMARY.md` and `.planning/phases/05-aggregation-framework-core-operators/05-SUMMARY.md` (phase-level summary).
</output>
