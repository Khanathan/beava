---
phase: 13.5.2
plan: 06
type: execute
wave: 4
depends_on: [13.5.2-05]
files_modified:
  - .planning/phases/13.5.2-decorator-only-derivations/13.5.2-SUMMARY.md
  - .planning/phases/13.5.2-decorator-only-derivations/13.5.2-VERIFICATION.md
  - .planning/throughput-baselines.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - D-04
tags:
  - docs
  - closure
  - phase-13.5.2

must_haves:
  truths:
    - "13.5.2-SUMMARY.md captures the 4 user-locked decisions D-01..D-04 + the work landed across plans 01-05 + the test-count delta + workspace gates"
    - "13.5.2-VERIFICATION.md walks each must_haves truth from the phase scope and marks PASS/FAIL with evidence"
    - ".planning/throughput-baselines.md gets a new ## Phase 13.5.2 section with a small/tcp regression-gate row (3-run median; ±10% PASS band per CLAUDE.md §End-to-end throughput regression contract)"
    - ".planning/STATE.md advances to record Phase 13.5.2 complete"
    - ".planning/ROADMAP.md Phase 13.5.2 entry status flips from 📋 PLANNED → ✅ COMPLETED with closing date"
    - "Closure plan invokes the CLAUDE.md §TDD Discipline item #7 doc-only-plan exemption verbatim — no paired test: commits required for this plan"
  artifacts:
    - path: ".planning/phases/13.5.2-decorator-only-derivations/13.5.2-SUMMARY.md"
      provides: "Phase 13.5.2 closure summary"
      contains: "D-01"
      contains_2: "D-02"
      contains_3: "D-03"
      contains_4: "D-04"
      min_lines: 80
    - path: ".planning/phases/13.5.2-decorator-only-derivations/13.5.2-VERIFICATION.md"
      provides: "Goal-backward verification of phase scope"
      contains: "PASS"
      min_lines: 50
    - path: ".planning/throughput-baselines.md"
      provides: "Phase 13.5.2 small/tcp regression-gate row"
      contains: "Phase 13.5.2"
      contains_2: "small/tcp"
      contains_3: "Verdict"
      min_lines: 2080
  key_links:
    - from: ".planning/phases/13.5.2-decorator-only-derivations/13.5.2-VERIFICATION.md"
      to: ".planning/phases/13.5.2-decorator-only-derivations/13.5.2-CONTEXT.md"
      via: "Goal-backward: each CONTEXT must_haves truth gets a PASS/FAIL verdict with evidence"
      pattern: "D-0[1-4]"
---

<objective>
Phase 13.5.2 closure plan. Per CLAUDE.md §TDD Discipline item #7 (doc-only-plan exemption — verbatim citation: **"CLAUDE.md §TDD Discipline item #7"** quoted in this plan body for plan-checker grep), this plan ships as a single `docs:` commit cluster WITHOUT paired `test:` commits. The validation captured here is the verification of work landed by upstream plans 01-05, which already shipped per-task red→green pairs.

Outputs:
1. **`13.5.2-SUMMARY.md`** — narrative closure: what we shipped, what the test-count delta was (73-74 → 78+ green), what the 4 user-locked decisions look like in the final code, what's deferred.
2. **`13.5.2-VERIFICATION.md`** — goal-backward verification: walk each `<must_haves>` truth from CONTEXT.md and confirm PASS with evidence (file paths, grep hits, pytest output).
3. **`.planning/throughput-baselines.md`** new section — Phase 13.5.2 small/tcp regression-gate row (3-run median per CLAUDE.md §End-to-end throughput regression contract). Phase 13.5.2 touches client-side decorator code only; no server hot-path edits → expect a no-op delta vs Phase 13.4.1 baseline (within ±10% PASS band).
4. **`.planning/STATE.md`** advance — record Phase 13.5.2 complete.
5. **`.planning/ROADMAP.md`** — flip Phase 13.5.2 entry from 📋 PLANNED → ✅ COMPLETED with closing date.

Commit cluster: 1-2 commits all of `type: docs`. No new tests, no new source code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.5.2-decorator-only-derivations/13.5.2-CONTEXT.md
@.planning/phases/13.5.2-decorator-only-derivations/13.5.2-01-PLAN.md
@.planning/phases/13.5.2-decorator-only-derivations/13.5.2-02-PLAN.md
@.planning/phases/13.5.2-decorator-only-derivations/13.5.2-03-PLAN.md
@.planning/phases/13.5.2-decorator-only-derivations/13.5.2-04-PLAN.md
@.planning/phases/13.5.2-decorator-only-derivations/13.5.2-05-PLAN.md
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/throughput-baselines.md
@.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-04-SUMMARY.md
@.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-VERIFICATION.md

<interfaces>
<!-- Key context the executor needs. Extracted from codebase 2026-05-04. -->

# DOC-ONLY-PLAN EXEMPTION — verbatim citation required for plan-checker:
# "CLAUDE.md §TDD Discipline item #7"
# (This is the positive enumeration grep target. Plan-checker greps PLAN files
# for this exact string to find plans claiming the exemption.)

# THROUGHPUT BENCH COMMAND (mirror Phase 13.4.1 row format at throughput-baselines.md:2037-2068):
# beava-bench-v18 --pipeline small --transport tcp --wire-format msgpack \
#   --blast-shape zipfian --cardinality 10000 \
#   --total-events 1000000 --parallel 16 --pipeline-depth 1024 --no-ledger
#
# Run 3 times, report median. Compare against Phase 13.4.1 baseline EPS (631,610).
# Expected delta: within ±10% PASS band (Phase 13.5.2 client-side-only changes; no server hot-path edits).

# THROUGHPUT-BASELINES.MD APPEND TEMPLATE (mirror Phase 13.4.1 section format):
##
## ---
##
## ## Phase 13.5.2 — Decorator-only derivation surface — small/tcp regression-gate run (Apple-M4)
##
## **Date:** {YYYY-MM-DD}
## **Harness:** `beava-bench-v18 --pipeline small --transport tcp --wire-format msgpack --blast-shape zipfian --cardinality 10000 --total-events 1000000 --parallel 16 --pipeline-depth 1024 --no-ledger`
## **Commit:** `{plan-05-merge-commit}` (Plan 13.5.2-05 GREEN cluster HEAD)
## **hw-class:** Darwin-24.3.0 / 10 cores (Apple M4)
## **Runs (sustained_eps):** {r1} / {r2} / {r3} → median **{median} EPS**
##
## ### Phase 13.5.2 small/tcp regression-gate row
##
## | Cell | Phase 13.5.2 EPS | Phase 13.4.1 baseline EPS | Δ% vs 13.4.1 | Verdict |
## |------|----------------:|--------------------------:|-------------:|---------|
## | small/tcp (gate) | {median} | 631,610 | **{Δ%}** | **{PASS|WARN|BLOCK}** (within ±10% gate band) |
##
## ### Headline
##
## **Verdict: PASS at the regression-gate cell.** Phase 13.5.2 ships **client-side-only** changes
## (`python/beava/_app.py` register-time guard, `python/beava/_table.py` + `python/beava/_events.py`
## decorator-time guards, `python/tests/v0/test_lit.py` rewrite, doc edits). Zero edits to
## `crates/beava-server/`. The {Δ%} delta is measurement variance on the dev box, consistent with
## the Phase 13.4.1 −6.30% / Phase 13.5 −7.1% range on the same gate cell with no Rust changes.
##
## ### Verdict
##
## **PASS.** Within the ±10% PASS band on the regression-gate cell; well clear of the 25% BLOCK
## threshold. Single-cell run is sufficient per CLAUDE.md (small/tcp is the canonical gate cell;
## medium / large / fraud-team / http variants not re-run for a client-side-only fix-up phase).

# STATE.md ADVANCE TEMPLATE — append to the end of "Phases" or "Current Work" section:
## - Phase 13.5.2 (Decorator-only derivation surface): COMPLETED {YYYY-MM-DD}.
##   v0 acceptance: {pre-count}/89 → {post-count}/89 GREEN (+{delta} from test_lit rewrite).
##   4 user-locked decisions D-01..D-04. SUMMARY:
##   .planning/phases/13.5.2-decorator-only-derivations/13.5.2-SUMMARY.md.

# ROADMAP.md FLIP — change line:
##   ### Phase 13.5.2: Decorator-only derivation surface (kill chain registration) — 📋 PLANNED 2026-05-04
## To:
##   ### Phase 13.5.2: Decorator-only derivation surface (kill chain registration) — ✅ COMPLETED {YYYY-MM-DD}
## And add a Status line below it:
##   **Closed:** {YYYY-MM-DD}. SUMMARY: `.planning/phases/13.5.2-decorator-only-derivations/13.5.2-SUMMARY.md`.
##   VERIFICATION: `.planning/phases/13.5.2-decorator-only-derivations/13.5.2-VERIFICATION.md`. v0 acceptance:
##   78+/89 GREEN; throughput small/tcp PASS (-X% within gate band).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 6.a (docs): Run small/tcp throughput regression-gate bench (3 runs, median)</name>
  <files>.planning/throughput-baselines.md</files>
  <read_first>
    - CLAUDE.md (§Performance Discipline → "End-to-end throughput regression contract" — confirm small/tcp is the canonical gate cell; ±10% PASS / 25% BLOCK thresholds)
    - .planning/throughput-baselines.md (Phase 13.4.1 section at lines 2037-2068 — mirror this format)
    - crates/beava-bench/src/bin/beava-bench-v18.rs (verify the bench binary exists; build if needed via `cargo build --release --bin beava-bench-v18`)
  </read_first>
  <action>
1. Build the bench binary if not already built:
   ```bash
   cargo build --release --bin beava-bench-v18 2>&1 | tail -5
   ```

2. Run the small/tcp gate cell 3 times consecutively, capturing `sustained_eps`:
   ```bash
   for i in 1 2 3; do
     ./target/release/beava-bench-v18 \
       --pipeline small --transport tcp --wire-format msgpack \
       --blast-shape zipfian --cardinality 10000 \
       --total-events 1000000 --parallel 16 --pipeline-depth 1024 --no-ledger \
       2>&1 | grep -E "sustained_eps|EPS"
   done
   ```

3. Compute the median of the 3 runs.

4. Compute the percentage delta vs Phase 13.4.1 baseline (631,610 EPS).

5. Append a new section to `.planning/throughput-baselines.md` using the template in `<interfaces>` above. Substitute:
   - `{YYYY-MM-DD}` → today's date
   - `{plan-05-merge-commit}` → output of `git log --format=%h -1 -- python/beava/_app.py python/beava/_table.py python/beava/_events.py | head -1` (the most recent commit touching the SDK files Plan 05 modified)
   - `{r1}`, `{r2}`, `{r3}`, `{median}` → measured EPS values
   - `{Δ%}` → computed delta vs 631,610
   - `{PASS|WARN|BLOCK}` → PASS if `|Δ%| < 10`, WARN if `10 ≤ |Δ%| < 25`, BLOCK if `|Δ%| ≥ 25`

If verdict is BLOCK: STOP and ask the user — do not silently complete the closure.

If verdict is WARN: still complete the closure but flag prominently in SUMMARY.md as a follow-up to investigate before OSS launch.
  </action>
  <verify>
- `tail -50 .planning/throughput-baselines.md` shows the new Phase 13.5.2 section with all template fields filled
- `grep -c "Phase 13.5.2" .planning/throughput-baselines.md` ≥ 2 (section header + table row)
- Verdict is PASS or WARN (not BLOCK)
  </verify>
  <acceptance_criteria>
    - `.planning/throughput-baselines.md` contains literal text `## Phase 13.5.2 — Decorator-only derivation surface — small/tcp regression-gate run`
    - File contains literal text `Phase 13.4.1 baseline EPS` (in the comparison table)
    - File contains literal text `Verdict: PASS` OR `Verdict: WARN` (not BLOCK)
    - File line count is ≥ 2080
    - The 3-run median EPS value appears in both the body text and the table row
  </acceptance_criteria>
  <commit>
    type: docs
    scope: 13.5.2-06
    subject: small/tcp regression-gate row appended to throughput-baselines.md
  </commit>
</task>

<task type="auto" tdd="false">
  <name>Task 6.b (docs): Write 13.5.2-SUMMARY.md + 13.5.2-VERIFICATION.md + advance STATE.md + flip ROADMAP.md</name>
  <files>
    - .planning/phases/13.5.2-decorator-only-derivations/13.5.2-SUMMARY.md
    - .planning/phases/13.5.2-decorator-only-derivations/13.5.2-VERIFICATION.md
    - .planning/STATE.md
    - .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/phases/13.5.2-decorator-only-derivations/13.5.2-CONTEXT.md (4 user-locked decisions D-01..D-04)
    - .planning/phases/13.5.2-decorator-only-derivations/13.5.2-01-PLAN.md through 13.5.2-05-PLAN.md (commit log to summarize)
    - .planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-04-SUMMARY.md (frontmatter + structure pattern to mirror)
    - .planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-VERIFICATION.md (verification structure to mirror)
    - .planning/STATE.md (find the Current Work / Recent Phases section to advance)
    - .planning/ROADMAP.md (find the Phase 13.5.2 entry to flip from PLANNED → COMPLETED)
  </read_first>
  <action>

**1. Write `.planning/phases/13.5.2-decorator-only-derivations/13.5.2-SUMMARY.md`:**

Frontmatter mirrors `13.5.1-04-SUMMARY.md`:
```yaml
---
phase: 13.5.2
plan: 06 (closure)
type: closure
wave: 4
status: COMPLETE
subsystem: python-sdk
tags: [python-sdk, decorator, closure, phase-13.5.2]
requirements: [D-01, D-02, D-03, D-04]
exemption: "CLAUDE.md §TDD Discipline item #7 (doc-only-plan exemption)"
key_files:
  modified:
    - python/beava/_app.py (Plan 03: D-01 register-time rejection, ~12 lines)
    - python/beava/_table.py (Plan 04: D-02 annotation-time rejection, ~10 lines)
    - python/beava/_events.py (Plan 04: D-02 rejection + _is_bv_event_function marker, ~14 lines)
    - python/tests/v0/test_lit.py (Plan 05: 5 tests rewritten to @bv.event def form)
    - docs/sdk-api/python.md (Plan 05: bv.lit examples rewritten)
    - .planning/decisions/ADR-003-global-aggregation-and-bv-lit.md (Plan 05: examples mirrored)
  created:
    - python/tests/internal/test_register_rejects_chain.py (Plan 01: 4 RED→GREEN tests for D-01)
    - python/tests/internal/test_decorator_rejects_chain_annotation.py (Plan 02: 5 RED→GREEN tests for D-02)
metrics:
  duration_minutes: ~360 (across 6 plans)
  v0_acceptance_pre: 73-74/89
  v0_acceptance_post: 78+/89
  test_lit_failures_resolved: 5
  throughput_small_tcp_delta: "{Δ% from Task 6.a}"
---
```

Body sections (mirror 13.5.1-04 structure):
- `## One-liner` — single sentence
- `## What we shipped` — 4 decisions D-01..D-04 each with a one-paragraph recap + the file/line landed
- `## Test count delta` — pre vs post; what's now green that wasn't before
- `## Throughput regression gate` — small/tcp result from Task 6.a
- `## Workspace gates at close` — `cargo test`, `cargo clippy`, `cargo fmt`, `pytest`, `mypy --strict` all GREEN
- `## What's deferred` — copy CONTEXT.md `<deferred>` verbatim with brief context
- `## Doc-only-plan exemption citation` — `**This closure plan invokes "CLAUDE.md §TDD Discipline item #7"** verbatim` (the grep target)

**2. Write `.planning/phases/13.5.2-decorator-only-derivations/13.5.2-VERIFICATION.md`:**

Goal-backward: walk each must_haves truth from CONTEXT.md `<domain>` "In scope" bullets and from each plan's must_haves and assert PASS with evidence.

Structure:
```markdown
# Phase 13.5.2 — Verification

**Verified:** {YYYY-MM-DD}
**Phase status:** COMPLETE-GREEN

## CONTEXT.md `<domain>` "In scope" — verification

### 1. `app.register()` rejects EventDerivation instances at top level with "wrap in @bv.event" hint
- **PASS** — `python/beava/_app.py::App.register` lines {N}-{M} (commit `{hash}`)
- Test evidence: `pytest python/tests/internal/test_register_rejects_chain.py -v` → 4/4 GREEN

### 2. `_resolve_upstream_proxies` (in `python/beava/_table.py`) rejects EventDerivation-instance annotation
- **PASS** — `python/beava/_table.py::_resolve_upstream_proxies` lines {N}-{M} (commit `{hash}`)
- Test evidence: `pytest python/tests/internal/test_decorator_rejects_chain_annotation.py::test_bv_table_rejects_chain_annotation_with_columns -v` → GREEN

### 3. `_make_event_derivation` (in `python/beava/_events.py`) rejects EventDerivation-instance annotation
- **PASS** — `python/beava/_events.py::_make_event_derivation` lines {N}-{M} (commit `{hash}`)
- Test evidence: `pytest python/tests/internal/test_decorator_rejects_chain_annotation.py::test_bv_event_def_rejects_chain_annotation_with_columns -v` → GREEN

### 4. 5 `python/tests/v0/test_lit.py` tests rewritten to @bv.event def form
- **PASS** — All 5 tests use `@bv.event def Name(upstream: Source): ...` form (commit `{hash}`)
- Test evidence: `pytest python/tests/v0/test_lit.py -v` → 5/5 GREEN

### 5. `docs/sdk-api/python.md:177-183` rewritten to @bv.event def form
- **PASS** — verified via `grep -c "@bv.event" docs/sdk-api/python.md` and absence of bare `events.with_columns(source=bv.lit(` chains (commit `{hash}`)

### 6. ADR-003:177-183 mirrored
- **PASS** — same shape as docs/sdk-api/python.md (commit `{hash}`)

### 7. Paired RED→GREEN unit tests for both rejection paths
- **PASS** — Plan 01 RED + Plan 03 GREEN (D-01 register-time); Plan 02 RED + Plan 04 GREEN (D-02 annotation-time). Commit chain in git log.

## Workspace gates at close

| Gate | Result | Command |
|------|--------|---------|
| Rust workspace tests | PASS | `cargo test --workspace --features testing` |
| Rust clippy | PASS | `cargo clippy --workspace --all-targets --all-features -- -D warnings` |
| Rust fmt | PASS | `cargo fmt --all --check` |
| Python pytest | PASS | `pytest python/tests` (failure count = HEAD baseline minus 5 test_lit) |
| Python mypy | PASS | `mypy --strict python/beava` |

## Throughput regression gate

| Cell | Phase 13.5.2 EPS | Phase 13.4.1 baseline EPS | Δ% | Verdict |
|------|----------------:|--------------------------:|---:|---------|
| small/tcp (gate) | {median} | 631,610 | {Δ%} | PASS |

## Final v0 acceptance count

- Pre-13.5.2 baseline: 73-74/89 GREEN (16 failed)
- Post-13.5.2: {N}/89 GREEN ({89-N} failed) — strictly improved by ≥ 5 tests (the 5 test_lit failures are all GREEN; the high-volume + reset failures stay deferred per CONTEXT.md `<deferred>`)

## Verdict

**Phase 13.5.2 PASSES.** All 4 user-locked decisions D-01..D-04 landed in code; all RED tests turned GREEN; v0 acceptance strictly improved; workspace gates clean; throughput regression-gate PASS.
```

**3. Advance `.planning/STATE.md`:**

Find the "Current Work" or "Recent Phases" section and append a new bullet for Phase 13.5.2 per the template in `<interfaces>` above.

**4. Flip `.planning/ROADMAP.md` Phase 13.5.2 entry:**

Find:
```
### Phase 13.5.2: Decorator-only derivation surface (kill chain registration) — 📋 PLANNED 2026-05-04
```

Replace with:
```
### Phase 13.5.2: Decorator-only derivation surface (kill chain registration) — ✅ COMPLETED {YYYY-MM-DD}

**Closed:** {YYYY-MM-DD}. SUMMARY: `.planning/phases/13.5.2-decorator-only-derivations/13.5.2-SUMMARY.md`. VERIFICATION: `.planning/phases/13.5.2-decorator-only-derivations/13.5.2-VERIFICATION.md`. v0 acceptance: 78+/89 GREEN; throughput small/tcp PASS ({Δ%} within gate band).
```

(Keep all other lines of the original Phase 13.5.2 entry intact — only flip the status line and add the Closed line.)
  </action>
  <verify>
- `cat .planning/phases/13.5.2-decorator-only-derivations/13.5.2-SUMMARY.md | head -50` shows valid YAML frontmatter + sections
- `cat .planning/phases/13.5.2-decorator-only-derivations/13.5.2-VERIFICATION.md` shows all 7 in-scope items marked PASS with evidence
- `grep "Phase 13.5.2.*COMPLETED" .planning/ROADMAP.md` matches
- `grep "Phase 13.5.2" .planning/STATE.md` shows the new entry
- `pytest python/tests/v0/ --no-header -q 2>&1 | tail -1` confirms ≥ 78 passes
- `cargo test --workspace --features testing 2>&1 | tail -3` exits 0
- `cargo clippy --workspace --all-targets --all-features -- -D warnings 2>&1 | tail -3` exits 0
- `mypy --strict python/beava 2>&1 | tail -3` exits 0
  </verify>
  <acceptance_criteria>
    - `.planning/phases/13.5.2-decorator-only-derivations/13.5.2-SUMMARY.md` exists with ≥ 80 lines
    - SUMMARY contains literal text `D-01`, `D-02`, `D-03`, `D-04` (all four decisions named)
    - SUMMARY contains literal text `CLAUDE.md §TDD Discipline item #7` (verbatim exemption citation for plan-checker grep)
    - `.planning/phases/13.5.2-decorator-only-derivations/13.5.2-VERIFICATION.md` exists with ≥ 50 lines
    - VERIFICATION contains literal text `PASS` for each of the 7 in-scope items
    - `.planning/STATE.md` contains literal text `Phase 13.5.2 (Decorator-only derivation surface): COMPLETED`
    - `.planning/ROADMAP.md` Phase 13.5.2 entry contains `✅ COMPLETED` (not `📋 PLANNED`)
    - `.planning/ROADMAP.md` Phase 13.5.2 entry contains a `**Closed:**` line with date + SUMMARY/VERIFICATION links
    - `pytest python/tests/v0/ --no-header -q 2>&1 | tail -1` shows passes ≥ 78
    - `cargo test --workspace --features testing` exits 0
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0
    - `cargo fmt --all --check` exits 0
    - `mypy --strict python/beava` exits 0
  </acceptance_criteria>
  <commit>
    type: docs
    scope: 13.5.2-06
    subject: closure — SUMMARY + VERIFICATION + STATE/ROADMAP advance for Phase 13.5.2
  </commit>
</task>

</tasks>

<verification>
- All acceptance_criteria for Tasks 6.a and 6.b hold
- The CLAUDE.md §TDD Discipline item #7 doc-only-plan exemption is invoked verbatim (string `"CLAUDE.md §TDD Discipline item #7"` appears in this PLAN body and in 13.5.2-SUMMARY.md so plan-checker grep deterministically finds it)
- Throughput regression-gate row landed in `.planning/throughput-baselines.md` with PASS verdict (small/tcp)
- `.planning/STATE.md` advanced; `.planning/ROADMAP.md` Phase 13.5.2 status flipped
- All workspace gates clean
- 2 commits total (one per task), both `type: docs`
</verification>

---

*Plan 13.5.2-06 — Closure plan invoking "CLAUDE.md §TDD Discipline item #7" doc-only-plan exemption. Wave 4. Depends on 05. Two docs:-typed commits.*
