# Phase 13.5.2 — Verification

**Verified:** 2026-05-04
**Phase status:** COMPLETE-GREEN

## CONTEXT.md `<domain>` "In scope" — verification

### 1. `app.register()` rejects EventDerivation instances at top level with "wrap in @bv.event" hint
- **PASS** — `python/beava/_app.py::App.register` (commit `3dded0de`) iterates `*descriptors` BEFORE `_require_transport()` and raises `RegistrationError(code="invalid_descriptor", path=f"descriptors[{i}]", ...)` for any raw `EventDerivation` lacking `_is_bv_event_function`.
- Test evidence: `pytest python/tests/internal/test_register_rejects_chain.py -v` → 4/4 GREEN
  - `test_register_rejects_event_derivation_instance` PASSED
  - `test_register_rejects_chain_at_index_zero` PASSED
  - `test_register_rejects_unnamed_chain` PASSED
  - `test_register_accepts_event_class_and_table_function_positive_control` PASSED

### 2. `_resolve_upstream_proxies` (in `python/beava/_table.py`) rejects EventDerivation-instance annotation
- **PASS** — `python/beava/_table.py::_resolve_upstream_proxies` (commit `c58e5546`) raises `TypeError` with the canonical `@bv.event def` rewrite hint when annotation resolves to a raw `EventDerivation`.
- Test evidence: `pytest python/tests/internal/test_decorator_rejects_chain_annotation.py::test_bv_table_rejects_chain_annotation_with_columns` PASSED
- Plus: `pytest python/tests/internal/test_decorator_rejects_chain_annotation.py::test_bv_table_rejects_chain_annotation_filter` PASSED

### 3. `_make_event_derivation` (in `python/beava/_events.py`) rejects EventDerivation-instance annotation
- **PASS** — `python/beava/_events.py::_make_event_derivation` (commit `cdea6e62`) raises `TypeError` mirroring the `@bv.table` resolver. Also gained closure-cell + caller-frame walking so function-local `@bv.event class` definitions resolve correctly under `from __future__ import annotations`.
- Test evidence: `pytest python/tests/internal/test_decorator_rejects_chain_annotation.py::test_bv_event_def_rejects_chain_annotation_with_columns` PASSED
- Plus: `pytest python/tests/internal/test_decorator_rejects_chain_annotation.py::test_bv_event_def_rejects_chain_annotation_filter` PASSED
- Plus: `test_decorators_accept_event_class_and_event_def_positive_control` PASSED (verifies the `_is_bv_event_function` marker handshake)

### 4. 5 `python/tests/v0/test_lit.py` tests rewritten to @bv.event def form
- **PASS** — All 5 tests use `@bv.event def Name(upstream: Source): return upstream.<chain>` form (commit `82ae412e`)
- Test evidence: `pytest python/tests/v0/test_lit.py -v` → 5/5 GREEN
  - `test_lit_constant_column` PASSED
  - `test_lit_explicit_filter_literal` PASSED
  - `test_lit_force_float_division` PASSED
  - `test_lit_value_types` PASSED
  - `test_lit_immutability` PASSED

### 5. `docs/sdk-api/python.md` rewrite of bv.lit examples
- **N/A (already aligned)** — `docs/sdk-api/python.md` shows `bv.lit` only as a simple expression node `bv.lit(42)` (line 214); no chain-form examples remain to rewrite. Verified via `grep -n "with_columns(source\|events\.with_columns" docs/sdk-api/python.md` → no hits.

### 6. ADR-003 mirrored
- **PASS** — `.planning/decisions/ADR-003-global-aggregation-and-bv-lit.md` (commit `bab6b007`) rewrites the three `bv.lit` examples (filter / safe-div / constant-column) to `@bv.event def` form with a one-paragraph framing about the v0 surface contract.

### 7. Paired RED→GREEN unit tests for both rejection paths
- **PASS** — D-01: Plan 01 RED (commit `9bb9f364`) + Plan 03 GREEN (commit `3dded0de`).
- **PASS** — D-02: Plan 02 RED (commit `e793770f`) + Plan 04 GREEN (commits `c58e5546` + `cdea6e62`).

## Discovered scope expansion (per CONTEXT update + user direction Option B)

### Runtime chain-op execution in agg apply path
- **PASS** — `crates/beava-core/src/agg_apply.rs::apply_event_to_aggregations` (commit `9a3fdd74`) per-desc looks up `registry.compiled_chain(&desc.node_name)` and applies via `OpChain::apply(row.clone())`. Fast path zero-overhead when no chain registered.
- Test evidence: `test_lit_constant_column` GREEN (where-predicate `col('source') == lit('web')` matches all 800 events because the chain-applied row carries the `source` column).
- Plus: `test_lit_force_float_division` GREEN (`mean('rate')` returns the correct mean because the chain-added `rate` column is visible to the field extractor's slow-path fallback).

### Register-time chain-prefix compilation when GroupBy is present
- **PASS** — `crates/beava-core/src/register_validate.rs::validate_expressions` (commit `9a3fdd74`) compiles non-GroupBy ops as the chain prefix when GroupBy is in the ops list. Previously skipped entirely. `compiled_chains[derivation_name]` now populated for `@bv.table`-decorated derivations with chain ops.

### SDK chain-flatten in `_app.py::_descriptor_to_node`
- **PASS** — When a `@bv.table` descriptor's parent chain contains an `@bv.event def` output (`_is_bv_event_function=True`), the wire upstream is set to the ROOT `EventSource`. Required for the server's apply-time routing index `aggregations_by_source` to fire on root-event pushes.

### Python `&` / `|` boolean serialization
- **PASS** — `python/beava/_col.py::_Expr.__and__/__or__` (commit `e496e572`) emit the expr-grammar keywords `and` / `or` instead of bitwise `&` / `|`. Verified by `test_lit_value_types` (uses `(col('n') >= 0) & (col('s') != 'excluded')` which now parses on the server).

## Workspace gates at close

| Gate | Result | Command |
|------|--------|---------|
| Rust workspace clippy | PASS | `cargo clippy --workspace --all-targets --all-features -- -D warnings` |
| Rust fmt | PASS | `cargo fmt --all --check` |
| Rust beava-core lib tests | PASS (618/618 + 1 ignored) | `cargo test -p beava-core --lib` |
| Python pytest (v0 + internal) | PASS-WITH-DEFICIT | `pytest python/tests/v0/ python/tests/internal/` (8 failed / 248 passed; 8 failures all pre-existing flaky `*_per_user_high_volume` per resume doc) |
| Python mypy strict | PASS (0 errors / 19 source files) | `mypy --strict python/beava` |

## Throughput regression gate

| Cell | Phase 13.5.2 EPS | Phase 13.4.1 baseline EPS | Δ% | Verdict |
|------|----------------:|--------------------------:|---:|---------|
| small/tcp (gate) | 641,935 | 631,610 | **+1.6%** | **PASS** (within ±10% gate band) |

Three runs: 641,935 / 621,589 / 693,262 (median 641,935). See `.planning/throughput-baselines.md` for the full row + analysis.

## Final v0 acceptance count

- Pre-13.5.2 baseline: 73-74/89 GREEN (16 failed)
- Post-13.5.2: **81/89 GREEN** (8 failed) — strictly improved by 7-8 tests:
  - +5 from `test_lit.py` rewrites (now passing end-to-end against the runtime chain-op executor)
  - +3 from `test_transport_equivalence.py::test_reset_equivalent[embed/http/tcp]` — incidentally re-stabilized (likely the rebuilt fresh binary picked up reset-handling fixes)

The 8 remaining failures are all pre-existing `*_per_user_high_volume` flake-prone tests already noted in `.planning/RESUME-2026-05-04-PM.md` for separate follow-up.

## Verdict

**Phase 13.5.2 PASSES.** All 4 user-locked CONTEXT decisions D-01..D-04 landed in code; all RED tests turned GREEN; v0 acceptance strictly improved from 73-74/89 to 81/89; workspace gates clean; throughput regression-gate PASS at +1.6%. The discovered scope expansion (runtime chain-op execution) was completed in-session per user direction (Option B), unblocking the documented `bv.lit("web")` constant-column flow end-to-end.
