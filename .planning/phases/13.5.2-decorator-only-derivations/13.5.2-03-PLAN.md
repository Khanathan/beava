---
phase: 13.5.2
plan: 03
type: execute
wave: 2
depends_on: [13.5.2-01]
files_modified:
  - python/beava/_app.py
autonomous: true
requirements:
  - D-01
tags:
  - python-sdk
  - decorator
  - tdd-green

must_haves:
  truths:
    - "Plan 13.5.2-01's 3 RED negative tests turn GREEN"
    - "Plan 13.5.2-01's 1 positive control test stays GREEN"
    - "All `python/tests/v0/` tests that pass at HEAD continue to pass (no behavior regression)"
    - "The 5 `test_lit.py` failures STILL fail (they need Plan 05's mechanical rewrite to pass; Plan 03 only adds the rejection)"
  artifacts:
    - path: "python/beava/_app.py"
      provides: "D-01 register-time rejection of EventDerivation instances"
      contains: "isinstance(d, EventDerivation)"
      contains_2: "Wrap the chain in @bv.event"
      contains_3: "code=\"invalid_descriptor\""
      min_lines: 590
  key_links:
    - from: "python/beava/_app.py::App.register"
      to: "python/tests/internal/test_register_rejects_chain.py"
      via: "GREEN impl that satisfies the RED contract from Plan 01"
      pattern: "isinstance.*EventDerivation"
---

<objective>
GREEN commit for D-01 (USER-LOCKED): add the EventDerivation-instance rejection check at the top of `App.register` in `python/beava/_app.py`. Plan 13.5.2-01's 3 RED negative tests turn GREEN; the 1 positive control stays GREEN.

Per CONTEXT D-01: BEFORE any wire serialization, iterate `descriptors` and reject any `EventDerivation` instance with `RegistrationError(code="invalid_descriptor", path=f"descriptors[{i}]", message=...)` whose body shows the canonical `@bv.event def` rewrite verbatim.

Output: ~12-line edit to `App.register` adding the guard loop + imports; commit type = `feat:` (green). No other code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.5.2-decorator-only-derivations/13.5.2-CONTEXT.md
@.planning/phases/13.5.2-decorator-only-derivations/13.5.2-01-PLAN.md
@CLAUDE.md
@python/beava/_app.py
@python/beava/_events.py
@python/beava/_errors.py
@python/tests/internal/test_register_rejects_chain.py

<interfaces>
<!-- Key context the executor needs. Extracted from codebase 2026-05-04. -->

# CURRENT App.register — python/beava/_app.py:566-586 (no rejection)
def register(
    self,
    *descriptors: Any,
    force: bool = False,
    dry_run: bool = False,
) -> dict[str, Any]:
    """Register one or more event/table descriptors with the server.

    Args:
        *descriptors: Descriptor objects produced by ``@bv.event`` /
            ``@bv.table`` decorators (Plan 03).
        force: If True, server replaces any existing pipeline of the
            same name.
        dry_run: If True, server validates and returns a categorized
            diff payload but does not commit. Implies dry_run=True
            response shape per docs/error-codes.md.
    """
    t = self._require_transport()
    payload = _to_register_json(descriptors, force=force, dry_run=dry_run)
    result: dict[str, Any] = t.send_register(payload)
    return result


# REPLACEMENT — D-01 USER-LOCKED:
def register(
    self,
    *descriptors: Any,
    force: bool = False,
    dry_run: bool = False,
) -> dict[str, Any]:
    """Register one or more event/table descriptors with the server.

    Args:
        *descriptors: Descriptor objects produced by ``@bv.event`` /
            ``@bv.table`` decorators (Plan 03).
        force: If True, server replaces any existing pipeline of the
            same name.
        dry_run: If True, server validates and returns a categorized
            diff payload but does not commit. Implies dry_run=True
            response shape per docs/error-codes.md.

    Raises:
        RegistrationError: If any descriptor is an ``EventDerivation``
            instance (a raw chain expression). Per Phase 13.5.2 D-01,
            chain expressions must be wrapped in ``@bv.event def F(...)``
            before being registered.
    """
    # Phase 13.5.2 D-01 (USER-LOCKED): reject raw EventDerivation instances
    # at register-time with a sharp client-side error pointing at the canonical
    # @bv.event def rewrite. Local imports to avoid a circular import at module
    # load time (_app ← _events ← _col).
    from beava._errors import RegistrationError
    from beava._events import EventDerivation

    for i, d in enumerate(descriptors):
        if isinstance(d, EventDerivation) and not getattr(
            d, "_is_bv_event_function", False
        ):
            raise RegistrationError(
                code="invalid_descriptor",
                path=f"descriptors[{i}]",
                message=(
                    f"argument {i} is an EventDerivation instance (a raw chain). "
                    f"Wrap the chain in @bv.event:\n"
                    f"    @bv.event\n"
                    f"    def Foo(click: Click):\n"
                    f"        return click.with_columns(...)"
                ),
                errors=[],
            )

    t = self._require_transport()
    payload = _to_register_json(descriptors, force=force, dry_run=dry_run)
    result: dict[str, Any] = t.send_register(payload)
    return result

# IMPORTANT — the `_is_bv_event_function` marker:
# An EventDerivation instance returned by `@bv.event def F(...)` is the
# legitimate output of the public function-form decorator; passing such an
# instance to `app.register()` is the canonical way to register a derivation.
# To distinguish "raw chain" (REJECT) from "@bv.event def output" (ACCEPT),
# the `event()` decorator (function form) sets `result._is_bv_event_function = True`
# on the returned EventDerivation. THIS MARKER IS LANDED IN PLAN 04, not here.
#
# Plan 03 alone (without Plan 04's marker) would over-reject @bv.event-decorated
# function outputs. The dependency chain is:
#   01 (RED tests) ← 03 (GREEN: register-time check + marker-aware)
#   02 (RED tests) ← 04 (GREEN: annotation-time check + sets the marker)
#
# Plan 03 adds the `not getattr(d, "_is_bv_event_function", False)` clause
# defensively — it's a no-op until Plan 04 lands the marker, but it's required
# for the test_register_accepts_event_class_and_table_function_positive_control
# test to keep passing once Plan 04 lands.
#
# Plan 04 will land the marker AND will mirror this check at decoration time.
# The two plans MUST land in the same merge window (Wave 2 parallel).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="green">
  <name>Task 3.a (green): Add D-01 register-time rejection in App.register</name>
  <files>python/beava/_app.py</files>
  <read_first>
    - python/beava/_app.py (lines 564-586 — current `App.register` site)
    - python/beava/_events.py (line 128 — `EventDerivation` class definition)
    - python/beava/_errors.py (RegistrationError signature: `__init__(self, code, path, message, errors)`)
    - python/tests/internal/test_register_rejects_chain.py (the 4 tests this impl must satisfy)
    - .planning/phases/13.5.2-decorator-only-derivations/13.5.2-CONTEXT.md (D-01 verbatim error message)
  </read_first>
  <action>
Edit `python/beava/_app.py` to add the D-01 rejection guard at the top of `App.register` (lines 566-586).

Use the EXACT replacement shown in `<interfaces>` above. Key elements:

1. **Local imports** — `from beava._errors import RegistrationError` and `from beava._events import EventDerivation` go INSIDE the method body (not at module top) to avoid the existing `_app ← _events ← _col` circular import at module load time. This pattern is already used elsewhere in the codebase (e.g. `python/beava/_wire.py:240` for `RegistrationError`).

2. **Marker-aware check** — `isinstance(d, EventDerivation) and not getattr(d, "_is_bv_event_function", False)`. The `_is_bv_event_function` marker is landed by Plan 04 on EventDerivation instances returned by `@bv.event def`. Plan 03 alone defends against future-Plan-04 — the check is a no-op for raw chains (no marker) but skips the legitimate `@bv.event def` output.

3. **Error shape** — `RegistrationError(code="invalid_descriptor", path=f"descriptors[{i}]", message=<multi-line hint>, errors=[])` with the EXACT message body from CONTEXT D-01. Preserve the indentation (4 spaces) inside the multi-line example block — that's the canonical rendering shape per `<specifics>` in CONTEXT.md.

4. **Docstring** — extend with a `Raises:` section noting the new contract.

DO NOT touch `_to_register_json` or `_descriptor_to_node`. The rejection is at the App boundary, not in the wire-serialization layer.
  </action>
  <verify>
GREEN proof:

1. `pytest python/tests/internal/test_register_rejects_chain.py -v 2>&1` — all 4 tests pass (3 negative + 1 positive control). The negative tests now fire the locked RegistrationError.

2. `pytest python/tests/v0/ --no-header -q 2>&1 | tail -5` — verify no NEW failures vs HEAD baseline (16 failed / 73 passed approximate; the 5 test_lit failures stay failing because Plan 05 hasn't rewritten them yet; the high-volume + reset failures are pre-existing flakes unrelated to this plan).

3. `mypy --strict python/beava 2>&1 | tail -5` — no new errors. The local imports are typed correctly because `RegistrationError` and `EventDerivation` already have full type signatures.

4. `cargo test --workspace --features testing 2>&1 | tail -5` — still green (no Rust changes).

5. `cargo clippy --workspace --all-targets --all-features -- -D warnings 2>&1 | tail -3` — still clean.

6. `cargo fmt --all --check 2>&1 | tail -3` — still clean.
  </verify>
  <acceptance_criteria>
    - File `python/beava/_app.py` `App.register` method (lines ~566-) contains literal text `isinstance(d, EventDerivation)`
    - Same method contains literal text `_is_bv_event_function`
    - Same method contains literal text `Wrap the chain in @bv.event`
    - Same method contains literal text `code="invalid_descriptor"`
    - Same method contains literal text `path=f"descriptors[{i}]"`
    - Local imports `from beava._errors import RegistrationError` and `from beava._events import EventDerivation` appear INSIDE the method body (not at module top)
    - `pytest python/tests/internal/test_register_rejects_chain.py` exits 0 (all 4 tests pass)
    - `pytest python/tests/v0/` failure count is ≤ HEAD-baseline failure count (no new regressions; the 5 test_lit failures still fail per design — Plan 05 fixes them)
    - `mypy --strict python/beava` exits 0
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0
    - `cargo fmt --all --check` exits 0
  </acceptance_criteria>
  <commit>
    type: feat
    scope: 13.5.2-03
    subject: GREEN — App.register rejects EventDerivation instances per D-01
  </commit>
</task>

</tasks>

<verification>
- All acceptance_criteria for Task 3.a hold
- Plan 01's 3 negative tests transition RED→GREEN; the 1 positive control stays GREEN
- No new failures in `python/tests/v0/` vs HEAD baseline
- Workspace gates green: `cargo test`, `cargo clippy`, `cargo fmt`, `pytest python/tests`, `mypy --strict python/beava`
</verification>

---

*Plan 13.5.2-03 — GREEN for D-01 (App.register rejects EventDerivation instances). Wave 2 (parallel with 04). Depends on 01. Single feat:-typed commit. Marker `_is_bv_event_function` is landed by Plan 04; Plan 03 defensively checks for it.*
