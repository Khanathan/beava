---
phase: 60-hotkey-mitigation-via-application-salting
plan: 04
type: execute
wave: 5
depends_on: [60-03]
files_modified:
  - src/shard/metrics.rs
  - src/server/shard_probe.rs
  - benches/pareto_workload.rs
  - scripts/verify-salt-feature-complete.sh
  - tests/hot_key_salting.rs
  - .planning/phases/60-hotkey-mitigation-via-application-salting/60-PERF-GATE.md
  - .planning/phases/60-hotkey-mitigation-via-application-salting/60-VERIFICATION.md
autonomous: false
requirements:
  - TPC-PERF-10
must_haves:
  truths:
    - "Metric `beava_shard_hot_key_owner_ratio{shard}` is emitted via `GET /metrics` with a non-zero value on the hot shard after a Zipf-1.2 workload."
    - "Metric `beava_salt_fanout_reads_total` + `beava_salt_ingest_writes_total` both registered + labeled."
    - "`GET /debug/shards.salted_streams` enumerates salted streams and their cardinality."
    - "Pareto-80/20 perf gate: salted aggregate EPS ≥ 1.5× unsalted baseline on the same harness."
    - "`/debug/shards` polled every 5s during the 60s salted run: every shard's `inbox_depth ≤ 0.5 × BEAVA_SHARD_INBOX_SIZE` for ≥ 5 consecutive samples."
    - "`scripts/verify-salt-feature-complete.sh` exits 0."
    - "60-PERF-GATE.md + 60-VERIFICATION.md committed."
  artifacts:
    - path: "src/shard/metrics.rs"
      provides: "3 new metric registrations"
      contains: "beava_shard_hot_key_owner_ratio"
    - path: "src/server/shard_probe.rs"
      provides: "salted_streams field on ShardDiagnosticsReport"
      contains: "salted_streams"
    - path: "benches/pareto_workload.rs"
      provides: "pareto_salted_c8_x8 real assertion (was stub from 60-00)"
      contains: "pareto_salted_c8_x8"
    - path: ".planning/phases/60-hotkey-mitigation-via-application-salting/60-PERF-GATE.md"
      provides: "Perf-gate evidence file"
    - path: ".planning/phases/60-hotkey-mitigation-via-application-salting/60-VERIFICATION.md"
      provides: "SC-1..SC-5 verification record"
  key_links:
    - from: "src/shard/metrics.rs"
      to: "GET /metrics Prometheus emission"
      via: "metrics-exporter-prometheus re-exports the 3 new metrics"
      pattern: "beava_shard_hot_key_owner_ratio|beava_salt_fanout_reads_total|beava_salt_ingest_writes_total"
    - from: "benches/pareto_workload.rs"
      to: "60-PERF-GATE.md"
      via: "captured EPS number from salted vs unsalted run"
      pattern: "pareto_salted_c8_x8"
---

<objective>
Close Phase 60: metrics surface, Pareto perf gate (+50%), `/debug/shards`
extension, human-verify checkpoint, and the phase's two evidence files
(60-PERF-GATE.md + 60-VERIFICATION.md).

Purpose: Prove SC-3 (perf +50%), SC-5 (hot-key owner-ratio metric), SC-4
(uniform workload unchanged) with reproducible evidence; flip the grep-ZERO
ship-gate to exit 0.

Output: 5 `src/` + bench file changes; 2 `.planning/` evidence files; one
human-verify checkpoint on perf interpretation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.planning/phases/59-binary-wire-format-for-push/59-PERF-GATE.md
@.planning/phases/59-binary-wire-format-for-push/59-VERIFICATION.md
</execution_context>

<context>
@.planning/phases/60-hotkey-mitigation-via-application-salting/60-CONTEXT.md
@.planning/phases/60-hotkey-mitigation-via-application-salting/60-03-PLAN.md
@src/shard/metrics.rs
@src/server/shard_probe.rs
@benches/pareto_workload.rs

<interfaces>
From src/shard/metrics.rs (existing — register new metrics here):
```rust
// Pattern used by Phase 50-02:
// register_counter!("beava_shard_events_total", "shard" => shard_id);
// Pre-register on boot to avoid per-event Registry::get_or_create_counter
// (Phase 61 does the global hoist; Phase 60 uses the same pattern for new metrics).
```

From src/server/shard_probe.rs::ShardDiagnosticsReport (existing — extend):
```rust
pub struct ShardDiagnosticsReport {
    pub shards: Vec<ShardInfo>,
    pub hot_shards: Vec<usize>,
    pub fleet_mean_keys_owned: f64,
    pub hot_shard_threshold: f64,
    // NEW (Wave 4):
    pub salted_streams: Vec<SaltedStreamInfo>,
}

pub struct SaltedStreamInfo {
    pub name: String,
    pub salt_cardinality: u8,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Metrics registration + /debug/shards extension + bench wire + grep-gate flip</name>
  <files>src/shard/metrics.rs, src/server/shard_probe.rs, benches/pareto_workload.rs, scripts/verify-salt-feature-complete.sh, tests/hot_key_salting.rs</files>
  <action>
  In `src/shard/metrics.rs`:

  1. At module init / shard-thread-spawn time, pre-register 3 new metrics
     using the existing pattern:
     ```rust
     metrics::describe_counter!(
         "beava_salt_fanout_reads_total",
         "Number of salt-variant scatter-reads issued (per stream, per salt cardinality)"
     );
     metrics::describe_counter!(
         "beava_salt_ingest_writes_total",
         "Number of salted PUSHes routed through salt suffix derivation"
     );
     metrics::describe_gauge!(
         "beava_shard_hot_key_owner_ratio",
         "Fraction of per-shard events attributable to the top-1% of keys observed over a rolling 60s window"
     );
     ```
  2. Add the hot-key sampler: per-shard `Mutex<LruCache<String, u64>>` sized
     at `BEAVA_HOT_KEY_SAMPLE_SIZE` (env, default 1024). On each `process_shard_event`,
     bump the entry for the incoming event's primary key. Every 60s, recompute
     top-1% concentration on each shard and publish to
     `beava_shard_hot_key_owner_ratio{shard}` gauge. Use a simple `Instant` +
     counter ring-buffer reset — no new async infrastructure.

  In `src/server/shard_probe.rs`:

  3. Add `SaltedStreamInfo` struct per `<interfaces>`. Extend
     `ShardDiagnosticsReport.salted_streams: Vec<SaltedStreamInfo>`. Populate
     from the engine's registered streams iter where
     `salt_cardinality.is_some()`.

  In `benches/pareto_workload.rs`:

  4. Replace the `#[cfg(any())]`-disabled `pareto_salted_c8_x8` stub (from
     60-00) with the real implementation:
     - Same Zipf-1.0 sampler as `pareto_c8_x8`.
     - Register the `Txns` stream with `shard_key="user_id:salt(16)"`.
     - Run both the unsalted baseline and the salted variant in the same
       `criterion_group!`; Criterion's built-in comparison captures the
       +50% assertion.
     - Remove the `unimplemented!()` test stub `salted_aggregate_eps_exceeds_unsalted_by_50pct`;
       replace with a real assertion that loads the Criterion output JSON
       and asserts ratio ≥ 1.5.

  In `scripts/verify-salt-feature-complete.sh`:

  5. The script is already correct from 60-00. Just re-verify it now
     exits 0. If not, fix any missing grep targets.

  In `tests/hot_key_salting.rs`:

  6. Flip `#[ignore = "60-W4"]` on `beava_shard_hot_key_owner_ratio_emits`.
     Real assertion: boot N=8, PUSH 10K Zipf-1.2 events, `GET /metrics`,
     parse Prometheus text format, assert the shard-0 gauge is > 0.5
     (hot shard has >50% top-1% concentration).

  Per 60-CONTEXT.md D-E1, D-E2, D-E3, D-E4, D-F1.
  </action>
  <verify>
    <automated>cargo test --release --lib metrics salt 2>&1 | tail -10 ; cargo test --release --test hot_key_salting beava_shard_hot_key 2>&1 | tail -10 ; bash scripts/verify-salt-feature-complete.sh ; test $? -eq 0</automated>
  </verify>
  <done>
    - 3 new metrics registered + emitted via `GET /metrics`.
    - `ShardDiagnosticsReport.salted_streams` populated.
    - `pareto_salted_c8_x8` Criterion bench runs and produces a comparable number.
    - `scripts/verify-salt-feature-complete.sh` exits 0.
    - All W4-tagged ignore markers removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run Pareto perf gate + write 60-PERF-GATE.md + 60-VERIFICATION.md</name>
  <files>.planning/phases/60-hotkey-mitigation-via-application-salting/60-PERF-GATE.md, .planning/phases/60-hotkey-mitigation-via-application-salting/60-VERIFICATION.md</files>
  <action>
  Run the following in sequence, capturing output:

  1. Criterion bench (A/B measurement):
     ```bash
     cargo bench --bench pareto_workload -- --sample-size 30
     ```
     Capture: baseline (unsalted) EPS + salted EPS + ratio. Target ≥ 1.50.

  2. End-to-end perf harness (3 × 60s runs, best-of-3 per Phase 59 convention):
     ```bash
     MODE=complex DURATION=60 CPUS=8 CLIENTS=8 \
       BEAVA_SHARD_INBOX_SIZE=1048576 BEAVA_MAX_CONNS_PER_SHARD=1024 \
       bash benchmark/fraud-pipeline/run_bench.sh
     ```
     Run once with NO salt (baseline, reproduces Phase 59 number), then
     once with a salted variant of the fraud pipeline (Transactions stream
     declares `salt(16)`). Target: salted ≥ +50% of baseline under Pareto
     workload. Record EPS numbers.

  3. `/debug/shards` polling during the salted run: background poll every 5s,
     log `inbox_depth` per shard. Assert: for ≥ 5 consecutive samples after
     steady-state (first 10s ramp excluded), every shard's `inbox_depth ≤ 524288`
     (half of 1048576). Record violation count.

  4. Uniform-workload regression: run `MODE=complex` with NO salt declared
     on any stream. Assert within ±2% of Phase 59 baseline (1,494,631 EPS).
     Record EPS delta.

  Write `60-PERF-GATE.md` with: cells run, EPS numbers, ratio, inbox-depth
  polling log, contingency-ladder decision (C0/C1/C2/C3). Follow the Phase 59
  format verbatim (cells table + ratio + decision + contingency notes).

  Write `60-VERIFICATION.md` with: SC-1 through SC-5 status, each with
  evidence pointer:
  - SC-1: SDK supports `@bv.stream(shard_key="user_id:salt(16)")` — PASSED
    (Wave 1 Python unit tests GREEN).
  - SC-2: Reads scatter-gather — PASSED (Wave 3 tests GREEN).
  - SC-3: Perf gate ≥ +50% EPS + inbox_depth ≤ 50% — status from Task 2
    run (PASSED / human_needed / C3 escalation).
  - SC-4: No regression on uniform workload — PASSED / delta captured.
  - SC-5: `beava_shard_hot_key_owner_ratio` emits — PASSED (Wave 4 test GREEN).

  If SC-3 misses the 50% floor, follow the D-F5 contingency ladder:
  - C1: re-run with salt(64); if passes, commit the recommendation, mark
    SC-3 PASSED with the updated default.
  - C2: double-salting (`salt(16):m(4)` micro-salt extension — requires
    additional Wave 4 spike; mark `needs_spike`).
  - C3: `human_needed` — document the gap, propose Phase 61 integration
    (metrics hoist may free enough CPU).
  </action>
  <verify>
    <automated>test -f .planning/phases/60-hotkey-mitigation-via-application-salting/60-PERF-GATE.md && test -f .planning/phases/60-hotkey-mitigation-via-application-salting/60-VERIFICATION.md && grep -E "SC-[1-5]" .planning/phases/60-hotkey-mitigation-via-application-salting/60-VERIFICATION.md | wc -l | awk '$1>=5 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    - `60-PERF-GATE.md` captures all 4 measurements (Criterion A/B, end-to-end
      salted EPS, inbox-depth polling, uniform-regression delta).
    - `60-VERIFICATION.md` records SC-1..SC-5 each with evidence.
    - Contingency ladder decision documented if SC-3 misses.
    - Both files use the Phase 59 format verbatim.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — Perf-gate interpretation + close</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>Pause for user review. Present 60-PERF-GATE.md + 60-VERIFICATION.md; await approval or contingency decision per `<resume-signal>`. On approval, commit STATE.md + ROADMAP.md updates closing Phase 60.</action>
  <what-built>
  Phase 60 engineering complete. Structural change:
  - Users can declare `@bv.stream(shard_key="user_id:salt(N)")` for N ∈
    {2, 4, 8, 16, 32, 64, 128, 256}.
  - Ingest-side derives `"key:salt_idx"` via `ahash(primary_event_id) % N`,
    spreading hot keys across N virtual sub-shards.
  - Read-side scatter-gathers N salt variants, combines via commutative
    semantics.
  - Source tables + mixed-salt joins are rejected / warned at register time.
  - `/debug/shards.salted_streams` enumerates salted streams; 3 new metrics
    surface hot-key concentration + salt fan-out rates.
  - Pareto-80/20 perf gate measurement captured in 60-PERF-GATE.md.
  </what-built>
  <how-to-verify>
  1. Read `.planning/phases/60-hotkey-mitigation-via-application-salting/60-PERF-GATE.md`.
     Confirm the salted aggregate EPS is ≥ 1.5× the unsalted baseline on
     the same hardware. If this cell passed, SC-3 is PASSED. If it missed
     within the documented variance window (Phase 59 precedent uses ±6%),
     either accept the measurement and flag `human_needed` for a
     Linux-prod-host re-run, OR approve the C1 contingency (salt(64) or
     equivalent).

  2. Inspect `60-VERIFICATION.md`. Confirm SC-1..SC-5 each have
     PASSED / human_needed status with evidence pointers.

  3. Confirm the uniform-workload regression cell is within ±2% of the
     Phase 59 baseline — this is the "opt-in zero overhead" contract.

  4. Smoke run: `cargo test --release` on the full suite; confirm lib test
     count ≥ Phase 59 close (825/0/35) and no regressions.

  5. Confirm `scripts/verify-salt-feature-complete.sh` exits 0.

  6. If approved → respond "approved" and the phase closes with a close
     commit: updates STATE.md + ROADMAP.md Phase 60 status.
  If C1/C3 needed → respond "C1: accept salt(64) as default" OR
     "C3: human_needed Linux prod-host re-run" to trigger the appropriate
     follow-up.
  </how-to-verify>
  <resume-signal>Type "approved" OR "C1" OR "C3 human_needed" with rationale</resume-signal>
  <verify>
    <automated>test -f .planning/phases/60-hotkey-mitigation-via-application-salting/60-PERF-GATE.md && test -f .planning/phases/60-hotkey-mitigation-via-application-salting/60-VERIFICATION.md</automated>
  </verify>
  <done>User typed "approved" (or equivalent contingency disposition); Phase 60 close commit landed; STATE.md + ROADMAP.md updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bench harness → captured EPS numbers | Reproducibility requires committing harness exactly (env vars + seed) |
| `/metrics` Prometheus scrape → operator | New labels expose stream names (no secrets) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04-01 | Repudiation | Perf-gate EPS number | mitigate | 60-PERF-GATE.md records full env + bench command + 3-run best-of-3 methodology |
| T-60-04-02 | DoS | `BEAVA_HOT_KEY_SAMPLE_SIZE` unbounded | mitigate | Default 1024; clamp [128, 65536] in env parse |
| T-60-04-03 | Information Disclosure | `salted_streams` in /debug/shards | accept | Stream names already publicly enumerable via list_streams |
</threat_model>

<verification>
- All 3 new metrics enumerable via `GET /metrics`.
- `GET /debug/shards` returns `salted_streams` array.
- `benches/pareto_workload.rs::pareto_salted_c8_x8` Criterion run produces
  a concrete EPS number ≥ 1.5× unsalted baseline.
- 60-PERF-GATE.md + 60-VERIFICATION.md committed.
- `scripts/verify-salt-feature-complete.sh` exits 0.
- Full test suite GREEN (count ≥ Phase 59 close baseline).
- Human verify approval received.
</verification>

<success_criteria>
- SC-1 (SDK surface) PASSED.
- SC-2 (reads scatter-gather) PASSED.
- SC-3 (perf +50% + inbox_depth ≤ 50%) PASSED / `human_needed` with
  contingency documented.
- SC-4 (uniform-workload ±2%) PASSED.
- SC-5 (hot-key owner ratio metric) PASSED.
- Phase 60 close commit: STATE.md + ROADMAP.md updated; TPC-PERF-10 marked
  closed (or re-opened pending contingency).
</success_criteria>

<output>
After completion, create `.planning/phases/60-hotkey-mitigation-via-application-salting/60-04-SUMMARY.md`
plus a Phase 60 close commit updating STATE.md (`progress.completed_phases +=
1`, `phase: 61`, accumulated-context entry) and ROADMAP.md (Phase 60 status
row).
</output>
