---
phase: 60-hotkey-mitigation-via-application-salting
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/hot_key_salting.rs
  - tests/salted_stream_register_warning.rs
  - scripts/verify-salt-feature-complete.sh
  - benches/pareto_workload.rs
autonomous: true
requirements:
  - TPC-PERF-10
must_haves:
  truths:
    - "REQUIREMENTS.md contains a TPC-PERF-10 row with the Phase 60 contract."
    - "Four RED integration tests exist, each tagged `#[ignore = \"60-W[1-4]\"]` until its wave lands."
    - "`scripts/verify-salt-feature-complete.sh` exists, is executable, and exits 1 before Wave 2 wires ingest."
    - "`benches/pareto_workload.rs` has a placeholder `pareto_salted_smoke` test group behind an `#[ignore]` that Wave 4 flips."
  artifacts:
    - path: "tests/hot_key_salting.rs"
      provides: "SC-1 + SC-2 + SC-4 RED scaffolding"
      min_lines: 80
    - path: "tests/salted_stream_register_warning.rs"
      provides: "register-time parse/reject RED scaffolding"
      min_lines: 60
    - path: "scripts/verify-salt-feature-complete.sh"
      provides: "grep-gate ship script"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TPC-PERF-10 row"
      contains: "TPC-PERF-10"
  key_links:
    - from: "tests/hot_key_salting.rs"
      to: "src/engine/join_validator.rs::parse_shard_key_with_salt"
      via: "direct call — will compile-error until Wave 1"
      pattern: "parse_shard_key_with_salt"
---

<objective>
Land the Wave 0 foundation: the TPC-PERF-10 requirement row, four RED integration
tests (tagged `#[ignore = "60-W[1-4]"]`), the grep-ZERO ship-gate script, and a
placeholder salted-Pareto bench group. Nothing here flips GREEN; the tests are
the contract every later wave targets.

Purpose: Goal-backward anchor. Every SC in ROADMAP § Phase 60 maps to a RED test
whose `#[ignore]` marker names the wave that will flip it. Downstream waves
cannot skip or reinterpret these tests.

Output: `.planning/REQUIREMENTS.md` row for TPC-PERF-10; 2 RED test files; 1
ship-gate script; 1 placeholder bench group.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-hotkey-mitigation-via-application-salting/60-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-binary-wire-format-for-push/59-00-PLAN.md
@.planning/phases/57-retraction-across-crossshard-joins/57-00-PLAN.md

<interfaces>
<!-- Existing call-surface the RED tests will target (will compile-fail until Wave 1). -->

From src/engine/join_validator.rs (existing — Phase 60 extends):
```rust
// existing
pub enum ShardKeySpec { Single(String), Tuple(Vec<String>) }

// NEW (Wave 1) — RED tests call this today expecting compile error
pub fn parse_shard_key_with_salt(
    raw: &str,
) -> Result<(ShardKeySpec, Option<u8>), ShardKeyParseError>;
```

From src/engine/pipeline.rs (existing StreamDefinition — Phase 60 adds field):
```rust
pub struct StreamDefinition {
    // ... existing fields ...
    pub shard_key: Option<ShardKeySpec>,
    // NEW (Wave 1):
    pub salt_cardinality: Option<u8>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TPC-PERF-10 row + traceability entry to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
  Insert a new section **after** the existing Phase 59 block (line 83 ends the
  TPC-PERF-09 row, before `## Future Requirements`), matching the prose style of
  TPC-PERF-09:

  ```markdown
  ### TPC-PERF (continued) — Phase 60

  - [ ] **TPC-PERF-10**: A user declaring `shard_key="user_id:salt(N)"` on
    `@bv.stream` (single-key or tuple — at most one tuple element may carry
    `:salt(N)`) triggers ingest-side salt suffixing (derived key =
    `"<key>:<salt_idx>"`, salt_idx = `ahash(primary_event_id) % N`) and
    read-side N-way scatter-gather across salt variants. `salt_cardinality`
    MUST be a power of 2 in `[2, 256]`. Source tables (`@bv.source_table`)
    and streams with non-commutative operators CANNOT declare salt (register
    rejects). Observable gates: (a) `benches/pareto_workload.rs::pareto_salted_c8_x8`
    asserts salted aggregate EPS ≥ 1.5× unsalted baseline; (b) under the
    Pareto-80/20 perf-run (`MODE=complex DURATION=60 CPUS=8 CLIENTS=8`
    with a fraud-pipeline variant declaring `salt(16)`), every shard's
    `/debug/shards.inbox_depth` stays ≤ 0.5 × `BEAVA_SHARD_INBOX_SIZE` in
    steady state for ≥ 5 consecutive 5-second polls; (c) the standard
    9-cell matrix with NO salted streams is within ±2% of the Phase 59
    baseline (opt-in zero-overhead); (d) `beava_shard_hot_key_owner_ratio`
    gauge emits per-shard top-1% concentration on a rolling 60s window;
    (e) `scripts/verify-salt-feature-complete.sh` exits 0. Verified by
    `tests/hot_key_salting.rs`, `tests/salted_stream_register_warning.rs`,
    extended `tests/sharding_parity.rs` + `tests/cross_shard_enrich_from_table.rs`,
    and `benches/pareto_workload.rs`. (Phase 60)
  ```

  Then append to the Traceability table (line 124) a new row:

  | 60 | hotkey-mitigation-via-application-salting | TPC-PERF-10 |

  Finally update the Coverage footer: **38/38 → 39/39 requirements mapped**
  (add `+ 1 Phase-60`).

  Per D-A1..D-G4 in 60-CONTEXT.md.
  </action>
  <verify>
    <automated>grep -c "TPC-PERF-10" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>TPC-PERF-10 row present; Traceability row present; Coverage footer updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED integration tests — hot-key salting behavioral envelope</name>
  <files>
  tests/hot_key_salting.rs,
  tests/salted_stream_register_warning.rs,
  benches/pareto_workload.rs,
  scripts/verify-salt-feature-complete.sh
  </files>
  <behavior>
  Test file `tests/hot_key_salting.rs` (SC-1, SC-2, SC-4):
  - `parse_salt_suffix_valid_power_of_2` — `parse_shard_key_with_salt("user_id:salt(16)")`
    returns `Ok((Single("user_id"), Some(16)))`. `#[ignore = "60-W1"]`
  - `parse_salt_suffix_rejects_non_power_of_2` — `salt(10)` errors with
    message containing "power of 2". `#[ignore = "60-W1"]`
  - `parse_salt_suffix_rejects_out_of_range` — `salt(512)` errors with
    message containing "[2, 256]". `#[ignore = "60-W1"]`
  - `ingest_salted_stream_spreads_across_shards` — at N=8, PUSHing 1000 events
    all with `user_id="hot"` into a stream declaring `shard_key="user_id:salt(16)"`
    produces events on AT LEAST 8 distinct shards (each salt index → distinct
    routing hash). `#[ignore = "60-W2"]`
  - `unsalted_stream_has_zero_overhead` — a stream with `salt_cardinality=None`
    routes ONE event through `shard_hint_for_event` exactly once (no fan-out).
    `#[ignore = "60-W2"]`
  - `read_scatter_gathers_across_salts` — `EnrichFromTable` right-side lookup
    against a salted table returns the populated row regardless of which salt
    variant the driving event's key landed on. `#[ignore = "60-W3"]`
  - `salted_fan_out_metric_increments` — `beava_salt_fanout_reads_total{stream,salt_cardinality}`
    increments by 1 per PUSH to a salted stream's read path. `#[ignore = "60-W3"]`
  - `beava_shard_hot_key_owner_ratio_emits` — after 10K writes with Zipf-1.2
    key distribution, `GET /metrics` surfaces `beava_shard_hot_key_owner_ratio`
    gauge with a non-zero value on the hot shard. `#[ignore = "60-W4"]`

  Test file `tests/salted_stream_register_warning.rs` (register-time):
  - `salted_source_table_rejected` — `@bv.source_table` with salt errors at
    `register()` citing D-D3. `#[ignore = "60-W1"]`
  - `salted_tuple_at_most_one_element` — `shard_key=("a:salt(4)", "b:salt(8)")`
    errors citing D-A5. `#[ignore = "60-W1"]`
  - `salted_join_emits_warning_not_reject` — registering a join with one side
    salted and other side not produces `CrossShardJoinWarning` via
    `/debug/warnings`; pipeline starts successfully (D-D2). `#[ignore = "60-W1"]`
  - `colon_in_key_rejects_salt_declaration` — register-time sample-event
    validation errors when `key_field` resolves a value containing `:` and
    salt is declared (D-G1). `#[ignore = "60-W2"]`

  `benches/pareto_workload.rs`:
  - Add a NEW `criterion_group!` named `pareto_salted_c8_x8` alongside the
    existing `pareto_c8_x8`. Group body is a `#[cfg(any())]`-disabled stub
    today (does nothing). Wave 4 replaces the stub with a real comparison run.
    Add a unit-level test `#[ignore = "60-W4"] fn salted_aggregate_eps_exceeds_unsalted_by_50pct()`
    that panics unimplemented — Wave 4 wires the real assertion.

  `scripts/verify-salt-feature-complete.sh`:
  - Bash script that exits 1 today, 0 after Wave 4.
  - Asserts `grep -c "parse_shard_key_with_salt" src/engine/join_validator.rs` ≥ 1.
  - Asserts `grep -c "salt_cardinality" src/engine/pipeline.rs` ≥ 3 (StreamDefinition
    field + at least 2 call sites in `derive_shard_idx`).
  - Asserts `grep -c "shard_hint_for_event_salted\|salt_cardinality" src/routing/shard_hint.rs` ≥ 1.
  - Asserts `grep -c "salted_streams" src/server/shard_probe.rs` ≥ 1.
  - Asserts `grep -c "beava_shard_hot_key_owner_ratio\|beava_salt_fanout_reads_total\|beava_salt_ingest_writes_total" src/shard/metrics.rs` ≥ 3.
  - Asserts `grep -c "pareto_salted_c8_x8" benches/pareto_workload.rs` ≥ 1.
  - `chmod +x` after write.
  </behavior>
  <action>
  Write the two test files, the bench stub, and the verify script exactly as
  described in `<behavior>`. All tests compile against the existing crate
  (some will require `parse_shard_key_with_salt` and similar — those tests
  use `#[ignore]` plus a placeholder body that only touches the symbol
  inside the ignored body, so the file compiles today via existing symbols
  or inside a `#[cfg(never)]` block if necessary).

  Concrete compile-today pattern for symbols that don't exist yet: wrap the
  body in `#[allow(dead_code)] #[ignore = "60-W1"] #[test] fn foo() { unimplemented!("60-W1: parse_shard_key_with_salt"); }`.
  The ignored test's body can be a bare `unimplemented!()` — downstream waves
  replace the body with the real assertion.

  Register the tests in the usual `tests/` cargo convention (file per binary
  target; each `#[test]` fn gets picked up).

  Per 60-CONTEXT.md §decisions D-A1..D-G4 and `<behavior>` above.
  </action>
  <verify>
    <automated>cargo test --release --test hot_key_salting --test salted_stream_register_warning -- --list 2>&1 | grep -cE "^(salted_|parse_|ingest_|unsalted_|read_|salted|beava_shard_hot_key_owner_ratio|colon_in_key)" ; bash scripts/verify-salt-feature-complete.sh ; test $? -eq 1</automated>
  </verify>
  <done>
    - Both test files compile with `cargo build --tests --release`.
    - All tests present in `cargo test -- --list`; all currently-ignored ones
      list with `#[ignore = "60-W[1-4]"]`.
    - `bash scripts/verify-salt-feature-complete.sh` exits 1 (grep targets
      absent pre-Wave 1).
    - `benches/pareto_workload.rs` still compiles; new group name `pareto_salted_c8_x8`
      grep-able.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 0 output → downstream waves | RED test contract; later waves flip ignores |
| REQUIREMENTS.md row → Wave 4 verification | TPC-PERF-10 observable gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-00-01 | Tampering | `scripts/verify-salt-feature-complete.sh` | mitigate | Script is grep-only, no network / file-mutation; checked into version control |
| T-60-00-02 | Repudiation | TPC-PERF-10 row prose | mitigate | Matches format of TPC-PERF-09 including verification file list (grep-testable) |
</threat_model>

<verification>
- `.planning/REQUIREMENTS.md` contains `TPC-PERF-10` row + traceability row + updated coverage count.
- `cargo test --release --test hot_key_salting --test salted_stream_register_warning -- --list` enumerates ≥ 10 ignored tests with `60-W[1-4]` tag.
- `bash scripts/verify-salt-feature-complete.sh; echo $?` prints `1`.
- `cargo build --release --benches` passes (pareto_workload.rs still compiles).
</verification>

<success_criteria>
- TPC-PERF-10 requirement row committed.
- 12+ ignored tests scaffolded (naming uses `60-W[1-4]` wave markers).
- Grep-ZERO ship-gate script present and exits 1 pre-Wave-1.
- `benches/pareto_workload.rs` has a `pareto_salted_c8_x8` placeholder group.
</success_criteria>

<output>
After completion, create `.planning/phases/60-hotkey-mitigation-via-application-salting/60-00-SUMMARY.md`.
</output>
