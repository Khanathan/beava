---
phase: 56
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/cross_shard_enrich_from_table.rs
  - tests/cross_shard_stream_stream_join.rs
  - tests/register_crossshard_join_warning.rs
  - tests/sharding_parity.rs
  - tests/crossshard_enrich_perf_smoke.rs
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - TPC-CORR-04
  - TPC-CORR-08
  - TPC-CORR-09
tags:
  - wave-0
  - red-tests
  - phase-56
  - tdd
  - requirements
must_haves:
  truths:
    - "5 RED integration tests exist, committed, and #[ignore = \"56-W{N}\"]'d with a comment 'passes at Wave N' matching each SC"
    - "sharding_parity.rs gains a proptest generator that produces mismatched-shard EnrichFromTable + SSJ scenarios; new cases currently fail or are #[ignore = \"56-W2|56-W3\"]'d pending Waves 2/3"
    - "REQUIREMENTS.md contains TPC-CORR-04 (relaxed text), TPC-CORR-08, TPC-CORR-09 rows"
    - "Every SC (1–5) has a dedicated RED test with acceptance criteria grep-verifiable against this PLAN"
    - "No production code (src/**) modified in this wave — tests-only + docs"
  artifacts:
    - path: tests/cross_shard_enrich_from_table.rs
      provides: "SC-1 RED test — Txn on shard-J + Country source-table row on shard-K (J≠K); asserts enrichment populated"
      contains: "fn enrich_from_table_crosses_shard_boundary"
      min_lines: 80
    - path: tests/cross_shard_stream_stream_join.rs
      provides: "SC-2 RED test — LeftStream(shard_key=user_id) × RightStream(shard_key=session_id) on user_id; asserts join output on hash(user_id)%N shard"
      contains: "fn stream_stream_join_routes_to_join_key_shard"
      min_lines: 80
    - path: tests/register_crossshard_join_warning.rs
      provides: "SC-3 RED test — register() succeeds + CrossShardJoinWarning captured via tracing subscriber; /debug/warnings includes cross_shard_joins"
      contains: "fn register_emits_crossshard_warning_not_error"
      min_lines: 60
    - path: tests/crossshard_enrich_perf_smoke.rs
      provides: "SC-4 RED test — synthetic 10K-event latency sample asserting p99 ≤ 2× baseline (captured constant); marked ignore until Wave 4"
      contains: "fn crossshard_enrich_p99_under_2x_baseline"
      min_lines: 50
    - path: tests/sharding_parity.rs
      provides: "Extended with MismatchedShardEnrichOrJoin proptest scenario; new generator produces EnrichFromTable + SSJ mismatched-shard cases"
      contains: "mismatched_shard_enrich_or_join"
    - path: .planning/REQUIREMENTS.md
      provides: "TPC-CORR-04 relaxed text + TPC-CORR-08 + TPC-CORR-09 rows + Phase 56 traceability row"
      contains: "TPC-CORR-08"
  key_links:
    - from: tests/cross_shard_enrich_from_table.rs
      to: src/engine/operators.rs (EnrichFromTable)
      via: "Test instantiates 4-shard engine, registers Txns stream + Countries source-table; pushes Txn on shard with hash(user_id); asserts Country fields populated in output"
      pattern: "hash_to_shard\\(\"country_code_\\w+\"\\)"
    - from: tests/cross_shard_stream_stream_join.rs
      to: src/engine/pipeline.rs (StreamStreamJoin eval)
      via: "Test pushes L then R with on='user_id'; asserts matched output visible via get_features_on_shard on hash(user_id)%N shard"
      pattern: "StreamStreamJoin"
    - from: tests/register_crossshard_join_warning.rs
      to: src/engine/register.rs (validate_shard_keys)
      via: "Test registers join with mismatched shard_key; asserts Ok(_) + tracing capture has CrossShardJoinWarning"
      pattern: "CrossShardJoinWarning"
user_setup: []
---

<objective>
Land the RED-first TDD baseline for Phase 56. Every success criterion from ROADMAP § Phase 56 gets a failing (or ignored-pending-wave) integration test committed to `tests/` before any production code changes. Follows the Phase 54/55 Wave-0 rhythm exactly.

Purpose: make the entire phase executable as GREEN transitions on predefined acceptance checks. Once Waves 1–4 flip the `#[ignore = "56-W{N}"]` markers off, the checker / verifier can run `cargo test --release` and observe end-to-end correctness without subjective judgment.

Output:
- 4 new integration test files + 1 extension to `tests/sharding_parity.rs` (proptest generator)
- Updated REQUIREMENTS.md: TPC-CORR-04 relaxation paragraph + TPC-CORR-08 + TPC-CORR-09 rows + Phase 56 traceability entry (already written by planner — verify intact at plan start)
- Zero production code touched (`src/**` unchanged)
- No implementation allowed; tests may reference APIs (`ShardOp::ReadEntityAt`, `ShardOp::ReadEntityBatch`, `ShardOp::SsjInsert`) that do not yet exist — gated behind `#[ignore]` markers until the wave that adds them flips the marker off. Compile errors in `#[ignore]`'d bodies are not acceptable — use string-references-only / feature-gated stubs where needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-CONTEXT.md
@.planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-00-PLAN.md
@.planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-00-SUMMARY.md
@.planning/phases/54-legacy-engine-removal/54-00-baseline-and-scaffolding-PLAN.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@tests/cross_shard_tt_cascade_ownership.rs
@tests/cross_shard_backpressure.rs
@tests/test_join_stream_stream.rs
@tests/test_debug_warnings_endpoint.rs
@tests/sharding_parity.rs

<interfaces>
<!-- Extracted from src/ — executor should not need to re-explore -->

From src/engine/join_validator.rs (Phase 51 — CURRENT behavior, to be relaxed in Wave 3):

```rust
pub struct JoinShardKeyMismatch {
    pub stream_a: String,
    pub stream_b: String,
    pub key_a: String,
    pub key_b: String,
    pub suggested_common: String,
    pub message: String,
}

// Called from PipelineEngine::register() at src/engine/pipeline.rs:949.
pub fn validate_shard_keys(
    streams: &AHashMap<String, StreamDefinition>,
    new_stream: &StreamDefinition,
) -> Result<(), JoinShardKeyMismatch>;
```

From src/engine/pipeline.rs:943-955 (register body that this phase relaxes):

```rust
// Phase 51-04: validate shard_key compatibility before inserting.
if let Err(mismatch) = crate::engine::join_validator::validate_shard_keys(&self.streams, &stream) {
    #[cfg(feature = "server")]
    if let Some(ref registry) = self.signals {
        crate::server::signals::emit_join_shard_key_mismatch(registry, &mismatch);
    }
    return Err(BeavaError::Protocol(mismatch.message.clone()));
}
```

From src/engine/pipeline.rs::FeatureDef (around line 150, 173):

```rust
FeatureDef::EnrichFromTable { right_table: String, on: Vec<String>, join_type: JoinType, right_fields: Vec<(String,String)>, ... }
FeatureDef::StreamStreamJoin { left_stream, right_stream, on, within_ms, join_type, left_fields, right_fields }
```

From src/shard/thread.rs (EXISTING ShardOp variants — Phase 56 adds ReadEntityAt, ReadEntityBatch, SsjInsert):

```rust
pub enum ShardOp {
    Push, Get { key }, Set {..}, Mset {..}, Tombstone {..}, MarkDirty {..}, Mget {..}, GetMulti {..},
    UpsertTableRow {..}, TombstoneTableRow {..}, UpsertTableBatch {..},
    UpsertSourceTableRow {..}, DeleteSourceTableRow {..},
    UpsertSourceTableBatch {..}, DeleteSourceTableBatch {..},
    PushTableRow {..}, DeleteTableRow {..},
    ClearBackfillOperators {..},
    ListEntityKeys, EntityCount,
    EvictExpired {..}, EvictExpiredTableRows {..},
    PushForBackfill {..},
    // --- Phase 56 adds:
    // ReadEntityAt { target_shard, table_name, key, reply: oneshot::Sender<Option<EntityState>> }
    // ReadEntityBatch { target_shard, table_name, keys: Vec<String>, reply: oneshot::Sender<Vec<Option<EntityState>>> }
    // SsjInsert { join_id, side: JoinSide, join_key, event: serde_json::Value, within_ms, reply: oneshot::Sender<Vec<Map<String,Value>>> }
}
```

From tests/cross_shard_tt_cascade_ownership.rs (Phase 55 — template for Phase 56 tests):

```rust
// Hash helper copied from prior tests to locate which shard owns a key.
fn hash_to_shard(key: &str, n_shards: usize) -> usize { /* ahash reduction */ }
// Pattern: spawn N shards, register streams, push event with deliberate key
// values, assert read_entity_from_shard returns expected on correct shard
// and None on others.
```

From tests/test_debug_warnings_endpoint.rs (Phase 51 — /debug/warnings schema):

```json
{
  "warnings": {
    "join_shard_key_mismatch": [...],       // Phase 51 (pre-56)
    "shard_key_missing": [...],
    "cross_shard_joins": [...]              // Phase 56 Wave 3 adds
  }
}
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → engine | Tests spawn an in-process engine; no network surface exposed in Wave 0. |
| none on wire | Wave 0 is tests + docs only; no new trust boundaries created. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-00-01 | Tampering | `.planning/REQUIREMENTS.md` edit | mitigate | Edit is planner-authored; checker reviews diff. No runtime surface. |
| T-56-00-02 | Information Disclosure | New test fixtures leaking secrets | accept | Tests use synthetic data only; no secrets touched. |
| T-56-00-03 | Denial of Service | Perf smoke test running unbounded | mitigate | SC-4 RED test is capped at 10K events + 5s wall-clock budget; fails fast if exceeded. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit SC-1 + SC-2 + SC-3 + SC-4 + SC-5 RED integration tests and verify REQUIREMENTS.md patch</name>
  <files>
    tests/cross_shard_enrich_from_table.rs,
    tests/cross_shard_stream_stream_join.rs,
    tests/register_crossshard_join_warning.rs,
    tests/crossshard_enrich_perf_smoke.rs,
    tests/sharding_parity.rs,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-CONTEXT.md (decisions D-A1..D-D4)
    - .planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-00-PLAN.md (RED-Wave-0 rhythm template)
    - .planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-00-SUMMARY.md (how Phase 55 closed W0)
    - tests/cross_shard_tt_cascade_ownership.rs (exact hash_to_shard helper + spawn_shards pattern to copy)
    - tests/cross_shard_backpressure.rs (ShardOverload assertion idiom)
    - tests/test_join_stream_stream.rs (StreamStreamJoin fixture shape; copy the 2-stream register + push pattern)
    - tests/test_debug_warnings_endpoint.rs (/debug/warnings JSON schema; extend with `cross_shard_joins` field)
    - tests/sharding_parity.rs (proptest generator style — copy the match-by-variant approach to add `MismatchedShardEnrichOrJoin`)
    - src/engine/join_validator.rs (error struct definition — SC-3 test captures the warning by name)
    - src/engine/pipeline.rs:943-955 (register body that SC-3 test exercises)
    - .planning/REQUIREMENTS.md (confirm TPC-CORR-04 relaxed text + TPC-CORR-08 + TPC-CORR-09 rows exist — if planner has not committed this yet, check via grep)
  </read_first>
  <behavior>
    - Test 1 (cross_shard_enrich_from_table.rs::enrich_from_table_crosses_shard_boundary): Spawn 4 shards. Register `Countries` source-table (shard_key=country_code), UPSERT one row {country_code:"CH", gdp_usd:800_000} such that hash("CH")%4 = K. Register `Txns(shard_key=user_id)` stream with `enrich_from(Countries, on=country_code)`. Push event {user_id:"u_J", country_code:"CH"} where hash("u_J")%4 = J and J≠K. Assert the output features include gdp_usd=800_000. Currently (pre-Wave-2) EnrichFromTable returns Missing for cross-shard lookups → test RED. Ignore marker: `#[ignore = "56-W2"]`.
    - Test 2 (cross_shard_stream_stream_join.rs::stream_stream_join_routes_to_join_key_shard): Spawn 4 shards. Register L(shard_key=user_id) and R(shard_key=session_id) with StreamStreamJoin on `user_id` within_ms=60_000. Push L{user_id:"u1", session_id:"s1", payload:"left"}; push R{user_id:"u1", session_id:"s1", payload:"right"}. Compute J=hash("u1")%4. Assert the joined-output EntityState lives on shard J by calling `read_entity_from_shard(shard=J, key="u1")` and NOT on the shard owning hash("s1")%4. Currently (pre-Wave-3) StreamStreamJoin buffer is written on the source-event's shard → test RED. Ignore marker: `#[ignore = "56-W3"]`.
    - Test 3 (register_crossshard_join_warning.rs::register_emits_crossshard_warning_not_error): Initialize tracing subscriber with a `Vec<String>`-collecting layer. Register L(shard_key=user_id) then R(shard_key=session_id) joined on `user_id`. Assert `register()` returns `Ok(_)` (pre-Phase-56 returns Err(BeavaError::Protocol("join operator between..."))). Assert the collected logs contain the substring `"CrossShardJoinWarning"` AND the join field name `"user_id"`. Separately, HTTP-level assertion: hit `GET /debug/warnings`, parse JSON, assert `warnings.cross_shard_joins` is an array with one entry having `{join_id, left_shard_key: "user_id", right_shard_key: "session_id", on_field: "user_id"}`. Currently register() returns Err → test RED. Ignore marker: `#[ignore = "56-W3"]`.
    - Test 4 (crossshard_enrich_perf_smoke.rs::crossshard_enrich_p99_under_2x_baseline): Spawn N=4 shards. Register 10 source-tables + 1 Txns stream with 5 EnrichFromTable features each with a key that forces cross-shard reads. Push 10_000 events, collect per-event wall-clock, compute p99. Capture a Phase-55-baseline-p99 constant BASELINE_P99_MICROS (start: 50 — executor fills from a Wave-4 re-measurement). Assert `p99_micros <= 2 * BASELINE_P99_MICROS`. Ignore marker: `#[ignore = "56-W4"]`. Test is SC-4 evidence; perf-gate proper is SC-5 (Wave 4 bench script).
    - Test 5 (crossshard_enrich_perf_smoke.rs::crossshard_enrich_eps_floor): Marker test that asserts the Wave 4 perf-gate script outputs ≥ 1,059,261 EPS. Implemented as a shell-invocation test (`std::process::Command` running `benchmark/fraud-pipeline/run_bench.sh`) gated by `BEAVA_PERF_GATE=1` env var AND `#[ignore = "56-W4"]`. Exists now so the Wave-4 executor has a golden target.
    - sharding_parity.rs extension: add enum variant `MismatchedShardEnrichOrJoin` to the scenario generator, producing test cases where either (a) a stream with `EnrichFromTable` has `shard_key=` differing from `on=` or (b) an SSJ has mismatched left/right `shard_key=`. New cases wrapped in `#[ignore = "56-W2"]` / `#[ignore = "56-W3"]` as appropriate — at N=1 these still pass today (parity is trivial); at N=8 they currently fail (silent missing enrichment / mis-routed join). Follows the Phase 55 pattern of adding a variant without breaking existing 9/9 passing cases.
    - Invariants:
        (a) `cargo build --release --tests` compiles cleanly (no undefined-symbol errors). Tests that reference future APIs (ReadEntityAt etc.) may only do so within `#[ignore]`'d `#[cfg(any())]` blocks OR via string literals in assertion messages.
        (b) Running the full default test suite `cargo test --release --tests -- --test-threads=1` shows the new tests as `ignored` count (5+), not as failures. Prior test counts preserved.
        (c) `cargo test --release --test cross_shard_enrich_from_table -- --ignored --test-threads=1` compiles + runs the body but fails (RED). Same for tests 2, 3.
        (d) `grep -c "56-W[0-9]" tests/*.rs` returns ≥ 5 (the new markers).
  </behavior>
  <action>
    Step 1 — Verify `.planning/REQUIREMENTS.md` already contains: (a) TPC-CORR-04 "RELAXED in Phase 56" paragraph, (b) TPC-CORR-08 row for EnrichFromTable cross-shard, (c) TPC-CORR-09 row for StreamStreamJoin cross-shard, (d) Phase 56 traceability entry. Already written by planner. If any missing, fix.

    Confirm via:
      grep -c "TPC-CORR-08" .planning/REQUIREMENTS.md   # ≥ 2 (header + traceability)
      grep -c "TPC-CORR-09" .planning/REQUIREMENTS.md   # ≥ 2
      grep -c "RELAXED in Phase 56" .planning/REQUIREMENTS.md   # ≥ 1

    Step 2 — Create tests/cross_shard_enrich_from_table.rs:

        //! Phase 56 SC-1 — EnrichFromTable cross-shard read correctness
        //! (TPC-CORR-08). RED until Wave 2 wires ShardOp::ReadEntityAt.
        //! Passes at Wave 2.
        #![cfg(feature = "server")]

        use ahash::AHasher;
        use std::hash::{Hash, Hasher};

        fn hash_to_shard(key: &str, n_shards: usize) -> usize {
            let mut h = AHasher::default();
            key.hash(&mut h);
            (h.finish() % n_shards as u64) as usize
        }

        #[test]
        #[ignore = "56-W2"]
        fn enrich_from_table_crosses_shard_boundary() {
            const N: usize = 4;
            // Build a server+engine at N=4 using the existing helpers
            // (build_state + spawn_shard_threads, pattern from
            // tests/cross_shard_tt_cascade_ownership.rs).
            // Register source-table Countries(key=country_code).
            // UPSERT {country_code:"CH", gdp_usd:800_000}.
            // Register Txns(shard_key=user_id) with enrich_from(Countries, on=country_code).
            // Find a user_id whose hash differs from hash("CH"):
            let country = "CH";
            let k = hash_to_shard(country, N);
            let mut user = String::from("u_0");
            for i in 0.. { user = format!("u_{}", i); if hash_to_shard(&user, N) != k { break; } }
            // Push event {user_id: user, country_code:"CH"}.
            // Assert output features contain gdp_usd == 800_000.
            todo!("wire full fixture at 56-W2; body stubbed for RED commit");
        }

    Step 3 — Create tests/cross_shard_stream_stream_join.rs:

        //! Phase 56 SC-2 — StreamStreamJoin buffer lives on hash(join.on)%N
        //! (TPC-CORR-09). RED until Wave 3 relocates the buffer.
        //! Passes at Wave 3.
        #![cfg(feature = "server")]

        mod util { pub(crate) fn hash_to_shard(key: &str, n: usize) -> usize { /* copy helper */ 0 } }

        #[test]
        #[ignore = "56-W3"]
        fn stream_stream_join_routes_to_join_key_shard() {
            const N: usize = 4;
            // Register L(shard_key=user_id), R(shard_key=session_id), join on user_id.
            // Push L{user_id:"u1", session_id:"s1", payload:"L"}.
            // Push R{user_id:"u1", session_id:"s1", payload:"R"}.
            // Compute J = hash_to_shard("u1", N).
            // Assert: read_entity_from_shard(shard=J, key="u1") returns Some with join output.
            // Assert: read_entity_from_shard(shard=hash("s1")%N, key="s1") returns None (no join buffer there).
            todo!("wire full fixture at 56-W3");
        }

    Step 4 — Create tests/register_crossshard_join_warning.rs:

        //! Phase 56 SC-3 — register() accepts mismatched shard_key joins with
        //! a logged CrossShardJoinWarning (TPC-CORR-04 relaxation).
        //! Passes at Wave 3 when join_validator relaxes to warning.
        #![cfg(feature = "server")]

        #[test]
        #[ignore = "56-W3"]
        fn register_emits_crossshard_warning_not_error() {
            // Initialize a tracing subscriber with a Vec<String> collector.
            // Register L(shard_key=user_id), R(shard_key=session_id) joining on user_id.
            // Assert register() returns Ok, not Err.
            // Assert the collected log lines include "CrossShardJoinWarning"
            //   AND "user_id" AND "session_id".
            todo!("wire at 56-W3; current code path returns Err(BeavaError::Protocol)");
        }

        #[tokio::test]
        #[ignore = "56-W3"]
        async fn debug_warnings_endpoint_lists_cross_shard_joins() {
            // Spawn full server, register cross-shard join, hit GET /debug/warnings.
            // Parse JSON, assert warnings.cross_shard_joins has one entry
            //   with {join_id, left_shard_key:"user_id", right_shard_key:"session_id",
            //         on_field:"user_id", perf_note: contains "+1 inbox hop"}.
            todo!("wire at 56-W3");
        }

    Step 5 — Create tests/crossshard_enrich_perf_smoke.rs:

        //! Phase 56 SC-4 — cross-shard EnrichFromTable p99 latency smoke.
        //! SC-5 — perf gate (harness invocation).
        //! Passes at Wave 4.
        #![cfg(feature = "server")]

        const BASELINE_P99_MICROS: u64 = 50; // Phase 55 baseline; re-measured at W4.

        #[test]
        #[ignore = "56-W4"]
        fn crossshard_enrich_p99_under_2x_baseline() {
            // 4 shards, 10 source-tables, Txns stream with 5 enrich features.
            // Push 10_000 events; collect per-event wall-clock; p99.
            // Budget: 5s wall-clock (T-56-00-03 mitigation).
            // Assert p99_micros <= 2 * BASELINE_P99_MICROS.
            todo!("wire at 56-W4");
        }

        #[test]
        #[ignore = "56-W4"]
        fn crossshard_enrich_eps_floor() {
            // Gated by env BEAVA_PERF_GATE=1 (otherwise returns early).
            // Runs: MODE=complex DURATION=60 CPUS=8 CLIENTS=8 BEAVA_SHARD_INBOX_SIZE=1048576
            //       BEAVA_ENRICH_CROSSSHARD_SCENARIO=1 benchmark/fraud-pipeline/run_bench.sh
            // Parse stdout for "Aggregate EPS: N".
            // Assert N >= 1_059_261.
            todo!("wire at 56-W4 — exact envelope in 56-04-PLAN.md");
        }

    Step 6 — Extend tests/sharding_parity.rs with a new scenario variant `MismatchedShardEnrichOrJoin`. Follow the existing proptest generator convention (look at how `TtCascade` was added in Phase 55). The new variant produces two sub-cases:
      - (a) stream with `EnrichFromTable(on=country_code)` but `shard_key=user_id` — N=1 passes trivially; N=8 should produce matching output post-Wave-2. Mark the N>1 assertion path with `#[ignore = "56-W2"]` or inline `if cfg!(ignore_56_w2) { return Ok(()); }` via a local constant; match the Phase 55 `tt_cascade` convention.
      - (b) SSJ with `left.shard_key=user_id` and `right.shard_key=session_id` joining on `user_id` — N=1 trivial, N=8 correct post-Wave-3.

    Step 7 — Build + run tests:
        cargo build --release --tests
        cargo test --release --test cross_shard_enrich_from_table -- --test-threads=1    # 1 ignored
        cargo test --release --test cross_shard_stream_stream_join -- --test-threads=1   # 1 ignored
        cargo test --release --test register_crossshard_join_warning -- --test-threads=1 # 2 ignored
        cargo test --release --test crossshard_enrich_perf_smoke -- --test-threads=1     # 2 ignored
        cargo test --release --test sharding_parity -- --test-threads=1                  # 11+ passed (9 existing + 2 tt_cascade + extension)
        cargo test --release --lib                                                       # unchanged baseline 796/0/35

    Step 8 — Grep gates:
        grep -c "56-W[0-9]" tests/cross_shard_enrich_from_table.rs tests/cross_shard_stream_stream_join.rs tests/register_crossshard_join_warning.rs tests/crossshard_enrich_perf_smoke.rs tests/sharding_parity.rs
          # expect: ≥ 5 total
        grep -c "TPC-CORR-08\|TPC-CORR-09" .planning/REQUIREMENTS.md
          # expect: ≥ 4 (2 headers + 2 traceability)
        grep -c "RELAXED in Phase 56" .planning/REQUIREMENTS.md
          # expect: ≥ 1

    Step 9 — Commit with message: `test(56-W0): add RED tests for SC-1..SC-5 + TPC-CORR-04 relaxation (TPC-CORR-08, TPC-CORR-09)`.

    DO NOT: add any code to src/**. This wave is tests-only + docs-only.
    DO NOT: use `todo!()` panics in NON-ignored test bodies — they'd fail the default suite. All 5 new tests MUST have `#[ignore]` attributes.
    DO NOT: reference unshipped `ShardOp::ReadEntityAt` / `ReadEntityBatch` / `SsjInsert` as real enum variants — tests compile cleanly without them because bodies are `todo!()`.
  </action>
  <verify>
    <automated>cargo build --release --tests 2>&1 | tail -5 && cargo test --release --test cross_shard_enrich_from_table -- --test-threads=1 2>&1 | tail -3 && cargo test --release --test cross_shard_stream_stream_join -- --test-threads=1 2>&1 | tail -3 && cargo test --release --test register_crossshard_join_warning -- --test-threads=1 2>&1 | tail -3 && cargo test --release --test crossshard_enrich_perf_smoke -- --test-threads=1 2>&1 | tail -3 && cargo test --release --test sharding_parity -- --test-threads=1 2>&1 | tail -3 && cargo test --release --lib 2>&1 | tail -3 && bash -c 'test $(grep -c "56-W[0-9]" tests/cross_shard_enrich_from_table.rs tests/cross_shard_stream_stream_join.rs tests/register_crossshard_join_warning.rs tests/crossshard_enrich_perf_smoke.rs tests/sharding_parity.rs 2>/dev/null | awk -F: "{s+=\$2} END {print s}") -ge 5' && bash -c 'test $(grep -c "TPC-CORR-08" .planning/REQUIREMENTS.md) -ge 2' && bash -c 'test $(grep -c "TPC-CORR-09" .planning/REQUIREMENTS.md) -ge 2'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --release --tests` → clean exit 0.
    - `cargo test --release --test cross_shard_enrich_from_table -- --test-threads=1` → output contains "1 ignored".
    - `cargo test --release --test cross_shard_stream_stream_join -- --test-threads=1` → "1 ignored".
    - `cargo test --release --test register_crossshard_join_warning -- --test-threads=1` → "2 ignored".
    - `cargo test --release --test crossshard_enrich_perf_smoke -- --test-threads=1` → "2 ignored".
    - `cargo test --release --test sharding_parity -- --test-threads=1` → at least 11 passed (9 existing + 2 tt_cascade), new cases either passed or ignored, 0 failed.
    - `cargo test --release --lib` → exactly `796 passed / 0 failed / 35 ignored` (default) OR `800 passed / 0 failed / 35 ignored` (state-inmem). Phase 55 closing baseline must be preserved.
    - `grep -c "56-W[0-9]" tests/cross_shard_enrich_from_table.rs tests/cross_shard_stream_stream_join.rs tests/register_crossshard_join_warning.rs tests/crossshard_enrich_perf_smoke.rs tests/sharding_parity.rs` → sum ≥ 5.
    - `grep -c "TPC-CORR-08" .planning/REQUIREMENTS.md` ≥ 2; `grep -c "TPC-CORR-09" .planning/REQUIREMENTS.md` ≥ 2.
    - `grep -n "RELAXED in Phase 56" .planning/REQUIREMENTS.md` returns a TPC-CORR-04 row.
    - No file under `src/**` modified by this plan (`git diff --name-only HEAD -- src/` empty).
    - Commit exists with message matching `^test\(56-W0\)`.
  </acceptance_criteria>
  <done>All 5 RED tests + sharding_parity extension committed with proper `#[ignore = "56-W{N}"]` markers. REQUIREMENTS.md contains TPC-CORR-04 relaxation + TPC-CORR-08 + TPC-CORR-09. `cargo test --release --lib` baseline preserved exactly.</done>
</task>

</tasks>

<verification>
1. **RED landing:** 5 new tests, 1 proptest extension, 5+ `56-W[0-9]` ignore markers present.
2. **No production drift:** `git diff src/` empty for this plan's commit.
3. **Requirements captured:** TPC-CORR-04 relaxation + TPC-CORR-08 + TPC-CORR-09 in REQUIREMENTS.md.
4. **Baseline preserved:** `cargo test --release --lib` matches Phase 55 closing (796/0/35 default; 800/0/35 state-inmem).
</verification>

<success_criteria>
Wave 0 done when:
- [ ] `tests/cross_shard_enrich_from_table.rs` — 1 `#[ignore = "56-W2"]` test exists (SC-1 contract)
- [ ] `tests/cross_shard_stream_stream_join.rs` — 1 `#[ignore = "56-W3"]` test exists (SC-2 contract)
- [ ] `tests/register_crossshard_join_warning.rs` — 2 `#[ignore = "56-W3"]` tests exist (SC-3 contract)
- [ ] `tests/crossshard_enrich_perf_smoke.rs` — 2 `#[ignore = "56-W4"]` tests exist (SC-4 + SC-5 contract)
- [ ] `tests/sharding_parity.rs` — extended with `MismatchedShardEnrichOrJoin` scenario variant
- [ ] REQUIREMENTS.md updated: TPC-CORR-04 relaxed text + TPC-CORR-08 + TPC-CORR-09 + Phase 56 row
- [ ] `cargo test --release --lib` baseline unchanged vs Phase 55 close
- [ ] Zero src/** modifications
- [ ] Commit message matches `test(56-W0): ...`
</success_criteria>

<output>
After completion, create `.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-00-SUMMARY.md` with:
- Which tests got which ignore markers and why
- Exact assertion hooks each test will use when the marker flips GREEN
- BASELINE_P99_MICROS starting value (justify the 50 µs default from a Phase-55 spot-measurement; Wave 4 remeasures)
- Grep-count evidence for all acceptance criteria
- REQUIREMENTS.md diff summary
- Next wave handoff: what Wave 1 must add to ShardOp / shard thread for Wave 2 to flip SC-1 GREEN
</output>
