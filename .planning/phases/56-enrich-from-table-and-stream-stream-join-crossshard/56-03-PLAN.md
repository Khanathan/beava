---
phase: 56
plan: 03
type: execute
wave: 3
depends_on: [56-02]
files_modified:
  - src/engine/pipeline.rs
  - src/engine/register.rs
  - src/engine/join_validator.rs
  - src/shard/mod.rs
  - src/server/signals.rs
  - src/server/http.rs
  - tests/cross_shard_stream_stream_join.rs
  - tests/register_crossshard_join_warning.rs
  - tests/sharding_parity.rs
autonomous: true
requirements:
  - TPC-CORR-04
  - TPC-CORR-09
tags:
  - wave-3
  - stream-stream-join
  - join-buffer-routing
  - tpc-corr-04-relaxation
  - debug-warnings
  - phase-56
must_haves:
  truths:
    - "StreamStreamJoin evaluation routes both L and R to hash(join.on) % N via ssj_insert_at_shard (Wave 1 helper); join buffer lives on the target shard only"
    - "When source_shard == target_shard (co-located case per D-B5), the code takes the same-shard fast path via Shard::apply_ssj_insert directly — zero extra hops, matches pre-Phase-56 perf for co-located joins"
    - "Cross-shard SSJ dispatches ShardOp::SsjInsert with (join_id, side, join_key, event); receives Vec<Map> of matched counterparty events; emits joined outputs via the existing downstream cascade path on the SOURCE shard"
    - "register() no longer returns Err on mismatched shard_key joins (TPC-CORR-04 relaxation): instead emits tracing::warn! CrossShardJoinWarning + increments beava_crossshard_joins_registered_total{join_id} + records to /debug/warnings"
    - "GET /debug/warnings response JSON gains warnings.cross_shard_joins: [{join_id, left_shard_key, right_shard_key, on_field, perf_note}]"
    - "D-B5 co-location preserved: when both sides declare shard_key=join.on, validate_shard_keys returns Ok silently (no warning) — matches pre-Phase-56 happy path exactly"
    - "Wave-0 RED tests flip GREEN: cross_shard_stream_stream_join (SC-2), register_crossshard_join_warning (SC-3) × 2"
    - "sharding_parity MismatchedShardEnrichOrJoin SSJ sub-case 56-W3 marker removed; passes at N=8"
    - "Phase 51 test_join_stream_stream (same-shard SSJ + co-located shard_key register-time ok) unregressed"
  artifacts:
    - path: src/engine/pipeline.rs
      provides: "StreamStreamJoin eval block at ~1733-1900 rewritten: route via ssj_insert_at_shard; emits joined outputs from returned matches"
      contains: "ssj_insert_at_shard"
    - path: src/engine/join_validator.rs
      provides: "validate_shard_keys returns Ok(Some(warning)) | Ok(None) — no longer Err on mismatch. New pub struct CrossShardJoinWarning with fields (join_id, left_shard_key, right_shard_key, on_field, perf_note)."
      contains: "pub struct CrossShardJoinWarning|CrossShardJoinWarning"
    - path: src/engine/register.rs
      provides: "register() calls validate_shard_keys, consumes Ok(Some(warning)) by emitting tracing::warn!, incrementing counter, recording to signals registry. No more Err(BeavaError::Protocol(mismatch.message)) path."
      contains: "CrossShardJoinWarning"
    - path: src/server/signals.rs
      provides: "emit_cross_shard_join_warning(registry, &warning) helper mirroring emit_join_shard_key_mismatch but non-fatal"
      contains: "emit_cross_shard_join_warning|cross_shard_joins"
    - path: src/server/http.rs
      provides: "/debug/warnings handler extended with cross_shard_joins array"
      contains: "cross_shard_joins"
    - path: src/shard/mod.rs
      provides: "ssj-<join_id>/ partition allocation (may be same fjall partition as entity table per CONTEXT D-B3 — Claude's discretion). Shard::apply_ssj_insert already lands in Wave 1; any new join-buffer persistence helpers land here."
      contains: "apply_ssj_insert"
    - path: tests/cross_shard_stream_stream_join.rs
      provides: "RED → GREEN; SC-2 fixture concrete"
    - path: tests/register_crossshard_join_warning.rs
      provides: "RED → GREEN; both SC-3 sub-tests concrete"
  key_links:
    - from: src/engine/pipeline.rs (StreamStreamJoin eval)
      to: src/engine/pipeline.rs::ssj_insert_at_shard (Wave 1 helper)
      via: "Compute target = shard_of_key(&state_key, N); ssj_insert_at_shard(shard_partitions, target, shard, join_id, side, &state_key, event, within_ms); iterate returned Vec<Map> for matches; emit joined output via the existing cascade path"
      pattern: "ssj_insert_at_shard"
    - from: src/engine/register.rs
      to: src/engine/join_validator.rs (validate_shard_keys relaxed)
      via: "Ok(()) → Ok(None) for match; Ok(Some(CrossShardJoinWarning)) for mismatch; no Err"
      pattern: "CrossShardJoinWarning|validate_shard_keys"
    - from: src/server/http.rs (/debug/warnings)
      to: src/server/signals.rs (signal registry)
      via: "Signal registry gains cross_shard_joins slot; HTTP handler serializes it alongside existing join_shard_key_mismatch + shard_key_missing fields"
      pattern: "cross_shard_joins"
user_setup: []
---

<objective>
Complete the Phase 56 correctness delivery: re-route StreamStreamJoin through the hash(join.on)%N-owning shard (TPC-CORR-09) and relax the register-time TPC-CORR-04 rejection to a runtime CrossShardJoinWarning.

This wave flips the remaining Wave-0 RED tests GREEN:
- `tests/cross_shard_stream_stream_join.rs::stream_stream_join_routes_to_join_key_shard` (SC-2)
- `tests/register_crossshard_join_warning.rs::register_emits_crossshard_warning_not_error` (SC-3 part 1)
- `tests/register_crossshard_join_warning.rs::debug_warnings_endpoint_lists_cross_shard_joins` (SC-3 part 2)
- `tests/sharding_parity.rs` SSJ sub-case at N=8

The StreamStreamJoin change mirrors the Wave-2 EnrichFromTable change structurally: replace the inline RMW on `shard.state` (at pipeline.rs:1805-1840) with a call to `ssj_insert_at_shard`. Same-shard fast path (D-B5 co-located case) is preserved via the Wave-1 helper's target==current short-circuit.

The TPC-CORR-04 relaxation replaces:

```rust
return Err(BeavaError::Protocol(mismatch.message.clone()));
```

with:

```rust
let warning = CrossShardJoinWarning::from_mismatch(&mismatch);
tracing::warn!(target: "beava::register", "CrossShardJoinWarning: {}", warning.message);
metrics::counter!(CROSSSHARD_JOINS_REGISTERED_TOTAL, "join_id" => warning.join_id.clone()).increment(1);
if let Some(registry) = &self.signals {
    emit_cross_shard_join_warning(registry, &warning);
}
// No return — registration proceeds.
```

Additionally, the `/debug/warnings` endpoint gains a `cross_shard_joins` array alongside the existing `join_shard_key_mismatch` + `shard_key_missing` fields.

Purpose: unblock SC-2, SC-3, SC-4. Complete correctness for Phase 56.

Output:
- Rewritten StreamStreamJoin eval block routing via `ssj_insert_at_shard`
- `CrossShardJoinWarning` struct in `join_validator.rs` (non-error)
- Relaxed `register()` path in `pipeline.rs` (via `register.rs`)
- Extended `/debug/warnings` JSON schema
- 3 RED tests flipped GREEN
- sharding_parity SSJ sub-case GREEN at N=8
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-CONTEXT.md
@.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-00-PLAN.md
@.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-01-PLAN.md
@.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-02-PLAN.md
@src/engine/pipeline.rs
@src/engine/register.rs
@src/engine/join_validator.rs
@src/engine/operators.rs
@src/server/signals.rs
@src/server/http.rs

<interfaces>
From src/engine/join_validator.rs (CURRENT — validate_shard_keys returns Err on mismatch; Wave 3 relaxes):

```rust
pub fn validate_shard_keys(
    streams: &AHashMap<String, StreamDefinition>,
    new_stream: &StreamDefinition,
) -> Result<(), JoinShardKeyMismatch>;
```

Wave 3 target signature:

```rust
pub fn validate_shard_keys(
    streams: &AHashMap<String, StreamDefinition>,
    new_stream: &StreamDefinition,
) -> Vec<CrossShardJoinWarning>;  // empty Vec = no warnings; never errors
```

Plus retained for introspection:

```rust
#[derive(Debug, Clone, Serialize)]
pub struct CrossShardJoinWarning {
    pub join_id: String,
    pub stream_a: String,
    pub stream_b: String,
    pub left_shard_key: String,
    pub right_shard_key: String,
    pub on_field: String,
    pub perf_note: String,  // "+1 inbox hop per event; +partition for join buffer"
    pub message: String,    // pre-formatted log-friendly summary
}
```

From src/engine/pipeline.rs:943-955 (register body — CURRENT, with Err return to remove):

```rust
if let Err(mismatch) = crate::engine::join_validator::validate_shard_keys(&self.streams, &stream) {
    #[cfg(feature = "server")]
    if let Some(ref registry) = self.signals {
        crate::server::signals::emit_join_shard_key_mismatch(registry, &mismatch);
    }
    return Err(BeavaError::Protocol(mismatch.message.clone()));
}
```

Wave 3 target:

```rust
let warnings = crate::engine::join_validator::validate_shard_keys(&self.streams, &stream);
for w in &warnings {
    tracing::warn!(target: "beava::register", "CrossShardJoinWarning: {}", w.message);
    metrics::counter!(
        crate::shard::metrics::CROSSSHARD_JOINS_REGISTERED_TOTAL,
        "join_id" => w.join_id.clone()
    ).increment(1);
    #[cfg(feature = "server")]
    if let Some(ref registry) = self.signals {
        crate::server::signals::emit_cross_shard_join_warning(registry, w);
    }
}
// No early return — proceed with registration.
```

From src/engine/pipeline.rs:1733-1900 (StreamStreamJoin eval — CURRENT; Wave 3 rewrites):

```rust
// StreamStreamJoin: read+write join buffer in shard.state.
if let Some((feat_name, left_stream, right_stream, on_keys, within_ms, join_type, _left_fields, right_fields)) = ss_join {
    ...
    let state_key = encode_group_by(&on_keys, &effective_event)?;  // hash input = join.on
    let event_time_ms = ...;
    let arriving_map = effective_event.as_object()...;

    // --- BUG: buffer lives on CURRENT shard, not hash(state_key)%N shard ---
    let matches: Vec<...> = {
        let mut view = StoreView::Sharded(shard);
        view.with_entity_mut(&state_key, |entity| {
            ...get_or_create buf...
            let probed = buf.probe(side, event_time_ms);
            buf.insert(side, event_time_ms, arriving_map.clone());
            buf.evict();
            probed
        })
    };
    // --- END BUG ---

    // Emit joined outputs for each match (unchanged — runs on SOURCE shard).
    for m in matches { emit_joined_output(...); }
}
```

From Wave 1 (src/engine/pipeline.rs — already exists):

```rust
impl PipelineEngine {
    pub fn ssj_insert_at_shard(
        &self, shard_partitions: &[ShardHandle], target_shard_idx: usize,
        current_shard: &mut Shard,
        join_id: &str, side: JoinSide, join_key: &str,
        event: serde_json::Value, within_ms: u64,
    ) -> Result<Vec<serde_json::Map<String, serde_json::Value>>, BeavaError>;
}
```

From src/server/signals.rs (CURRENT — Phase 51 pattern to mirror):

```rust
pub fn emit_join_shard_key_mismatch(
    registry: &Arc<SignalRegistry>,
    mismatch: &JoinShardKeyMismatch,
);
// Wave 3 adds:
// pub fn emit_cross_shard_join_warning(registry: &Arc<SignalRegistry>, warning: &CrossShardJoinWarning);
```

From src/server/http.rs (CURRENT /debug/warnings JSON shape — inspect via grep):

```json
{
  "warnings": {
    "join_shard_key_mismatch": [],
    "shard_key_missing": []
  }
}
```

Wave 3 adds `cross_shard_joins: [ ... ]`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| register() caller → validate_shard_keys | Stream definition comes from authenticated ingest or admin endpoint; validated at API boundary. |
| source shard → SSJ target shard | SPSC SsjInsert carrying in-process Rust struct; same trust as Wave 2 enrichment read. |
| /debug/warnings consumer → signal registry | Read-only HTTP endpoint; no mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-03-01 | Denial of Service | signals registry unbounded growth from repeated cross-shard join registrations | mitigate | Dedupe by `join_id` inside signals registry (matches Phase 51 join_shard_key_mismatch dedupe); only one entry per unique (join_id, left_shard_key, right_shard_key) tuple. Covered by dedupe test in Phase 51 (tests/test_warnings_dedupe.rs) — extend it to cover cross_shard_joins. |
| T-56-03-02 | Tampering | User reads /debug/warnings and treats it as authoritative correctness output | accept | Warnings are advisory; TPC-CORR-09 runtime path delivers correctness regardless. Documented in perf_note string. |
| T-56-03-03 | Repudiation | Relaxation loses audit trail of previously-rejected registrations | mitigate | tracing::warn! log captures every relaxation event with full (stream_a, stream_b, keys) context; metric `beava_crossshard_joins_registered_total{join_id}` is a persistent counter. |
| T-56-03-04 | Denial of Service | SSJ target partition unbounded growth (buffer eviction is within_ms-bounded but cold keys linger) | accept | Eviction is within_ms per Phase 51 — unchanged. Buffer-level TTL eviction deferred to Phase 57 (retraction work). |
| T-56-03-05 | Elevation of Privilege | — | accept | No auth boundary added. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relax TPC-CORR-04 — convert validate_shard_keys to non-fatal CrossShardJoinWarning + wire register() + signals + /debug/warnings</name>
  <files>
    src/engine/join_validator.rs,
    src/engine/register.rs,
    src/engine/pipeline.rs,
    src/server/signals.rs,
    src/server/http.rs,
    tests/register_crossshard_join_warning.rs
  </files>
  <read_first>
    - src/engine/join_validator.rs full file (current validate_shard_keys impl + JoinShardKeyMismatch struct; keep JoinShardKeyMismatch type for back-compat per CONTEXT D-C2 — additive, not destructive)
    - src/engine/register.rs full file (registration entry points)
    - src/engine/pipeline.rs:943-955 (register() body — the lines to rewrite)
    - src/server/signals.rs (SignalRegistry struct + emit_join_shard_key_mismatch fn — copy shape)
    - src/server/http.rs (find /debug/warnings handler — usually around a large axum route group; grep for "debug/warnings")
    - tests/test_debug_warnings_endpoint.rs + tests/test_warnings_dedupe.rs + tests/test_warnings_feed.rs + tests/test_warnings_integration.rs (Phase 51 test suite for warnings — extend, don't replace)
    - tests/register_crossshard_join_warning.rs (Wave 0 stubbed bodies — fill in)
    - src/shard/metrics.rs (CROSSSHARD_JOINS_REGISTERED_TOTAL constant — already landed Wave 1)
  </read_first>
  <behavior>
    Behavior 1 (join_validator): new struct `CrossShardJoinWarning` and new function signature `validate_shard_keys(...) -> Vec<CrossShardJoinWarning>`. NEVER returns Err. For every mismatched join peer-pair, emits one warning. For `EnrichFromTable` joins (between a Stream and its right_table), a mismatch produces a warning. For `StreamStreamJoin`, each peer-pair mismatch produces a warning. The `keys_match(None, None)` path continues to produce no warning (both default).
    Behavior 2 (CrossShardJoinWarning fields):
        join_id: String          — derive from "{stream_a}_x_{stream_b}_on_{on_field}"
        stream_a: String         — the new stream being registered
        stream_b: String         — the peer
        left_shard_key: String   — stream_a's shard_key.display()
        right_shard_key: String  — stream_b's shard_key.display()
        on_field: String         — suggested_common
        perf_note: String        — "+1 inbox hop per event; +partition for join buffer. Co-locate by setting shard_key='{on_field}' on both streams if this is hot-path."
        message: String          — human-readable single-line summary (log-friendly)
    Behavior 3 (back-compat): keep `JoinShardKeyMismatch` struct and `emit_join_shard_key_mismatch` function defined (per CONTEXT §Specifics "additive, not destructive") — no callers from `register()` any more, but the type stays so external consumers matching on `BeavaError::Protocol(...)` don't break. Mark `JoinShardKeyMismatch` with `#[deprecated(since = "56.0", note = "Phase 56 relaxation: use CrossShardJoinWarning instead.")]`.
    Behavior 4 (register()): Call `validate_shard_keys`, get `Vec<CrossShardJoinWarning>`. Loop: tracing::warn! + counter increment + emit_cross_shard_join_warning. NO early return. Registration proceeds. Co-located case (empty Vec) is silent — no log, no counter, no signal.
    Behavior 5 (signals): `src/server/signals.rs` gains `emit_cross_shard_join_warning(registry, warning)` that inserts the warning into the registry under a new `cross_shard_joins` field (dedupe by join_id — T-56-03-01 mitigation). Mirror the structure of `emit_join_shard_key_mismatch`.
    Behavior 6 (/debug/warnings): HTTP handler includes `cross_shard_joins` in the JSON response. Schema:

        {
          "warnings": {
            "join_shard_key_mismatch": [...],
            "shard_key_missing": [...],
            "cross_shard_joins": [
              {"join_id": "...", "left_shard_key": "...", "right_shard_key": "...", "on_field": "...", "perf_note": "..."}
            ]
          }
        }

    Behavior 7 (SC-3 part 1 test): `register_emits_crossshard_warning_not_error` — set up a tracing subscriber that captures structured log events into a `Vec<String>`. Register L(shard_key=user_id) then R(shard_key=session_id) joined on user_id. Assert:
      - `register()` returns `Ok(_)` for both.
      - Captured logs contain at least one line with `"CrossShardJoinWarning"` AND `"user_id"` AND `"session_id"`.
      - `beava_crossshard_joins_registered_total` counter is ≥ 1 (via metrics-exporter-prometheus scrape helper or a test-only metric recorder — use whichever pattern Phase 51 tests adopted).
    Behavior 8 (SC-3 part 2 test): `debug_warnings_endpoint_lists_cross_shard_joins` — spawn full server; register the same mismatched join; GET `/debug/warnings`; parse JSON; assert `warnings.cross_shard_joins` has 1 entry with `left_shard_key == "user_id"` AND `right_shard_key == "session_id"` AND `on_field == "user_id"`.
    Behavior 9 (back-compat smoke): Phase 51 test `test_join_stream_stream` with co-located shard_key MUST still pass unchanged. Phase 51 test `test_warnings_dedupe` must also pass (extend with cross_shard_joins dedupe coverage — optional but encouraged).
    Invariant: `grep -n "return Err(BeavaError::Protocol(mismatch.message" src/engine/pipeline.rs` returns 0.
    Invariant: `grep -n "pub struct CrossShardJoinWarning" src/engine/join_validator.rs` returns 1.
  </behavior>
  <action>
    Step 1 — Add `CrossShardJoinWarning` struct in src/engine/join_validator.rs:

        #[derive(Debug, Clone, Serialize)]
        pub struct CrossShardJoinWarning {
            pub join_id: String,
            pub stream_a: String,
            pub stream_b: String,
            pub left_shard_key: String,
            pub right_shard_key: String,
            pub on_field: String,
            pub perf_note: String,
            pub message: String,
        }

        impl CrossShardJoinWarning {
            pub fn new(stream_a: &str, stream_b: &str,
                       left_shard_key: &str, right_shard_key: &str,
                       on_field: &str) -> Self {
                let join_id = format!("{}_x_{}_on_{}", stream_a, stream_b, on_field);
                let perf_note = format!(
                    "+1 inbox hop per event; +partition for join buffer. \
                     Co-locate by setting shard_key='{}' on both streams if this is hot-path.",
                    on_field);
                let message = format!(
                    "join '{}' between '{}' and '{}' has mismatched shard_key (left='{}' right='{}') on '{}'. {}",
                    join_id, stream_a, stream_b, left_shard_key, right_shard_key, on_field, perf_note);
                Self { join_id, stream_a: stream_a.into(), stream_b: stream_b.into(),
                       left_shard_key: left_shard_key.into(), right_shard_key: right_shard_key.into(),
                       on_field: on_field.into(), perf_note, message }
            }
        }

    Step 2 — Rewrite `validate_shard_keys`:

        pub fn validate_shard_keys(
            streams: &AHashMap<String, StreamDefinition>,
            new_stream: &StreamDefinition,
        ) -> Vec<CrossShardJoinWarning> {
            let mut out = Vec::new();
            let new_key = &new_stream.shard_key;
            for (_fname, def) in &new_stream.features {
                match def {
                    FeatureDef::EnrichFromTable { right_table, on, .. } => {
                        if let Some(peer) = streams.get(right_table) {
                            if !keys_match(new_key, &peer.shard_key) {
                                out.push(CrossShardJoinWarning::new(
                                    &new_stream.name, right_table,
                                    &shard_key_display(new_key), &shard_key_display(&peer.shard_key),
                                    &suggested_common(on),
                                ));
                            }
                        }
                    }
                    FeatureDef::StreamStreamJoin { left_stream, right_stream, on, .. } => {
                        if left_stream != &new_stream.name {
                            if let Some(peer) = streams.get(left_stream) {
                                if !keys_match(new_key, &peer.shard_key) {
                                    out.push(CrossShardJoinWarning::new(
                                        &new_stream.name, left_stream,
                                        &shard_key_display(new_key), &shard_key_display(&peer.shard_key),
                                        &suggested_common(on),
                                    ));
                                }
                            }
                        }
                        if right_stream != &new_stream.name {
                            if let Some(peer) = streams.get(right_stream) {
                                if !keys_match(new_key, &peer.shard_key) {
                                    out.push(CrossShardJoinWarning::new(
                                        &new_stream.name, right_stream,
                                        &shard_key_display(new_key), &shard_key_display(&peer.shard_key),
                                        &suggested_common(on),
                                    ));
                                }
                            }
                        }
                    }
                    _ => {}
                }
            }
            out
        }

    Keep the old `JoinShardKeyMismatch` struct + `build_mismatch` fn + `#[deprecated]` marker.

    Step 3 — Update register() in src/engine/pipeline.rs (or wherever register body lives) with the Behavior-4 loop.

    Step 4 — Add `emit_cross_shard_join_warning` in src/server/signals.rs:

        pub fn emit_cross_shard_join_warning(
            registry: &Arc<SignalRegistry>,
            warning: &CrossShardJoinWarning,
        ) {
            // Dedupe by join_id — see T-56-03-01.
            let mut cs = registry.cross_shard_joins.lock();
            if cs.iter().all(|w: &CrossShardJoinWarning| w.join_id != warning.join_id) {
                cs.push(warning.clone());
            }
        }

    Add `cross_shard_joins: Mutex<Vec<CrossShardJoinWarning>>` field to `SignalRegistry`.

    Step 5 — Update /debug/warnings handler in src/server/http.rs. Grep for the existing handler (it formats a JSON response with `warnings.join_shard_key_mismatch` and `warnings.shard_key_missing`). Add:

        let cross_shard_joins = registry.cross_shard_joins.lock().clone();
        ...
        json!({
            "warnings": {
                "join_shard_key_mismatch": existing,
                "shard_key_missing": existing,
                "cross_shard_joins": cross_shard_joins,   // NEW
            }
        })

    Step 6 — Fill out tests/register_crossshard_join_warning.rs from Wave 0 stubs:
      Test 1 (sync, no server): use `tracing_subscriber::fmt::Layer` with a `Vec<u8>` sink via `Arc<Mutex<Vec<u8>>>`; register streams against a standalone PipelineEngine; assert Ok + log contains the strings per Behavior 7.
      Test 2 (tokio, full server): use the same helper as test_debug_warnings_endpoint.rs; register + GET /debug/warnings; parse + assert per Behavior 8.
      Remove both `#[ignore = "56-W3"]` markers.

    Step 7 — Run:
        cargo build --release
        cargo build --release --features state-inmem
        cargo test --release --test register_crossshard_join_warning -- --test-threads=1         # 2/2
        cargo test --release --test test_join_stream_stream                                       # same-shard SSJ unregressed
        cargo test --release --test test_debug_warnings_endpoint                                  # Phase 51 unregressed
        cargo test --release --test test_warnings_dedupe                                          # Phase 51 unregressed
        cargo test --release --test test_warnings_feed                                            # Phase 51 unregressed
        cargo test --release --test test_warnings_integration                                     # Phase 51 unregressed
        cargo test --release --lib                                                                # baseline + possibly new unit tests for CrossShardJoinWarning

    Step 8 — Grep gates:
        grep -c "return Err(BeavaError::Protocol(mismatch.message" src/engine/pipeline.rs        # 0
        grep -c "pub struct CrossShardJoinWarning" src/engine/join_validator.rs                  # 1
        grep -c "fn emit_cross_shard_join_warning" src/server/signals.rs                         # 1
        grep -c '"cross_shard_joins"' src/server/http.rs                                          # ≥ 1

    DO NOT: delete JoinShardKeyMismatch (keep deprecated — back-compat).
    DO NOT: break any existing Phase 51 warning endpoint test.
    DO NOT: touch StreamStreamJoin eval path in pipeline.rs — that's Task 2.
  </action>
  <verify>
    <automated>cargo build --release 2>&1 | tail -3 && cargo build --release --features state-inmem 2>&1 | tail -3 && cargo test --release --test register_crossshard_join_warning -- --test-threads=1 2>&1 | tail -5 && cargo test --release --test test_join_stream_stream 2>&1 | tail -3 && cargo test --release --test test_debug_warnings_endpoint 2>&1 | tail -3 && cargo test --release --test test_warnings_dedupe 2>&1 | tail -3 && cargo test --release --test test_warnings_integration 2>&1 | tail -3 && cargo test --release --lib 2>&1 | tail -3 && bash -c 'test $(grep -c "return Err(BeavaError::Protocol(mismatch.message" src/engine/pipeline.rs) -eq 0' && bash -c 'test $(grep -c "pub struct CrossShardJoinWarning" src/engine/join_validator.rs) -eq 1' && bash -c 'test $(grep -c "fn emit_cross_shard_join_warning" src/server/signals.rs) -eq 1'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --release` + `cargo build --release --features state-inmem` clean.
    - `cargo test --release --test register_crossshard_join_warning -- --test-threads=1` — 2 passed, 0 failed, 0 ignored.
    - `cargo test --release --test test_join_stream_stream` — unregressed.
    - All 4 Phase 51 warning tests (test_debug_warnings_endpoint, test_warnings_dedupe, test_warnings_feed, test_warnings_integration) — unregressed.
    - `cargo test --release --lib` — baseline preserved (+ CrossShardJoinWarning unit tests if added).
    - `grep -c "return Err(BeavaError::Protocol(mismatch.message" src/engine/pipeline.rs` — 0.
    - `grep -c "pub struct CrossShardJoinWarning" src/engine/join_validator.rs` — exactly 1.
    - `grep -c "fn emit_cross_shard_join_warning" src/server/signals.rs` — exactly 1.
    - `grep -c '"cross_shard_joins"' src/server/http.rs` — ≥ 1.
    - `grep -c "#\[deprecated" src/engine/join_validator.rs` — ≥ 1 (JoinShardKeyMismatch marked deprecated).
    - Commit message matches `feat(56-W3a): relax TPC-CORR-04 → CrossShardJoinWarning at register time`.
  </acceptance_criteria>
  <done>register() no longer rejects mismatched shard_key joins. CrossShardJoinWarning flows through tracing, metric counter, signals registry, and /debug/warnings. Phase 51 warning endpoint tests all green. SC-3 tests 2/2 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite StreamStreamJoin eval to route via ssj_insert_at_shard + flip SC-2 RED test GREEN + unmark sharding_parity SSJ sub-case</name>
  <files>
    src/engine/pipeline.rs,
    tests/cross_shard_stream_stream_join.rs,
    tests/sharding_parity.rs
  </files>
  <read_first>
    - src/engine/pipeline.rs:1733-1900 (StreamStreamJoin eval block — the BIG rewrite target)
    - src/engine/pipeline.rs::ssj_insert_at_shard (Wave 1 helper — exact signature + same-shard fast path behavior)
    - src/shard/mod.rs::apply_ssj_insert (Wave 1 — how the target-side buffer insert works)
    - src/engine/operators.rs StreamJoinBuffer + JoinSide (unchanged)
    - .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-02-SUMMARY.md (Wave 2 close; gives the exact pattern for target_shard computation)
    - tests/cross_shard_stream_stream_join.rs (Wave 0 body; fill)
    - tests/sharding_parity.rs MismatchedShardEnrichOrJoin SSJ sub-case
    - tests/test_join_stream_stream.rs (Phase 51 fixture — ensures co-located same-shard path unchanged)
    - Grep: `grep -n "StreamStreamJoin" src/engine/pipeline.rs` to find every touch
  </read_first>
  <behavior>
    Behavior 1 (rewrite SSJ eval): replace the inline `StoreView::Sharded(shard).with_entity_mut(&state_key, |entity| { ... probe + insert + evict ... })` block with:

        let target_shard = crate::shard::shard_of_key(&state_key, shard_partitions.len());
        let join_id = feat_name.clone();   // feat_name IS the join_id (Phase 51 naming convention)
        let matches: Vec<serde_json::Map<String, serde_json::Value>> =
            self.ssj_insert_at_shard(
                shard_partitions, target_shard, shard,
                &join_id, side, &state_key,
                serde_json::Value::Object(arriving_map.clone()),
                within_ms,
            )?;
        // matches may be empty; downstream loop handles empty case already.

    Same-shard fast path preserved automatically: ssj_insert_at_shard short-circuits to shard.apply_ssj_insert when target_shard == shard.shard_id().

    Behavior 2 (emit joined outputs unchanged): the existing code that takes `matches: Vec<Map>` and emits one joined-output event per match (via the downstream cascade path) runs on the SOURCE shard — unchanged. This is the Phase 55 cascade behaviour: joined output rows may themselves cross-shard-cascade to their own output_key shard.
    Behavior 3 (D-B5 co-location preserved): When both sides declare `shard_key=join.on`, every event on L or R lands on the same shard that owns `hash(join.on)%N`. `shard.shard_id() == target_shard` → fast path → zero extra hop → perf identical to pre-Phase-56.
    Behavior 4 (SC-2 concrete fixture):
      1. Spawn N=4 shards.
      2. Register L(shard_key="user_id") with no features.
      3. Register R(shard_key="session_id") with a StreamStreamJoin feature keyed on "user_id", within_ms=60_000, joining L and R.
      4. Assert register returns Ok (expected by relaxed TPC-CORR-04).
      5. Push L event `{user_id:"u1", session_id:"s1", payload:"L"}`; compute LJ=shard_of_key("u1",4), LS=shard_of_key("s1",4). L routes at ingress to shard owning "user_id" (LJ).
         ACTUALLY — the ingest-side shard_key=user_id on L means the event goes to shard hash("u1")%4 = LJ. From there, the SSJ eval computes state_key = encode_group_by(["user_id"], event) = "u1"; target_shard = shard_of_key("u1",4) = LJ. Same shard — fast path.
         SWAP the fixture: register R's side event to hit the other side. Push R event `{user_id:"u1", session_id:"s1", payload:"R"}`; ingress-side shard_key=session_id = "s1" → shard RS = hash("s1") % 4. From RS, SSJ eval computes state_key = "u1"; target_shard = LJ. RS != LJ by construction → cross-shard SsjInsert fires.
         Choose u1, s1 such that shard_of_key("u1",4) != shard_of_key("s1",4). Deterministic search loop.
      6. Assert the resulting joined-output EntityState lives on shard LJ:
           `read_entity_from_shard(shard=LJ, key="u1")` returns Some with the joined features (payload combined).
           `read_entity_from_shard(shard=RS, key="s1")` returns None (no join buffer leaks to session shard).
         Match evaluation: after pushing L on LJ (fast path, stores L on LJ's buffer) and then R on RS (cross-shard SsjInsert to LJ, probes L buffer, gets match), the joined output is emitted on LJ's downstream via the Phase 55 cascade.
    Behavior 5 (metric assertion): `beava_ssj_cross_shard_total{join_id}` increments ≥ 1 after the R push (cross-shard path was taken). Optional in the test; counter-based checks via the Phase 51 metric recorder helper.
    Invariant: `grep -E "with_entity_mut.*state_key|StreamJoinBuffer::new" src/engine/pipeline.rs` returns 0 inside the StreamStreamJoin eval block (the old inline probe+insert is gone; construction is in apply_ssj_insert).
    Invariant: `grep -c "ssj_insert_at_shard" src/engine/pipeline.rs` ≥ 2 (1 def + ≥1 call).
  </behavior>
  <action>
    Step 1 — Open src/engine/pipeline.rs:1733-1900 (the StreamStreamJoin eval block). Read the full block first — 1805-1840 is the probe+insert+evict RMW.

    Step 2 — Replace the RMW block with the ssj_insert_at_shard call per Behavior 1. Preserve the surrounding code: `side_opt` extraction, `state_key` derivation, `event_time_ms` unchanged (but note: `event_time_ms` is passed INTO `apply_ssj_insert`? — check Wave 1 signature. If Wave 1's apply_ssj_insert takes within_ms only and derives event_time internally from the `event` map, adjust here; otherwise extend the ShardOp and helper to carry event_time_ms. Plan: pass event_time_ms into apply_ssj_insert for deterministic behaviour; update Wave-1 helper signature if not already done).

    **If Wave 1 helper doesn't carry event_time_ms:** extend both `ShardOp::SsjInsert` and `Shard::apply_ssj_insert` signatures in THIS task to include `event_time_ms: u64`. Document the adjustment in SUMMARY. (This is a permitted Wave-2/3 boundary crossing — Wave 1's primary charter was the variant scaffold; signature-completeness is finalized when the consumer wires up.)

    Step 3 — Iterate matches loop: unchanged. The Vec<Map> returned from ssj_insert_at_shard is the same structure the previous probe produced. Downstream emit-joined-output code paths untouched.

    Step 4 — Fill tests/cross_shard_stream_stream_join.rs per Behavior 4. Remove `#[ignore = "56-W3"]`. Deterministic u1/s1 search loop in the fixture:

        fn find_distinct_shard_pair(n: usize) -> (String, String) {
            for i in 0..1000 {
                let u = format!("u_{}", i);
                for j in 0..1000 {
                    let s = format!("s_{}", j);
                    if shard_of_key(&u, n) != shard_of_key(&s, n) {
                        return (u, s);
                    }
                }
            }
            panic!("couldn't find distinct shards for u/s in 4 shards — hash catastrophe?")
        }

    Step 5 — sharding_parity.rs: find SSJ sub-case's `#[ignore = "56-W3"]` (or gated early-return), remove/unmark. Run the proptest at N=8 — expect GREEN.

    Step 6 — Run:
        cargo build --release
        cargo build --release --features state-inmem
        cargo test --release --test cross_shard_stream_stream_join        # 1 passed, 0 ignored
        cargo test --release --test test_join_stream_stream               # Phase 51 same-shard SSJ unregressed (co-located happy path)
        cargo test --release --test sharding_parity -- --test-threads=1   # ≥ 13 passed (prior 12 + SSJ sub-case)
        cargo test --release --test cross_shard_enrich_from_table         # Wave 2 still GREEN
        cargo test --release --test cross_shard_tt_cascade_ownership      # Phase 55 unregressed
        cargo test --release --test cascade_metrics                       # Phase 55 unregressed
        cargo test --release --lib                                        # baseline preserved
        grep -cE '#\[ignore = "56-W3"' tests/                              # 0 (all 56-W3 markers removed)
        grep -cE '#\[ignore = "56-W4"' tests/                              # ≥ 2 (perf tests remain)

    Step 7 — Final sanity grep:
        grep -c "ssj_insert_at_shard" src/engine/pipeline.rs                              # ≥ 2
        grep -nE "with_entity_mut.*&state_key|StreamJoinBuffer::new" src/engine/pipeline.rs # 0 in SSJ eval block
          (allow hits in other contexts — read the line ranges to confirm)

    DO NOT: touch EnrichFromTable eval (Wave 2 work).
    DO NOT: touch register.rs / join_validator.rs beyond what Task 1 did.
    DO NOT: alter the downstream joined-output emit path.
    DO NOT: flip 56-W4 markers (perf tests in crossshard_enrich_perf_smoke.rs).
  </action>
  <verify>
    <automated>cargo build --release 2>&1 | tail -3 && cargo build --release --features state-inmem 2>&1 | tail -3 && cargo test --release --test cross_shard_stream_stream_join 2>&1 | tail -5 && cargo test --release --test test_join_stream_stream 2>&1 | tail -3 && cargo test --release --test sharding_parity -- --test-threads=1 2>&1 | tail -5 && cargo test --release --test cross_shard_enrich_from_table 2>&1 | tail -3 && cargo test --release --test cross_shard_tt_cascade_ownership 2>&1 | tail -3 && cargo test --release --test cascade_metrics 2>&1 | tail -3 && cargo test --release --lib 2>&1 | tail -3 && bash -c 'test $(grep -crE "#\\[ignore = \"56-W3\"" tests/ 2>/dev/null) -eq 0' && bash -c 'test $(grep -crE "#\\[ignore = \"56-W4\"" tests/ 2>/dev/null) -ge 2' && bash -c 'test $(grep -c "ssj_insert_at_shard" src/engine/pipeline.rs) -ge 2'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --release` + `cargo build --release --features state-inmem` clean.
    - `cargo test --release --test cross_shard_stream_stream_join` — 1 passed, 0 ignored.
    - `cargo test --release --test test_join_stream_stream` (co-located SSJ) — unregressed.
    - `cargo test --release --test sharding_parity -- --test-threads=1` — ≥ 13 passed, 0 failed.
    - `cargo test --release --test cross_shard_enrich_from_table` — 1 passed (Wave 2 unregressed).
    - Phase 55 tests: `cross_shard_tt_cascade_ownership` 2/2, `cascade_metrics` 2/2 unregressed.
    - `cargo test --release --lib` — baseline preserved.
    - `grep -rcE '#\[ignore = "56-W3"' tests/` — 0.
    - `grep -rcE '#\[ignore = "56-W4"' tests/` — ≥ 2 (perf smoke tests remain until Wave 4).
    - `grep -c "ssj_insert_at_shard" src/engine/pipeline.rs` — ≥ 2 (1 def + ≥ 1 call inside SSJ eval block).
    - No `with_entity_mut.*state_key` pattern in the SSJ eval block (visually confirmed via line-range grep).
    - Commit message matches `feat(56-W3b): StreamStreamJoin routes via hash(join.on) shard (TPC-CORR-09)`.
  </acceptance_criteria>
  <done>StreamStreamJoin buffer lives on hash(join.on)%N. Cross-shard L×R joins work. Co-located fast path preserved. 56-W3 markers all removed. All Phase 56 correctness gates GREEN pre-perf.</done>
</task>

</tasks>

<verification>
1. **TPC-CORR-04 relaxed:** register() no longer errors on mismatch; CrossShardJoinWarning flows through tracing + metrics + signals + /debug/warnings.
2. **StreamStreamJoin cross-shard routing:** eval block calls ssj_insert_at_shard; buffer lives on hash(join.on)%N shard.
3. **D-B5 co-located preserved:** no extra hops when shard_key=join.on on both sides.
4. **All Phase 56 Wave-0 RED tests (except W4) GREEN.**
5. **Phase 51 warning endpoint tests + same-shard SSJ tests:** unregressed.
6. **Phase 55 tests:** unregressed.
7. **sharding_parity at N=8:** enrich + SSJ sub-cases both GREEN.
8. **Ignore markers: 56-W2, 56-W3 = 0; 56-W4 ≥ 2 (Wave 4 perf).**
</verification>

<success_criteria>
Wave 3 done when:
- [ ] `CrossShardJoinWarning` struct + `validate_shard_keys -> Vec<CrossShardJoinWarning>`
- [ ] register() loop emits warnings (tracing + metric + signals) — no Err
- [ ] `emit_cross_shard_join_warning` in signals.rs with join_id dedupe
- [ ] /debug/warnings JSON gains `cross_shard_joins` array
- [ ] StreamStreamJoin eval routes via ssj_insert_at_shard
- [ ] `cross_shard_stream_stream_join` (SC-2) GREEN
- [ ] `register_crossshard_join_warning` × 2 (SC-3) GREEN
- [ ] `sharding_parity` ≥ 13 GREEN at N=8
- [ ] Phase 51 warning tests × 4 unregressed
- [ ] Phase 51 test_join_stream_stream (co-located) unregressed
- [ ] Phase 55 integration tests unregressed
- [ ] lib baseline preserved
- [ ] All 56-W3 markers removed from tests/
- [ ] JoinShardKeyMismatch retained with #[deprecated] back-compat
</success_criteria>

<output>
After completion, create `.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-03-SUMMARY.md` with:
- Exact lib test counts at wave close (both feature backends)
- Grep evidence for acceptance criteria
- Any apply_ssj_insert signature adjustments made in Task 2 (event_time_ms carry-through) and why
- Commit hash range (Task 1 + Task 2 = 2 commits)
- Wave 4 handoff: exact perf-gate invocation (bench script args, BEAVA_SHARD_INBOX_SIZE override, scenario variant), expected ≥ 1,059,261 EPS floor, tracing of the new BEAVA_ENRICH_CROSSSHARD_SCENARIO env var
- 56-NEXT candidates surfaced during this wave (e.g., cross-target parallel dispatch if Wave 4 perf is marginal)
</output>
