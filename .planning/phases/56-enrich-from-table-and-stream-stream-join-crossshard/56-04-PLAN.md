---
phase: 56
plan: 04
type: execute
wave: 4
depends_on: [56-03]
files_modified:
  - benchmark/fraud-pipeline/run_bench.sh
  - benchmark/fraud-pipeline/scenario_crossshard_enrich.py
  - tests/crossshard_enrich_perf_smoke.rs
  - scripts/verify-crossshard-metrics.sh
  - .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-PERF-GATE.md
  - .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-VERIFICATION.md
  - .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/deferred-items.md
  - .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/perf-evidence/.gitkeep
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - TPC-CORR-04
  - TPC-CORR-08
  - TPC-CORR-09
tags:
  - wave-4
  - perf-gate
  - verification
  - phase-close
  - ship-gate
must_haves:
  truths:
    - "A new bench scenario variant forces ≥1 cross-shard EnrichFromTable per event by registering a Countries source-table with shard_key=country_code while Txns uses shard_key=user_id"
    - "benchmark/fraud-pipeline/run_bench.sh accepts BEAVA_ENRICH_CROSSSHARD_SCENARIO=1 and wires the new scenario through the bench harness"
    - "Perf gate result ≥ 1,059,261 EPS under MODE=complex DURATION=60 CPUS=8 CLIENTS=8 BEAVA_SHARD_INBOX_SIZE=1048576 BEAVA_ENRICH_CROSSSHARD_SCENARIO=1 OR contingency path is explicitly invoked"
    - "SC-4 p99 latency smoke test (crossshard_enrich_p99_under_2x_baseline) flips GREEN; BASELINE_P99_MICROS constant updated to the measured Phase 55 value"
    - "SC-5 perf-gate test (crossshard_enrich_eps_floor) flips GREEN behind BEAVA_PERF_GATE=1 env"
    - "56-PERF-GATE.md captures the measured EPS, floor, headroom, delta vs Phase 55 baseline; perf-evidence/<ts>.txt contains raw bench stdout"
    - "56-VERIFICATION.md mirrors 55-VERIFICATION.md format — per-SC rows (SC-1..SC-5) with evidence + status"
    - "scripts/verify-crossshard-metrics.sh confirms beava_enrich_cross_shard_total, beava_enrich_intra_shard_total, beava_enrich_missing_total, beava_ssj_cross_shard_total, beava_crossshard_joins_registered_total all appear in /metrics after a test run"
    - "Phase 56 engineering-complete state recorded: ROADMAP.md Phase 56 row flipped Complete; STATE.md Current Position updated to next phase; deferred-items.md lists any 56-NEXT items"
    - "All 56-W{0..4} ignore markers removed from tests/"
  artifacts:
    - path: benchmark/fraud-pipeline/scenario_crossshard_enrich.py
      provides: "Bench scenario definition forcing cross-shard enrichment per event"
      min_lines: 40
      contains: "shard_key=\"user_id\"|shard_key=\"country_code\""
    - path: benchmark/fraud-pipeline/run_bench.sh
      provides: "BEAVA_ENRICH_CROSSSHARD_SCENARIO=1 branch sourcing scenario_crossshard_enrich.py + preserving the Phase 55 inbox sizing contract"
      contains: "BEAVA_ENRICH_CROSSSHARD_SCENARIO"
    - path: tests/crossshard_enrich_perf_smoke.rs
      provides: "BASELINE_P99_MICROS updated to measured value; both ignore markers removed"
    - path: scripts/verify-crossshard-metrics.sh
      provides: "Shell-invocable check that /metrics response contains all 5 Phase-56 counter names (similar to scripts/verify-source-lsn-echoed.sh from Phase 55)"
      contains: "beava_enrich_cross_shard_total"
    - path: .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-PERF-GATE.md
      provides: "Perf-gate evidence document. Candidate EPS, floor EPS, headroom, baseline (Phase 55 = 1,246,190), delta. PASSED/FAILED verdict."
      contains: "1,059,261"
      min_lines: 40
    - path: .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-VERIFICATION.md
      provides: "Per-SC verification doc mirroring 55-VERIFICATION.md"
      contains: "TPC-CORR-08|TPC-CORR-09"
      min_lines: 60
    - path: .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/deferred-items.md
      provides: "56-NEXT list"
    - path: .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/perf-evidence/
      provides: "Raw bench stdout artifact"
    - path: .planning/ROADMAP.md
      provides: "Phase 56 row marked complete + plan checklist checked"
    - path: .planning/STATE.md
      provides: "Current Position advanced; Phase 56 entry in Accumulated Context"
  key_links:
    - from: benchmark/fraud-pipeline/run_bench.sh
      to: benchmark/fraud-pipeline/scenario_crossshard_enrich.py
      via: "Shell env BEAVA_ENRICH_CROSSSHARD_SCENARIO=1 selects the scenario .py module"
      pattern: "scenario_crossshard_enrich"
    - from: scripts/verify-crossshard-metrics.sh
      to: /metrics
      via: "Start server with BEAVA_SHARDS=4; push 100 events forcing cross-shard; curl /metrics; grep for all 5 counter names; assert non-zero for the two cross-shard counters"
      pattern: "curl.*metrics|beava_enrich_cross_shard"
    - from: .planning/phases/56-*/56-VERIFICATION.md
      to: 56-PERF-GATE.md
      via: "SC-5 row references the PERF-GATE doc for candidate EPS + floor"
      pattern: "56-PERF-GATE.md"
user_setup: []
---

<objective>
Close Phase 56. Measure the perf gate (≥ 1,059,261 EPS on MODE=complex N=8 with forced cross-shard enrichment), record per-SC verification, write the phase SUMMARY, flip ROADMAP.md, update STATE.md, file 56-NEXT items.

This wave does NOT write new production code for the correctness path — Waves 1–3 already delivered TPC-CORR-04 relaxation + TPC-CORR-08 + TPC-CORR-09. This wave:

1. Extends the bench harness with a scenario variant that forces ≥1 cross-shard EnrichFromTable per event.
2. Runs the perf gate.
3. Captures evidence.
4. Writes VERIFICATION + PERF-GATE docs.
5. Updates ROADMAP.md + STATE.md.
6. Files 56-NEXT items.

If the perf gate fails (< 1,059,261 EPS), this wave invokes the **contingency path** rather than silently accepting regression. Contingency menu:
- **C1 (preferred):** cross-target parallel dispatch in `read_entity_batch_at_shard` callsite — wrap per-target flush in `std::thread::scope` so multiple targets fire oneshots concurrently. Measured delta ~5–10%. Lands as a Task-3 addendum if needed.
- **C2:** defer the SC-4 p99 assertion sharpening to 56-NEXT (document in VERIFICATION as `human_needed`) while the EPS floor remains the blocking gate.
- **C3 (last resort):** relax the 85% floor to 80% = 996,952 EPS and document the Phase 55→56 cross-shard overhead delta explicitly. Requires user sign-off per the ROADMAP "non-negotiable" clause — surface as a checkpoint.

Purpose: evidence-bearing close; every success criterion (SC-1..SC-5) transitions to `passed` or `human_needed` with grep-verifiable evidence.

Output:
- Bench scenario variant committed
- Perf-gate evidence (raw stdout file + structured 56-PERF-GATE.md)
- 56-VERIFICATION.md per-SC table
- 56-SUMMARY.md for this wave
- ROADMAP.md + STATE.md updates
- deferred-items.md with 56-NEXT entries
- All Wave 0 RED tests (56-W{0..4}) removed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-CONTEXT.md
@.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-00-PLAN.md
@.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-01-PLAN.md
@.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-02-PLAN.md
@.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-03-PLAN.md
@.planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-04-PLAN.md
@.planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-04-SUMMARY.md
@.planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-PERF-GATE.md
@.planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-VERIFICATION.md
@benchmark/fraud-pipeline/run_bench.sh
@scripts/verify-source-lsn-echoed.sh

<interfaces>
From benchmark/fraud-pipeline/run_bench.sh (CURRENT — Phase 55 invocation; Wave 4 adds scenario branch):

```bash
# Canonical Phase 55 invocation:
MODE=complex DURATION=60 CPUS=8 CLIENTS=8 BEAVA_SHARD_INBOX_SIZE=1048576 \
  bash benchmark/fraud-pipeline/run_bench.sh
# Produces 1,246,190 EPS on the reference laptop.
```

From scripts/verify-source-lsn-echoed.sh (Phase 55 — template for scripts/verify-crossshard-metrics.sh):

```bash
#!/usr/bin/env bash
set -euo pipefail
# Phase 55 source_lsn echo verification — starts a test server, pushes rows,
# curls /metrics, greps for expected counter.
```

From .planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-VERIFICATION.md (format to copy):

```markdown
| SC | Req             | Evidence                                    | Status          |
|----|-----------------|---------------------------------------------|-----------------|
| 1  | TPC-CORR-07     | cross_shard_tt_cascade_ownership 2/2; ...   | passed          |
| 6  | TPC-CORR-07     | boot_rematerialization 5/5 at N=1; ...      | human_needed    |
```

From .planning/ROADMAP.md Phase 56 row (needs update at wave close):

```
### Phase 56: 56-enrich-from-table-and-stream-stream-join-crossshard
...
**Plans**: TBD   ← to be replaced with "5 plans" + checklist
```

And progress table row:

```
| 56. EnrichFromTable + StreamStreamJoin cross-shard | 0/? | Not started ... | — |
```

→ flip to 5/5 Complete at wave close.

From .planning/STATE.md (headers to update):

- `stopped_at:` update to Phase 56 close state
- `progress.completed_phases`: +1
- `progress.completed_plans`: +5 (56-00..56-04)
- Accumulated Context: add Phase 56 section
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench client → server | localhost test harness; no external input. |
| /metrics scrape → Prometheus | Read-only endpoint; no mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-04-01 | Repudiation | Perf-evidence file absent or dishonest | mitigate | Raw stdout tee'd to perf-evidence/<ts>.txt + committed alongside 56-PERF-GATE.md. Follows Phase 55 convention. |
| T-56-04-02 | Information Disclosure | Bench client or scenario leaks secrets | accept | Synthetic data only; no secrets. |
| T-56-04-03 | Denial of Service | Perf gate itself exhausts laptop memory | mitigate | BEAVA_SHARD_INBOX_SIZE=1048576 is the Phase 55 value — known to fit 10-core laptop with 64 GB RAM (Phase 55 precedent). DURATION=60 caps wall-clock exposure. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement cross-shard enrichment bench scenario + run perf gate + capture evidence</name>
  <files>
    benchmark/fraud-pipeline/scenario_crossshard_enrich.py,
    benchmark/fraud-pipeline/run_bench.sh,
    tests/crossshard_enrich_perf_smoke.rs,
    scripts/verify-crossshard-metrics.sh,
    .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-PERF-GATE.md,
    .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/perf-evidence/.gitkeep
  </files>
  <read_first>
    - benchmark/fraud-pipeline/run_bench.sh full file (how scenarios are selected; inventory env vars)
    - benchmark/fraud-pipeline/*.py (existing scenario shapes — copy the closest parallel)
    - .planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-04-PLAN.md Task 1 (Phase 55's perf-gate landing recipe)
    - .planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-PERF-GATE.md (evidence format)
    - .planning/phases/55-stream-table-cascade-crossshard-and-source-tables/perf-evidence/20260420T220619Z.txt (raw-stdout template)
    - scripts/verify-source-lsn-echoed.sh (shape for scripts/verify-crossshard-metrics.sh)
    - tests/crossshard_enrich_perf_smoke.rs (Wave 0 stub with BASELINE_P99_MICROS=50)
    - .planning/STATE.md §Phase 54-05 baseline notes (inbox sizing rationale — reuse 1048576)
    - .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-01-SUMMARY.md..56-03-SUMMARY.md (post-waves state)
  </read_first>
  <behavior>
    Behavior 1 (scenario): `scenario_crossshard_enrich.py` declares:
      - `@bv.source_table(key="country_code", entity_ttl=None)` `Countries` with fields {gdp_usd: i64, continent: string}
      - Pre-loaded with 50 rows keyed by randomly distributed country_code strings (ensures multiple shards own rows at N=8).
      - `@bv.stream(shard_key="user_id")` `Txns` with one `enrich_from(Countries, on=["country_code"], right_fields=[("gdp_usd","enriched_gdp"),("continent","enriched_continent")])` feature.
      - Bench driver (Python) generates events `{user_id: Zipf-1.2 choice, country_code: uniform 50-choice, amount: uniform}`. Because country_code is uniform over 50 countries while shard_key=user_id, ~(1 − 1/8) = 87.5% of events trigger cross-shard enrichment at N=8.
    Behavior 2 (run_bench.sh branch): when `BEAVA_ENRICH_CROSSSHARD_SCENARIO=1` in env, the harness sources `scenario_crossshard_enrich.py` instead of the default complex scenario. All other env vars (MODE, DURATION, CPUS, CLIENTS, BEAVA_SHARD_INBOX_SIZE) flow through unchanged.
    Behavior 3 (perf gate run):
      - Command: `MODE=complex DURATION=60 CPUS=8 CLIENTS=8 BEAVA_SHARD_INBOX_SIZE=1048576 BEAVA_ENRICH_CROSSSHARD_SCENARIO=1 bash benchmark/fraud-pipeline/run_bench.sh 2>&1 | tee .planning/phases/56-*/perf-evidence/$(date -u +%Y%m%dT%H%M%SZ).txt`
      - Parse stdout for aggregate EPS.
      - Compute: candidate_eps, floor=1_059_261, baseline=1_246_190, headroom=(candidate − floor), delta_vs_baseline_pct = (candidate − baseline)/baseline × 100.
      - Verdict: PASSED if candidate ≥ floor; FAILED otherwise (invoke contingency).
    Behavior 4 (56-PERF-GATE.md): structured doc in 55-PERF-GATE.md's format:
      - YAML-style header with date, hardware, scenario
      - Candidate / Floor / Baseline / Headroom / Delta table (matches 55-PERF-GATE.md structure)
      - Raw evidence file reference
      - Verdict PASSED/FAILED
      - If FAILED: contingency ladder + recommended next step
    Behavior 5 (perf smoke test): crossshard_enrich_p99_under_2x_baseline and crossshard_enrich_eps_floor — fill in the bodies from Wave 0 stubs:
      - p99 test spawns N=4 in-process, registers Countries + Txns per scenario, pushes 10_000 events, captures per-event wall-clock via Instant::now(). Computes p99. Updates BASELINE_P99_MICROS to a value measured against Phase 55 HEAD of this workload WITHOUT the cross-shard variant (a brief 1-minute setup at wave start). Asserts `p99 ≤ 2 * BASELINE_P99_MICROS`.
      - eps_floor test short-circuits unless BEAVA_PERF_GATE=1; runs the bench script as a subprocess via std::process::Command; parses aggregate EPS; asserts ≥ 1_059_261.
    Behavior 6 (verify-crossshard-metrics.sh): shell script that:
      - Starts a BEAVA_SHARDS=4 server in the background (PID captured)
      - Registers Countries + Txns with enrich feature (via python SDK or curl inline POST)
      - Pushes ~100 events with uniform country_code + random user_id
      - curl http://127.0.0.1:<port>/metrics
      - greps for all 5 Phase 56 counter names — must be present
      - greps for beava_enrich_cross_shard_total > 0 AND beava_enrich_intra_shard_total >= 0
      - teardown (kill server)
      - exit 0 on success; non-zero + message on failure
    Behavior 7 (contingency if FAILED):
      - Measure delta vs baseline.
      - If delta ≥ -15% (between floor and baseline), contingency C1 (parallel dispatch): land a 10-15 LOC patch in pipeline.rs's coalesce flush replacing sequential per-target loop with `std::thread::scope` spawning per-target closures. Re-measure.
      - If re-measure still FAILS, contingency C2 or C3 per plan frontmatter. C3 requires a checkpoint:decision — escalate to user via plan-level checkpoint (see Task 2).
    Invariant: 56-PERF-GATE.md exists + references "1,059,261" (floor) at least 3 times.
    Invariant: perf-evidence/<ts>.txt is committed alongside the doc.
    Invariant: `grep -rcE "#\[ignore = \"56-W[0-4]\"" tests/` returns 0 at wave close.
  </behavior>
  <action>
    Step 1 — Create `benchmark/fraud-pipeline/scenario_crossshard_enrich.py` per Behavior 1. Inspect neighboring scenario files to match style.

    Step 2 — Extend `benchmark/fraud-pipeline/run_bench.sh` with the BEAVA_ENRICH_CROSSSHARD_SCENARIO branch:

        if [ "${BEAVA_ENRICH_CROSSSHARD_SCENARIO:-0}" = "1" ]; then
          SCENARIO=benchmark/fraud-pipeline/scenario_crossshard_enrich.py
          echo "[bench] using cross-shard enrichment scenario (Phase 56 perf gate)"
        else
          SCENARIO=benchmark/fraud-pipeline/scenario_complex.py   # existing default; grep for actual name
        fi
        # Then pass $SCENARIO into the server-register + client-generate paths.

    Step 3 — Run the perf gate:

        mkdir -p .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/perf-evidence
        TS=$(date -u +%Y%m%dT%H%M%SZ)
        EVIDENCE=.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/perf-evidence/${TS}.txt
        MODE=complex DURATION=60 CPUS=8 CLIENTS=8 \
          BEAVA_SHARD_INBOX_SIZE=1048576 \
          BEAVA_ENRICH_CROSSSHARD_SCENARIO=1 \
          bash benchmark/fraud-pipeline/run_bench.sh 2>&1 | tee "$EVIDENCE"

        CANDIDATE=$(grep -oE "Aggregate EPS: [0-9,]+" "$EVIDENCE" | tr -d , | awk '{print $3}')
        FLOOR=1059261
        BASELINE=1246190

        if [ "$CANDIDATE" -ge "$FLOOR" ]; then
          echo "PASSED"
        else
          echo "FAILED — invoking contingency ladder"
          # See Behavior 7.
        fi

    Step 4 — Write `.planning/phases/56-*/56-PERF-GATE.md`:

        # Phase 56 Perf Gate

        **Date:** 2026-04-21 (placeholder — use run date)
        **Hardware:** Darwin arm64 M-series, 10 cores (reference laptop; matches Phase 55 baseline hardware)
        **Scenario:** scenario_crossshard_enrich — Countries source-table (shard_key=country_code) + Txns stream (shard_key=user_id) + 1 EnrichFromTable per event. ~87.5% cross-shard rate at N=8.

        ## Result

        | Field                             | Value                |
        |-----------------------------------|----------------------|
        | Baseline (Phase 55 close)         | 1,246,190 EPS        |
        | Gate floor (85% × baseline)       | **1,059,261 EPS**    |
        | Candidate (Phase 56 HEAD)         | **<MEASURED> EPS**   |
        | Headroom over floor               | <delta> EPS (<pct>%) |
        | Delta vs baseline                 | <delta> EPS (<pct>%) |
        | Gate result                       | **PASSED / FAILED**  |

        60-second measurement window. Inbox sizing: BEAVA_SHARD_INBOX_SIZE=1048576 (Phase 55 operating point).
        Full stdout: `perf-evidence/<ts>.txt`.

        ## Cross-Shard Metric Snapshot

        <scraped /metrics excerpt showing beava_enrich_cross_shard_total, beava_enrich_intra_shard_total, beava_enrich_missing_total, beava_ssj_cross_shard_total, beava_crossshard_joins_registered_total>

        ## If FAILED: Contingency Path Invoked

        <populate only if FAILED; document which contingency was invoked and the re-run result>

    Step 5 — Update tests/crossshard_enrich_perf_smoke.rs:
      - Measure a Phase 55 baseline p99 (brief in-process benchmark WITHOUT cross-shard variant) and set BASELINE_P99_MICROS to that measured value. If Wave 0 stub value (50) is accurate, keep; else update with a comment citing the measurement.
      - Fill in the test bodies per Behavior 5.
      - Remove both `#[ignore = "56-W4"]` markers.

    Step 6 — Write `scripts/verify-crossshard-metrics.sh`:

        #!/usr/bin/env bash
        # Phase 56 cross-shard metrics surface check.
        # Starts a test server with BEAVA_SHARDS=4, registers a cross-shard
        # enrichment pipeline, pushes ~100 events, scrapes /metrics, greps
        # for the 5 Phase-56 counter names.
        set -euo pipefail
        PORT=$(python3 -c "import socket;s=socket.socket();s.bind(('',0));print(s.getsockname()[1])")
        BEAVA_SHARDS=4 BEAVA_PORT=$PORT tally serve &
        PID=$!
        trap "kill $PID 2>/dev/null" EXIT
        sleep 2
        # Register Countries + Txns; push events; curl /metrics
        curl -s http://127.0.0.1:$PORT/metrics | \
          grep -E "^beava_enrich_cross_shard_total|^beava_enrich_intra_shard_total|^beava_enrich_missing_total|^beava_ssj_cross_shard_total|^beava_crossshard_joins_registered_total" | \
          wc -l | grep -q '^5$' || { echo "not all 5 counters present"; exit 1; }
        echo "all 5 Phase 56 counters present"

        chmod +x scripts/verify-crossshard-metrics.sh

    Step 7 — Run final gates:
        cargo test --release --test crossshard_enrich_perf_smoke                           # 1 ignored (SC-5 under BEAVA_PERF_GATE env), 1 passed (SC-4)
        BEAVA_PERF_GATE=1 cargo test --release --test crossshard_enrich_perf_smoke -- --ignored --test-threads=1    # 1 passed (SC-5)
        cargo test --release --tests -- --test-threads=1 2>&1 | tail -10                   # all GREEN
        cargo test --release --lib                                                          # baseline preserved
        bash scripts/verify-crossshard-metrics.sh                                           # exit 0
        grep -rcE "#\[ignore = \"56-W[0-4]\"" tests/                                        # 0

    Step 8 — Commit: `perf(56-W4): run cross-shard enrichment perf gate + commit 56-PERF-GATE.md + verify-crossshard-metrics.sh + bench scenario`

    DO NOT: skip the verify-crossshard-metrics.sh smoke. It's the quick operator check that the Wave 1 metric surface survived through Wave 3.
    DO NOT: publish a FAILED gate without invoking contingency. The plan's frontmatter pre-approves C1; C2 needs documentation; C3 escalates to a checkpoint.
  </action>
  <verify>
    <automated>ls .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/perf-evidence/*.txt 2>&1 | head -3 && cat .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-PERF-GATE.md 2>&1 | head -30 && cargo test --release --test crossshard_enrich_perf_smoke 2>&1 | tail -5 && bash scripts/verify-crossshard-metrics.sh 2>&1 | tail -5 && bash -c 'test $(grep -crE "#\\[ignore = \"56-W[0-4]\"" tests/ 2>/dev/null) -eq 0' && bash -c 'test $(grep -c "1,059,261\|1059261" .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-PERF-GATE.md) -ge 3' && cargo test --release --lib 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `benchmark/fraud-pipeline/scenario_crossshard_enrich.py` exists with source_table + stream declarations per Behavior 1.
    - `benchmark/fraud-pipeline/run_bench.sh` contains BEAVA_ENRICH_CROSSSHARD_SCENARIO branch.
    - `56-PERF-GATE.md` exists, references "1,059,261" (floor) ≥ 3 times, references the baseline "1,246,190" ≥ 2 times, has a verdict line "PASSED" or "FAILED" (+ contingency section if FAILED).
    - `perf-evidence/<ts>.txt` exists with raw bench stdout.
    - `scripts/verify-crossshard-metrics.sh` executable + exits 0.
    - `tests/crossshard_enrich_perf_smoke.rs` has zero `#[ignore = "56-W4"]` markers; one test passes in default run (SC-4); one test passes under `BEAVA_PERF_GATE=1 --ignored` (SC-5).
    - `grep -rcE "#\[ignore = \"56-W[0-4]\"" tests/` → 0.
    - `cargo test --release --lib` baseline preserved (from Wave 3 close).
    - Commit message matches `perf(56-W4): run perf gate + commit 56-PERF-GATE.md`.
  </acceptance_criteria>
  <done>Perf gate measured; evidence committed; verdict documented. 5 Phase-56 counters visible at /metrics. All SC-4 + SC-5 tests pass. Zero 56-W markers in tests/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write 56-VERIFICATION.md + deferred-items.md + update ROADMAP.md + STATE.md + close phase</name>
  <files>
    .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-VERIFICATION.md,
    .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/deferred-items.md,
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-VERIFICATION.md (format template — per-SC rows)
    - .planning/phases/55-stream-table-cascade-crossshard-and-source-tables/deferred-items.md (55-NEXT format)
    - .planning/ROADMAP.md (Phase 56 row — update to "Complete" pattern like Phase 55 row)
    - .planning/STATE.md (Current Position + progress + Accumulated Context + Session Continuity sections)
    - .planning/phases/56-*/56-00-SUMMARY.md..56-03-SUMMARY.md (for wave-level evidence citations)
    - .planning/phases/56-*/56-PERF-GATE.md (Task 1 output — SC-5 evidence)
  </read_first>
  <behavior>
    Behavior 1 (56-VERIFICATION.md): mirror 55-VERIFICATION.md format:

        | SC | Req             | Evidence                                                              | Status           |
        |----|-----------------|-----------------------------------------------------------------------|------------------|
        | 1  | TPC-CORR-08     | cross_shard_enrich_from_table 1/1; sharding_parity enrich sub 11/11   | passed           |
        | 2  | TPC-CORR-09     | cross_shard_stream_stream_join 1/1; sharding_parity SSJ sub 11/11     | passed           |
        | 3  | TPC-CORR-04     | register_crossshard_join_warning 2/2; test_warnings_* unregressed     | passed           |
        | 4  | TPC-CORR-08/09  | crossshard_enrich_p99_under_2x_baseline passed; p99 = <X> µs ≤ 2×<Y>  | passed           |
        | 5  | TPC-CORR-08/09  | 56-PERF-GATE.md EPS = <CANDIDATE> ≥ 1,059,261 floor                   | passed           |

    Include a "Requirements Closure" section:

        - TPC-CORR-04 (relaxed): closed — register() returns Ok + CrossShardJoinWarning surfaced
        - TPC-CORR-08: closed — EnrichFromTable cross-shard GREEN on SC-1 + SC-4 + SC-5
        - TPC-CORR-09: closed — StreamStreamJoin cross-shard GREEN on SC-2 + SC-4 + SC-5

    Include a "Known Pre-existing Issues" section citing any test_concurrent.rs / source_table_cdc.rs carry-over from Phase 55.

    Behavior 2 (deferred-items.md): capture 56-NEXT items surfaced during Waves 1–4. Minimum candidates:
      1. Cross-target parallel dispatch in read_entity_batch_at_shard (if Wave 4 perf was tight, promote this from 56-NEXT to a 57-00 Wave if user approves — otherwise leave as 56-NEXT).
      2. SSJ buffer TTL eviction improvements (intertwined with Phase 57 retraction).
      3. ssj-<join_id>/ partition-per-join vs shared partition decision revisit post-Wave-4 perf data.
      4. Async / non-blocking cross-shard enrichment reads if SC-4 headroom narrowed.
      5. /debug/warnings cross_shard_joins TTL / pruning — warnings never expire today.

    Behavior 3 (ROADMAP.md Phase 56 row): update from "0/?" Not started to "5/5 Complete YYYY-MM-DD". Fill plan checklist:

        **Plans**: 5 plans
        Plans:
        - [x] 56-00-PLAN.md — Wave 0: RED tests for SC-1..SC-5 (TPC-CORR-04 relax, add TPC-CORR-08 + TPC-CORR-09)
        - [x] 56-01-PLAN.md — Wave 1: ShardOp::ReadEntityAt / ReadEntityBatch / SsjInsert + Shard methods + 5 metrics
        - [x] 56-02-PLAN.md — Wave 2: EnrichFromTable cross-shard read with same-shard fast path + per-batch coalesce
        - [x] 56-03-PLAN.md — Wave 3: StreamStreamJoin routes via hash(join.on)%N + TPC-CORR-04 relaxation + /debug/warnings
        - [x] 56-04-PLAN.md — Wave 4: perf gate (≥ 1,059,261 EPS) + VERIFICATION + close

    Update Progress table row to reflect Complete status.

    Behavior 4 (STATE.md updates):
      - `stopped_at:`: "Phase 56 closed YYYY-MM-DD (EPS = <candidate>); starting Phase 57 (retraction across crossshard joins)"
      - `progress.completed_phases`: +1 (from 7 to 8)
      - `progress.completed_plans`: +5 (from 51 to 56)
      - `progress.percent`: recompute
      - Current Position block: advance to Phase 57
      - Accumulated Context: add a new section mirroring existing entries with key Phase 56 findings (waves, EPS, counters added, 56-NEXT items)
      - Session Continuity: update "Stopped at" and "Next action"

    Invariant: grep "Phase 56.*Complete" .planning/ROADMAP.md returns ≥ 2 (row + progress table).
    Invariant: grep "56 " .planning/STATE.md shows updated Current Position.
  </behavior>
  <action>
    Step 1 — Write 56-VERIFICATION.md per Behavior 1 with actual measured values from Task 1.

    Step 2 — Write deferred-items.md per Behavior 2.

    Step 3 — Update ROADMAP.md per Behavior 3. Carefully preserve the rest of the file (Phases 57-64).

    Step 4 — Update STATE.md per Behavior 4. Preserve all existing sections; add Phase 56 to Accumulated Context.

    Step 5 — Sanity gates:
        grep -c "TPC-CORR-08\|TPC-CORR-09" .planning/phases/56-*/56-VERIFICATION.md             # ≥ 3
        grep -c "1,059,261" .planning/phases/56-*/56-VERIFICATION.md                             # ≥ 1
        grep "Phase 56.*Complete" .planning/ROADMAP.md                                            # ≥ 2
        grep -c "^### Phase 56 Plan " .planning/STATE.md || grep -c "Phase 56" .planning/STATE.md # ≥ 1 new
        cargo test --release --tests -- --test-threads=1 2>&1 | tail -10                         # all GREEN
        cargo test --release --lib                                                                # baseline preserved

    Step 6 — Final commit: `docs(56-W4): 56-VERIFICATION + deferred-items + ROADMAP + STATE — Phase 56 engineering-complete`

    DO NOT: create a SUMMARY.md in this task — 56-04-SUMMARY.md is the post-execution artefact for the executor's self-check output section.
    DO NOT: modify phase entries for 57-64 in ROADMAP.
  </action>
  <verify>
    <automated>test -f .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-VERIFICATION.md && test -f .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/deferred-items.md && bash -c 'test $(grep -c "TPC-CORR-08" .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-VERIFICATION.md) -ge 1' && bash -c 'test $(grep -c "TPC-CORR-09" .planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-VERIFICATION.md) -ge 1' && bash -c 'test $(grep -c "Phase 56.*Complete\|56.*5/5" .planning/ROADMAP.md) -ge 1' && cargo test --release --tests -- --test-threads=1 2>&1 | tail -5 && cargo test --release --lib 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `56-VERIFICATION.md` exists with SC-1..SC-5 rows, TPC-CORR-04 + TPC-CORR-08 + TPC-CORR-09 referenced, Requirements Closure section.
    - `deferred-items.md` exists with ≥ 3 56-NEXT entries.
    - ROADMAP.md Phase 56 row shows "5/5 Complete" pattern + plan checklist all checked.
    - STATE.md Current Position advanced past Phase 56; Accumulated Context gains Phase 56 section; progress counts updated.
    - `cargo test --release --tests -- --test-threads=1` — all GREEN.
    - `cargo test --release --lib` — baseline preserved.
    - `grep -c "1,059,261" .planning/phases/56-*/56-VERIFICATION.md` ≥ 1.
    - Final commit present with message matching `docs(56-W4): 56-VERIFICATION`.
  </acceptance_criteria>
  <done>Phase 56 engineering-complete. Documentation, verification, roadmap, state all updated. 56-NEXT items filed.</done>
</task>

</tasks>

<verification>
1. **Perf gate measured and evidence committed:** 56-PERF-GATE.md + perf-evidence/<ts>.txt exist.
2. **Metrics surface verified:** scripts/verify-crossshard-metrics.sh exits 0.
3. **All 5 Phase-56 counters scrape via /metrics.**
4. **Tests:** 56-W* markers all removed; SC-4 + SC-5 GREEN; lib + integration baselines preserved.
5. **Documentation:** VERIFICATION + PERF-GATE + deferred-items + ROADMAP + STATE all updated.
6. **Requirements closure:** TPC-CORR-04 (relaxed), TPC-CORR-08, TPC-CORR-09 all `passed` or `human_needed` with evidence.
</verification>

<success_criteria>
Wave 4 done when:
- [ ] scenario_crossshard_enrich.py + run_bench.sh branch committed
- [ ] Perf gate run + evidence file + 56-PERF-GATE.md committed
- [ ] Candidate EPS ≥ 1,059,261 OR contingency path engaged + documented
- [ ] crossshard_enrich_p99_under_2x_baseline + eps_floor GREEN
- [ ] scripts/verify-crossshard-metrics.sh exits 0 (5 counters present)
- [ ] 56-VERIFICATION.md with per-SC rows
- [ ] deferred-items.md with ≥ 3 56-NEXT entries
- [ ] ROADMAP.md Phase 56 marked Complete; plan checklist 5/5
- [ ] STATE.md Current Position + progress + Accumulated Context + Session Continuity updated
- [ ] All 56-W* ignore markers removed
- [ ] cargo test full suite + lib baseline GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/56-enrich-from-table-and-stream-stream-join-crossshard/56-04-SUMMARY.md` with:
- Final perf-gate result (candidate / floor / baseline / delta)
- Whether contingency path was invoked; if so, which one and the result
- Lib + integration test baselines at wave close (both feature backends)
- 56-NEXT items filed (bullet list from deferred-items.md)
- Commit hash range for Wave 4 (2 commits expected)
- Phase 56 aggregate outcomes: requirements closed (TPC-CORR-04 relaxation + TPC-CORR-08 + TPC-CORR-09), LOC delta, metric counters added, ShardOp variants added, helper functions added.
- Phase 56 commit range across all waves (W0 first commit → W4 last commit).
- Handoff to Phase 57: retraction across cross-shard joins — pending-retraction markers from Phase 55 DELETE path + SSJ tombstone propagation. Key files: src/engine/operators.rs StreamJoinBuffer, src/state/event_log.rs PendingRetraction.
</output>
