---
phase: 58
plan: 04
type: execute
wave: 4
depends_on:
  - 58-01
  - 58-02
  - 58-03
files_modified:
  - .planning/phases/58-tokio-connection-handling-rewrite/58-PERF-GATE.md
  - .planning/phases/58-tokio-connection-handling-rewrite/58-VERIFICATION.md
  - .planning/phases/58-tokio-connection-handling-rewrite/perf-evidence
  - .planning/phases/58-tokio-connection-handling-rewrite/samply-after
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - TPC-PERF-08
tags: [perf-gate, samply, verification, phase-close, wave-4]
must_haves:
  truths:
    - "A re-run of MODE=complex DURATION=60 CPUS=8 CLIENTS=8 BEAVA_SHARD_INBOX_SIZE=1048576 measures aggregate ≥ 1,621,616 EPS (= Phase 57 baseline 1,297,293 × 1.25) — D-C2."
    - "A samply re-run on the same harness shows tokio::runtime::task::* combined ≤ 15% of leaf samples — D-C4."
    - "The measured p99 per-event push latency is ≤ Phase 57 baseline (30,667.5 µs median-of-p99 client-observed, per 57-PERF-GATE.md) — D-C3."
    - "58-PERF-GATE.md exists with headroom table, samply deltas, contingency-ladder status."
    - "58-VERIFICATION.md exists with per-SC (SC-1..SC-4) status matching ROADMAP Phase 58."
    - "ROADMAP.md Phase 58 row shows Complete; STATE.md current position advances to Phase 59."
  artifacts:
    - path: ".planning/phases/58-tokio-connection-handling-rewrite/58-PERF-GATE.md"
      provides: "Perf-gate evidence — EPS delta vs Phase 57, samply tokio share before/after, p99 latency comparison"
      min_lines: 80
    - path: ".planning/phases/58-tokio-connection-handling-rewrite/58-VERIFICATION.md"
      provides: "Per-SC (SC-1..SC-4) status: passed / human_needed / failed"
      min_lines: 40
    - path: ".planning/phases/58-tokio-connection-handling-rewrite/perf-evidence/<ts>.txt"
      provides: "Raw bench run output (checkpoints + per-client breakdown + summary.json excerpt)"
      min_lines: 50
    - path: ".planning/phases/58-tokio-connection-handling-rewrite/samply-after/beava_ingest.top.txt"
      provides: "samply top-20 leaf output post-Wave-3 — zero DashMap (preserved from Phase 54), tokio::runtime::task share ≤ 15%"
      min_lines: 20
  key_links:
    - from: "benchmark/fraud-pipeline/run_bench.sh"
      to: "58-PERF-GATE.md Candidate row"
      via: "candidate EPS number from summary.json aggregated or per-client checkpoint fallback (Phase 57 precedent)"
      pattern: "1,621,616|1621616|Candidate.*EPS"
    - from: "scripts/samply-probe-tokio-share.sh"
      to: "58-PERF-GATE.md samply delta table"
      via: "TOKIO_SHARE_PCT value captured post-Wave-3"
      pattern: "TOKIO_SHARE_PCT"
    - from: "58-VERIFICATION.md"
      to: "ROADMAP.md Phase 58 Complete + STATE.md current position"
      via: "phase-close commit"
      pattern: "Phase 58.*Complete"
---

<objective>
Wave 4 measures the Phase 58 perf gate, re-runs samply, captures p99 latency,
and closes the phase with VERIFICATION + PERF-GATE evidence + ROADMAP/STATE
updates. Mirrors the Phase 57 Wave 4 cadence exactly (same file names, same
commit pattern, same contingency ladder).

Purpose: Satisfies D-C2 (≥ 1,621,616 EPS floor), D-C3 (no p99 regression),
D-C4 (≤ 15% tokio share in samply). On pass: phase closes. On miss: contingency
ladder (C1 raise MAX_CONNS / C2 drop TCP_NODELAY experiment / C3 human_needed
with pprof evidence) per 58-CONTEXT.md / the goal block's Wave 4 plan.

Output: 58-PERF-GATE.md + 58-VERIFICATION.md + perf-evidence file + samply-after
directory + ROADMAP/STATE phase-close commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-tokio-connection-handling-rewrite/58-CONTEXT.md
@.planning/phases/58-tokio-connection-handling-rewrite/58-01-PLAN.md
@.planning/phases/58-tokio-connection-handling-rewrite/58-02-PLAN.md
@.planning/phases/58-tokio-connection-handling-rewrite/58-03-PLAN.md
@.planning/phases/57-retraction-across-crossshard-joins/57-04-PLAN.md
@.planning/phases/57-retraction-across-crossshard-joins/57-PERF-GATE.md
@.planning/phases/57-retraction-across-crossshard-joins/57-VERIFICATION.md
@.planning/phases/54-legacy-engine-removal/54-05-SUMMARY.md
@benchmark/fraud-pipeline/run_bench.sh
@scripts/samply-probe-tokio-share.sh

<!-- Phase 57 Wave 4 precedent (57-04-PLAN.md + 57-PERF-GATE.md) is the template.
     Wave 4 is mechanical: re-run bench, re-run samply, produce evidence files,
     update ROADMAP + STATE, commit. Contingency ladder is pre-defined in
     58-CONTEXT.md; no design decisions left. -->

<interfaces>
<!-- Numeric gates (from 58-CONTEXT.md + goal): -->
<!--   D-C2 EPS floor:        1,621,616 EPS (Phase 57 baseline 1,297,293 × 1.25) -->
<!--   D-C3 p99 ceiling:      Phase 57 median-of-p99 = 30,667.5 µs — Wave 4 measurement MUST be ≤ this -->
<!--   D-C4 tokio leaf share: ≤ 15.0% from samply top.txt -->

<!-- Contingency ladder (from goal block): -->
<!--   C1: raise BEAVA_MAX_CONNS_PER_SHARD from 256 to 1024, re-run. -->
<!--   C2: drop TCP_NODELAY experiment — set TCP_NODELAY=0 via socket2 on accepted streams, re-run. -->
<!--   C3: human_needed escalation — commit partial evidence + pprof delta analysis; user decides. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run perf gate + samply re-run; capture evidence</name>
  <files>.planning/phases/58-tokio-connection-handling-rewrite/perf-evidence, .planning/phases/58-tokio-connection-handling-rewrite/samply-after</files>
  <read_first>
    - .planning/phases/57-retraction-across-crossshard-joins/57-04-PLAN.md (Wave 4 precedent — same shape of perf-evidence file)
    - .planning/phases/57-retraction-across-crossshard-joins/57-PERF-GATE.md (template for 58-PERF-GATE.md)
    - .planning/phases/54-legacy-engine-removal/54-05-SUMMARY.md Task 2 (the per-client-checkpoint fallback aggregation pattern — invoked when bench.py clients crash on trailing-edge backpressure)
    - benchmark/fraud-pipeline/run_bench.sh (the harness invoked by the gate)
    - scripts/samply-probe-tokio-share.sh (Wave 0 script — Wave 4 runs it and captures output)
  </read_first>
  <behavior>
    - `.planning/phases/58-tokio-connection-handling-rewrite/perf-evidence/<ts>.txt` exists with raw bench stdout (per-client checkpoints + final aggregate — matching the shape of `.planning/phases/57-*/perf-evidence/*.txt`).
    - `.planning/phases/58-tokio-connection-handling-rewrite/samply-after/beava_ingest.top.txt` exists (samply top-20 leaf) — matches the shape of `.planning/phases/54-*/pprof-after/beava_ingest.top.txt`.
    - `.planning/phases/58-tokio-connection-handling-rewrite/samply-after/beava_ingest.flamegraph.svg` exists.
    - Both directories' `.keep` placeholders committed; actual artifact files on-disk (may be gitignored like Phase 54 pprof-before/after — check 54-05-SUMMARY pattern).
    - Candidate aggregate EPS computed and captured in a machine-parseable form (e.g. last-line `Aggregate EPS: 1,621,616` in the perf-evidence .txt file).
  </behavior>
  <action>
    1. **Run the perf gate:**

       ```bash
       cd /Users/petrpan26/work/tally
       ts=$(date -u +%Y%m%dT%H%M%SZ)
       mkdir -p .planning/phases/58-tokio-connection-handling-rewrite/perf-evidence
       BEAVA_SHARD_INBOX_SIZE=1048576 \
         MODE=complex DURATION=60 CPUS=8 CLIENTS=8 \
         bash benchmark/fraud-pipeline/run_bench.sh 2>&1 \
         | tee .planning/phases/58-tokio-connection-handling-rewrite/perf-evidence/${ts}.txt
       ```

       If clients hit trailing-edge backpressure and no "phase=final" marker
       emits (Phase 57 precedent — 55-NEXT #8): aggregate from per-client
       last-checkpoint counters as Phase 54-05 Task 2 documents. DO NOT invent
       numbers. If aggregate cannot be computed cleanly, invoke contingency
       ladder C1 (raise `BEAVA_MAX_CONNS_PER_SHARD` from 256 to 1024) and re-run.

       Append a final machine-parseable line to the evidence file:
       ```
       Aggregate EPS: <N>
       ```

    2. **Run samply re-run:**

       ```bash
       mkdir -p .planning/phases/58-tokio-connection-handling-rewrite/samply-after
       bash scripts/samply-probe-tokio-share.sh \
         > .planning/phases/58-tokio-connection-handling-rewrite/samply-after/probe-stdout.txt 2>&1

       # Extract final TOKIO_SHARE_PCT value:
       grep "TOKIO_SHARE_PCT=" .planning/phases/58-tokio-connection-handling-rewrite/samply-after/probe-stdout.txt | tail -1
       ```

       Also run raw samply to capture the top.txt + flamegraph (Phase 54-05 pattern):
       ```bash
       samply record --save-only --output samply-profile.json \
         -- cargo test --release --test profile_ingest -- --nocapture --ignored profile_ingest_hot_path
       samply import --format=top samply-profile.json > .planning/phases/58-tokio-connection-handling-rewrite/samply-after/beava_ingest.top.txt
       samply import --format=flamegraph samply-profile.json > .planning/phases/58-tokio-connection-handling-rewrite/samply-after/beava_ingest.flamegraph.svg
       ```

       (Exact samply CLI syntax may vary by version; match what Phase 54-05 used
       for pprof — substitute samply equivalents. The top.txt format is
       grep-parseable for the TOKIO_SHARE_PCT extraction.)

    3. **Verify numeric gates:**

       - `Aggregate EPS >= 1_621_616` → D-C2 PASS.
       - `TOKIO_SHARE_PCT <= 15.0` → D-C4 PASS.
       - Extract p99 from the evidence .txt per-client table (same format Phase 57 used);
         median-of-p99 across clients ≤ 30,667.5 µs → D-C3 PASS.

    4. **Contingency ladder:**

       - If D-C2 fails by < 10%: C1 — raise `BEAVA_MAX_CONNS_PER_SHARD=1024`, re-run. Capture second evidence file.
       - If D-C2 still fails after C1: C2 — add `TCP_NODELAY=false` to the accept path (experimental; set via socket2 on accepted streams in handle_connection / handle_connection_blocking), re-run. Capture third evidence file.
       - If all three fail or D-C4 fails: C3 — `human_needed`. Commit the best evidence, document the delta, submit to user.

       Document the contingency tier invoked (if any) in the evidence files.

    5. **Git**: `perf-evidence/*.txt` and `samply-after/*.txt|*.svg` may be large; follow Phase 54-05 precedent — the top.txt + flamegraph gitignored on-disk, one summary file committed. Check `.gitignore` and adjust if needed.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && ls .planning/phases/58-tokio-connection-handling-rewrite/perf-evidence/*.txt 2>&1 | tail -1 && ls .planning/phases/58-tokio-connection-handling-rewrite/samply-after/beava_ingest.top.txt 2>&1 | tail -1 && grep -E 'Aggregate EPS:' .planning/phases/58-tokio-connection-handling-rewrite/perf-evidence/*.txt | tail -1 && grep -E 'TOKIO_SHARE_PCT=' .planning/phases/58-tokio-connection-handling-rewrite/samply-after/probe-stdout.txt | tail -1</automated>
  </verify>
  <done>
    - `perf-evidence/<ts>.txt` contains raw bench output + machine-parseable `Aggregate EPS: <N>` line.
    - `samply-after/beava_ingest.top.txt` + `beava_ingest.flamegraph.svg` + `probe-stdout.txt` exist.
    - `TOKIO_SHARE_PCT=<num>` captured in probe-stdout.txt.
    - p99 per-event push latency extracted and documented.
    - If contingency ladder invoked: C1/C2/C3 tier documented in evidence file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write 58-PERF-GATE.md + 58-VERIFICATION.md; update ROADMAP + STATE; close phase</name>
  <files>.planning/phases/58-tokio-connection-handling-rewrite/58-PERF-GATE.md, .planning/phases/58-tokio-connection-handling-rewrite/58-VERIFICATION.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/57-retraction-across-crossshard-joins/57-PERF-GATE.md (template — match section structure exactly: Summary Table, Per-client checkpoints, Advisory, Contingency Ladder Status, Interpretation, Hardware context, Raw Evidence Files, grep invariant checks)
    - .planning/phases/57-retraction-across-crossshard-joins/57-VERIFICATION.md (template for VERIFICATION.md — match section structure)
    - .planning/ROADMAP.md (§ Phase 58 — update status)
    - .planning/STATE.md (current position + progress table)
    - .planning/phases/58-tokio-connection-handling-rewrite/perf-evidence/*.txt (Task 1 output — extracts go into PERF-GATE.md)
    - .planning/phases/58-tokio-connection-handling-rewrite/samply-after/*.txt (Task 1 output — extracts go into PERF-GATE.md samply table)
  </read_first>
  <behavior>
    - `58-PERF-GATE.md` exists with sections matching the 57-PERF-GATE.md template:
      - `# Phase 58 — Perf Gate Evidence` header
      - `**Status:** PASSED` (or `HUMAN_NEEDED` + ladder tier documented)
      - Summary Table with: Baseline (Phase 57 = 1,297,293 EPS), Gate floor (1,621,616 EPS = × 1.25), Candidate EPS, Headroom %, Δ vs Phase 57 %, Gate result
      - Samply delta table: Phase 57 tokio share ≈ 60% (per 58-CONTEXT.md's pprof finding) → Wave 4 tokio share ≤ 15%; percentage-point swing
      - Per-client checkpoints excerpt from evidence .txt
      - p99 latency comparison (vs Phase 57 30,667.5 µs median-of-p99)
      - Contingency Ladder Status table (C1/C2/C3 with Triggered? column)
      - Hardware context (same reference laptop class as Phase 55/56/57)
      - Raw Evidence Files section referencing perf-evidence/*.txt and samply-after/*
      - Wave-4 grep invariant checks (`grep -c "Aggregate EPS:" ...`, `grep -c "TOKIO_SHARE_PCT" ...`)

    - `58-VERIFICATION.md` exists with per-SC status (SC-1..SC-4 per ROADMAP Phase 58):
      - SC-1: samply tokio leaf ≤ 15% — passed (with evidence reference)
      - SC-2: per-shard accept topology — passed (tests/per_shard_listener_smoke.rs + tests/tokio_spawn_absence_smoke.rs GREEN)
      - SC-3: ≥ +25% EPS (≥ 1,621,616 EPS) — passed / human_needed based on Task 1 result
      - SC-4: no p99 regression — passed / human_needed based on Task 1 result
      - Each row has: requirement, evidence, status, closing note

    - `ROADMAP.md`: Phase 58 row updated:
      - Plans: `5 plans` → list of 5 plans each with `[x]` checkbox + one-liner summary (mirror Phase 57's ROADMAP entry)
      - Status: `Complete` or `Engineering-complete — <ladder tier> human_needed` depending on result
      - Completion date: today (2026-04-21 or later)

    - `STATE.md`: Current Position updated to Phase 59; `completed_phases` incremented to 10; "Stopped at" line rewritten; "Accumulated Context" gains a Phase 58 section summarizing the waves (same cadence as the Phase 57 section).

    - Final commit message: `docs(phase-58): complete phase execution — engineering done, TPC-PERF-08 passed` (or `…human_needed` per result).
  </behavior>
  <action>
    1. **Draft `58-PERF-GATE.md`** — copy the 57-PERF-GATE.md structure verbatim and fill in Wave 4's actual numbers. Machine-parseable lines MUST be included so Wave-5-equivalent external tooling (/gsd-verify-work, scripts/verify-*) can grep. Key invariants:
       - Header `**Status:** <PASSED|HUMAN_NEEDED>` exactly.
       - `Aggregate EPS:` line matches the evidence file.
       - `TOKIO_SHARE_PCT=<N>` line from probe-stdout.
       - Per-SC check marks reconcile with VERIFICATION.md.

    2. **Draft `58-VERIFICATION.md`** — per-SC table + closing notes + reference to the 4 Wave-0 RED tests now GREEN (tokio_spawn_absence_smoke, per_shard_listener_smoke (L+M halves), http_push_still_works retained GREEN, replica_ingest_routing 58-W3 assertions GREEN).

    3. **Update ROADMAP.md § Phase 58** — replace the `**Plans**: TBD` + unchecked plan list with the 5 plan rows:

       ```markdown
       **Plans**: 5 plans
       Plans:
       - [x] 58-00-PLAN.md — Wave 0: RED tests (tokio_spawn_absence, per_shard_listener, http_push_still_works) + samply-probe-tokio-share.sh + REQUIREMENTS TPC-PERF-08 row
       - [x] 58-01-PLAN.md — Wave 1: Linux SO_REUSEPORT per-shard TcpListener + FuturesUnordered inline handler + BEAVA_MAX_CONNS_PER_SHARD=256; delete spawn_linux_per_shard_accept_loops
       - [x] 58-02-PLAN.md — Wave 2: macOS dedicated std::thread per shard (D-B1) + BEAVA_SHARDS_SINGLE_LISTENER=1 fallback (D-B2); handle_connection_blocking + MacosConnSlot RAII
       - [x] 58-03-PLAN.md — Wave 3: Replica ingest rides unified per-shard accept path; opcode-dispatch parity audit + replica_ingest_routing extension at N=4
       - [x] 58-04-PLAN.md — Wave 4: perf gate (<result>) + samply re-run (<tokio-share>) + 58-VERIFICATION + close
       ```

       And update the Progress table row:

       ```
       | 58. Tokio connection-handling rewrite | 5/5 | Complete    | 2026-04-21 |
       ```

       If contingency C3 invoked: `Engineering-complete` + `human_needed` with brief explanatory note (mirror Phase 56's SC-5 row).

    4. **Update STATE.md**:
       - `stopped_at`: rewrite to "Phase 58 closed <date> …"
       - `completed_phases`: 9 → 10
       - `percent`: 95 → appropriate increment (~ 100%)
       - Phase 58 section under `## Accumulated Context` (mirror Phase 57's section)
       - `## Session Continuity` → `**Next action (engineering):**` lists Phase 59 (binary wire format for PUSH)

    5. **Commit structure** (match Phase 57 Wave 4 precedent — 2 commits):
       - Commit 1: `feat(58-04): perf gate evidence + VERIFICATION` — adds PERF-GATE.md + VERIFICATION.md + commits any gitignored-exception evidence.
       - Commit 2: `docs(phase-58): complete phase execution — engineering done, TPC-PERF-08 passed` — ROADMAP + STATE + any final SUMMARY.md adjustments.

    6. **Final sanity checks:**
       - `grep -c "Aggregate EPS:" .planning/phases/58-*/perf-evidence/*.txt` ≥ 1.
       - `grep -c "1,621,616\|1621616" .planning/phases/58-*/58-PERF-GATE.md` ≥ 1.
       - `grep -cE '#\[ignore = "58-W[0-4]"\]' tests/` = 0 (all 58-W markers removed as waves flipped RED → GREEN, except the http_push_still_works which never had one).
       - `cargo test --release --lib` baseline preserved.
       - `bash scripts/verify-no-dashmap.sh` + `verify-no-statestore.sh` + `verify-no-legacy-push.sh` still exit 0 (Phase 54's grep-ZERO gates NOT regressed).
       - `bash scripts/verify-retraction-metrics.sh` (Phase 57) still exits 0.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && test -f .planning/phases/58-tokio-connection-handling-rewrite/58-PERF-GATE.md && test -f .planning/phases/58-tokio-connection-handling-rewrite/58-VERIFICATION.md && grep -cE '^\*\*Status:\*\* (PASSED|HUMAN_NEEDED)' .planning/phases/58-tokio-connection-handling-rewrite/58-PERF-GATE.md | grep -qx 1 && grep -cE '1,621,616|1621616' .planning/phases/58-tokio-connection-handling-rewrite/58-PERF-GATE.md && grep -c "58. Tokio connection-handling rewrite | 5/5" .planning/ROADMAP.md && grep -cE 'Phase 58 closed' .planning/STATE.md && bash scripts/verify-no-dashmap.sh && bash scripts/verify-retraction-metrics.sh</automated>
  </verify>
  <done>
    - 58-PERF-GATE.md exists with headroom table, samply delta, contingency-ladder status, machine-parseable grep targets.
    - 58-VERIFICATION.md exists with SC-1..SC-4 per-criterion status.
    - ROADMAP.md Phase 58 row shows 5/5 Complete (or Engineering-complete + ladder tier human_needed).
    - STATE.md current position advanced; completed_phases = 10.
    - All prior-phase grep-gates preserved (no-dashmap, no-statestore, no-legacy-push, retraction-metrics).
    - 2 commits: evidence + close.
    - 58-W markers on integration tests all removed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench harness → production server | Localhost-only bench, same as Phase 57. |
| samply probe → production binary | Release build profiled; no auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04-01 | Tampering | Perf-evidence .txt file hand-edited to pass gate | mitigate | Machine-parseable `Aggregate EPS: <N>` line + TOKIO_SHARE_PCT line are cross-checked by Wave-4 grep invariants; commit history + evidence file mtime visible in git blame. |
| T-58-04-02 | Information Disclosure | samply flamegraph.svg may contain file paths / host info | accept | Same pattern as Phase 54 pprof-after/* — gitignored or stripped, operator discretion. |
| T-58-04-03 | Denial of Service | Wave 4 bench run on a shared machine contends with other processes | accept | Operator runs on a quiet box per Phase 55/56/57 precedent; documented in Hardware context section. |
</threat_model>

<verification>
1. `cargo check --release --tests` exits 0.
2. `cargo test --release --lib` baseline preserved vs Phase 57 + Wave-1/2/3 additions.
3. `cargo test --release --test tokio_spawn_absence_smoke -- --ignored` — GREEN (D-C4 confirmed).
4. `cargo test --release --test per_shard_listener_smoke -- --ignored` — GREEN on both platforms (D-B1 confirmed).
5. `cargo test --release --test http_push_still_works` — GREEN (D-B3 confirmed).
6. `cargo test --release --test tcp_ingest_routing` — GREEN.
7. `cargo test --release --test replica_ingest_routing` + `-- --ignored` — GREEN.
8. `bash scripts/verify-no-dashmap.sh` + `verify-no-statestore.sh` + `verify-no-legacy-push.sh` + `verify-retraction-metrics.sh` all exit 0.
9. `grep -c "Aggregate EPS:" .planning/phases/58-*/perf-evidence/*.txt` ≥ 1.
10. `grep -cE '1,621,616|1621616' .planning/phases/58-*/58-PERF-GATE.md` ≥ 1.
11. `grep -cE '#\[ignore = "58-W[0-4]"\]' tests/` = 0.
</verification>

<success_criteria>
- Perf gate: ≥ 1,621,616 EPS measured (D-C2) OR contingency ladder tier documented.
- samply tokio leaf share: ≤ 15% (D-C4) OR contingency ladder tier documented.
- p99 latency: ≤ Phase 57 baseline (D-C3) OR documented regression with user-accept signal.
- 58-PERF-GATE.md + 58-VERIFICATION.md committed with machine-parseable gate rows.
- ROADMAP + STATE phase-close landed.
- All prior-phase gates unregressed.
</success_criteria>

<output>
After completion, create `.planning/phases/58-tokio-connection-handling-rewrite/58-04-SUMMARY.md`
</output>
