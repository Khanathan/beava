---
plan: 13.4.1-04
phase: 13.4.1
captured: 2026-05-04
verdict: PASS
tasks: 5/5 substantive + 1 cleanup commit (6 commits total)
red_tests_landed: 16/16 GREEN (6 + 6 + 4)
recovery_note: SUMMARY written by orchestrator after executor agent died mid-run before reaching its docs commit; agent had escaped its worktree (cd'd to main repo path) so the 6 commits landed directly on v2/greenfield rather than a temporary worktree branch — work was preserved in the right place by accident.
---

# Plan 13.4.1-04 — GREEN cluster (server-side wire-spec verb-style migration)

## Commits (in order)

| # | SHA | Subject |
|---|-----|---------|
| 1 | `58928e70` | `feat(13.4.1-04): add GlueResponse::UnsupportedRequestShape variant + HTTP/TCP encoder mappings (D-05)` |
| 2 | `061741e9` | `feat(13.4.1-04): migrate POST /get + OP_GET to verb-style {table, key, features?} (D-01 + D-05)` |
| 3 | `60a0542c` | `feat(13.4.1-04): migrate BatchGetReqEntry + flatten dispatch_batch_get_sync (D-02 + D-03 + D-04 + D-06)` |
| 4 | `37b7a58e` | `refactor(13.4.1-04): flatten phase13_4_op_batch_get.rs Tests 1, 2, 5 to FLAT-row contract (D-03 follow-through)` |
| 5 | `1d5c241f` | `docs(13.4.1-04): schedule v0.0.x removal of BatchGetReqEntry entity_id serde alias (D-04)` |
| 6 | `90693a88` | `fix(13.4.1-04): cargo fmt + clippy + serialise all WARN-capture tests under one lock` |

All authored and committed by Hoang Phan; no AI attribution.

## Decision implementation sites

| D-XX | Decision | Implementation |
|------|----------|----------------|
| D-01 | Verb-style `{table, key, features?}` on POST /get + OP_GET | Commit 2 (061741e9) — `WireRequest::HttpGet` and `WireRequest::TcpGet` body parsers in `crates/beava-server/src/apply_shard.rs`; three-step ladder (try new shape, detect legacy `{keys, features}` or `{feature, key}` and structured-400 reject, fall through to `InternalError` on other parse failures) |
| D-02 | Verb-style per-entry `{table, key, features?}` on POST /batch_get + OP_BATCH_GET | Commit 3 (60a0542c) — `BatchGetReqEntry` struct rewritten with custom `Deserialize` impl in `apply_shard.rs`; `dispatch_batch_get_sync` builds per-entry payload from new shape |
| D-03 | FLAT row response shape on /batch_get | Commit 3 (60a0542c) — `dispatch_batch_get_sync` per-row constructor emits `Value::Object(feature_map)` directly; envelope `{table, entity_id, features}` removed. Commit 4 (37b7a58e) — `phase13_4_op_batch_get.rs` Tests 1, 2, 5 expected payloads flattened to match |
| D-04 | `entity_id` → `key` rename with one-release `serde(alias)` + WARN log | Commit 3 (60a0542c) — custom `Deserialize` tracks `from_alias: bool`; emits `tracing::warn!("BatchGetReqEntry: deprecated 'entity_id' field name; rename to 'key'; alias removed in v0.0.x")` when set. Commit 5 (1d5c241f) — appended removal entry to `.planning/ideas/v0.1-deferrals.md` |
| D-05 | Structured 400 with doc-hint on legacy request shapes | Commit 1 (58928e70) — new `GlueResponse::UnsupportedRequestShape { hint }` variant + HTTP/TCP encoder mappings emit `400 Bad Request` with `{"error": {"code": "unsupported_request_shape", "message": <hint>}}` body. Commit 2 (061741e9) — three-step ladder in HttpGet/TcpGet uses it; whole-batch reject for legacy `{keys, features}` |
| D-06 | Missing-feature filter omits from response; registry-typo whole-batch reject | Commit 3 (60a0542c) — features-filter narrowing pass omits keys absent on the entity (Redis HMGET-style sparsity); registry-typo `resolve_feature` upfront check moved BEFORE per-entry dispatch loop, returns whole-batch `GlueResponse::InternalError { reason: "feature_not_found: missing=[...]" }` matching the single-row `dispatch_get_single_verb_style_sync` disposition |

## RED tests now GREEN

```
cargo test --package beava-server --features testing \
  --test phase13_4_1_verb_style_get \
  --test phase13_4_1_verb_style_batch_get \
  --test phase13_4_1_entity_id_alias_warn
```

| Test file | Tests | Verdict |
|-----------|-------|---------|
| `phase13_4_1_verb_style_get.rs` | 6 | 6/6 GREEN |
| `phase13_4_1_verb_style_batch_get.rs` | 6 | 6/6 GREEN |
| `phase13_4_1_entity_id_alias_warn.rs` | 4 | 4/4 GREEN |
| **Total** | **16** | **16/16 GREEN** |

Notable test names confirming D-XX coverage:
- `phase13_4_1_verb_style_get_returns_flat_dict` (D-01)
- `phase13_4_1_verb_style_get_with_features_filter_narrows` (D-06)
- `phase13_4_1_op_get_returns_flat_dict_json` / `_msgpack` (D-01 + Plan 12-09 codec carry-forward)
- `phase13_4_1_legacy_keys_features_shape_rejected` (D-05)
- `phase13_4_1_legacy_feature_key_shape_rejected` (D-05)
- `phase13_4_1_alias_entity_id_emits_warn_log` (D-04 — verbatim WARN message substrings asserted)
- `phase13_4_1_canonical_key_does_not_emit_alias_warn` (D-04 — no false positive on canonical path)

## Workspace gate status

| Gate | Status |
|------|--------|
| `cargo test --no-run --package beava-server --features testing --tests` | PASS (workspace compiles) |
| `cargo fmt --all --check` | PASS |
| `cargo clippy --workspace --all-targets --all-features` | PASS (zero warnings; lock contention serialised in commit 6) |
| `cargo test --package beava-server --features testing phase12_6_mio_only_dataplane` (mio-only invariant) | PASS |
| `cargo test --package beava-server --features testing phase12_7_no_table_surface` (events-only invariant) | PASS (3/3) |
| `cargo test --workspace --features testing --exclude beava-bench` | Verified by orchestrator post-recovery (commit 6 was the agent's final cleanup before death) |

## Architectural invariants

- **Phase 12.6 mio-only invariant:** No `axum::*` symbols added outside `http_admin.rs`. Dispatch path stays single-runtime. `phase12_6_mio_only_dataplane.rs` test passes.
- **Phase 12.7 events-only invariant:** No `OpNode::Table*` / `TemporalStore` / `MvccVersion` / `push_table` / `delete_table` / `retract` / `RecordType::TableUpsert/TableDelete/Retract` / `temporal_http` symbols added. `phase13_4_op_batch_get.rs` test flattening (commit 4) modifies expected payloads only — no schema or aggregation surface change. `phase12_7_no_table_surface.rs` test passes 3/3.

## Files modified

- `crates/beava-server/src/apply_shard.rs` — `WireRequest::HttpGet`/`TcpGet` body parsers (commit 2); `BatchGetReqEntry` struct + custom Deserialize + `dispatch_batch_get_sync` per-row constructor (commit 3)
- `crates/beava-server/src/runtime_core_glue.rs` — single-row dispatch shape + flat-row response constructor (commit 3)
- `crates/beava-server/src/server.rs` — encoder match arm for `GlueResponse::UnsupportedRequestShape` (commit 1)
- `crates/beava-server/tests/phase13_4_op_batch_get.rs` — flattened expected payloads in Tests 1, 2, 5 (commit 4)
- `crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs` — serialised under one lock for stable WARN-capture in parallel test runs (commit 6)
- `.planning/ideas/v0.1-deferrals.md` — appended `entity_id` alias removal entry (commit 5)

## Recovery / executor agent failure note

The Plan-04 executor agent was dispatched in a worktree at `.claude/worktrees/agent-aa3c0fada78ef8a19` with base commit `7c8241ad`. The agent escaped its worktree mid-execution (one of its bash commands cd'd to `/Users/petrpan26/work/tally`, the main repo path) and subsequent `git commit` calls landed on `v2/greenfield` directly rather than the temporary worktree branch. The agent then died silently after committing `90693a88` (commit 6) — its JSONL transcript shows the next intended action was `cargo test --workspace --features testing --exclude beava-bench` but that tool call never returned.

The orchestrator detected the death, verified the workspace state (16/16 RED tests now GREEN; fmt + clippy + invariant tests pass), removed the abandoned worktree, and authored this SUMMARY post-hoc. No commits or work were lost. The work landed in the correct branch by accident — had the cd-escape not happened, the orchestrator would have needed to merge from the temporary worktree branch.

**Lesson for future executor prompts:** explicitly forbid `cd` outside the worktree path; pin the worktree path as a `WORKTREE_ROOT` env var in the prompt and require all bash invocations to use that path explicitly.

## Resume next

`/gsd-execute-phase 13.4.1 --wave 4` (Plan 05 closure: examples/wire round-trip, throughput regression-gate, criterion microbench, VERIFICATION + SUMMARY + ROADMAP flip).
