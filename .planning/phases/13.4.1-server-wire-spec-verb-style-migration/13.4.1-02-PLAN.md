---
phase: 13.4.1
plan: 02
type: execute
wave: 2
depends_on:
  - 13.4.1-01
files_modified:
  - crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs  # Cargo.toml entry for this test was registered by Plan 01 (Wave 1 exclusive Cargo.toml owner)
autonomous: true
requirements:
  - D-02
  - D-03
  - D-06
tdd: true
tags:
  - server-wire-spec
  - tdd-red
  - http-batch-get
  - tcp-op-batch-get
  - verb-style
  - flat-rows
  - features-filter

must_haves:
  truths:
    - "A failing integration test asserts `POST /batch_get` accepts per-entry `{table, key}` and returns FLAT rows (one feature dict per request entry, no `{table, entity_id, features:{...}}` envelope) — D-02 + D-03"
    - "A failing integration test asserts the per-entry `features: [...]` filter narrows each row independently — different entries can request different feature subsets and get different narrowed dicts — D-02 + D-06"
    - "A failing integration test asserts a feature in the registry but NOT present on this entity is OMITTED from the row dict (NOT set to null, NOT errored) — D-06"
    - "A failing integration test asserts a feature name not in the registry triggers a WHOLE-BATCH reject with `feature_not_found` upfront (4xx status, `feature_not_found` in body, NO `results` array — preserving the `runtime_core_glue.rs:392-396` precedent and matching the single-row verb-style disposition per CONTEXT.md D-06) — separating registry-typo (whole-batch reject) from per-entity-sparsity (silent per-row omit)"
    - "A failing integration test asserts `OP_BATCH_GET` (TCP) accepts the same per-entry shape and returns flat rows in `OP_GET_RESPONSE` (D-02 + D-03)"
    - "A failing integration test asserts the per-tuple unknown_table error row is FLAT — `{\"error\":{\"code\":\"unknown_table\",...}}` with NO `table`/`entity_id` envelope fields (D-03)"
    - "All tests run via `#[cfg(feature = \"testing\")]` and the new `[[test]]` entry; `cargo test --package beava-server --features testing --test phase13_4_1_verb_style_batch_get` exits NON-ZERO at end of this plan (RED commit)"
    - "Cargo.toml `[[test]]` table registers `phase13_4_1_verb_style_batch_get` with `required-features = [\"testing\"]`"
  artifacts:
    - path: "crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs"
      provides: "RED integration tests for D-02 (verb-style POST /batch_get + OP_BATCH_GET per-entry shape), D-03 (flat-row response shape), D-06 (omit-on-absent features-filter semantics)"
      contains: "phase13_4_1_batch_get_returns_flat_rows"
      contains_2: "phase13_4_1_batch_get_per_entry_features_filter_narrows"
      contains_3: "phase13_4_1_batch_get_omits_absent_feature_from_row"
      contains_4: "phase13_4_1_batch_get_unknown_feature_in_registry_errors_upfront"
      contains_5: "phase13_4_1_op_batch_get_returns_flat_rows"
      contains_6: "phase13_4_1_batch_get_unknown_table_returns_flat_error_row"
      min_lines: 280
    - path: "crates/beava-server/Cargo.toml"
      provides: "[[test]] table entry for phase13_4_1_verb_style_batch_get with required-features = [\"testing\"]"
      contains: "phase13_4_1_verb_style_batch_get"
  key_links:
    - from: "crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs"
      to: "crates/beava-server/src/apply_shard.rs::BatchGetReqEntry struct + dispatch_batch_get_sync"
      via: "POST /batch_get + OP_BATCH_GET — Plan 04 makes this GREEN by adding `features: Option<Vec<String>>` field, renaming entity_id → key, and flattening the response constructor"
      pattern: "phase13_4_1_batch_get_returns_flat_rows"
    - from: "crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs"
      to: "crates/beava-server/src/apply_shard.rs::dispatch_batch_get_sync per-entry features filter"
      via: "expect per-row narrowing — Plan 04 adds the filter pass at the `feature_map.insert` site per PATTERNS.md lines 178-202"
      pattern: "phase13_4_1_batch_get_per_entry_features_filter_narrows"
---

<objective>
RED commit — write the failing integration tests that nail down the verb-style `POST /batch_get` and `OP_BATCH_GET` contracts (D-02), the FLAT row response shape (D-03), and the per-entry `features` filter with omit-on-absent semantics (D-06). The tests MUST fail when this plan lands; Plan 04 makes them GREEN.

Purpose: per CLAUDE.md §Conventions TDD discipline, every code-touching task splits into a `test:` red commit (failing test) followed by `feat:`/`fix:`/`refactor:` green commit. This plan is the RED for the batch_get migration.

Output: one new file `crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs` (~280 LOC, 6 tests) plus a `[[test]]` table entry in `crates/beava-server/Cargo.toml`. The test binary compiles, but EVERY test inside it MUST fail at runtime.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-CONTEXT.md
@.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-PATTERNS.md
@CLAUDE.md
@docs/http-api.md
@docs/wire-spec.md
@examples/wire/batch_get-heterogeneous.request.json
@examples/wire/batch_get-heterogeneous.response.json

<analog_files>
- crates/beava-server/tests/phase13_4_op_batch_get.rs (full file — direct analog; copy `register_payload` + `push_seed_events` + `tcp_send_recv_frame` helpers verbatim, and the request/response shape pattern. The new test file asserts the FLAT-row contract; the existing tests still assert the envelope shape — they'll be updated in Plan 04)
- crates/beava-server/tests/phase13_4_1_verb_style_get.rs (Plan 01 sibling — same TestServer + tracing test scaffolding pattern; do NOT depend on Plan 01's tests, only borrow the scaffold pattern)
- crates/beava-server/src/runtime_core_glue.rs:382-396 (cross-cutting pattern §2 in PATTERNS.md — `feature_not_found` upfront-resolve precedent for Test 4 below)
</analog_files>

<interfaces>
<!-- Same interfaces as Plan 01 — these are existing exports the test consumes. -->

From `beava_core::wire`:
```rust
pub const OP_BATCH_GET: u16 = 0x0024;
pub const OP_GET_RESPONSE: u16 = 0x0023;
pub const CT_JSON: u8 = 0x01;
pub fn encode_frame(frame: &Frame, out: &mut BytesMut);
pub fn decode_frame(buf: &mut BytesMut, max: usize) -> Result<Option<Frame>, ...>;
pub struct Frame { /* op, content_type, payload */ }
```

From `beava_server::testing::TestServer`: same as Plan 01.

The pipeline registration (copy `register_payload` from `phase13_4_op_batch_get.rs:39-94`) registers BOTH `UserSpend(user_id) → cnt, total` AND `MerchantSpend(merchant_id) → merchant_cnt`. The seed pushes (`push_seed_events`) give us:
- alice: `UserSpend.cnt = 2, UserSpend.total = 42.5`
- bob:   `UserSpend.cnt = 1, UserSpend.total = 5.0`
- acme:  `MerchantSpend.merchant_cnt = 3`

Use these known answers in assertions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.a (precheck): Verify Plan 01 has registered the phase13_4_1_verb_style_batch_get [[test]] entry in Cargo.toml</name>
  <files>(no file changes — verification step)</files>
  <read_first>
    - crates/beava-server/Cargo.toml — confirm the `phase13_4_1_verb_style_batch_get` `[[test]]` entry already exists (registered by Plan 01 as the exclusive Wave 1 Cargo.toml owner)
  </read_first>
  <action>
    Plan 01 (Wave 1) is the EXCLUSIVE owner of `crates/beava-server/Cargo.toml` for Phase 13.4.1 — it registered all 3 Phase 13.4.1 [[test]] entries up front in Task 1.a. This task is a precheck to confirm Plan 01 has landed and the `phase13_4_1_verb_style_batch_get` [[test]] entry is present BEFORE creating the .rs file in Task 2.b.

    Run `grep -c 'name = "phase13_4_1_verb_style_batch_get"' crates/beava-server/Cargo.toml`. If it returns 0, Plan 01 has NOT landed yet and this plan must abort — surface the missing dependency.

    Do NOT touch Cargo.toml. Do NOT add or modify any [[test]] entries. Plan 02 only creates the test .rs file (Task 2.b) — Cargo.toml ownership is exclusive to Plan 01.
  </action>
  <verify>
    <automated>grep -c 'name = "phase13_4_1_verb_style_batch_get"' crates/beava-server/Cargo.toml | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'name = "phase13_4_1_verb_style_batch_get"' crates/beava-server/Cargo.toml` returns exactly 1 (Plan 01 has landed)
    - This task makes NO file changes (`git status --porcelain` reports unchanged tracked files, i.e., zero new diffs from this task alone)
  </acceptance_criteria>
  <done>
    Plan 01 dependency confirmed; Plan 02 Task 2.b can proceed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.b (red): Create phase13_4_1_verb_style_batch_get.rs with 6 failing tests for D-02 + D-03 + D-06</name>
  <files>crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs</files>
  <read_first>
    - crates/beava-server/tests/phase13_4_op_batch_get.rs (full file — `register_payload` lines 39-94, `push_seed_events` lines 110-130, `tcp_send_recv_frame` lines 270-297; all reused verbatim)
    - PATTERNS.md §`apply_shard.rs::BatchGetReqEntry struct (D-02 + D-04)` lines 78-126 — the migration target
    - PATTERNS.md §`dispatch_batch_get_sync per-row response (D-03 + D-06)` lines 129-205 — the FLAT-row constructor target
    - PATTERNS.md cross-cutting §2 lines 496-522 — the feature_not_found / sparsity distinction
    - CONTEXT.md D-02 lines 58-65, D-03 lines 66-73, D-06 lines 105-117 (verbatim semantics for each assertion)
    - examples/wire/batch_get-heterogeneous.request.json + .response.json (the canonical fixture this test mirrors — note the `{}` in `results[1]` for cold-start, and the narrowed `{tx_count_1h: 3}` in `results[2]` for the per-entry `features: ["tx_count_1h"]` filter)
  </read_first>
  <action>
    Create `crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs` (new file) with `#![cfg(feature = "testing")]` and 6 `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]` tests. Copy the helpers from `phase13_4_op_batch_get.rs` verbatim (`register_payload`, `register`, `push_seed_events`, `tcp_send_recv_frame`).

    All 6 tests MUST be RED. Plan 04 makes them GREEN.

    Test 1 — `phase13_4_1_batch_get_returns_flat_rows` (D-02 + D-03):
    - POST `/batch_get` body: `{"requests": [{"table": "UserSpend", "key": "alice"}, {"table": "MerchantSpend", "key": "acme"}]}` — note `key` (not `entity_id`).
    - Assert `resp.status() == 200`
    - Parse body; assert `body["results"]` is array of length 2
    - Assert `body["results"][0]["cnt"] == 2` (FLAT — `cnt` directly on the row dict, NOT under `["features"]["cnt"]`)
    - Assert `(body["results"][0]["total"].as_f64().unwrap() - 42.5).abs() < 1e-9`
    - Assert `body["results"][0].get("table").is_none()` (FLAT — no envelope)
    - Assert `body["results"][0].get("entity_id").is_none()` (FLAT — no envelope)
    - Assert `body["results"][0].get("features").is_none()` (FLAT — no `features` wrapping)
    - Assert `body["results"][1]["merchant_cnt"] == 3`

    Test 2 — `phase13_4_1_batch_get_per_entry_features_filter_narrows` (D-02 + D-06 happy path):
    - POST body: `{"requests": [{"table": "UserSpend", "key": "alice", "features": ["cnt"]}, {"table": "UserSpend", "key": "bob", "features": ["total"]}]}`
    - Per-entry filters DIFFER — alice gets `["cnt"]`, bob gets `["total"]`.
    - Assert `body["results"][0] == json!({"cnt": 2})` (only `cnt`)
    - Assert `body["results"][1].get("cnt").is_none()` (filtered out for bob)
    - Assert `(body["results"][1]["total"].as_f64().unwrap() - 5.0).abs() < 1e-9` (bob's total, included)

    Test 3 — `phase13_4_1_batch_get_omits_absent_feature_from_row` (D-06 omit-on-absent):
    - Register an extra event-driven derivation that has TWO features but only ONE has data for a particular entity (or push events such that one feature has data and another does not). Simplest approach: use the `UserSpend` table, push events for `alice` (so `cnt` and `total` both populate), then register a SECOND aggregation `UserHits(user_id) → hit_count` driven by a NON-PUSHED event `Hit` so `alice` has no `hit_count` value at all.
    - POST body: `{"requests": [{"table": "UserHits", "key": "alice", "features": ["hit_count"]}]}`
    - Assert `resp.status() == 200`
    - Assert `body["results"][0] == json!({})` — `hit_count` is in the registry/descriptor for `UserHits` BUT alice has not been touched by any `Hit` event, so it's absent on the entity → OMITTED from the row dict (NOT `{"hit_count": null}`, NOT an error)
    - Assert `!body["results"][0].as_object().unwrap().contains_key("hit_count")`

    Test 4 — `phase13_4_1_batch_get_unknown_feature_in_registry_errors_upfront` (D-06 distinction: feature-not-in-registry vs feature-absent-on-entity):
    - POST body: `{"requests": [{"table": "UserSpend", "key": "alice", "features": ["does_not_exist"]}]}`
    - This is the OTHER side of D-06: `does_not_exist` is NOT a feature in the `UserSpend` descriptor (typo). Per CONTEXT.md D-06 verbatim ("Feature not in registry... → reject the whole request with `feature_not_found` error") and per PATTERNS.md §2's `runtime_core_glue.rs:382-394` precedent, the request MUST be rejected as a WHOLE-BATCH reject — NOT a 200 response with a per-tuple error row. The disposition matches the single-row verb-style path which also returns `GlueResponse::InternalError { reason: "feature_not_found: ..." }` upfront (whole-request reject).
    - Concretely (whole-batch reject — three structural assertions: 4xx status, `feature_not_found` mention in body, NO `results` array in body):
      ```rust
      let resp_status = resp.status().as_u16();
      let body: serde_json::Value = resp.json().await.expect("json");
      let body_text = body.to_string();
      assert!(
          resp_status >= 400,
          "expected 4xx for feature-not-in-registry per D-06 (whole-batch reject); got status {resp_status}"
      );
      assert!(
          body_text.contains("feature_not_found"),
          "body should mention feature_not_found per D-06; body: {body_text}"
      );
      // Whole-batch rejection per D-06; NOT per-tuple error rows in a 200 response.
      // The structural difference between the two dispositions is that whole-batch
      // reject has NO `results` key in the body (the request never reached the
      // per-entry dispatch loop).
      assert!(
          !body_text.contains("\"results\""),
          "expected whole-batch rejection, not per-tuple error rows in 200 response; body: {body_text}"
      );
      ```
    - This test exercises Test 3's distinct case; both must coexist in Plan 04 — registry-typo errors WHOLE-BATCH (no `results` array), per-entity-absence omits silently per-row (still inside a 200 `results` array).

    Test 5 — `phase13_4_1_op_batch_get_returns_flat_rows` (D-02 + D-03 — TCP):
    - Use `tcp_send_recv_frame` with `OP_BATCH_GET = 0x0024` and `CT_JSON`.
    - Frame body: `serde_json::to_vec(&json!({"requests":[{"table":"UserSpend","key":"alice"},{"table":"MerchantSpend","key":"acme"}]}))`
    - Assert `resp_frame.op == OP_GET_RESPONSE`
    - Parse `resp_frame.payload` as JSON; assert `body["results"][0]["cnt"] == 2` (flat) and `body["results"][1]["merchant_cnt"] == 3` (flat)
    - Assert `body["results"][0].get("table").is_none()` and `body["results"][0].get("features").is_none()`

    Test 6 — `phase13_4_1_batch_get_unknown_table_returns_flat_error_row` (D-03 flat-error tuple):
    - POST body: `{"requests": [{"table": "UserSpend", "key": "alice"}, {"table": "DoesNotExist", "key": "alice"}]}`
    - Assert `resp.status() == 200` (per-tuple partial-failure semantics — preserved from Phase 13.4)
    - Assert `body["results"][0]["cnt"] == 2` (success row, flat)
    - Assert `body["results"][1]["error"]["code"] == "unknown_table"`
    - Assert `body["results"][1].get("table").is_none()` — FLAT error row, NO envelope
    - Assert `body["results"][1].get("entity_id").is_none()` — FLAT error row, NO envelope
    - Assert `body["results"][1].get("features").is_none()` — FLAT, no wrapping

    Each test calls `ts.shutdown().await.ok();`. Do NOT use `MagicMock`. Do NOT mark any test `#[ignore]`.
  </action>
  <verify>
    <automated>cargo build --package beava-server --features testing --tests 2>&1 | tail -5; test -f crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs` exits 0
    - `wc -l crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs` >= 280
    - File header `#![cfg(feature = "testing")]` present
    - All 6 test names present: `grep -cE '^async fn (phase13_4_1_batch_get_returns_flat_rows|phase13_4_1_batch_get_per_entry_features_filter_narrows|phase13_4_1_batch_get_omits_absent_feature_from_row|phase13_4_1_batch_get_unknown_feature_in_registry_errors_upfront|phase13_4_1_op_batch_get_returns_flat_rows|phase13_4_1_batch_get_unknown_table_returns_flat_error_row)' crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs == 6`
    - All 6 tests carry `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]` (`grep -c '^#\[tokio::test(flavor = "multi_thread", worker_threads = 4)\]' crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs >= 6`)
    - Test 6 asserts FLAT error tuple: `grep -F 'get("table").is_none()' crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs` exits 0
    - No `MagicMock` / `#[ignore]`: `grep -cE 'MagicMock|mock!|#\[ignore' crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs == 0`
    - File compiles: `cargo build --package beava-server --features testing --tests` exits 0
  </acceptance_criteria>
  <done>
    Test binary compiles cleanly. `cargo test --package beava-server --features testing --test phase13_4_1_verb_style_batch_get -- --nocapture` exits NON-ZERO with assertion failures across all 6 tests (RED state). Failure modes: Tests 1, 5, 6 fail because `dispatch_batch_get_sync` returns `{table, entity_id, features:{...}}` envelope; Tests 2 + 3 fail because `BatchGetReqEntry` has no `features` field, so the parse rejects `key` (not `entity_id`) and the filter is never applied; Test 4 fails because (a) `BatchGetReqEntry` won't accept the `features` field at all so the request never reaches the per-row dispatch.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.c (red commit): commit RED tests as test: commit per CLAUDE.md TDD discipline</name>
  <files>(commit only — no file changes beyond Tasks 2.a + 2.b)</files>
  <read_first>
    - CLAUDE.md §Conventions TDD discipline section
  </read_first>
  <action>
    Stage `crates/beava-server/Cargo.toml` and `crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs`. Commit:

    ```
    test(13.4.1-02): add RED integration tests for verb-style POST /batch_get + OP_BATCH_GET (D-02 + D-03 + D-06)

    Phase 13.4.1 Plan 02 — adds 6 failing integration tests asserting:
    - Per-entry {table, key, features?} body shape on /batch_get + OP_BATCH_GET (D-02)
    - FLAT row response shape (no {table, entity_id, features:{...}} envelope) (D-03)
    - Per-entry features filter narrows each row independently (D-02 + D-06)
    - Feature absent on entity is OMITTED from row dict (D-06)
    - Feature not in registry errors upfront (D-06 distinction)
    - Flat error tuples preserve partial-failure semantics (D-03)

    All 6 tests RED. Plan 04 turns them GREEN by migrating BatchGetReqEntry
    (key + features fields), flattening dispatch_batch_get_sync's per-row
    constructor, and adding the per-entry features-filter narrowing pass.
    ```

    No `Co-Authored-By: Claude` line.
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -q '^test(13.4.1-02):'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%s'` starts with `test(13.4.1-02):`
    - `git log -1 --format='%B' | grep -c "Co-Authored-By: Claude" == 0`
    - `git log -1 --stat | grep -c "phase13_4_1_verb_style_batch_get.rs" >= 1`
    - Working tree clean: `git status --porcelain | wc -l == 0`
  </acceptance_criteria>
  <done>
    RED commit landed. Plan 04 will make these 6 tests GREEN.
  </done>
</task>

</tasks>

<verification>
- Cargo.toml registration + test file creation + commit, in order.
- `cargo build --package beava-server --features testing --tests` succeeds.
- `cargo test --package beava-server --features testing --test phase13_4_1_verb_style_batch_get -- --nocapture` exits non-zero with assertion failures (canonical RED state).
- `cargo clippy --package beava-server --features testing --tests -- -D warnings` exits 0.
- `cargo fmt --all --check` exits 0.
- One commit added with message `test(13.4.1-02): ...`.
</verification>

<success_criteria>
1. New file `crates/beava-server/tests/phase13_4_1_verb_style_batch_get.rs` exists, ≥ 280 LOC, compiles cleanly under `--features testing`.
2. New `[[test]]` entry in `crates/beava-server/Cargo.toml`.
3. All 6 tests inside the new file FAIL when run.
4. `cargo clippy --package beava-server --features testing --tests -- -D warnings` exits 0.
5. `cargo fmt --all --check` exits 0.
6. One `test(13.4.1-02):`-prefixed commit; no AI attribution.
</success_criteria>

<output>
After completion, create `.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-02-SUMMARY.md` documenting:
- The 6 tests created and what each one asserts (one bullet per test)
- The exact failure mode for each test
- The hand-off to Plan 04 (which sites Plan 04 must touch to flip these tests GREEN — `BatchGetReqEntry` struct + per-row response constructor + features-filter narrowing pass)
</output>
