---
phase: 13.4.1
plan: 03
type: execute
wave: 2
depends_on:
  - 13.4.1-01
files_modified:
  - crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs  # Cargo.toml entry for this test was registered by Plan 01 (Wave 1 exclusive Cargo.toml owner)
autonomous: true
requirements:
  - D-04
tdd: true
tags:
  - server-wire-spec
  - tdd-red
  - serde-alias
  - deprecation-warn
  - alias-precedent

must_haves:
  truths:
    - "A failing integration test asserts `POST /batch_get` accepts the LEGACY per-entry field name `entity_id` (not just the canonical `key`) and still returns a flat row — exercises `serde(alias = \"entity_id\")` deserialise path (D-04)"
    - "A failing integration test asserts the canonical `key` field also works (no false-positive — alias should NOT change the canonical path)"
    - "A failing integration test asserts a WARN-level tracing log line is emitted when the request uses the legacy `entity_id` field, with message text containing the verbatim deprecation string `BatchGetReqEntry: deprecated 'entity_id' field name; rename to 'key'; alias removed in v0.0.x` (D-04 line 76 of CONTEXT.md)"
    - "A failing integration test asserts the WARN log is NOT emitted on the canonical `key` path (no false-positive churn — only the alias path warns)"
    - "All tests run via `#[cfg(feature = \"testing\")]` and the new `[[test]]` entry; `cargo test --package beava-server --features testing --test phase13_4_1_entity_id_alias_warn` exits NON-ZERO at end of this plan (RED commit)"
    - "Cargo.toml `[[test]]` table registers `phase13_4_1_entity_id_alias_warn` with `required-features = [\"testing\"]`"
    - "If Plan 04 chooses the pragmatic 'always-warn' detection strategy (PATTERNS.md cross-cutting §4 lines 549-558), this test must pass under that strategy too — accept either strict (alias-only WARN) or pragmatic (always-WARN) by structuring the canonical-key test to allow either disposition; assert the absence of the deprecation message text from the WARN log on the canonical path under STRICT, and accept any number of warns under PRAGMATIC if the canonical path was also warned for OTHER reasons. Concretely: the canonical-key test asserts that the EXACT deprecation message from D-04 is NOT among captured logs (under strict); under pragmatic-always-warn this assertion will fail and Plan 04 will pick strict during the green pass to satisfy both tests."
  artifacts:
    - path: "crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs"
      provides: "RED integration tests for D-04 (entity_id → key serde alias + WARN log on alias use)"
      contains: "phase13_4_1_alias_entity_id_deserializes"
      contains_2: "phase13_4_1_canonical_key_deserializes"
      contains_3: "phase13_4_1_alias_entity_id_emits_warn_log"
      contains_4: "phase13_4_1_canonical_key_does_not_emit_alias_warn"
      contains_5: "deprecated 'entity_id' field name"
      min_lines: 220
    - path: "crates/beava-server/Cargo.toml"
      provides: "[[test]] table entry for phase13_4_1_entity_id_alias_warn with required-features = [\"testing\"]"
      contains: "phase13_4_1_entity_id_alias_warn"
  key_links:
    - from: "crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs"
      to: "crates/beava-server/src/apply_shard.rs::BatchGetReqEntry"
      via: "post /batch_get with `entity_id` field — Plan 04 adds `#[serde(alias = \"entity_id\")]` to the renamed `key` field"
      pattern: "phase13_4_1_alias_entity_id_deserializes"
    - from: "crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs"
      to: "crates/beava-server/src/apply_shard.rs::dispatch_batch_get_sync (D-04 alias detection + WARN log emission)"
      via: "in-test tracing-subscriber capture — Plan 04 wires `tracing::warn!` per PATTERNS.md cross-cutting §4 (`use tracing::warn;` + `warn!(\"BatchGetReqEntry: deprecated 'entity_id'...\")`)"
      pattern: "phase13_4_1_alias_entity_id_emits_warn_log"
---

<objective>
RED commit — write the failing integration tests that nail down the `entity_id` → `key` rename with one-release `#[serde(alias = "entity_id")]` deprecation path (D-04). The tests assert (a) the alias deserialises, (b) the canonical `key` deserialises, (c) a WARN-level tracing log line is emitted on alias use with the exact deprecation message text from CONTEXT.md, and (d) the canonical path does NOT spuriously emit the alias-deprecation log.

Purpose: per CONTEXT.md `<estimate>` Plan-checker contract item 5 — "D-04 entity_id alias log path must be exercised in a precedent test (otherwise the alias removal in v0.0.x has no precedent test to flip)." This plan creates that precedent test.

Output: one new file `crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs` (~220 LOC, 4 tests) plus a `[[test]]` table entry in `crates/beava-server/Cargo.toml`. The test binary compiles, but the 4 tests inside MUST fail.

NOTE on the alias-detection strategy: PATTERNS.md cross-cutting §4 lines 549-558 enumerates three options for detecting alias use (strict custom-deserialize, pragmatic always-warn, sample-on-first-occurrence). Plan 04 will pick one. The tests below are written so the STRICT option (alias-only WARN) makes all 4 GREEN; the pragmatic always-WARN option would fail Test 4 (no false-positive on canonical path). Plan 04 SHOULD pick STRICT for that reason — call this out in Plan 04's decision log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-CONTEXT.md
@.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-PATTERNS.md
@CLAUDE.md

<analog_files>
- crates/beava-server/tests/phase13_4_op_batch_get.rs (TestServer + register helper analog)
- crates/beava-server/src/register.rs lines 22 + 266-271 (the `tracing::warn!(...)` idiom used elsewhere in the crate — PATTERNS.md cross-cutting §4)
- workspace `Cargo.toml` — `tracing` and `tracing-subscriber` are workspace deps already; this test does NOT need a new dep
</analog_files>

<interfaces>
<!-- Tracing capture pattern. Use the `tracing-subscriber::fmt` builder with a `MakeWriter` that writes
     into an in-memory `Arc<Mutex<Vec<u8>>>` so the test can inspect emitted lines. -->

```rust
use std::sync::{Arc, Mutex};
use tracing_subscriber::fmt::MakeWriter;

#[derive(Clone)]
struct TestWriter(Arc<Mutex<Vec<u8>>>);

impl<'a> MakeWriter<'a> for TestWriter {
    type Writer = TestWriterHandle;
    fn make_writer(&'a self) -> Self::Writer {
        TestWriterHandle(self.0.clone())
    }
}

struct TestWriterHandle(Arc<Mutex<Vec<u8>>>);
impl std::io::Write for TestWriterHandle {
    fn write(&mut self, b: &[u8]) -> std::io::Result<usize> {
        self.0.lock().unwrap().extend_from_slice(b);
        Ok(b.len())
    }
    fn flush(&mut self) -> std::io::Result<()> { Ok(()) }
}
```

Use `tracing::subscriber::with_default(...)` to scope the subscriber to the duration of one test run, then read `capture.lock().unwrap()` and convert via `String::from_utf8_lossy(...)` to assert against. The `tracing-subscriber` workspace dep already supports this — no new deps required.

Existing `TestServer` from `beava_server::testing` (same as Plans 01 + 02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 3.a (precheck): Verify Plan 01 has registered the phase13_4_1_entity_id_alias_warn [[test]] entry in Cargo.toml</name>
  <files>(no file changes — verification step)</files>
  <read_first>
    - crates/beava-server/Cargo.toml — confirm the `phase13_4_1_entity_id_alias_warn` `[[test]]` entry already exists (registered by Plan 01 as the exclusive Wave 1 Cargo.toml owner)
  </read_first>
  <action>
    Plan 01 (Wave 1) is the EXCLUSIVE owner of `crates/beava-server/Cargo.toml` for Phase 13.4.1 — it registered all 3 Phase 13.4.1 [[test]] entries up front. This task is a precheck to confirm Plan 01 has landed and the `phase13_4_1_entity_id_alias_warn` [[test]] entry is present BEFORE creating the .rs file in Task 3.b.

    Run `grep -c 'name = "phase13_4_1_entity_id_alias_warn"' crates/beava-server/Cargo.toml`. If it returns 0, Plan 01 has NOT landed yet — surface the missing dependency.

    Do NOT touch Cargo.toml. Do NOT add or modify any [[test]] entries. Plan 03 only creates the test .rs file (Task 3.b).
  </action>
  <verify>
    <automated>grep -c 'name = "phase13_4_1_entity_id_alias_warn"' crates/beava-server/Cargo.toml | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'name = "phase13_4_1_entity_id_alias_warn"' crates/beava-server/Cargo.toml` returns exactly 1 (Plan 01 has landed)
    - This task makes NO file changes
  </acceptance_criteria>
  <done>
    Plan 01 dependency confirmed; Plan 03 Task 3.b can proceed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.b (red): Create phase13_4_1_entity_id_alias_warn.rs with 4 failing tests for D-04</name>
  <files>crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs</files>
  <read_first>
    - crates/beava-server/tests/phase13_4_op_batch_get.rs (full file — copy `register_payload` + `register` + `push_seed_events` helpers verbatim)
    - crates/beava-server/src/register.rs lines 22 + 266-271 (`use tracing::{info, warn};` + `warn!(...)` idiom)
    - PATTERNS.md cross-cutting §4 lines 538-563 (tracing::warn precedent + the three detection strategies)
    - CONTEXT.md D-04 lines 74-83 — exact WARN message text and rationale
  </read_first>
  <action>
    Create `crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs` (new file) with `#![cfg(feature = "testing")]` and 4 tests. Copy the `register_payload` + `register` + `push_seed_events` helpers from `phase13_4_op_batch_get.rs:39-130` verbatim (or adapt to a single-pipeline `UserSpend(user_id) → cnt`).

    Define an in-test `TestWriter` + `MakeWriter` impl per the `<interfaces>` block above. Each test that captures logs:
    1. Creates `let capture = Arc::new(Mutex::new(Vec::<u8>::new()));`
    2. Builds a `tracing_subscriber::fmt().with_writer(TestWriter(capture.clone())).with_max_level(tracing::Level::WARN).finish()`
    3. Wraps the request with `tracing::subscriber::with_default(subscriber, || { /* spawn server, run request, await */ })` — note that since `with_default` wraps a closure that returns a future, we need to use `tokio::task::block_in_place` OR set the subscriber via `set_default()` for the test's scope. Equivalent pattern: use `tracing::subscriber::set_default(subscriber)` which returns a `DefaultGuard` that drops at end of test, scoping the subscriber to this test.
    4. Reads `let logs = String::from_utf8_lossy(&capture.lock().unwrap()).to_string();` after the awaited request returns.
    5. Asserts on `logs.contains(...)` against the deprecation message text.

    All 4 tests `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]`.

    Test 1 — `phase13_4_1_alias_entity_id_deserializes` (D-04 alias path):
    - POST `/batch_get` body uses LEGACY field name: `{"requests": [{"table": "UserSpend", "entity_id": "alice"}]}` — note `entity_id`, not `key`.
    - Assert `resp.status() == 200`
    - Parse body; assert `body["results"][0]["cnt"] == 2` (the alias path produces the same row data as the canonical path)
    - This will fail today because `BatchGetReqEntry` uses `entity_id` as the canonical field — wait, IT WILL PASS. Hmm. Today it works with `entity_id`. After Plan 04 the field renames to `key` with `#[serde(alias = "entity_id")]`. So this test ALSO passes today. The assertion that makes it RED today is that `body["results"][0]` is FLAT (per Plan 02's D-03). Re-state: assert `body["results"][0]["cnt"] == 2` AND `body["results"][0].get("table").is_none()` (FLAT — no envelope). The `is_none()` check fails today because the response is wrapped — turns this into a RED test that depends on Plan 04's flatten work too. THIS is the point: Plan 04's flatten + alias work happen together; both must land for this test to pass.

    Test 2 — `phase13_4_1_canonical_key_deserializes` (D-04 canonical-path no-regression):
    - POST body: `{"requests": [{"table": "UserSpend", "key": "alice"}]}` — canonical field name.
    - Assert `resp.status() == 200`
    - Assert `body["results"][0]["cnt"] == 2` (flat)
    - Assert `body["results"][0].get("table").is_none()` (flat)
    - This fails today because `BatchGetReqEntry` only accepts `entity_id`, so `key` is missing → 500 InternalError.

    Test 3 — `phase13_4_1_alias_entity_id_emits_warn_log` (D-04 WARN log assertion):
    - Set up tracing capture as described above.
    - POST body: `{"requests": [{"table": "UserSpend", "entity_id": "alice"}]}` (alias path)
    - Assert `resp.status() == 200`
    - Read `logs`; assert `logs.contains("deprecated 'entity_id' field name")` (verbatim substring from D-04)
    - Assert `logs.contains("rename to 'key'")` (verbatim substring from D-04)
    - Assert `logs.contains("alias removed in v0.0.x")` (verbatim substring from D-04)
    - This fails today because no such log is emitted (the field `entity_id` is the canonical name today; no alias detection or deprecation logging exists).

    Test 4 — `phase13_4_1_canonical_key_does_not_emit_alias_warn` (D-04 no-false-positive):
    - Set up tracing capture.
    - POST body: `{"requests": [{"table": "UserSpend", "key": "alice"}]}` (canonical path)
    - Assert `resp.status() == 200`
    - Read `logs`; assert `!logs.contains("deprecated 'entity_id'")` — the canonical path must NOT emit the alias-deprecation log, otherwise every legitimate batch_get request would log a spurious WARN, defeating the purpose.
    - This fails today (a) because canonical `key` doesn't deserialise yet (Test 2 fails), AND (b) because there's no alias-detection logging at all.

    Each test calls `ts.shutdown().await.ok();` and the tracing `DefaultGuard` (from `set_default`) drops at end of test scope. Do NOT use `MagicMock`. Do NOT mark any test `#[ignore]`. Do NOT use a global `tracing_subscriber::fmt::init()` (it's process-global and breaks parallel tests) — use scoped `set_default` per test.
  </action>
  <verify>
    <automated>cargo build --package beava-server --features testing --tests 2>&1 | tail -5; test -f crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs` exits 0
    - `wc -l crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs` >= 220
    - File header `#![cfg(feature = "testing")]` present
    - All 4 test names present: `grep -cE '^async fn (phase13_4_1_alias_entity_id_deserializes|phase13_4_1_canonical_key_deserializes|phase13_4_1_alias_entity_id_emits_warn_log|phase13_4_1_canonical_key_does_not_emit_alias_warn)' crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs == 4`
    - All 4 tests carry `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]`
    - Verbatim D-04 substrings present in test source: `grep -cF "deprecated 'entity_id' field name" crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs >= 2` (Test 3 asserts substring; Test 4 asserts negation)
    - `grep -cF "rename to 'key'" crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs >= 1`
    - `grep -cF "alias removed in v0.0.x" crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs >= 1`
    - Tracing capture infrastructure present: `grep -cE 'MakeWriter|set_default|with_default' crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs >= 1`
    - No global `tracing_subscriber::fmt::init()`: `grep -c 'tracing_subscriber::fmt::init' crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs == 0`
    - No `MagicMock` / `#[ignore]`: `grep -cE 'MagicMock|mock!|#\[ignore' crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs == 0`
    - File compiles: `cargo build --package beava-server --features testing --tests` exits 0
  </acceptance_criteria>
  <done>
    Test binary compiles cleanly. `cargo test --package beava-server --features testing --test phase13_4_1_entity_id_alias_warn -- --nocapture` exits NON-ZERO with assertion failures across all 4 tests (RED state). Failure modes: Test 1 — the response is wrapped (envelope), so `body["results"][0].get("table").is_none()` is false. Test 2 — `BatchGetReqEntry` doesn't accept `key`, so the request 500s. Test 3 — no WARN log emitted because no alias-detection code exists. Test 4 — `key` field rejected, request 500s before any logging happens.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.c (red commit): commit RED test as test: commit per CLAUDE.md TDD discipline</name>
  <files>(commit only — no file changes beyond Tasks 3.a + 3.b)</files>
  <read_first>
    - CLAUDE.md §Conventions TDD discipline section
  </read_first>
  <action>
    Stage `crates/beava-server/Cargo.toml` and `crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs`. Commit:

    ```
    test(13.4.1-03): add RED entity_id alias precedent test (D-04)

    Phase 13.4.1 Plan 03 — adds 4 failing tests for the BatchGetReqEntry
    `entity_id` → `key` rename with one-release `serde(alias = "entity_id")`
    deprecation, exercising:

    1. Alias `entity_id` deserialises and produces a flat row
    2. Canonical `key` deserialises and produces a flat row
    3. WARN-level tracing log emitted on alias use, message text matches
       D-04 verbatim ("deprecated 'entity_id' field name; rename to 'key';
       alias removed in v0.0.x")
    4. Canonical path does NOT emit alias-deprecation log (no false positive)

    Per CONTEXT.md plan-checker item 5 — D-04 alias log path MUST be
    exercised in a test, otherwise the alias removal in v0.0.x has no
    precedent test to flip.

    All 4 tests RED. Plan 04 turns them GREEN by adding the alias attribute
    on the renamed `key` field plus the WARN-on-alias detection path.
    ```

    No `Co-Authored-By: Claude` line.
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -q '^test(13.4.1-03):'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%s'` starts with `test(13.4.1-03):`
    - `git log -1 --format='%B' | grep -c "Co-Authored-By: Claude" == 0`
    - `git log -1 --stat | grep -c "phase13_4_1_entity_id_alias_warn.rs" >= 1`
    - Working tree clean: `git status --porcelain | wc -l == 0`
  </acceptance_criteria>
  <done>
    RED commit landed. Plan 04 will make these 4 tests GREEN.
  </done>
</task>

</tasks>

<verification>
- Cargo.toml registration + test file creation + commit, in order.
- `cargo build --package beava-server --features testing --tests` succeeds.
- `cargo test --package beava-server --features testing --test phase13_4_1_entity_id_alias_warn -- --nocapture` exits non-zero with assertion failures.
- `cargo clippy --package beava-server --features testing --tests -- -D warnings` exits 0.
- `cargo fmt --all --check` exits 0.
- One commit added with message `test(13.4.1-03): ...`.
</verification>

<success_criteria>
1. New file `crates/beava-server/tests/phase13_4_1_entity_id_alias_warn.rs` exists, ≥ 220 LOC.
2. New `[[test]]` entry in `crates/beava-server/Cargo.toml`.
3. All 4 tests inside the new file FAIL when run.
4. `cargo clippy` clean; `cargo fmt --all --check` clean.
5. One `test(13.4.1-03):`-prefixed commit; no AI attribution.
</success_criteria>

<output>
After completion, create `.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-03-SUMMARY.md` documenting:
- The 4 tests created and what each one asserts
- The exact failure mode for each test
- Recommendation to Plan 04: pick the STRICT alias-detection strategy (alias-only WARN) so Test 4 (no-false-positive on canonical path) passes alongside Test 3 (alias path WARNs)
- Hand-off note: Plan 04 must add `use tracing::warn;` (or equivalent) to apply_shard.rs and emit the WARN at dispatch_batch_get_sync entry when any per-entry deserialised from the `entity_id` alias source
</output>
