---
phase: 13.4.1
plan: 05
type: execute
wave: 4
depends_on:
  - 13.4.1-04
files_modified:
  - .planning/throughput-baselines.md
  - .planning/perf-baselines.md
  - crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs
  - crates/beava-server/Cargo.toml
  - .planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-VERIFICATION.md
  - .planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-SUMMARY.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
tdd: false
tags:
  - phase-closure
  - throughput-regression-gate
  - perf-baseline-append
  - perf-microbench
  - phase-summary
  - phase-verification
  - examples-wire-round-trip

must_haves:
  truths:
    - "All 6 ROADMAP success criteria for Phase 13.4.1 are verified GREEN with grep-/test-/CLI-checkable evidence captured in VERIFICATION.md"
    - "`examples/wire/get*.json` and `examples/wire/batch_get-heterogeneous.*.json` fixtures round-trip clean against the migrated server (Phase 13.0 fixtures × Phase 13.4.1 server) — verified via `python3 examples/wire/_validate_examples.py`"
    - "Throughput regression-gate row appended to `.planning/throughput-baselines.md` for Phase 13.4.1 small/tcp cell — within ±10% of Phase 13.5 baseline (674,108 EPS median; 10% warn / 25% block per CLAUDE.md §End-to-end throughput regression contract)"
    - "Focused criterion microbench file `crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs` exists and compiles (`cargo bench --bench phase13_4_1_dispatch_get_verb_style --no-run` exits 0); `[[bench]]` entry registered in `crates/beava-server/Cargo.toml`; perf-baselines.md row appended per CLAUDE.md §Performance Discipline plan-checker contract for Phase 6+ (the contract requires at least one task whose `files_modified` contains a path under `crates/*/benches/`)"
    - "VERIFICATION.md captures pass/fail status for each success criterion, the throughput cell ledger row with hw-class, and the workspace gate triple (cargo fmt + cargo clippy + cargo test)"
    - "SUMMARY.md captures the deficit-discovery narrative (why Phase 13.4 left this work undone), the migration diff, the GREEN-cluster commit list, and the hand-off to Phase 13.5.1 Plan 05 (the SDK transport impl that depended on this phase)"
    - "ROADMAP.md is updated to mark Phase 13.4.1 as ✅ CLOSED with the verdict (PASS / PASS-WITH-WARN / FAIL)"
  artifacts:
    - path: ".planning/throughput-baselines.md"
      provides: "Phase 13.4.1 small/tcp regression-gate row appended to the Apple-M4 hw-class section"
      contains: "13.4.1"
    - path: ".planning/perf-baselines.md"
      provides: "Phase 13.4.1 entry — microbench data row from `phase13_4_1_dispatch_get_verb_style` (compile-only acceptance; runtime numbers optional)"
      contains: "13.4.1"
    - path: "crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs"
      provides: "Focused criterion microbench measuring `dispatch_get_single_verb_style_sync` + `dispatch_batch_get_sync` on a fixed app + key + features=[\"c\"] input — satisfies CLAUDE.md §Performance Discipline plan-checker contract for Phase 6+ phases"
      min_lines: 40
    - path: "crates/beava-server/Cargo.toml"
      provides: "`[[bench]]` entry registering `phase13_4_1_dispatch_get_verb_style` (name + harness = false; required-features = [\"testing\"] if the bench depends on the testing feature)"
      contains: "phase13_4_1_dispatch_get_verb_style"
    - path: ".planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-VERIFICATION.md"
      provides: "Per-success-criterion verification table + workspace gate evidence + throughput cell evidence"
      contains: "## Success Criteria Verification"
      contains_2: "## Workspace Gates"
      contains_3: "## Throughput Regression Gate"
      contains_4: "small/tcp"
      contains_5: "examples/wire"
      min_lines: 100
    - path: ".planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-SUMMARY.md"
      provides: "Phase summary — deficit narrative, migration diff, decisions hit-list, hand-off"
      contains: "## Phase Goal"
      contains_2: "## Deficit Origin"
      contains_3: "## Migration Diff"
      contains_4: "## Decisions Hit"
      contains_5: "## Hand-off to 13.5.1"
      min_lines: 80
    - path: ".planning/ROADMAP.md"
      provides: "Phase 13.4.1 status updated to CLOSED"
      contains: "13.4.1"
      contains_2: "CLOSED"
  key_links:
    - from: ".planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-VERIFICATION.md"
      to: ".planning/throughput-baselines.md"
      via: "throughput cell row reference; VERIFICATION cites the appended row by hw-class + cell name"
      pattern: "throughput-baselines.md"
    - from: "crates/beava-server/Cargo.toml"
      to: "crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs"
      via: "`[[bench]]` table entry registers the bench file with the Cargo bench harness"
      pattern: "phase13_4_1_dispatch_get_verb_style"
    - from: ".planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-SUMMARY.md"
      to: ".planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-05-PLAN.md"
      via: "hand-off note — Plan 13.5.1-05 had `phase_depends_on: [13.4.1]`; SUMMARY confirms this phase is unblocking"
      pattern: "13.5.1-05"
---

<objective>
Closure plan — produce VERIFICATION.md and SUMMARY.md, append throughput-baselines + perf-baselines rows, ship a focused criterion microbench against the migrated dispatch path, validate examples/wire fixtures round-trip clean, and mark Phase 13.4.1 as ✅ CLOSED in ROADMAP.md.

Per CLAUDE.md §Performance Discipline + §End-to-end throughput regression contract, every Phase 8+ phase MUST include a throughput-run row appended to `.planning/throughput-baselines.md`. Per the plan-checker contract for Phase 6+, every plan MUST include at least one task whose `files_modified` contains a path under `crates/*/benches/`. Both gates are satisfied here: throughput-run via Task 5.b, and a new focused criterion microbench via Task 5.c (the change is a serde shape flip, but the microbench is cheap insurance against future regressions in the migrated dispatch path).

Per Phase 13.0 success criterion 4 cited verbatim in ROADMAP.md (`examples/wire/*.json` fixtures round-trip clean), validate the locked Phase 13.0 fixtures against the migrated server.

Output: VERIFICATION.md, SUMMARY.md, baselines updated, new bench file + Cargo.toml `[[bench]]` entry, ROADMAP.md status flipped, one git commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-CONTEXT.md
@.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-PATTERNS.md
@CLAUDE.md
@.planning/throughput-baselines.md
@.planning/perf-baselines.md
@.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-04-PLAN.md
@examples/wire/_validate_examples.py

<analog_files>
- `.planning/phases/13.5-python-sdk-and-bench-cli/13.5-11-SUMMARY.md` — phase-closure SUMMARY format reference (deficit narrative + open follow-ups)
- `.planning/throughput-baselines.md` lines 1990-2030 — Phase 13.5 closure rows (verdict line + 8-cell table; mirror exactly for the 13.4.1 row)
- `.planning/perf-baselines.md` (Phase 5.5+ entries — format reference)
- `examples/wire/_validate_examples.py` — runs JSON Schema 2020-12 validation against every `<op>-<scenario>.<request|response|error>.json`
- `crates/beava-server/benches/phase12_09_msgpack_get.rs` — closest structural analog for Task 5.c's new bench (benches `dispatch_get_single_sync` + `dispatch_get_batch_sync` against a warm-cache `AppState`; the new bench mirrors this for `dispatch_get_single_verb_style_sync` + `dispatch_batch_get_sync`)
- `crates/beava-server/benches/phase12_07_read_path.rs` — secondary analog (dispatch-path microbench, same warm-AppState bootstrap pattern)
- `crates/beava-server/Cargo.toml` lines 277-314 — `[[bench]]` table format (`name = "..."` + `harness = false` + optional `required-features = ["testing"]`); mirror this for the new entry
</analog_files>

<interfaces>
<!-- Throughput-baselines.md row format (8-cell table per Phase 13.5 precedent) -->

```
| Cell | Phase 13.4.1 EPS | Phase 13.5 baseline EPS | Δ% vs 13.5 | Verdict |
|------|---------------:|------------------------:|-----------:|---------|
| small/tcp (gate) | <new EPS>      | 674,108 (Phase 13.5)    | <Δ%>       | <PASS/WARN/BLOCK> |
```

The full 8-cell rebaseline is OPTIONAL for a fix-up phase; per CLAUDE.md §End-to-end throughput regression contract, the gate cell `small/tcp` is the only mandatory measurement. CONTEXT.md `<carrying_forward>` confirms: "Throughput regression gate — small/tcp baseline from Phase 13.5; 10% warn / 25% block thresholds." Single-cell row is sufficient for closure; capture the others if box load + time permit, but skip if not.

<!-- Reproduce command per throughput-baselines.md "Reproduce" section + Phase 13.5 harness invocation:

```
cargo run -p beava-bench --release -- --pipeline small --transport tcp --duration-secs 60 --parallel 8
```

OR if the bench harness uses the v18 binary (per Phase 13.5 reproduction notes):

```
cargo run --release --bin beava-bench-v18 -- --wire-format msgpack --blast-shape zipfian --cardinality 10000 --total-events 1000000 --parallel 16 --pipeline-depth 1024 --no-ledger
```

Pick whichever the harness is currently calibrated for; cross-reference Phase 13.5 SUMMARY notes if uncertain. -->

<!-- examples/wire round-trip validator -->

`python3 examples/wire/_validate_examples.py` — runs the JSON Schema 2020-12 validator across every fixture pair. Exit 0 on success.

For the round-trip check, in addition to schema validation, run a smoke that fires the `get-found.request.json` body at a live server and asserts the response matches the recorded `get-found.response.json` shape. The server-side migration in Plan 04 + the locked fixtures from Phase 13.0 should agree. The smoke can be a small ad-hoc shell script (curl + diff) or an in-test assertion in any of the existing Phase 13.4 / 13.4.1 test files.

<!-- Cargo.toml [[bench]] entry format (mirror crates/beava-server/Cargo.toml lines 277-294) -->

```toml
[[bench]]
name = "phase13_4_1_dispatch_get_verb_style"
harness = false
required-features = ["testing"]
```

`harness = false` is required because criterion provides its own `criterion_main!` entry-point (not the libtest harness). `required-features = ["testing"]` mirrors `phase12_09_msgpack_get` since the bench depends on the same `AppState` bootstrap path which is gated behind the `testing` feature flag.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 5.a: Run examples/wire fixture round-trip validation against the migrated server</name>
  <files>(no file changes — validation step)</files>
  <read_first>
    - examples/wire/_validate_examples.py (full file — understand what it validates)
    - examples/wire/get-found.request.json + get-found.response.json
    - examples/wire/batch_get-heterogeneous.request.json + batch_get-heterogeneous.response.json
    - examples/wire/schemas/ (each `*.schema.json` — these are the JSON Schema 2020-12 contracts the validator runs)
  </read_first>
  <action>
    Step 1 — Run the schema validator: `python3 examples/wire/_validate_examples.py`. Assert exits 0. If it fails, capture the failure modes and address them (likely fixture content needs updating to match the migrated server, OR schemas need an update). Phase 13.0 locked the fixtures — they SHOULD already match the verb-style contract this phase implements.

    Step 2 — Spawn an ad-hoc round-trip smoke. Easiest path: write a shell snippet that
    (a) starts a `TestServer` (cargo test target) OR boots `cargo run --release --bin beava` with `BEAVA_TEST_MODE=1`,
    (b) registers the `register-fraud-team.request.json` payload (already exists in examples/wire/) via `curl -X POST .../register`,
    (c) fires the `get-found.request.json` body at `POST /get` via `curl -X POST -H 'Content-Type: application/json' --data @examples/wire/get-found.request.json .../get`,
    (d) compares the response (after pretty-printing via jq if needed) against `get-found.response.json`. Some fields may differ if the example fixture was synthesised — that's OK; the structural assertion is that the response is a flat dict (NOT envelope) AND the schema validator passes.

    For closure-plan simplicity, this round-trip check can be deferred to a single `cargo test --package beava-server --features testing --test phase13_4_get_row_shape` (an existing Phase 13.4 test that already exercises the row shape) — assert it passes after the migration. Add an explicit pointer in VERIFICATION.md.

    Step 3 — Capture both results into VERIFICATION.md (Task 5.d).

    NOTE: if `python3 examples/wire/_validate_examples.py` is not run-able in the current environment (missing python deps like `jsonschema`), surface that and document the manual workaround (read the schema files + read the fixture files; visually confirm shape match against the locked spec). For Phase 13.4.1 closure, the workspace `cargo test --workspace --features testing` GREEN state is the harder gate; the `_validate_examples.py` run is supplementary.
  </action>
  <verify>
    <automated>python3 examples/wire/_validate_examples.py 2>&1 | tail -5; cargo test --package beava-server --features testing --test phase13_4_get_row_shape -- --nocapture 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python3 examples/wire/_validate_examples.py` exits 0 (OR documented manual workaround captured if dep missing)
    - `cargo test --package beava-server --features testing --test phase13_4_get_row_shape` exits 0
    - The wire fixtures `examples/wire/get-found.response.json` + `examples/wire/batch_get-heterogeneous.response.json` are FLAT-row shaped (visual / schema confirmation): `grep -c '"features":' examples/wire/get-found.response.json == 0` (no nested features wrapping in fixtures) AND `grep -c '"features":' examples/wire/batch_get-heterogeneous.response.json == 0`
  </acceptance_criteria>
  <done>
    Examples-wire fixtures round-trip clean against the migrated server. Recorded in working memory for Task 5.d (VERIFICATION.md).
  </done>
</task>

<task type="auto">
  <name>Task 5.b: Run throughput regression-gate small/tcp + append to throughput-baselines.md</name>
  <files>
    - .planning/throughput-baselines.md
  </files>
  <read_first>
    - .planning/throughput-baselines.md lines 1990-2030 (Phase 13.5 closure rows — exact format to mirror)
    - CLAUDE.md §End-to-end throughput regression contract (10% warn / 25% block on small/tcp)
    - The `<interfaces>` block above for the reproduce command
    - Phase 13.5 baseline: 674,108 EPS median of 3 (small/tcp) — 10% warn floor = 606,697 EPS; 25% block floor = 505,581 EPS
  </read_first>
  <action>
    Step 1 — Capture the hw-class string:
    ```bash
    echo "$(uname -sr | tr ' ' '-') / $(getconf _NPROCESSORS_ONLN) cores"
    ```

    Step 2 — Run the throughput harness against the small/tcp cell. Use the SAME harness invocation as Phase 13.5 for an apples-to-apples comparison (per `.planning/throughput-baselines.md` line 1990):
    ```bash
    cargo build --release --workspace
    BEAVA_TEST_MODE=1 cargo run --release --bin beava-bench-v18 -- \
        --wire-format msgpack \
        --blast-shape zipfian \
        --cardinality 10000 \
        --total-events 1000000 \
        --parallel 16 \
        --pipeline-depth 1024 \
        --no-ledger
    ```

    Run 3 times (per Phase 13.5 precedent for the gate cell) and take the median EPS. Capture the median number.

    Step 3 — Compute Δ%: `((post-13.4.1 EPS - 674108) / 674108) * 100`. Determine verdict:
    - PASS if Δ% within ±10% (i.e., post-13.4.1 EPS >= 606,697 OR post-13.4.1 EPS shows improvement)
    - WARN if Δ% between -10% and -25% (i.e., post-13.4.1 EPS in 505,581..606,697 range)
    - BLOCK if Δ% worse than -25% (i.e., post-13.4.1 EPS < 505,581)

    Step 4 — Append a new section to `.planning/throughput-baselines.md` following the Phase 13.5 8-cell-table format:

    ```markdown
    ## Phase 13.4.1 — Server-side wire-spec verb-style migration — small/tcp regression-gate run (Apple-M4)

    **Date:** <today>
    **Harness:** `beava-bench-v18 --wire-format msgpack --blast-shape zipfian --cardinality 10000 --total-events 1000000 --parallel 16 --pipeline-depth 1024 --no-ledger` (small/tcp gate cell — 3 runs, median)
    **Commit:** `<HEAD short SHA after Plan 04>`
    **hw-class:** Darwin-24.3.0 / 10 cores (Apple M4)

    ### Phase 13.4.1 small/tcp regression-gate row

    | Cell | Phase 13.4.1 EPS | Phase 13.5 baseline EPS | Δ% vs 13.5 | Verdict |
    |------|---------------:|------------------------:|-----------:|---------|
    | small/tcp (gate) | <median of 3>      | 674,108                 | <Δ%>       | <PASS/WARN/BLOCK> |

    ### Headline

    **Verdict: <PASS/WARN/BLOCK> at the regression-gate cell.** Phase 13.4.1's small/tcp cell measured **<Δ%>** vs Phase 13.5 baseline (674,108 EPS median). <one-sentence narrative>

    ### Phase 13.4.1 root-cause analysis

    Phase 13.4.1 modifies these hot-path-adjacent sites in `crates/beava-server/`:
    - `apply_shard.rs::WireRequest::HttpGet` body parser — three-step ladder (new shape parse → legacy detection → InternalError fallback). Adds one extra `serde_json::from_slice<Value>` call only on the legacy-shape path; happy-path adds zero overhead beyond the new `HttpGetReq` struct (vs the old `BatchGetBody` struct).
    - `apply_shard.rs::WireRequest::TcpGet` body parser — same three-step ladder; same happy-path overhead profile.
    - `apply_shard.rs::BatchGetReqEntry` custom Deserialize impl — one bool field + visit_map traversal vs the prior derive(Deserialize). Negligible per-entry cost (~10ns).
    - `apply_shard.rs::dispatch_batch_get_sync` — adds a `for entry in &req.requests` alias-WARN scan loop (only emits WARN when `from_alias`); adds a per-entry features-filter `iter().any(...)` check inside the descriptor.features loop; flattens the response constructor (one `Value::Object` push vs one `json!({...})` macro — actually FASTER).
    - `runtime_core_glue.rs::dispatch_get_single_verb_style_sync` — NEW function; single-row hot path that mirrors Case 2 of `dispatch_get_single`. Hot-path EPS attribution depends on which path the bench exercises.

    Expected EPS impact: NEUTRAL TO SLIGHTLY POSITIVE on small/tcp. The legacy `dispatch_get_batch` path (which `WireRequest::HttpGet` previously called) does feature-resolution + cell-cap checks that the new `dispatch_get_single_verb_style_sync` skips, so the migrated path is structurally cheaper.

    ### Verdict

    **<PASS/WARN/BLOCK>.** <verdict justification>
    ```

    Fill in `<median of 3>`, `<Δ%>`, `<PASS/WARN/BLOCK>`, `<HEAD short SHA after Plan 04>`, and the headline narrative based on the actual measurements.

    Step 5 — If the verdict is BLOCK (i.e., -25% or worse), STOP and surface the regression. Do NOT mark the phase CLOSED. The user will need to investigate.
    If the verdict is WARN (-10% to -25%), document the WARN and proceed to closure but note the WARN in VERIFICATION.md.
    If the verdict is PASS, proceed.

    Step 6 — Optional: also run the medium/tcp / large/tcp / fraud-team/tcp cells if box load permits, capture as informational rows. NOT required for closure.

    NOTE: Task 5.b's verdict feeds the SUMMARY narrative, but does NOT gate Task 5.c. Task 5.c ships the focused criterion microbench unconditionally (the CLAUDE.md plan-checker contract for Phase 6+ requires a `crates/*/benches/` path in `files_modified`; we satisfy that gate by default rather than conditionally).
  </action>
  <verify>
    <automated>grep -c '13.4.1' .planning/throughput-baselines.md ; tail -50 .planning/throughput-baselines.md | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## Phase 13.4.1' .planning/throughput-baselines.md >= 1` (new section appended)
    - `grep -c 'small/tcp (gate)' .planning/throughput-baselines.md >= 2` (Phase 13.5 + Phase 13.4.1 rows present)
    - `grep -E 'Verdict.*(PASS|WARN|BLOCK)' .planning/throughput-baselines.md | tail -1` shows a verdict for the new row
    - The Δ% number is recorded as actual measured value (NOT placeholder text)
    - If verdict = BLOCK: PHASE FAILS; do NOT proceed to Task 5.d/5.e/5.f/5.g — instead stop and surface the regression. Task 5.c (microbench file + Cargo.toml entry) MAY still proceed since it's an additive bench skeleton orthogonal to the throughput verdict, but at the user's discretion.
    - If verdict = PASS or WARN: proceed.
  </acceptance_criteria>
  <done>
    Throughput regression-gate row appended; verdict captured. Verdict feeds SUMMARY/VERIFICATION narrative but does not gate Task 5.c.
  </done>
</task>

<task type="auto">
  <name>Task 5.c: Add focused criterion microbench for the migrated dispatch path + register [[bench]] entry + append perf-baselines.md row</name>
  <files>
    - crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs
    - crates/beava-server/Cargo.toml
    - .planning/perf-baselines.md
  </files>
  <read_first>
    - `crates/beava-server/benches/phase12_09_msgpack_get.rs` (full file — closest structural analog; benches `dispatch_get_single_sync` + `dispatch_get_batch_sync` against a warm-cache `AppState`. The new bench mirrors this for the verb-style siblings.)
    - `crates/beava-server/benches/phase12_07_read_path.rs` (secondary analog — same warm-AppState bootstrap pattern)
    - `crates/beava-server/benches/common.rs` (shared bootstrap helpers; reuse if applicable)
    - `crates/beava-server/Cargo.toml` lines 277-314 (`[[bench]]` table format — name + harness = false + optional required-features)
    - `crates/beava-server/src/runtime_core_glue.rs` — pull the public signature of `dispatch_get_single_verb_style_sync` (added by Plan 04). Used to build the bench input.
    - `crates/beava-server/src/apply_shard.rs::dispatch_batch_get_sync` — pull the public signature (already exists pre-Plan-04; Plan 04 modifies internals but signature stable).
    - `.planning/perf-baselines.md` (full file — find the Apple-M4 hw-class section; check format of recent entries, especially Phase 12.09's msgpack/json read-path row for an analog)
    - CLAUDE.md §Performance Discipline (10% warn / 25% block thresholds; plan-checker contract requiring Phase 6+ plans to include at least one task whose `files_modified` contains a path under `crates/*/benches/`)
  </read_first>
  <action>
    CLAUDE.md §Performance Discipline plan-checker contract requires every Phase 6+ plan to include at least one task whose `files_modified` contains a path under `crates/*/benches/`. This task satisfies that contract by adding a focused criterion microbench against the dispatch path Plan 04 migrated.

    The change is a serde shape flip (NEUTRAL-to-POSITIVE delta, covered by the throughput-baselines small/tcp gate in Task 5.b), but the microbench is cheap insurance — a few dozen LOC mirroring `phase12_09_msgpack_get.rs` — that future regressions in the migrated dispatch path will be caught.

    **Step 1 — Create `crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs`**:

    Mirror `phase12_09_msgpack_get.rs` structurally:
    - Single-threaded tokio runtime + warm `AppState` bootstrap via `setup_warm_app_state(n_keys, events_per_key)` (or reuse from `benches/common.rs` if a shared helper exists).
    - Two `criterion_group!` benchmarks:
      1. `dispatch_get_single_verb_style_sync` — fixed input: `app: warm AppState`, `table: "txn"`, `key: "k0"`, `features: Some(vec!["c".into()])` (single-feature filter), `body_format: CT_JSON`. Bench loop calls `dispatch_get_single_verb_style_sync(&app, "txn", "k0", &features, CT_JSON)` in a tight criterion `iter` closure.
      2. `dispatch_batch_get_sync` — fixed input: 10-entry `BatchGetReq` with each entry `{table: "txn", key: format!("k{i}"), features: Some(vec!["c".into()])}`. Bench loop calls `dispatch_batch_get_sync(&app, &req, CT_JSON)` in a tight criterion `iter` closure.
    - `criterion_main!` entry point binding both groups.

    Header doc-comment cites: "Plan 13.4.1-05 Wave 4 — verb-style dispatch microbench. Per CLAUDE.md §Performance Discipline plan-checker contract for Phase 6+. The change is a serde shape flip (NEUTRAL-to-POSITIVE delta, covered by the throughput-baselines small/tcp gate in Plan 13.4.1-05 Task 5.b); this bench is cheap insurance against future regressions."

    Keep the file lean — ≥40 LOC, ≤200 LOC. Use existing `setup_warm_app_state` if `benches/common.rs` exposes it; otherwise lift the pattern from `phase12_09_msgpack_get.rs` lines 38-90 (warm `AppState` with N keys × M Txn events).

    **Step 2 — Register the `[[bench]]` entry in `crates/beava-server/Cargo.toml`**:

    Append after the existing `phase12_8_memory_gov_apply` entry (around line 314) — a NEW additive entry:

    ```toml
    # ─── Plan 13.4.1-05 Task 5.c — verb-style dispatch microbench (perf-discipline gate) ───
    # Two cells covering Plan 13.4.1's migrated dispatch path:
    #   - dispatch_get_single_verb_style_sync (single-row, features filter)
    #   - dispatch_batch_get_sync (10-entry batch, per-entry features filter)
    # The change is a serde shape flip (NEUTRAL-to-POSITIVE delta, covered by
    # the throughput-baselines small/tcp gate); this bench is regression
    # insurance per CLAUDE.md §Performance Discipline. See
    # .planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-05-PLAN.md.
    [[bench]]
    name = "phase13_4_1_dispatch_get_verb_style"
    harness = false
    required-features = ["testing"]
    ```

    `harness = false` is required (criterion provides its own entry point). `required-features = ["testing"]` mirrors `phase12_09_msgpack_get` since the bench depends on the same `AppState` bootstrap path gated behind the `testing` feature.

    NOTE: this is a different, additive entry from Plan 01's Cargo.toml ownership (Plan 01 owned `[[test]]` entries, Task 5.c owns this one new `[[bench]]` entry). No conflict.

    **Step 3 — Compile-check the bench**:

    ```bash
    cargo bench --bench phase13_4_1_dispatch_get_verb_style --no-run --features testing
    ```

    Must exit 0. Running the actual bench is OPTIONAL and not in the path of the workspace gates (it's expensive — minutes of warmup). The compile-only check satisfies the plan-checker contract.

    OPTIONAL: if box load + time permit, run the bench once to capture median ns/op for the perf-baselines row. If skipped, the perf-baselines row records "compile-only — runtime numbers deferred to first regression-suspect run".

    **Step 4 — Append a row to `.planning/perf-baselines.md` Apple-M4 section**:

    ```markdown
    ### Phase 13.4.1 — verb-style dispatch microbench

    Captured: <today>. Commit: `<HEAD short SHA>`. Bench: `crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs`. hw-class: Darwin-24.3.0 / 10 cores (Apple M4).

    | Cell | Median ns/op | Notes |
    |------|------------:|-------|
    | dispatch_get_single_verb_style_sync (1 feature) | `<n>` ns or "compile-only" | New verb-style hot path (Plan 04). Baseline anchor for v0.0.x regression detection. |
    | dispatch_batch_get_sync (10 entries × 1 feature) | `<n>` ns or "compile-only" | Migrated batch dispatch with per-entry features filter + flat-row response constructor. |

    Phase 13.4.1's hot-path change is a server-side wire-spec migration: BatchGetReqEntry custom Deserialize + features-filter loop + flattened response constructor + verb-style `dispatch_get_single_verb_style_sync` function. The change is NEUTRAL-to-POSITIVE on dispatch hot-path EPS:

    - The custom Deserialize visit_map adds ~10 ns/entry vs the prior `derive(Deserialize)` — amortised across batch sizes.
    - The features-filter `iter().any(...)` per-feature check is O(features × filter_len), but `filter_len` is typically 1-3 names; ~15ns per feature.
    - The flattened response constructor (`Value::Object(feature_map)` push vs `json!({"table": ..., "entity_id": ..., "features": Object(...)})`) is FASTER (one allocation vs four).

    The throughput-baselines small/tcp gate (Task 5.b in 13.4.1-05-PLAN) is the canonical end-to-end perf gate for this phase. <Verdict from Task 5.b — PASS at <Δ%>>. This focused microbench is the criterion-level companion: it isolates the dispatch hot path from the transport stack so future regressions in JUST the dispatch code (without touching the transport) will be caught even if end-to-end EPS stays in the noise band.
    ```

    Fill in `<n>` if the bench was actually run, OR leave as "compile-only" with rationale "runtime numbers deferred to first regression-suspect run".
  </action>
  <verify>
    <automated>cargo bench --bench phase13_4_1_dispatch_get_verb_style --no-run --features testing 2>&1 | tail -5; grep -c '13.4.1' .planning/perf-baselines.md; grep -c 'phase13_4_1_dispatch_get_verb_style' crates/beava-server/Cargo.toml; test -f crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs && wc -l crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs</automated>
  </verify>
  <acceptance_criteria>
    - `crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs` exists; `wc -l >= 40`
    - `grep -c 'phase13_4_1_dispatch_get_verb_style' crates/beava-server/Cargo.toml >= 1` (`[[bench]]` entry registered)
    - `grep -c 'harness = false' crates/beava-server/Cargo.toml` count INCREASED by 1 vs HEAD (new bench's harness flag added)
    - `cargo bench --bench phase13_4_1_dispatch_get_verb_style --no-run --features testing` exits 0 (compile-only verification — running the actual bench is optional and not in the workspace-gate path)
    - `grep -c '13.4.1' .planning/perf-baselines.md >= 1` (entry present)
    - The bench file's `criterion_group!` registers BOTH `dispatch_get_single_verb_style_sync` AND `dispatch_batch_get_sync` (or two separate groups bound by `criterion_main!`)
    - `grep -c '13.4.1' crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs >= 1` (file references the phase ID in its header doc-comment)
  </acceptance_criteria>
  <done>
    Verb-style dispatch microbench file exists and compiles; `[[bench]]` entry registered; perf-baselines row appended. CLAUDE.md §Performance Discipline plan-checker contract for Phase 6+ satisfied unconditionally.
  </done>
</task>

<task type="auto">
  <name>Task 5.d: Write VERIFICATION.md — per-success-criterion verification table + workspace gate evidence + throughput cell evidence</name>
  <files>
    - .planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/13.5-python-sdk-and-bench-cli/13.5-11-SUMMARY.md (sibling-phase closure document — format reference for verification narratives)
    - The 6 success criteria from ROADMAP.md Phase 13.4.1 entry (already cited in this plan's must_haves)
    - Tasks 5.a + 5.b + 5.c outputs (memory carried forward)
    - The 3 RED-then-GREEN test files: phase13_4_1_verb_style_get.rs (6 tests), phase13_4_1_verb_style_batch_get.rs (6 tests), phase13_4_1_entity_id_alias_warn.rs (4 tests)
  </read_first>
  <action>
    Create `.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-VERIFICATION.md` (new file) with these sections:

    ```markdown
    # Phase 13.4.1 — Server-side wire-spec verb-style migration — VERIFICATION

    **Verified:** <today>
    **HEAD commit:** `<short SHA>`
    **Verdict:** <PASS / PASS-WITH-WARN / FAIL>

    ## Success Criteria Verification

    Per ROADMAP.md Phase 13.4.1 entry — 6 success criteria:

    | # | Criterion | Evidence | Status |
    |---|-----------|----------|--------|
    | 1 | `POST /get` and `OP_GET` accept `{table, key, features?}` JSON body; reject the legacy `{keys, features}` and `{feature, key}` shapes with structured error | `cargo test --package beava-server --features testing --test phase13_4_1_verb_style_get` reports `6 passed; 0 failed` (Plan 01 GREEN — Tests 1, 2 cover happy paths; Tests 3, 4 cover legacy-shape rejection on HTTP; Tests 5, 6 cover OP_GET CT_JSON + CT_MSGPACK). | ✅ PASS |
    | 2 | `POST /batch_get` and `OP_BATCH_GET` accept per-entry `{table, key, features?}` and return flat rows | `cargo test --package beava-server --features testing --test phase13_4_1_verb_style_batch_get` reports `6 passed; 0 failed` (Plan 02 GREEN — Tests 1, 5, 6 cover flat-row shape on HTTP + TCP; Test 2 covers per-entry filter; Tests 3, 4 cover D-06 omit-on-absent vs feature-not-found). | ✅ PASS |
    | 3 | When `features` is present, response is narrowed to those keys only | Test 2 of `phase13_4_1_verb_style_batch_get.rs` (per-entry filter narrows) + Test 3 (omit-on-absent) — both GREEN. | ✅ PASS |
    | 4 | `examples/wire/*.json` fixtures (Phase 13.0) round-trip clean | `python3 examples/wire/_validate_examples.py` exits 0 (Task 5.a); existing `phase13_4_get_row_shape.rs` still GREEN. Fixtures already match the verb-style FLAT-row contract (locked Phase 13.0). | ✅ PASS |
    | 5 | `cargo test --workspace --features testing` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` + `cargo fmt --all --check` GREEN | See "Workspace Gates" section below — all three exit 0. | ✅ PASS |
    | 6 | Throughput regression-gate `small/tcp` within ±10% of Phase 13.5 baseline | See "Throughput Regression Gate" section below — Δ% = `<actual>` vs Phase 13.5 baseline of 674,108 EPS. Verdict: `<PASS/WARN>`. | <✅ PASS / ⚠ WARN> |

    ## Decision Coverage

    All 6 USER-LOCKED decisions from CONTEXT.md are implemented and tested:

    | Decision | Implementation | Test |
    |----------|----------------|------|
    | D-01 — verb-style `POST /get` + `OP_GET` | `apply_shard.rs::WireRequest::HttpGet`/`TcpGet` migrated; `runtime_core_glue.rs::dispatch_get_single_verb_style_sync` added | `phase13_4_1_verb_style_get.rs` Tests 1, 2, 5, 6 |
    | D-02 — verb-style per-entry `{table, key, features?}` on `POST /batch_get` + `OP_BATCH_GET` | `apply_shard.rs::BatchGetReqEntry` added `features: Option<Vec<String>>` field; per-entry filter in `dispatch_batch_get_sync` | `phase13_4_1_verb_style_batch_get.rs` Tests 1, 2, 5 |
    | D-03 — FLAT row response shape on `/batch_get` | `apply_shard.rs::dispatch_batch_get_sync` per-row constructor flattened (success + error tuples); `phase13_4_op_batch_get.rs` Tests 1, 2, 5 expected-payload assertions migrated | `phase13_4_1_verb_style_batch_get.rs` Tests 1, 5, 6; `phase13_4_op_batch_get.rs` Tests 1, 2, 5 |
    | D-04 — `entity_id` → `key` rename + one-release `serde(alias)` deprecation | `apply_shard.rs::BatchGetReqEntry` custom Deserialize impl with `from_alias` flag; `tracing::warn!` emitted in dispatch loop on alias use | `phase13_4_1_entity_id_alias_warn.rs` Tests 1, 2, 3, 4 |
    | D-05 — Structured 400 rejection of legacy request shapes | `runtime_core_glue.rs::GlueResponse::UnsupportedRequestShape` variant; HTTP encoder maps to 400, TCP encoder to OP_ERROR_RESPONSE; three-step detection ladder in `WireRequest::HttpGet`/`TcpGet` | `phase13_4_1_verb_style_get.rs` Tests 3, 4 |
    | D-06 — Missing-feature filter behavior: OMIT from response | `dispatch_get_single_verb_style_sync` + `dispatch_batch_get_sync` both have features-filter narrowing pass that skips on absent values; upfront feature-not-in-descriptor errors with `feature_not_found` (preserves the registry-typo vs per-entity-sparsity distinction) | `phase13_4_1_verb_style_batch_get.rs` Tests 2, 3, 4 |

    ## Workspace Gates

    All run at HEAD `<short SHA>`:

    | Gate | Command | Result |
    |------|---------|--------|
    | Format | `cargo fmt --all --check` | ✅ exits 0 |
    | Lint | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | ✅ exits 0 |
    | Tests | `cargo test --workspace --features testing` | ✅ exits 0 (`<N>` tests passed; `0` failed) |
    | mio-only invariant | `cargo test --package beava-server --features testing --test phase12_6_mio_only_dataplane` | ✅ exits 0 |
    | events-only invariant | `cargo test --package beava-server --features testing --test phase12_7_no_table_surface` | ✅ exits 0 |
    | Verb-style dispatch microbench compile | `cargo bench --bench phase13_4_1_dispatch_get_verb_style --no-run --features testing` | ✅ exits 0 |

    ## Phase 13.4.1 RED→GREEN summary

    16 RED tests landed in Plans 01 + 02 + 03; all 16 turned GREEN by Plan 04:

    - Plan 01 RED → GREEN: `phase13_4_1_verb_style_get.rs` (6 tests)
    - Plan 02 RED → GREEN: `phase13_4_1_verb_style_batch_get.rs` (6 tests)
    - Plan 03 RED → GREEN: `phase13_4_1_entity_id_alias_warn.rs` (4 tests)

    Plus 3 envelope-shape assertions in `phase13_4_op_batch_get.rs` (Tests 1, 2, 5) flattened to assert the new D-03 contract; Tests 3 + 4 unchanged.

    ## Throughput Regression Gate

    Per CLAUDE.md §End-to-end throughput regression contract:

    | Cell | Phase 13.4.1 EPS | Phase 13.5 baseline EPS | Δ% | Verdict |
    |------|---------------:|------------------------:|---:|---------|
    | small/tcp (gate) | `<median of 3>` | 674,108 | `<Δ%>` | `<PASS/WARN/BLOCK>` |

    Full row appended to `.planning/throughput-baselines.md` (Apple-M4 hw-class section).

    ## Verb-Style Dispatch Microbench (CLAUDE.md §Performance Discipline)

    Per CLAUDE.md §Performance Discipline plan-checker contract for Phase 6+:

    | Bench | Compile | Cells | Path |
    |-------|---------|-------|------|
    | `phase13_4_1_dispatch_get_verb_style` | ✅ exits 0 | `dispatch_get_single_verb_style_sync (1 feature)` + `dispatch_batch_get_sync (10x1 feature)` | `crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs` |

    Row appended to `.planning/perf-baselines.md` (Apple-M4 hw-class section).

    ## Examples-Wire Round-Trip Validation

    `python3 examples/wire/_validate_examples.py`: ✅ exits 0. All `<op>-<scenario>.<request|response|error>.json` fixtures match their JSON Schema 2020-12 contracts. The locked Phase 13.0 fixtures (`get-found.*`, `batch_get-heterogeneous.*`) ARE flat-row shaped — they were authored against the same verb-style contract this phase implements.

    ## Open Items

    - <list any deferrals or follow-ups discovered during the phase — e.g., the `OP_MGET 0x0021` / `OP_GET_MULTI 0x0022` opcodes still exist in the codebase but are not in the v0 wire-spec; v0.0.x cleanup should remove them>
    - <if Task 5.b verdict = WARN, surface the warning here with investigation steps>
    ```

    Fill in the `<...>` placeholders with actual values captured from Tasks 5.a, 5.b, 5.c, and the final HEAD SHA.
  </action>
  <verify>
    <automated>test -f .planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-VERIFICATION.md && wc -l .planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-VERIFICATION.md`
    - `wc -l` >= 100
    - All 6 success-criteria rows in the table show evidence + status (✅ PASS or ⚠ WARN)
    - All 6 D-XX decision rows present in the Decision Coverage table
    - Workspace Gates section reports actual gate exit-status results AND includes the new microbench compile-check row
    - Throughput Regression Gate row has actual Δ% (not placeholder)
    - Verb-Style Dispatch Microbench section cites the new bench file path
    - `grep -c "✅" .planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-VERIFICATION.md >= 5`
  </acceptance_criteria>
  <done>
    VERIFICATION.md captures the closure evidence; phase verdict (PASS / PASS-WITH-WARN / FAIL) determined.
  </done>
</task>

<task type="auto">
  <name>Task 5.e: Write SUMMARY.md — phase narrative + migration diff + hand-off</name>
  <files>
    - .planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-04-PLAN.md (the GREEN cluster — pull commit list + sub-task narratives)
    - .planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-05-PLAN.md (downstream consumer — `phase_depends_on: [13.4.1]`)
    - Tasks 5.b + 5.c + 5.d outputs (memory carried forward)
  </read_first>
  <action>
    Create `.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-SUMMARY.md` (new file) with these sections:

    ```markdown
    # Phase 13.4.1 — Server-side wire-spec verb-style migration — SUMMARY

    **Closed:** <today>
    **Verdict:** <PASS / PASS-WITH-WARN / FAIL>
    **Plans:** 5 (3 RED in Wave 1, 1 GREEN cluster in Wave 2, 1 closure in Wave 3)
    **Commits:** 8 (3 `test:` + 5 `feat:`/`refactor:`/`docs:`)
    **HEAD:** `<short SHA>`

    ## Phase Goal

    Migrate server-side `POST /get`, `OP_GET`, `POST /batch_get`, and `OP_BATCH_GET` to the locked verb-style wire contract from Phase 13.0 (`docs/wire-spec.md` + `docs/http-api.md`). Single-row body `{table, key, features?}` on POST /get + OP_GET; per-entry `{table, key, features?}` on batch_get + OP_BATCH_GET. Response shape FLAT ROWS — one flat dict per entry, no `{table, entity_id, features:{...}}` envelope. Apply `features` filter at server. `serde(alias = "entity_id")` on the renamed `key` field for one-release back-compat.

    ## Deficit Origin

    Phase 13.4 (engine prep) was responsible for migrating the server to the verb-style contract but landed with the legacy Phase 12.x shapes still in place:
    - `WireRequest::HttpGet` parsed legacy `{keys, features}` (the 2D-cell shape from Plan 12-07/12-09)
    - `WireRequest::TcpGet` parsed legacy `{feature, key}` (legacy single-feature shape)
    - `BatchGetReqEntry` used `entity_id` (not `key`) and had no per-entry `features` field
    - `dispatch_batch_get_sync` response was wrapped `{table, entity_id, features:{...}}` — not flat

    Discovered during `/gsd-plan-phase 13.5.1` goal-backward audit on 2026-05-04 — the 13.5.1 SDK transports were planned to send verb-style bodies, which would 500 at the server boundary. This phase (Phase 13.4.1) was inserted to fix the server-side gap before Phase 13.5.1 Plan 05 (SDK transport impl) executes.

    ## Migration Diff

    - **`crates/beava-server/src/runtime_core_glue.rs`:**
      - Added `GlueResponse::UnsupportedRequestShape { hint: String }` variant.
      - Added `pub fn dispatch_get_single_verb_style_sync(app, table, key, features, body_format)` function — verb-style single-row dispatch with features filter.
      - Legacy `dispatch_get_single` and `dispatch_get_batch` functions PRESERVED (legacy `HttpGetSingle` + `TcpMGet`/`TcpGetMulti` arms still call them).

    - **`crates/beava-server/src/apply_shard.rs`:**
      - `WireRequest::HttpGet { body }` arm migrated to verb-style three-step ladder: (a) try new `{table, key, features?}` shape; (b) on parse fail, detect legacy shapes (`{keys, features}` 2D-cell or `{feature, key}` single-feature) and return `UnsupportedRequestShape`; (c) on truly malformed JSON fall through to `InternalError`.
      - `WireRequest::TcpGet { body, body_format }` arm migrated identically with CT_JSON / CT_MSGPACK codec dispatch (D-A/D-B carry-forward).
      - `BatchGetReqEntry` struct rewritten with custom Deserialize impl tracking `from_alias: bool` (D-04 STRICT alias detection).
      - `dispatch_batch_get_sync` per-row response constructor flattened: `Value::Object(feature_map)` push instead of `json!({"table": ..., "entity_id": ..., "features": Object(...)})` wrap. Per-entry features filter applied. Per-tuple error tuples flattened. Upfront feature-not-in-descriptor validation preserves `feature_not_found` distinction from per-entity sparsity.
      - `tracing::warn!` emitted on alias use with verbatim D-04 message text.
      - Legacy arms preserved untouched: `WireRequest::HttpGetSingle`, `WireRequest::TcpMGet`, `WireRequest::TcpGetMulti`, `WireRequest::HttpBatchGet`.

    - **`crates/beava-server/src/server.rs`:**
      - HTTP encoder match arm: `GlueResponse::UnsupportedRequestShape { hint }` → 400 with body `{"error":{"code":"unsupported_request_shape","message":hint}}`.
      - TCP encoder match arm: same body wrapped in `OP_ERROR_RESPONSE` (0xFFFF) frame.

    - **`crates/beava-server/tests/phase13_4_op_batch_get.rs`:**
      - Tests 1, 2, 5 expected-payload assertions flattened from envelope to flat-row form.
      - Test 1 also exercises the `entity_id` alias path on one entry to smoke-test D-04.
      - Tests 3 (empty batch) + 4 (HTTP/TCP byte-equivalent) unchanged.

    - **`crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs`:** NEW criterion microbench measuring `dispatch_get_single_verb_style_sync` + `dispatch_batch_get_sync` against a warm `AppState`. Mirrors the structure of `phase12_09_msgpack_get`. Two cells: 1-feature single-row + 10-entry × 1-feature batch. Compiles green at HEAD; runtime numbers optional (deferred to first regression-suspect run).

    - **`crates/beava-server/Cargo.toml`:** Added `[[bench]]` entry registering `phase13_4_1_dispatch_get_verb_style` (`harness = false`, `required-features = ["testing"]`).

    - **`.planning/ideas/v0.1-deferrals.md`:**
      - New entry scheduling v0.0.x removal of the `entity_id` serde alias.

    ## Decisions Hit

    All 6 USER-LOCKED decisions from CONTEXT.md implemented:

    1. **D-01** — verb-style `{table, key, features?}` on POST /get + OP_GET — Plan 04 Task 4.c.
    2. **D-02** — verb-style per-entry `{table, key, features?}` on batch_get + OP_BATCH_GET — Plan 04 Task 4.b.
    3. **D-03** — FLAT row response on /batch_get + flattened phase13_4 tests — Plan 04 Tasks 4.b + 4.d.
    4. **D-04** — `entity_id` → `key` rename with one-release alias + WARN log (STRICT detection via custom Deserialize) — Plan 04 Task 4.b + Task 4.e (v0.0.x schedule entry).
    5. **D-05** — structured 400 rejection of legacy shapes via `GlueResponse::UnsupportedRequestShape` — Plan 04 Tasks 4.a + 4.c.
    6. **D-06** — missing-feature filter omits from response (registry-typo vs per-entity-sparsity distinction preserved) — Plan 04 Tasks 4.b + 4.c.

    ## Test Count Delta

    - 16 new RED tests landed in Plans 01 + 02 + 03; all 16 GREEN at HEAD.
    - 3 existing tests in `phase13_4_op_batch_get.rs` flattened to new contract; 5/5 GREEN.
    - 0 tests deleted.
    - 1 new criterion microbench file (`phase13_4_1_dispatch_get_verb_style.rs`); 2 cells. Compiles green; runtime optional.

    ## Throughput Regression Gate

    `small/tcp` regression-gate cell: Δ% = `<actual>` vs Phase 13.5 baseline (674,108 EPS). Verdict: `<PASS/WARN/BLOCK>`. Full row: `.planning/throughput-baselines.md` Apple-M4 section.

    ## Hand-off to 13.5.1

    Phase 13.5.1 Plan 05 has `phase_depends_on: [13.4.1]`. Plan 05's must_haves include:
    > "Phase 13.4.1 lands BEFORE this plan executes — server-side `POST /get`, `OP_GET`, `POST /batch_get`, and `OP_BATCH_GET` already speak verb-style `{table, key, features?}` and `/batch_get` returns FLAT ROWS (one flat dict per entry, no `{table, entity_id, features:{...}}` wrapping)."

    With Phase 13.4.1 CLOSED, Phase 13.5.1 Plan 05 (SDK transport impl) is unblocked. The Python SDK can now safely send verb-style requests against `POST /get` + `OP_GET` + `POST /batch_get` + `OP_BATCH_GET` and consume flat-row responses directly from `body["results"]` without per-element unwrapping.

    ## Open Items / v0.0.x Cleanup

    - **`entity_id` serde alias on BatchGetReqEntry** — schedule entry added to `.planning/ideas/v0.1-deferrals.md`. Removal in first v0.0.x patch cycle.
    - **`OP_MGET 0x0021` and `OP_GET_MULTI 0x0022`** — these opcodes still exist in `crates/beava-runtime-core/src/wire_request.rs` and `apply_shard.rs:513-577` but are NOT in the locked v0 wire-spec (`docs/wire-spec.md` lists only OP_GET, OP_BATCH_GET, OP_GET_RESPONSE). Out of scope for Phase 13.4.1 (would have been scope creep); should be removed in v0.0.x cleanup.
    - **`WireRequest::HttpGetSingle` (`GET /get/:feature/:key` legacy path-encoded route)** — preserved untouched in this phase. Its removal also belongs to v0.0.x cleanup.

    ## Cross-references

    - `.planning/ROADMAP.md` Phase 13.4.1 entry — status now ✅ CLOSED
    - `.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-CONTEXT.md` — 6 user-locked decisions
    - `.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-PATTERNS.md` — analog pattern map
    - `.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-VERIFICATION.md` — sister-doc with the closure evidence table
    - `.planning/phases/13.5.1-transport-impl-decorator-hardening/13.5.1-05-PLAN.md` — downstream consumer, now unblocked
    - `.planning/throughput-baselines.md` — Phase 13.4.1 small/tcp row appended
    - `.planning/perf-baselines.md` — Phase 13.4.1 verb-style dispatch microbench row appended
    - `crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs` — new focused criterion microbench (compile-only acceptance)
    ```

    Fill in the `<...>` placeholders with actual values from Tasks 5.b + 5.c + 5.d.
  </action>
  <verify>
    <automated>test -f .planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-SUMMARY.md && wc -l .planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `wc -l >= 80`
    - All 6 D-XX decisions cited
    - Hand-off section explicitly mentions Phase 13.5.1 Plan 05
    - Throughput regression gate row cited with actual Δ%
    - Migration Diff section cites the new bench file + Cargo.toml `[[bench]]` entry
    - Open Items section lists the OP_MGET / OP_GET_MULTI / HttpGetSingle v0.0.x cleanup followups
  </acceptance_criteria>
  <done>
    SUMMARY.md captures the closing narrative.
  </done>
</task>

<task type="auto">
  <name>Task 5.f: Update ROADMAP.md — flip Phase 13.4.1 status to CLOSED</name>
  <files>
    - .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/ROADMAP.md lines 807-833 (Phase 13.4.1 entry — current status `📋 PLANNED 2026-05-04`)
    - Tasks 5.b + 5.d outputs (verdict)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md`:

    Step 1 — Update line 807 from:
    ```
    ### Phase 13.4.1: Server-side wire-spec verb-style migration (Phase 13.4 fix-up) — 📋 PLANNED 2026-05-04
    ```
    to:
    ```
    ### Phase 13.4.1: Server-side wire-spec verb-style migration (Phase 13.4 fix-up) — ✅ CLOSED <today> (<VERDICT>)
    ```
    where `<VERDICT>` is `PASS` or `PASS-WITH-WARN` per Tasks 5.b + 5.d outputs.

    Step 2 — Append a closing paragraph after line 833 (the existing "Estimated wall-clock: 1-2 days." line). Format:

    ```markdown

    **Closed:** <today>. SUMMARY: `.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-SUMMARY.md`. VERIFICATION: `.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-VERIFICATION.md`. All 6 success criteria GREEN. 16 RED tests turned GREEN across Plans 01/02/03; 3 envelope-shape assertions in `phase13_4_op_batch_get.rs` flattened. Throughput regression-gate small/tcp = `<Δ%>` vs Phase 13.5 baseline — `<PASS/WARN>`. New focused criterion microbench `crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs` registered + compile-green (perf-discipline gate). Phase 13.5.1 Plan 05 (SDK transport impl) is now unblocked. v0.0.x cleanup followups: drop `entity_id` serde alias on BatchGetReqEntry; remove `OP_MGET`/`OP_GET_MULTI` (not in v0 wire-spec); remove `WireRequest::HttpGetSingle` legacy `GET /get/:feature/:key` route.
    ```

    Step 3 — If a top-of-file phase-progress summary exists (e.g., line 76-94 with "Total active phases" or similar), update the count of CLOSED phases to reflect Phase 13.4.1's closure. Inspect the existing summary and adjust if needed.

    Do NOT modify any other Phase entry. Do NOT renumber any phases. Do NOT touch the v0 phase block (Phase 13 umbrella) status.
  </action>
  <verify>
    <automated>grep -F "Phase 13.4.1" .planning/ROADMAP.md | head -3 ; grep -c "✅ CLOSED" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 13.4.1.*✅ CLOSED" .planning/ROADMAP.md >= 1` (status flipped)
    - `grep -c "Phase 13.4.1.*📋 PLANNED" .planning/ROADMAP.md == 0` (old status removed)
    - `grep -c "13.4.1-SUMMARY.md" .planning/ROADMAP.md >= 1` (cross-reference present)
    - Closing paragraph mentions Phase 13.5.1 Plan 05 unblocking: `grep -F "13.5.1 Plan 05" .planning/ROADMAP.md` exits 0 OR `grep -F "13.5.1" .planning/ROADMAP.md` reports an existing reference plus the new one
  </acceptance_criteria>
  <done>
    ROADMAP.md status updated. Phase 13.4.1 visibly CLOSED.
  </done>
</task>

<task type="auto">
  <name>Task 5.g: Final commit — closure artifacts + ROADMAP update</name>
  <files>(commit only)</files>
  <read_first>
    - CLAUDE.md §Conventions; memory `feedback_no_ai_attribution_in_commits`
  </read_first>
  <action>
    Stage:
    - `.planning/throughput-baselines.md`
    - `.planning/perf-baselines.md`
    - `crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs`
    - `crates/beava-server/Cargo.toml`
    - `.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-VERIFICATION.md`
    - `.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-SUMMARY.md`
    - `.planning/ROADMAP.md`

    Commit with message:

    ```
    docs(13.4.1-05): close phase 13.4.1 — server-side verb-style wire-spec migration (<VERDICT>)

    Closure artefacts for Phase 13.4.1 — Server-side wire-spec verb-style
    migration (Phase 13.4 fix-up):

    - throughput-baselines.md: small/tcp regression-gate row appended
      (<Δ%> vs Phase 13.5 baseline of 674,108 EPS — <PASS/WARN>)
    - perf-baselines.md: Phase 13.4.1 verb-style dispatch microbench row
      appended (compile-only acceptance; runtime numbers optional)
    - benches/phase13_4_1_dispatch_get_verb_style.rs: NEW focused criterion
      microbench for dispatch_get_single_verb_style_sync +
      dispatch_batch_get_sync (perf-discipline gate per CLAUDE.md)
    - Cargo.toml: [[bench]] entry registering the new bench
    - 13.4.1-VERIFICATION.md: all 6 success criteria GREEN; 6 D-XX decisions
      mapped to implementation + tests; workspace gates GREEN
    - 13.4.1-SUMMARY.md: deficit narrative + migration diff + decisions
      hit-list + hand-off to Phase 13.5.1 Plan 05 (SDK transport impl, now
      unblocked)
    - ROADMAP.md: phase 13.4.1 status flipped to ✅ CLOSED

    All 16 RED tests from Plans 01 + 02 + 03 turned GREEN by Plan 04. Phase
    13.4 sibling test file (phase13_4_op_batch_get.rs) had 3 envelope-shape
    assertions flattened to assert the new D-03 flat-row contract;
    remaining 2 tests (empty batch + HTTP/TCP byte-equivalent) unchanged.

    Phase 13.5.1 Plan 05 unblocked.

    v0.0.x cleanup followups documented: drop entity_id serde alias on
    BatchGetReqEntry; remove OP_MGET (0x0021) / OP_GET_MULTI (0x0022)
    opcodes (not in v0 wire-spec); remove WireRequest::HttpGetSingle
    legacy path-encoded GET route.
    ```

    No `Co-Authored-By: Claude` line.

    Final assertion: `git log --format='%s' -10 | grep -c '13.4.1' >= 9` (3 RED + 5 GREEN + 1 closure = 9 commits referencing 13.4.1).
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -q '^docs(13.4.1-05):' ; git status --porcelain | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%s'` starts with `docs(13.4.1-05):`
    - `git log -1 --format='%B' | grep -c "Co-Authored-By: Claude" == 0`
    - `git log -1 --stat | grep -cE "ROADMAP.md|throughput-baselines.md|perf-baselines.md|VERIFICATION.md|SUMMARY.md|phase13_4_1_dispatch_get_verb_style.rs|Cargo.toml" >= 7`
    - Working tree clean: `git status --porcelain | wc -l == 0`
    - `git log --format='%s' -10 | grep -c '13.4.1' >= 9`
  </acceptance_criteria>
  <done>
    Phase 13.4.1 closed. All artifacts committed. Phase 13.5.1 Plan 05 unblocked.
  </done>
</task>

</tasks>

<verification>
- Tasks 5.a → 5.g executed in order.
- Examples-wire fixtures round-trip clean.
- Throughput-baselines small/tcp row appended; verdict captured.
- New focused criterion microbench file `crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs` exists and compiles; `[[bench]]` entry registered in `crates/beava-server/Cargo.toml`; perf-baselines.md row appended (CLAUDE.md §Performance Discipline plan-checker contract for Phase 6+ satisfied unconditionally).
- VERIFICATION.md captures per-success-criterion evidence.
- SUMMARY.md captures deficit narrative + migration diff + decisions hit-list + hand-off.
- ROADMAP.md status flipped to ✅ CLOSED.
- One closure commit added; tree clean; 9+ commits reference 13.4.1.
</verification>

<success_criteria>
1. `python3 examples/wire/_validate_examples.py` exits 0 (or documented manual workaround).
2. Throughput-baselines small/tcp row appended; verdict PASS or PASS-WITH-WARN (BLOCK aborts the phase).
3. `crates/beava-server/benches/phase13_4_1_dispatch_get_verb_style.rs` exists, compiles (`cargo bench --bench phase13_4_1_dispatch_get_verb_style --no-run --features testing` exits 0), and is registered as a `[[bench]]` in `crates/beava-server/Cargo.toml`. Perf-baselines.md row appended.
4. `13.4.1-VERIFICATION.md` exists, ≥ 100 LOC, captures all 6 success criteria + decision coverage + workspace gates + throughput row + microbench compile-row.
5. `13.4.1-SUMMARY.md` exists, ≥ 80 LOC, captures deficit narrative + migration diff + decisions + hand-off.
6. ROADMAP.md Phase 13.4.1 entry shows `✅ CLOSED <date> (<VERDICT>)`.
7. One `docs(13.4.1-05):`-prefixed commit landed; no AI attribution; ≥ 9 commits in history reference 13.4.1.
8. `cargo test --workspace --features testing` still GREEN at HEAD.
</success_criteria>

<output>
This IS the closure plan — its outputs are SUMMARY.md + VERIFICATION.md + the new bench file + the perf-baselines/throughput-baselines rows (artifacts of THIS plan, not a deliverable for a future plan). No further plan-summary is needed.
</output>
</content>
</invoke>