---
phase: 13.4.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/beava-server/tests/phase13_4_1_verb_style_get.rs
  - crates/beava-server/Cargo.toml  # exclusive Wave 1 Cargo.toml ownership — also registers Plans 02 + 03 [[test]] entries up front so Plans 02 + 03 only touch their own test .rs files
autonomous: true
requirements:
  - D-01
  - D-05
tdd: true
tags:
  - server-wire-spec
  - tdd-red
  - http-get
  - tcp-op-get
  - verb-style

must_haves:
  truths:
    - "A failing integration test asserts `POST /get` accepts `{table, key}` JSON body and returns a flat dict (the verb-style D-01 contract)"
    - "A failing integration test asserts `POST /get` with `features: [\"c\"]` narrows the response to that single key (D-01 + D-06 sparsity preview)"
    - "A failing integration test asserts `POST /get` rejects the legacy `{keys, features}` shape with HTTP 400 + body `{\"error\":{\"code\":\"unsupported_request_shape\",\"message\": \"<doc-hint>\"}}` (D-05)"
    - "A failing integration test asserts `POST /get` rejects the legacy `{feature, key}` shape with the same structured 400 (D-05)"
    - "A failing integration test asserts `OP_GET` (TCP) accepts `{table, key, features?}` body and returns the flat dict in an `OP_GET_RESPONSE` frame (CT_JSON path; D-01)"
    - "A failing integration test asserts `OP_GET` (TCP) accepts the same body via `CT_MSGPACK` content-type byte and returns `OP_GET_RESPONSE` with `CT_MSGPACK` body (D-01 + D-A/D-B codec discipline carry-forward)"
    - "All tests run via `#[cfg(feature = \"testing\")]` and the new `[[test]]` entry; `cargo test --package beava-server --features testing --test phase13_4_1_verb_style_get` exits NON-ZERO at end of this plan (RED commit)"
    - "Cargo.toml `[[test]]` table registers `phase13_4_1_verb_style_get` with `required-features = [\"testing\"]`"
  artifacts:
    - path: "crates/beava-server/tests/phase13_4_1_verb_style_get.rs"
      provides: "RED integration tests for D-01 (verb-style POST /get + OP_GET) and D-05 (structured-400 rejection of legacy shapes)"
      contains: "phase13_4_1_verb_style_get_returns_flat_dict"
      contains_2: "phase13_4_1_legacy_keys_features_shape_rejected"
      contains_3: "phase13_4_1_legacy_feature_key_shape_rejected"
      contains_4: "phase13_4_1_op_get_returns_flat_dict_msgpack"
      contains_5: "unsupported_request_shape"
      min_lines: 200
    - path: "crates/beava-server/Cargo.toml"
      provides: "[[test]] table entry for phase13_4_1_verb_style_get with required-features = [\"testing\"]"
      contains: "phase13_4_1_verb_style_get"
  key_links:
    - from: "crates/beava-server/tests/phase13_4_1_verb_style_get.rs"
      to: "crates/beava-server/src/apply_shard.rs::WireRequest::HttpGet body parser"
      via: "POST /get with verb-style body — Plan 04 makes this GREEN by migrating the parser"
      pattern: "phase13_4_1_verb_style_get_returns_flat_dict"
    - from: "crates/beava-server/tests/phase13_4_1_verb_style_get.rs"
      to: "crates/beava-server/src/apply_shard.rs (D-05 unsupported_request_shape detection)"
      via: "expect 400 + structured error body — Plan 04 adds GlueResponse::UnsupportedRequestShape and wires the legacy-shape detection"
      pattern: "unsupported_request_shape"
    - from: "crates/beava-server/tests/phase13_4_1_verb_style_get.rs"
      to: "crates/beava-server/src/apply_shard.rs::WireRequest::TcpGet body parser"
      via: "OP_GET (0x0020) frame — Plan 04 migrates the TcpGet body parser to {table, key, features?}"
      pattern: "OP_GET_RESPONSE"
---

<objective>
RED commit — write the failing integration tests that nail down the verb-style `POST /get` and `OP_GET` contracts (D-01) plus the structured-400 rejection of the legacy `{keys, features}` and `{feature, key}` shapes (D-05). The tests MUST fail when this plan lands; Plan 04 makes them GREEN.

Purpose: per CLAUDE.md §Conventions TDD discipline, every code-touching task splits into a `test:` red commit (failing test) followed by `feat:`/`fix:`/`refactor:` green commit. This plan is the RED for the GET migration.

Output: one new file `crates/beava-server/tests/phase13_4_1_verb_style_get.rs` (~200 LOC, 6 tests) plus a `[[test]]` table entry in `crates/beava-server/Cargo.toml`. The test binary will compile and run, but EVERY test inside it MUST fail (assertion-fail or status-mismatch) at the end of this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-CONTEXT.md
@.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-PATTERNS.md
@CLAUDE.md
@docs/http-api.md
@docs/wire-spec.md
@examples/wire/get-found.request.json
@examples/wire/get-found.response.json

<analog_files>
- crates/beava-server/tests/phase13_4_op_batch_get.rs — closest analog (TestServer spawn + register helper + tcp_send_recv_frame harness; this plan REUSES the same helpers but for `POST /get` + `OP_GET 0x0020`)
- crates/beava-server/tests/phase12_09_dispatch_msgpack_test.rs — analog for CT_MSGPACK frame round-trip (`encode_frame(OP_GET, CT_MSGPACK, ...)` + `decode_frame` body parse via `rmp_serde::from_slice`)
- crates/beava-server/tests/phase12_07_tcp_get_test.rs — analog for OP_GET frame send/receive
</analog_files>

<interfaces>
<!-- Key types the new test file needs. All are existing exports. -->

From `beava_core::wire`:
```rust
pub const OP_GET: u16 = 0x0020;
pub const OP_GET_RESPONSE: u16 = 0x0023;
pub const CT_JSON: u8 = 0x01;
pub const CT_MSGPACK: u8 = 0x02;
pub fn encode_frame(frame: &Frame, out: &mut BytesMut);
pub fn decode_frame(buf: &mut BytesMut, max: usize) -> Result<Option<Frame>, ...>;
pub struct Frame { pub op: u16, pub content_type: u8, pub payload: Bytes; pub fn new(op, ct, payload) -> Frame }
```

From `beava_server::testing`:
```rust
pub struct TestServer { /* ... */ }
impl TestServer {
    pub async fn spawn() -> std::io::Result<Self>;
    pub fn base_url(&self) -> String;
    pub fn tcp_addr(&self) -> Option<std::net::SocketAddr>;
    pub async fn post_json<T: Serialize>(&self, path: &str, body: &T) -> reqwest::Result<reqwest::Response>;
    pub async fn shutdown(self) -> std::io::Result<()>;
}
```

The pipeline registration helper (copy/adapt from `phase13_4_op_batch_get.rs::register_payload`) MUST register a single `UserSpend(user_id) → cnt, total` aggregation driven by a `Tx` event so we have a known table + key + multi-feature row to query.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (red): Register ALL THREE Phase 13.4.1 [[test]] entries in Cargo.toml (exclusive Wave 1 owner)</name>
  <files>crates/beava-server/Cargo.toml</files>
  <read_first>
    - crates/beava-server/Cargo.toml lines 22-100 (existing `[[test]]` blocks; note the consistent `required-features = ["testing"]` pattern)
    - PATTERNS.md cross-cutting pattern §1 (CT_JSON / CT_MSGPACK codec dispatch)
  </read_first>
  <action>
    **Wave coordination note:** Plan 01 is the EXCLUSIVE Wave 1 owner of `crates/beava-server/Cargo.toml`. To avoid a 3-way merge conflict on parallel-run Wave 2 plans (02 + 03), this task registers ALL THREE Phase 13.4.1 test [[test]] entries up front. Plans 02 and 03 only create their .rs test files — they do NOT touch Cargo.toml.

    Append THREE new `[[test]]` table entries to `crates/beava-server/Cargo.toml` AFTER the existing `phase13_4_op_batch_get` entry. The Plan 02 + Plan 03 .rs files do not yet exist when Plan 01 runs — that is OK; cargo only validates the [[test]] entries when their respective tests are invoked. Plan 02 + Plan 03 will create their .rs files in Wave 2.

    Append exactly:

    ```toml
    [[test]]
    name = "phase13_4_1_verb_style_get"
    path = "tests/phase13_4_1_verb_style_get.rs"
    required-features = ["testing"]

    [[test]]
    name = "phase13_4_1_verb_style_batch_get"
    path = "tests/phase13_4_1_verb_style_batch_get.rs"
    required-features = ["testing"]

    [[test]]
    name = "phase13_4_1_entity_id_alias_warn"
    path = "tests/phase13_4_1_entity_id_alias_warn.rs"
    required-features = ["testing"]
    ```

    Do NOT modify any other `[[test]]` entries. Do NOT add `[lib]` changes. Do NOT touch `[dependencies]` / `[dev-dependencies]` — every dep these tests need (`reqwest`, `tokio`, `serde_json`, `bytes`, `rmp-serde`, `beava-core`, `beava-runtime-core`, `tracing`, `tracing-subscriber`) is already present per the `phase13_4_op_batch_get.rs` analog.
  </action>
  <verify>
    <automated>cargo metadata --manifest-path crates/beava-server/Cargo.toml --no-deps --format-version 1 2>&1 | grep -cE '"name":"phase13_4_1_(verb_style_get|verb_style_batch_get|entity_id_alias_warn)"' | grep -q '^3$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'name = "phase13_4_1_verb_style_get"' crates/beava-server/Cargo.toml` returns exactly 1
    - `grep -c 'name = "phase13_4_1_verb_style_batch_get"' crates/beava-server/Cargo.toml` returns exactly 1
    - `grep -c 'name = "phase13_4_1_entity_id_alias_warn"' crates/beava-server/Cargo.toml` returns exactly 1
    - `cargo metadata --manifest-path crates/beava-server/Cargo.toml --no-deps --format-version 1` exits 0 (Cargo.toml still valid)
    - No other `[[test]]` block in the file is modified (`git diff crates/beava-server/Cargo.toml` shows ONLY additions, no removals or context-replacement)
  </acceptance_criteria>
  <done>
    Cargo.toml registers all 3 Phase 13.4.1 test binaries up front. Plan 01's own test target (`phase13_4_1_verb_style_get`) errors with "no source file" until Task 1.b creates the .rs file; Plans 02 + 03 will land their .rs files in Wave 2 — until then those targets also fail to build, which is fine because Wave 2 has not started yet.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (red): Create phase13_4_1_verb_style_get.rs with 6 failing tests for D-01 + D-05</name>
  <files>crates/beava-server/tests/phase13_4_1_verb_style_get.rs</files>
  <read_first>
    - crates/beava-server/tests/phase13_4_op_batch_get.rs (full file — copy the `TestServer` spawn pattern, the `register` + `push_seed_events` helpers, and the `tcp_send_recv_frame` harness from lines 270-297)
    - crates/beava-server/tests/phase12_09_dispatch_msgpack_test.rs (analog for `CT_MSGPACK` frame round-trip + `rmp_serde::from_slice` body parse)
    - PATTERNS.md §`apply_shard.rs::WireRequest::HttpGet body parser (D-01)` lines 18-75 — the migration target shape
    - PATTERNS.md §`apply_shard.rs` D-05 structured 400 rejection of legacy shapes lines 208-285 — the locked error body shape
    - CONTEXT.md D-05 lines 84-103 — the EXACT message text to assert
    - examples/wire/get-found.request.json + examples/wire/get-found.response.json — the canonical fixture shape this test mirrors
  </read_first>
  <action>
    Create `crates/beava-server/tests/phase13_4_1_verb_style_get.rs` (new file) with the file header `#![cfg(feature = "testing")]` and 6 `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]` tests. Reuse the `register_payload` + `push_seed_events` helpers from `phase13_4_op_batch_get.rs` (copy them verbatim into a `// ─── Shared helpers ───` block at the top of this new file — they spin up a `Tx → UserSpend(user_id) → cnt, total` pipeline and push 2 events for `alice` so `cnt=2, total=42.5` is the known answer).

    All 6 tests MUST be RED (fail at `assert_eq!` or status-check) when this file lands. Plan 04 makes them GREEN.

    Test 1 — `phase13_4_1_verb_style_get_returns_flat_dict` (D-01):
    - POST `/get` body: `{"table": "UserSpend", "key": "alice"}`
    - Assert `resp.status() == 200`
    - Parse body as JSON; assert it's a flat dict (NOT wrapped in `{table, entity_id, features:{...}}`).
    - Assert `body["cnt"] == 2`
    - Assert `body["total"].as_f64()` ≈ 42.5 (within 1e-9)
    - Assert `body.get("table").is_none()` (FLAT — no envelope)
    - Assert `body.get("entity_id").is_none()` (FLAT — no envelope)
    - This will fail today because `WireRequest::HttpGet` routes to `dispatch_get_batch_sync` which expects `{keys, features}`.

    Test 2 — `phase13_4_1_verb_style_get_with_features_filter_narrows` (D-01 + D-06 preview):
    - POST `/get` body: `{"table": "UserSpend", "key": "alice", "features": ["cnt"]}`
    - Assert `resp.status() == 200`
    - Parse body; assert `body == json!({"cnt": 2})` (only `cnt`, no `total`)
    - Assert `body.get("total").is_none()`

    Test 3 — `phase13_4_1_legacy_keys_features_shape_rejected` (D-05):
    - POST `/get` body: `{"keys": ["alice"], "features": ["cnt"]}` (the LEGACY 2D-cell shape)
    - Assert `resp.status() == 400`
    - Parse body; assert `body["error"]["code"] == "unsupported_request_shape"`
    - Assert `body["error"]["message"]` contains the substring `"POST /get expects {table, key, features?}"` AND contains `"docs/http-api.md#post-get"` (verbatim from CONTEXT.md D-05 line 92)

    Test 4 — `phase13_4_1_legacy_feature_key_shape_rejected` (D-05):
    - POST `/get` body: `{"feature": "cnt", "key": "alice"}` (the LEGACY single-feature TCP shape sent over HTTP)
    - Assert `resp.status() == 400`
    - Assert `body["error"]["code"] == "unsupported_request_shape"`
    - Assert `body["error"]["message"]` contains `"docs/http-api.md#post-get"`

    Test 5 — `phase13_4_1_op_get_returns_flat_dict_json` (D-01 — TCP CT_JSON path):
    - Reuse the `tcp_send_recv_frame` helper from `phase13_4_op_batch_get.rs:270-297` (copy verbatim) but pass `OP_GET = 0x0020` and `CT_JSON`.
    - Frame body: `serde_json::to_vec(&json!({"table": "UserSpend", "key": "alice"}))`
    - Assert `resp_frame.op == OP_GET_RESPONSE` (0x0023)
    - Assert `resp_frame.content_type == CT_JSON`
    - Parse `resp_frame.payload` as JSON; assert flat dict with `body["cnt"] == 2` and `body["total"]` ≈ 42.5
    - Assert `body.get("table").is_none()` and `body.get("entity_id").is_none()`

    Test 6 — `phase13_4_1_op_get_returns_flat_dict_msgpack` (D-01 + carry-forward D-A/D-B codec discipline):
    - Same as Test 5 but with `CT_MSGPACK`. Encode body via `rmp_serde::to_vec_named(&json!({"table": "UserSpend", "key": "alice"}))`.
    - Assert `resp_frame.op == OP_GET_RESPONSE`
    - Assert `resp_frame.content_type == CT_MSGPACK` (msgpack-in → msgpack-out per Plan 12-09 D-B)
    - Parse `resp_frame.payload` via `rmp_serde::from_slice::<serde_json::Value>(&resp_frame.payload)`; assert flat dict with `cnt == 2` and `total ≈ 42.5`

    Each test calls `ts.shutdown().await.ok();` at the end (matches the analog).

    NOTE: do NOT add any helpers that touch the server-side migration code — this is a pure RED test plan; the assertions describe the contract Plan 04 must satisfy. Do NOT mark any test `#[ignore]`. Do NOT use `MagicMock` (anti-pattern; we have a real `TestServer` here).
  </action>
  <verify>
    <automated>cargo build --package beava-server --features testing --tests 2>&1 | tail -5; test -f crates/beava-server/tests/phase13_4_1_verb_style_get.rs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f crates/beava-server/tests/phase13_4_1_verb_style_get.rs` exits 0
    - `wc -l crates/beava-server/tests/phase13_4_1_verb_style_get.rs` reports >= 200
    - File header `#![cfg(feature = "testing")]` present (`grep -c '#!\[cfg(feature = "testing")\]' crates/beava-server/tests/phase13_4_1_verb_style_get.rs >= 1`)
    - All 6 test names present: `grep -c '^async fn phase13_4_1_verb_style_get_returns_flat_dict\|^async fn phase13_4_1_verb_style_get_with_features_filter_narrows\|^async fn phase13_4_1_legacy_keys_features_shape_rejected\|^async fn phase13_4_1_legacy_feature_key_shape_rejected\|^async fn phase13_4_1_op_get_returns_flat_dict_json\|^async fn phase13_4_1_op_get_returns_flat_dict_msgpack' crates/beava-server/tests/phase13_4_1_verb_style_get.rs == 6`
    - All 6 tests carry `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]` attribute (`grep -c '^#\[tokio::test(flavor = "multi_thread", worker_threads = 4)\]' crates/beava-server/tests/phase13_4_1_verb_style_get.rs >= 6`)
    - Locked D-05 substring present: `grep -F 'POST /get expects {table, key, features?}' crates/beava-server/tests/phase13_4_1_verb_style_get.rs` exits 0
    - Locked D-05 substring present: `grep -F 'docs/http-api.md#post-get' crates/beava-server/tests/phase13_4_1_verb_style_get.rs` exits 0
    - Locked D-05 substring present: `grep -F 'unsupported_request_shape' crates/beava-server/tests/phase13_4_1_verb_style_get.rs` exits 0
    - No `MagicMock` / `mock!` / `#[ignore]` (`grep -cE 'MagicMock|mock!|#\[ignore' crates/beava-server/tests/phase13_4_1_verb_style_get.rs == 0`)
    - File compiles: `cargo build --package beava-server --features testing --tests 2>&1 | tail -1 | grep -E "Finished|^[ \t]*Finished"` (NB: must compile cleanly even though tests will fail at runtime)
  </acceptance_criteria>
  <done>
    Test binary compiles cleanly. Running `cargo test --package beava-server --features testing --test phase13_4_1_verb_style_get -- --nocapture` exits NON-ZERO with assertion failures across all 6 tests (the canonical RED state per CLAUDE.md TDD §Conventions). The exact failure modes are: Tests 1-2 fail because `WireRequest::HttpGet` parses the legacy 2D-cell shape so the verb-style body returns 500 (`InternalError`); Tests 3-4 fail because the legacy shape returns 500 (`InternalError`) instead of the structured 400; Tests 5-6 fail because `WireRequest::TcpGet` parses `{feature, key}` and rejects the `{table, key}` body.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.c (red commit): commit RED tests as test: commit per CLAUDE.md TDD discipline</name>
  <files>(commit only — no file changes beyond Tasks 1.a + 1.b)</files>
  <read_first>
    - CLAUDE.md §Conventions TDD discipline section (red→green commit cadence)
  </read_first>
  <action>
    Stage `crates/beava-server/Cargo.toml` and `crates/beava-server/tests/phase13_4_1_verb_style_get.rs`. Commit with message:

    ```
    test(13.4.1-01): add RED integration tests for verb-style POST /get + OP_GET (D-01 + D-05)

    Phase 13.4.1 Plan 01 — adds 6 failing integration tests (4 HTTP + 2 TCP)
    asserting the locked verb-style {table, key, features?} contract for
    POST /get and OP_GET (0x0020), plus the structured 400 rejection of the
    legacy {keys, features} and {feature, key} shapes (D-05).

    All 6 tests RED. Plan 04 makes them GREEN by migrating
    WireRequest::HttpGet + WireRequest::TcpGet body parsers and adding
    GlueResponse::UnsupportedRequestShape.
    ```

    Do NOT add `Co-Authored-By: Claude` (memory `feedback_no_ai_attribution_in_commits` — only Hoang Phan as committer).
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -q '^test(13.4.1-01):'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%s'` starts with `test(13.4.1-01):`
    - `git log -1 --format='%B' | grep -c "Co-Authored-By: Claude" == 0` (no AI attribution)
    - `git log -1 --stat | grep -c "phase13_4_1_verb_style_get.rs" >= 1`
    - `git log -1 --stat | grep -c "Cargo.toml" >= 1`
    - Working tree clean: `git status --porcelain | wc -l` returns 0
  </acceptance_criteria>
  <done>
    RED commit landed. The test suite for `phase13_4_1_verb_style_get` is registered, compiles, and fails — the canonical pre-implementation state for the green pass in Plan 04.
  </done>
</task>

</tasks>

<verification>
- All three tasks executed in order (Cargo.toml registration → file creation → commit).
- `cargo build --package beava-server --features testing --tests` succeeds.
- `cargo test --package beava-server --features testing --test phase13_4_1_verb_style_get -- --nocapture` exits non-zero with assertion failures (RED state expected; this is the success criterion for the RED commit).
- `cargo clippy --package beava-server --features testing --tests -- -D warnings` exits 0 (the test file itself is clippy-clean even though the tests fail at runtime).
- `cargo fmt --all --check` exits 0.
- One commit added to history with message starting `test(13.4.1-01):`.
</verification>

<success_criteria>
1. New file `crates/beava-server/tests/phase13_4_1_verb_style_get.rs` exists, ≥ 200 LOC, compiles cleanly under `--features testing`.
2. New `[[test]]` entry registered in `crates/beava-server/Cargo.toml`.
3. All 6 tests inside the new file FAIL when run (the RED state — assertion or status-mismatch errors). This is what Plan 04 will turn GREEN.
4. `cargo clippy --package beava-server --features testing --tests -- -D warnings` exits 0.
5. `cargo fmt --all --check` exits 0.
6. One `test(13.4.1-01):`-prefixed commit added to git history; no `Co-Authored-By: Claude` attribution.
</success_criteria>

<output>
After completion, create `.planning/phases/13.4.1-server-wire-spec-verb-style-migration/13.4.1-01-SUMMARY.md` documenting:
- The 6 tests created and what each one asserts (one bullet per test)
- The exact failure mode for each test (e.g., "Test 1 fails with status=500 InternalError because HttpGet routes to dispatch_get_batch_sync which expects {keys, features}")
- The hand-off to Plan 04 (which sites Plan 04 must touch to flip these tests GREEN)
</output>
