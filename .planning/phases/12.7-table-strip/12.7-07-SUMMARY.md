---
phase: 12.7-table-strip
plan: 07
subsystem: requirements-doc-sweep
tags: [doc-only, requirements, descope, v0-events-only, sweep]
type: execute
autonomous: true
wave: 3
depends_on: [01, 02]
provides:
  - 8 REQ-IDs marked DESCOPED with uniform banner
  - V0-EVENTS-ONLY-01 positive architectural-invariant REQ-ID
  - Forward-compat ADR anchor for v0.1+ table revival
requires:
  - Plan 01 (runtime shim that V0-EVENTS-ONLY-01 anchors)
  - Plan 02 (architectural test pair satisfies V0-EVENTS-ONLY-01)
affects:
  - .planning/REQUIREMENTS.md
tech-stack:
  added: []
  patterns:
    - PATTERNS.md §Pattern 6 — Section-ownership for shared docs (REQUIREMENTS.md owned by this plan; STATE.md/ROADMAP.md/CORRECTNESS-PATH.md owned by Plan 10)
    - 12.6 Plan 03 surgical-sweep analog extended to "comprehensive sweep" per CONTEXT D-04
    - CLAUDE.md TDD §Note 4 — doc-only plan uses `docs:` prefix; RED is precondition shell-grep, GREEN is the edit
key-files:
  created: []
  modified:
    - .planning/REQUIREMENTS.md
decisions:
  - "Uniform DESCOPED banner: applied verbatim text 'DESCOPED 2026-04-30 per `project_v0_events_only_scope`; tables return v0.1+ alongside joins/aggregation. Phase 12.7 strips the surface.' to all 8 swept REQ-IDs (including the 3 already partially banner-stamped: SDK-DEC-04, SDK-DEC-05, V0.1-TABLE-01 — refresh from REMOVED/ARCHIVED-INDEFINITELY phrasing to uniform DESCOPED phrasing)"
  - "V0-EVENTS-ONLY-01 placement: new `### V0-INVARIANT — Architectural commitments locked at CI` subsection inserted after SRV-API-13 (line 198), before SRV-REG section. Mirrors the architectural-invariant-as-REQ-ID pattern; placement co-locates the invariant with the wire-API surface it bounds (the 'rejected at /register' shim semantics)"
  - "PLANNER-SURFACED scope decision propagated from Plan 06: SDK-AGG-02 (TableDerivation as .agg() output) is the only AGG-* REQ-ID swept. Operator-catalogue REQ-IDs AGG-CORE-* / AGG-SKETCH-* / AGG-POINT-* / AGG-DECAY-* / AGG-VEL-* / AGG-RECENCY-* / AGG-BUFFER-* / AGG-GEO-* / AGG-Z-* LEFT ACTIVE — Rust runtime still uses operators for state tracking, only the Python `.agg()` builder is gone. SDK-AGG-02's banner explicitly notes this distinction so future audit can see the intentional split"
metrics:
  duration_minutes: 2
  completed_date: 2026-05-01
  commits: [ace00b8]
  files_modified: 1
  net_loc_delta: "+12 / -8 = +4 lines"
  banner_count_before: 0
  banner_count_after: 8
  positive_anchors_added: 1
---

# Phase 12.7 Plan 07: REQUIREMENTS.md Comprehensive Table-Surface Sweep — Summary

**Doc-only sweep of `.planning/REQUIREMENTS.md` per CONTEXT D-04: 8 table-surface REQ-IDs uniformly banner-stamped DESCOPED 2026-04-30; new positive REQ-ID `V0-EVENTS-ONLY-01` added as the architectural-test anchor for the events-only commitment.**

## What changed

### 1. 8 REQ-IDs uniformly banner-stamped (`DESCOPED 2026-04-30 per project_v0_events_only_scope`)

All 8 use identical banner text — `**DESCOPED 2026-04-30 per `project_v0_events_only_scope`; tables return v0.1+ alongside joins/aggregation. Phase 12.7 strips the surface.**` — except SDK-AGG-02 which carries an extended banner noting the operator-family ACTIVE distinction.

| REQ-ID | Original status | New status | Why |
|--------|-----------------|------------|-----|
| SDK-DEC-04 | already REMOVED 2026-04-30 (non-uniform) | DESCOPED 2026-04-30 (uniform) | banner-phrasing harmonized to uniform text |
| SDK-DEC-05 | already REMOVED 2026-04-30 (non-uniform) | DESCOPED 2026-04-30 (uniform) | banner-phrasing harmonized to uniform text |
| SDK-AGG-02 | `[x]` (checked complete from 12.6) | DESCOPED 2026-04-30 (extended banner) | Plan 06 made `GroupBy.agg()` raise RuntimeError; the `TableDerivation` output channel is gone |
| SRV-API-06 | `[ ]` (active) | DESCOPED 2026-04-30 | POST `/push-table` endpoint deleted in Plan 03 |
| SRV-API-07 | `[ ]` (active) | DESCOPED 2026-04-30 | POST `/delete-table` endpoint deleted in Plan 03 |
| SDK-APP-07 | `[ ]` (active) | DESCOPED 2026-04-30 | `App.upsert()` method deleted in Plan 06 |
| SDK-APP-08 | `[ ]` (active) | DESCOPED 2026-04-30 | `App.delete()` method deleted in Plan 06 |
| V0.1-TABLE-01 | already ARCHIVED-INDEFINITELY 2026-04-30 (non-uniform) | DESCOPED 2026-04-30 (uniform) | banner-phrasing harmonized to uniform text |

### 2. NEW positive REQ-ID: `V0-EVENTS-ONLY-01`

Inserted as a new `### V0-INVARIANT — Architectural commitments locked at CI` subsection after SRV-API-13 (line 198, before SRV-REG):

> **V0-EVENTS-ONLY-01**: No `@bv.table` decorator in public `bv` namespace; no `OpNode::Table*` variants; no `TemporalStore` / `MvccVersion`; no `RecordType::TableUpsert/TableDelete/Retract`; no `temporal_http` / `push_table` / `delete_table` / `app.retract` symbols. Enforced at CI runtime by `crates/beava-server/tests/phase12_7_no_table_surface.rs` and `crates/beava-server/tests/phase12_7_legacy_table_handlers_killed.rs` (architectural-test pair, Plan 12.7-02). Reviving any forbidden symbol requires explicit user override + new ADR overturning `project_v0_events_only_scope`.

This is the testable invariant counterpart to the negative descope sweep — it expresses the events-only commitment as a positive contract that the architectural-test pair from Plan 02 satisfies.

## TDD gate compliance

- **RED (precondition):** banner count of `'DESCOPED 2026-04-30 per project_v0_events_only_scope'` = **0**; `V0-EVENTS-ONLY-01` **absent**. Verified by shell grep before edits.
- **GREEN (post-edit, single commit `ace00b8`):** banner count = **8**; `V0-EVENTS-ONLY-01` **present**; `phase12_7_no_table_surface` referenced; all 8 swept REQ-IDs carry the strikethrough; all 9 surviving operator-family REQ-IDs (AGG-CORE-01 / AGG-SKETCH-01 / AGG-POINT-01 / AGG-DECAY-01 / AGG-VEL-01 / AGG-RECENCY-01 / AGG-BUFFER-01 / AGG-GEO-01 / AGG-Z-01) untouched; SDK-AGG-01/03/04/05/06 untouched.

Per CLAUDE.md TDD §Note 4, doc-only plans use the `docs:` commit prefix and the RED→GREEN structure is shell-precondition then file-state-after-edit (not separate test/impl commits).

## Surface preservation

The surgical scope was strictly enforced. Confirmed via grep:

- **9 operator-family REQ-IDs** (one canonical per family) intact: AGG-CORE-01, AGG-SKETCH-01, AGG-POINT-01, AGG-DECAY-01, AGG-VEL-01, AGG-RECENCY-01, AGG-BUFFER-01, AGG-GEO-01, AGG-Z-01.
- **5 SDK-AGG-* REQ-IDs** (all except SDK-AGG-02) intact: SDK-AGG-01, SDK-AGG-03, SDK-AGG-04, SDK-AGG-05, SDK-AGG-06.
- All non-table REQ-IDs (SRV-REG, SRV-APPLY, SRV-DUR, SRV-RECOV, OBS, PERF, THROUGHPUT, TEST, DOC, PKG) untouched.
- Other partial decommissions (SDK-DEC-02b event_time tolerate_delay; SDK-DEC-08 event_time field; SDK-JOIN-*; SDK-SESSION-01; SRV-APPLY-08 join; TEST-07 join; v2 EMIT-01/TIMER-01/etc.) untouched — those use different banner phrasings ("REMOVED", "ARCHIVED-INDEFINITELY", "DEFERRED") tied to different scope decisions (no-event-time pivot, future-release queue) and intentionally NOT harmonized to the events-only-scope banner.

## Deviations from plan

**None.** The plan executed verbatim. Banner text matches the CONTEXT D-04 specification verbatim across all 8 REQ-IDs. The V0-EVENTS-ONLY-01 placement and wording match the plan's `<interfaces>` block exactly.

No Rule 1/2/3 auto-fixes triggered. No Rule 4 architectural escalations. No authentication gates. No deferred items.

## PLANNER-SURFACED items for Plan 10 review

Already documented in the SDK-AGG-02 banner itself, but recapped here for Plan 10's audit attention:

> **The aggregation-operator-family REQ-IDs (AGG-CORE-* / AGG-SKETCH-* / AGG-POINT-* / AGG-DECAY-* / AGG-VEL-* / AGG-RECENCY-* / AGG-BUFFER-* / AGG-GEO-* / AGG-Z-*) are LEFT ACTIVE.** Per the planner-surfaced concern in Plan 06: the operator catalogue is still used by the Rust runtime for state tracking, even though the Python `.agg()` builder is removed. SDK-AGG-02 is the one REQ-ID specifically about producing `TableDerivation`, which is gone — that's the only AGG-family REQ-ID swept here.

**Plan 10 closure SUMMARY should also note the D-04 enumeration vs reality discrepancy** (per the plan-checker WARNING-1): CONTEXT.md D-04 enumerated wildcard ID prefixes (`TABLE-*`, `RETRACT-*`, `SDK-DEC-TABLE-*`, etc.) that don't exist in REQUIREMENTS.md. The plan-checker confirmed only 8 actual REQ-IDs match. The sweep is comprehensive against the actual REQ-ID surface; the wildcard enumeration is wider than the actual surface ever was.

## Section-ownership contract honored

This plan owns ONLY `.planning/REQUIREMENTS.md` per the section-partition contract (PATTERNS.md §Pattern 6, mirroring 12.6 Plans 09 + 13):

- Plan 07 (this plan) — REQUIREMENTS.md ✅
- Plan 08 — `.planning/phases/11.5-temporal-tables-retraction-primitive/{SUMMARY,VERIFICATION,CONTEXT}.md` banners (does NOT touch REQUIREMENTS.md)
- Plan 10 (closure) — STATE.md / ROADMAP.md / CORRECTNESS-PATH.md (does NOT touch REQUIREMENTS.md)

Verified via `git diff --name-only HEAD~1 HEAD`: only `.planning/REQUIREMENTS.md` modified.

## Verification

| Check | Result |
|-------|--------|
| Banner count `DESCOPED 2026-04-30 per project_v0_events_only_scope` ≥ 8 | **8** (PASS) |
| `V0-EVENTS-ONLY-01` present | **PASS** |
| `V0-EVENTS-ONLY-01` references `phase12_7_no_table_surface` | **PASS** |
| All 8 swept REQ-IDs have strikethrough `~~**REQ-ID**~~` | **8/8 PASS** |
| All 9 operator-family canonical REQ-IDs intact | **9/9 PASS** |
| All 5 surviving SDK-AGG-* REQ-IDs intact | **5/5 PASS** |
| File parses as markdown (top heading present) | **PASS** |
| Commit prefix `docs(12.7-07):` | **PASS** |
| Diff scope = REQUIREMENTS.md only | **PASS** |
| Net LOC delta | **+12 / -8 = +4 lines** |

## Self-Check

- [x] 8 REQ-IDs DESCOPED with uniform banner
- [x] V0-EVENTS-ONLY-01 added with architectural-test reference
- [x] Operator-family REQ-IDs intact (AGG-CORE-* / AGG-SKETCH-* / AGG-POINT-* / AGG-DECAY-* / AGG-VEL-* / AGG-RECENCY-* / AGG-BUFFER-* / AGG-GEO-* / AGG-Z-*)
- [x] Banner text matches CONTEXT D-04 verbatim
- [x] Doc-only commit `docs(12.7-07):` (single commit `ace00b8`)
- [x] PLANNER-SURFACED note: SDK-AGG-02 swept; AGG-CORE-* etc. left active for Plan 10 review
- [x] Section-ownership: only REQUIREMENTS.md modified (STATE/ROADMAP/CORRECTNESS-PATH untouched per Plan 10's ownership)
- [x] No code changes, no test changes, no Rust/Python source changes

## Self-Check: PASSED

All acceptance criteria verified on disk. Commit `ace00b8` exists in git log. File `.planning/REQUIREMENTS.md` modified at line 17, 18, 51, 161, 162, 191, 192, 200-202 (V0-INVARIANT block insertion), 295.

---

*Phase: 12.7-table-strip*
*Plan: 07 (Wave 3 — REQUIREMENTS.md sweep)*
*Status: CLOSED 2026-05-01 PASS*
*Commit: `ace00b8` (`docs(12.7-07): comprehensive REQUIREMENTS.md sweep — table/MVCC/retract REQ-IDs DESCOPED + V0-EVENTS-ONLY-01 anchor`)*
*Predecessor: Plan 06 (HEAD `991c8cf` — Python SDK strip)*
*Successor: Plans 08 (11.5 banner) + 10 (closure) — both blocked-on-this-plan-closing for the closure plan only; Plan 08 is independent*
