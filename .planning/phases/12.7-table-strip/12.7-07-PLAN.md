---
phase: 12.7-table-strip
plan: 07
type: execute
wave: 3
depends_on: [01, 02]                     # Plan 01's shim provides the runtime enforcement that V0-EVENTS-ONLY-01 anchors. Plan 02's architectural test satisfies the new positive REQ-ID. No code-deletion dependency.
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  # Comprehensive sweep — every REQ-ID listed in CONTEXT D-04 enumeration:
  - SDK-DEC-04             # already REMOVED 2026-04-30; banner update
  - SDK-DEC-05             # already REMOVED 2026-04-30; banner update
  - SDK-AGG-02             # NEW DESCOPED 2026-04-30 — produces TableDerivation; tables gone
  - SRV-API-06             # POST /push-table/{table_name} REMOVED — wire endpoint deleted Plan 03
  - SRV-API-07             # POST /delete-table/{table_name} REMOVED — wire endpoint deleted Plan 03
  - SDK-APP-07             # app.push(Table, ...) REMOVED — App.upsert deleted Plan 06
  - SDK-APP-08             # app.delete(Table, ...) REMOVED — App.delete deleted Plan 06
  - V0.1-TABLE-01          # already ARCHIVED-INDEFINITELY 2026-04-30; banner update
  - V0-EVENTS-ONLY-01      # NEW positive REQ-ID added in this plan
addresses_decisions:
  - "D-04 first half — comprehensive REQUIREMENTS.md sweep with uniform reason banner: 'per project_v0_events_only_scope; tables return v0.1+ alongside joins/aggregation'."
  - "D-04 — Add positive requirement V0-EVENTS-ONLY-01 — 'no @bv.table decorator in public bv namespace' — to anchor the architectural commitment as a testable invariant. The Plan 02 architectural test satisfies it."
  - "PLANNER-SURFACED in Plan 06: SDK-AGG-02 (GroupBy.agg returns TableDerivation) is added to the sweep because Plan 06 made it raise RuntimeError. AGG-CORE-* / AGG-SKETCH-* / AGG-POINT-* / AGG-DECAY-* / AGG-VEL-* / AGG-RECENCY-* / AGG-BUFFER-* / AGG-GEO-* / AGG-Z-* operator-family REQ-IDs are LEFT ACTIVE because the operators themselves are still used by the Rust runtime for state tracking — only the Python builder method is gone. User should review at Plan 10 SUMMARY."
must_haves:
  truths:
    - "REQUIREMENTS.md `SDK-DEC-04` line has the uniform DESCOPED banner reason text"
    - "REQUIREMENTS.md `SDK-DEC-05` line has the uniform banner"
    - "REQUIREMENTS.md `SDK-AGG-02` (was `[x]` checked) is now marked `~~SDK-AGG-02~~ ... DESCOPED 2026-04-30 ...`"
    - "REQUIREMENTS.md `SRV-API-06` line has the uniform banner"
    - "REQUIREMENTS.md `SRV-API-07` line has the uniform banner"
    - "REQUIREMENTS.md `SDK-APP-07` line has the uniform banner"
    - "REQUIREMENTS.md `SDK-APP-08` line has the uniform banner"
    - "REQUIREMENTS.md `V0.1-TABLE-01` line has the uniform banner (was already partially banner-stamped 2026-04-30; harmonize wording to match the comprehensive sweep)"
    - "REQUIREMENTS.md has a NEW positive `V0-EVENTS-ONLY-01` entry under SRV-API or a new dedicated section (planner picks)"
    - "All REQ-IDs in the sweep enumeration above carry the EXACT phrase 'DESCOPED 2026-04-30' (uniformity check)"
    - "Reason banner text matches verbatim across all swept REQ-IDs: 'per project_v0_events_only_scope; tables return v0.1+ alongside joins/aggregation'"
    - "No new REMOVED-but-not-DESCOPED REQ-IDs introduced (REMOVED is reserved for 12.6 sweep targets — joins, event-time)"
    - "Sanity: surviving requirements (operator catalogues AGG-CORE-* etc., SDK-EVENT, OBS-*, SRV-DUR-*, SRV-RECOV-*, PERF-*, etc.) are UNTOUCHED"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Comprehensive table-surface sweep + V0-EVENTS-ONLY-01 positive anchor"
      contains: "V0-EVENTS-ONLY-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md::V0-EVENTS-ONLY-01"
      to: "crates/beava-server/tests/phase12_7_no_table_surface.rs (Plan 02)"
      via: "Architectural test enforces the V0-EVENTS-ONLY-01 invariant; reviving any forbidden symbol fails CI"
      pattern: "V0-EVENTS-ONLY-01|phase12_7_no_table_surface"
---

<objective>
Comprehensive sweep of REQUIREMENTS.md for the table / temporal / retract / MVCC / upsert surface per CONTEXT D-04. Every REQ-ID in the enumeration list gets the uniform `DESCOPED 2026-04-30` banner with the verbatim reason text. Add a positive `V0-EVENTS-ONLY-01` REQ-ID that anchors the events-only architectural commitment; the Plan 02 architectural test satisfies it.

After this plan lands:
- 8 REQ-IDs marked DESCOPED with uniform banner: SDK-DEC-04 (banner refresh), SDK-DEC-05 (banner refresh), SDK-AGG-02 (NEW DESCOPED), SRV-API-06, SRV-API-07, SDK-APP-07, SDK-APP-08, V0.1-TABLE-01 (banner refresh)
- 1 NEW positive REQ-ID added: V0-EVENTS-ONLY-01

PATTERNS.md §Pattern 6 + §"Section-ownership for shared docs" applies: this plan's edits are confined to REQUIREMENTS.md only — STATE.md / ROADMAP.md / CORRECTNESS-PATH.md are owned by Plan 10 (closure plan).

**Wave 3 placement:** independent of Plans 06 + 08 (different files). All 3 Wave 3 plans can run parallel.

**The aggregation-operator-family REQ-IDs (AGG-CORE-* / AGG-SKETCH-* / AGG-POINT-* / AGG-DECAY-* / AGG-VEL-* / AGG-RECENCY-* / AGG-BUFFER-* / AGG-GEO-* / AGG-Z-*) are LEFT ACTIVE.** Per the planner-surfaced concern in Plan 06: the operator catalogue is still used by the Rust runtime for state tracking, even though the Python `.agg()` builder is removed. SDK-AGG-02 is the one REQ-ID specifically about producing TableDerivation, which is gone — that's the only AGG-family REQ-ID swept here. User confirmation requested at Plan 10 SUMMARY review.

Output:
- 8 banner-stamped REQ-IDs in REQUIREMENTS.md
- 1 NEW positive REQ-ID (V0-EVENTS-ONLY-01)
- Diff is doc-only; no code change; no test change
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.7-table-strip/12.7-CONTEXT.md
@.planning/phases/12.7-table-strip/12.7-PATTERNS.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.6-v0-surface-reduction/12.6-03-SUMMARY.md

<interfaces>
<!-- Verified current state of REQUIREMENTS.md (read at planning time) -->

Existing REQ-IDs to sweep:
- Line 17: `~~SDK-DEC-04~~` already marked REMOVED 2026-04-30 (12.6 work). This plan refreshes the banner to use uniform DESCOPED phrasing if needed.
- Line 18: `~~SDK-DEC-05~~` same.
- Line 51: `[x] SDK-AGG-02` (was checked complete because TableDerivation flow worked in 12.6). This plan rewrites to `~~SDK-AGG-02~~ ... DESCOPED 2026-04-30 ...`.
- Line 191: `[ ] SRV-API-06: POST /push-table/{table_name} upserts a row by primary key` → strikethrough + DESCOPED.
- Line 192: `[ ] SRV-API-07: POST /delete-table/{table_name} tombstones a row by primary key` → strikethrough + DESCOPED.
- Line 161: `[ ] SDK-APP-07: app.push(Table, key, row_dict)` → strikethrough + DESCOPED.
- Line 162: `[ ] SDK-APP-08: app.delete(Table, key)` → strikethrough + DESCOPED.
- Line 292: `~~V0.1-TABLE-01~~` already partially banner-stamped 2026-04-30; harmonize wording.

NEW positive REQ-ID to add (planner picks placement; recommended: under SRV-API section or under a new `### V0-INVARIANT` section):
```markdown
- [ ] **V0-EVENTS-ONLY-01**: No `@bv.table` decorator in public `bv` namespace; no `OpNode::Table*` variants; no `TemporalStore` / `MvccVersion`; no `RecordType::TableUpsert/TableDelete/Retract`; no `temporal_http` / `push_table` / `delete_table` / `app.retract` symbols. Enforced at CI runtime by `crates/beava-server/tests/phase12_7_no_table_surface.rs` (architectural test). Reviving any forbidden symbol requires explicit user override + new ADR overturning `project_v0_events_only_scope`.
```

Uniform banner text (per CONTEXT D-04 verbatim):
```
DESCOPED 2026-04-30 per project_v0_events_only_scope; tables return v0.1+ alongside joins/aggregation
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (RED): Failing test asserting REQUIREMENTS sweep complete</name>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — read full file before edits)
    - .planning/phases/12.7-table-strip/12.7-CONTEXT.md (D-04 — verbatim banner text)
  </read_first>
  <files>(no new file — RED gate is shell assertions in this task; commit lands on Task 1.b)</files>
  <behavior>
    - The "test" for a doc-only sweep is a shell-level grep audit. Before Plan 07 runs, the count of `DESCOPED 2026-04-30 per project_v0_events_only_scope` strings in REQUIREMENTS.md is 0 (the prior 11.5 banner used different phrasing).
    - After Plan 07 lands, the count must be ≥ 8 (one per swept REQ-ID).
    - Also: V0-EVENTS-ONLY-01 must appear at least once.
  </behavior>
  <action>
This is a doc-only plan; the "RED" gate is a precondition check via shell assertions:

```bash
# RED gate (must hold BEFORE Task 1.b runs):
grep -c 'DESCOPED 2026-04-30 per project_v0_events_only_scope' .planning/REQUIREMENTS.md
# Should be 0 or near-0 (any prior occurrences are from 11.5 retro-banner work which lives in 11.5 dir, not REQUIREMENTS.md)

! grep -q 'V0-EVENTS-ONLY-01' .planning/REQUIREMENTS.md
# Should hold (positive REQ-ID doesn't exist yet)
```

NO commit lands for this task — Task 1.b's commit captures the diff. This is a precondition-confirmation step.
  </action>
  <verify>
    <automated>echo "RED preconditions: $(grep -c 'DESCOPED 2026-04-30 per project_v0_events_only_scope' .planning/REQUIREMENTS.md) banner stamps; V0-EVENTS-ONLY-01 absent: $(! grep -q V0-EVENTS-ONLY-01 .planning/REQUIREMENTS.md && echo OK)"</automated>
    EXPECTED: banner count is 0 or low; V0-EVENTS-ONLY-01 reports OK absent
  </verify>
  <acceptance_criteria>
    - Banner count is < 8: `[ $(grep -c 'DESCOPED 2026-04-30 per project_v0_events_only_scope' .planning/REQUIREMENTS.md) -lt 8 ]`
    - V0-EVENTS-ONLY-01 absent: `! grep -q 'V0-EVENTS-ONLY-01' .planning/REQUIREMENTS.md`
  </acceptance_criteria>
  <done>RED precondition: REQUIREMENTS.md doesn't yet have the comprehensive sweep applied. Task 1.b does the sweep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (GREEN): Apply comprehensive table-surface sweep + add V0-EVENTS-ONLY-01</name>
  <read_first>
    - .planning/REQUIREMENTS.md (full file)
    - .planning/phases/12.7-table-strip/12.7-CONTEXT.md (D-04 verbatim text)
    - .planning/phases/12.7-table-strip/12.7-PATTERNS.md (§Pattern 6)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
**Step 1 — Locate each REQ-ID** to sweep (use grep to verify line numbers; current numbers may shift):

```bash
grep -n '^- .*\(SDK-DEC-04\|SDK-DEC-05\|SDK-AGG-02\|SRV-API-06\|SRV-API-07\|SDK-APP-07\|SDK-APP-08\|V0\.1-TABLE-01\)' .planning/REQUIREMENTS.md
```

**Step 2 — Apply uniform banner to each.** Replace each REQ-ID line with the strikethrough form + uniform banner. Verbatim banner text:

```
**DESCOPED 2026-04-30 per `project_v0_events_only_scope`; tables return v0.1+ alongside joins/aggregation. Phase 12.7 strips the surface.**
```

For example, REQ-ID **SRV-API-06** (currently line 191):
```markdown
# Before:
- [ ] **SRV-API-06**: `POST /push-table/{table_name}` upserts a row by primary key

# After:
- ~~**SRV-API-06**: `POST /push-table/{table_name}` upserts a row by primary key~~ — **DESCOPED 2026-04-30 per `project_v0_events_only_scope`; tables return v0.1+ alongside joins/aggregation. Phase 12.7 strips the surface.**
```

For **SDK-AGG-02** (currently line 51, has `[x]`):
```markdown
# Before:
- [x] **SDK-AGG-02**: `GroupBy.agg(**named_features)` accepts named aggregation operator descriptors; returns a `TableDerivation`

# After:
- ~~**SDK-AGG-02**: `GroupBy.agg(**named_features)` accepts named aggregation operator descriptors; returns a `TableDerivation`~~ — **DESCOPED 2026-04-30 per `project_v0_events_only_scope`; tables return v0.1+ alongside joins/aggregation. Phase 12.7's Plan 06 makes `GroupBy.agg()` raise `RuntimeError` with v0 framing. The operator catalogue (AGG-CORE-* / AGG-SKETCH-* / AGG-POINT-* / AGG-DECAY-* / AGG-VEL-* / AGG-RECENCY-* / AGG-BUFFER-* / AGG-GEO-* / AGG-Z-*) remains ACTIVE — only the Python builder method that produces `TableDerivation` is gone.**
```

For REQ-IDs already partially banner-stamped (SDK-DEC-04, SDK-DEC-05, V0.1-TABLE-01): harmonize the existing wording to the uniform banner. If the existing banner text differs, REPLACE with the uniform text (preserve the strikethrough).

**Step 3 — Add positive REQ-ID V0-EVENTS-ONLY-01.** Place it under the SRV-API section (most natural fit; near SRV-API-13 — it's an architectural invariant, not a wire endpoint). Insert AFTER the last SRV-API line:

```markdown
### V0-INVARIANT — Architectural commitments locked at CI

- [ ] **V0-EVENTS-ONLY-01**: No `@bv.table` decorator in public `bv` namespace; no `OpNode::Table*` variants; no `TemporalStore` / `MvccVersion`; no `RecordType::TableUpsert/TableDelete/Retract`; no `temporal_http` / `push_table` / `delete_table` / `app.retract` symbols. Enforced at CI runtime by `crates/beava-server/tests/phase12_7_no_table_surface.rs` and `crates/beava-server/tests/phase12_7_legacy_table_handlers_killed.rs` (architectural-test pair, Plan 12.7-02). Reviving any forbidden symbol requires explicit user override + new ADR overturning `project_v0_events_only_scope`.
```

**Step 4 — Verify:**

```bash
# Sweep is comprehensive (≥ 8 uniform banners):
grep -c 'DESCOPED 2026-04-30 per .project_v0_events_only_scope' .planning/REQUIREMENTS.md   # ≥ 8

# Positive REQ-ID exists:
grep -q 'V0-EVENTS-ONLY-01' .planning/REQUIREMENTS.md

# Each swept REQ-ID has the strikethrough + banner:
for req in SDK-DEC-04 SDK-DEC-05 SDK-AGG-02 SRV-API-06 SRV-API-07 SDK-APP-07 SDK-APP-08 V0.1-TABLE-01; do
  grep -E "~~\*\*${req}\*\*" .planning/REQUIREMENTS.md || echo "MISSING strikethrough for $req"
done

# Sanity: surviving aggregation-operator REQ-IDs UNTOUCHED:
for req in AGG-CORE-01 AGG-SKETCH-01 AGG-POINT-01 AGG-DECAY-01 AGG-VEL-01 AGG-RECENCY-01 AGG-BUFFER-01 AGG-GEO-01 AGG-Z-01; do
  grep -E "^- \[ \] \*\*${req}\*\*" .planning/REQUIREMENTS.md || echo "$req unexpectedly missing/changed!"
done
```

All checks must pass.

Commit message:
```
docs(12.7-07): comprehensive REQUIREMENTS.md sweep — table/MVCC/retract REQ-IDs DESCOPED + V0-EVENTS-ONLY-01 anchor

Per CONTEXT D-04 — uniform banner across the table-surface REQ-ID family.
8 REQ-IDs marked DESCOPED 2026-04-30 with the verbatim reason banner:
'per project_v0_events_only_scope; tables return v0.1+ alongside joins/aggregation':

- SDK-DEC-04 (banner refresh; was already REMOVED 2026-04-30)
- SDK-DEC-05 (banner refresh; was already REMOVED 2026-04-30)
- SDK-AGG-02 (NEW DESCOPED — Plan 06 made GroupBy.agg raise RuntimeError;
  the operator catalogue REQ-IDs AGG-CORE-* etc. remain ACTIVE)
- SRV-API-06 (POST /push-table — endpoint deleted Plan 03)
- SRV-API-07 (POST /delete-table — endpoint deleted Plan 03)
- SDK-APP-07 (app.push(Table, ...) — App.upsert deleted Plan 06)
- SDK-APP-08 (app.delete(Table, ...) — App.delete deleted Plan 06)
- V0.1-TABLE-01 (banner refresh; was already ARCHIVED-INDEFINITELY 2026-04-30)

NEW positive REQ-ID added: V0-EVENTS-ONLY-01 — the architectural test
pair from Plan 02 (`phase12_7_no_table_surface.rs` + 
`phase12_7_legacy_table_handlers_killed.rs`) provides CI-runtime
enforcement.

PLANNER-SURFACED: aggregation-operator-family REQ-IDs (AGG-CORE-* /
AGG-SKETCH-* / etc.) LEFT ACTIVE. The operator catalogue is still used
by the Rust runtime for state tracking; only the Python `.agg()` builder
that produces TableDerivation is gone. User confirmation requested at
Plan 10 SUMMARY review.
```
  </action>
  <verify>
    <automated>echo "Banner count: $(grep -c 'DESCOPED 2026-04-30 per .project_v0_events_only_scope' .planning/REQUIREMENTS.md); V0-EVENTS-ONLY-01 present: $(grep -q V0-EVENTS-ONLY-01 .planning/REQUIREMENTS.md && echo YES)"</automated>
    EXPECTED: banner count ≥ 8; V0-EVENTS-ONLY-01 present
  </verify>
  <acceptance_criteria>
    - Banner count ≥ 8: `[ $(grep -c 'DESCOPED 2026-04-30 per .project_v0_events_only_scope' .planning/REQUIREMENTS.md) -ge 8 ]`
    - V0-EVENTS-ONLY-01 present: `grep -q 'V0-EVENTS-ONLY-01' .planning/REQUIREMENTS.md`
    - V0-EVENTS-ONLY-01 references the architectural test: `grep -q 'phase12_7_no_table_surface' .planning/REQUIREMENTS.md`
    - Each swept REQ-ID has strikethrough: `for req in SDK-DEC-04 SDK-DEC-05 SDK-AGG-02 SRV-API-06 SRV-API-07 SDK-APP-07 SDK-APP-08 V0.1-TABLE-01; do grep -E "~~\*\*${req}\*\*" .planning/REQUIREMENTS.md > /dev/null || exit 1; done`
    - Surviving operator REQ-IDs intact: `for req in AGG-CORE-01 AGG-SKETCH-01 AGG-POINT-01 AGG-DECAY-01 AGG-VEL-01 AGG-RECENCY-01 AGG-BUFFER-01 AGG-GEO-01 AGG-Z-01; do grep -qE "^- \[ \] \*\*${req}\*\*" .planning/REQUIREMENTS.md || exit 1; done`
    - Surviving SDK-AGG-01/03/04/05/06 untouched (only SDK-AGG-02 swept): `grep -qE '^- \[ \] \*\*SDK-AGG-01\*\*' .planning/REQUIREMENTS.md`
    - File still parses as markdown (no broken structure): `head -20 .planning/REQUIREMENTS.md | grep -q '^# '`
    - Commit: `git log -1 --format=%s | grep -E '^docs\(12\.7-07\):'`
  </acceptance_criteria>
  <done>GREEN gate: 8 REQ-IDs DESCOPED with uniform banner; V0-EVENTS-ONLY-01 added; operator-family REQ-IDs intact. Doc-only commit `docs(12.7-07):`.</done>
</task>

</tasks>

<threat_model>
Phase 12.7-07 is doc-only. No threat surface change.
</threat_model>

<verification>
- 8 REQ-IDs banner-stamped uniformly
- 1 NEW positive REQ-ID added
- Aggregation-operator REQ-IDs (AGG-CORE-* etc.) untouched
- 1 commit: `docs(12.7-07):` (doc-only — no test↔impl pair needed; per CLAUDE.md TDD §Note 4, smoke/acceptance docs commits are exempt from the red-then-green requirement)
</verification>

<success_criteria>
- All 8 swept REQ-IDs have uniform banner text
- V0-EVENTS-ONLY-01 exists and references the architectural test
- AGG-CORE-* / AGG-SKETCH-* / AGG-POINT-* / AGG-DECAY-* / AGG-VEL-* / AGG-RECENCY-* / AGG-BUFFER-* / AGG-GEO-* / AGG-Z-* all UNTOUCHED
- Phase 12.7-07 commit lands as `docs(12.7-07):`
</success_criteria>

<output>
After completion, create `.planning/phases/12.7-table-strip/12.7-07-SUMMARY.md`. Self-Check:
- [x] 8 REQ-IDs DESCOPED with uniform banner
- [x] V0-EVENTS-ONLY-01 added
- [x] Operator-family REQ-IDs intact
- [x] Banner text matches CONTEXT D-04 verbatim
- [x] Doc-only commit `docs(12.7-07):`
- [x] PLANNER-SURFACED note: SDK-AGG-02 swept; AGG-CORE-* etc. left active for Plan 10 review
</output>