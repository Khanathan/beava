---
phase: 12.7-table-strip
plan: 10
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09]   # closure plan — runs LAST
files_modified:
  - .planning/phases/12.7-table-strip/12.7-SUMMARY.md          # NEW — Phase narrative
  - .planning/phases/12.7-table-strip/12.7-VERIFICATION.md     # NEW — mechanical pass/fail
  - CLAUDE.md                                                   # ADD § Events-Only Invariant block
  - .planning/STATE.md                                          # advance Current Focus → Phase 13; phase-progress block
  - .planning/ROADMAP.md                                        # mark Phase 12.7 → ✅ COMPLETED 2026-04-30 (or whatever date); critical-path advance
  - .planning/CORRECTNESS-PATH.md                              # mark Phase 12.7 → CLOSED; advance to Phase 13
autonomous: true
requirements: []                             # Closure — covers all phase REQ-IDs collectively
addresses_decisions:
  - "D-03 second half — CLAUDE.md `§ Events-Only Invariant (locked Phase 12.7)` block placed alongside the existing `§ mio-only Hot-Path Invariant (locked Phase 12.6)` block. Documents that reviving `@bv.table` requires explicit user override + new ADR overturning project_v0_events_only_scope."
  - "Section-ownership contract (12.6 Plans 09 + 13 lesson): this plan OWNS Phase 12.7 entry + critical-path block in STATE/ROADMAP/CORRECTNESS-PATH. Plan 08 (Wave 3) owned the 11.5 banner stamps. Plan 07 owned REQUIREMENTS sweep. Earlier plans don't touch the closure sections."
  - "PLANNER-SURFACED CONCERN reviewable here: Plan 06 made GroupBy.agg() raise RuntimeError; Plan 07 marked SDK-AGG-02 DESCOPED. AGG-CORE-* / AGG-SKETCH-* etc. operator-family REQ-IDs left ACTIVE. User reviews scope at this SUMMARY's Self-Check section."
must_haves:
  truths:
    - "Phase 12.7 SUMMARY.md exists with frontmatter (phase, status, overall_verdict, plan_count, decisions, metrics, etc.) matching 12.6-SUMMARY.md shape"
    - "Phase 12.7 VERIFICATION.md exists with mechanical pass/fail per ROADMAP success criteria + CONTEXT decisions D-01..D-04"
    - "CLAUDE.md has `§ Events-Only Invariant (locked Phase 12.7)` block immediately after the existing `§ mio-only Hot-Path Invariant (locked Phase 12.6)` block"
    - "STATE.md `Current Focus` line advanced to Phase 13"
    - "STATE.md phase-progress block updated (completed_phases incremented)"
    - "ROADMAP.md Phase 12.7 entry updated to ✅ COMPLETED with date + plan count + verdict"
    - "ROADMAP.md v0 critical-path block advances Phase 12.7 → Phase 13"
    - "CORRECTNESS-PATH.md Phase 12.7 marked → CLOSED; Phase 13 → NEXT"
    - "Workspace cargo test green: 1067/0/3 (or whatever count post-Plan-06's test deletes; expect approximately 1067 - ~12 deleted + 4 new = ~1059)"
    - "Architectural test pair fully GREEN"
    - "Microbench + throughput verdicts documented in VERIFICATION.md"
  artifacts:
    - path: ".planning/phases/12.7-table-strip/12.7-SUMMARY.md"
      provides: "Phase narrative — executive summary + plan-by-plan TOC + architectural outcomes + decisions honored + lessons learned"
      contains: "Phase 12.7 — v0 table strip — SUMMARY"
      min_lines: 200
    - path: ".planning/phases/12.7-table-strip/12.7-VERIFICATION.md"
      provides: "Mechanical pass/fail per ROADMAP success criteria + CONTEXT decisions + perf gates"
      contains: "OVERALL"
      min_lines: 100
    - path: "CLAUDE.md"
      provides: "§ Events-Only Invariant block; sibling to § mio-only Hot-Path Invariant"
      contains: "Events-Only Invariant (locked Phase 12.7)"
    - path: ".planning/STATE.md"
      provides: "Phase 12.7 closed; Current Focus → Phase 13; progress incremented"
      contains: "Phase 12.7 CLOSED"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 12.7 marked ✅ COMPLETED; v0 critical-path → Phase 13"
      contains: "Phase 12.7"
    - path: ".planning/CORRECTNESS-PATH.md"
      provides: "Phase 12.7 → CLOSED; Phase 13 → NEXT/P0"
      contains: "Phase 12.7"
  key_links:
    - from: "CLAUDE.md `§ Events-Only Invariant`"
      to: "Plan 02's architectural-test pair"
      via: "How enforced subsection cites both architectural test files"
      pattern: "Events-Only Invariant|phase12_7_no_table_surface"
---

<objective>
Phase 12.7 closure. Lands the Phase 12.7 SUMMARY narrative, VERIFICATION mechanical pass/fail, CLAUDE.md `§ Events-Only Invariant` convention block, and the section-partitioned closure edits to STATE.md / ROADMAP.md / CORRECTNESS-PATH.md.

After this plan lands:
- Phase 12.7 is officially CLOSED
- Workspace state: cargo test green; clippy + fmt clean; architectural test pair fully GREEN
- v0 critical path advances: Phase 12.7 (CLOSED) → Phase 13 (NEXT — final v0 ship)
- CLAUDE.md has the events-only invariant locked at the same convention level as mio-only

**Wave 4 placement:** runs LAST. Depends on Plans 01-09 closing.

**Section-ownership contract (per 12.6 Plans 09 + 13 lesson):**
- This plan OWNS: Phase 12.7 entry + v0 critical-path block in STATE/ROADMAP/CORRECTNESS-PATH; Phase 12.7 SUMMARY/VERIFICATION; CLAUDE.md events-only invariant block; phase-progress block in STATE.md; Current Focus line.
- This plan DOES NOT touch:
  - 11.5 banner content (Plan 08's territory)
  - REQUIREMENTS.md (Plan 07's territory)
  - Architectural test files (Plan 02's territory; KEEP as-is — they're the locked CI gate)
  - Microbench + throughput baseline files (Plan 09's territory)

Verify section boundaries via `git diff` BEFORE each commit (per 12.6 Plan 13's contract).

Output:
- 6 file edits (2 new files in phase dir; 4 edits to top-level docs/CLAUDE.md)
- Final closure of Phase 12.7
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.7-table-strip/12.7-CONTEXT.md
@.planning/phases/12.7-table-strip/12.7-PATTERNS.md
@.planning/phases/12.6-v0-surface-reduction/12.6-SUMMARY.md
@.planning/phases/12.6-v0-surface-reduction/12.6-VERIFICATION.md
@.planning/phases/12.6-v0-surface-reduction/12.6-13-SUMMARY.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/CORRECTNESS-PATH.md

<interfaces>
<!-- CLAUDE.md `§ Events-Only Invariant` block — verbatim from PATTERNS.md §Pattern 8 lines 595-605 -->

To insert in CLAUDE.md immediately after the existing `### mio-only Hot-Path Invariant (locked Phase 12.6)` block (around line 87 of CLAUDE.md):

```markdown
### Events-Only Invariant (locked Phase 12.7)

**Rule:** Beava v0 ships events-only. The public `bv` namespace exposes `@bv.event` and the operator catalogue; `@bv.table`, `app.upsert`, `app.delete`, `app.retract`, `OpNode::Table*`, `RecordType::TableUpsert/TableDelete/Retract`, `TemporalStore`, `MvccVersion`, `temporal_http`, `push_table`, `delete_table` are all gone. Register payloads with `{"kind": "table", ...}` are rejected at the JSON-prelude layer with structured error code `unsupported_node_kind`. Wire-format reset to version=1 (WAL `FORMAT_VERSION = 1`, snapshot `SNAPSHOT_FORMAT_VERSION = 1`, snapshot body `SNAPSHOT_BODY_FORMAT_VERSION = 1`); pre-12.7 dev WALs / snapshots fail at the version-byte check and require operators to clear `.beava/wal` + `.beava/snapshots` before boot.

**Why:** Locked architectural commitments — `project_v0_events_only_scope` (locked 2026-04-30): v0 strips tables / table-aggregation / session windows / `bv.fork` / playground. Tables/joins/aggregation return together in v0.1+ if/when justified by demand. Combined with `project_redis_shaped_no_event_time_ever`, v0's surface is push events / register events+derivations / get features.

**How enforced:** `crates/beava-server/tests/phase12_7_no_table_surface.rs` walks the workspace at test runtime and fails if any forbidden symbol resurfaces (`OpNode::Table*`, `TemporalStore`, `MvccVersion`, `RecordType::TableUpsert/TableDelete/Retract`, `WireRequest::HttpUpsert/HttpDelete/HttpRetract/HttpTableGet`, `Route::Upsert/Delete/Retract/TableGet`, `temporal_http`, `push_table`, `delete_table`, `fn retract(`). CI runs the test on every PR via `cargo test --workspace`. Companion: `crates/beava-server/tests/phase12_7_legacy_table_handlers_killed.rs` asserts the deleted files (`temporal_http.rs`, `temporal.rs`, `python/beava/_tables.py`) stay deleted.

**Reviving `@bv.table` requires:** explicit user override + new ADR overturning `project_v0_events_only_scope`.
```

12.6-13's SUMMARY structure (frontmatter shape) is the template for 12.7-SUMMARY.md.
12.6-VERIFICATION.md is the template for 12.7-VERIFICATION.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (RED): Confirm preconditions — all prior plans GREEN; closure files don't yet exist</name>
  <read_first>
    - .planning/phases/12.7-table-strip/ (current contents — 9 plans + 9 SUMMARYs from prior plans)
    - CLAUDE.md (current state — § mio-only Hot-Path Invariant present; § Events-Only Invariant absent)
  </read_first>
  <files>(no new file — RED gate is precondition assertion)</files>
  <action>
Confirm preconditions:

```bash
# 1. All prior 9 SUMMARYs exist:
for n in 01 02 03 04 05 06 07 08 09; do
  [ -f .planning/phases/12.7-table-strip/12.7-${n}-SUMMARY.md ] || echo "MISSING: 12.7-${n}-SUMMARY.md"
done

# 2. Closure files don't yet exist:
[ ! -f .planning/phases/12.7-table-strip/12.7-SUMMARY.md ] || echo "12.7-SUMMARY.md unexpectedly exists!"
[ ! -f .planning/phases/12.7-table-strip/12.7-VERIFICATION.md ] || echo "12.7-VERIFICATION.md unexpectedly exists!"

# 3. CLAUDE.md events-only invariant absent:
grep -q '§ Events-Only Invariant\|Events-Only Invariant.*locked Phase 12.7' CLAUDE.md && echo "CLAUDE.md unexpectedly has events-only invariant block!"

# 4. ROADMAP Phase 12.7 still in PLANNED state (not COMPLETED):
grep -E '^\| 12\.7 \|' .planning/ROADMAP.md | grep -q '📋 PLANNED' && echo "OK — ROADMAP still PLANNED" || echo "ROADMAP unexpectedly COMPLETED!"

# 5. Workspace clean:
cargo test --workspace 2>&1 | tail -1 | grep -q '0 failed' || echo "Workspace cargo test FAILED — fix Wave 3 plans first"

# 6. Architectural test pair GREEN:
cargo test -p beava-server --test phase12_7_no_table_surface 2>&1 | grep -q 'test result: ok' || echo "phase12_7_no_table_surface FAILED"
cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed 2>&1 | grep -q 'test result: ok' || echo "phase12_7_legacy_table_handlers_killed FAILED"

# 7. Plan 09 perf rebaseline complete:
grep -q '^### Phase 12.7' .planning/perf-baselines.md || echo "perf-baselines.md missing Phase 12.7 section"
grep -q '^## Phase 12.7' .planning/throughput-baselines.md || echo "throughput-baselines.md missing Phase 12.7 section"
```

Any "unexpected" or "MISSING" output means a precondition is broken; fix the upstream plan first.

NO commit lands for this task.
  </action>
  <verify>
    <automated>cargo test --workspace 2>&1 | tail -1 | grep -q '0 failed' && cargo test -p beava-server --test phase12_7_no_table_surface 2>&1 | grep -q 'test result: ok' && cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed 2>&1 | grep -q 'test result: ok' && grep -q '^### Phase 12.7' .planning/perf-baselines.md && grep -q '^## Phase 12.7' .planning/throughput-baselines.md && [ ! -f .planning/phases/12.7-table-strip/12.7-SUMMARY.md ]</automated>
    EXPECTED: all conditions hold (precondition met)
  </verify>
  <acceptance_criteria>
    - All 9 prior SUMMARYs exist: `for n in 01 02 03 04 05 06 07 08 09; do [ -f .planning/phases/12.7-table-strip/12.7-${n}-SUMMARY.md ] || exit 1; done`
    - Closure files absent: `[ ! -f .planning/phases/12.7-table-strip/12.7-SUMMARY.md ]` AND `[ ! -f .planning/phases/12.7-table-strip/12.7-VERIFICATION.md ]`
    - CLAUDE.md missing events-only block: `! grep -q 'Events-Only Invariant.*locked Phase 12.7' CLAUDE.md`
    - Workspace cargo test green
    - Architectural test pair GREEN
    - Plan 09 perf section appended
  </acceptance_criteria>
  <done>RED preconditions held: all upstream plans complete; closure files don't exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (GREEN, part 1): Write 12.7-VERIFICATION.md (mechanical pass/fail)</name>
  <read_first>
    - .planning/phases/12.6-v0-surface-reduction/12.6-VERIFICATION.md (template)
    - .planning/phases/12.7-table-strip/12.7-CONTEXT.md (decisions D-01..D-04)
    - .planning/ROADMAP.md (Phase 12.7 success criteria)
    - All 9 prior 12.7-NN-SUMMARY.md (each plan's outcomes)
    - .planning/perf-baselines.md (Plan 09's microbench section)
    - .planning/throughput-baselines.md (Plan 09's throughput section)
  </read_first>
  <files>.planning/phases/12.7-table-strip/12.7-VERIFICATION.md</files>
  <action>
Write `.planning/phases/12.7-table-strip/12.7-VERIFICATION.md` modeled on 12.6-VERIFICATION.md. Required sections:

1. **OVERALL verdict line** — PASS / PASS-WITH-WARN / BLOCK
2. **Per-CONTEXT-decision pass/fail:**
   - D-01 — RESET FORMAT_VERSION 2→1: pass criterion check (3 constants reset; verifiable by grep)
   - D-02 — Hard errors framed "not supported in v0": pass criterion check (Plan 01 shim + 404s + AttributeError + RuntimeError)
   - D-03 — Two-file architectural-test pair: pass criterion check (both tests GREEN)
   - D-04 — REQUIREMENTS sweep + 11.5 banner: pass criterion check (Plan 07's banner count + Plan 08's 3 files banner-stamped)
3. **Per-must_have-truth check** — walk through every must_have from each plan's frontmatter; mark as VERIFIED with the literal grep/test command
4. **Code-level checks:**
   - `cargo test --workspace`: 0 failures
   - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: 0 errors
   - `cargo fmt --all --check`: 0 diffs
   - `pytest python/tests/`: 0 failures
   - Test count delta: pre-12.7 1067 → post-12.7 ~1067 - 12 (deleted) + 4 (new) = ~1059 (record actual)
5. **Architectural commitments:**
   - phase12_7_no_table_surface.rs: GREEN
   - phase12_7_legacy_table_handlers_killed.rs: GREEN
   - All 18 forbidden patterns absent (sample-grep verification)
   - 3 deleted files (temporal_http.rs, temporal.rs, _tables.py) absent
6. **Performance:**
   - Microbench Δ% per cell + verdict (PASS / WARN / BLOCK)
   - Throughput Δ% per cell + regression-gate (small/tcp) verdict
7. **PLANNER-SURFACED CONCERN review:** SDK-AGG-02 swept; AGG-CORE-* etc. left ACTIVE. User confirmation requested.

Sample shape:

```markdown
---
phase: 12.7-table-strip
verdict: PASS / PASS-WITH-WARN / BLOCK
verified_at: <ISO8601>
verified_by: Plan 12.7-10
---

# Phase 12.7 — v0 table strip — VERIFICATION

## OVERALL: <PASS / PASS-WITH-WARN / BLOCK>

## CONTEXT decisions D-01..D-04

### D-01 — RESET FORMAT_VERSION 2→1: PASS
- record.rs::FORMAT_VERSION == 1: VERIFIED (`grep 'FORMAT_VERSION: u32 = 1' crates/beava-persistence/src/record.rs`)
- ...

### D-02 — Hard errors framed "not supported in v0": PASS
- ...

(etc.)

## Code-level checks

| Check | Result |
|---|---|
| cargo test --workspace | 0 failed (1059/0/3) |
| cargo clippy ... -D warnings | 0 errors |
| cargo fmt --all --check | 0 diffs |
| pytest python/tests/ | 0 failed (NN passed) |

## Architectural commitments
| Test | Verdict |
|---|---|
| phase12_7_no_table_surface | GREEN |
| phase12_7_legacy_table_handlers_killed | GREEN |
| ... | ... |

## Performance gates

(microbench + throughput tables)

## PLANNER-SURFACED CONCERN: SDK-AGG-02 + .agg() RuntimeError

(write up the planner-surfaced point from Plans 06 + 07; mark as user-action-required if scope expansion needed)
```

Commit:
```
docs(12.7-10): VERIFICATION.md — mechanical pass/fail per ROADMAP + CONTEXT decisions
```
  </action>
  <verify>
    <automated>[ -f .planning/phases/12.7-table-strip/12.7-VERIFICATION.md ] && grep -q 'OVERALL' .planning/phases/12.7-table-strip/12.7-VERIFICATION.md && [ $(wc -l < .planning/phases/12.7-table-strip/12.7-VERIFICATION.md) -ge 100 ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists, ≥ 100 LOC: `[ $(wc -l < .planning/phases/12.7-table-strip/12.7-VERIFICATION.md) -ge 100 ]`
    - Has OVERALL verdict: `grep -q 'OVERALL' .planning/phases/12.7-table-strip/12.7-VERIFICATION.md`
    - Has all 4 D-XX decisions: `grep -c 'D-0[1-4]' .planning/phases/12.7-table-strip/12.7-VERIFICATION.md` returns ≥ 4
    - Has code-level checks: `grep -q 'cargo test\|cargo clippy\|cargo fmt' .planning/phases/12.7-table-strip/12.7-VERIFICATION.md`
    - Has architectural commitments check: `grep -q 'phase12_7_no_table_surface\|phase12_7_legacy_table_handlers_killed' .planning/phases/12.7-table-strip/12.7-VERIFICATION.md`
    - Has performance gates: `grep -q 'microbench\|throughput' .planning/phases/12.7-table-strip/12.7-VERIFICATION.md`
    - Has SDK-AGG-02 concern: `grep -q 'SDK-AGG-02\|PLANNER-SURFACED' .planning/phases/12.7-table-strip/12.7-VERIFICATION.md`
    - Commit: `git log -1 --format=%s | grep -E '^docs\(12\.7-10\):.*VERIFICATION'`
  </acceptance_criteria>
  <done>VERIFICATION.md exists with mechanical pass/fail. Commit `docs(12.7-10):` (first of two closure commits).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.c (GREEN, part 2): Write 12.7-SUMMARY.md + CLAUDE.md events-only block + STATE/ROADMAP/CORRECTNESS-PATH closure</name>
  <read_first>
    - .planning/phases/12.6-v0-surface-reduction/12.6-SUMMARY.md (template)
    - .planning/phases/12.6-v0-surface-reduction/12.6-13-SUMMARY.md (closure-plan template)
    - All 9 prior 12.7-NN-SUMMARY.md (per-plan narratives)
    - CLAUDE.md (current state — § mio-only Hot-Path Invariant location for adjacency)
    - .planning/STATE.md (Current Focus + phase-progress)
    - .planning/ROADMAP.md (Phase 12.7 entry + critical-path block)
    - .planning/CORRECTNESS-PATH.md (Phase 12.7 entry — currently 🔴 P0)
    - .planning/phases/12.7-table-strip/12.7-PATTERNS.md (§Pattern 8 — verbatim CLAUDE.md block)
  </read_first>
  <files>
    - .planning/phases/12.7-table-strip/12.7-SUMMARY.md
    - CLAUDE.md
    - .planning/STATE.md
    - .planning/ROADMAP.md
    - .planning/CORRECTNESS-PATH.md
  </files>
  <action>
**Step 1 — Write 12.7-SUMMARY.md:**

Model on 12.6-SUMMARY.md frontmatter + sections. Required content:

- Frontmatter: phase, subsystem, tags, provides[], affects[], tech-stack, key-files (created/modified/deleted), decisions, metrics, plan_count, overall_verdict, date_opened, date_closed, commits_phase, ...
- **Executive summary** (1-2 paragraphs): what 12.7 accomplished and why
- **Headline numbers**: total LOC deleted (~2150 across temporal_http + temporal.rs + _tables.py + test files); commits in plan range; workspace test count delta; perf verdict
- **Plan-by-plan TOC**: 10 plans (01-10); for each: wave, brief description, commit count (estimate 1-3 per plan), LOC delta, link to per-plan SUMMARY
- **Architectural outcomes**:
  1. v0 events-only commitment locked at CI level (architectural test pair)
  2. Wire/router/listener layer slimmer by 4 endpoints
  3. Persistence schema reset 2→1 (3 constants)
  4. Python SDK trimmed of table surface
  5. REQUIREMENTS.md comprehensive sweep (8 REQ-IDs DESCOPED + 1 NEW positive)
  6. Phase 11.5 retroactively descoped (3 banner-stamped files)
- **Decisions honored (D-01..D-04)** — verbatim from CONTEXT, with cite to which plan honored each
- **Lessons learned** (carryover from 12.6's lessons + any 12.7-specific):
  - Architectural test inversion (Plan 02): "RED at write-time" via NEGATIVE assertions (symbol/file absence) — deletions turn it GREEN
  - Section-ownership contract held (Plans 07 + 08 + 10 partition)
  - Pure-deletion phase has small perf delta (0±10% on all cells)
  - Planner-surfaced concern: when deletion cascades to non-listed surface (`.agg()` flow), document it openly in PLAN frontmatter and SUMMARY for user review
- **Open work** that did NOT land in 12.7 (per CONTEXT's Out-of-scope):
  - bv.fork, playground, packaging, docs sweep → Phase 13
  - Pre-pivot WAL/snapshot migration → operator-clear (D-01)
- **Coordination notes for Phase 13**: handoff surface (perf baselines, architectural-test gate, V0-EVENTS-ONLY-01 anchor)
- **Self-Check** appended at end: per-acceptance-criterion verification

**Step 2 — Add CLAUDE.md `§ Events-Only Invariant` block:**

Use the verbatim block from PATTERNS.md §Pattern 8 lines 595-605 (also reproduced in this plan's interfaces section). Insert IMMEDIATELY AFTER the existing `### mio-only Hot-Path Invariant (locked Phase 12.6)` block in CLAUDE.md (around line 87).

Verify section-ownership: this is the ONLY plan that touches CLAUDE.md.

**Step 3 — STATE.md updates:**

Update sections this plan owns (per section-ownership contract):

- **Current Focus** (line ~106): change from "Phase 12.6 CLOSED ... — v0 surface reduction landed" + "Next: v0 critical path → Phase 13" to a Phase 12.7 closure narrative + "Next: v0 critical path → Phase 13 (final ship)".
- **Phase-progress block** (frontmatter `progress:` block at top): increment `completed_phases`. Update `last_updated`. Update `status` line to mention Phase 12.7 closure.
- **Session continuity comments** (top HTML comment block): add Phase 12.7 closure note.

DO NOT touch the worktree-status block (Plan 09 of 12.6 owned it; 12.7 doesn't update worktrees).

**Step 4 — ROADMAP.md updates:**

- Phase 12.7 entry (line ~51): change from `📋 PLANNED 2026-04-30` to `✅ COMPLETED <date> (PASS / PASS-WITH-WARN per VERIFICATION verdict)` with plan count + headline. Use 12.6's ✅ COMPLETED entry as the template.
- v0 critical-path block: advance "v0 ship critical path = Phase 12.7 (...) → Phase 13 (...)" to "v0 ship critical path = Phase 13 (final ship — SDK polish + benchmarks + min-viable docs + PyPI/Docker/GitHub Releases)".
- Insertion-history note (post 2026-04-30 paragraph): append a Phase 12.7 closed line.

**Step 5 — CORRECTNESS-PATH.md updates:**

- Phase 12.7 section (currently 🔴 P0): move to 🟢 CLOSED section with closure narrative matching 12.6's format
- 🔴 P0 list: Phase 13 becomes the new top item (REFRAMED — already framed; just confirm it's #1)

**Step 6 — Verify section-ownership boundaries via git diff:**

Before each commit, run `git diff` on the section files and confirm only the assigned sections are touched. The contract from 12.6 Plans 09 + 13:

- This plan TOUCHES: Phase 12.7 entry / Current Focus / phase-progress / v0 critical-path / Phase 12.7 SUMMARY/VERIFICATION + CLAUDE.md events-only block.
- This plan DOES NOT TOUCH: 11.5 banners (Plan 08); REQUIREMENTS.md (Plan 07); architectural test files (Plan 02); microbench/throughput files (Plan 09); other phase entries.

**Step 7 — Final workspace verification before commit:**

```bash
cargo test --workspace 2>&1 | tail -1 | grep '0 failed'
cargo clippy --workspace --all-targets --all-features -- -D warnings 2>&1 | grep -cE '^error\['
cargo fmt --all --check
cd python && pytest tests/ 2>&1 | tail -3
cd ..
cargo test -p beava-server --test phase12_7_no_table_surface 2>&1 | grep 'test result: ok'
cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed 2>&1 | grep 'test result: ok'
```

ALL must hold. If anything regressed since Plan 09 closed, escalate.

**Step 8 — Commit:**

```
docs(12.7-10): SUMMARY.md + CLAUDE.md (Events-Only Invariant) + STATE.md (phase-progress + Current Focus) + ROADMAP.md (Phase 12.7 ✅ + v0 critical-path) + CORRECTNESS-PATH.md (Phase 12.7 → CLOSED) — Phase 12.7 CLOSED

Phase 12.7 closure plan owns this section-partitioned set of edits per
the section-ownership contract from 12.6 Plans 09 + 13.

Closure deltas:
- 12.7-SUMMARY.md: phase narrative (~250 LOC; mirrors 12.6-SUMMARY.md shape)
- CLAUDE.md: § Events-Only Invariant (locked Phase 12.7) block — sibling to existing § mio-only Hot-Path Invariant block
- STATE.md: Current Focus → Phase 13; phase-progress completed_phases++
- ROADMAP.md: Phase 12.7 ✅ COMPLETED <date>; v0 critical-path advances → Phase 13
- CORRECTNESS-PATH.md: Phase 12.7 → 🟢 CLOSED; Phase 13 stays as 🔴 P0 #1

Verdict: <PASS / PASS-WITH-WARN per VERIFICATION>
Total LOC delta across 10 plans: approximately -<N> (mostly deletion)
Commits in Phase 12.7 range: <count>
Workspace test count: <count>/0/<ignored>
Microbench: <verdict-summary>
Throughput regression-gate (small/tcp): <Δ%>

Phase 12.7 hands off to Phase 13 (final v0 ship — SDK polish + benchmarks
+ minimum-viable docs + PyPI/Docker/GitHub Releases).
```
  </action>
  <verify>
    <automated>[ -f .planning/phases/12.7-table-strip/12.7-SUMMARY.md ] && [ $(wc -l < .planning/phases/12.7-table-strip/12.7-SUMMARY.md) -ge 200 ] && grep -q 'Events-Only Invariant.*locked Phase 12.7' CLAUDE.md && grep -q 'Phase 12.7' .planning/STATE.md && grep -E '^\| 12\.7 \|' .planning/ROADMAP.md | grep -q '✅\|COMPLETED'</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY.md exists ≥ 200 LOC: `[ $(wc -l < .planning/phases/12.7-table-strip/12.7-SUMMARY.md) -ge 200 ]`
    - SUMMARY.md has frontmatter with overall_verdict: `grep -q 'overall_verdict' .planning/phases/12.7-table-strip/12.7-SUMMARY.md`
    - SUMMARY.md has plan-by-plan TOC: `grep -c '^\| 12\.7-' .planning/phases/12.7-table-strip/12.7-SUMMARY.md` returns ≥ 10
    - SUMMARY.md has decisions D-01..D-04: `grep -c 'D-0[1-4]' .planning/phases/12.7-table-strip/12.7-SUMMARY.md` returns ≥ 4
    - CLAUDE.md has events-only block: `grep -q 'Events-Only Invariant.*locked Phase 12.7' CLAUDE.md`
    - CLAUDE.md events-only block placement: events-only block appears AFTER mio-only block (line numbers): `[ $(grep -n 'Events-Only Invariant' CLAUDE.md | head -1 | cut -d: -f1) -gt $(grep -n 'mio-only Hot-Path Invariant' CLAUDE.md | head -1 | cut -d: -f1) ]`
    - CLAUDE.md events-only block cites architectural test: `grep -A 20 'Events-Only Invariant' CLAUDE.md | grep -q 'phase12_7_no_table_surface'`
    - STATE.md `Current Focus` updated: `grep -A 3 'Current Focus' .planning/STATE.md | grep -q 'Phase 12.7\|Phase 13'`
    - STATE.md phase-progress block updated: `grep -q 'completed_phases:' .planning/STATE.md` (frontmatter intact; counts updated — verify by manual read)
    - ROADMAP.md Phase 12.7 marked COMPLETED: `grep -E '^\| 12\.7 \|' .planning/ROADMAP.md | grep -qE '✅|COMPLETED'`
    - ROADMAP.md v0 critical-path advances to Phase 13: `grep -q 'critical path = Phase 13\|critical path → Phase 13' .planning/ROADMAP.md`
    - CORRECTNESS-PATH.md Phase 12.7 marked CLOSED: `grep -A 5 'Phase 12.7' .planning/CORRECTNESS-PATH.md | grep -qE 'CLOSED|🟢'`
    - Section-ownership boundary held: this plan does NOT touch 11.5 banner files (Plan 08's territory): `git log -p HEAD -- .planning/phases/11.5-temporal-tables-retraction-primitive/ 2>&1 | head -5` — verify last commit touching those files was Plan 08's commit
    - Section-ownership boundary held: this plan does NOT touch REQUIREMENTS.md: `git log -p HEAD -- .planning/REQUIREMENTS.md 2>&1 | head -5` — verify last commit was Plan 07's
    - Workspace remains clean post-closure: `cargo test --workspace 2>&1 | grep -q '0 failed' && cargo clippy --workspace --all-targets --all-features -- -D warnings 2>&1 | grep -cE '^error\[' | grep -q '^0$'`
    - Architectural tests still GREEN: `cargo test -p beava-server --test phase12_7_no_table_surface 2>&1 | grep -q 'test result: ok'`
    - Commit: `git log -1 --format=%s | grep -E '^docs\(12\.7-10\):.*Phase 12\.7 CLOSED'`
  </acceptance_criteria>
  <done>GREEN gate: SUMMARY + CLAUDE.md events-only block + STATE/ROADMAP/CORRECTNESS-PATH closure. Section-ownership held. Workspace + architectural tests still GREEN. Commit `docs(12.7-10):` (second closure commit). Phase 12.7 OFFICIALLY CLOSED.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.7-10-01 | Information Disclosure | CLAUDE.md events-only invariant block | accept | Public convention block; intentional disclosure to future contributors. |
</threat_model>

<verification>
- 12.7-SUMMARY.md exists ≥ 200 LOC with proper frontmatter + plan-by-plan TOC + decisions + lessons learned
- 12.7-VERIFICATION.md exists ≥ 100 LOC with mechanical pass/fail per CONTEXT D-01..D-04
- CLAUDE.md `§ Events-Only Invariant (locked Phase 12.7)` block present, placed after § mio-only Hot-Path Invariant
- STATE.md Current Focus + phase-progress updated (section-partitioned per Plan 09's worktree block; this plan only owns the closure sections)
- ROADMAP.md Phase 12.7 marked ✅ COMPLETED; v0 critical-path advances to Phase 13
- CORRECTNESS-PATH.md Phase 12.7 → CLOSED
- Workspace cargo test + clippy + fmt clean
- Architectural test pair fully GREEN
- Section-ownership boundaries respected (verified via git diff)
- 2 commits: VERIFICATION.md (Task 1.b) + closure commit covering SUMMARY + CLAUDE.md + STATE/ROADMAP/CORRECTNESS-PATH (Task 1.c)
</verification>

<success_criteria>
- Phase 12.7 OFFICIALLY CLOSED with verdict (PASS / PASS-WITH-WARN per VERIFICATION)
- All 4 CONTEXT decisions D-01..D-04 honored verbatim
- 10 plans landed in Phase 12.7 commit range
- Workspace state at HEAD: cargo test green, clippy clean, fmt clean, pytest green
- v0 critical path advances Phase 12.7 → Phase 13
- CLAUDE.md events-only invariant locked at CI convention level
- Section-ownership contract held (verified via git diff)
- 2 closure commits land per CLAUDE.md TDD §Note 4 (smoke/acceptance docs land at end of plan)
- PLANNER-SURFACED CONCERN documented in SUMMARY for user review
</success_criteria>

<output>
This IS the closure SUMMARY. No follow-on output needed.

After closure: user runs `/gsd-discuss-phase 13` to start the final v0 ship phase.

Self-Check appended at end of 12.7-SUMMARY.md:
- [x] All 10 plan SUMMARYs exist
- [x] Self-check section appended at end of each prior SUMMARY (verified for representative plans 01, 03, 05, 06)
- [x] No code files modified by Plan 10 (D-05-equivalent boundary held — closure plan is doc-only)
- [x] Section-ownership boundary held: 11.5 banner files UNCHANGED from Plan 08 commit; REQUIREMENTS.md UNCHANGED from Plan 07 commit; architectural test files UNCHANGED from Plan 02 commit (verified via git diff before closure commit)
- [x] Workspace cargo test still green at HEAD: <count>/0/<ignored>
- [x] Workspace cargo clippy + cargo fmt still clean
- [x] CLAUDE.md `§ Events-Only Invariant (locked Phase 12.7)` block intact
- [x] phase12_7_no_table_surface.rs + phase12_7_legacy_table_handlers_killed.rs both GREEN
- [x] PLANNER-SURFACED CONCERN documented for user review (SDK-AGG-02 swept; AGG-CORE-* operator-family REQ-IDs left ACTIVE pending user confirmation)
</output>
