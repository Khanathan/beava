---
phase: 12.7-table-strip
plan: 03
type: execute
wave: 2
depends_on: [01, 02]                  # 01 lands the JSON-prelude shim that catches register-time table payloads after variants are gone; 02's architectural test guides the deletion list
files_modified:
  - crates/beava-runtime-core/src/wire_request.rs                # delete HttpUpsert/HttpDelete/HttpRetract/HttpTableGet variants
  - crates/beava-runtime-core/src/router.rs                       # delete Route::Upsert/Delete/Retract/TableGet variants + parse arms
  - crates/beava-runtime-core/src/http_listener.rs                # delete the 4 Route→WireRequest translate arms
  - crates/beava-server/src/apply_shard.rs                        # delete the 4 dispatch arms (lines 400-459)
autonomous: true
requirements:
  - SDK-DEC-04             # @bv.table decorator REMOVED — server-side wire surface for upsert/delete/retract/table_get gone
  - SDK-DEC-05             # @bv.table function form REMOVED
  - SRV-API-06             # POST /push-table/{table_name} REMOVED — wire endpoint deleted at router/listener level
  - SRV-API-07             # POST /delete-table/{table_name} REMOVED — wire endpoint deleted at router/listener level
  - SDK-APP-07             # app.push(Table, ...) REMOVED — corresponding mio dispatch gone
  - SDK-APP-08             # app.delete(Table, ...) REMOVED — corresponding mio dispatch gone
  - V0-EVENTS-ONLY-01      # invariant satisfied at the wire/router/listener layer
addresses_decisions:
  - "D-02 — Mio HTTP routes for the deleted paths return plain 404 (mio default for unknown routes). No special deny-handler. Stale curl callers see 'not found' — same as any unknown route."
  - "Variant-delete surgery posture (12.6 Plan 04 lesson — see 12.6-SUMMARY.md §Lessons learned item 2): Plan 01's JSON-prelude shim is already live, so deleting these variants doesn't break the rejection path — register payloads get caught at the JSON layer before reaching the (now-gone) WireRequest::Http* variants."
must_haves:
  truths:
    - "WireRequest enum has no `HttpUpsert`, `HttpDelete`, `HttpRetract`, or `HttpTableGet` variant"
    - "Route enum has no `Upsert`, `Delete`, `Retract`, or `TableGet` variant"
    - "router.rs does not parse `/upsert/{table}`, `/delete`, `/retract`, or `/table/{name}` paths"
    - "http_listener.rs does not translate any of the deleted Route variants"
    - "apply_shard.rs has no `WireRequest::HttpUpsert | HttpDelete | HttpRetract | HttpTableGet` arm"
    - "POST /upsert/X returns plain 404 from mio (no special handler — default unknown-route response)"
    - "POST /delete returns plain 404 from mio"
    - "POST /retract returns plain 404 from mio"
    - "GET /table/X returns plain 404 from mio"
    - "Workspace `cargo check --workspace` is clean — no callers of the deleted variants remain"
    - "phase12_7_legacy_table_handlers_killed.rs::wire_request_table_variants_deleted turns GREEN"
    - "phase12_7_legacy_table_handlers_killed.rs::route_table_variants_deleted turns GREEN"
  artifacts:
    - path: "crates/beava-runtime-core/src/wire_request.rs"
      provides: "WireRequest enum without HttpUpsert/HttpDelete/HttpRetract/HttpTableGet variants"
      contains: "WireRequest"
    - path: "crates/beava-runtime-core/src/router.rs"
      provides: "Route enum without Upsert/Delete/Retract/TableGet variants + parse fn without those arms"
      contains: "enum Route"
    - path: "crates/beava-runtime-core/src/http_listener.rs"
      provides: "Route → WireRequest translate fn without 4 deleted arms"
      contains: "Route::Push|Route::Get"
    - path: "crates/beava-server/src/apply_shard.rs"
      provides: "Mio dispatch loop without 4 table-flavored WireRequest arms (lines 400-459 region deleted; surrounding push/get/register arms intact)"
      contains: "dispatch_one"
  key_links:
    - from: "Plan 01's `pre_check_unsupported_node_kind` shim"
      to: "Deleted Route::Upsert/etc. paths"
      via: "Plan 01's shim catches register payloads with `kind: table` at the JSON layer; deleted Route paths return plain 404 (mio default). Both layers reject table operations, but at different surfaces (register-time vs runtime URL routing)."
      pattern: "unsupported_node_kind|Route::Upsert"
---

<objective>
Strip the wire/router/listener layer of the table surface. Delete `WireRequest::Http{Upsert,Delete,Retract,TableGet}` variants, `Route::{Upsert,Delete,Retract,TableGet}` variants, the matching path-parse arms in `router.rs`, the 4 translate arms in `http_listener.rs`, and the 4 dispatch arms in `apply_shard.rs` (lines 400-459).

After this plan lands:
- `POST /upsert/X` returns plain 404 (mio's default unknown-route response — D-02 verbatim).
- `POST /delete` returns plain 404.
- `POST /retract` returns plain 404.
- `GET /table/X` returns plain 404.
- `WireRequest::Http*` table variants gone — `phase12_7_legacy_table_handlers_killed.rs::wire_request_table_variants_deleted` and `::route_table_variants_deleted` turn GREEN.

The TableResponse glue path remains alive at this commit because the `temporal_http::*_via_mio` callees still exist in `temporal_http.rs` (deleted by Plan 04). That's deliberate — this plan ONLY surgeries the wire/router/dispatch layer, leaving Plan 04 to delete the temporal_http module + AppState fields.

**Wave 2 placement:** depends on Plan 01's shim (so register payloads still get rejected with structured error after WireRequest::Http* gone) and Plan 02's architectural test (which guides the deletion list). Independent of Plan 04 (different files) and Plan 05 (different crate).

Output:
- 4 enum variants gone from WireRequest (in wire_request.rs)
- 4 enum variants gone from Route (in router.rs)
- 4 path-parse arms gone from router.rs's `parse` fn
- 4 translate arms gone from http_listener.rs
- 4 dispatch arms gone from apply_shard.rs (lines 400-459)
- Workspace `cargo check` clean
- Two architectural-test assertions in `phase12_7_legacy_table_handlers_killed.rs` turn GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.7-table-strip/12.7-CONTEXT.md
@.planning/phases/12.7-table-strip/12.7-PATTERNS.md
@crates/beava-runtime-core/src/wire_request.rs
@crates/beava-runtime-core/src/router.rs
@crates/beava-runtime-core/src/http_listener.rs
@crates/beava-server/src/apply_shard.rs

<interfaces>
<!-- Concrete deletion sites, verified at PATTERNS.md write time. -->

`crates/beava-runtime-core/src/wire_request.rs:69-77` (current — to be deleted):
```rust
HttpUpsert { table: String, body: Bytes },
HttpDelete { table: String, body: Bytes },
HttpRetract { body: Bytes },
HttpTableGet { table: String, query: String },
```

`crates/beava-runtime-core/src/router.rs:19-27` (current — variants to delete):
```rust
Upsert { table: String },
Delete { table: String },
Retract,
TableGet { table: String },
```
Plus parse arms at `router.rs:121-158` that match `/upsert/...`, `/delete`, `/retract`, `/table/...` paths.

`crates/beava-runtime-core/src/http_listener.rs:234-240` (current — translate arms to delete):
```rust
Route::Upsert { table } => WireRequest::HttpUpsert { table, body },
Route::Delete { table } => WireRequest::HttpDelete { table, body },
Route::Retract => WireRequest::HttpRetract { body },
Route::TableGet { table } => WireRequest::HttpTableGet { table, query: ... },
```

`crates/beava-server/src/apply_shard.rs:400-459` (current — 4 dispatch arms to delete):
```rust
WireRequest::HttpUpsert { table, body } => { ... temporal_http::upsert_via_mio ... }
WireRequest::HttpDelete { table, body } => { ... temporal_http::delete_via_mio ... }
WireRequest::HttpRetract { body } => { ... temporal_http::retract_via_mio ... }
WireRequest::HttpTableGet { table, query } => { ... temporal_http::table_get_via_mio ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (RED): Failing integration test for plain 404 on deleted table routes</name>
  <read_first>
    - .planning/phases/12.7-table-strip/12.7-CONTEXT.md (D-02 — mio HTTP routes return plain 404)
    - crates/beava-server/tests/phase12_6_14_mio_temporal.rs (existing test that asserts 200/4xx for /upsert/etc.; this test will be deleted in Plan 06's test sweep — DO NOT depend on it)
    - crates/beava-server/src/testing.rs (TestServer harness)
  </read_first>
  <files>crates/beava-server/tests/phase12_7_table_routes_404.rs</files>
  <behavior>
    - Test 1 — `post_upsert_returns_404`: POST `/upsert/UserProfile` with arbitrary body returns 404 (no special handler; mio default unknown-route response).
    - Test 2 — `post_delete_returns_404`: POST `/delete` returns 404.
    - Test 3 — `post_retract_returns_404`: POST `/retract` returns 404.
    - Test 4 — `get_table_returns_404`: GET `/table/UserProfile?key=foo` returns 404.
    - Test 5 — `surviving_routes_still_work`: POST `/register` with valid event payload returns 2xx (sanity — deletion didn't break the surviving routes).
  </behavior>
  <action>
Create `crates/beava-server/tests/phase12_7_table_routes_404.rs`. Use TestServer harness; for each of the 4 deleted routes, post a request and assert HTTP 404 status. The 5th test posts a normal `/register` payload and asserts 2xx so we don't accidentally break the surviving HTTP surface.

At end of this task, the test FAILS RED for tests 1-4 because the routes still exist (and respond with 200 or 4xx-not-404 due to the temporal_http handlers). Test 5 passes (register still works).

Sample assertion:
```rust
let resp = http_post(&server, "/upsert/UserProfile", b"{}");
assert_eq!(resp.status(), 404, "POST /upsert/X must return 404 after Plan 12.7-03 deletes Route::Upsert");
```

Commit message:
```
test(12.7-03): add failing tests for 404 on deleted table routes
```
  </action>
  <verify>
    <automated>cargo test -p beava-server --test phase12_7_table_routes_404 2>&1 | grep -E 'test result: FAILED|FAILED\.' | head -1</automated>
    EXPECTED: `test result: FAILED` (RED — at least 4 of 5 tests fail because routes still exist)
  </verify>
  <acceptance_criteria>
    - File exists: `[ -f crates/beava-server/tests/phase12_7_table_routes_404.rs ]`
    - 5 tests present: `grep -c '#\[test\]' crates/beava-server/tests/phase12_7_table_routes_404.rs` returns 5
    - Tests assert HTTP 404 for the 4 deleted routes: `grep -c '404' crates/beava-server/tests/phase12_7_table_routes_404.rs` returns ≥ 4
    - One test asserts `/register` still works (sanity for surviving routes): `grep -q '/register' crates/beava-server/tests/phase12_7_table_routes_404.rs`
    - Tests 1-4 fail (RED gate): `cargo test -p beava-server --test phase12_7_table_routes_404 2>&1 | grep -c FAILED` returns ≥ 4
    - Commit: `git log -1 --format=%s | grep -E '^test\(12\.7-03\):'`
  </acceptance_criteria>
  <done>RED gate: file exists with 5 tests; 4 fail because routes still exist. Commit with `test(12.7-03):`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (GREEN): Delete WireRequest variants + Route variants + parse arms + translate arms + dispatch arms</name>
  <read_first>
    - crates/beava-runtime-core/src/wire_request.rs (lines 69-77 to delete)
    - crates/beava-runtime-core/src/router.rs (lines 19-27 + 121-158 to delete)
    - crates/beava-runtime-core/src/http_listener.rs (lines 234-240 to delete)
    - crates/beava-server/src/apply_shard.rs (lines 400-459 to delete)
    - .planning/phases/12.7-table-strip/12.7-PATTERNS.md (§Pattern 5 — Variant + dispatch surgery)
  </read_first>
  <files>
    - crates/beava-runtime-core/src/wire_request.rs
    - crates/beava-runtime-core/src/router.rs
    - crates/beava-runtime-core/src/http_listener.rs
    - crates/beava-server/src/apply_shard.rs
  </files>
  <action>
**Step 1 — `crates/beava-runtime-core/src/wire_request.rs`:**

Delete lines 69-77 (4 variants: `HttpUpsert`, `HttpDelete`, `HttpRetract`, `HttpTableGet`). Preserve all other variants (`Register`, `Push`, `Get`, `TcpGet`, `TcpGetMulti`, `HttpUnsupportedMediaType`, etc.). Use a precise edit — this is mechanical deletion of 4 contiguous variant declarations + any per-variant doc-comments.

**Step 2 — `crates/beava-runtime-core/src/router.rs`:**

Delete lines 19-27 (4 Route variants: `Upsert { table }`, `Delete { table }`, `Retract`, `TableGet { table }`). Then delete the matching path-parse arms in the `parse` fn (lines 121-158, the 4 arms that produce these Route variants — covers `/upsert/{table}`, `/delete`, `/retract`, `/table/{name}` paths). The deleted parse arms ALL fall through to the default unknown-route case (404).

Use grep to confirm no surviving references to `Route::Upsert | Delete | Retract | TableGet` after the edit:
```bash
grep -rn 'Route::Upsert\|Route::Delete\|Route::Retract\|Route::TableGet' crates/ python/  # MUST return 0
```

**Step 3 — `crates/beava-runtime-core/src/http_listener.rs`:**

Delete lines 234-240 (the 4 translate arms `Route::Upsert => WireRequest::HttpUpsert`, etc.). Comment-block at line 210 ("For Route::TableGet we pass the query through to the dispatcher.") goes with the surgery — leave only the comment shape that documents surviving routes.

**Step 4 — `crates/beava-server/src/apply_shard.rs`:**

Delete lines 400-459 (the 60-line block containing the 4 `WireRequest::Http*` dispatch arms + the leading `// ─── Upsert / delete / retract (table ops...) ──────` comment block). Preserve the surrounding arms (Register, Push, Get, TcpGet*, HttpUnsupportedMediaType, /health shim).

**Step 5 — verify:**

Run `cargo check --workspace` — MUST be clean. Any caller still referencing `WireRequest::HttpUpsert | etc.` would fail compilation; clean compile means no callers remain.

Run the integration tests from Task 1.a:
```bash
cargo test -p beava-server --test phase12_7_table_routes_404
```
EXPECTED: all 5 tests pass GREEN (404 for the 4 deleted routes; /register still works).

Run the architectural-test pair:
```bash
cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed wire_request_table_variants_deleted
cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed route_table_variants_deleted
```
EXPECTED: both pass GREEN now.

`phase12_7_no_table_surface.rs` will still fail because Plan 04/05/06's deletions haven't run — that's expected (still RED at end of Plan 03).

Run the workspace test suite:
```bash
cargo test --workspace 2>&1 | tail -5
```
EXPECTED: existing tests still pass (or, more likely, some Phase 11.5 / 18.07 / 12.6.14 tests fail because they depend on the deleted variants — those tests will be deleted by Plan 06's test sweep).

If existing-test failures exceed 25, escalate as architectural checkpoint per 12.6 Plan 14/15 lesson. Likely failure list (estimated):
- `phase11_5_temporal_smoke.rs` — references `app.upsert/delete/retract`, will be deleted in Plan 06
- `phase18_07_upsert_delete_rename_test.rs` — same, deleted in Plan 06
- `phase12_6_14_mio_temporal.rs` — references the same dispatch arms, deleted in Plan 06

These are EXPECTED failures; they confirm the deletion was effective. Note them in SUMMARY but do NOT chase them — Plan 06 deletes them in Wave 3.

Commit message:
```
feat(12.7-03): delete WireRequest::Http* table variants + Route::* + dispatch arms

- crates/beava-runtime-core/src/wire_request.rs: delete HttpUpsert/HttpDelete/HttpRetract/HttpTableGet
- crates/beava-runtime-core/src/router.rs: delete Route::Upsert/Delete/Retract/TableGet + parse arms
- crates/beava-runtime-core/src/http_listener.rs: delete 4 translate arms
- crates/beava-server/src/apply_shard.rs: delete 4 dispatch arms (~60 LOC region)

POST /upsert/X, POST /delete, POST /retract, GET /table/X all now return
plain 404 (mio default for unknown routes — D-02 verbatim). Register-time
shim from Plan 01 still catches `kind: table` payloads at the JSON layer.

Two architectural-test assertions turn GREEN:
- phase12_7_legacy_table_handlers_killed::wire_request_table_variants_deleted
- phase12_7_legacy_table_handlers_killed::route_table_variants_deleted

phase12_7_no_table_surface still RED because Plan 04 (temporal_http delete)
and Plan 06 (Python SDK strip) haven't run yet.

Some existing tests fail due to caller-site deletions (phase11_5_temporal_smoke,
phase18_07_upsert_delete_rename_test, phase12_6_14_mio_temporal — all marked
for deletion in Plan 06's test sweep, Wave 3). Workspace `cargo check` clean.
```
  </action>
  <verify>
    <automated>cargo check --workspace 2>&1 | grep -E 'error\[' | wc -l</automated>
    EXPECTED: 0 errors (clean compile)
  </verify>
  <acceptance_criteria>
    - `! grep -E 'HttpUpsert|HttpDelete|HttpRetract|HttpTableGet' crates/beava-runtime-core/src/wire_request.rs` (4 variants deleted)
    - `! grep -E 'Route::Upsert|Route::Delete|Route::Retract|Route::TableGet' crates/beava-runtime-core/src/router.rs` (4 variants deleted)
    - Surviving Route variants still present (sanity): `grep -E 'Route::Register|Route::Push|Route::Get' crates/beava-runtime-core/src/router.rs | wc -l` returns ≥ 3
    - Path-parse arms gone: `! grep -E '"/upsert"|"/delete"|"/retract"|"/table"' crates/beava-runtime-core/src/router.rs`
    - Translate arms gone: `! grep -E 'Route::Upsert|Route::Delete|Route::Retract|Route::TableGet' crates/beava-runtime-core/src/http_listener.rs`
    - Dispatch arms gone: `! grep -E 'WireRequest::HttpUpsert|WireRequest::HttpDelete|WireRequest::HttpRetract|WireRequest::HttpTableGet' crates/beava-server/src/apply_shard.rs`
    - Workspace check clean: `cargo check --workspace 2>&1 | grep -E '^error' | wc -l` returns `0`
    - Workspace clippy clean: `cargo clippy --workspace --all-targets --all-features -- -D warnings 2>&1 | grep -E '^error' | wc -l` returns `0`
    - Workspace fmt clean: `cargo fmt --all --check`
    - Plan 12.7-03 integration tests pass: `cargo test -p beava-server --test phase12_7_table_routes_404 2>&1 | grep -q 'test result: ok'`
    - Two architectural-test assertions turn GREEN: `cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed wire_request_table_variants_deleted 2>&1 | grep -q 'test result: ok'`
    - And: `cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed route_table_variants_deleted 2>&1 | grep -q 'test result: ok'`
    - Commit: `git log -1 --format=%s | grep -E '^feat\(12\.7-03\):'`
  </acceptance_criteria>
  <done>GREEN gate: 4 wire variants gone, 4 Route variants gone, parse/translate/dispatch arms gone. Workspace cargo check clean. Plan 03's own integration tests pass; 2 architectural-test sub-tests turn GREEN. Commit with `feat(12.7-03):`.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.7-03-01 | Information Disclosure | 404 vs structured rejection | accept | Per D-02, deleted routes return plain 404 (mio default). No information leaked beyond "endpoint absent". Stale curl callers see same response shape as any unknown route. |
| T-12.7-03-02 | DoS | Reduced HTTP attack surface | mitigate-by-deletion | 4 endpoints removed → 4 fewer attack vectors. Net security improvement. |
</threat_model>

<verification>
- `cargo check --workspace` clean
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean
- `cargo fmt --all --check` clean
- Plan 03's own integration test (`phase12_7_table_routes_404`) all 5 tests pass
- 2 architectural-test sub-tests turn GREEN: `wire_request_table_variants_deleted` + `route_table_variants_deleted`
- Test failures in `phase11_5_temporal_smoke`, `phase18_07_upsert_delete_rename_test`, `phase12_6_14_mio_temporal` are EXPECTED — those tests get deleted by Plan 06's test sweep
- 2 commits: `test(12.7-03):` red + `feat(12.7-03):` green
</verification>

<success_criteria>
- 4 enum variants gone from WireRequest, 4 from Route
- 4 parse arms gone from router.rs, 4 translate arms gone from http_listener.rs, 4 dispatch arms gone from apply_shard.rs
- Plan 03's integration test green; 2 architectural-test sub-tests green
- Test failures in 3 known-doomed test files are tolerated (Plan 06 deletes them) — must not exceed those 3 files' tests
- Phase 12.7-03 commit pair lands with proper TDD prefix
</success_criteria>

<output>
After completion, create `.planning/phases/12.7-table-strip/12.7-03-SUMMARY.md`. Self-Check:
- [x] 4 WireRequest::Http* variants deleted
- [x] 4 Route::* variants deleted
- [x] 4 router.rs parse arms deleted
- [x] 4 http_listener.rs translate arms deleted
- [x] 4 apply_shard.rs dispatch arms deleted (lines 400-459 region)
- [x] cargo check + clippy + fmt clean
- [x] phase12_7_table_routes_404 all 5 tests green
- [x] 2 architectural sub-tests turn GREEN
- [x] Document expected-failure list (phase11_5_temporal_smoke, phase18_07_upsert_delete_rename_test, phase12_6_14_mio_temporal) for Plan 06 to clean up
- [x] Commit trace: `test(12.7-03):` → `feat(12.7-03):`
</output>