---
phase: 12.7-table-strip
plan: 02
subsystem: architectural-tests
tags: [architectural-test, symbol-grep, file-absence, events-only, ignored-red, v0-scope, d-03]
requires:
  - PHASE12_6_MIO_ONLY_DATAPLANE_TEST       # 12.6 Plan 10 — symbol-grep scaffold
  - PHASE12_6_LEGACY_AXUM_KILLED_TEST       # 12.6 Plan 07 — file/symbol-absence scaffold
  - PRE-CHECK-UNSUPPORTED-NODE-KIND          # 12.7 Plan 01 — first-wave register-time enforcement
provides:
  - PHASE12_7_NO_TABLE_SURFACE_TEST          # symbol-grep architectural test (RED, ignored)
  - PHASE12_7_LEGACY_TABLE_HANDLERS_KILLED_TEST  # file/symbol-absence test (RED, ignored)
  - V0-EVENTS-ONLY-CI-TRIPWIRE-PAIR          # CI-runtime enforcement of project_v0_events_only_scope (locks GREEN once Plans 03-06 land)
affects:
  - crates/beava-server/tests/phase12_7_no_table_surface.rs (NEW)
  - crates/beava-server/tests/phase12_7_legacy_table_handlers_killed.rs (NEW)
  - 12.7 Plans 03/04/05/06 (each Wave 2-3 deletion plan turns one or more of these tests GREEN)
  - 12.7 Plan 09 closure (removes the #[ignore] annotations as the final tests-pass moment)
addresses_decisions:
  - "D-03 — Two-file architectural-test pair mirroring 12.6 Plan 10's `phase12_6_mio_only_dataplane.rs` + 12.6 Plan 07's `phase12_6_legacy_axum_killed.rs`. Locks `project_v0_events_only_scope` at the same architectural level as `project_phase18_no_dual_runtime`."
  - "TDD inversion for negative architectural tests (12.6 Plan 10 lesson): tests assert NEGATIVE state (symbol absent / file absent). They land in Wave 1 in RED state (fail because surface still exists); subsequent waves' deletions turn them GREEN. This is the natural shape — write the architectural commitment as test, deletions satisfy it."
tech-stack:
  added: []
  patterns:
    - "Symbol-grep architectural test (Pattern 2 from PATTERNS.md): walks `crates/beava-{core,server,runtime-core,persistence,bench}/src/` at `cargo test` time, strips line comments, greps each .rs file for any FORBIDDEN_PATTERNS token outside the allowlist. Failure message lists every (file, pattern) pair so subsequent waves know exactly what to delete."
    - "File/symbol-absence test (Pattern 3 from PATTERNS.md): six narrow per-file/per-symbol tests pin specific deletion sites by name so partial deletions during Waves 2-3 can be diagnosed precisely. Sanity-positive assertions confirm the surviving v0 surface stays intact (Event = 0x01, RegistryBump = 0x02, HttpPush/HttpGet/Register, Push/Get/Register routes, etc.)."
    - "RED-management via `#[ignore]` with plan-name annotation: each test's #[ignore] reason string names the plan that turns it GREEN (`Wave 1 RED state; turns GREEN after Plan 12.7-XX`). This keeps `cargo test --workspace` green during the RED-state interlude AND telegraphs the deletion ordering in code. Equivalent to 12.6 Plan 10's RED-state confirmation-test pattern but with explicit ignore-reasons replacing the verbal RED→GREEN-via-impl convention."
    - "Sanity-allowlist anti-no-op test: a third sanity test in `phase12_7_no_table_surface.rs` reads the test file itself and confirms ≥1 FORBIDDEN_PATTERNS token appears verbatim (proving the allowlist exclusion is doing something). Without this, the architectural test could become a silent no-op if a future refactor moved or renamed the patterns slice. Matches 12.6 Plan 10's `sanity_allowlisted_callers_actually_contain_patterns` convention."
key-files:
  created:
    - crates/beava-server/tests/phase12_7_no_table_surface.rs (286 lines; symbol-grep with 20 forbidden patterns × 5 walk dirs; 3 tests — forbidden_pattern_walk + 2 sanity tests)
    - crates/beava-server/tests/phase12_7_legacy_table_handlers_killed.rs (338 lines; 6 file/symbol-absence tests + sanity-positive surviving-variant assertions)
  modified: []
  deleted: []
key-decisions:
  - "Used `#[ignore]` with descriptive ignore-reason instead of `#[should_panic]` or test-deletion to manage RED state. Choice driven by the user's prompt critical-constraints which named `#[ignore]` as the cleanest pattern: the workspace stays green during the RED-state interlude (`cargo test --workspace` passes), the failure list can still be inspected manually via `cargo test -- --ignored`, sanity tests stay in the green unconditional path, and Plan 12.7-09 (closure) removes the #[ignore] annotations as the final tests-pass moment."
  - "Allowlist explicitly contains both new test file basenames (`phase12_7_no_table_surface.rs`, `phase12_7_legacy_table_handlers_killed.rs`) even though neither file lives under any WALK_DIRS path (both are in `crates/beava-server/tests/`, not `src/`). Defensive: if a future refactor moved an architectural test under `src/`, the allowlist would still keep it from self-triggering. Matches 12.6 Plan 10's anti-self-trigger convention."
  - "Did NOT include the Plan 01 test file `phase12_7_unsupported_node_kind.rs` in the allowlist — by inspection it contains only one forbidden pattern reference (the comment `OpNode::Table*` on line 11) which is stripped by the line-comment filter before the grep runs. So no allowlist entry is needed; the test file in `tests/` is also not under any WALK_DIRS path so the question is moot in practice. Verified by confirming the test passes the comment-stripping filter against all 20 patterns."
  - "Wired both forbidden-pattern variant declaration forms (`HttpUpsert {` / `Upsert {` / `TableGet {`) AND qualified call-site forms (`WireRequest::HttpUpsert` / `Route::Upsert` / `Route::TableGet`) into the file/symbol-absence tests. Production declarations use the bare form (in the enum body); production callers use the qualified form. Both must go for the test to turn GREEN. Unit variant `Retract` (no struct body) gets a special line-trimmed match — `Retract,` or `Retract` standalone — to avoid false-positives from `Retract` appearing inside doc comments or qualified names."
  - "Forbidden pattern `fn retract(` chosen over bare `retract` to avoid false-positives on `retraction` doc-strings and `retract_test_*` names that may survive in phase-doc text. Per CONTEXT D-03 / PATTERNS.md commentary; the only production occurrence today is `fn retract(...)` in `crates/beava-core/src/temporal.rs` (deleted in Plan 12.7-04)."
  - "RED gate verified manually via `cargo test -- --ignored`: 32 (file, pattern) pairs reported across 13 production files for the symbol-grep test, and 6/6 file/symbol-absence tests fail with informative messages. The failure list IS the deletion roadmap for Plans 12.7-03/04/05/06 — the test is doing its job."
  - "Test failure messages name the target deletion plan (e.g. `must be deleted post-Plan-12.7-04/06`, `(v0 events-only per Plan 12.7-03)`) so partial-deletion diagnosis during Waves 2-3 reveals exactly which next plan to land. Architectural-test-as-progress-map per the 12.6 Plan 07/10 dual-test convention."
patterns-established:
  - "Two-file architectural-test pair (Pattern 2 + Pattern 3 from PATTERNS.md): pair a wide-net symbol-grep with narrow per-file/per-symbol assertions. The grep gives a long unstructured failure list; the per-file tests give a structured progress map that turns GREEN incrementally as each deletion plan lands. Mirrors 12.6 Plan 10 (`phase12_6_mio_only_dataplane.rs` symbol grep) + 12.6 Plan 07 (`phase12_6_legacy_axum_killed.rs` file/symbol-absence) verbatim with new pattern + file sets."
  - "RED-state architectural test management via `#[ignore]` + descriptive ignore-reason: when an architectural test is RED at end of Plan N because subsequent plans land its dependencies, mark `#[ignore]` with a string naming the unblocking plan. Closure plan (last wave) removes the #[ignore]. Keeps workspace green during the interlude; manual `cargo test -- --ignored` can verify the RED state at any point. Equivalent in spirit to TDD's RED→GREEN cycle but inverted (the deletion is the GREEN, not the impl)."
  - "Sanity-allowlist anti-no-op test as belt-and-braces: when a symbol-grep test relies on an allowlist to exclude its own source from the walk, add a third sanity test that reads the allowlisted file and confirms ≥1 FORBIDDEN_PATTERNS token appears verbatim. Without this guard, a refactor that emptied or renamed the patterns slice would silently render the architectural test a no-op."
  - "Variant-declaration vs call-site coverage in file-absence tests: when a forbidden enum variant has both a declaration site (in the enum body, bare `Variant {` form) AND call sites (qualified `Enum::Variant` form across the workspace), the file-absence test for the declaration file must check BOTH forms. Production declarations use the bare form; production callers use the qualified form. Both removals are required for the GREEN gate."
requirements-completed:
  - V0-EVENTS-ONLY-01    # CI-runtime enforcement layer (the architectural-test pair satisfies this requirement at the test level; runtime register-time enforcement was satisfied by Plan 01's pre_check_unsupported_node_kind shim)

# Metrics
duration: ~30min
completed: 2026-05-01
---

# Phase 12.7 Plan 02: Architectural Test Pair Summary

**Two-file architectural-test pair (`phase12_7_no_table_surface.rs` symbol-grep + `phase12_7_legacy_table_handlers_killed.rs` file/symbol-absence) lands in Wave 1, locking the events-only commitment at the CI level. Tests are `#[ignore]`-marked at HEAD because the table surface still exists in 13 production files (32 forbidden-pattern occurrences); each subsequent Wave 2-3 deletion plan turns specific tests GREEN until Plan 12.7-09 closure removes the `#[ignore]` annotations as the final tests-pass moment.**

## Performance

- **Duration:** ~30 min
- **Started:** 2026-05-01T11:08:15Z (immediately after Plan 12.7-01 close)
- **Completed:** 2026-05-01T11:22:39Z
- **Tasks:** 2 (Task 1.a + Task 1.b — both architectural tests in RED state per plan design)
- **Files created:** 2 (both architectural tests)
- **Files modified:** 0

## Accomplishments

- Created `crates/beava-server/tests/phase12_7_no_table_surface.rs` (286 lines): symbol-grep architectural test with 20 forbidden patterns × 5 WALK_DIRS (`crates/beava-{core,server,runtime-core,persistence,bench}/src/`). Three tests — `forbidden_pattern_walk` (#[ignore]-marked, RED) + `sanity_walk_dirs_actually_contain_rust_files` + `sanity_allowlist_actually_contains_pattern` (both pass at HEAD).
- Created `crates/beava-server/tests/phase12_7_legacy_table_handlers_killed.rs` (338 lines): six file/symbol-absence tests pinning specific deletion sites by name so partial deletions during Waves 2-3 can be diagnosed precisely. Each test's #[ignore] reason names the plan that turns it GREEN (Plan 03 for wire/router variants, Plan 04 for Rust-side files + DevAggState fields, Plan 05 for RecordType variants, Plan 06 for Python SDK + _tables.py).
- Verified RED gate manually: `cargo test -- --ignored` reports 32 (file, pattern) pairs across 13 production files for the symbol grep, and 6/6 file/symbol-absence tests fail with informative messages naming target deletion plans. The failure list IS the deletion roadmap.
- Workspace stays green: `cargo test --workspace` (all 0 failed; new tests are 9 ignored = 6 + 1 + 0 + 2 sanity-pass), `cargo clippy --workspace --all-targets --all-features -- -D warnings` (0 warnings), `cargo fmt --all --check` (0 diffs).

## Task Commits

Each task committed atomically per CLAUDE.md §Conventions § TDD Discipline. Both commits use `test(12.7-02):` prefix because no `feat:` commit is needed in this plan — the architectural tests are RED→GREEN via deletions in Waves 2-3, not via impl in this plan.

1. **Task 1.a (RED, ignored):** `30c1ce4` (test) — `test(12.7-02): add phase12_7_no_table_surface.rs — events-only architectural test (RED, ignored until Waves 2-3)`
2. **Task 1.b (RED, ignored):** `02f45a7` (test) — `test(12.7-02): add phase12_7_legacy_table_handlers_killed.rs — file/symbol absence test (RED, ignored until Waves 2-3)`

TDD inversion correct: tests assert NEGATIVE state (symbol absent / file absent); they land RED in Wave 1 because the table surface still exists; subsequent Wave 2-3 deletion plans turn them GREEN. Equivalent in spirit to TDD's RED→GREEN cycle but inverted (the deletion is the GREEN, not the impl).

## Files Created/Modified

- `crates/beava-server/tests/phase12_7_no_table_surface.rs` (NEW, 286 lines) — symbol-grep architectural test:
  - 5 WALK_DIRS (`crates/beava-{core,server,runtime-core,persistence,bench}/src/`)
  - 20 FORBIDDEN_PATTERNS (`OpNode::Table[*]`, `TemporalStore`, `MvccVersion`, `RecordType::TableUpsert/TableDelete/Retract`, `temporal_http`, `push_table`, `delete_table`, `fn retract(`, `WireRequest::Http{Upsert,Delete,Retract,TableGet}`, `Route::{Upsert,Delete,Retract,TableGet}`)
  - 2 ALLOWLIST entries (`phase12_7_no_table_surface.rs`, `phase12_7_legacy_table_handlers_killed.rs`)
  - 3 tests: `forbidden_pattern_walk` (#[ignore], RED) + `sanity_walk_dirs_actually_contain_rust_files` (passes) + `sanity_allowlist_actually_contains_pattern` (passes)

- `crates/beava-server/tests/phase12_7_legacy_table_handlers_killed.rs` (NEW, 338 lines) — file/symbol-absence test:
  - 6 #[ignore]-marked tests: `legacy_table_files_deleted` (Plan 04+06) / `temporal_record_type_variants_deleted` (Plan 05) / `wire_request_table_variants_deleted` (Plan 03) / `route_table_variants_deleted` (Plan 03) / `python_bv_table_re_export_deleted` (Plan 06) / `app_temporal_fields_deleted` (Plan 04)
  - Each test includes sanity-positive assertions for surviving v0 surface (Event = 0x01, RegistryBump = 0x02, HttpPush/HttpGet/Register, Push/Get/Register routes, "event" __all__ entry, state_tables/registry/next_event_id/query_time_ms fields)
  - Variant declaration vs call-site coverage: tests check BOTH `WireRequest::HttpUpsert` (qualified call-site) AND `HttpUpsert {` (bare declaration in enum body); same for Route variants. Unit variant `Retract` gets a line-trimmed match (`Retract,` or `Retract` standalone) to avoid false-positives from doc-comment occurrences.

## Verification

| Check | Command | Result |
|-------|---------|--------|
| Workspace tests green | `cargo test --workspace` | All test groups: `0 failed`; new tests are `2 ignored` (Task 1.a) + `6 ignored` (Task 1.b) + `2 passed` (sanity) |
| Clippy clean | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | 0 warnings |
| Fmt clean | `cargo fmt --all --check` | 0 diffs |
| Sanity tests pass | `cargo test -p beava-server --test phase12_7_no_table_surface sanity` | 2/2 GREEN |
| RED gate (symbol grep) confirmed | `cargo test -p beava-server --test phase12_7_no_table_surface forbidden_pattern_walk -- --ignored` | FAILED with 32 (file, pattern) pairs across 13 files |
| RED gate (file/symbol absence) confirmed | `cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed -- --ignored` | 6/6 FAILED with target-plan-named messages |

## Failing-Pair Inventory (from `forbidden_pattern_walk -- --ignored`)

The symbol-grep test fails with 32 (file, pattern) pairs at HEAD. Each pair is a deletion target for Plans 12.7-03..06:

```
crates/beava-core/src/temporal.rs:           TemporalStore, MvccVersion, fn retract(   [Plan 04]
crates/beava-core/src/wire.rs:               push_table, delete_table                  [Plan 04]
crates/beava-server/src/temporal_http.rs:    RecordType::TableUpsert, RecordType::Retract  [Plan 04]
crates/beava-server/src/recovery.rs:         RecordType::TableUpsert, RecordType::TableDelete, RecordType::Retract  [Plan 05]
crates/beava-server/src/lib.rs:              temporal_http                              [Plan 04]
crates/beava-server/src/registry_debug.rs:   TemporalStore                              [Plan 04]
crates/beava-server/src/apply_shard.rs:      temporal_http, WireRequest::HttpUpsert,
                                              WireRequest::HttpDelete, WireRequest::HttpRetract,
                                              WireRequest::HttpTableGet                [Plan 03+04]
crates/beava-runtime-core/src/router.rs:     Route::Upsert, Route::Delete, Route::Retract,
                                              Route::TableGet                           [Plan 03]
crates/beava-runtime-core/src/http_listener.rs: WireRequest::Http{Upsert,Delete,Retract,TableGet},
                                                Route::{Upsert,Delete,Retract,TableGet}  [Plan 03]
crates/beava-persistence/src/record.rs:      RecordType::TableUpsert, RecordType::TableDelete,
                                              RecordType::Retract                       [Plan 05]
```

Total: **13 production files**, **32 occurrences**, mapping to **4 deletion plans** (03, 04, 05, 06). Plan 09 (closure) removes the `#[ignore]` annotations once all 32 pairs are gone.

## Decisions Made

- **`#[ignore]` over `#[should_panic]`** for RED-state management: workspace stays green; failure list inspectable via `cargo test -- --ignored`; sanity tests run unconditionally; Plan 12.7-09 closure removes annotations as final tests-pass moment. Per the user's prompt critical-constraints §5 directive ("`#[ignore]` with a comment 'Wave 1 RED state; turns GREEN after Plan 06'").
- **Allowlist contains both new test file basenames** even though neither lives under any WALK_DIRS path. Defensive against future refactors that might move architectural tests under `src/`. Matches 12.6 Plan 10's anti-self-trigger convention.
- **Did NOT add `phase12_7_unsupported_node_kind.rs` (Plan 01's test) to the allowlist**: inspection confirms it contains only one forbidden-pattern reference (the comment `OpNode::Table*` on line 11) which the line-comment filter strips before the grep runs. The test file in `tests/` is also not under any WALK_DIRS path so the question is moot in practice. Verified by confirming the test passes comment-stripping against all 20 patterns.
- **Test failure messages name the target deletion plan** (e.g. `must be deleted post-Plan-12.7-04/06`, `(v0 events-only per Plan 12.7-03)`) so partial-deletion diagnosis during Waves 2-3 reveals exactly which next plan to land. Architectural-test-as-progress-map.
- **`fn retract(` chosen over bare `retract`** to avoid false-positives on `retraction` doc-strings and `retract_test_*` names. Per CONTEXT D-03 / PATTERNS.md commentary.
- **Wired both declaration and call-site forms** for variant tests: `WireRequest::HttpUpsert` (qualified call-site) AND `HttpUpsert {` (bare declaration). Both must go for GREEN. Unit variant `Retract` gets a special line-trimmed match.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Clippy `doc_lazy_continuation` warnings on multi-line doc comments**
- **Found during:** Task 1.b post-write `cargo clippy` verification
- **Issue:** Two multi-line doc-comment paragraphs (`//! and 10:` continuation in module docstring; `///` continuation in test 6 doc) tripped clippy's `doc_lazy_continuation` lint because the first line had a list-like marker (the `+ 10:` reading as list item; the bullet-listed `temporal_stores` / `event_id_index` field paragraph followed by a continuation paragraph). Three errors total, all cosmetic but blocking `-D warnings`.
- **Fix:** Replaced `+ 10:` with `and 10:` in the module docstring (no semantic change, removes the `+` list-marker false-positive). Added blank doc-comment line between the bulleted-fields paragraph and the next paragraph in test 6's docstring (clippy's standard fix per its help text).
- **Files modified:** `crates/beava-server/tests/phase12_7_legacy_table_handlers_killed.rs`
- **Verification:** `cargo clippy --workspace --all-targets --all-features -- -D warnings` returns 0 warnings.
- **Committed in:** `02f45a7` (folded into Task 1.b GREEN commit before push)

**2. [Rule 1 - Bug] Fmt diff on long .expect() string literal**
- **Found during:** Task 1.b post-write `cargo fmt --all --check`
- **Issue:** Two `let src = std::fs::read_to_string(&path).expect("very long error message")` lines exceeded the rustfmt 100-char line limit. Cosmetic but blocking `cargo fmt --all --check`.
- **Fix:** Wrapped each as `let src =\n    std::fs::read_to_string(&path).expect("...")` per fmt's suggested layout (one in `route_table_variants_deleted`, one in `wire_request_table_variants_deleted`).
- **Files modified:** `crates/beava-server/tests/phase12_7_legacy_table_handlers_killed.rs`
- **Verification:** `cargo fmt --all --check` returns 0 diffs.
- **Committed in:** `02f45a7` (folded into Task 1.b GREEN commit before push)

**3. [Rule 1 - Bug] Fmt diff on inline ALLOWLIST literal**
- **Found during:** Task 1.a post-write `cargo fmt -p beava-server -- --check`
- **Issue:** `const ALLOWLIST: &[&str] = &["phase12_7_no_table_surface.rs", "phase12_7_legacy_table_handlers_killed.rs"];` declared on one line exceeded the rustfmt 100-char line limit.
- **Fix:** Split across multiple lines per fmt's suggested layout (one entry per line, indented).
- **Files modified:** `crates/beava-server/tests/phase12_7_no_table_surface.rs`
- **Verification:** `cargo fmt --all --check` returns 0 diffs.
- **Committed in:** `30c1ce4` (folded into Task 1.a RED commit before push)

---

**Total deviations:** 3 auto-fixed (Rule 1 - cosmetic clippy/fmt blockers).
**Impact on plan:** All three are CI-gate cosmetic fixes (clippy + fmt) folded into the same commit as the test creation; no scope creep, no semantic changes, no impact on the test bodies' assertions or coverage.

## Issues Encountered

- One initial `cargo test --workspace` run reported a single `FAILED` group, which on re-run with `--no-fail-fast` showed all 0 failures. Likely a transient build-graph race in the test harness; subsequent runs all show clean. No persistent failure.

## TDD Gate Compliance

- This plan inverts the TDD polarity: the architectural tests assert NEGATIVE state. There is NO `feat:` commit in this plan; both commits are `test(12.7-02):` prefixed because the GREEN gate is satisfied by deletions in Waves 2-3, not by impl in this plan.
- Both commits land in correct sequence (Task 1.a `30c1ce4` then Task 1.b `02f45a7`); the architectural-test invariant is now in place at the CI level. Plans 12.7-03..06 turn the tests GREEN incrementally; Plan 12.7-09 (closure) removes the `#[ignore]` annotations as the final tests-pass moment.
- This is the natural shape for negative architectural tests per CONTEXT D-03 ("write the architectural commitment as test, deletions satisfy it"); equivalent to the 12.6 Plan 10 + 12.6 Plan 07 architectural-test pair convention.

## Next Plan Readiness

- **Plan 12.7-03 (Wave 2 — wire/router variant deletion):** unblocked. Will turn `wire_request_table_variants_deleted` + `route_table_variants_deleted` + the `Route::*` / `WireRequest::Http*` patterns from `forbidden_pattern_walk` GREEN.
- **Plan 12.7-04 (Wave 2 — temporal_http.rs + temporal.rs + DevAggState fields):** unblocked. Will turn `legacy_table_files_deleted` (partial, Rust side) + `app_temporal_fields_deleted` + the `TemporalStore` / `MvccVersion` / `fn retract(` / `temporal_http` / `push_table` / `delete_table` patterns from `forbidden_pattern_walk` GREEN.
- **Plan 12.7-05 (Wave 2 — RecordType variants):** unblocked. Will turn `temporal_record_type_variants_deleted` + the `RecordType::Table[*]` / `RecordType::Retract` patterns from `forbidden_pattern_walk` GREEN.
- **Plan 12.7-06 (Wave 3 — Python SDK + _tables.py):** unblocked. Will turn `python_bv_table_re_export_deleted` + complete `legacy_table_files_deleted` GREEN.
- **Plan 12.7-09 (Wave 4 closure):** removes the `#[ignore]` annotations from all 7 RED-state tests as the final tests-pass moment, locking the events-only invariant into CI on every PR.

No blockers. No dependencies on subsequent plans needing to revisit this one.

## Self-Check: PASSED

- [x] Both new test files exist on disk:
  - `crates/beava-server/tests/phase12_7_no_table_surface.rs` (286 lines, ≥200 ✅)
  - `crates/beava-server/tests/phase12_7_legacy_table_handlers_killed.rs` (338 lines, ≥100 ✅)
- [x] Both commits exist in git log:
  - `30c1ce4 test(12.7-02): add phase12_7_no_table_surface.rs ...`
  - `02f45a7 test(12.7-02): add phase12_7_legacy_table_handlers_killed.rs ...`
- [x] Sanity tests in `phase12_7_no_table_surface.rs` PASS at HEAD: 2/2 GREEN
- [x] Main assertions FAIL when run with `--ignored` (RED gate held — by design):
  - `forbidden_pattern_walk`: FAILED with 32 (file, pattern) pairs
  - 6/6 file/symbol-absence tests: FAILED with target-plan-named messages
- [x] Workspace `cargo test --workspace` GREEN — all test groups `0 failed`
- [x] `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean (0 warnings)
- [x] `cargo fmt --all --check` clean (0 diffs)
- [x] 3 #[test] functions in `phase12_7_no_table_surface.rs`; 6 #[test] functions in `phase12_7_legacy_table_handlers_killed.rs`
- [x] FORBIDDEN_PATTERNS slot count: 20 distinct patterns covering all CONTEXT D-03 enumerated symbols
- [x] WALK_DIRS count: 5 (covers `beava-core/server/runtime-core/persistence/bench`)
- [x] Allowlist contains both architectural-test files (defensive, anti-self-trigger)
- [x] Each `#[ignore]` reason names the unblocking plan (`Wave 1 RED state; turns GREEN after Plan 12.7-XX`)
- [x] Sanity-positive surviving-variant assertions present: `Event = 0x01`, `RegistryBump = 0x02`, `Register {`, `HttpGet {`, `Ping`, `Push`, `Get`, `Register`, `"event",`, `state_tables`, `registry`, `next_event_id`, `query_time_ms`
- [x] Plan-level TDD shape: this plan has 2 `test(12.7-02):` commits and zero `feat:` commits because the GREEN gate is satisfied by Waves 2-3 deletions (architectural-test inversion per CONTEXT D-03)

---

*Phase: 12.7-table-strip*
*Plan: 02*
*Completed: 2026-05-01*
*Predecessor commit: `9bd6fcc` (Plan 12.7-01 close)*
*Architectural-test pair commits: `30c1ce4` (Task 1.a) → `02f45a7` (Task 1.b)*
*Next: Plan 12.7-03 (Wave 2 — wire/router variant deletion) per CONTEXT.md wave order*
