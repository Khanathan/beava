---
phase: 12.7-table-strip
plan: 08
type: execute
wave: 3
depends_on: []                              # Pure doc edits in another phase's directory; no code or REQUIREMENTS dep
files_modified:
  - .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-SUMMARY.md
  - .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-VERIFICATION.md
  - .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-CONTEXT.md
autonomous: true
requirements: []                             # No REQ-IDs — pure historical-doc archival
addresses_decisions:
  - "D-04 second half — Phase 11.5 retroactive-descope banner stamps on all 3 named files. Pattern matches Plan 12.6-09's 12.5-CONTEXT.md banner-stamp shape verbatim."
  - "Banner text per CONTEXT D-04 verbatim: 'RETROACTIVELY-DESCOPED 2026-05-01 by Phase 12.7 per project_v0_events_only_scope. Original landed work preserved in git history; surface stripped in Phase 12.7. Tables/retraction return in v0.1+ alongside joins/aggregation if/when justified by demand.'"
  - "Phase 11.5 directory STAYS in place per `feedback_logistics_autonomy` (no rename to `_archived-...`). Banner-stamp matches Phase 12.5 archival pattern from 12.6 Plan 09; preserves traceability without churning paths."
must_haves:
  truths:
    - "11.5-SUMMARY.md has the retro-descope banner near the top of the file (immediately after YAML frontmatter close)"
    - "11.5-VERIFICATION.md has the same banner"
    - "11.5-CONTEXT.md has the same banner"
    - "Banner text is verbatim across all 3 files"
    - "Banner cites `project_v0_events_only_scope` and Phase 12.7"
    - "Original 11.5 file content is PRESERVED — banner is additive at the top, no content edits below"
  artifacts:
    - path: ".planning/phases/11.5-temporal-tables-retraction-primitive/11.5-SUMMARY.md"
      provides: "11.5-SUMMARY with retroactive-descope banner header"
      contains: "RETROACTIVELY-DESCOPED 2026-05-01"
    - path: ".planning/phases/11.5-temporal-tables-retraction-primitive/11.5-VERIFICATION.md"
      provides: "11.5-VERIFICATION with retroactive-descope banner header"
      contains: "RETROACTIVELY-DESCOPED 2026-05-01"
    - path: ".planning/phases/11.5-temporal-tables-retraction-primitive/11.5-CONTEXT.md"
      provides: "11.5-CONTEXT with retroactive-descope banner header"
      contains: "RETROACTIVELY-DESCOPED 2026-05-01"
  key_links:
    - from: "Phase 11.5 directory"
      to: "Phase 12.7 directory"
      via: "Banner cites .planning/phases/12.7-table-strip/12.7-CONTEXT.md as the architectural-decision source"
      pattern: "12.7-table-strip|RETROACTIVELY-DESCOPED 2026-05-01"
---

<objective>
Stamp the Phase 11.5 retroactive-descope banner on all 3 named files in `.planning/phases/11.5-temporal-tables-retraction-primitive/` per CONTEXT D-04 second half. The banner is mandatory for the audit trail — without it, future readers landing on `11.5-SUMMARY.md` would think temporal tables shipped successfully (they did, then got walked back; the banner makes the walkback explicit).

Phase 11.5 directory STAYS in place per `feedback_logistics_autonomy` (no rename to `_archived-...`). The banner-stamp pattern matches Phase 12.5 archival from 12.6 Plan 09.

After this plan lands:
- `11.5-SUMMARY.md` has the retro-descope banner near the top (immediately after YAML frontmatter close)
- `11.5-VERIFICATION.md` same
- `11.5-CONTEXT.md` same
- Original file contents PRESERVED — banner is additive only

**Wave 3 placement:** independent of Plans 06 + 07 (different files entirely). All 3 Wave 3 plans run parallel.

Output:
- 3 file-header banner additions
- Doc-only diff
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.7-table-strip/12.7-CONTEXT.md
@.planning/phases/12.7-table-strip/12.7-PATTERNS.md
@.planning/phases/12.5-push-and-get/12.5-CONTEXT.md
@.planning/phases/11.5-temporal-tables-retraction-primitive/11.5-SUMMARY.md
@.planning/phases/11.5-temporal-tables-retraction-primitive/11.5-VERIFICATION.md
@.planning/phases/11.5-temporal-tables-retraction-primitive/11.5-CONTEXT.md

<interfaces>
<!-- Verbatim banner text per CONTEXT D-04 + PATTERNS.md §Pattern 7 -->

Banner block to insert immediately after YAML frontmatter close (the line containing only `---`):

```
> **WARNING — RETROACTIVELY-DESCOPED 2026-05-01 by Phase 12.7** per `project_v0_events_only_scope`.
> Original landed work preserved in git history; surface stripped in Phase 12.7.
> Tables/retraction return in v0.1+ alongside joins/aggregation if/when justified by demand.
>
> See `.planning/phases/12.7-table-strip/12.7-CONTEXT.md` for the architectural decision.

---
```

This matches the Phase 12.5 banner from 12.6 Plan 09 (see PATTERNS.md §Pattern 7 lines 538-552). The trailing `---` is part of the banner block; it visually separates the banner from the original file content below.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (RED): Confirm 3 files lack retro-descope banner</name>
  <read_first>
    - .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-SUMMARY.md
    - .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-VERIFICATION.md
    - .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-CONTEXT.md
  </read_first>
  <files>(no new file — RED gate is shell precondition assertion)</files>
  <action>
Confirm the 3 files DO NOT yet have the retro-descope banner:

```bash
grep -L 'RETROACTIVELY-DESCOPED 2026-05-01 by Phase 12.7' \
  .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-SUMMARY.md \
  .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-VERIFICATION.md \
  .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-CONTEXT.md
```

EXPECTED: returns all 3 file paths (none has the banner yet).

NO commit lands for this task — Task 1.b's commit captures the diff. This is a precondition-confirmation step.
  </action>
  <verify>
    <automated>grep -L 'RETROACTIVELY-DESCOPED 2026-05-01 by Phase 12.7' .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-SUMMARY.md .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-VERIFICATION.md .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-CONTEXT.md | wc -l</automated>
    EXPECTED: returns 3 (all 3 files lack the banner)
  </verify>
  <acceptance_criteria>
    - All 3 files lack the banner: `[ $(grep -L 'RETROACTIVELY-DESCOPED 2026-05-01 by Phase 12.7' .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-SUMMARY.md .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-VERIFICATION.md .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-CONTEXT.md | wc -l) -eq 3 ]`
  </acceptance_criteria>
  <done>RED precondition: 3 files lack the banner. Task 1.b adds it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (GREEN): Add retro-descope banner to all 3 Phase 11.5 files</name>
  <read_first>
    - .planning/phases/12.7-table-strip/12.7-CONTEXT.md (D-04 verbatim banner)
    - .planning/phases/12.7-table-strip/12.7-PATTERNS.md (§Pattern 7 — banner shape from 12.5-CONTEXT.md analog)
    - .planning/phases/12.5-push-and-get/12.5-CONTEXT.md (banner placement reference — line 1-9)
    - All 3 target 11.5 files (full content — banner is additive ONLY; no content edits below)
  </read_first>
  <files>
    - .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-SUMMARY.md
    - .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-VERIFICATION.md
    - .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-CONTEXT.md
  </files>
  <action>
For each of the 3 files, insert the banner block immediately AFTER the YAML frontmatter closing `---` line (typical placement at line 7-15 of each file). The banner block:

```
> **WARNING — RETROACTIVELY-DESCOPED 2026-05-01 by Phase 12.7** per `project_v0_events_only_scope`.
> Original landed work preserved in git history; surface stripped in Phase 12.7.
> Tables/retraction return in v0.1+ alongside joins/aggregation if/when justified by demand.
>
> See `.planning/phases/12.7-table-strip/12.7-CONTEXT.md` for the architectural decision.

---

```

The trailing `---` separator + blank line above is part of the banner block — visually separates banner from the original content.

If any of the 3 files does NOT start with YAML frontmatter (e.g., `11.5-SUMMARY.md` may have a different header structure), insert the banner immediately after the H1 title line. Use `head -20 <file>` to inspect each file's top before placing the banner.

DO NOT edit any content below the banner placement. Banner is additive ONLY.

After insertion, verify each file:

```bash
for f in 11.5-SUMMARY.md 11.5-VERIFICATION.md 11.5-CONTEXT.md; do
  full=".planning/phases/11.5-temporal-tables-retraction-primitive/$f"
  grep -q 'RETROACTIVELY-DESCOPED 2026-05-01 by Phase 12.7' "$full" || echo "MISSING: $f"
  grep -q 'project_v0_events_only_scope' "$full" || echo "MISSING memory ref: $f"
done
```

All 3 files MUST contain the banner.

Commit message:
```
docs(12.7-08): retro-descope banner on 11.5 SUMMARY + VERIFICATION + CONTEXT

Per CONTEXT D-04 second half — Phase 11.5 (temporal tables + retraction
primitive) retroactively descoped 2026-05-01 by Phase 12.7 per
project_v0_events_only_scope. Banner stamped on all 3 named files;
original file content preserved (banner is additive only).

Phase 11.5 directory stays in place per feedback_logistics_autonomy
(no rename to _archived-...). Banner-stamp matches Phase 12.5
archival pattern from 12.6 Plan 09.
```
  </action>
  <verify>
    <automated>grep -l 'RETROACTIVELY-DESCOPED 2026-05-01 by Phase 12.7' .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-SUMMARY.md .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-VERIFICATION.md .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-CONTEXT.md | wc -l</automated>
    EXPECTED: returns 3 (all 3 files now have the banner)
  </verify>
  <acceptance_criteria>
    - All 3 files have the banner: `[ $(grep -l 'RETROACTIVELY-DESCOPED 2026-05-01 by Phase 12.7' .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-SUMMARY.md .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-VERIFICATION.md .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-CONTEXT.md | wc -l) -eq 3 ]`
    - All 3 cite project_v0_events_only_scope: `[ $(grep -l 'project_v0_events_only_scope' .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-SUMMARY.md .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-VERIFICATION.md .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-CONTEXT.md | wc -l) -eq 3 ]`
    - All 3 cite Phase 12.7 dir path: `[ $(grep -l '\.planning/phases/12\.7-table-strip' .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-SUMMARY.md .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-VERIFICATION.md .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-CONTEXT.md | wc -l) -eq 3 ]`
    - Banner appears near top of each file (within first 30 lines): `for f in .planning/phases/11.5-temporal-tables-retraction-primitive/11.5-{SUMMARY,VERIFICATION,CONTEXT}.md; do head -30 "$f" | grep -q 'RETROACTIVELY-DESCOPED' || exit 1; done`
    - Original content preserved (line count INCREASED, not changed otherwise — sanity check). Compare line counts before/after; should be ≥ original + ~6 lines per file.
    - Commit: `git log -1 --format=%s | grep -E '^docs\(12\.7-08\):'`
  </acceptance_criteria>
  <done>GREEN gate: 3 files have the banner; original content preserved. Commit `docs(12.7-08):`.</done>
</task>

</tasks>

<threat_model>
Phase 12.7-08 is doc-only. No threat surface change.
</threat_model>

<verification>
- 3 files banner-stamped
- Original content preserved (banner additive only)
- 1 commit: `docs(12.7-08):` (doc-only — exempt from red-then-green per CLAUDE.md TDD §Note 4)
</verification>

<success_criteria>
- 3 Phase 11.5 files contain the verbatim banner block
- Phase 11.5 directory NOT renamed (stays at canonical path)
- Phase 12.7-08 commit lands as `docs(12.7-08):`
</success_criteria>

<output>
After completion, create `.planning/phases/12.7-table-strip/12.7-08-SUMMARY.md`. Self-Check:
- [x] 3 11.5 files banner-stamped
- [x] Original content preserved
- [x] Phase 11.5 dir stays in place (no rename)
- [x] Doc-only commit `docs(12.7-08):`
</output>
