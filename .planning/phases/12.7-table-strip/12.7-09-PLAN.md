---
phase: 12.7-table-strip
plan: 09
type: execute
wave: 4
depends_on: [03, 04, 05, 06]                # Need all code-deletion waves complete; perf measurement on the post-strip workspace
files_modified:
  - crates/beava-server/benches/phase12_6_post_axum_kill_apply.rs    # RE-RUN; potentially RENAME or duplicate to phase12_7_*
  - .planning/perf-baselines.md                                       # APPEND Phase 12.7 microbench section
  - .planning/throughput-baselines.md                                 # APPEND Phase 12.7 throughput row block
autonomous: true
requirements:
  - PERF-04                # bench harness exposes reproducible scenario per operator family
  - THROUGHPUT-HARNESS-01  # reusable harness drives live server
  - THROUGHPUT-HARNESS-02  # ledger format + hw-class tagging
  - THROUGHPUT-HARNESS-03  # per-phase regression thresholds
  - THROUGHPUT-PIPELINES-01 # 3 pipeline configs ship
  - THROUGHPUT-WORKLOAD-01  # 60s wall-time time-bounded workload
addresses_decisions:
  - "CLAUDE.md §Performance Discipline (locked Phase 6+) — every phase from 6 onward ships a microbench. Phase 12.7's microbench reruns the 12.6 cells against the post-strip workspace; 10% warn / 25% block thresholds vs 12.6 baseline."
  - "CLAUDE.md §End-to-end throughput regression contract (locked Phase 8+) — every phase from 8 onward MUST include a throughput-run task that re-runs `crates/beava-bench` against the small/medium/large pipelines (plus fraud-team variant) and appends a row per (pipeline-shape, transport) tuple to `.planning/throughput-baselines.md`. Same 10% warn / 25% block thresholds. Plan-checker rejects Phase 8+ plans without this."
  - "Why expect no regression: Phase 12.7 is pure deletion. The apply hot path (parse + descriptor lookup + WAL append + agg-stage) is unchanged — none of the deleted code (temporal_http, TemporalStore, table dispatch arms) was on the events-push hot path."
must_haves:
  truths:
    - "perf-baselines.md has a NEW `### Phase 12.7 — Post-table-strip apply microbench` section"
    - "throughput-baselines.md has a NEW `## Phase 12.7` section with 8 cells (small/medium/large/fraud-team × http+tcp)"
    - "Microbench cells captured for at least 3 cells: simple_counter, sketch_heavy, windowed_60s_sum"
    - "Microbench regression-gate: each cell within ±10% of 12.6 baseline (PASS); within ±25% required for non-BLOCK (BLOCK if exceeded)"
    - "Throughput regression-gate cell `small/tcp` measured against post-12.6 baseline (700,571 EPS); ≥ 90% for PASS (≥ 630,514 EPS); ≥ 75% for non-BLOCK"
    - "If any cell exceeds 10% slower → WARN logged in this plan's SUMMARY"
    - "If any cell exceeds 25% slower → BLOCK logged + plan fails verification"
  artifacts:
    - path: ".planning/perf-baselines.md"
      provides: "Phase 12.7 microbench section appended"
      contains: "Phase 12.7"
    - path: ".planning/throughput-baselines.md"
      provides: "Phase 12.7 throughput rebaseline section appended"
      contains: "Phase 12.7"
  key_links:
    - from: "crates/beava-server/benches/phase12_6_post_axum_kill_apply.rs (or 12.7 rename)"
      to: ".planning/perf-baselines.md::Phase 12.7"
      via: "Criterion bench output → markdown table append"
      pattern: "Phase 12.7|simple_counter|sketch_heavy|windowed_60s_sum"
    - from: "crates/beava-bench (small/medium/large/fraud-team pipelines)"
      to: ".planning/throughput-baselines.md::Phase 12.7"
      via: "60s wall-time runs over HTTP + TCP transports → 8-cell markdown table append"
      pattern: "Phase 12.7|small/tcp|fraud-team"
---

<objective>
Phase 8+ contract: every phase ships a microbench (regression tripwire) and a throughput-run (end-to-end EPS verification) per CLAUDE.md §Performance Discipline + §End-to-end throughput regression contract. Phase 12.7's gates compare against the post-12.6 baselines:

**Microbench baselines (Plan 12.6-11):**
| Cell | 12.6 baseline (Apple-M4) |
|---|---|
| simple_counter/100_events | 81.034 µs (810 ns/event) |
| sketch_heavy/100_events | 88.400 µs (884 ns/event) |
| windowed_60s_sum/100_events | 88.294 µs (883 ns/event) |

**Throughput baseline (Plan 12.6-12):**
| Cell | 12.6 baseline (Apple-M4) |
|---|---|
| small/tcp (regression-gate cell) | 700,571 EPS |
| fraud-team/tcp (primary tuning) | 102,788 EPS |
| (5 other cells: small/http, medium/{http,tcp}, large/{http,tcp}, fraud-team/http) |

**Verdict thresholds (CLAUDE.md):**
- 10% slower than baseline → WARN logged in SUMMARY
- 25% slower → BLOCK (plan fails; must fix or explicit override)

**Why expect no regression:** Phase 12.7 is pure deletion; the apply hot path (parse + descriptor lookup + WAL append + agg-stage) is unchanged. None of the deleted code (temporal_http, TemporalStore, table dispatch arms in apply_shard.rs lines 400-459) was on the events-push hot path.

**Wave 4 placement:** depends on Plans 03 + 04 + 05 + 06 closing (all code deletions complete; this plan measures the post-strip baseline). Independent of Plan 07 + 08 (REQUIREMENTS sweep + 11.5 banner — doc-only; no perf impact). Runs parallel with Plan 10 (closure) only if Plan 10 doesn't need Plan 09's measurements; in practice Plan 09 → Plan 10 sequential because Plan 10's SUMMARY references the perf verdict.

Output:
- 3 microbench cells captured + appended to perf-baselines.md
- 8 throughput cells captured + appended to throughput-baselines.md
- Verdict logged in this plan's SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.7-table-strip/12.7-CONTEXT.md
@.planning/phases/12.7-table-strip/12.7-PATTERNS.md
@.planning/perf-baselines.md
@.planning/throughput-baselines.md
@.planning/phases/12.6-v0-surface-reduction/12.6-11-SUMMARY.md
@.planning/phases/12.6-v0-surface-reduction/12.6-12-SUMMARY.md
@crates/beava-server/benches/phase12_6_post_axum_kill_apply.rs

<interfaces>
<!-- Templates from PATTERNS.md §Pattern 10 lines 661-693 -->

Microbench append template (perf-baselines.md):
```markdown
### Phase 12.7 — Post-table-strip apply microbench (Apple-M4)

**Captured:** 2026-05-XX (Phase 12.7 Plan 09).
**hw-class:** `Apple-M4 / Darwin-24.3.0 / 10 cores`.
**HEAD at measurement:** `<sha>` (post Plans 12.7-01..06 — entire table surface stripped, FORMAT_VERSION reset 2→1).
**Methodology:** Same harness as Phase 12.6 — `crates/beava-server/benches/phase12_6_post_axum_kill_apply.rs` (re-run on post-strip workspace); 100 samples, 3s warm-up, 5s collection.

| Cell | Median (per 100-event batch) | ns/event | Range | Outliers | Comparison baseline | Verdict |
|---|---|---|---|---|---|---|
| `phase12_6/simple_counter/100_events` | <result> | <ns> | <range> | <%> | 81.034 µs (12.6 Plan 11) | <PASS / WARN / BLOCK> |
| `phase12_6/sketch_heavy/100_events` | <result> | <ns> | <range> | <%> | 88.400 µs (12.6 Plan 11) | <verdict> |
| `phase12_6/windowed_60s_sum/100_events` | <result> | <ns> | <range> | <%> | 88.294 µs (12.6 Plan 11) | <verdict> |

**Verdict thresholds (CLAUDE.md §Performance Discipline):**
- 10% slower than this baseline → WARN
- 25% slower than this baseline → BLOCK

**Why expect no regression:** Phase 12.7 is pure deletion (table dispatch arms + temporal modules); the apply hot path is unchanged.
```

Throughput append template (throughput-baselines.md): copy the 8-cell matrix from §"Phase 12.6" lines 1399-1410 verbatim, replace cell numbers with new measurements, add a `## Phase 12.7` heading. Regression-gate cell is `small/tcp` per Plan 12.6-12 convention.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (RED): Confirm post-strip workspace clean before measurement</name>
  <read_first>
    - .planning/perf-baselines.md (current state — 12.6 section is the most recent)
    - .planning/throughput-baselines.md (current state — Phase 12.6 section)
  </read_first>
  <files>(no new file — RED gate is shell precondition)</files>
  <action>
Confirm preconditions for measurement:
- Workspace `cargo test --workspace` clean (0 failures): `cargo test --workspace 2>&1 | grep -q '0 failed'`
- Workspace clippy clean: `cargo clippy --workspace --all-targets --all-features -- -D warnings 2>&1 | grep -cE '^error\['` returns 0
- Architectural test pair fully GREEN: `cargo test -p beava-server --test phase12_7_no_table_surface 2>&1 | grep -q 'test result: ok' && cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed 2>&1 | grep -q 'test result: ok'`
- perf-baselines.md does NOT yet have Phase 12.7 section: `! grep -q '^### Phase 12.7' .planning/perf-baselines.md`
- throughput-baselines.md does NOT yet have Phase 12.7 section: `! grep -q '^## Phase 12.7' .planning/throughput-baselines.md`

If any precondition fails, do NOT proceed — fix the upstream plan first.

NO commit lands for this task.
  </action>
  <verify>
    <automated>cargo test --workspace 2>&1 | grep -E '0 failed|FAILED' | head -1</automated>
    EXPECTED: `0 failed` (workspace clean post-Wave 3)
  </verify>
  <acceptance_criteria>
    - Workspace cargo test green: `cargo test --workspace 2>&1 | grep -q '0 failed'`
    - Workspace clippy clean: `cargo clippy --workspace --all-targets --all-features -- -D warnings 2>&1 | grep -cE '^error\['` returns 0
    - Architectural test pair fully GREEN: `cargo test -p beava-server --test phase12_7_no_table_surface 2>&1 | grep -q 'test result: ok'` AND `cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed 2>&1 | grep -q 'test result: ok'`
    - perf-baselines.md lacks Phase 12.7 section: `! grep -q '^### Phase 12.7' .planning/perf-baselines.md`
    - throughput-baselines.md lacks Phase 12.7 section: `! grep -q '^## Phase 12.7' .planning/throughput-baselines.md`
  </acceptance_criteria>
  <done>RED precondition: workspace clean, architectural tests GREEN, baseline files don't yet have Phase 12.7 sections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (GREEN): Run microbench + throughput rebaseline + append to ledger files</name>
  <read_first>
    - crates/beava-server/benches/phase12_6_post_axum_kill_apply.rs (the bench; reused as-is)
    - crates/beava-bench/src/main.rs or similar (the throughput harness; verify path during planning)
    - .planning/phases/12.6-v0-surface-reduction/12.6-11-SUMMARY.md (microbench Plan 11 reference)
    - .planning/phases/12.6-v0-surface-reduction/12.6-12-SUMMARY.md (throughput Plan 12 reference)
    - .planning/phases/12.7-table-strip/12.7-PATTERNS.md (§Pattern 10)
  </read_first>
  <files>
    - .planning/perf-baselines.md
    - .planning/throughput-baselines.md
  </files>
  <action>
**Step 1 — Run microbench:**

```bash
cargo bench -p beava-server --bench phase12_6_post_axum_kill_apply 2>&1 | tee /tmp/12.7-microbench.log
```

The bench produces 3 cells. Capture the median for each. Note the HEAD sha at measurement: `git rev-parse HEAD`.

**Step 2 — Append microbench section to perf-baselines.md:**

Append at the END of perf-baselines.md (after the existing `### Phase 12.6 — Post-axum-kill apply microbench (Apple-M4)` section):

```markdown
### Phase 12.7 — Post-table-strip apply microbench (Apple-M4)

**Captured:** 2026-05-01 (Phase 12.7 Plan 09).
**hw-class:** `Apple-M4 / Darwin-24.3.0 / 10 cores`.
**HEAD at measurement:** `<sha-from-git-rev-parse>` (post Plans 12.7-01..06 — entire table surface stripped, FORMAT_VERSION reset 2→1).
**Methodology:** Same harness as Phase 12.6 — `crates/beava-server/benches/phase12_6_post_axum_kill_apply.rs` (re-run on post-strip workspace); 100 samples, 3s warm-up, 5s collection.

| Cell | Median (per 100-event batch) | ns/event | Range | Outliers | Comparison baseline (12.6 Plan 11) | Δ% | Verdict |
|---|---|---|---|---|---|---|---|
| `phase12_6/simple_counter/100_events` | <X µs> | <Y ns> | <range> | <%> | 81.034 µs / 810 ns | <±%>  | <PASS / WARN / BLOCK> |
| `phase12_6/sketch_heavy/100_events` | <X µs> | <Y ns> | <range> | <%> | 88.400 µs / 884 ns | <±%> | <verdict> |
| `phase12_6/windowed_60s_sum/100_events` | <X µs> | <Y ns> | <range> | <%> | 88.294 µs / 883 ns | <±%> | <verdict> |

**Verdict thresholds:** 10% slower → WARN; 25% slower → BLOCK.

**Why expect no regression:** Phase 12.7 is pure deletion (table dispatch arms + temporal modules + RecordType variants); the apply hot path (parse + descriptor lookup + WAL append + agg-stage) is unchanged. None of the deleted code paths sit on the events-push hot path.
```

Replace each `<X>` placeholder with the actual measurement.

**Step 3 — Run throughput rebaseline:**

```bash
# Confirm beava-bench location:
ls crates/beava-bench/

# Run the 8 cells (small/medium/large/fraud-team × http+tcp) — replicate Plan 12.6-12 invocation
# Reference 12.6-12-SUMMARY.md for the exact CLI invocations
```

For each cell, run a 60s wall-time time-bounded workload. Record EPS, P50/P95/P99 push latency, P99 batch-get latency, RSS at peak.

**Step 4 — Append throughput section to throughput-baselines.md:**

Append at the END (after existing Phase 12.6 section):

```markdown
## Phase 12.7 — Post-table-strip throughput rebaseline (Apple-M4)

**Captured:** 2026-05-01 (Phase 12.7 Plan 09).
**hw-class:** `Apple-M4 / Darwin-24.3.0 / 10 cores`.
**HEAD at measurement:** `<sha>`.
**Methodology:** Same harness as Phase 12.6 Plan 12 (`crates/beava-bench/`); 60s wall-time per cell; small/medium/large pipelines + fraud-team zipfian variant; HTTP + framed-TCP transports.

| Cell | Sustained EPS | P50 push (µs) | P99 push (µs) | P99 get (ms) | RSS peak (MB) | Comparison baseline (12.6 Plan 12) | Δ% | Verdict |
|---|---|---|---|---|---|---|---|---|
| small/http | <X> | <Y> | <Y> | <Y> | <Y> | <12.6 baseline> | <±%> | <PASS/WARN/BLOCK> |
| **small/tcp (regression-gate)** | <X> | <Y> | <Y> | <Y> | <Y> | **700,571** | <±%> | <PASS/WARN/BLOCK> |
| medium/http | <X> | ... | | | | | | |
| medium/tcp | <X> | ... | | | | | | |
| large/http | <X> | ... | | | | | | |
| large/tcp | <X> | ... | | | | | | |
| fraud-team/http | <X> | ... | | | | <12.6 baseline> | <±%> | <verdict> |
| fraud-team/tcp (primary tuning) | <X> | ... | | | | **102,788** | <±%> | <verdict> |

**Regression-gate cell:** `small/tcp` per Plan 12.6-12 convention. Δ% measured against 700,571 EPS; ≥ 90% for PASS; ≥ 75% for non-BLOCK.

**Why expect no regression:** Same as microbench — pure deletion; hot path unchanged.
```

Replace each `<X>` with the actual measurement.

**Step 5 — Verdict logging:**

Compute Δ% for each cell. For any cell with Δ% > 10% slower:
- Log WARN in this plan's SUMMARY: "Cell <name> regressed N% (within WARN band, requires investigation before Phase 13)"

For any cell with Δ% > 25% slower:
- Log BLOCK in this plan's SUMMARY: "Cell <name> regressed N% (BLOCK threshold)"
- Plan 12.7-09 verification fails; root-cause analysis required before Plan 10 closure.

If all cells PASS (within ±10%), record `verdict: PASS` in SUMMARY.

**Step 6 — Commit:**

```
docs(12.7-09): Phase 12.7 microbench + throughput rebaseline — <verdict>

Microbench (perf-baselines.md): 3 cells captured at HEAD <sha>.
- simple_counter/100_events: <Δ%>
- sketch_heavy/100_events: <Δ%>
- windowed_60s_sum/100_events: <Δ%>

Throughput rebaseline (throughput-baselines.md): 8 cells captured.
- small/tcp (regression-gate): <Δ%> vs 700,571 EPS post-12.6 baseline
- fraud-team/tcp (primary tuning): <Δ%> vs 102,788 EPS

Verdict: <PASS / PASS-WITH-WARN / BLOCK>
Reason: <brief — e.g., "no regression on any cell" or "small/tcp -3.2% (PASS within ±10%)">

Pure-deletion plan: hot path unchanged; expected ~neutral.
```
  </action>
  <verify>
    <automated>grep -q '^### Phase 12.7' .planning/perf-baselines.md && grep -q '^## Phase 12.7' .planning/throughput-baselines.md</automated>
    EXPECTED: both sections appended
  </verify>
  <acceptance_criteria>
    - perf-baselines.md has Phase 12.7 section: `grep -q '^### Phase 12.7' .planning/perf-baselines.md`
    - perf-baselines.md captures all 3 cells: `grep -A 20 '^### Phase 12.7' .planning/perf-baselines.md | grep -c 'simple_counter\|sketch_heavy\|windowed_60s_sum'` returns ≥ 3
    - throughput-baselines.md has Phase 12.7 section: `grep -q '^## Phase 12.7' .planning/throughput-baselines.md`
    - throughput-baselines.md captures 8 cells: `grep -A 30 '^## Phase 12.7' .planning/throughput-baselines.md | grep -c 'small/\|medium/\|large/\|fraud-team/'` returns ≥ 8
    - Regression-gate cell `small/tcp` recorded: `grep -A 30 '^## Phase 12.7' .planning/throughput-baselines.md | grep -q 'small/tcp'`
    - HEAD sha recorded in both: `grep -A 5 '^### Phase 12.7' .planning/perf-baselines.md | grep -q 'HEAD'` AND `grep -A 5 '^## Phase 12.7' .planning/throughput-baselines.md | grep -q 'HEAD'`
    - No cell exceeds 25% slower (BLOCK gate): SUMMARY documents verdict; if BLOCK present, plan verification fails
    - Commit: `git log -1 --format=%s | grep -E '^docs\(12\.7-09\):'`
  </acceptance_criteria>
  <done>GREEN gate: 3 microbench cells + 8 throughput cells captured and appended; verdict logged. Commit `docs(12.7-09):`.</done>
</task>

</tasks>

<threat_model>
Phase 12.7-09 is pure measurement. No code change; no threat surface.
</threat_model>

<verification>
- 3 microbench cells captured + appended to perf-baselines.md
- 8 throughput cells captured + appended to throughput-baselines.md
- Verdict logged (PASS / PASS-WITH-WARN / BLOCK)
- 1 commit: `docs(12.7-09):` (measurement-only — exempt from red-then-green per CLAUDE.md TDD §Note 4)
</verification>

<success_criteria>
- Both ledger files have Phase 12.7 sections
- Regression-gate cell `small/tcp` measured against 700,571 EPS baseline
- Verdict computed for every cell; PASS/WARN/BLOCK clearly marked
- If any cell BLOCK → plan fails and Plan 10 cannot close (escalate)
- Phase 12.7-09 commit lands as `docs(12.7-09):`
</success_criteria>

<output>
After completion, create `.planning/phases/12.7-table-strip/12.7-09-SUMMARY.md`. Self-Check:
- [x] perf-baselines.md has Phase 12.7 section with 3 cells
- [x] throughput-baselines.md has Phase 12.7 section with 8 cells
- [x] Regression-gate verdict (small/tcp) computed
- [x] Microbench verdict per cell (10/25% thresholds)
- [x] If WARN: investigation note recorded for Plan 13 followup
- [x] If BLOCK: escalation logged; closure plan blocked
- [x] Doc-only commit `docs(12.7-09):`
</output>
