---
phase: 12.7-table-strip
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]                # 01 shim still active; 02 architectural test guides; 03 deleted the WireRequest::Http* dispatch arms so temporal_http callees become orphans
files_modified:
  - crates/beava-server/src/temporal_http.rs       # DELETE entire file (~756 LOC)
  - crates/beava-core/src/temporal.rs              # DELETE entire file (~394 LOC)
  - crates/beava-server/src/lib.rs                  # remove `pub mod temporal_http;` declaration
  - crates/beava-core/src/lib.rs                    # remove `pub mod temporal;` declaration (if present)
  - crates/beava-server/src/registry_debug.rs      # delete TemporalStore import + `temporal_stores` + `event_id_index` fields from DevAggState
  - crates/beava-server/src/runtime_core_glue.rs   # strip temporal_http references in module docstring + GlueResponse::TemporalResponse variant if it's now an orphan
  - crates/beava-server/src/server.rs              # strip residual temporal_http reference (line 2184 is a doc-comment; trace any remaining import)
  - crates/beava-server/src/apply_shard.rs         # delete the event_id_index lock in line 816 (Phase 11.5 retract path)
autonomous: true
requirements:
  - SDK-DEC-04             # @bv.table — server-side TemporalStore implementation deleted
  - SDK-DEC-05             # @bv.table function form — same
  - V0-EVENTS-ONLY-01      # the entire MVCC machinery gone
addresses_decisions:
  - "D-02 — TemporalStore + temporal_http modules deleted entirely. No silent-ignore at any layer; routes are 404 (Plan 03), modules are gone (this plan)."
  - "Section-ownership pre-coordination: this plan does NOT touch RecordType variants (Plan 05's territory) nor Python SDK (Plan 06's territory). Cleaner partition."
  - "Cleanup of orphan AppState fields after Plan 03 surgery: Plan 03 removed the WireRequest::Http* dispatch arms; this plan removes the AppState fields those arms used (`temporal_stores`, `event_id_index`) since no other code touches them."
must_haves:
  truths:
    - "`crates/beava-server/src/temporal_http.rs` no longer exists on disk"
    - "`crates/beava-core/src/temporal.rs` no longer exists on disk"
    - "`crates/beava-server/src/lib.rs` has no `pub mod temporal_http;` declaration"
    - "`crates/beava-core/src/lib.rs` has no `pub mod temporal;` declaration"
    - "`DevAggState` (in registry_debug.rs) has no `temporal_stores` field and no `event_id_index` field"
    - "Workspace `cargo check --workspace` is clean — no residual references to `TemporalStore`, `MvccVersion`, `temporal_http::*_via_mio`, `temporal_stores`, `event_id_index`, or `GlueResponse::TemporalResponse`"
    - "phase12_7_legacy_table_handlers_killed.rs::legacy_table_files_deleted partially turns GREEN (2 of 3 deleted Rust files; the python/_tables.py path lands in Plan 06)"
    - "phase12_7_legacy_table_handlers_killed.rs::app_temporal_fields_deleted turns GREEN"
  artifacts:
    - path: "crates/beava-server/src/temporal_http.rs"
      provides: "ABSENT (whole-file delete; ~756 LOC removed)"
      contains: "MUST NOT EXIST"
    - path: "crates/beava-core/src/temporal.rs"
      provides: "ABSENT (whole-file delete; ~394 LOC removed)"
      contains: "MUST NOT EXIST"
    - path: "crates/beava-server/src/registry_debug.rs"
      provides: "DevAggState struct without `temporal_stores` and `event_id_index` fields. Surviving fields (registry, query_time_ms, dev_endpoints, etc.) intact."
      contains: "DevAggState"
  key_links:
    - from: "crates/beava-server/src/lib.rs"
      to: "DELETED temporal_http module"
      via: "module declaration line removed; no other module references it"
      pattern: "pub mod temporal_http"
    - from: "crates/beava-core/src/lib.rs"
      to: "DELETED temporal module"
      via: "module declaration line removed"
      pattern: "pub mod temporal"
---

<objective>
Delete the temporal HTTP shim module + the TemporalStore core machinery + the AppState fields that hold per-table TemporalStore instances. After Plan 03 deleted the wire/router/dispatch arms, these modules became orphans — this plan completes the strip.

After this plan lands:
- `crates/beava-server/src/temporal_http.rs` GONE (~756 LOC removed)
- `crates/beava-core/src/temporal.rs` GONE (~394 LOC removed; deletes `TemporalStore`, `MvccVersion`, `RetractError`, retention sweep)
- Module declarations in both `lib.rs` files removed
- `DevAggState.temporal_stores` field gone
- `DevAggState.event_id_index` field gone (it was used solely for retract bookkeeping — `apply_shard.rs:816` is the only remaining use site, deleted alongside)
- `GlueResponse::TemporalResponse` variant gone if it has no other producer (verify during implementation; Plan 03 deleted the only producer in apply_shard.rs)
- `phase12_7_legacy_table_handlers_killed::legacy_table_files_deleted` partially GREEN (2 of 3 paths; Plan 06 covers `_tables.py`)
- `phase12_7_legacy_table_handlers_killed::app_temporal_fields_deleted` GREEN

**Wave 2 placement:** depends on Plan 03 (the `WireRequest::Http*` dispatch arms must be gone before deleting the `temporal_http::*_via_mio` functions they called). Independent of Plan 05 (different files — Plan 05 is the persistence schema reset).

Output:
- 2 whole-module deletions
- AppState struct slimmed
- `cargo check` clean post-deletion
- 1 architectural-test assertion turns fully GREEN; 1 partially GREEN (Python file pending Plan 06)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.7-table-strip/12.7-CONTEXT.md
@.planning/phases/12.7-table-strip/12.7-PATTERNS.md
@.planning/phases/12.6-v0-surface-reduction/12.6-07-SUMMARY.md
@crates/beava-server/src/temporal_http.rs
@crates/beava-core/src/temporal.rs
@crates/beava-server/src/registry_debug.rs

<interfaces>
<!-- Verified deletion sites -->

`crates/beava-server/src/registry_debug.rs:38` (current — to delete):
```rust
use beava_core::temporal::TemporalStore;
```

`crates/beava-server/src/registry_debug.rs:137` (current — to delete from DevAggState):
```rust
pub temporal_stores: Arc<Mutex<HashMap<String, TemporalStore>>>,
```

`crates/beava-server/src/registry_debug.rs:147` (current — to delete from DevAggState):
```rust
pub event_id_index: Arc<Mutex<hashbrown::HashMap<u64, EventIdEntry, fxhash::FxBuildHasher>>>,
```

`crates/beava-server/src/registry_debug.rs:157-158` (current — to delete from `Default` impl):
```rust
temporal_stores: Arc::new(Mutex::new(HashMap::new())),
event_id_index: Arc::new(Mutex::new(hashbrown::HashMap::with_hasher(...))),
```

`crates/beava-server/src/apply_shard.rs:816` (current — to delete; the only surviving event_id_index use site):
```rust
let mut idx = self.state.dev_agg.event_id_index.lock();
```
This is part of the retract bookkeeping path. With `app.retract` gone (no producer; Plan 03 deleted the WireRequest::HttpRetract dispatch arm), this code is unreachable orphan.

`crates/beava-server/src/runtime_core_glue.rs:10, 105` (current — doc-comments referencing temporal_http):
```rust
//! `temporal_http::*_via_mio`, `feature_query::parse_entity_key`). No
...
/// pre-serialised by the apply-thread `temporal_http` helpers
```
Update doc-comments to reflect post-strip reality.

`crates/beava-server/src/server.rs:2184` (doc-comment):
```rust
// pre-serialised by `temporal_http::*_via_mio` to match legacy
```
Same — update or remove.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (RED): Failing test asserting temporal_http + temporal modules gone</name>
  <read_first>
    - crates/beava-server/tests/phase12_7_legacy_table_handlers_killed.rs (the architectural test asserts these files absent — already RED at Plan 02)
    - crates/beava-server/src/temporal_http.rs (file to be deleted; read FIRST to understand current scope)
    - crates/beava-core/src/temporal.rs (file to be deleted; read FIRST to understand current scope)
  </read_first>
  <files>(no new file in this task — RED gate is the existing architectural test from Plan 02)</files>
  <behavior>
    - The architectural test `phase12_7_legacy_table_handlers_killed::legacy_table_files_deleted` is already RED at Plan 02 because both files still exist.
    - The architectural test `phase12_7_legacy_table_handlers_killed::app_temporal_fields_deleted` is also RED at Plan 02.
    - Confirm both tests fail before Task 1.b deletes the files; the failure messages name the specific files/fields that need deletion.
  </behavior>
  <action>
This task does NOT add a new test file — Plan 02 already wrote the architectural test that gates this deletion. Confirm both tests are RED right now:

```bash
cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed legacy_table_files_deleted 2>&1 | grep FAILED
cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed app_temporal_fields_deleted 2>&1 | grep FAILED
```

Both MUST report FAILED before Task 1.b runs. If either is somehow GREEN, abort the plan — the precondition is broken.

NO commit lands for this task (it's a precondition check, not a code change). Plan 02 already wrote the failing tests.
  </action>
  <verify>
    <automated>cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed legacy_table_files_deleted 2>&1 | grep -c FAILED && cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed app_temporal_fields_deleted 2>&1 | grep -c FAILED</automated>
    EXPECTED: both return ≥ 1 (RED gate confirmed for both architectural sub-tests)
  </verify>
  <acceptance_criteria>
    - `phase12_7_legacy_table_handlers_killed::legacy_table_files_deleted` reports FAILED at start of Plan 04: `cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed legacy_table_files_deleted 2>&1 | grep -q FAILED`
    - `phase12_7_legacy_table_handlers_killed::app_temporal_fields_deleted` reports FAILED: `cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed app_temporal_fields_deleted 2>&1 | grep -q FAILED`
    - `crates/beava-server/src/temporal_http.rs` still exists: `[ -f crates/beava-server/src/temporal_http.rs ]`
    - `crates/beava-core/src/temporal.rs` still exists: `[ -f crates/beava-core/src/temporal.rs ]`
    - `DevAggState` still has temporal fields: `grep -q 'temporal_stores' crates/beava-server/src/registry_debug.rs`
  </acceptance_criteria>
  <done>RED precondition confirmed: 2 architectural tests still fail (these gate this plan's deletions). No commit needed; Task 1.b does the deletions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (GREEN): Delete temporal_http.rs + temporal.rs + AppState temporal fields + cleanup orphans</name>
  <read_first>
    - crates/beava-server/src/temporal_http.rs (read once before deletion to spot any final dependencies)
    - crates/beava-core/src/temporal.rs (same)
    - crates/beava-server/src/registry_debug.rs (lines 38, 137, 147, 157, 158 — DevAggState surgery sites)
    - crates/beava-server/src/runtime_core_glue.rs (lines 10, 105 — doc-comment updates)
    - crates/beava-server/src/apply_shard.rs (line 816 — orphan event_id_index lock)
    - crates/beava-server/src/server.rs (line 2184 — doc-comment)
    - .planning/phases/12.7-table-strip/12.7-PATTERNS.md (§Pattern 5 + §"Files with no clear analog" — whole-module delete posture)
  </read_first>
  <files>
    - crates/beava-server/src/temporal_http.rs (DELETE)
    - crates/beava-core/src/temporal.rs (DELETE)
    - crates/beava-server/src/lib.rs
    - crates/beava-core/src/lib.rs
    - crates/beava-server/src/registry_debug.rs
    - crates/beava-server/src/runtime_core_glue.rs
    - crates/beava-server/src/server.rs
    - crates/beava-server/src/apply_shard.rs
  </files>
  <action>
**Step 1 — Delete the 2 module files:**
```bash
rm crates/beava-server/src/temporal_http.rs
rm crates/beava-core/src/temporal.rs
```

**Step 2 — Remove module declarations:**

In `crates/beava-server/src/lib.rs`: locate and DELETE the line `pub mod temporal_http;` (verify via `grep -n 'temporal_http' crates/beava-server/src/lib.rs` first).

In `crates/beava-core/src/lib.rs`: locate and DELETE the line `pub mod temporal;` (verify via `grep -n 'pub mod temporal' crates/beava-core/src/lib.rs` first; if not present, skip — but the module file existed so the declaration must be somewhere; check both `lib.rs` and any `mod.rs` patterns).

**Step 3 — DevAggState surgery (`crates/beava-server/src/registry_debug.rs`):**

- Line 21 (module docstring): update reference to remove "+ `temporal_stores` + `event_id_index`" — leave only the surviving counters reference
- Line 26 (module docstring): same — strip the `temporal_http::*_via_mio` reference
- Line 38: DELETE `use beava_core::temporal::TemporalStore;`
- Line 137: DELETE `pub temporal_stores: Arc<Mutex<HashMap<String, TemporalStore>>>,`
- Line 147: DELETE `pub event_id_index: Arc<Mutex<hashbrown::HashMap<u64, EventIdEntry, fxhash::FxBuildHasher>>>,`
- Line 157-158: DELETE the `temporal_stores: Arc::new(Mutex::new(HashMap::new())),` and `event_id_index: Arc::new(...)` lines from the `Default` impl
- Check for any unused imports left over (e.g., `HashMap` if only used for `temporal_stores`). Run `cargo check` after this step; clippy warnings about unused imports become errors with `-D warnings`. Strip unused imports.

**Step 4 — Doc-comment cleanup (`crates/beava-server/src/runtime_core_glue.rs`):**

- Line 10 (module docstring): rewrite to remove `temporal_http::*_via_mio` reference. Survivor reference is `feature_query::parse_entity_key`.
- Line 105 (doc-comment near GlueResponse): same — strip `temporal_http` mention.

**Step 5 — `GlueResponse::TemporalResponse` orphan check:**

Run `grep -rn 'GlueResponse::TemporalResponse' crates/beava-server/src/`. If Plan 03's apply_shard.rs deletion was the ONLY producer (likely), then `GlueResponse::TemporalResponse` variant is now an unused enum variant that may trigger `dead_code` warnings. Either:
  - DELETE the variant from the GlueResponse enum (preferred — clean strip), OR
  - Confirm that some other producer still emits it (less likely after Plan 03)

Choose deletion if no other producer remains. Update any consumer (`match` arms) accordingly — they should either disappear with the producer or get an explicit `unreachable!()` shape.

**Step 6 — `apply_shard.rs:816` orphan event_id_index lock:**

Read the surrounding ~20 lines of `crates/beava-server/src/apply_shard.rs` around line 816. The line `let mut idx = self.state.dev_agg.event_id_index.lock();` is the ONLY surviving caller of `event_id_index` after Plan 03's WireRequest::HttpRetract deletion. With `event_id_index` gone from DevAggState (Step 3), this line + its surrounding match arm or function block must also go. Surgical deletion: identify the function/branch containing line 816 and either:
  - Delete the whole branch (if it's a `match` arm tied to the now-deleted retract path), OR
  - Delete just the orphan lines while preserving the surrounding flow

Confirm `cargo check` after this step.

**Step 7 — `server.rs:2184` doc-comment:**

Update the doc-comment to remove `temporal_http::*_via_mio` reference. Trivial.

**Step 8 — Workspace verification:**

```bash
cargo check --workspace 2>&1 | grep -E '^error\[' | wc -l   # MUST be 0
cargo clippy --workspace --all-targets --all-features -- -D warnings 2>&1 | grep -E '^error\[' | wc -l   # MUST be 0
cargo fmt --all --check
```

If clippy reports unused imports or dead code, sweep them. Re-run.

**Step 9 — Architectural test verification:**

```bash
cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed legacy_table_files_deleted 2>&1 | grep -E 'test result: ok|FAILED'
cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed app_temporal_fields_deleted 2>&1 | grep -E 'test result: ok|FAILED'
```

EXPECTED:
- `app_temporal_fields_deleted` → GREEN (DevAggState fields gone)
- `legacy_table_files_deleted` → still RED for ONE remaining file (`python/beava/_tables.py`); the 2 Rust paths are gone. Note this in SUMMARY; Plan 06 finishes the file deletion.

Run workspace tests; same expected-failure list as Plan 03 (`phase11_5_temporal_smoke`, `phase18_07_upsert_delete_rename_test`, `phase12_6_14_mio_temporal` — Plan 06 deletes them).

Commit message:
```
feat(12.7-04): delete temporal_http.rs + temporal.rs + AppState temporal fields

- crates/beava-server/src/temporal_http.rs: DELETED (~756 LOC)
- crates/beava-core/src/temporal.rs: DELETED (~394 LOC; deletes TemporalStore, MvccVersion, RetractError, retention sweep)
- crates/beava-server/src/lib.rs: removed `pub mod temporal_http;`
- crates/beava-core/src/lib.rs: removed `pub mod temporal;`
- crates/beava-server/src/registry_debug.rs: deleted TemporalStore import + temporal_stores + event_id_index fields from DevAggState
- crates/beava-server/src/runtime_core_glue.rs: stripped temporal_http references in doc-comments
- crates/beava-server/src/apply_shard.rs: deleted line-816 orphan event_id_index lock + surrounding retract path
- crates/beava-server/src/server.rs: stripped temporal_http reference from doc-comment
- GlueResponse::TemporalResponse variant deleted (no producer remained after Plan 03)

Architectural-test status:
- app_temporal_fields_deleted → GREEN
- legacy_table_files_deleted → 2 of 3 paths green (python/beava/_tables.py pending Plan 06)
- wire_request_table_variants_deleted → still GREEN (Plan 03)
- route_table_variants_deleted → still GREEN (Plan 03)

Workspace cargo check + clippy + fmt clean. ~1150 LOC deleted.
Plan 12.7-05 (persistence schema reset) and Plan 12.7-06 (Python SDK) are
unblocked and can run in parallel.
```
  </action>
  <verify>
    <automated>cargo check --workspace 2>&1 | grep -cE '^error\[' && cargo clippy --workspace --all-targets --all-features -- -D warnings 2>&1 | grep -cE '^error\['</automated>
    EXPECTED: both return `0` (no errors)
  </verify>
  <acceptance_criteria>
    - `! [ -f crates/beava-server/src/temporal_http.rs ]` (file gone)
    - `! [ -f crates/beava-core/src/temporal.rs ]` (file gone)
    - `! grep -q 'pub mod temporal_http' crates/beava-server/src/lib.rs`
    - `! grep -q 'pub mod temporal' crates/beava-core/src/lib.rs` (or no remnant declaration anywhere — check `mod.rs` candidates too)
    - `! grep -q 'TemporalStore\|temporal_stores\|event_id_index' crates/beava-server/src/registry_debug.rs`
    - `! grep -q 'event_id_index' crates/beava-server/src/apply_shard.rs`
    - `! grep -q 'temporal_http' crates/beava-server/src/runtime_core_glue.rs` (doc-comments cleaned)
    - `! grep -q 'temporal_http' crates/beava-server/src/server.rs` (doc-comment cleaned)
    - Surviving DevAggState fields preserved (sanity): `grep -q 'pub registry:' crates/beava-server/src/registry_debug.rs && grep -q 'pub query_time_ms:' crates/beava-server/src/registry_debug.rs`
    - Workspace check clean: `cargo check --workspace 2>&1 | grep -cE '^error\[' ` returns `0`
    - Workspace clippy clean: `cargo clippy --workspace --all-targets --all-features -- -D warnings 2>&1 | grep -cE '^error\['` returns `0`
    - Workspace fmt clean: `cargo fmt --all --check`
    - Architectural sub-tests turn GREEN: `cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed app_temporal_fields_deleted 2>&1 | grep -q 'test result: ok'`
    - Architectural file-absence test partially turns GREEN: `cargo test -p beava-server --test phase12_7_legacy_table_handlers_killed legacy_table_files_deleted 2>&1` — at this point still RED for `python/beava/_tables.py` (Plan 06 deletes); Rust files green
    - Plan 03's tests still pass: `cargo test -p beava-server --test phase12_7_table_routes_404 2>&1 | grep -q 'test result: ok'`
    - Plan 01's test still passes: `cargo test -p beava-server --test phase12_7_unsupported_node_kind 2>&1 | grep -q 'test result: ok'`
    - Commit: `git log -1 --format=%s | grep -E '^feat\(12\.7-04\):'`
  </acceptance_criteria>
  <done>GREEN gate: 2 module files deleted, AppState temporal fields gone, doc-comments cleaned, workspace clean. Architectural sub-test `app_temporal_fields_deleted` turns GREEN; `legacy_table_files_deleted` partially GREEN (Plan 06 finishes). Commit `feat(12.7-04):`.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.7-04-01 | DoS | TemporalStore retention sweep deletion | mitigate-by-deletion | Removes the periodic retention sweep that walked every entity's MVCC chain. Sweep cost was ~O(N entities × M versions). Net memory + CPU win. |
| T-12.7-04-02 | Information Disclosure | Reduced surface | mitigate-by-deletion | ~1150 LOC of MVCC-shaped state deleted. Fewer surfaces to leak via panic/log. |
</threat_model>

<verification>
- 2 whole files deleted from disk
- 2 module declarations removed from lib.rs files
- AppState shrunk by 2 fields
- Doc-comments cleaned in 3 files
- Orphan `event_id_index` lock removed from apply_shard.rs
- Workspace cargo check + clippy + fmt all clean
- Architectural sub-test `app_temporal_fields_deleted` GREEN
- 2 commits: precondition check (no commit) + `feat(12.7-04):` GREEN
</verification>

<success_criteria>
- temporal_http.rs deleted (0 bytes on disk; file does not exist)
- temporal.rs deleted (0 bytes on disk; file does not exist)
- DevAggState slimmer by 2 fields with all callers cleaned up
- Workspace cargo check clean post-deletion (no orphan callers)
- Architectural test `app_temporal_fields_deleted` GREEN; `legacy_table_files_deleted` partially GREEN
- Plan 12.7-04 commit lands with proper TDD prefix
</success_criteria>

<output>
After completion, create `.planning/phases/12.7-table-strip/12.7-04-SUMMARY.md`. Self-Check:
- [x] temporal_http.rs deleted on disk
- [x] temporal.rs deleted on disk
- [x] Module declarations removed from beava-server/lib.rs and beava-core/lib.rs
- [x] DevAggState `temporal_stores` + `event_id_index` fields gone
- [x] orphan use sites cleaned (apply_shard.rs:816 + doc-comments in runtime_core_glue.rs + server.rs)
- [x] cargo check + clippy + fmt clean
- [x] `app_temporal_fields_deleted` architectural test GREEN
- [x] `legacy_table_files_deleted` architectural test partially GREEN (Rust files; Python file pending Plan 06)
- [x] Commit `feat(12.7-04):`
- Document approximately ~1150 LOC delta (vs Plan 12.6-07's ~7475 LOC bigger delete; smaller because surface is narrower)
</output>