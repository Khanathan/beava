# Plan 13.7-02 Summary — Pagefind index + link audit

**Commit:** `63c14ac9 docs(13.7-02): Pagefind search index + cross-link audit script`
**Status:** PASS

## What shipped

- **`beava-website/scripts/build-search.mjs`** — Pagefind 1.5.2 index builder using the official `pagefind` npm package (no CLI shell-out). Crawls `project/docs/**/*.html` via `addDirectory` (every rendered page has `data-pagefind-body` on `<main>`); also adds 7 curated `addCustomRecord` entries for the legacy React+Babel pages (`/`, `/guide/`, `/guide/chapter-1/`, `/guide/recipes/fraud/`, `/field-guide-ch1.html`, `/field-guide-ch2.html`, `/design-system/`).
- **`beava-website/scripts/check-links.mjs`** — Node link auditor. Walks `project/docs/**/*.html`, extracts every `<a href>`, classifies as `OK / BROKEN / WARN / EXTERNAL_SKIP / ANCHOR_SKIP`. Exits non-zero on BROKEN.
- **`render-docs.mjs` updates:**
  - Wire Pagefind UI script + stylesheet into the site nav search box (`#search`).
  - Fixed breadcrumb path-construction bug (was producing `/docs//concepts/` style double-slashes).
  - Intermediate breadcrumbs only render as clickable links for routes that have an index page on disk (avoids dead intermediate links like `/docs/sdk-api/`).
- 86 pages re-rendered + `_pagefind/` regenerated (93 fragments = 86 docs + 7 legacy custom records).

## Pagefind output

- `project/_pagefind/pagefind-entry.json` reports `page_count: 93`.
- `project/_pagefind/fragment/` = 93 `.pf_fragment` files.
- `project/_pagefind/index/` = sharded inverted index files.
- Pagefind UI bundle (`pagefind-ui.js` + `pagefind-ui.css`) loaded by every docs page.

## Link audit

```
check-links scanned 86 html files
OK=4570 BROKEN=0 WARN=545 EXTERNAL=437 ANCHOR=1754
BROKEN_COUNT=0 WARN_COUNT=545
```

- **OK = 4570** — internal `/docs/...`, `/styles/...`, `/_pagefind/...`, `/assets/...`, `/guide/...` paths that resolve to a file on disk.
- **BROKEN = 0** — exit code 0; no internal docs links 404.
- **WARN = 545** — every WARN is a cross-repo `https://github.com/beava-dev/beava/blob/main/...` URL the renderer generates from `.md` source links to `.planning/...`, `examples/...`, `crates/...`, `python/...`, `src/...`, `tests/...`. CONTEXT.md explicitly accepts cross-repo links as warnings.
- **EXTERNAL_SKIP = 437** — other `https://` (mostly `github.com/beava-dev/beava` repo home, `.dev` external sites). Skipped per scope.
- **ANCHOR_SKIP = 1754** — pure `#section` anchors. Anchor validity is best-effort (out of scope per Plan 02).

## Search smoke test

Pagefind index covers all 86 rendered docs pages + 7 legacy pages. Direct DOM-level test deferred to Plan 04 manual checkpoint (post-Cloudflare-deploy: type "histogram" in site search → expect hits at `/docs/operators/buffer-geo/histogram/`). The fragment count + entry.json confirm content is in the index.

## Source-markdown fixes

None needed — every `docs/*.md` cross-link to another `docs/*.md` resolved via `srcToRouteMap`. No Cause-B (typo'd target) found.

## Next

Plan 03 — quickstart asset polish + /guide/ "coming soon" banners.
