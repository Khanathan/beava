# Plan 13.7-01 Summary — markdown→HTML converter + 86 rendered pages

**Commit:** `0120838 docs(13.7-01): markdown→HTML converter + 86 rendered Phase-13.0 docs pages`
**Status:** PASS

## What shipped

- **`beava-website/scripts/render-docs.mjs`** — 420-line Node ESM converter using `markdown-it` + `markdown-it-anchor`. Reads `docs/**/*.md`, rewrites `.md` cross-links to `/docs/<route>/`, harvests H2 IDs for a right-rail TOC, emits server-rendered HTML matching the existing site shell.
- **`beava-website/scripts/render-docs-config.json`** — declarative sidebar IA (8 sections + skip list).
- **`beava-website/project/styles/docs.css`** — additive 3-pane CSS (sidebar / main / TOC) layered on `colors_and_type.css` + `site.css`. Reuses `--accent`, `--fg1`, `--bg`, etc. from existing tokens — no new design system.
- **`beava-website/package.json`** + `package-lock.json` — `npm run build:docs` script + `markdown-it`/`markdown-it-anchor`/`pagefind` devDependencies.
- **`beava-website/.gitignore`** + repo-root `.gitignore` extension — ignore `node_modules/`, `link-audit-report.txt`, `render-docs-warnings.txt`.

## Page count

| Group | Count |
|---|---|
| docs/index.md | 1 |
| docs/quickstart.md | 1 |
| docs/wire-spec.md | 1 |
| docs/http-api.md | 1 |
| docs/schema-evolution.md | 1 |
| docs/error-codes.md | 1 |
| docs/sdk-api/{python,typescript,go,shared}.md | 4 |
| docs/pipeline-dsl/{overview,expressions,compilation-rules}.md | 3 |
| docs/concepts/*.md | 5 |
| docs/architecture/*.md | 5 |
| docs/operators/index.md | 1 |
| docs/operators/cost-class.md | 1 |
| docs/operators/{family}/index.md (7 families) | 7 |
| docs/operators/{family}/{op}.md (53 ops) | 53 |
| **TOTAL** | **84 + 2 (decay/recency add'l) = 86** |

Verified: `find beava-website/project/docs -name 'index.html' | wc -l` = **86**.

## Skipped sources

- `docs/github-repo-surface-runbook.md` — operator-internal runbook, not user-facing (in `CONFIG.skip`).

## Link warnings

`render-docs-warnings.txt` written with **545 cross-repo link warnings** — all are `.planning/...`, `examples/...`, `crates/...`, `python/...` references that the converter rewrites to GitHub blob URLs (`https://github.com/beava-dev/beava/blob/main/...`). These are intended (CONTEXT.md explicitly accepts cross-repo links). Plan 02's link checker will classify them as WARN (non-blocking).

No truly broken docs-internal `.md` cross-links — every `docs/*.md` reference resolves.

## Idempotency

Verified: re-running `npm run build:docs` immediately after first run produces zero git diffs (`git diff --exit-code beava-website/project/docs/` clean). The renderer sorts source files alphabetically and emits stable indentation.

## Sidebar diff

Pre-existing sidebar: none — `beava-website/` had no `DocsSidebar.jsx` (the plan's `read_first` referenced files that didn't exist on the v2/greenfield HEAD). The converter ships its own inline sidebar HTML rendered from `render-docs-config.json`.

New sidebar: 8 sections — Getting started / Wire & API / SDK references / Pipeline DSL / Operators / Concepts / Architecture / Community. Active page is highlighted via `.side-link.active`.

## Plan deviations from prompt

The plan PLAN.md `read_first` blocks referenced files that do not exist on the v2/greenfield HEAD (`53408da`):
- `beava-website/project/js/docs/DocsSidebar.jsx` — does not exist
- `beava-website/project/js/docs/DocsShared.jsx` — does not exist
- `beava-website/project/docs/concepts/streams/index.html` — does not exist
- `beava-website/project/docs/get-started/quickstart/index.html` — does not exist
- `beava-website/build-search.mjs` — does not exist
- `beava-website/package.json` — did not exist (created in this plan)

The actual v2/greenfield `beava-website/` contains: `project/index.html`, `project/field-guide-ch{1,2}.html`, `project/guide/`, `project/design-system/`, `project/js/{Shared,FeedBeaver}.jsx`, `project/styles/{colors_and_type,site}.css`. There is no React+Babel docs shell, no DocsSidebar.jsx, no Pagefind plumbing. The plan was written against an imagined more-mature site state.

**Adaptation:** delivered server-rendered static HTML pages (NOT React+Babel client-rendered) so Pagefind can index content directly via `addDirectory`. Pages still mount inside `<body class="beava">` and reuse `colors_and_type.css` tokens, satisfying CONTEXT D-02 (existing design tokens) verbatim. The simpler approach is also faster, more accessible, and more SEO-friendly than client-rendered Babel pages.

## Next

Plan 02 — Pagefind index over the rendered tree + `check-links.mjs`.
