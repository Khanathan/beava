---
phase: 16-sdk-source-annotation
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - python/tests/test_phase3_smoke.py
  - python/tests/test_phase4_smoke.py
  - python/tests/test_phase5_smoke.py
  - python/tests/test_app.py
  - python/tests/test_agg.py
  - python/tests/test_events.py
  - python/tests/test_tables.py
  - python/tests/test_sdk_ops.py
  - python/tests/test_validate.py
  - python/tests/test_decay_velocity.py
  - python/tests/test_transport_http.py
  - python/tests/test_transport_tcp.py
  - python/tests/test_embed.py
  - python/tests/bench_register_compile.py
  - docs/blog/streaming-shouldnt-require-a-platform-team.md
  - beava-website/project/field-guide-ch1.html
  - beava-website/project/field-guide-ch2.html
  - beava-website/project/index.html
  - beava-website/project/guide/chapter-1/index.html
  - beava-website/project/guide/recipes/personalization/index.html
  - beava-website/project/guide/recipes/fraud/index.html
  - beava-website/project/guide/recipes/leaderboard/index.html
  - beava-website/project/guide/recipes/usage-metering/index.html
  - beava-website/project/guide/recipes/rate-limiting/index.html
  - python/tests/bench_source_decoration.py
  - .planning/perf-baselines.md
  - .planning/throughput-baselines.md
  - .planning/phases/16-sdk-source-annotation/16-VERIFICATION.md
  - .planning/phases/16-sdk-source-annotation/16-SUMMARY.md
autonomous: true
requirements: []  # aggregates all Plan 01 + 02 REQ-IDs; no new ones

must_haves:
  truths:
    - "Every class-form @bv.event / @bv.table in the repo is preceded by @bv.source"
    - "pytest python/tests/ is fully green post-migration (zero `source_missing` errors from legacy sites)"
    - "beava-website docs render without regression (smoke: dev server up; sample pages load)"
    - "Decoration-time overhead of @bv.source is negligible (< 5% slower than bare @bv.event decoration) — regression bench committed"
    - "Phase 16 throughput run row added to .planning/throughput-baselines.md with no > 25% simple-fraud regression"
  artifacts:
    - path: "python/tests/bench_source_decoration.py"
      provides: "criterion-equivalent Python bench measuring @bv.source decoration overhead"
      min_lines: 40
    - path: ".planning/perf-baselines.md"
      provides: "new Phase 16 row: decoration_with_source_ns vs baseline decoration_bare_ns"
      contains: "Phase 16"
    - path: ".planning/throughput-baselines.md"
      provides: "new Phase 16 row — simple-fraud shape re-run confirms no regression"
      contains: "Phase 16"
    - path: ".planning/phases/16-sdk-source-annotation/16-VERIFICATION.md"
      provides: "SC1–SC6 evidence + threat register outcome + grey-area disposition"
      min_lines: 60
    - path: ".planning/phases/16-sdk-source-annotation/16-SUMMARY.md"
      provides: "Phase 16 outcome summary"
      min_lines: 40
  key_links:
    - from: "migrated test files"
      to: "python/beava/_source.py"
      via: "import source from beava; prepend @bv.source on class-form"
      pattern: "@bv\\.source"
    - from: ".planning/throughput-baselines.md (Phase 16 row)"
      to: "crates/beava-bench small-pipeline shape"
      via: "throughput-run convention enforced Phase 8+"
      pattern: "simple-fraud"
---

<objective>
Migrate every class-form `@bv.event` / `@bv.table` decoration across the
repository to prepend `@bv.source`, verify the full test suite is green,
and close Phase 16 with:

- a micro-bench proving `@bv.source` decoration overhead is negligible
  (per CLAUDE.md §Conventions perf-regression gate for Phase 6+)
- a throughput-run row in `.planning/throughput-baselines.md` confirming
  no simple-fraud regression (per CLAUDE.md §Conventions throughput gate
  for Phase 8+)
- a VERIFICATION.md tracing SC1–SC6 to evidence
- a SUMMARY.md capturing REQ-IDs closed, grey-area dispositions, and any
  carry-overs for Phase 12 / 14 / 14.1.

Purpose: Land Phase 16 ship-ready. The public API surface is stable at
v0.0 and every test, doc sample, and website example demonstrates the
new surface.

Output: ~103 migrated decoration sites; 1 bench; 2 baseline rows;
VERIFICATION + SUMMARY.

**Scope note:** This plan is mostly mechanical diff. Task 1 is grep-driven.
Budget: ~10 LoC per test site × 26 Python sites + ~70 website HTML sites
+ ~7 doc sites = largest plan by LoC, smallest by conceptual load.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-sdk-source-annotation/16-CONTEXT.md
@.planning/phases/16-sdk-source-annotation/16-01-PLAN.md
@.planning/phases/16-sdk-source-annotation/16-02-PLAN.md
@.planning/perf-baselines.md
@.planning/throughput-baselines.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (mechanical migration): prepend @bv.source to all class-form decorations + add imports</name>
  <files>python/tests/*.py, docs/blog/streaming-shouldnt-require-a-platform-team.md, beava-website/project/**/*.html</files>
  <action>
    No red/green split — this is a mechanical migration task. The red evidence is
    already present: running `pytest python/tests/ -x` immediately after merging
    Plans 01 + 02 will fail with BV-E-SOURCE-MISSING on every class-form test.
    That IS the red. Task 1 green is: every site migrated, full suite green.

    Step 1 — inventory:
    ```bash
    grep -rnE '^@bv\.(event|table)' python/tests/ docs/ beava-website/ > /tmp/phase16-migration-sites.txt
    wc -l /tmp/phase16-migration-sites.txt  # expect ~103
    ```

    Step 2 — Python test migration (26 sites):
    For each file in python/tests/*.py that imports `import beava as bv`:
    - find every `^@bv.event...\nclass ` or `^@bv.table(...)\nclass ` pattern
    - prepend `@bv.source\n` as a new line above `@bv.event` / `@bv.table`
    - (no import change needed — `bv.source` is available from `import beava as bv` via Plan 01's __init__.py export)

    Do this in 3 batches of ~9 files each to keep per-commit diffs reviewable:
    - Batch 1 commit: `refactor(16-03): migrate phase 3-5 smokes + test_app + test_agg to @bv.source`
    - Batch 2 commit: `refactor(16-03): migrate test_events/test_tables/test_sdk_ops/test_validate to @bv.source`
    - Batch 3 commit: `refactor(16-03): migrate remaining phase-specific tests + benches to @bv.source`

    Between batches, run `pytest python/tests/ -x` — expect gradual fix-up (failures shrink batch-by-batch).

    Step 3 — docs migration (~7 sites):
    - docs/blog/streaming-shouldnt-require-a-platform-team.md — update code fences
    Commit: `docs(16-03): migrate blog post code samples to @bv.source`

    Step 4 — website migration (~70 sites):
    - beava-website/project/*.html: HTML <code> / <pre> blocks containing decorator Python samples
    - beava-website/project/guide/**/*.html: same
    - This is bulk find/replace (`sed -i '' -e 's/^@bv\.event/@bv.source\n@bv.event/' ...` — be careful with indentation inside HTML; manual review required for each file)
    - Run the website dev server (`cd beava-website && python3 -m http.server`) and spot-check 3 pages render: home, chapter-1, recipes/fraud
    Commit: `docs(16-03): migrate beava-website sample code to @bv.source`

    Step 5 — final suite run:
    ```bash
    pytest python/tests/ -x -q                 # must be green
    cargo test --workspace                      # must be green
    cargo clippy --workspace --all-targets --all-features -- -D warnings
    cargo fmt --all --check
    ```
    If any bare class-form survives (grep misses): fix it; commit.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && test $(grep -rnE '^@bv\.(event|table)' python/ docs/ beava-website/ 2>/dev/null | grep -v '@bv.source' | wc -l) -eq 0 && pytest python/tests/ -x -q && cargo test --workspace</automated>
  </verify>
  <done>
    Zero unguarded `^@bv.event` / `^@bv.table` class-form sites remain in python/ docs/ beava-website/. Full Python test suite green. Full cargo workspace test green. No regressions in unrelated tests. Website dev-server spot-check confirms sample pages render.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (red+green): @bv.source decoration-overhead microbench + perf-baselines row</name>
  <files>python/tests/bench_source_decoration.py, .planning/perf-baselines.md</files>
  <behavior>
    Bench measures:
    - `decorate_bare_event_class` — baseline: how long does `@bv.event` class-form decoration take (1000 fresh classes via `types.new_class`)
    - `decorate_sourced_event_class` — how long does `@bv.source @bv.event` class-form decoration take
    - Same pair for `@bv.table(key='x')`
    - Same pair for function-form (confirms no overhead shift)
    - Output: ns/iter median across 5000 trials, written to .planning/perf-baselines.md in the same markdown-table format as existing rows, keyed by hw-class.

    Regression threshold: `decorate_sourced_event_class` median / `decorate_bare_event_class` median < 1.05 (source adds < 5% overhead — it is just setting an attribute + type check).

    Bench is a Python script (the repo already uses python timeit-based benches for the SDK — see python/tests/bench_register_compile.py), not a Rust criterion bench, because the hot path is in Python. CLAUDE.md §Conventions perf-regression-gate language says "at least one `criterion` microbench" but clarifies "or language-equivalent" via the plan-checker contract. Python timeit bench fulfills the contract for a Python-SDK-heavy phase.
  </behavior>
  <action>
    Red subtask 2.a: create python/tests/bench_source_decoration.py with the bench + a threshold assertion (`assert ratio < 1.05, f"source overhead {ratio:.2%} exceeds 5% threshold"`). Run: fails because … actually, expected to pass on first run since source is trivially cheap. So the "red" here is documentary — we run the bench once to record the number, commit as an empty placeholder row `TBD`, then task 2.b fills the row.
    Commit red: `test(16-03): add @bv.source decoration-overhead bench (placeholder row in perf-baselines.md)`.

    Green subtask 2.b: run the bench; record actual ns/iter numbers in .planning/perf-baselines.md under a new "## Phase 16 — decoration overhead (2026-04-24)" section keyed by the running hw-class (copy the shape used by existing Phase 3 baseline rows). Commit: `perf(16-03): record @bv.source decoration-overhead baseline on <hw-class>`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && python python/tests/bench_source_decoration.py && grep -q "Phase 16" .planning/perf-baselines.md</automated>
  </verify>
  <done>
    Bench exists; ratio < 1.05 asserted; .planning/perf-baselines.md has a Phase 16 section with numbers; regression gate honored per CLAUDE.md §Conventions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase 16 throughput-run + VERIFICATION.md + SUMMARY.md</name>
  <files>.planning/throughput-baselines.md, .planning/phases/16-sdk-source-annotation/16-VERIFICATION.md, .planning/phases/16-sdk-source-annotation/16-SUMMARY.md</files>
  <action>
    Step 1 — throughput run (CLAUDE.md Phase 8+ convention):
    - Run `crates/beava-bench` small-pipeline (simple-fraud shape) × HTTP and × TCP transports for 60s each
    - Append rows to .planning/throughput-baselines.md under a new "## Phase 16 — SDK surface v0 (2026-04-24)" section keyed by hw-class
    - Compare simple-fraud (small) EPS to the most recent prior baseline in the same hw-class (Phase 13.1 or 13.3 row, whichever is most recent)
    - If > 25% slower: STOP, investigate, fix or escalate. Phase 16 should have ZERO wire-path impact — any regression is unexpected.
    - If 10-25% slower: record as WARN in the row.
    - Else: record as OK.

    Step 2 — VERIFICATION.md:
    Write ~60 lines covering:
    - SC1–SC6 evidence (test file + test function names)
    - Plan 01/02/03 commit list
    - REQ-IDs closed (9 total)
    - Threat-register outcome (T-16-01 through T-16-08 all OK)
    - Grey-area dispositions:
      * GA-1 outcome (did user accept register-time enforcement for SC3?)
      * GA-2 outcome (did user accept no push_table/delete_table aliases?)
      * GA-3..5 dispositions
    - Carry-overs to Phase 12 / 14 / 14.1

    Step 3 — SUMMARY.md:
    Write ~40 lines in the standard summary template shape (artifacts, commits,
    tests added, REQ-IDs, open questions for next phase).

    Commit: `docs(16-03): Phase 16 throughput run + VERIFICATION + SUMMARY`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && grep -q "Phase 16" .planning/throughput-baselines.md && test -f .planning/phases/16-sdk-source-annotation/16-VERIFICATION.md && test -f .planning/phases/16-sdk-source-annotation/16-SUMMARY.md</automated>
  </verify>
  <done>
    Throughput-baselines row appended; VERIFICATION + SUMMARY committed; phase 16 ship-ready.
  </done>
</task>

</tasks>

<threat_model>
No new threats introduced by Plan 03. This plan is migration + measurement only; the Plan 01 and Plan 02 threat registers apply unchanged after migration.

| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-16-09 | Tampering | Broken migration leaves a bare class-form that then errors at runtime in user demo | mitigate | Task 1 verify clause greps for any `^@bv.event` / `^@bv.table` not preceded by `@bv.source`; zero tolerance — build fails if any bare site remains. |
| T-16-10 | Information disclosure | Website samples leak internal API details | accept | Samples are already public; @bv.source is public API. |
</threat_model>

<verification>
- `grep -rnE '^@bv\.(event|table)' python/ docs/ beava-website/ | grep -v '@bv.source'` returns empty (zero bare class-form sites)
- `pytest python/tests/ -x -q` green
- `cargo test --workspace` green
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean
- `cargo fmt --all --check` clean
- `.planning/perf-baselines.md` has a Phase 16 row with decoration overhead < 1.05×
- `.planning/throughput-baselines.md` has a Phase 16 row, no > 25% simple-fraud regression
- VERIFICATION + SUMMARY committed
</verification>

<success_criteria>
1. All ~103 class-form decorator sites migrated to `@bv.source` prefix
2. Full pytest + cargo test green post-migration (SC6)
3. Decoration-overhead bench committed with < 5% regression (Phase 6+ perf gate honored)
4. Throughput-baseline row committed with no > 25% simple-fraud regression (Phase 8+ throughput gate honored)
5. VERIFICATION.md + SUMMARY.md committed; Phase 16 shippable
6. Grey areas GA-1..GA-5 resolved and documented
</success_criteria>

<output>
After completion the phase is complete. Final artifacts:
- `.planning/phases/16-sdk-source-annotation/16-VERIFICATION.md`
- `.planning/phases/16-sdk-source-annotation/16-SUMMARY.md`
- `.planning/perf-baselines.md` updated with Phase 16 row
- `.planning/throughput-baselines.md` updated with Phase 16 row

Orchestrator should update .planning/ROADMAP.md Phase 16 status from `📋 PLANNED` to `✅ COMPLETE` and proceed to Phase 14 Chunk A or the v0 ship-gate tag per the roadmap.
</output>
