---
phase: 16-sdk-source-annotation
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - .planning/throughput-baselines.md
  - .planning/phases/16-sdk-source-annotation/16-VERIFICATION.md
  - .planning/phases/16-sdk-source-annotation/16-SUMMARY.md
  - CHANGELOG.md
autonomous: true
requirements: []  # aggregates all upstream Plan 01-05 REQ-IDs

must_haves:
  truths:
    - "Phase 16 throughput-run row appended to .planning/throughput-baselines.md; simple-fraud EPS within 25% of prior baseline in the same hw-class"
    - "VERIFICATION.md traces SC1..SC9 to concrete test file + test function names"
    - "SUMMARY.md captures REQ-IDs closed (14 total), grey-area dispositions, cross-phase carry-overs"
    - "CHANGELOG.md notes the D-09 hard-break route rename under Unreleased / breaking"
  artifacts:
    - path: ".planning/throughput-baselines.md"
      provides: "Phase 16 row(s) — simple-fraud small x HTTP + TCP transport"
      contains: "Phase 16"
    - path: ".planning/phases/16-sdk-source-annotation/16-VERIFICATION.md"
      provides: "SC1-SC9 evidence + threat register outcomes + grey-area dispositions"
      min_lines: 80
    - path: ".planning/phases/16-sdk-source-annotation/16-SUMMARY.md"
      provides: "Phase 16 outcome summary"
      min_lines: 50
    - path: "CHANGELOG.md"
      provides: "Unreleased section entry documenting hard-break wire rename + new SDK ergonomics"
      contains: "/upsert/"
  key_links:
    - from: ".planning/throughput-baselines.md (Phase 16 row)"
      to: "crates/beava-bench small-pipeline (simple-fraud shape)"
      via: "throughput-run convention enforced Phase 8+"
      pattern: "simple-fraud"
---

<objective>
Close Phase 16 with:
- a throughput-run row in `.planning/throughput-baselines.md` confirming
  no simple-fraud regression vs prior baseline (Phase 8+ convention)
- VERIFICATION.md tracing SC1..SC9 to test evidence
- SUMMARY.md capturing commits, REQ-IDs, grey-area dispositions, and
  carry-overs for Phase 12 / 14 / 14.1
- CHANGELOG.md entry documenting D-09 hard-break + new SDK ergonomics

Purpose: Phase 16 ship-ready. Every gate (perf, throughput, SC,
REQ-IDs, threat register) reconciled in one document.

Output: throughput row; VERIFICATION.md; SUMMARY.md; CHANGELOG.md entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-sdk-source-annotation/16-CONTEXT.md
@.planning/phases/16-sdk-source-annotation/16-01-PLAN.md
@.planning/phases/16-sdk-source-annotation/16-02-PLAN.md
@.planning/phases/16-sdk-source-annotation/16-03-PLAN.md
@.planning/phases/16-sdk-source-annotation/16-04-PLAN.md
@.planning/phases/16-sdk-source-annotation/16-05-PLAN.md
@.planning/throughput-baselines.md
@.planning/perf-baselines.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 16 throughput run (CLAUDE.md Phase 8+ convention)</name>
  <files>.planning/throughput-baselines.md</files>
  <action>
    Step 1 - run crates/beava-bench small-pipeline (simple-fraud shape) x HTTP
    and x TCP transports for 60s each:
    ```bash
    cargo run -p beava-bench --release --bin temporal_throughput -- --shape simple-fraud --transport http --duration 60
    cargo run -p beava-bench --release --bin temporal_throughput -- --shape simple-fraud --transport tcp  --duration 60
    ```
    (Adjust CLI to match actual crates/beava-bench flags; the harness
    supports the small-pipeline shape per CLAUDE.md Phase 7.5 note.)

    Step 2 - append rows to .planning/throughput-baselines.md under a new
    section "## Phase 16 - SDK surface v0 (2026-04-24)" keyed by hw-class.
    Row format mirrors existing rows.

    Step 3 - compare to the most recent prior baseline in the same hw-class
    (Phase 13.1 or 13.3 row). Apply CLAUDE.md thresholds:
    - > 25% slower -> STOP; investigate; confirm on a second run before
      blocking. Phase 16 has no wire-path impact (SDK kwargs opt-in;
      /upsert handler is same code as old /push-table handler renamed).
    - 10-25% slower -> record WARN in the row.
    - Within 10% -> record OK.

    Commit: `perf(16-06): record Phase 16 throughput baseline (simple-fraud x http + tcp)`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && grep -q "Phase 16" .planning/throughput-baselines.md</automated>
  </verify>
  <done>
    Two Phase 16 rows (http + tcp) appended under a Phase 16 section; no > 25% regression vs prior baseline; disposition recorded.
  </done>
</task>

<task type="auto">
  <name>Task 2: VERIFICATION.md + SUMMARY.md + CHANGELOG.md</name>
  <files>.planning/phases/16-sdk-source-annotation/16-VERIFICATION.md, .planning/phases/16-sdk-source-annotation/16-SUMMARY.md, CHANGELOG.md</files>
  <action>
    Step 1 - VERIFICATION.md (~80 lines):
    - SC1..SC9 evidence table: `SC# | criterion | test file::test name | commit`
      * SC1 - test_source.py::test_source_on_event_class_outer + ::test_source_on_event_class_inner
      * SC2 - test_source.py::test_source_on_event_function_raises
      * SC3 - test_validate.py::test_missing_source_annotation_register_fails
      * SC4 - test_app.py::test_upsert_source_table_happy_path + test_phase16_smoke.py::test_e2e_upsert_delete_source_table
      * SC5 - test_app.py::test_upsert_derived_table_raises + phase16_push_to_derivation_smoke.rs
      * SC6 - pytest python/tests/ green post-Plan 03 migration
      * SC7 - phase16_wire_rename_smoke.rs::test_legacy_push_table_404 + test_legacy_delete_table_404
      * SC8 - test_linger.py::test_linger_ms_positive_buffers + ::test_max_batch_flush + ::test_context_manager_flushes_on_exit
      * SC9 - test_pipeline.py::test_e2e_pipeline_strict_fifo + ::test_e2e_pipeline_round_trip_timing + ::test_pipeline_http_raises
    - REQ-IDs closed (14 total): SDK-SOURCE-01..05, SDK-UPSERT-01..03, SRV-PUSH-DERIV-01, SRV-WIRE-RENAME-01, SDK-LINGER-01, SDK-LINGER-02, SDK-PIPELINE-01, SDK-PIPELINE-02.
    - Threat register outcomes: T-16-01..T-16-19 all OK (T-16-13 noted as accepted v0 tradeoff).
    - Grey-area dispositions: GA-1/2/3/5 resolved; GA-4 (Phase 12 alignment) still open; GA-6 (push_many wire availability) - record Plan 04's disposition.
    - Carry-overs: Phase 12 must adopt `_beava_is_source` for `app.push` (GA-4); Phase 14 references D-06 for tolerate_delay_ms; Phase 14.1 references D-06 for modifiable=True.

    Step 2 - SUMMARY.md (~50 lines) per standard template:
    - Phase 16 shipped 2026-04-24
    - Scope: ~500 LoC
    - Plans 01-06 commit list
    - Files touched (aggregated)
    - REQ-IDs closed (14)
    - Test deltas
    - Grey-area dispositions
    - Open questions / carry-overs
    - Ship-gate status: Phase 16 ready; proceed to Phase 14 Chunk A or v0 ship-gate tag per ROADMAP.

    Step 3 - CHANGELOG.md (Unreleased section; create if missing):

        ## Unreleased

        ### Breaking
        - HTTP wire paths renamed (hard break; no deprecation window - pre-v0):
          POST /push-table/{table} -> POST /upsert/{table}
          POST /delete-table/{table} -> POST /delete/{table}
          Old paths return 404.

        ### Added
        - @bv.source required marker on class-form @bv.event / @bv.table;
          commutative with the wrapped decorator (either order works).
        - app.upsert(target, row) + app.delete(target, *, key={...}) verbs
          on the Python client (table sources only; derivations rejected
          with BV-E-PUSH-TO-DERIVATION client-side and 400
          cannot_push_to_derivation server-side).
        - bv.App(url, linger_ms=0, max_batch=256) - opt-in client-side
          lingering for high-throughput producers (Python SDK).
        - with app.pipeline() as pipe: - framed-TCP strict-FIFO batch
          context manager for multi-op round-trip compression (TCP only;
          HTTP raises BV-E-PIPELINE-HTTP).

        ### Error codes (new)
        - BV-E-SOURCE-MISSING, BV-E-SOURCE-ON-DERIVATION,
          BV-E-SOURCE-FLAG-ON-DERIVATION, BV-E-PUSH-TO-DERIVATION,
          BV-E-PIPELINE-HTTP, server-side cannot_push_to_derivation.

    Commit: `docs(16-06): VERIFICATION + SUMMARY + CHANGELOG for Phase 16 ship`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && test -f .planning/phases/16-sdk-source-annotation/16-VERIFICATION.md && test -f .planning/phases/16-sdk-source-annotation/16-SUMMARY.md && grep -q "Phase 16" .planning/phases/16-sdk-source-annotation/16-VERIFICATION.md && grep -q "Unreleased" CHANGELOG.md && grep -q "/upsert/" CHANGELOG.md</automated>
  </verify>
  <done>
    VERIFICATION.md, SUMMARY.md, and CHANGELOG.md entries committed; every SC traced to a test; every REQ-ID accounted for; grey-area dispositions recorded; cross-phase carry-overs flagged for Phase 12 / 14 / 14.1.
  </done>
</task>

</tasks>

<threat_model>
No new runtime surface. Documentation + measurement only.

| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-16-20 | Information disclosure | CHANGELOG leaks implementation detail prematurely | accept | Phase 16 is pre-v0; CHANGELOG Unreleased section is the right home for these notes. |
</threat_model>

<verification>
- `.planning/throughput-baselines.md` has Phase 16 rows
- `.planning/phases/16-sdk-source-annotation/16-VERIFICATION.md` exists and references SC1-SC9
- `.planning/phases/16-sdk-source-annotation/16-SUMMARY.md` exists
- `CHANGELOG.md` Unreleased section documents D-09 hard break and new SDK surface
</verification>

<success_criteria>
1. Throughput-baselines.md updated; no > 25% simple-fraud regression (Phase 8+ gate)
2. VERIFICATION.md traces all 9 SCs to concrete tests
3. SUMMARY.md captures phase outcomes; GA dispositions recorded
4. CHANGELOG.md notes the hard-break rename + new SDK ergonomics
5. Phase 16 shippable; ROADMAP can flip status from PLANNED to COMPLETE after this plan
</success_criteria>

<output>
Phase 16 complete. Orchestrator should flip .planning/ROADMAP.md Phase 16 from "PLANNED" to "COMPLETE" and proceed to Phase 14 Chunk A or v0 ship-gate tag per roadmap.
</output>
