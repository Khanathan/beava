---
phase: 59
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/wire_negotiation_handshake.rs
  - tests/binary_push_bytes_passthrough.rs
  - tests/json_over_tcp_still_accepted.rs
  - tests/protocol_binary_decode_fuzz.rs
  - scripts/samply-probe-json-share.sh
  - scripts/verify-no-tcp-json-reserialize.sh
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - TPC-PERF-09
must_haves:
  truths:
    - "A developer running `cargo test --test wire_negotiation_handshake` sees the test fail with 'unknown opcode 0x18' BEFORE Wave 2 (RED)."
    - "A developer running `cargo test --test binary_push_bytes_passthrough -- --ignored` sees a scoped probe confirm at least one `serde_json::to_vec` leaf frame on the TCP push path (RED — proves the WASTE exists before Wave 1 eliminates it)."
    - "A developer running `cargo test --test json_over_tcp_still_accepted` sees it PASS on the current codebase (JSON OP_PUSH is accepted today; this test is the D-B3 ≥ 1 release cycle regression guard for every later wave)."
    - "A developer running `bash scripts/samply-probe-json-share.sh` sees `JSON_SHARE_PCT=` on stdout and the coverage sentinel fires if the harness does not exercise a TCP push (floor PCT ≥ 1.0)."
    - "A developer running `bash scripts/verify-no-tcp-json-reserialize.sh` sees exit code 1 and output naming the tcp.rs:2159 + tcp.rs:2538 offenders (RED until Wave 1)."
    - "`.planning/REQUIREMENTS.md` contains a TPC-PERF-09 row with Phase 59 traceability."
  artifacts:
    - path: "tests/wire_negotiation_handshake.rs"
      provides: "OP_NEGOTIATE_WIRE_FORMAT handshake contract (RED, flips GREEN at Wave 2)"
    - path: "tests/binary_push_bytes_passthrough.rs"
      provides: "Bytes-end-to-end passthrough contract + WASTE-exists probe (RED, flips GREEN at Wave 1)"
    - path: "tests/json_over_tcp_still_accepted.rs"
      provides: "D-B3 backward-compat guard (GREEN Wave 0, must stay GREEN every wave through phase close)"
    - path: "tests/protocol_binary_decode_fuzz.rs"
      provides: "D-E3 decoder fuzz regression (Arbitrary-generated inputs; GREEN Wave 0)"
    - path: "scripts/samply-probe-json-share.sh"
      provides: "D-D3 samply JSON-share probe with coverage sentinel"
    - path: "scripts/verify-no-tcp-json-reserialize.sh"
      provides: "D-C3 grep invariant enforcer (RED Wave 0, GREEN Wave 1+)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TPC-PERF-09 row with Phase 59 traceability"
  key_links:
    - from: "tests/binary_push_bytes_passthrough.rs"
      to: "scripts/samply-probe-json-share.sh"
      via: "both parse `serde_json::*` leaf share from pprof top.txt"
      pattern: "serde_json::(to_vec|from_slice|format_escaped_str)"
    - from: "scripts/verify-no-tcp-json-reserialize.sh"
      to: "src/server/tcp.rs"
      via: "grep -cE 'serde_json::to_vec\\(payload\\)|serde_json::to_vec\\(r\\.payload\\)'"
      pattern: "serde_json::to_vec\\(payload\\)"
---

<objective>
Wave 0 plants the TDD RED scaffolding for Phase 59. After this wave lands,
`cargo test --release --tests -- --ignored` will include 4 new integration
tests (3 `#[ignore = "59-W{N}"]`'d, 1 always-on regression guard), 2 new
shell scripts, and 1 REQUIREMENTS.md row. Every Phase-59 plan (01-04) flips
one or more of these RED markers to GREEN; phase close (Wave 4) verifies
all are GREEN except the D-B3 `json_over_tcp_still_accepted` which stays
GREEN throughout (regression guard).

Purpose: establishes the measurable contract for TPC-PERF-09 BEFORE any
src/ change lands. Matches Phase 55/56/57/58 Wave-0 TDD pattern.

Output: 7 files added, 0 production code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-binary-wire-format-for-push/59-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-tokio-connection-handling-rewrite/58-CONTEXT.md
@.planning/phases/58-tokio-connection-handling-rewrite/58-00-PLAN.md
@.planning/phases/55-stream-table-cascade-crossshard-and-source-tables/55-00-PLAN.md
@src/server/protocol.rs
@src/server/tcp.rs
@src/shard/thread.rs
@scripts/samply-probe-tokio-share.sh

<interfaces>
<!-- Extracted from src/server/protocol.rs (existing surface the tests reference) -->

Opcode constants (Phase 59 adds 0x18):
```rust
pub const OP_PUSH: u8 = 0x01;          // existing — binary push
pub const OP_REGISTER: u8 = 0x05;      // existing — needed to register test stream
pub const OP_DELETE_TABLE_BATCH: u8 = 0x17;  // existing — last-used opcode
pub const OP_NEGOTIATE_WIRE_FORMAT: u8 = 0x18;  // NEW — Phase 59 Wave 2 will define

pub const STATUS_OK: u8 = 0x00;
pub const STATUS_ERROR: u8 = 0x01;
```

Binary event type tags (existing — tests MUST use these):
```rust
pub const TYPE_NULL: u8 = 0x00;
pub const TYPE_BOOL: u8 = 0x01;
pub const TYPE_I64: u8 = 0x02;
pub const TYPE_F64: u8 = 0x03;
pub const TYPE_STR: u8 = 0x04;
```

Wire encoding helpers (existing — tests use verbatim):
```rust
pub fn encode_frame(opcode: u8, payload: &[u8]) -> Vec<u8>;  // [u32 BE len][opcode][payload]
pub fn write_string(s: &str) -> Vec<u8>;                     // [u16 BE len][utf-8]
pub fn decode_event_binary(buf: &mut &[u8]) -> Result<serde_json::Value, BeavaError>;
```

ShardEvent (current shape before Phase 59 extends it in Wave 1):
```rust
pub struct ShardEvent {
    pub payload: bytes::Bytes,        // NOTE: Wave 1 adds payload_fmt sibling
    pub stream_name: Arc<str>,
    pub shard_hint: u32,
    pub response_tx: Option<tokio::sync::oneshot::Sender<ShardResult>>,
    pub op: ShardOp,
}
```

JSON round-trip WASTE locations (Wave-0 grep invariant enforces these EXIST now):
- `src/server/tcp.rs:2159` — `bytes::Bytes::from(serde_json::to_vec(payload).unwrap_or_default())`
- `src/server/tcp.rs:2538` — `bytes::Bytes::from(serde_json::to_vec(r.payload).unwrap_or_default())`
- `src/shard/thread.rs:724` — `let payload: serde_json::Value = match serde_json::from_slice(&event.payload) { ... }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TPC-PERF-09 row to REQUIREMENTS.md + write 4 RED integration tests + 2 probe scripts</name>
  <files>
    .planning/REQUIREMENTS.md,
    tests/wire_negotiation_handshake.rs,
    tests/binary_push_bytes_passthrough.rs,
    tests/json_over_tcp_still_accepted.rs,
    tests/protocol_binary_decode_fuzz.rs,
    scripts/samply-probe-json-share.sh,
    scripts/verify-no-tcp-json-reserialize.sh
  </files>
  <action>
  Per D-D1 (59-CONTEXT.md), plant the RED set atomically.

  ### 1. REQUIREMENTS.md — TPC-PERF-09 row + traceability

  Add a new section under `### TPC-PERF (continued) — Phase 58` (after
  TPC-PERF-08's line 79), labeled `### TPC-PERF (continued) — Phase 59`,
  with a single checkbox row for TPC-PERF-09. Copy the Phase 58
  TPC-PERF-08 row's format verbatim for style consistency. Row text:

  ```
  - [ ] **TPC-PERF-09**: The TCP PUSH hot path MUST NOT re-serialize
    events via `serde_json::to_vec` between the listener's
    `parse_command` and the shard thread's engine call. Every TCP
    OP_PUSH / OP_PUSH_ASYNC / OP_PUSH_BATCH frame's binary wire body
    (already in the established Beava TYPE_NULL/BOOL/I64/F64/STR shape
    per `decode_event_binary`, production since Phase 11) carries
    `bytes::Bytes` end-to-end from `src/server/tcp.rs::parse_command`
    → `ShardEvent.payload` → `shard_event_loop` → `push_with_cascade_on_shard`
    with ZERO intermediate `serde_json::Value` round-trip. HTTP PUSH
    path unchanged (stays JSON per D-A4). New opcode
    `OP_NEGOTIATE_WIRE_FORMAT = 0x18` advertises capability bit
    `WIRE_BINARY_PASSTHROUGH = 1 << 0`; JSON-over-TCP OP_PUSH backward
    compat window ≥ 1 release cycle (D-B3). Server-side DoS cap via
    `BEAVA_MAX_PAYLOAD_BYTES` env (default 1 MiB; D-E1). Observable
    gates: (a) `serde_json::*` + `from_utf8` combined ≤ 3% of samply
    leaf samples on TCP PUSH (D-D3); (b) `bash scripts/verify-no-tcp-json-reserialize.sh`
    exit 0 (D-C3); (c) ≥ +10% EPS vs Phase 58 C1 baseline on same
    hardware (D-D2 floor 1,514,095 EPS = 1,376,450 × 1.10); (d) p99
    parity vs Phase 58 30,632.5 µs median-of-p99. Verified by
    `tests/binary_push_bytes_passthrough.rs`,
    `tests/wire_negotiation_handshake.rs`,
    `tests/json_over_tcp_still_accepted.rs`,
    `tests/protocol_binary_decode_fuzz.rs`, and
    `scripts/samply-probe-json-share.sh`. (Phase 59)
  ```

  Then update the Traceability table at the bottom of REQUIREMENTS.md:
  add `| 59 | binary-wire-format-for-push | TPC-PERF-09 |` as the last
  row. Update the "Coverage" footnote count from `37/37` to `38/38`
  (add 1 for TPC-PERF-09).

  ### 2. tests/wire_negotiation_handshake.rs — RED until Wave 2

  Integration test that:
  1. Boots beava server with BEAVA_SHARDS=2 on an ephemeral port via
     the existing test harness pattern from
     `tests/tcp_ingest_routing.rs` (follow that file's boot boilerplate
     verbatim — `ConcurrentAppState::new_test` / `spawn_shard_threads`
     / `run_tcp_server_with_listener`).
  2. Opens a TCP connection, sends `OP_NEGOTIATE_WIRE_FORMAT = 0x18`
     frame with `[u32 BE capability_bits=0x00000001][u16 BE client_version=2]`.
  3. Reads response frame. Expects `STATUS_OK + [u32 BE server_bits][u16 BE server_version]`
     where `server_bits & 0x01 == 1` (WIRE_BINARY_PASSTHROUGH advertised).
  4. Asserts `server_version >= 2`.

  Mark the test `#[ignore = "59-W2"]` per D-D1. The test will fail today
  with `"unknown opcode 0x18"` on the server side (current protocol.rs
  only defines up to 0x17); that's the RED contract. Wave 2 wires the
  opcode → test flips GREEN → marker removed.

  ### 3. tests/binary_push_bytes_passthrough.rs — RED until Wave 1

  Integration test that:
  1. Boots beava with BEAVA_SHARDS=1 + a registered test stream
     `Txns` with one `i64` field `amount` (use the same REGISTER
     shape as `tests/tcp_ingest_routing.rs`).
  2. Sends an `OP_PUSH` frame with binary body: TYPE_I64 encoding
     of `amount=100`. Use `encode_frame(OP_PUSH, payload)` +
     `write_string("Txns")` + manual binary tail (`[u16 BE 1 field][varint key_len "amount"]["amount" bytes][TYPE_I64 0x02][i64 BE 100]`).
  3. Reads STATUS_OK response.
  4. Asserts `state.events_total.load(Relaxed) == 1`.
  5. **PROBE ASSERTION (RED):** use `std::backtrace::Backtrace` /
     a scoped panic hook to observe at least one `serde_json::to_vec`
     frame was traversed during the push. Implement via a
     `thread_local!` counter that the Wave 0 test can bump from a
     `#[cfg(test_json_reserialize_probe)]` hook; for Wave 0 the probe
     is a SIMPLER grep-based runtime invariant — spawn a subprocess
     with `BEAVA_JSON_PROBE=1` and read a per-process counter
     `beava::server::tcp::json_reserialize_count()` (Wave 1 adds a
     `#[cfg(debug_assertions)]` counter; for Wave 0 the assertion is
     `>= 1` reflecting the current WASTE state).

     **Pragmatic RED instead:** assert on a simpler static grep
     against the binary: the test reads its OWN source file at
     `env!("CARGO_MANIFEST_DIR")/src/server/tcp.rs` and asserts
     `grep -c "serde_json::to_vec(payload)" src/server/tcp.rs >= 1`.
     This is a source-level RED contract — it flips GREEN in Wave 1
     when tcp.rs:2159 is deleted. Document this pragma in the test's
     module docstring.

  Mark `#[ignore = "59-W1"]` per D-D1. Wave 1 flips GREEN by deleting
  the `serde_json::to_vec(payload)` hot-path callsites.

  ### 4. tests/json_over_tcp_still_accepted.rs — GREEN from Wave 0, must stay GREEN

  Integration test (NOT `#[ignore]`d) that:
  1. Boots beava with BEAVA_SHARDS=1 + registered stream `Txns`.
  2. Sends OP_PUSH with a **JSON body** (legacy wire shape):
     `write_string("Txns")` + raw JSON bytes `br#"{"amount":100}"#`.
  3. Expects STATUS_OK + `events_total == 1` + feature read returns
     the correct amount.
  4. Documents in module docstring that this test is the **D-B3 regression
     guard**: Phase 59 server accepts JSON-over-TCP for ≥ 1 release
     cycle. MUST stay GREEN every wave through phase close. If a later
     wave's changes break this test → Wave roll back and re-plan.

  On current HEAD (pre-Wave 1), this test **already passes** because
  the current listener does `decode_event_binary(&mut buf)` and its
  first failure mode is "format byte 0x7b" which the JSON-fallback
  path at `parse_command` has always handled. Wave 0 commit asserts
  current-green.

  ### 5. tests/protocol_binary_decode_fuzz.rs — GREEN from Wave 0 (D-E3)

  Pure unit-level fuzz test. Does NOT boot a server. Uses
  `proptest` (already in dev-deps per Phase 52):
  1. Generate 500 random inputs — mixed valid / truncated / oversized
     `field_count` / invalid type tags / NaN f64s (the existing
     failure modes `decode_event_binary` catches).
  2. Call `decode_event_binary(&mut &bytes[..])` and assert: either
     returns `Ok(Value::Object(_))` OR returns `Err(BeavaError::Protocol(_))`.
     **No panics allowed.** Currently the decoder is panic-free; this
     test is a regression guard for Wave 1's codec-module refactor
     (if it introduces any new decode path, the fuzz must stay GREEN).

  Use existing proptest dev-dep (Phase 52). 500 cases is enough for
  the baseline; Wave 1 can bump to 5000 if a real fuzz harness lands.

  ### 6. scripts/samply-probe-json-share.sh — new (D-D3)

  Mirror of `scripts/samply-probe-tokio-share.sh`. Contract:
  - Input: no args. Reads `samply-after/beava_ingest.top.txt`
    (same location as 58).
  - Output: single stdout line `JSON_SHARE_PCT=<decimal>` + exit 0
    on success, exit 1 on harness-unable.
  - Computation: sum leaf self-percent for frames matching
    `serde_json::` OR `std::str::from_utf8` OR `format_escaped_str`.
  - **Coverage sentinel (D-D1 adopted from 58-NEXT #1):** if the
    top.txt does not contain at least one frame matching
    `handle_push_batch|handle_push_core|decode_event_binary`,
    emit `JSON_SHARE_PCT=SENTINEL_FAILED` and exit 2. Prevents the
    harness-unable false-pass that Phase 58 SC-1 hit.

  Copy 58's script verbatim, then substitute `tokio::runtime::task`
  → the above serde patterns. Add `set -euo pipefail`. `chmod +x`.

  ### 7. scripts/verify-no-tcp-json-reserialize.sh — new (D-C3)

  Grep-ZERO invariant enforcer. Contract:
  - Hits `grep -cE 'serde_json::to_vec\(payload\)|serde_json::to_vec\(r\.payload\)' src/server/tcp.rs`.
  - If count > 0: print the offending file + line numbers via
    `grep -nE ...`, exit 1.
  - If count == 0: print `OK: zero TCP JSON re-serialize patterns in src/server/tcp.rs (excluding comments)` and exit 0.
  - Excludes comment lines (`grep -v '^\s*//'` pre-filter).
  - MUST also verify the HTTP path still has the JSON round-trip
    (`grep -cE 'serde_json::to_vec' src/server/http_ingest.rs >= 1`)
    — D-A4 says HTTP stays JSON; if HTTP loses its JSON path this
    invariant also fails (prevents accidental over-scope).

  Copy the pattern from `scripts/verify-no-dashmap.sh` + the Phase 54
  grep-ZERO scripts. `chmod +x`. Expected exit 1 on Wave 0 HEAD (RED);
  exit 0 from Wave 1 onward.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/petrpan26/work/tally && cargo check --release --tests 2>&1 | tail -5 && grep -c "TPC-PERF-09" .planning/REQUIREMENTS.md && ls -la tests/wire_negotiation_handshake.rs tests/binary_push_bytes_passthrough.rs tests/json_over_tcp_still_accepted.rs tests/protocol_binary_decode_fuzz.rs scripts/samply-probe-json-share.sh scripts/verify-no-tcp-json-reserialize.sh'</automated>
  </verify>
  <done>
  - `.planning/REQUIREMENTS.md` contains "TPC-PERF-09" row + traceability update.
  - 4 `tests/*.rs` files compile via `cargo check --release --tests`.
  - `cargo test --release --test json_over_tcp_still_accepted` exits 0 GREEN.
  - `cargo test --release --test protocol_binary_decode_fuzz` exits 0 GREEN.
  - `cargo test --release --test wire_negotiation_handshake` shows 1 ignored (RED latent).
  - `cargo test --release --test binary_push_bytes_passthrough` shows 1 ignored (RED latent).
  - `bash scripts/samply-probe-json-share.sh` exits 0 or 2 (0 if top.txt exists; 2 SENTINEL_FAILED if not — Wave 4 produces real top.txt).
  - `bash scripts/verify-no-tcp-json-reserialize.sh` exits 1 (RED — WASTE still present on Wave-0 HEAD).
  - Both scripts are `chmod +x`.
  - Commit atomically.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add always-on ConcurrentAppState counter fields + Wave-0 grep invariants committed</name>
  <files>src/server/tcp.rs</files>
  <action>
  Per the Phase 58 Wave-0 precedent (adds `accept_threads_spawned_total` +
  `inline_handler_events_total` always-on counters so Wave 1+ signals have
  measurable zero-state baseline), Phase 59 adds TWO always-on counters:

  1. `json_reserialize_count_total: AtomicU64` — counter incremented every
     time the hot path calls `serde_json::to_vec(payload)` on the shard-bound
     path. Current tcp.rs:2159 + :2538 fire this counter. Wave 1 removes the
     fires; Wave 4 samply verifies counter stays at 0 after a push load.
  2. `binary_passthrough_count_total: AtomicU64` — counter incremented every
     time Phase 59's Bytes-passthrough path is taken (Wave 1 adds the fires).
     Counter is 0 on Wave 0 HEAD; `>= 1` after Wave 1 integration.

  Add to `ConcurrentAppState` struct body (follow Phase 58's pattern at
  the `accept_threads_spawned_total` definition location — grep for that
  constant to find the correct block). Initialize both to `AtomicU64::new(0)`
  in every `ConcurrentAppState::new*` constructor. Emit both via
  `/debug/status` JSON output using the existing `serde_json::json!` macro
  (grep for `accept_threads_spawned_total` in the status handler to find
  the right spot; match its format).

  **Invariant to verify Wave 0 close:**
  - `grep -c "json_reserialize_count_total" src/server/tcp.rs >= 3`
    (struct def + 2 `.fetch_add(1, Relaxed)` call sites at tcp.rs:2159
    area + tcp.rs:2538 area).
  - `grep -c "binary_passthrough_count_total" src/server/tcp.rs >= 1`
    (struct def only — no `.fetch_add(1)` call sites yet; Wave 1 adds).

  No tests added by this task — the Wave-0 tests in Task 1 cover the
  verification surface. This task is pure always-on instrumentation so
  Wave 1+'s src/ changes have a numeric-counter signal.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/petrpan26/work/tally && cargo build --release --lib 2>&1 | tail -3 && grep -c "json_reserialize_count_total" src/server/tcp.rs && grep -c "binary_passthrough_count_total" src/server/tcp.rs && cargo test --release --lib 2>&1 | tail -1'</automated>
  </verify>
  <done>
  - `src/server/tcp.rs` compiles (`cargo build --release --lib` exit 0).
  - `grep -c "json_reserialize_count_total" src/server/tcp.rs` ≥ 3.
  - `grep -c "binary_passthrough_count_total" src/server/tcp.rs` ≥ 1.
  - `cargo test --release --lib` exit 0 with baseline unchanged (812/0/35 fjall).
  - No behavior regression — counters are fetch_add-only, no branch.
  - Commit atomically.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→TCP server | Untrusted binary wire bytes enter via `decode_event_binary` (existing path); Phase 59 adds ONE new opcode (0x18) + 1 new env var cap. |
| Wave 0 scripts → shell | `samply-probe-json-share.sh` + `verify-no-tcp-json-reserialize.sh` are operator-run; no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-00-01 | Tampering | `tests/binary_push_bytes_passthrough.rs` grep-self assertion | mitigate | Assertion reads `env!("CARGO_MANIFEST_DIR")/src/server/tcp.rs` (compile-time path, not runtime arg); attacker cannot redirect via symlink mid-run. |
| T-59-00-02 | Denial-of-service | `protocol_binary_decode_fuzz.rs` — a badly-seeded proptest could run 500× with a pathological input | accept | proptest's default time budget is 10s/test; worst case adds 10s to `cargo test`. Acceptable — this is the point of the fuzz. |
| T-59-00-03 | Information disclosure | REQUIREMENTS.md TPC-PERF-09 row's Phase 58 baseline citation (1,376,450 EPS, 30,632.5 µs p99) | accept | Numbers are from public-committed Phase 58 artifacts; no secrets. |
| T-59-00-04 | Elevation of privilege | Wave 0 adds no opcode dispatch (that's Wave 2); no new auth surface | N/A | No new auth paths this wave. |
</threat_model>

<verification>
```bash
# RED signals expected at Wave 0 close:
cargo test --release --test wire_negotiation_handshake 2>&1 | tail -1
# → test result: ok. 0 passed; 0 failed; 1 ignored  (RED contract)

cargo test --release --test binary_push_bytes_passthrough 2>&1 | tail -1
# → test result: ok. 0 passed; 0 failed; 1 ignored  (RED contract)

bash scripts/verify-no-tcp-json-reserialize.sh; echo exit=$?
# → grep hits tcp.rs:2159 + :2538 named + exit=1  (RED contract)

# GREEN signals expected at Wave 0 close:
cargo test --release --test json_over_tcp_still_accepted 2>&1 | tail -1
# → test result: ok. 1 passed; 0 failed; 0 ignored  (D-B3 regression guard)

cargo test --release --test protocol_binary_decode_fuzz 2>&1 | tail -1
# → test result: ok. N passed; 0 failed; 0 ignored  (D-E3 fuzz)

cargo test --release --lib 2>&1 | tail -1
# → test result: ok. 812 passed; 0 failed; 35 ignored  (baseline preserved)

grep -c "TPC-PERF-09" .planning/REQUIREMENTS.md
# → >= 2 (traceability row + text-row)

grep -c "json_reserialize_count_total" src/server/tcp.rs
# → >= 3 (struct def + 2 fire sites — existing WASTE callsites bumped)

grep -c "binary_passthrough_count_total" src/server/tcp.rs
# → >= 1 (struct def; Wave 1 adds fire sites)
```
</verification>

<success_criteria>
- REQUIREMENTS.md has TPC-PERF-09 row with measurable gates (D-D2, D-D3, D-C3, D-D4).
- 4 new test files compile + run with expected RED/GREEN disposition.
- 2 new scripts (`scripts/samply-probe-json-share.sh` + `scripts/verify-no-tcp-json-reserialize.sh`) run with expected exit codes.
- 2 always-on counters added to `ConcurrentAppState` (zero-init, exposed via /debug/status).
- Lib test baseline preserved: 812/0/35 fjall, 804/0/35 state-inmem.
- No src/ behavior changes — only counter struct-field addition + `.fetch_add(1)` at existing WASTE call sites.
</success_criteria>

<output>
After completion, create `.planning/phases/59-binary-wire-format-for-push/59-00-SUMMARY.md`
following `$HOME/.claude/get-shit-done/templates/summary.md` structure. Summary MUST
include: counter baseline (both at 0), RED/GREEN test disposition matrix, REQUIREMENTS.md
row diff, probe-script exit-code table, Wave-0 grep invariant baseline (RED for
`verify-no-tcp-json-reserialize.sh`), next-wave handoff (Wave 1 implements Bytes
passthrough → flips `binary_push_bytes_passthrough` + `verify-no-tcp-json-reserialize` to GREEN).
</output>
