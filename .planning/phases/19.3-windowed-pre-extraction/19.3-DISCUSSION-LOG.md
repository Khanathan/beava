# Phase 19.3 — Discussion Log

**Captured:** 2026-04-28
**Phase:** 19.3-windowed-pre-extraction

This is a human-reference audit log of the discuss-phase session. Downstream agents read CONTEXT.md, not this file.

---

## Areas discussed

Four gray areas presented via AskUserQuestion (all four selected for batch discussion since the phase scope is well-defined by ROADMAP.md and `19.2-INVESTIGATION.md` already enumerated the implementation paths).

### Area 1: Sub-goal 19.3-B specialize-arms scope

**Question:** Which windowed kinds get the inner-state inline fast-path (skipping AggOp::update_with_row dispatch)?

**Options presented:**
1. Count + Sum only (Recommended) — tightest scope, ~1,100 ns/event saved
2. Count + Sum + Min/Max — adds 2 trivial arms, ~400 ns more
3. **All trivial Tier-1 windowed (Count/Sum/Min/Max/Avg/Variance) ← SELECTED** — maximal Tier-1 coverage, ~2,000 ns ceiling
4. Skip 19.3-B entirely — defer specialization

**User selection:** Option 3 (full Tier-1 windowed).

**Why this matters:** User chose to expand scope from the recommended Count+Sum-only. Variance inline path is non-trivial (Welford state); CONTEXT.md flags this for the planner with a bit-identical state cross-check acceptance criterion.

### Area 2: Landing order

**Question:** Sequential proof gate or one wave?

**Options presented:**
1. **Sequential: A → measure → B → measure → C (Recommended) ← SELECTED**
2. Parallel: A + B + C in one wave
3. Hybrid: A solo, B+C bundled

**User selection:** Option 1 (sequential).

**Why this matters:** Phase 19.2's microbench-wins-without-EPS-lift pattern motivated the gating discipline. CONTEXT.md encodes 75%-of-predicted measurement gates between sub-goals; if a sub-goal's measured lift undershoots the floor, planning re-evaluates before proceeding.

### Area 3: ExtractedFields hoist storage (19.3-C)

**Question:** Storage shape for per-event extracted buffer?

**Options presented:**
1. **Reused per-thread Vec<Value> with clear() (Recommended) ← SELECTED**
2. Per-source-schema cached buffer
3. Per-event arena allocation

**User selection:** Option 1 (reused per-thread Vec).

**Why this matters:** Single-threaded apply (locked 2026-04-26) makes per-thread storage sufficient. Other options add complexity without measurable benefit.

### Area 4: WindowedOp::update_at signature + bench scope

**Question:** How invasive — signature shape + criterion bench coverage?

**Options presented:**
1. **Forward &ExtractedFields + field_idx; warm-key bench only (Recommended) ← SELECTED**
2. Forward &ExtractedFields; add cold-key sibling bench too
3. Bucket-local field_idx recompute

**User selection:** Option 1.

**Why this matters:** Mirrors the non-windowed `update_at` signature exactly — clean protocol extension. Cold-key bench deferred (would add CI time without distinct signal Phase 19.2's `apply_path/cold_key` doesn't already cover); deferred ideas section in CONTEXT.md captures this.

---

## Deferred ideas

- Cold-key windowed criterion bench (defer until cold-path regression observed)
- EventTypeMix windowed specialization (out of scope — currently non-windowed)
- WAL group-commit batching (potential Phase 19.4 if 19.3 leaves residual gap)
- `io_uring` apply-path (out of scope — Linux Xeon production hardware not v0 target)

---

## Claude's discretion (not asked)

- **Plan structure preview:** 5 plans (bench amendment + 19.3-A + 19.3-B + 19.3-C + rebaseline/verification) with measurement gates. Documented in CONTEXT.md "Plan structure (preview for planner)" section. Planner may consolidate but must preserve the gates.
- **Open questions for researcher:** 4 items captured in CONTEXT.md (per-thread buffer location, callers of `WindowedOp::update_with_row` outside `agg_op.rs`, criterion bench windowed-registry support, RetractableOp protocol extension). Researcher answers these; user does not need to.
- **TDD discipline reminder:** CONTEXT.md flags that the criterion bench baseline must be a "test:" red-commit BEFORE 19.3-A's "feat:" green-commit, per CLAUDE.md TDD rule.
- **Anti-pattern checks:** 6 enumerated mandatory checks plan-checker must enforce (wrapper-bypass, verifier-conjecture-without-measurement, same-key batching, cross-event reordering, multi-thread apply, sonnet "deferred / TODO / if absent" smell).

---

## Prior decisions carried forward

From Phase 19.2 CONTEXT.md (`19.2-CONTEXT.md`):
- D-01 indexed array pre-extraction: 19.3 extends this protocol unchanged.
- D-02a/b hashers: untouched by 19.3.
- D-03 EntityKey hybrid: untouched by 19.3.
- D-04 cluster shape (split-by-agg_id): untouched by 19.3.

From 2026-04-26 architectural lock:
- Single-threaded apply forever — justifies per-thread `Vec<Value>` storage in D-03.
