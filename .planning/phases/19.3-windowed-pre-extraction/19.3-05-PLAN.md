---
phase: 19.3-windowed-pre-extraction
plan: 05
type: execute
wave: 5
depends_on: ["19.3-02", "19.3-03", "19.3-04"]
files_modified:
  - .planning/throughput-baselines.md
  - .planning/perf-baselines.md
  - .planning/phases/19.3-windowed-pre-extraction/19.3-VERIFICATION.md
autonomous: true
requirements:
  - SC-19.3-PHASE-EPS: "fraud-team K=10k zipfian N=1M ≥ 100k EPS (vs 70,639 post-19.2)"
  - SC-19.3-LADDER-NO-REGRESSION: "no regression > 10% on small/medium/large/large_phase9 ladder cells"
  - SC-19.3-BENCH-LIFT: "apply_path/warm_key/14_aggs_windowed shows ≥ 4× speedup vs Plan 19.3-01 baseline"
  - SC-19.3-LIVE-TRACE: "live BEAVA_TRACE_AGG_TIMING run produces full per-stage + per-AggKind tables"
  - SC-19.3-DUAL-VERIFICATION: "verification includes BOTH criterion microbench AND live trace (Phase 19.2 anti-pattern remediation)"
  - SC-19.3-ROW-GET-COUNT: "grep -c 'row.get(' on apply-time hot paths in agg_state.rs ≤ 5 (was 12)"
  - SC-19.3-NO-API-CHANGE: "no public API surface change; internal refactor only"
must_haves:
  truths:
    - "Throughput ledger has new section `## 1M-event blast (rebaseline 19.3)` with rows for fraud-team + small/medium/large/large_phase9 × tcp+msgpack"
    - "perf-baselines.md has post-19.3 row for apply_path/warm_key/14_aggs_windowed showing ≥ 4× speedup vs Plan 19.3-01 baseline"
    - "VERIFICATION.md exists with PASS verdict (or PASS-WITH-DEFICIT / FAIL with explicit blockers)"
    - "VERIFICATION.md contains BOTH criterion bench rows AND live trace per-stage tables (Phase 19.2 lesson — criterion alone is insufficient)"
    - "fraud-team K=10k zipfian N=1M EPS ≥ 100,000"
    - "Anti-pattern checks all pass (wrapper-bypass enumeration, no same-key batching, no cross-event reorder, no multi-thread apply, no TODO smell)"
  artifacts:
    - path: ".planning/throughput-baselines.md"
      provides: "rebaseline 19.3 ledger section"
      contains: "1M-event blast (rebaseline 19.3)"
    - path: ".planning/phases/19.3-windowed-pre-extraction/19.3-VERIFICATION.md"
      provides: "phase verdict"
      contains: "Verdict:"
  key_links:
    - from: "Verification verdict"
      to: "fraud-team EPS measurement"
      via: "live bench run on bench-v18 binary"
      pattern: "fraud-team.*zipfian.*EPS"
threat_model:
  notes: "Verification only. No code changes. Risk = silent FAIL masquerading as PASS — mitigated by mandatory dual measurement (criterion + live trace) per Phase 19.2 lesson."
---

<objective>
Verify Phase 19.3 ships its goal: fraud-team K=10k zipfian apply-path drops from 70,639 EPS (post-19.2) → ≥ 100k EPS, agg-stage from 14,059 ns → ≤ 8,500 ns, with NO regression > 10% on the small/medium/large/large_phase9 ladder cells.

**Mandatory: dual measurement.** Phase 19.2's PASS-WITH-DEFICIT verdict was caused by verifier-conjecture-without-measurement (per `19.2-VERIFICATION.md` 2026-04-28 correction). Phase 19.3 verification MUST run BOTH:
1. Criterion microbench (`apply_path/warm_key/14_aggs_windowed` — created by Plan 19.3-01)
2. Live trace (`BEAVA_TRACE_APPLY_TIMING=1 BEAVA_TRACE_AGG_TIMING=1` against fraud-team K=10k zipfian per `19.2-INVESTIGATION.md` §5)

Criterion alone is INSUFFICIENT. The verification document is rejected if it relies on conjecture for any per-stage or per-AggKind threshold.

Purpose: ship the phase with measured, defensible results.
Output: `19.3-VERIFICATION.md` with verdict + appended ledger sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.3-windowed-pre-extraction/19.3-CONTEXT.md
@.planning/phases/19.3-windowed-pre-extraction/19.3-RESEARCH.md
@.planning/phases/19.3-windowed-pre-extraction/19.3-02-SUMMARY.md
@.planning/phases/19.3-windowed-pre-extraction/19.3-03-SUMMARY.md
@.planning/phases/19.3-windowed-pre-extraction/19.3-04-SUMMARY.md
@.planning/phases/19.2-big-apply-path-optimization/19.2-INVESTIGATION.md
@.planning/phases/19.2-big-apply-path-optimization/19.2-VERIFICATION.md
@.planning/throughput-baselines.md
@.planning/perf-baselines.md
</context>

<tasks>

<task type="auto">
  <name>Task 5.a: Run full ladder + fraud-team rebaseline</name>
  <read_first>
    - .planning/throughput-baselines.md (last 200 lines — for ledger row format and most recent hw-class)
    - .planning/phases/19.2-big-apply-path-optimization/19.2-VERIFICATION.md (for the post-19.2 numbers to compare against)
    - CLAUDE.md §Conventions → end-to-end throughput contract (Phase 8+ requires throughput-baselines.md row per (pipeline-shape, transport))
  </read_first>
  <action>
    1. Build a release binary: `cargo build --release -p beava-bench --bin beava-bench-v18`.
    2. Run the rebaseline matrix per CLAUDE.md Phase 8+ contract — append rows for each (pipeline-shape, transport) tuple. Required cells:
       - small × tcp × msgpack × continuous-pipeline
       - medium × tcp × msgpack × continuous-pipeline
       - large × tcp × msgpack × continuous-pipeline
       - large_phase9 × tcp × msgpack × continuous-pipeline
       - **fraud-team × tcp × msgpack × continuous-pipeline × K=10k zipfian N=1M (the primary tuning bench per memory `project_fraud_team_primary_bench`)**
    3. For each cell, capture: EPS mean, p50/p99 latency, wall-clock duration. Use the `19.2-INVESTIGATION.md` §5 reproduction command shape.
    4. Append a new section `## 1M-event blast (rebaseline 19.3)` to `.planning/throughput-baselines.md` with the 5 rows. Schema matches the existing `## 1M-event blast (rebaseline 19.2)` section above it (read those rows for column shape).
    5. Compute per-cell delta vs Phase 19.2 baseline (`rebaseline 19.2` section). **Acceptance per cell:**
       - fraud-team: EPS ≥ 100,000 (PASS gate). If < 100,000, verdict is PASS-WITH-DEFICIT or FAIL.
       - small/medium/large/large_phase9: no regression > 10% in EPS (warn at 10%, block at 25% per CLAUDE.md Performance Discipline).
    6. Commit: `chore(19.3-05): rebaseline 19.3 throughput ledger`
  </action>
  <verify>
    <automated>grep -c '1M-event blast (rebaseline 19.3)' .planning/throughput-baselines.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '1M-event blast (rebaseline 19.3)' .planning/throughput-baselines.md` ≥ 1
    - The new section contains ≥ 5 ledger rows (small, medium, large, large_phase9, fraud-team).
    - fraud-team row shows EPS ≥ 100,000.
    - No non-fraud-team cell shows EPS regression > 10% vs `rebaseline 19.2` row.
    - Commit message starts with `chore(19.3-05):`.
  </acceptance_criteria>
  <done>Ledger appended; cell deltas computed.</done>
</task>

<task type="auto">
  <name>Task 5.b: Run live trace + record per-stage and per-AggKind tables</name>
  <read_first>
    - .planning/phases/19.2-big-apply-path-optimization/19.2-INVESTIGATION.md §5 (full reproduction command + python parser)
    - .planning/phases/19.2-big-apply-path-optimization/19.2-INVESTIGATION.md §1 (per-AggKind table format — replicate this shape for the post-19.3 table)
  </read_first>
  <action>
    1. Run live trace per `19.2-INVESTIGATION.md` §5: `BEAVA_TRACE_APPLY_TIMING=1 BEAVA_TRACE_AGG_TIMING=1` against fraud-team K=10k zipfian N=100k events.
    2. Run the python parser at the bottom of §5 to produce:
       - Per-stage table (parse, lookup, validate, wal_build, wal_append, agg, bookkeeping, TOTAL)
       - Per-AggKind table (calls/evt, ns/call, ns/evt, share — same shape as `19.2-INVESTIGATION.md` §1 Table)
    3. Save raw trace output to `/tmp/beava-trace-19.3-final.txt` and the parsed tables to `19.3-VERIFICATION.md` (created in Task 5.c).
    4. **Verification thresholds (must all hit; record verdict per threshold):**
       - agg-stage mean ≤ 8,500 ns (target post-19.3-C; was 14,059 post-19.2)
       - features-stage mean ≤ 11,000 ns (was 17,718 post-19.2)
       - TOTAL apply mean ≤ 12,500 ns (was 19,737 post-19.2)
       - per-AggKind windowed Count ≤ 30 ns/call
       - per-AggKind windowed Sum ≤ 30 ns/call
       - per-AggKind windowed Min/Max/Avg/Variance ≤ 60 ns/call
    5. Commit: `chore(19.3-05): record live trace per-stage + per-AggKind tables`
  </action>
  <verify>
    <automated>ls /tmp/beava-trace-19.3-final.txt &amp;&amp; grep -c 'TRACE_AGG ns:' /tmp/beava-trace-19.3-final.txt</automated>
  </verify>
  <acceptance_criteria>
    - Trace file `/tmp/beava-trace-19.3-final.txt` exists and contains ≥ 100,000 `TRACE_AGG ns:` lines.
    - Per-stage table captured (printed/recorded for use in Task 5.c).
    - Per-AggKind table captured.
    - All 6 numeric thresholds met (or, if missed, recorded with magnitude of miss for Task 5.c verdict computation).
  </acceptance_criteria>
  <done>Live trace data captured; thresholds evaluated.</done>
</task>

<task type="auto">
  <name>Task 5.c: Re-run criterion bench; append post-19.3 row to perf-baselines.md</name>
  <read_first>
    - .planning/perf-baselines.md (Plan 19.3-01's pre-19.3 baseline row + adjacent row format)
  </read_first>
  <action>
    1. Run `cargo bench -p beava-core --bench apply_path_bench -- apply_path/warm_key/14_aggs_windowed`. Capture time/iter.
    2. Append a new row in `.planning/perf-baselines.md` (same hw-class section as Plan 19.3-01's baseline):
       ```
       | apply_path/warm_key/14_aggs_windowed | <captured ns> | <stddev> | post-19.3 (≥ 4× speedup vs pre-19.3 baseline) | 2026-04-28 | <commit-sha> |
       ```
    3. Compute speedup multiplier vs Plan 19.3-01 baseline. Acceptance: ≥ 4×. **If < 4×, verdict moves to PASS-WITH-DEFICIT or FAIL.**
    4. Commit: `chore(19.3-05): record post-19.3 criterion baseline`
  </action>
  <verify>
    <automated>grep -c 'post-19.3' .planning/perf-baselines.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'post-19.3' .planning/perf-baselines.md` ≥ 1
    - New row's time/iter is ≤ 25% of Plan 19.3-01 baseline (≥ 4× speedup).
    - 10%/25% regression check vs prior phases passes (this should be a strict improvement, not a regression).
  </acceptance_criteria>
  <done>perf-baselines.md updated with post-19.3 measurement.</done>
</task>

<task type="auto">
  <name>Task 5.d: Anti-pattern enumeration sweep</name>
  <read_first>
    - .planning/phases/19.3-windowed-pre-extraction/19.3-CONTEXT.md "Anti-patterns this phase must avoid" (six items)
  </read_first>
  <action>
    Run each of the six anti-pattern checks and record the result. ALL must pass for verdict PASS:
    1. **Wrapper-bypass enumeration:** `grep -c 'row.get(' crates/beava-core/src/agg_state.rs` — must be ≤ 5 (was 12 per RESEARCH.md R6). Also confirm `WindowedOp` is the only `AggOp` wrapper variant: `grep -E 'enum AggOp' crates/beava-core/src/agg_op.rs && grep -c 'Box<WindowedOp>' crates/beava-core/src/agg_op.rs` ≥ 1; `grep -rn 'RetractableOp' crates/` returns 0 hits.
    2. **Verifier conjecture without measurement:** verify Tasks 5.a + 5.b + 5.c all ran AND the verification document references real measured numbers (not predicted/conjectured). If any threshold lacks a backing trace/bench output, FAIL the check.
    3. **Same-key sketch batching:** `grep -rE 'batch.*sketch|same.key.*batch|sketch.*batch' crates/beava-core/src/` returns 0 hits in Plan 19.3 modified files (agg_windowed.rs, agg_op.rs, agg_apply.rs, registry.rs, agg_state.rs).
    4. **Cross-event aggregation reordering:** read `agg_apply.rs` modifications — confirm event ordering is preserved (no new sorting / reorder of `descs` based on event payload).
    5. **Multi-thread apply parallelism:** `grep -E 'spawn|tokio::spawn|rayon|par_iter' crates/beava-core/src/agg_windowed.rs crates/beava-core/src/agg_apply.rs` — must return 0 hits introduced by Phase 19.3.
    6. **Sonnet "deferred / TODO / if absent" smell:** `grep -E 'TODO|FIXME|todo!\(\)|unimplemented!\(\)|deferred to follow-up' crates/beava-core/src/agg_windowed.rs crates/beava-core/src/agg_op.rs crates/beava-core/src/agg_apply.rs crates/beava-core/src/registry.rs` — must return 0 hits introduced by Phase 19.3 commits (use `git log --oneline 19.2..HEAD -- <files>` to scope).
    Record each check's result in `19.3-VERIFICATION.md` (Task 5.e).
  </action>
  <verify>
    <automated>grep -c 'row.get(' crates/beava-core/src/agg_state.rs</automated>
  </verify>
  <acceptance_criteria>
    - All 6 anti-pattern checks PASS.
    - `grep -c 'row.get(' crates/beava-core/src/agg_state.rs` ≤ 5.
    - `grep -rn 'RetractableOp' crates/` == 0.
    - No TODO/FIXME/todo!()/unimplemented!() introduced by Phase 19.3 commits.
  </acceptance_criteria>
  <done>Anti-pattern sweep complete; results recorded for Task 5.e.</done>
</task>

<task type="auto">
  <name>Task 5.e: Author 19.3-VERIFICATION.md with verdict</name>
  <read_first>
    - .planning/phases/19.2-big-apply-path-optimization/19.2-VERIFICATION.md (for document shape — verdict block, per-threshold table, dual-measurement section)
    - All Plan 19.3 SUMMARYs (01..04)
    - Output of Tasks 5.a, 5.b, 5.c, 5.d
  </read_first>
  <action>
    1. Create `.planning/phases/19.3-windowed-pre-extraction/19.3-VERIFICATION.md` with this structure:
       ```markdown
       # Phase 19.3 — Verification

       **Verdict:** {PASS | PASS-WITH-DEFICIT | FAIL}
       **Captured:** 2026-04-28
       **Predecessor:** Phase 19.2 (PASS-WITH-DEFICIT, commit 2f4cbe6)

       ## Goal recap
       Extend pre-extraction across WindowedOp wrapper. Predicted: 70k → ~115-125k EPS on fraud-team K=10k zipfian; agg-stage 14,059 → ~8,450 ns.

       ## Dual-measurement contract (Phase 19.2 anti-pattern remediation)
       - [x] Criterion microbench: ... (commit, value, speedup vs baseline)
       - [x] Live trace: ... (file path, event count, parser command)

       ## Per-stage table (live trace)
       | Stage | Phase 19.2 baseline | Post-19.3 measured | Delta | Threshold | Verdict |
       |---|---:|---:|---:|---|---|
       | parse | ... | ... | ... | (no regression) | ... |
       | lookup | ... | ... | ... | ... | ... |
       | validate | ... | ... | ... | ... | ... |
       | wal_build | ... | ... | ... | ... | ... |
       | wal_append | ... | ... | ... | ... | ... |
       | **agg** | **14,059 ns** | **<measured>** | ... | **≤ 8,500 ns** | ... |
       | bookkeeping | ... | ... | ... | ... | ... |
       | TOTAL | 19,737 ns | <measured> | ... | ≤ 12,500 ns | ... |

       ## Per-AggKind table (windowed cost — primary 19.3-B verification)
       | Kind | Phase 19.2 ns/call | Post-19.3 ns/call | Threshold | Verdict |
       |---|---:|---:|---|---|
       | Count (windowed) | 181.5 | <m> | ≤ 30 | ... |
       | Sum (windowed) | 199.9 | <m> | ≤ 30 | ... |
       | Min (windowed) | <m> | <m> | ≤ 60 | ... |
       | Max (windowed) | <m> | <m> | ≤ 60 | ... |
       | Avg (windowed) | <m> | <m> | ≤ 60 | ... |
       | Variance (windowed) | <m> | <m> | ≤ 60 | ... |
       | CountDistinct (windowed) | 435.1 | <m> | (no regression) | ... |
       | Percentile (windowed) | 370.4 | <m> | (no regression) | ... |
       | TopK (windowed) | 723.7 | <m> | (no regression) | ... |
       | Entropy (windowed) | 357.9 | <m> | (no regression) | ... |

       ## Throughput ledger delta
       (5 cells: fraud-team, small, medium, large, large_phase9 — copy from rebaseline 19.3 section)

       ## Phase success criteria checklist
       (Ten items from CONTEXT.md success criteria block — each marked PASS/FAIL with backing measurement)

       ## Anti-pattern sweep (Task 5.d output)
       1. Wrapper-bypass enumeration: PASS — grep -c 'row.get(' agg_state.rs = <N> ≤ 5; RetractableOp grep = 0
       2. Verifier conjecture: PASS — both criterion + live trace ran
       3. Same-key sketch batching: PASS
       4. Cross-event reorder: PASS
       5. Multi-thread apply: PASS
       6. TODO/deferred smell: PASS

       ## Welford bit-identical invariant
       Test `variance_windowed_inline_matches_non_windowed`: PASS (Plan 19.3-03 commit <sha>)

       ## Verdict reasoning
       {Explicit reasoning: which thresholds hit, which missed, magnitude of miss}

       ## Next steps
       {If PASS: Phase 19.4 candidates per CONTEXT deferred ideas. If PASS-WITH-DEFICIT/FAIL: explicit remediation plan.}
       ```
    2. Fill in actual numbers from Tasks 5.a, 5.b, 5.c, 5.d.
    3. Compute verdict:
       - **PASS** iff: fraud-team EPS ≥ 100k AND agg-stage ≤ 8,500 ns AND criterion ≥ 4× AND no ladder regression > 10% AND all 6 anti-patterns clean AND Welford invariant green.
       - **PASS-WITH-DEFICIT** iff: fraud-team EPS ≥ 90k but < 100k OR agg-stage ≤ 10,200 but > 8,500 (sub-goal A landed, sub-goals B/C partial). Document deficit and pointer to Phase 19.4.
       - **FAIL** iff: fraud-team EPS < 90k OR Welford invariant red OR any anti-pattern check fails.
    4. Commit: `docs(19.3-05): write Phase 19.3 verification — verdict <V>`
  </action>
  <verify>
    <automated>ls .planning/phases/19.3-windowed-pre-extraction/19.3-VERIFICATION.md &amp;&amp; grep -c 'Verdict:' .planning/phases/19.3-windowed-pre-extraction/19.3-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `19.3-VERIFICATION.md` exists.
    - Document contains: verdict block, dual-measurement section, per-stage table, per-AggKind table, throughput ledger delta, success-criteria checklist, anti-pattern sweep, Welford invariant check, verdict reasoning, next steps.
    - All numeric cells are filled with measured values (no `<m>` placeholders).
    - Verdict is one of PASS / PASS-WITH-DEFICIT / FAIL.
    - Commit message starts with `docs(19.3-05):`.
  </acceptance_criteria>
  <done>Verification document complete; phase ready for `/gsd-verify-work 19.3`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. Verification only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.3.05-01 | T (verifier conjecture masquerading as measurement) | Phase verdict | mitigate | Mandatory dual measurement (criterion + live trace); both raw artifacts cited (commit hashes, /tmp/ trace path); thresholds expressed numerically not qualitatively. Phase 19.2's PASS-WITH-DEFICIT lesson is the auth source. |
| T-19.3.05-02 | T (silent regression on non-fraud-team cells) | small/medium/large/large_phase9 ladder | mitigate | Task 5.a explicitly enumerates all 5 cells; 10% warn / 25% block thresholds applied per CLAUDE.md. |
</threat_model>

<verification>
- 5 commits land in order: chore(19.3-05) ledger, chore(19.3-05) trace, chore(19.3-05) criterion, (no separate commit for 5.d — folded into 5.e), docs(19.3-05) verification.
- `19.3-VERIFICATION.md` exists with explicit verdict.
- Throughput baseline section `## 1M-event blast (rebaseline 19.3)` exists with ≥ 5 rows.
- Perf baseline contains post-19.3 row showing ≥ 4× speedup.
- Live trace data captured at `/tmp/beava-trace-19.3-final.txt` with ≥ 100k events.
- All 6 anti-pattern checks recorded as PASS.
- Welford invariant test verified GREEN (re-run if needed).
- `cargo test --workspace` passes.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes.
- `cargo fmt --all --check` passes.
</verification>

<success_criteria>
- Phase 19.3 verdict is **PASS** with fraud-team K=10k zipfian N=1M ≥ 100,000 EPS.
- Both criterion microbench AND live trace measurements are recorded (Phase 19.2 anti-pattern remediation).
- All 10 success criteria from CONTEXT.md are demonstrably met.
- All 6 anti-pattern checks are clean.
- No regression > 10% on small/medium/large/large_phase9 ladder cells.
</success_criteria>

<output>
After completion, create `.planning/phases/19.3-windowed-pre-extraction/19.3-VERIFICATION.md` (Task 5.e) AND `.planning/phases/19.3-windowed-pre-extraction/19.3-05-SUMMARY.md` recapping ledger + criterion appends + verdict.
</output>
