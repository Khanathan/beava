---
phase: 19.3-windowed-pre-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/beava-core/benches/apply_path_bench.rs
  - .planning/perf-baselines.md
autonomous: true
requirements:
  - SC-19.3-BENCH-EXISTS: "apply_path/warm_key/14_aggs_windowed criterion bench exists with windowable 14-feature registry"
  - SC-19.3-BENCH-BASELINE: "pre-19.3 baseline row committed to .planning/perf-baselines.md before any 19.3-A code lands"
must_haves:
  truths:
    - "Criterion bench `apply_path/warm_key/14_aggs_windowed` runs against a 14-feature registry where every feature is wrapped in WindowedOp(window_ms = 24h)"
    - "Pre-19.3 baseline (the slow `update_with_row` path) is captured as a row in `.planning/perf-baselines.md` BEFORE Plan 19.3-02 lands"
    - "Plan 19.3-02 has a measurable target (≥ 4× speedup vs this baseline) it can verify against"
  artifacts:
    - path: "crates/beava-core/benches/apply_path_bench.rs"
      provides: "windowed sibling of 14_aggs bench; parametric `build_fraud_team_synthetic_registry_inner(window_ms: Option<u64>)`"
      contains: "apply_path/warm_key/14_aggs_windowed"
    - path: ".planning/perf-baselines.md"
      provides: "baseline row for 14_aggs_windowed pre-19.3"
      contains: "14_aggs_windowed"
  key_links:
    - from: "build_fraud_team_synthetic_registry_windowed"
      to: "AggOpDescriptor::window_ms = Some(86_400_000)"
      via: "parametric inner builder"
      pattern: "window_ms: window_ms"
threat_model:
  trust_boundaries: []
  notes: "Bench-only change. No new attack surface. Internal microbench infrastructure."
---

<objective>
Add a `apply_path/warm_key/14_aggs_windowed` criterion bench group that exercises the WindowedOp wrapper path the rest of Phase 19.3 will optimize. Capture the pre-19.3 baseline row in `.planning/perf-baselines.md` so Plan 19.3-02's ≥ 4× speedup acceptance gate has a measurable comparison point.

Without this baseline-first commit, Phase 19.3 repeats Phase 19.2's measurement gap: a microbench that improves while the live workload stays flat (per `19.2-INVESTIGATION.md` §closing-note). The bench MUST land RED (showing the slow ~5 µs windowed path) before Plan 19.3-02's GREEN commit drops it to ~1 µs.

Purpose: establish the criterion measurement instrument for 19.3-A.
Output: parametric bench helper + new bench group + committed baseline row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.3-windowed-pre-extraction/19.3-CONTEXT.md
@.planning/phases/19.3-windowed-pre-extraction/19.3-RESEARCH.md
@.planning/phases/19.2-big-apply-path-optimization/19.2-INVESTIGATION.md
@.planning/perf-baselines.md
@crates/beava-core/benches/apply_path_bench.rs

<interfaces>
From `crates/beava-core/src/agg_op.rs`:
```rust
// AggOpDescriptor carries window_ms: Option<u64> per feature.
// AggOp::new wraps in WindowedOp ONLY if AggKind::supports_windowed_wrap() is true.
// Per RESEARCH.md Q3: BloomMember, EventTypeMix, Ewma do NOT support windowed wrap.
// Windowable Tier-1 kinds: Count, Sum, Avg, Min, Max, Variance, StdDev, Ratio,
// CountDistinct, Percentile, TopK, Entropy.
```

From `crates/beava-core/benches/apply_path_bench.rs:43-375` (current state — 14 features ALL hard-code `window_ms: None`):
- 14 features at lines 79, 95, 111, 132, 148, 169, 188, 226, 242, 259, 274, 311, 327, 343
- Three features are non-windowable per RESEARCH.md Q3:
  - line 311 (`txn_event_type_mix` — EventTypeMix)
  - line 343 (`txn_card_bloom` — BloomMember)
  - line 132 (`txn_amount_ewma` — Ewma)
- These three MUST be swapped for windowable Tier-1 substitutes per RESEARCH.md §2 Q3 recommendation (a): keep "14_aggs" naming parity by adding 3 windowable substitutes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (red): Refactor build_fraud_team_synthetic_registry to be parametric on window_ms; add failing bench group</name>
  <read_first>
    - crates/beava-core/benches/apply_path_bench.rs (full file, all 408 lines)
    - .planning/phases/19.3-windowed-pre-extraction/19.3-RESEARCH.md §2 Q3 (windowable-kind filtering, substitution recommendations)
    - crates/beava-core/src/agg_op.rs:467-477 (AggOp::new wrap logic)
    - crates/beava-core/src/agg_windowed.rs:464-473 (supports_windowed_wrap definition)
  </read_first>
  <behavior>
    - Test (bench compile): `cargo bench -p beava-core --bench apply_path_bench --no-run` succeeds with the new `apply_path/warm_key/14_aggs_windowed` group declared.
    - Test (bench run, RED for measurement purposes): the new group runs and reports time/iter ≥ 3× the existing `apply_path/warm_key/14_aggs` group (proves it's exercising the slow WindowedOp path; pre-19.3 baseline is expected ~5 µs vs ~1 µs for non-windowed).
    - The original `apply_path/warm_key/14_aggs` group continues to run unchanged (call new `build_fraud_team_synthetic_registry()` wrapper that delegates to inner with `None`).
  </behavior>
  <action>
    1. Refactor `build_fraud_team_synthetic_registry` at `apply_path_bench.rs:43` into:
       ```rust
       fn build_fraud_team_synthetic_registry() -> Arc<Registry> {
           build_fraud_team_synthetic_registry_inner(None)
       }
       fn build_fraud_team_synthetic_registry_windowed(window_ms: u64) -> Arc<Registry> {
           build_fraud_team_synthetic_registry_inner(Some(window_ms))
       }
       fn build_fraud_team_synthetic_registry_inner(window_ms: Option<u64>) -> Arc<Registry> {
           // existing body, but every `window_ms: None` slot becomes `window_ms: window_ms`
       }
       ```
    2. Replace the 3 non-windowable features per RESEARCH.md §2 Q3 substitution plan:
       - `txn_amount_ewma` (line ~132) → `txn_amount_stddev` (AggKind::StdDev, field `amount`) — windowable
       - `txn_event_type_mix` (line ~311) → `txn_amount_min` (AggKind::Min, field `amount`) — windowable Tier-1
       - `txn_card_bloom` (line ~343) → `txn_amount_max` (AggKind::Max, field `amount`) — windowable Tier-1
       Keep total feature count at 14 so naming parity with non-windowed `14_aggs` group is preserved (per D-04 acceptance criterion).
    3. Add a comment block at the top of the inner builder citing RESEARCH.md §2 Q3 explaining why these 3 features were swapped (BloomMember/EventTypeMix/Ewma don't support windowed wrap per `agg_windowed.rs:464,473`).
    4. Add a new bench group `apply_path/warm_key/14_aggs_windowed` mirroring the structure of the existing `apply_path/warm_key/14_aggs` group (same warmup, same iter count, same `feed_event` shape) but constructing its registry via `build_fraud_team_synthetic_registry_windowed(86_400_000)` (24h window per D-04).
    5. **NO cold-key sibling** per D-04 ("Add `apply_path/warm_key/14_aggs_windowed` criterion group ONLY ... No cold-key sibling for this phase.").
    6. Run `cargo bench -p beava-core --bench apply_path_bench --no-run` to confirm compilation. Run `cargo bench -p beava-core --bench apply_path_bench -- apply_path/warm_key/14_aggs_windowed` to capture the pre-19.3 baseline number.
    7. Commit with message: `test(19.3-01): add apply_path/warm_key/14_aggs_windowed criterion bench (pre-19.3 baseline)`
  </action>
  <verify>
    <automated>cargo bench -p beava-core --bench apply_path_bench --no-run 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'apply_path/warm_key/14_aggs_windowed' crates/beava-core/benches/apply_path_bench.rs` ≥ 1
    - `grep -c 'build_fraud_team_synthetic_registry_inner' crates/beava-core/benches/apply_path_bench.rs` ≥ 3 (declaration + 2 wrappers)
    - `grep -c 'window_ms: None' crates/beava-core/benches/apply_path_bench.rs` == 0 inside the inner builder body (every slot now uses the parameter); the only `None` literal in the file may be in the `build_fraud_team_synthetic_registry()` wrapper that calls `_inner(None)`.
    - `grep -E 'BloomMember|EventTypeMix|Ewma' crates/beava-core/benches/apply_path_bench.rs` returns zero hits in feature-construction code (the 3 non-windowable kinds are GONE from the bench's 14-feature set; substitutes Min/Max/StdDev are present).
    - `cargo bench -p beava-core --bench apply_path_bench --no-run` exits 0.
    - `cargo bench -p beava-core --bench apply_path_bench -- apply_path/warm_key/14_aggs_windowed` runs and emits a time/iter measurement ≥ 3× the time/iter of `apply_path/warm_key/14_aggs` (proves slow-path engagement; this is the pre-19.3 baseline).
    - `cargo clippy -p beava-core --benches --all-features -- -D warnings` exits 0.
    - `cargo fmt --all --check` exits 0.
    - Commit message starts with `test(19.3-01):` per CLAUDE.md TDD convention.
  </acceptance_criteria>
  <done>Bench compiles, runs, reports a slow-windowed-path baseline; commit exists in git log with `test(19.3-01):` prefix.</done>
</task>

<task type="auto">
  <name>Task 1.b (green): Append pre-19.3 baseline row to .planning/perf-baselines.md</name>
  <read_first>
    - .planning/perf-baselines.md (last 80 lines — to see existing row format)
    - Output of Task 1.a's `cargo bench -- apply_path/warm_key/14_aggs_windowed` run (the captured time/iter number)
  </read_first>
  <action>
    1. Append a new row under the most recent hw-class section in `.planning/perf-baselines.md`:
       ```
       | apply_path/warm_key/14_aggs_windowed | <captured_ns/iter from Task 1.a> | <stddev> | pre-19.3 baseline (slow WindowedOp path); Plan 19.3-02 must drop this ≥ 4× | 2026-04-28 | <commit-sha> |
       ```
       Match the exact column shape of adjacent rows (read 5 prior rows in the same section to confirm format; the file's row schema is `name | time | stddev | notes | date | commit`).
    2. The "notes" cell MUST include both: (a) "pre-19.3 baseline" and (b) "Plan 19.3-02 must drop ≥ 4×" — these are the two verification anchors Plan 19.3-05 reads.
    3. Commit with message: `chore(19.3-01): record pre-19.3 baseline for apply_path/warm_key/14_aggs_windowed`
  </action>
  <verify>
    <automated>grep -c '14_aggs_windowed' .planning/perf-baselines.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '14_aggs_windowed' .planning/perf-baselines.md` ≥ 1
    - `grep -c 'pre-19.3 baseline' .planning/perf-baselines.md` ≥ 1
    - `grep -c 'Plan 19.3-02 must drop' .planning/perf-baselines.md` ≥ 1
    - Row appears in the most recent hw-class section (verified by reading the table heading immediately above the new row).
    - Commit message starts with `chore(19.3-01):` per CLAUDE.md convention.
  </acceptance_criteria>
  <done>Baseline row exists; commit lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. Bench-only / planning-doc-only change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.3.01-01 | I | bench infrastructure | accept | Microbench is run locally / in CI; no production data, no network surface. |
</threat_model>

<verification>
- `cargo bench -p beava-core --bench apply_path_bench --no-run` compiles.
- `apply_path/warm_key/14_aggs_windowed` runs and reports a time/iter ≥ 3× the non-windowed sibling.
- `.planning/perf-baselines.md` contains a new row for `14_aggs_windowed` annotated as the pre-19.3 baseline.
- `cargo test --workspace` still passes (bench-only changes do not regress lib tests).
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes.
- `cargo fmt --all --check` passes.
- Two commits land in order: `test(19.3-01):` then `chore(19.3-01):`.
</verification>

<success_criteria>
- Pre-19.3 baseline measurement is recorded and committed.
- Plan 19.3-02 has a concrete numeric target (≥ 4× speedup vs this baseline) to verify against.
- The 3 non-windowable features (BloomMember, EventTypeMix, Ewma) are removed from the bench's 14-feature set; 3 windowable Tier-1 substitutes (StdDev, Min, Max) take their slots.
</success_criteria>

<output>
After completion, create `.planning/phases/19.3-windowed-pre-extraction/19.3-01-SUMMARY.md` with: bench numbers captured, commits landed, and the exact baseline value Plan 19.3-02 will compare against.
</output>
