---
phase: 19.3-windowed-pre-extraction
plan: 01
subsystem: bench-infrastructure
tags: [criterion, bench, baseline, windowed-op, pre-extraction]
dependency_graph:
  requires: []
  provides:
    - "apply_path/warm_key/14_aggs_windowed criterion bench group"
    - "pre-19.3 baseline measurement (463.82 ns) for Plan 19.3-02's ≥ 4× acceptance gate"
  affects:
    - "Plan 19.3-02 (compares against this baseline)"
    - "Plan 19.3-05 (verification reads pre-19.3 baseline anchors)"
tech-stack:
  added: []
  patterns:
    - "parametric bench-registry helper (build_fraud_team_synthetic_registry_inner(window_ms: Option<u64>))"
key-files:
  created: []
  modified:
    - "crates/beava-core/benches/apply_path_bench.rs"
    - ".planning/perf-baselines.md"
decisions:
  - "Synthetic-bench ratio (1.46×) is below the plan's predicted 3× ratio; plan acceptance criterion was anchored on the 88-feature fraud-team.json investigation, not on this 14-feature synthetic. The bench correctly engages the slow update_with_row fallback path. Plan 19.3-02's gate uses the absolute baseline (463.82 ns) with a ≥ 4× target, not the windowed-vs-non-windowed ratio."
metrics:
  completed: "2026-04-28"
  tasks: 2
  files-modified: 2
  duration: "~25 min"
commits:
  - "172ce65: test(19.3-01) add bench (red)"
  - "33d8bc2: chore(19.3-01) record baseline (green)"
---

# Phase 19.3 Plan 01: Criterion Bench Amendment + Pre-19.3 Baseline Summary

Added `apply_path/warm_key/14_aggs_windowed` criterion bench group exercising the slow `WindowedOp::update_with_row` fallback path; captured the **463.82 ns** pre-19.3 baseline that Plan 19.3-02's ≥ 4× speedup gate (target ≤ 116 ns) will compare against.

## Tasks

### Task 1.a (red) — Refactor + new bench group

**Commit:** `172ce65` — `test(19.3-01): add apply_path/warm_key/14_aggs_windowed criterion bench (pre-19.3 baseline)`

- Refactored `build_fraud_team_synthetic_registry` into a parametric inner builder `build_fraud_team_synthetic_registry_inner(window_ms: Option<u64>)`.
- Added windowed sibling `build_fraud_team_synthetic_registry_windowed(window_ms: u64)`.
- Threaded `window_ms` through all 14 feature descriptors (replacing 14 hard-coded `window_ms: None` slots).
- Swapped 3 non-windowable features per RESEARCH §2 Q3 (`agg_windowed.rs:464,473`):
  - `txn_amount_ewma` (Ewma) → `txn_amount_stddev` (StdDev)
  - `txn_event_type_mix` (EventTypeMix) → `txn_amount_min` (Min)
  - `device_merchant_bloom` (BloomMember) → `device_amount_max` (Max)
- Added new bench function `14_aggs_windowed` inside `bench_apply_warm_key`, building a 24h-window registry, pre-warming with 200 events, then measuring steady-state.

**Acceptance criteria (8 total):**

| # | Criterion | Result |
|---|---|---|
| 1 | `grep -c 'apply_path/warm_key/14_aggs_windowed'` ≥ 1 | ✅ 2 |
| 2 | `grep -c 'build_fraud_team_synthetic_registry_inner'` ≥ 3 | ✅ 3 |
| 3 | No bare `window_ms: None,` inside inner builder body | ✅ 0 (only `dedupe_window_ms`, `sub_window_ms` remain — different fields) |
| 4 | Zero `AggKind::BloomMember` / `AggKind::EventTypeMix` / `AggKind::Ewma` in feature-construction code | ✅ 0 |
| 5 | `cargo bench --no-run` exits 0 | ✅ |
| 6 | Bench runs and emits time/iter | ✅ 463.82 ns |
| 7 | `cargo clippy -p beava-core --benches --all-features -- -D warnings` exits 0 | ✅ |
| 8 | `cargo fmt --all --check` exits 0 | ✅ |
| 9 | Commit message starts with `test(19.3-01):` | ✅ |

**Deviation note:** the criterion `bench-windowed-time ≥ 3× bench-non-windowed-time` was **NOT met** (actual ratio: 1.46×, 463.82/316.95). Investigated and documented as a Rule-4-equivalent acceptance-criterion-vs-reality misalignment — the plan's 3× expectation was anchored on the 88-feature fraud-team.json cost model, not on this 14-feature synthetic. On the synthetic, much of the per-event cost is sketch-bound (Percentile UDDSketch, TopK, Entropy CountDistinct), making the WindowedOp dispatch tax proportionally smaller than the inner-state update cost. Verified the slow path IS engaged (read `agg_op.rs:865-869` — Windowed arm of `update_with_extracted` falls back to `update_with_row` at line 868). Plan 19.3-02's measurement gate uses the **absolute** baseline value (463.82 ns) with the ≥ 4× target, which remains a strong, falsifiable criterion.

### Task 1.b (green) — Append baseline row

**Commit:** `33d8bc2` — `chore(19.3-01): record pre-19.3 baseline for apply_path/warm_key/14_aggs_windowed`

- Added new "Phase 19.3 — pre-19.3 windowed baseline" section to `.planning/perf-baselines.md`.
- Two rows added: re-measured non-windowed `14_aggs` (316.95 ns, same-day reference) + new windowed `14_aggs_windowed` (463.82 ns, the pre-19.3 baseline).
- Notes column carries the verifier-consumption anchors: `pre-19.3 baseline`, `Plan 19.3-02 must drop ≥ 4×`, commit hash `172ce65`.

**Acceptance criteria (5 total):**

| # | Criterion | Result |
|---|---|---|
| 1 | `grep -c '14_aggs_windowed'` ≥ 1 | ✅ 1 |
| 2 | `grep -c 'pre-19.3 baseline'` ≥ 1 | ✅ 1 |
| 3 | `grep -c 'Plan 19.3-02 must drop'` ≥ 1 | ✅ 1 |
| 4 | Row appears in most recent hw-class section | ✅ Apple-M4 / Darwin-24.3.0 |
| 5 | Commit message starts with `chore(19.3-01):` | ✅ |

**Note on row format:** plan body suggested a 6-column schema (`name | time | stddev | notes | date | commit`). Actual file schema is **5 columns** (`Bench | Median | Date | Phase | Notes`). Matched the existing file schema; embedded commit hash and notes anchors in the Notes column.

## Bench Numbers (Apple-M4 / Darwin-24.3.0 / 10 cores)

| Group | Median | Notes |
|---|---|---|
| apply_path/warm_key/14_aggs            | 316.95 ns | re-measured 2026-04-28 (was 362.71 ns on 2026-04-27; -13% drift from intervening Phase 19.2 commits) |
| apply_path/warm_key/14_aggs_windowed   | **463.82 ns** | **pre-19.3 baseline — Plan 19.3-02's gate** |

**Plan 19.3-02 target:** ≤ **116 ns** (≥ 4× speedup).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 4-equivalent — acceptance criterion vs reality] Synthetic ratio < predicted 3×**
- **Found during:** Task 1.a verification step.
- **Issue:** Plan acceptance criterion expected the windowed bench to run ≥ 3× the non-windowed sibling. Measured ratio is 1.46× on the 14-feature synthetic.
- **Investigation:** Read `agg_op.rs:865-869` to confirm the WindowedOp slow path is genuinely engaged (it is — Windowed arm of `update_with_extracted` falls through to `update_with_row` at line 868). Cause: 14-feature synthetic has heavy sketch content (Percentile, TopK, Entropy, CountDistinct) where dispatch tax is proportionally smaller than inner-state update cost. The 3× expectation was anchored on the 88-feature fraud-team.json investigation cost model.
- **Resolution:** Documented in commit message + perf-baselines.md "Predicted-vs-measured ratio note" + this SUMMARY. Plan 19.3-02's gate uses the absolute baseline (463.82 ns → ≤ 116 ns target), which is the stronger and more falsifiable criterion.
- **Files modified:** `crates/beava-core/benches/apply_path_bench.rs`, `.planning/perf-baselines.md`
- **Commit:** baked into `172ce65`'s message + `33d8bc2`'s note section

### Auth Gates

None.

## Verification Status

- ✅ `cargo bench -p beava-core --bench apply_path_bench --no-run` compiles
- ✅ `apply_path/warm_key/14_aggs_windowed` runs and reports 463.82 ns
- ✅ `.planning/perf-baselines.md` contains the new row with required anchors
- ✅ `cargo clippy -p beava-core --benches --all-features -- -D warnings` passes
- ✅ `cargo fmt --all --check` passes
- ✅ Two commits land in order: `test(19.3-01):` (172ce65) then `chore(19.3-01):` (33d8bc2)
- ⚠️ Predicted ≥ 3× synthetic-ratio criterion not met (1.46× actual); documented as deviation; absolute-baseline acceptance gate for Plan 19.3-02 remains intact

## Self-Check

Verified files and commits:

```
[ -f crates/beava-core/benches/apply_path_bench.rs ] → FOUND
[ -f .planning/perf-baselines.md ] → FOUND
[ -f .planning/phases/19.3-windowed-pre-extraction/19.3-01-SUMMARY.md ] → FOUND
git log --oneline | grep 172ce65 → FOUND (test(19.3-01): add bench)
git log --oneline | grep 33d8bc2 → FOUND (chore(19.3-01): record baseline)
```

## Self-Check: PASSED

## Threat Flags

None — bench-only / planning-doc-only change. No new attack surface, no API changes.
