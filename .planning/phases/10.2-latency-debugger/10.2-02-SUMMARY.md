# Plan 10.2-02 Summary: Backend Integration — Instrumentation + Endpoint

## Result: PASS

## What Was Built
- **AppState**: `latency: LatencyTracker` field added to struct + all 5 construction sites
- **PUSH instrumentation**: records into command + per-stream histogram after existing push_elapsed capture, key_preview extracted only if slow-query heap would accept (Pitfall 5)
- **GET instrumentation**: Instant::now() before lock (wire-to-wire), record_command after features computed
- **SET instrumentation**: same pattern as GET
- **MSET instrumentation**: timing around async handle_mset, fresh lock acquisition after completion (no .await across lock)
- **`/debug/latency` endpoint**: lock-once-then-build-JSON handler, registered in build_router
- **Integration tests**: 2 new tests validating endpoint JSON structure and histogram bin counts

## Files Modified
- `src/server/tcp.rs` — AppState field + 4 command instrumentation sites
- `src/server/http.rs` — debug_latency handler + route
- `src/main.rs` — latency field in AppState construction
- `tests/test_debug_ui.rs` — 2 new tests + AppState field
- `tests/test_server.rs` — AppState field
- `tests/test_pipeline.rs` — AppState field

## Test Results
- `cargo test`: 569 passed (549 existing + 18 latency unit + 2 integration)
- push_latency_seconds scalar preserved
- No .await across any mutex lock
