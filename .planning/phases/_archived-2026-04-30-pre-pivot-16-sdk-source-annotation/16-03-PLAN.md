---
phase: 16-sdk-source-annotation
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - python/tests/test_phase3_smoke.py
  - python/tests/test_phase4_smoke.py
  - python/tests/test_phase5_smoke.py
  - python/tests/test_app.py
  - python/tests/test_agg.py
  - python/tests/test_events.py
  - python/tests/test_tables.py
  - python/tests/test_sdk_ops.py
  - python/tests/test_validate.py
  - python/tests/test_decay_velocity.py
  - python/tests/test_transport_http.py
  - python/tests/test_transport_tcp.py
  - python/tests/test_embed.py
  - python/tests/bench_register_compile.py
  - docs/blog/streaming-shouldnt-require-a-platform-team.md
  - beava-website/project/field-guide-ch1.html
  - beava-website/project/field-guide-ch2.html
  - beava-website/project/index.html
  - beava-website/project/guide/chapter-1/index.html
  - beava-website/project/guide/recipes/personalization/index.html
  - beava-website/project/guide/recipes/fraud/index.html
  - beava-website/project/guide/recipes/leaderboard/index.html
  - beava-website/project/guide/recipes/usage-metering/index.html
  - beava-website/project/guide/recipes/rate-limiting/index.html
  - python/tests/bench_source_decoration.py
  - .planning/perf-baselines.md
autonomous: true
requirements: []  # aggregates Plan 01 + 02 REQ-IDs via migration; no new ones

must_haves:
  truths:
    - "Every class-form @bv.event / @bv.table in python/ docs/ beava-website/ is preceded by @bv.source"
    - "pytest python/tests/ is fully green post-migration (zero source_missing errors from legacy sites)"
    - "Every curl / HTTP snippet in docs + website referencing /push-table/ or /delete-table/ has been updated to /upsert/ + /delete/ (D-09 tail)"
    - "Decoration-time overhead of @bv.source is negligible (< 5% slower than bare @bv.event decoration) — regression bench committed"
  artifacts:
    - path: "python/tests/bench_source_decoration.py"
      provides: "Python timeit-based bench measuring @bv.source decoration overhead"
      min_lines: 40
    - path: ".planning/perf-baselines.md"
      provides: "new Phase 16 row: decoration_with_source_ns vs baseline decoration_bare_ns"
      contains: "Phase 16"
  key_links:
    - from: "migrated test files"
      to: "python/beava/_source.py"
      via: "import source from beava; prepend @bv.source on class-form"
      pattern: "@bv\\.source"
    - from: "migrated docs + website curl snippets"
      to: "server /upsert + /delete routes"
      via: "path rename tail (D-09)"
      pattern: "/upsert/|/delete/"
---

<objective>
Migrate every class-form `@bv.event` / `@bv.table` decoration across the
repository to prepend `@bv.source`, and migrate every documentation /
website curl snippet from `/push-table/` + `/delete-table/` to the new
`/upsert/` + `/delete/` paths (D-09 tail). Commit a decoration-overhead
microbench to honor the Phase 6+ perf-regression gate.

Purpose: Land Phase 16's public surface end-to-end in the repo.
Everything a user reads (tests, docs, blog, website) demonstrates the
v0 surface correctly.

Output: ~103 migrated decoration sites; an unknown number of curl
snippet migrations in docs + website; 1 bench; 1 new baseline row.

**Scope note:** Mostly mechanical diff. Budget ~10 LoC per test site ×
26 Python sites + ~70 website HTML sites + ~7 doc sites (decorator) +
documentation curl snippets (smaller count). Largest plan by LoC,
smallest by conceptual load. VERIFICATION + SUMMARY + throughput run
moved to Plan 16-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-sdk-source-annotation/16-CONTEXT.md
@.planning/phases/16-sdk-source-annotation/16-01-PLAN.md
@.planning/phases/16-sdk-source-annotation/16-02-PLAN.md
@.planning/perf-baselines.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (mechanical migration): prepend @bv.source to all class-form decorations + migrate curl snippets to /upsert + /delete</name>
  <files>python/tests/*.py, docs/blog/*.md, beava-website/project/**/*.html</files>
  <action>
    No red/green split — mechanical migration. Red evidence: running
    `pytest python/tests/ -x` after Plans 01 + 02 land will fail with
    BV-E-SOURCE-MISSING on every legacy class-form test; that IS the red.

    Step 1 — inventory:
    ```bash
    grep -rnE '^@bv\.(event|table)' python/tests/ docs/ beava-website/ > /tmp/phase16-decorator-sites.txt
    wc -l /tmp/phase16-decorator-sites.txt  # ~103
    rg '/push-table/|/delete-table/' docs/ beava-website/ > /tmp/phase16-wire-sites.txt
    wc -l /tmp/phase16-wire-sites.txt
    ```

    Step 2 — Python test decorator migration (26 sites):
    For each file in python/tests/*.py importing `import beava as bv`:
    - find every `^@bv.event...` / `^@bv.table(...)` directly above `class `
    - prepend `@bv.source\n` (no import change — `bv.source` exported by Plan 01's __init__.py)

    Batch into 3 commits of ~9 files each for reviewability:
    - Batch 1: `refactor(16-03): migrate phase 3-5 smokes + test_app + test_agg to @bv.source`
    - Batch 2: `refactor(16-03): migrate test_events/test_tables/test_sdk_ops/test_validate to @bv.source`
    - Batch 3: `refactor(16-03): migrate remaining phase-specific tests + benches to @bv.source`

    Run `pytest python/tests/ -x` between batches; failures shrink batch-by-batch.

    Step 3 — docs decorator migration:
    - docs/blog/streaming-shouldnt-require-a-platform-team.md (code fences)
    Commit: `docs(16-03): migrate blog post code samples to @bv.source`

    Step 4 — docs curl snippet migration (D-09 tail):
    - Any `curl` / HTTP example in docs/ referencing `/push-table/` → `/upsert/`
    - Any reference to `/delete-table/` → `/delete/`
    Commit: `docs(16-03): migrate docs curl snippets to /upsert + /delete paths`

    Step 5 — website decorator migration (~70 sites):
    - beava-website/project/*.html and beava-website/project/guide/**/*.html
    - HTML <code> / <pre> blocks with decorator samples — bulk find/replace with indentation care
    - Spot-check 3 pages render (home, chapter-1, recipes/fraud) via `cd beava-website && python3 -m http.server`
    Commit: `docs(16-03): migrate beava-website sample code to @bv.source`

    Step 6 — website curl snippet migration (D-09 tail):
    - Any `/push-table/` or `/delete-table/` reference in beava-website/project/**/*.html → new paths
    Commit: `docs(16-03): migrate beava-website curl snippets to /upsert + /delete`

    Step 7 — final suite run:
    ```bash
    pytest python/tests/ -x -q
    cargo test --workspace
    cargo clippy --workspace --all-targets --all-features -- -D warnings
    cargo fmt --all --check
    rg '/push-table/|/delete-table/' docs/ beava-website/ python/beava/ crates/  # expect zero (markdown CHANGELOG notes from Plan 06 excluded)
    ```
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && test $(grep -rnE '^@bv\.(event|table)' python/ docs/ beava-website/ 2>/dev/null | grep -v '@bv.source' | wc -l) -eq 0 && pytest python/tests/ -x -q && cargo test --workspace</automated>
  </verify>
  <done>
    Zero unguarded ^@bv.event / ^@bv.table remain in python/ docs/ beava-website/. Zero /push-table/ or /delete-table/ references remain outside a deliberate CHANGELOG note. Full Python test suite green. Full cargo workspace green. Website spot-check renders.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (red+green): @bv.source decoration-overhead microbench + perf-baselines row</name>
  <files>python/tests/bench_source_decoration.py, .planning/perf-baselines.md</files>
  <behavior>
    Bench measures:
    - `decorate_bare_event_class` — how long does `@bv.event` class-form decoration take (1000 fresh classes via `types.new_class`)
    - `decorate_sourced_event_class` — how long does `@bv.source @bv.event` class-form decoration take
    - Same pair for `@bv.table(key='x')`
    - Same pair for reversed order `@bv.event + @bv.source` (commutative — confirms no cost shift)
    - Output: ns/iter median across 5000 trials, written to .planning/perf-baselines.md keyed by hw-class.

    Regression threshold: `decorate_sourced_event_class` median / `decorate_bare_event_class` median < 1.05 (source adds < 5% overhead).

    Python timeit bench fulfills the Phase 6+ "criterion or language-equivalent" perf-regression-gate contract per CLAUDE.md §Conventions.
  </behavior>
  <action>
    Red subtask 2.a: create python/tests/bench_source_decoration.py with the bench + threshold assertion (`assert ratio < 1.05, f"source overhead {ratio:.2%} exceeds 5% threshold"`). Run once to record baseline; initial commit records a placeholder "TBD" row in perf-baselines.md.
    Commit red: `test(16-03): add @bv.source decoration-overhead bench (placeholder row in perf-baselines.md)`.

    Green subtask 2.b: run the bench; fill in actual ns/iter numbers under a new "## Phase 16 — decoration overhead (2026-04-24)" section keyed by hw-class (shape mirrored from existing Phase 3 baseline rows).
    Commit green: `perf(16-03): record @bv.source decoration-overhead baseline on <hw-class>`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && python python/tests/bench_source_decoration.py && grep -q "Phase 16" .planning/perf-baselines.md</automated>
  </verify>
  <done>
    Bench exists; ratio < 1.05 asserted; .planning/perf-baselines.md has a Phase 16 section with numbers; Phase 6+ perf gate honored.
  </done>
</task>

</tasks>

<threat_model>
No new threats. Migration + measurement only.

| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-16-09 | Tampering | Broken migration leaves a bare class-form that errors at runtime in user demo | mitigate | Task 1 verify clause greps for any `^@bv.event` / `^@bv.table` not preceded by `@bv.source`; zero tolerance. |
| T-16-10 | Information disclosure | Website samples leak API details | accept | Already public. |
| T-16-12 | Tampering | Leftover `/push-table/` curl snippet in docs sends users at a 404 route | mitigate | Task 1 step 4 + 6 grep for legacy paths; zero tolerance outside a deliberate CHANGELOG note. |
</threat_model>

<verification>
- `grep -rnE '^@bv\.(event|table)' python/ docs/ beava-website/ | grep -v '@bv.source'` empty
- `rg '/push-table/|/delete-table/' docs/ beava-website/ python/beava/ crates/` empty (CHANGELOG exception only, owned by Plan 06)
- `pytest python/tests/ -x -q` green
- `cargo test --workspace` green
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean
- `cargo fmt --all --check` clean
- `.planning/perf-baselines.md` has a Phase 16 row with decoration overhead < 1.05×
</verification>

<success_criteria>
1. All ~103 class-form decorator sites migrated to `@bv.source` prefix
2. All legacy `/push-table/` + `/delete-table/` curl/HTTP snippets in docs + website migrated to new paths (D-09 tail)
3. Full pytest + cargo test green post-migration (SC6)
4. Decoration-overhead bench with < 5% regression (Phase 6+ gate honored)
</success_criteria>

<output>
Plan 03 produces no SUMMARY of its own — Plan 06 aggregates. (Or optionally a short 16-03-SUMMARY.md noting migration counts.)
</output>
