---
phase: 13.0-design-contract-spec-docs
plan: 08
subsystem: docs
tags: [operators, catalog, polish, decay, ewma, ema, half-life, processing-time, twa]

# Dependency graph
requires:
  - phase: 13.0-design-contract-spec-docs
    plan: 05
    provides: 54 op page stubs scaffolded under docs/operators/<family>/<op>.md with H1 + 9 mandatory H2 sections and TODO prose markers labeled by polish plan number; Plan 08 fills the 6 stubs labeled "Plan 13.0-08" (decay family). Plan 05 also pre-installed the `## Aliases` section in ewma.md documenting bv.ema — preserved verbatim by Plan 08.
  - phase: 13.0-design-contract-spec-docs
    plan: 06
    provides: Per-op page template established (134-157 LOC range, 9 H2 sections, cost-class cross-link, V0-MEM-GOV-02 citation pattern, cold-start convention, family index template) — Plan 08 inherits verbatim
  - phase: 13.0-design-contract-spec-docs
    plan: 07
    provides: Family index template (~25-35 LOC, summary table + key invariants + see also) + processing-time citation pattern + cold-entity eviction cross-link convention — Plan 08 inherits verbatim and applies the processing-time citation to all 6 decay ops (decay coefficients use server now_ms() Δt)
  - phase: 12.8-memory-governance
    provides: V0-MEM-GOV-02 lifetime-aggregation contract (decay ops classified as O(1) lifetime-bound at register_validate.rs ~line 436) — cited in Complexity table for all 6 ops
  - phase: 19.2-operator-uniformity
    provides: docs/operators/cost-class.md (alive Phase 19.2 metadata, KEPT, NOT touched) — cross-linked from each polished page's Complexity section for CPU tier (all 6 decay ops are Tier 1 — ewma 35ns / ewvar 38ns / ew_zscore 38ns / decayed_sum 35ns / decayed_count 32ns / twa 35ns post-19.2 expected)
provides:
  - 6 decay/ op pages polished from Plan 05 stubs (ewma, ewvar, ew_zscore, decayed_sum, decayed_count, twa) — each 148-159 LOC; TODO markers replaced with full Description / Parameters / Returns / Complexity / Examples / Edge cases / See also prose
  - docs/operators/decay/index.md (43 LOC) — Decay family overview + 6-op summary table + key invariants section + when-to-use-which guidance + cross-link to velocity/rate_of_change
  - bv.ema documented INLINE in ewma.md `## Aliases` section (Plan 05 pre-installed the section; Plan 08 enriched the prose); NO separate ema.md file created (catalog stays at 53 unique AggKind variants + 1 alias = 54 page paths total)
  - 5 EW-decay ops (ewma / ewvar / ew_zscore / decayed_sum / decayed_count) cite `half_life` as REQUIRED in Parameters table + Edge cases (rejecting "forever" + zero) + Description (explaining the alpha = 1 - 0.5^(Δt/half_life) formula)
  - 1 TWA op cites `window` as REQUIRED (TWA does not decay — it integrates held-time exactly) AND notes `window="forever"` is allowed because TWA is classified O(1) lifetime-bound at register_validate.rs
  - Processing-time citations on all 6 ops: decay coefficient Δt uses server now_ms() per project_redis_shaped_no_event_time_ever; late events (Δt ≤ 0) fall back to unweighted blend and don't advance last_now_ms
  - Cold-start return convention: all 6 return null for empty stream; ewvar / ew_zscore additionally return null after only one matching event (no spread to normalize); twa returns last_v after exactly one event (no held-time integral yet)
  - Cold-entity eviction (V0-MEM-GOV-01) cross-linked on every decay op
affects:
  - 13.0-09 (velocity polish — 9 ops): inherits the per-page template and processing-time citation pattern; Plan 09 owns rate_of_change which is cross-linked from this plan's decay/index.md "Note" block
  - 13.0-10 / 11 (buffer-geo polish): inherit the per-page template and family index template established by Plans 06 + 07 + 08
  - 13.0-12 (pipeline-dsl/compilation-rules.md): forward-referenced from every polished decay page's See also section; the link target page does not yet exist (Plan 12 creates it)
  - 13.0-15 (closure plan): runs scripts/check_op_page_coverage.py + verifies remaining TODO markers across the catalogue; this plan's contribution: 0 TODOs in decay/
  - 13.4 (engine prep): polished pages reference target post-13.4 wire shapes (verb-style routes, `kind=table` + derivation `output_kind="table"`) — 13.4 implements the engine to match

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-op page template inherited from Plan 06/07 verbatim: H1 + one-line tagline + 9 H2 sections (Signature / Description / Parameters / Returns / Complexity / Examples / Wire / Edge cases / See also). Length 148-159 LOC across all 6 ops; all Tier 1 / O(1) lifetime-bound."
    - "half_life citation pattern: 5 EW-decay ops (ewma / ewvar / ew_zscore / decayed_sum / decayed_count) cite half_life as REQUIRED in (1) Parameters row, (2) Description prose with the alpha formula, (3) Edge cases (missing → ValueError, 'forever' → rejected by _validate_half_life, 0… → rejected by regex, server-side fallback aggregation_invalid_half_life). Establishes the half_life-mandatory convention before downstream SDK porters in Plans 13.5/13.6."
    - "twa window citation pattern: twa cites window as REQUIRED (TWA needs a horizon — without one the denominator is undefined) AND notes window=\"forever\" is explicitly allowed because crates/beava-core/src/register_validate.rs (~line 436) classifies twa under the O(1) lifetime-bound family alongside the EW-decay ops. Distinguishes twa from the 5 half_life ops that REJECT \"forever\"."
    - "Processing-time citation pattern (project_redis_shaped_no_event_time_ever): all 6 decay ops state 'decay coefficient Δt uses server now_ms() at apply' explicitly in Description. Late-event handling (Δt ≤ 0 → unweighted blend, last_now_ms NOT advanced) documented per op in Edge cases — this is the load-bearing edge case for replay determinism."
    - "Read-does-not-decay-forward pattern: app.get(...) returns the running statistic AS OF the last matching event — does NOT re-decay the value to query time. Documented uniformly across all 5 EW-decay ops + decayed_count + decayed_sum Edge cases. Important because users coming from in-process EWMA libraries often expect 'EWMA at t=now' semantics; beava's contract is 'EWMA at t=last-event' for state-non-mutation reasons."
    - "ema-as-alias-not-as-page pattern: ema is documented INLINE in ewma.md ## Aliases section (Plan 05 pre-installed the section; Plan 08 enriched the prose with cross-references to crates/beava-core/src/agg_op.rs and python/beava/_agg.py line numbers). NO separate ema.md file. The operator catalog stays at 53 unique AggKind variants + 1 alias documented inline = 54 page paths total. Same pattern any future alias-only op should follow (e.g. if ADR-002 introduces deprecation aliases for renamed ops in v0.0.x)."
    - "Family index template (~40 LOC for decay): H1 + one-line family overview + 5-column summary table (op | required kwarg | returns | CPU tier | notes) + 'Key invariants' section (decay-specific: processing-time-only, half_life mandatory and finite, twa accepts forever, reads don't decay forward, cold-start returns null) + 'When to use which' guidance bullets + 'Note' block clarifying that rate_of_change is NOT in this family + See also section. Enriched over recency/index.md template by adding the when-to-use-which decision-bullets, since the 6 decay ops have similar shapes that require user judgment to disambiguate."
    - "Cross-family note pattern: decay/index.md includes an explicit 'Note: bv.rate_of_change is NOT in the decay family — it lives under velocity/rate_of_change.md' block, with a forward-link to Plan 13.0-09 ownership. Eliminates the most likely user confusion (Phase 9 grouped both decay and velocity ops under the same plan, but the catalog separates them). Establishes the cross-family-note pattern for any future ambiguous-classification op."

# Files
key-files:
  created:
    - docs/operators/decay/index.md (43 LOC) — Decay family overview + 6-op summary table + key invariants + when-to-use-which + rate_of_change note
    - .planning/phases/13.0-design-contract-spec-docs/13.0-08-SUMMARY.md (this file)
  modified:
    - docs/operators/decay/ewma.md (74 → 157 LOC; +83 LOC; full polish — Tier 1, O(1), half_life REQUIRED rejecting "forever"; ## Aliases section preserved + enriched documenting bv.ema)
    - docs/operators/decay/ewvar.md (69 → 149 LOC; +80 LOC; full polish — Tier 1, O(1), half_life REQUIRED; companion to ewma)
    - docs/operators/decay/ew_zscore.md (69 → 150 LOC; +81 LOC; full polish — Tier 1, O(1), half_life REQUIRED; wraps ewvar; null when variance is 0 from single observation)
    - docs/operators/decay/decayed_sum.md (69 → 155 LOC; +86 LOC; full polish — Tier 1, O(1), half_life REQUIRED; Cormode forward-decay; steady-state ≈ rate * value * half_life / ln(2))
    - docs/operators/decay/decayed_count.md (69 → 148 LOC; +79 LOC; full polish — Tier 1, O(1), half_life REQUIRED, NO field arg; cheapest decay op at ~32 ns; steady-state ≈ rate * half_life / ln(2))
    - docs/operators/decay/twa.md (69 → 159 LOC; +90 LOC; full polish — Tier 1, O(1), window REQUIRED, "forever" allowed; integrates held-time exactly, no decay)

# Decisions
decisions:
  - "Per-op page template inherited verbatim from Plans 06/07 (no structural changes to the H2 layout); polish only fills the 5 prose sections per op (Description / Parameters / Returns / Examples / Edge cases) plus enriches Complexity + See also where applicable. The Signature + Wire blocks were rewritten to reflect actual Python signatures (e.g. ewma takes positional `field` + kwarg-only `half_life` + optional `where`) rather than the Plan 05 placeholder `(...)`."
  - "All 6 ops are Tier 1 per cost-class.md — no rare-tier polishing required. All 6 are O(1) memory bound; lifetime mode classifications: 5 EW-decay ops are 'Required' (no window= kwarg, half_life is the lifetime control); 1 TWA op is 'Allowed' (window=\"forever\" explicitly accepted)."
  - "ewma.md ## Aliases section preservation rule: Plan 05 pre-installed the ## Aliases header documenting bv.ema; Plan 08 had a hard requirement to NOT remove it (acceptance gate). Plan 08 retained the structure verbatim, enriched the prose with explicit cross-references to (1) crates/beava-core/src/agg_op.rs (AggKind::Ewma variant — same kind for both names), (2) crates/beava-core/src/register_validate.rs ~line 436 (lifetime-bound classification lists both names together), (3) python/beava/_agg.py lines 345-347 (the bv.ema pass-through). The wire `op` value is `\"ewma\"` for both — documented explicitly so SDK porters in 13.6 don't accidentally introduce two opcodes."
  - "rate_of_change EXCLUSION rule (Plan 08 acceptance gate): rate_of_change is a Phase 9 velocity-family op, NOT a decay-family op (it computes a slope across two adjacent windows, not an exponentially-weighted statistic). Plan 08 verifies `[ ! -f docs/operators/decay/rate_of_change.md ]` and includes a Note block in decay/index.md cross-linking to velocity/rate_of_change.md (Plan 13.0-09 owns its polish). Establishes the cross-family-note pattern for any future ambiguous-classification op."
  - "half_life vs window distinction made explicit in decay/index.md summary table: 5 ops require half_life (the EW-decay family) and 1 op requires window (twa). Both kwarg families share the duration-string grammar (\\d+(ms|s|m|h|d)), but half_life rejects \"forever\" while window accepts it. The distinction matters because user mistakes (passing window to ewma or half_life to twa) raise TypeError at the SDK helper layer — documented per-op in Edge cases."
  - "Read-does-not-decay-forward pattern documented uniformly: app.get(...) returns the running statistic as of the last matching event — the helper does NOT re-decay the value to query time. This is by design (querying state would mutate it on every read, breaking idempotence); but it can surprise users who expect 'EWMA at t=now' semantics from in-process EWMA libraries. Documented in Edge cases for ewma + ewvar + ew_zscore + decayed_sum + decayed_count, plus an explicit bullet in decay/index.md key invariants. SDK porters in 13.5/13.6 must inherit this contract verbatim."
  - "Cold-start return convention documented per-op consistently: all 6 ops return null for empty stream; ewvar / ew_zscore additionally return null after only one matching event (no spread / no z-divisor); twa returns last_v after exactly one event (no held-time integral yet — sum_dt = 0 → query returns last_v directly per crates/beava-core/src/agg_state_decay.rs line ~362). This per-op cold-start contract is uniformly documented in each Edge cases section."
  - "Late-event handling (Δt ≤ 0 → unweighted blend, last_now_ms NOT advanced) documented per op in Edge cases for ewma + ewvar + ew_zscore + decayed_sum + decayed_count + twa. This is the load-bearing edge case for replay determinism — the architecture commitment is 'time never moves backward', and the unweighted-blend fallback preserves the per-event update count without advancing the time cursor. SDK porters need to know that late events still update state, just not in the time-weighted manner. (Note: twa uses dt = max(now - last_t, 0) and only adds to the integral when dt > 0; the last_v gauge value still updates so a later proper-Δt event can integrate from the new value.)"
  - "Cold-entity eviction (V0-MEM-GOV-01) cross-linked on every decay op: @bv.event(cold_after=...) drops the underlying state per Redis-TTL pattern; decay ops rebuild fresh from the next post-eviction matching event (the new event reseeds value/last_now_ms with initialized=true). Establishes how decay ops interact with cold-after TTL for SDK porters."
  - "Per CLAUDE.md TDD §Note 4 doc-only-plan exemption: single docs(13.0-08) commit covering 6 polished pages + 1 family index page — no red-test commit required since the 'test' (acceptance grep loops + per-op LOC checks) is mechanical and passes by construction post-polish."

# Metrics
metrics:
  duration_min: 25
  completed: 2026-05-03T23:35:00Z
  tasks_completed: 2
  loc_added: 675
  loc_removed: 127
  files_added: 1
  files_modified: 6
  commits: 1
---

# Phase 13.0 Plan 08: Operator catalog polish — decay (6) Summary

## One-liner

Polished 6 decay-family op pages (`ewma` with inline `bv.ema` alias,
`ewvar`, `ew_zscore`, `decayed_sum`, `decayed_count`, `twa`) from Plan 05
stubs into full-prose spec documents — Description / Parameters / Returns /
Complexity / Examples / Edge cases / See also — and added 1 family overview
index page (`docs/operators/decay/index.md`) including a "Note" block that
cross-links `bv.rate_of_change` to its actual home in `velocity/`. The 5
EW-decay ops cite `half_life` as REQUIRED with `"forever"` rejected;
`bv.twa` cites `window` as REQUIRED with `"forever"` explicitly allowed
(integrates held-time exactly; classified `O(1)` lifetime-bound at
`register_validate.rs` ~line 436). All 6 cite
`project_redis_shaped_no_event_time_ever` for processing-time `Δt`
semantics. Single `docs(13.0-08)` commit (95ebc44) per CLAUDE.md TDD §Note
4 doc-only-plan exemption.

## What landed

### 6 decay/ op pages polished (148-159 LOC each)

| Op | LOC | Required kwarg | Returns | Notes |
|----|----:|----------------|---------|-------|
| `ewma.md` | 157 | `half_life` | `f64` or `null` | Tier 1 ~35 ns; alpha = 1 - 0.5^(Δt/half_life); ## Aliases section documents bv.ema inline (same AggKind::Ewma variant) |
| `ewvar.md` | 149 | `half_life` | `f64` or `null` | Tier 1 ~38 ns; EW-Welford; companion to ewma |
| `ew_zscore.md` | 150 | `half_life` | `f64` or `null` | Tier 1 ~38 ns; wraps ewvar; null when variance is 0 (only one observation) |
| `decayed_sum.md` | 155 | `half_life` | `f64` or `null` | Tier 1 ~35 ns; Cormode forward-decay; steady-state ≈ rate * value * half_life / ln(2) |
| `decayed_count.md` | 148 | `half_life` | `f64` or `null` | Tier 1 ~32 ns (cheapest decay op); NO field arg; steady-state ≈ rate * half_life / ln(2) |
| `twa.md` | 159 | `window` | `f64` or `null` | Tier 1 ~35 ns; integrates held-time exactly (no decay); `window="forever"` allowed |

### 1 family overview index page

- `docs/operators/decay/index.md` (43 LOC) — Decay family overview + 6-op
  summary table (op | required kwarg | returns | CPU tier | notes columns)
  + **Key invariants** section locking the processing-time-only contract,
  half_life-mandatory-and-finite rule for the EW family, twa-accepts-forever
  exception, read-does-not-decay-forward rule, cold-start returns null
  convention, cold-entity eviction reference + **When to use which**
  guidance bullets + **Note** block cross-linking
  `bv.rate_of_change` to `velocity/rate_of_change.md` (Plan 13.0-09 owns
  it) + See also pointers (operator catalog index, cost-class.md, velocity
  family, core family, shared.md window grammar, V0-MEM-GOV-02,
  pipeline-dsl/compilation-rules.md).

### Aliases handling

- `bv.ema` documented INLINE in `ewma.md` `## Aliases` section. Plan 05
  pre-installed the section header + initial pointer; Plan 08 enriched the
  prose with explicit cross-references to:
  - `crates/beava-core/src/agg_op.rs` — both names map to `AggKind::Ewma`
  - `crates/beava-core/src/register_validate.rs` ~line 436 — lifetime-bound
    classification lists `"ewma" | "ema"` together
  - `python/beava/_agg.py` lines 345-347 — `bv.ema(...)` is a thin
    pass-through to `bv.ewma(...)`
- The wire `op` value is `"ewma"` for both — explicitly stated so SDK
  porters in 13.6 don't accidentally introduce two opcodes.
- NO separate `ema.md` file. Catalog stays at 53 unique `AggKind` variants
  + 1 alias documented inline = 54 page paths total.

### TODO markers eliminated

Per Plan 05's split, the 6 op stubs in decay/ collectively carried ~30
TODO markers (5 prose sections × 6 ops). After Plan 08 polish:
**0 TODO markers remain** in decay/. The remaining TODO markers are
distributed across velocity/, buffer-geo/, sketch/, core/ — owned by
Plans 09 / 10 / 11 (already polished by Plans 06/07: core, sketch,
point-ordinal, recency).

```bash
$ grep -rcE 'TODO \(Plan 13\.0-' docs/operators/decay/ | grep -v ':0$'
(empty — 0 TODOs across all 7 files)
```

### Cross-link discipline

Every polished page cross-links to:

- `docs/operators/cost-class.md` (Phase 19.2 alive metadata) — for CPU
  tier (all 6 decay ops are Tier 1; cited with the post-19.2 expected
  per-op number — 32-38 ns range)
- `docs/operators/decay/index.md` — family overview (added by this plan)
- `docs/operators/index.md` — master 53-op catalog index (via family index)
- `docs/pipeline-dsl/compilation-rules.md` — forward-reference (Plan 13.0-12
  creates the file)
- `examples/wire/register-fraud-team.request.json` — full payload example
  placeholder (Plan 06/07 also forward-referenced this fixture; created by
  a later plan)
- `.planning/PROJECT.md` (project_redis_shaped_no_event_time_ever) — for
  processing-time-only semantics on the decay-coefficient Δt
- `.planning/REQUIREMENTS.md` (V0-MEM-GOV-02 — twa lifetime mode allowed;
  V0-MEM-GOV-01 — cold-after eviction) — for the lifetime-bound contract

## Verification (acceptance criteria)

All Task 1 / Task 2 acceptance criteria verified post-commit:

### Task 1 — 6 decay ops

- All 6 decay ops polished, no TODO markers (verified via `for op in ewma
  ewvar ew_zscore decayed_sum decayed_count twa; do ! grep -qE 'TODO
  \(Plan 13\.0-' docs/operators/decay/${op}.md; done`)
- All 6 ≥ 80 lines (got 148-159 LOC)
- ewma.md preserves Aliases section with bv.ema reference (`grep -q
  '^## Aliases' && grep -q 'bv.ema'` — both PASS)
- 5 EW-decay ops cite half_life (`for op in ewma ewvar ew_zscore
  decayed_sum decayed_count; do grep -q 'half_life'; done` — all PASS)
- twa cites window (`grep -q 'window' docs/operators/decay/twa.md` — PASS)
- rate_of_change is NOT in decay/ (`[ ! -f
  docs/operators/decay/rate_of_change.md ]` — PASS)
- decay/ family directory has exactly 6 ops + index (`find
  docs/operators/decay -name '*.md' -not -name 'index.md' | wc -l` →
  6 — PASS)

### Task 2 — family index page + commit

- decay/index.md exists ≥ 25 lines (got 43 LOC)
- Index links all 6 ops (`for op in ewma ewvar ew_zscore decayed_sum
  decayed_count twa; do grep -q "${op}.md" docs/operators/decay/index.md;
  done` — all PASS)
- Index notes bv.ema alias (`grep -q 'bv.ema'` — PASS)
- Index cross-links to velocity/rate_of_change (`grep -q
  'velocity/rate_of_change'` — PASS)
- Single docs(13.0-08) commit at 95ebc44 (`git log -1 --format=%s | grep
  -qE '^docs\(13\.0-08\)'` — PASS)
- Commit body refs decay + ewma + ema (`git log -1 --format=%B | grep -qE
  'decay|ewma|ema'` — PASS)
- 7 files staged in commit (6 decay op pages + 1 decay-index)

### Plan-level invariants

- STATE.md / ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md NOT modified
  (none in commit)
- docs/operators/cost-class.md NOT modified
- 0 TODOs remain in docs/operators/decay/
- No separate `ema.md` file created (alias stays inline in ewma.md per
  Plan 08 acceptance gate)
- No `rate_of_change.md` created in decay/ (lives in velocity/ per
  Plan 13.0-09; this plan's index.md cross-links to it)
- Single docs() commit per CLAUDE.md TDD §Note 4 doc-only-plan exemption

## Threat Flags

None. Plan 08 is doc-only polish — no new network surface, auth paths,
file access patterns, or schema changes at trust boundaries are
introduced. The single threat from the plan's `<threat_model>`
(T-13.0-08-01: operator-catalog information disclosure) is `accept` per
CONTEXT D-02 (operator pages are SEO/docs targets — intentional public
surface for OSS launch).

## Known Stubs

None in scope. The ~30 TODO markers Plan 05 sowed in the 6 decay op
stubs are all eliminated by this plan. The remaining TODO markers
(velocity/, buffer-geo/) are owned by Plans 09-11 and are NOT in this
plan's goal scope.

The forward-referenced `docs/pipeline-dsl/compilation-rules.md` link
target does not yet exist (Plan 13.0-12 creates it). This is intentional —
the cross-link is a contract for the page that's coming, not a broken
link in the steady-state docs site.

The forward-referenced `examples/wire/register-fraud-team.request.json`
link target also does not yet exist (a later plan in the 13.0 wave creates
it). Inherited from Plans 06/07's pattern; not a contract bug.

The forward-referenced `docs/operators/velocity/rate_of_change.md` link
target exists as a Plan 05 stub (not yet polished — Plan 13.0-09 owns its
polish). The cross-link in `decay/index.md` will resolve to a polished
page after Plan 09 lands.

## Deviations from Plan

None — plan executed exactly as written. The plan's `<action>` blocks
specified per-op content guidance (mathematical formula for ewma,
half_life REQUIRED for the EW family, window for twa, server
processing-time Δt, cold-start behavior, structured error codes); this
executor wrote the prose directly into each Plan 05 stub via the Write
tool, replacing the TODO markers with concrete content. All 11
acceptance criteria verified pre-commit + post-commit (see Verification
section above). The Wire JSON blocks were rewritten with concrete
field/half_life/window values matching the Examples (e.g. ewma's wire
block now shows `field: "amount", half_life: "1h"` matching Example 1)
rather than the Plan 05 placeholder `params: {}`.

The plan's `<action>` block for `twa.md` flagged "verify against
agg_op.rs" the question "twa is a windowed-only op?". Investigation
confirmed: `_validate_window(window, "twa", requires_window=True)` in
`python/beava/_agg.py` line ~396 enforces `window` is required at the
SDK layer; `crates/beava-core/src/register_validate.rs` ~line 436 also
classifies twa as `O(1)` lifetime-bound (alongside the EW family).
Conclusion documented in `twa.md`: window is REQUIRED, but
`window="forever"` is explicitly accepted (TWA integrates held-time
exactly so the lifetime form is well-defined). The 5 EW-decay ops differ
— they reject `"forever"` because exponential decay with infinite
half-life is degenerate (just freezes on the first observation).

## Self-Check: PASSED

- FOUND: docs/operators/decay/ewma.md (157 LOC, 0 TODOs, ## Aliases section + bv.ema cited)
- FOUND: docs/operators/decay/ewvar.md (149 LOC, 0 TODOs, half_life cited)
- FOUND: docs/operators/decay/ew_zscore.md (150 LOC, 0 TODOs, half_life cited)
- FOUND: docs/operators/decay/decayed_sum.md (155 LOC, 0 TODOs, half_life cited)
- FOUND: docs/operators/decay/decayed_count.md (148 LOC, 0 TODOs, half_life cited)
- FOUND: docs/operators/decay/twa.md (159 LOC, 0 TODOs, window cited)
- FOUND: docs/operators/decay/index.md (43 LOC, 6 op links + bv.ema + velocity/rate_of_change cross-link)
- FOUND: commit 95ebc44 (`docs(13.0-08): polish 6 decay-family op pages + family index page`)
- FOUND-NOT-MODIFIED: docs/operators/cost-class.md (NOT in commit; last touched 27 Apr by Phase 19.2)
- FOUND-NOT-MODIFIED: STATE.md / ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md (NOT in commit)
- FOUND-NOT-CREATED: docs/operators/decay/rate_of_change.md (correctly absent — lives in velocity/)
- FOUND-NOT-CREATED: docs/operators/decay/ema.md (correctly absent — alias documented inline in ewma.md)
- FOUND-PASSING: 0 TODOs remain in docs/operators/decay/
