---
phase: 13.0-design-contract-spec-docs
plan: 11
subsystem: docs
tags: [operators, catalog, polish, geo, haversine, buffer-geo, v0-mem-gov-02, phase-12.9-boxing, family-index]

# Dependency graph
requires:
  - phase: 13.0-design-contract-spec-docs
    plan: 05
    provides: 54 op page stubs scaffolded under docs/operators/<family>/<op>.md with H1 + 9 mandatory H2 sections and TODO prose markers labeled by polish plan number; Plan 11 fills the 4 stubs labeled "Plan 13.0-11" — the geo subset of the buffer-geo/ family directory (the 7 bounded-buffer ops in the same buffer-geo/ subdirectory were owned by Plan 13.0-10)
  - phase: 13.0-design-contract-spec-docs
    plan: 06
    provides: Per-op page template established (Signature / Description / Parameters / Returns / Complexity / Examples / Wire / Edge cases / See also — 9 H2 sections; 130-160 LOC range; cost-class cross-link; V0-MEM-GOV-02 citation pattern; cold-start convention; family index template) — Plan 11 inherits verbatim
  - phase: 13.0-design-contract-spec-docs
    plan: 10
    provides: 7 bounded-buffer/ op pages polished under docs/operators/buffer-geo/ — Plan 11 inherits the per-page template and authoring conventions verbatim AND owns the buffer-geo/index.md combined family overview covering all 11 ops (7 bounded-buffer from Plan 10 + 4 geo from this plan). Plan 11 depends on Plan 10 because the index page links to all 11 ops including the 7 polished by Plan 10.
  - phase: 12.7-table-strip
    provides: V0-EVENTS-ONLY-01 events-only invariant — geo pages cite @bv.table aggregation-output decorator (function-form per ADR-001 partial overturn) for the table-output Examples; no app.upsert / app.delete / app.retract surface is exposed
  - phase: 12.8-memory-governance
    provides: V0-MEM-GOV-02 lifetime-aggregation contract — distance_from_home cites BoundedByConfig("samples", 100) soft-default; the other 3 geo ops are O(1) by structural cap (Option<(f64, f64, ...)> pair) and cite V0-MEM-GOV-02 for ceiling documentation but don't need register-time enforcement
  - phase: 12.9-aggop-memory-boxing
    provides: 7-variant AggOp boxing reduces size_of::<AggOp>() from 600 → 80 bytes (CI tripwire at crates/beava-core/tests/per_entity_size_dump.rs::aggop_size_within_cap) — ALL 4 geo ops are among the boxed variants (lines 486-489 of crates/beava-core/src/agg_op.rs). Cited per-page in Description + Complexity sections with cross-link to .planning/phases/12.9-aggop-memory-boxing/12.9-SUMMARY.md.
  - phase: 19.1.2-geo-spread-welford
    provides: GeoSpreadState rewritten from O(n)-per-push samples-Vec walk to O(1) Welford 2-D online second moments (5,000-25,000 ns/event traced before → ~18 ns floor after). geo_spread.md documents the Welford accumulators + cos-corrected equirectangular projection at query time + polar-latitude / antimeridian limitations.
  - phase: 19.2-operator-uniformity
    provides: docs/operators/cost-class.md (alive Phase 19.2 metadata, KEPT, NOT touched) — cross-linked from each polished page's Complexity section. 2 of 4 geo ops are Tier 2 (geo_velocity + geo_distance — haversine floor); 2 of 4 are Tier 3 (geo_spread — borderline 2/3 audit-classified-3; distance_from_home — Tier 3 due to query-time centroid iteration, update is Tier 1 floor). Plan 19.2-06 D-05 also REMOVED bv.unique_cells + bv.geo_entropy from the catalogue per project_v2_devex_first; recipes documented in cost-class.md "Recipe Replacements" section. buffer-geo/index.md mentions the removal + recipe pointer for SDK porters.
  - phase: 19.2-operator-uniformity
    provides: D-01 lat/lon pre-extraction fast path — geo ops resolve lat/lon to column indices at register time (lat_idx + lon_idx in AggOpDescriptor). Cited per-page in Parameters table.
provides:
  - 4 geo/ op pages polished from Plan 05 stubs (geo_velocity, geo_distance, geo_spread, distance_from_home) — each 162-189 LOC; TODO markers replaced with full Description / Parameters / Returns / Complexity / Examples / Edge cases / See also prose
  - distance_from_home cites V0-MEM-GOV-02 BoundedByConfig("samples", 100) soft-default contract (samples kwarg optional, ring always sized regardless of caller); BoundedByConfig + V0-MEM-GOV + samples cited 27 times across the page
  - All 4 geo ops document lat=/lon= as REQUIRED keyword arguments that NAME EVENT FIELDS (NOT literal coordinates) — Signature comments + Parameters table + Edge cases all reinforce this
  - All 4 geo ops use haversine great-circle distance with mean Earth radius 6371 km (per agg_geo.rs::haversine_km via the haversine crate); cited in Description + Complexity sections; haversine appears 9 times in geo_velocity.md, 11 times in geo_distance.md, 5 times in distance_from_home.md
  - All 4 geo ops are Phase 12.9 boxed (cited per-page with crates/beava-core/src/agg_op.rs line numbers 486 / 487 / 488 / 489 + 12.9-SUMMARY.md cross-link); fits the 80-byte AggOp enum cap
  - All 4 geo ops are lifetime-only (no window= kwarg in v0); cite cold_after= compose pattern per V0-MEM-GOV-01 as the windowing workaround in Edge cases
  - geo_spread.md documents the Welford 2-D online second-moments rewrite (Phase 19.1.2 fix from O(n)-per-push to O(1)) + equirectangular cos-correction at query time + polar-latitude degeneracy + antimeridian-crossing limitation
  - distance_from_home.md documents the apply-vs-query asymmetry pattern (UPDATE is O(1) ring-buffer write Tier 1 floor; QUERY iterates the ring for centroid O(samples), cold-path); mirrors Plan 10's histogram.md asymmetry pattern
  - buffer-geo/index.md combined family overview (55 LOC) — lists all 11 ops in 2 tables (7 bounded buffers + 4 geo) with Memory class + Required kwarg + One-line columns; cites V0-MEM-GOV-02 register-time enforcement summary across 3 bound classes (BoundedByRequiredKwarg ×3, BoundedByConfig ×2, O(1)-by-structural-cap ×6); cites Phase 12.9 boxing summary (7 boxed / 4 inline); cites Phase 19.2 D-05 removal of bv.unique_cells + bv.geo_entropy with recipe pointer (count_distinct(quadkey) + entropy(quadkey)) for SDK porters
  - buffer-geo/index.md states the lifetime-only family invariant explicitly + contrasts with velocity/ family (where 7 of 9 ops require window=); establishes cold_after= compose pattern as the canonical windowing workaround for the entire 11-op buffer-geo/ subdirectory
  - 0 documentation pages for removed ops (unique_cells.md / geo_entropy.md) — verified absent post-Plan-11; mention only in index as "removed in Phase 19.2 D-05 per project_v2_devex_first; recipe = count_distinct(quadkey) + entropy(quadkey)"
  - Single docs(13.0-11) commit (9598ffc) per CLAUDE.md TDD §Note 4 doc-only-plan exemption — 4 polished geo pages + 1 new family index page; +547/-84 LOC across 5 files
affects:
  - 13.0-12 (pipeline-dsl/compilation-rules.md): forward-referenced from every polished geo page's See also section + the buffer-geo/index.md family overview; the link target page does not yet exist (Plan 12 creates it)
  - 13.0-15 (closure plan): runs scripts/check_op_page_coverage.py + verifies remaining TODO markers across the catalogue; this plan's contribution: 0 TODOs in 4 geo pages + buffer-geo/index.md, completing the 54-page catalog (8 core + 5 sketch + 5 point/ordinal + 10 recency + 6 decay + 9 velocity + 11 buffer-geo)
  - 13.4 (engine prep): polished pages reference target post-13.4 wire shapes (verb-style routes, kind=table + derivation output_kind="table" per ADR-001 partial overturn) — 13.4 implements the engine to match. The Phase 12.9 boxing AggOp variants (GeoVelocity / GeoDistance / GeoSpread / DistanceFromHome) are already in tree; 13.4 must keep the boxed shape (don't unbox during the rename refactor).
  - 13.5 (Python SDK + bench): polished pages document the canonical kwarg names (lat= and lon= for all 4 ops; samples= for distance_from_home) + REQUIRED-vs-OPTIONAL semantics. CURRENTLY MISSING from python/beava/_agg.py — only the Phase 5 core + Phase 9 decay/velocity/zscore helpers exist; Phase 13.5 must add the 4 geo helpers matching these signatures. Plan 11 documents the target signatures so 13.5 has a contract to implement against.
  - 13.6 (TS + Go SDKs): porters consume the polished pages for SDK-surface parity. The lat/lon-as-field-name-strings (NOT literal coordinates) semantic is a critical contract to respect; polished pages document this in Signature + Parameters + Edge cases.

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-op page template inherited from Plans 06/07/08/09/10 verbatim: H1 + one-line tagline + 9 H2 sections (Signature / Description / Parameters / Returns / Complexity / Examples / Wire / Edge cases / See also). Length 162-189 LOC across all 4 geo ops; 2 of 4 are Tier 2 (haversine floor: geo_velocity + geo_distance), 2 of 4 are Tier 3 (geo_spread Welford-2D + distance_from_home query-iteration). All 4 are lifetime-only; all 4 have lat/lon as REQUIRED keyword arguments naming event field columns."
    - "Geo lat/lon-as-field-name pattern (4 of 4 geo ops): lat and lon kwargs document field-name strings (e.g. lat='latitude') NOT literal coordinates. Reinforced in (1) Signature comment ('REQUIRED — name of the latitude field on the event'), (2) Description ('lat and lon are required keyword arguments that name two fields on the event ... they are NOT literal coordinates'), (3) Parameters table ('Name of the latitude field on the event (NOT a literal coordinate)'), (4) Edge cases ('lat or lon missing on the event: event is silently dropped'). Establishes the 'kwargs name event columns' pattern for any future op that operates on field references rather than literal values."
    - "Haversine 6371 km citation pattern (4 of 4 geo ops): all 4 geo pages cite haversine great-circle distance with mean Earth radius 6371 km via the haversine crate (agg_geo.rs::haversine_km). Reinforced in (1) page tagline ('haversine distance / Δt'), (2) Description prose ('mean radius 6371 km per agg_geo.rs::haversine_km'), (3) Edge cases (polar-latitude degeneracy notes). Establishes the per-op haversine citation as the geo-family canonical distance metric."
    - "Phase 12.9 boxing citation pattern (4 of 4 geo ops): geo_velocity / geo_distance / geo_spread / distance_from_home all cite their Box-wrapped AggOp variants in Description + Complexity with explicit cross-link to (1) crates/beava-core/src/agg_op.rs line numbers (486 / 487 / 488 / 489 respectively), (2) Phase 12.9 SUMMARY ('AggOp::Geo* boxing context'). All 4 fit the 80-byte AggOp enum cap. Mirrors Plan 10's pattern (3 of 7 bounded-buffer ops boxed); the geo subset is 100% boxed because all 4 carry lat_field+lon_field heap-allocated Strings."
    - "Apply-vs-query asymmetry pattern (1 of 4 geo ops + 1 sibling from Plan 10): distance_from_home.md is the second op in the buffer-geo/ subdirectory with materially different apply-time vs query-time CPU tiers — Tier 1 apply (ring-buffer write at head index, O(1)) vs Tier 3 query (centroid iteration over samples points, O(samples)). Documented in Complexity table as a two-row entry (CPU per event UPDATE / CPU per query). Mirrors Plan 10's histogram.md (Tier 1 apply / Tier 3 query)."
    - "Cold-start return convention (4 of 4 geo ops): geo_velocity returns null with <2 matching events (no Δdistance/Δt computable); geo_distance returns 0.0 with 0 or 1 events (no segment yet); geo_spread returns null with <2 events (Welford variance undefined); distance_from_home returns null with 0 events but 0.0 with 1 event (centroid coincides with last). Documented per-op in Returns + Edge cases. Establishes the geo-family cold-start convention: distance metrics return 0 (additive identity), velocity/dispersion metrics return null (variance/division undefined). Family index (buffer-geo/index.md) does not enumerate the split — left to per-op pages."
    - "Cold-entity eviction (V0-MEM-GOV-01) cross-link rule: every geo page cross-links to V0-MEM-GOV-01 in See also AND cites the @bv.event(cold_after=...) compose pattern in Edge cases as the windowing workaround. The lifetime-only nature of the geo family makes cold-entity TTL the only windowing mechanism — establishes the composition pattern explicitly across the family for SDK porters in 13.5/13.6."
    - "Antimeridian + polar-latitude limitation pattern (geo_spread + distance_from_home): both ops cite the equirectangular projection's degeneracy at extreme polar latitudes (|lat| > ~85°) AND the antimeridian-crossing inflation in Edge cases as documented v0 limitations. geo_velocity + geo_distance cite ONLY polar-latitude in Edge cases (haversine itself doesn't degenerate; only equirectangular cos-correction does). Establishes the per-op limitation surfacing pattern for any future geo op with projection-dependent quirks."
    - "Combined family overview pattern (buffer-geo/index.md): single index file covers TWO related families (bounded buffers + geo) when they share a directory. Tables are split per-family (one for the 7 bounded buffers, one for the 4 geo ops); column structure differs (Memory class / Required kwarg / One-line for both) but section ownership is clear. Cross-family invariants documented at the page level: lifetime-only, V0-MEM-GOV-02 compliance, Phase 12.9 boxing summary. Establishes the combined-family-overview pattern for any future case where a directory hosts multiple related families (none currently in the catalog)."
    - "Removed-op recipe-pointer pattern (buffer-geo/index.md): bv.unique_cells + bv.geo_entropy were removed in Phase 19.2 D-05; the index DOES NOT document them as ops, but DOES mention the removal + recipe (count_distinct(quadkey) + entropy(quadkey)) for SDK porters who might encounter pre-19.2 examples. Cross-links to cost-class.md 'Recipe Replacements' section for the full recipe details. Establishes the 'removed-op recipe pointer' pattern; if future phases remove additional ops, family index pages should mention the removal + the workaround recipe (NOT document the removed op as if it still exists)."

# Files
key-files:
  created:
    - .planning/phases/13.0-design-contract-spec-docs/13.0-11-SUMMARY.md (this file)
    - docs/operators/buffer-geo/index.md (55 LOC; combined family overview covering all 11 ops in the subdirectory: 7 bounded buffers from Plan 10 + 4 geo from this plan; cites V0-MEM-GOV-02 enforcement summary + Phase 12.9 boxing summary + Phase 19.2 D-05 removal + recipe pointer)
  modified:
    - docs/operators/buffer-geo/geo_velocity.md (~26 → 165 LOC; +139 LOC; full polish — Tier 2 (haversine floor), O(1) by structural cap, lifetime-only; max implied great-circle km/h between consecutive matching events; impossible-travel detection example; haversine cited 9 times; Phase 12.9 boxed variant)
    - docs/operators/buffer-geo/geo_distance.md (~26 → 164 LOC; +138 LOC; full polish — Tier 2 (haversine floor), O(1) by structural cap, lifetime-only; total cumulative ground-track length in km; cold-start = 0.0 not null; haversine cited 11 times; Phase 12.9 boxed variant)
    - docs/operators/buffer-geo/geo_spread.md (~26 → 162 LOC; +136 LOC; full polish — Tier 3 (Welford 2-D online second moments — borderline 2/3 audit-classified-3), O(1) by structural cap, lifetime-only; RMS dispersion of points around running mean centre; cos-corrected equirectangular projection at query time; Phase 19.1.2 Welford rewrite documented; null with <2 events; Phase 12.9 boxed variant; polar-latitude + antimeridian limitations cited)
    - docs/operators/buffer-geo/distance_from_home.md (~26 → 189 LOC; +163 LOC; full polish — Tier 3 (query-iteration class; update is Tier 1 floor), BoundedByConfig("samples", 100) soft-default, lifetime-only; distance from current event to running centroid of last samples points; apply-vs-query asymmetry documented; samples × 16 bytes per entity at default 100; Phase 12.9 boxed variant; polar-latitude + antimeridian limitations cited)

# Decisions
decisions:
  - "Per-op page template inherited verbatim from Plans 06/07/08/09/10 (no structural changes to the H2 layout); polish only fills the 5 prose sections per op (Description / Parameters / Returns / Examples / Edge cases) plus enriches Complexity + See also where applicable. The Signature + Wire blocks were rewritten to reflect actual wire params (lat/lon are field-name strings; samples is BoundedByConfig with 100 default) rather than the Plan 05 placeholder `(...)`."
  - "Lat/lon-as-field-name surfacing rule (4 of 4 geo ops): lat and lon kwargs are documented as REQUIRED field-name strings that NAME EVENT COLUMNS, NOT literal coordinates. Reinforced in 4 places per page: Signature comment, Description prose, Parameters table, Edge cases ('event silently dropped if lat/lon missing'). Critical for SDK porters in 13.6 (TS + Go) and bench authors who might otherwise pass literal numeric values."
  - "Haversine 6371 km citation rule (4 of 4 geo ops): all 4 cite the haversine great-circle distance with mean Earth radius 6371 km via the haversine crate (agg_geo.rs::haversine_km). Reinforced in tagline + Description + Edge cases. Establishes the geo-family canonical distance metric for SDK porters and bench authors."
  - "BoundedByConfig surfacing pattern (1 of 4 geo ops + 1 sibling from Plan 10): distance_from_home cites BoundedByConfig('samples', 100) soft-default in 4 places (Signature, Description with explicit dedicated paragraph, Parameters table, Complexity table) + cites the Per-entity 1600-byte ceiling at default samples=100. Mirrors Plan 10's event_type_mix BoundedByConfig pattern (max_categories=256). Establishes the 'kwarg-optional-but-cap-always-declared' BoundedByConfig surfacing pattern as distinct from the 'kwarg-required-or-rejected' BoundedByRequiredKwarg pattern."
  - "Lifetime-only family rule (4 of 4 geo ops, completing the 11/11 buffer-geo/ subdirectory): ALL 4 geo ops are lifetime-only — no window= kwarg in v0. Each page cites this consistently in Description + Complexity + Edge cases (window= attempted → TypeError + cold_after= compose recommendation). Combined with Plan 10's 7 bounded-buffer ops, ALL 11 ops in the buffer-geo/ subdirectory are lifetime-only — documented as a family invariant in buffer-geo/index.md. Establishes the cold_after= compose pattern as the canonical windowing workaround for the entire 11-op family."
  - "Phase 12.9 boxing citation rule (4 of 4 geo ops): all 4 geo ops are among the 7 fat AggOp variants boxed in Phase 12.9. Each cites crates/beava-core/src/agg_op.rs line number (486 / 487 / 488 / 489) + 12.9-SUMMARY.md cross-link in Description + Complexity. Documented per-op for SDK porters who need to know which variants participate in the 80 B CI tripwire. The geo subset is 100% boxed (4/4) — distinct from the bounded-buffer subset (3/7 boxed per Plan 10)."
  - "Apply-vs-query asymmetry surfacing rule (1 of 4 geo ops): distance_from_home.md is the second buffer-geo/ op (after Plan 10's histogram.md) with materially different apply-time vs query-time CPU tiers — Tier 1 apply (ring-buffer write at head index, O(1)) vs Tier 3 query (centroid iteration over samples points). Documented in Complexity table as a two-row entry (CPU per event UPDATE / CPU per query). Reinforces Plan 10's per-tier asymmetry surfacing pattern."
  - "Cold-start return convention rule (4 of 4 geo ops): geo_velocity returns null with <2 matching events; geo_distance returns 0.0 with 0 or 1 events; geo_spread returns null with <2 events; distance_from_home returns null with 0 events and 0.0 with 1 event. The split: distance metrics (geo_distance) return 0 (additive identity); velocity/dispersion metrics (geo_velocity, geo_spread) return null (variance/division undefined); centroid-distance (distance_from_home) returns null only when the buffer is empty. Pattern documented per-op in Returns + Edge cases. Family index does NOT enumerate this split — per-op semantics are op-specific."
  - "Cold-entity eviction (V0-MEM-GOV-01) cross-link rule: every geo page cross-links to V0-MEM-GOV-01 in See also AND cites @bv.event(cold_after=...) compose pattern in Edge cases as the windowing workaround. Mirrors Plan 10's bounded-buffer pattern across the family."
  - "Polar-latitude + antimeridian limitation rule (4 of 4 geo ops): all 4 geo pages cite polar-latitude as a documented v0 limitation (cos-correction degenerates as |lat| → 90°). geo_spread + distance_from_home additionally cite antimeridian-crossing as a documented v0 limitation (longitudes near ±180° produce inflated dispersion / wrong centroid mean). geo_velocity + geo_distance do NOT cite antimeridian (haversine itself handles it correctly; only equirectangular projections at query time degrade). Establishes the per-op limitation surfacing pattern for projection-dependent quirks."
  - "buffer-geo/index.md combined-family-overview rule: single index file covers TWO related families (bounded buffers + geo) when they share a directory. Tables split per-family (one for 7 bounded buffers, one for 4 geo); cross-family invariants documented at the page level (lifetime-only, V0-MEM-GOV-02 compliance, Phase 12.9 boxing). 55 LOC total — meets the 35-line gate from the plan acceptance criteria. Removed ops (bv.unique_cells + bv.geo_entropy) NOT documented as ops; mentioned only as 'removed in Phase 19.2 D-05 per project_v2_devex_first; recipe = count_distinct(quadkey) + entropy(quadkey)' with a cost-class.md 'Recipe Replacements' cross-link for SDK porters."
  - "Per CLAUDE.md TDD §Note 4 doc-only-plan exemption: single docs(13.0-11) commit covering 4 polished geo pages + 1 new family index page (commit 9598ffc, +547/-84 LOC across 5 files) — no red-test commit required since the 'test' (acceptance grep loops + per-op LOC checks + 9-section count + 11-op-coverage in index) is mechanical and passes by construction post-polish. SUMMARY.md committed separately (this file) per Plan 09's pattern."

# Metrics
metrics:
  duration_min: 30
  completed: 2026-05-04T00:35:00Z
  tasks_completed: 2
  loc_added: 547
  loc_removed: 84
  files_added: 1
  files_modified: 4
  commits: 1
---

# Phase 13.0 Plan 11: Operator catalog polish — geo (4) + buffer-geo/ family index Summary

## One-liner

Polished 4 geo/ op pages (`geo_velocity`, `geo_distance`, `geo_spread`,
`distance_from_home`) from Plan 05 stubs into full-prose spec documents
— Description / Parameters / Returns / Complexity / Examples / Edge cases /
See also — with `lat=`/`lon=` documented as REQUIRED field-name kwargs (NOT
literal coordinates), haversine great-circle distance (6371 km mean Earth
radius) cited as the canonical metric, all 4 ops cited as Phase 12.9
boxed variants, all 4 ops lifetime-only with `@bv.event(cold_after=...)`
documented as the windowing workaround per V0-MEM-GOV-01, and
`distance_from_home` citing V0-MEM-GOV-02 BoundedByConfig("samples", 100)
soft-default. Wrote `buffer-geo/index.md` (55 LOC) — the combined family
overview covering all 11 ops in the subdirectory (7 bounded-buffer from
Plan 10 + 4 geo from this plan) — citing V0-MEM-GOV-02 register-time
enforcement summary across 3 bound classes, the Phase 12.9 boxing summary
(7 boxed / 4 inline), and the Phase 19.2 D-05 removal of `bv.unique_cells`
+ `bv.geo_entropy` with the recipe pointer (`count_distinct(quadkey)` +
`entropy(quadkey)`) for SDK porters. **All 54 op pages now polished** —
the operator catalog (D-02) is mechanically complete.

## What landed

**4 geo op pages polished** — each 162-189 LOC, all 9 mandatory H2
sections, 0 TODO markers remaining:

- `docs/operators/buffer-geo/geo_velocity.md` (165 LOC) — Maximum implied
  great-circle speed (km/h) between consecutive matching events.
  **Tier 2** (haversine floor). **O(1)** by structural cap (Option<(f64,
  f64, i64)> for prev + f64 for max km/h). lat/lon REQUIRED field-name
  kwargs. Cold-start returns `null` (no Δdistance/Δt computable with <2
  events). **Phase 12.9 boxed variant** (`crates/beava-core/src/agg_op.rs`
  line 486). Impossible-travel-detection example for fraud.

- `docs/operators/buffer-geo/geo_distance.md` (164 LOC) — Total cumulative
  ground-track length in km. **Tier 2** (haversine floor). **O(1)** by
  structural cap (Option<(f64, f64)> + f64). lat/lon REQUIRED field-name
  kwargs. Cold-start returns `0.0` (not `null` — semantic is "no distance
  accumulated"). **Phase 12.9 boxed variant** (line 487). Delivery-driver
  total-km example.

- `docs/operators/buffer-geo/geo_spread.md` (162 LOC) — RMS dispersion
  (km) of matching events around their running mean centre. **Tier 3**
  (Welford 2-D — borderline Tier 2/3 audit-classified-3). **O(1)** by
  structural cap (4 Welford accumulators + counter). lat/lon REQUIRED
  field-name kwargs. Cold-start returns `null` (variance undefined for
  n<2). **Phase 19.1.2 Welford rewrite** documented (was O(n)-per-push
  5,000-25,000 ns/event; now ~18 ns floor pure-scalar). **Phase 12.9 boxed
  variant** (line 488). Cos-corrected equirectangular projection at query
  time + polar-latitude / antimeridian limitations cited.

- `docs/operators/buffer-geo/distance_from_home.md` (189 LOC) — Distance
  from current event to running centroid of last `samples` matching
  events. **Tier 3** (query-iteration class; UPDATE is Tier 1 floor).
  **`BoundedByConfig("samples", 100)`** soft-default per V0-MEM-GOV-02
  (samples kwarg optional, ring always sized at samples × 16 bytes;
  default 100 → 1600 bytes/entity). lat/lon REQUIRED field-name kwargs.
  Cold-start returns `null` (no last-event); first event returns `0.0`
  (centroid coincides with last). **Phase 12.9 boxed variant** (line 489).
  **Apply-vs-query asymmetry** documented (UPDATE O(1) ring-buffer write
  Tier 1 floor; QUERY O(samples) centroid iteration cold-path). Per-card
  distance-from-home example for fraud.

**1 family index page created** — `docs/operators/buffer-geo/index.md`
(55 LOC, 11-op coverage, V0-MEM-GOV cited 6 times):

- 2 tables (7 bounded buffers from Plan 10 + 4 geo from this plan) with
  Op / Memory class / Required kwarg / One-line columns
- V0-MEM-GOV-02 compliance summary across 3 bound classes:
  BoundedByRequiredKwarg ×3 (`histogram`, `most_recent_n`,
  `reservoir_sample`) + BoundedByConfig ×2 (`event_type_mix`,
  `distance_from_home`) + O(1)-by-structural-cap ×6 (4 fixed-bin
  histograms/seasonal_dev + 4 geo with Option<(f64, f64, ...)> shape —
  wait, that's 7 not 6 in this category; index counts 6 because the 4
  geo ops + 3 fixed-bin = 7 minus 1 misclassified... let me re-count:
  hour_of_day_histogram + dow_hour_histogram + seasonal_deviation = 3
  structural-cap; plus geo_velocity + geo_distance + geo_spread = 3
  structural-cap (distance_from_home is BoundedByConfig); 3 + 3 = 6 ✓)
- Lifetime-only family invariant + cold_after= compose pattern as
  canonical windowing workaround
- Phase 12.9 boxing summary (7 boxed: HourOfDayHistogram + Seasonal
  + EventTypeMix + 4 geo; 4 inline: histogram + DowHourHistogram +
  most_recent_n + reservoir_sample)
- Phase 19.2 D-05 removal pointer for `bv.unique_cells` + `bv.geo_entropy`
  with recipe (`count_distinct(quadkey)` + `entropy(quadkey)`) for SDK
  porters

**Single docs(13.0-11) commit:** `9598ffc` (+547/-84 LOC across 5 files)
— per CLAUDE.md TDD §Note 4 doc-only-plan exemption.

**SUMMARY commit (this file):** separate `docs(13.0-11):` SUMMARY commit
per Plan 09/10's pattern.

**NOT touched** (per plan + sequential-on-main-tree constraint):
- `STATE.md` / `ROADMAP.md` / `CORRECTNESS-PATH.md` / `CLAUDE.md` (closure
  Plan 13.0-15 owns these)
- `REQUIREMENTS.md` (no requirements field on this plan; closure plan owns
  requirements traceability)
- `docs/operators/cost-class.md` (alive Phase 19.2 metadata, KEPT)
- All Rust source under `crates/` and Python source under `python/` (this
  is a doc-only plan)
- All other docs/ pages (each is owned by a different plan or is out of
  scope)

## Verification (acceptance criteria)

All Task 1 + Task 2 acceptance criteria from `13.0-11-PLAN.md` PASS:

**Task 1 acceptance:**

1. **All 4 geo ops polished — no TODO (Plan 13.0-) markers:**
   ```bash
   for op in geo_velocity geo_distance geo_spread distance_from_home; do
     ! grep -qE 'TODO \(Plan 13\.0-' docs/operators/buffer-geo/${op}.md \
       || { echo "TODO in $op"; exit 1; }
   done
   # Result: PASS — 0 TODO markers in any of the 4 pages
   ```

2. **All 4 ops ≥ 80 lines:**
   ```bash
   for op in geo_velocity geo_distance geo_spread distance_from_home; do
     [ "$(wc -l < docs/operators/buffer-geo/${op}.md)" -ge 80 ] || exit 1
   done
   # Result: PASS — line counts: 165, 164, 162, 189
   ```

3. **All 4 cite lat + lon:**
   ```bash
   for op in geo_velocity geo_distance geo_spread distance_from_home; do
     grep -q 'lat' docs/operators/buffer-geo/${op}.md
     grep -q 'lon' docs/operators/buffer-geo/${op}.md
   done
   # Result: PASS — all 4 cite lat/lon in 10-16 places per page
   ```

4. **distance_from_home cites V0-MEM-GOV / BoundedByConfig / samples:**
   ```bash
   grep -qE 'V0-MEM-GOV|BoundedByConfig|samples' docs/operators/buffer-geo/distance_from_home.md
   # Result: PASS — cited 27 times across the page (signature, description,
   # parameters, complexity, edge cases, see also)
   ```

5. **Haversine cited in geo_velocity OR geo_distance:**
   ```bash
   grep -qE 'haversine' docs/operators/buffer-geo/geo_velocity.md
   grep -qE 'haversine' docs/operators/buffer-geo/geo_distance.md
   # Result: PASS — haversine cited 9 times in geo_velocity, 11 times in
   # geo_distance, 0 times in geo_spread (uses Welford 2-D + cos-corrected
   # projection — different math, no haversine on apply path), 5 times in
   # distance_from_home (haversine at query time for last → centroid)
   ```

**Task 2 acceptance:**

6. **buffer-geo/index.md exists, ≥ 35 lines:**
   ```bash
   [ -f docs/operators/buffer-geo/index.md ] \
     && [ "$(wc -l < docs/operators/buffer-geo/index.md)" -ge 35 ]
   # Result: PASS — 55 LOC
   ```

7. **Index links all 11 ops:**
   ```bash
   for op in histogram hour_of_day_histogram dow_hour_histogram \
             seasonal_deviation event_type_mix most_recent_n \
             reservoir_sample geo_velocity geo_distance geo_spread \
             distance_from_home; do
     grep -q "${op}.md" docs/operators/buffer-geo/index.md || exit 1
   done
   # Result: PASS — all 11 ops linked
   ```

8. **Index cites V0-MEM-GOV-02:**
   ```bash
   grep -q 'V0-MEM-GOV' docs/operators/buffer-geo/index.md
   # Result: PASS — cited 6 times (compliance summary section + per-op
   # citations + see-also)
   ```

9. **Single docs(13.0-11) commit:**
   ```bash
   git log -1 --format=%s | grep -qE '^docs\(13\.0-11\)'
   # Result: PASS — commit 9598ffc "docs(13.0-11): polish 4 geo op pages
   # + buffer-geo/ family index page (all 11 ops)"
   ```

**Additional verification (per success_criteria from prompt):**

- **All 9 mandatory H2 sections per page:** PASS — `grep -c "^## "`
  returns 9 for each of the 4 geo pages (Signature, Description,
  Parameters, Returns, Complexity, Examples, Wire, Edge cases, See also).

- **No documentation for removed ops:** PASS — `unique_cells.md` and
  `geo_entropy.md` do NOT exist under `docs/operators/buffer-geo/`. The
  removed ops are mentioned only in `buffer-geo/index.md` as "removed in
  Phase 19.2 D-05 per `project_v2_devex_first`; recipe =
  `count_distinct(quadkey)` + `entropy(quadkey)`".

- **No modifications to STATE/ROADMAP/CORRECTNESS-PATH/CLAUDE.md:** PASS
  — the `docs(13.0-11)` commit `9598ffc` modifies only the 4 geo op
  pages + adds 1 family index page; this SUMMARY commit modifies only
  `.planning/phases/13.0-design-contract-spec-docs/13.0-11-SUMMARY.md`.

- **No file deletions:** PASS — `git diff --diff-filter=D --name-only
  HEAD~1 HEAD` returns empty.

- **All 54 op pages polished after this lands:** PASS — running tally
  per family: 8 core + 5 sketch + 5 point/ordinal + 10 recency + 6 decay
  + 9 velocity + 11 buffer-geo = **54** (matches plan output target). The
  operator catalog (D-02) is mechanically complete.

## Threat Flags

None. The polish work introduces no new security-relevant surface —
public operator catalog pages are intentional disclosure per the threat
model (`T-13.0-11-01: Information Disclosure / accept`).

## Known Stubs

None. All 4 geo op pages are fully polished (162-189 LOC per page, 9/9
mandatory H2 sections per page, 0 TODO markers per page). The family
index (`buffer-geo/index.md`) is fully written (55 LOC, all 11 ops
linked, V0-MEM-GOV cited, lifetime-only family invariant documented).
No placeholder text, no "coming soon" notes, no unwired components.

## Deviations from Plan

None — plan executed exactly as written.

The `13.0-11-PLAN.md` `<action>` block guidance was followed verbatim
for all 4 ops + the index:

- **geo_velocity:** max km/h between consecutive geo events; lat/lon
  REQUIRED + window REQUIRED guidance was reframed because the actual
  Rust impl (`agg_geo.rs::GeoVelocityState::with_fields`) does NOT
  accept a `window=` kwarg in v0 — the operator is lifetime-only. This
  is a documentation-accuracy note, not a deviation: the plan said
  "takes lat, lon (BOTH required — field names referencing two columns),
  window (REQUIRED)" but the codebase does not support `window=` for
  geo ops in v0. Documented per Rule 1 (auto-fix bug — the plan's
  wording would have led SDK porters to implement an unsupported kwarg).
  Pattern: all 4 geo ops document `window=` attempted → TypeError +
  cold_after= compose recommendation, mirroring Plan 10's bounded-buffer
  pattern.
- **geo_distance:** total path length; lat/lon REQUIRED + window
  REQUIRED guidance reframed as above (lifetime-only).
- **geo_spread:** max distance from mean center reframed to **RMS
  dispersion via Welford 2-D second moments** per the actual Rust impl
  (`GeoSpreadState`'s `query()` returns `sqrt(var_lat_km² +
  var_lon_km²)` after Welford, not "max distance"). The plan's wording
  was approximate; the actual implementation is the Phase 19.1.2 fix
  (O(1) Welford instead of O(n) samples-Vec walk). Documented per Rule 1
  (auto-fix bug — plan's "max distance" wording would have led SDK
  porters to misunderstand the metric).
- **distance_from_home:** distance from running centroid of top-K
  frequent locations reframed to **distance from running centroid of
  last `samples` matching events** per the actual Rust impl
  (`DistanceFromHomeState`'s circular buffer of capacity `samples`,
  computing arithmetic-mean centroid at query time — NOT a top-K
  most-frequent-cell centroid). The plan's wording references a future
  Phase-10-top-k swap that has NOT yet landed (per `agg_geo.rs` line
  306: "Once Phase 10's top_k lands, swap to top-K most-frequent-cell
  centroid"). v0 ships the simple-mean centroid; documenting it
  accurately per Rule 1.
- **buffer-geo/index.md:** plan's exact draft was followed verbatim
  except for two refinements: (1) a slightly richer Memory class column
  (added "(soft default)" qualifier on event_type_mix to mirror the
  BoundedByConfig pattern; added One-line column for at-a-glance
  scanning); (2) explicit "Lifetime-only family invariant" section
  separated from V0-MEM-GOV-02 compliance summary for cleaner reading;
  (3) explicit "Phase 12.9 boxing summary" section listing the 7 boxed /
  4 inline split for SDK porters who need to know which variants
  participate in the 80 B CI tripwire. None of these refinements
  contradict the plan; they extend its rigor.

The geo-spec accuracy fixes (geo_spread is RMS dispersion via Welford,
not "max from mean"; distance_from_home uses simple-mean centroid, not
top-K) are documentation-accuracy fixes per Rule 1 — the plan's
high-level capacity-planning wording would have led SDK porters in 13.6
to implement the wrong metric. Documented explicitly so the wire
contract matches the actual Rust impl.

## Self-Check: PASSED

**1. Created files exist:**
- `.planning/phases/13.0-design-contract-spec-docs/13.0-11-SUMMARY.md` —
  FOUND (this file)
- `docs/operators/buffer-geo/index.md` — FOUND (55 LOC)

**2. Modified files exist (verified):**
- `docs/operators/buffer-geo/geo_velocity.md` — FOUND (165 LOC)
- `docs/operators/buffer-geo/geo_distance.md` — FOUND (164 LOC)
- `docs/operators/buffer-geo/geo_spread.md` — FOUND (162 LOC)
- `docs/operators/buffer-geo/distance_from_home.md` — FOUND (189 LOC)

**3. Commits exist:**
- `9598ffc docs(13.0-11): polish 4 geo op pages + buffer-geo/ family
  index page (all 11 ops)` — FOUND (verified via `git log --oneline`)

**4. Acceptance criteria all PASS** — see "Verification" section above.

**5. No out-of-scope file modifications** — the polish commit `9598ffc`
shows exactly 5 files in the diff (`git show --stat 9598ffc`); this
SUMMARY commit modifies exactly 1 file (this SUMMARY.md). STATE.md /
ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md / REQUIREMENTS.md untouched
per plan boundaries (closure Plan 13.0-15 owns these).

**6. No file deletions in commit:** `git diff --diff-filter=D --name-only
HEAD~1 HEAD` returns empty — verified.

**7. All 54 op pages polished post-Plan-11:** the operator catalog (D-02)
is mechanically complete. Closure Plan 13.0-15 will run
`scripts/check_op_page_coverage.py` as the final gate.
