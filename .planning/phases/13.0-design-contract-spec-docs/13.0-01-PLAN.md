---
phase: 13.0-design-contract-spec-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture.md
  - docs/http-api.md
  - docs/python-sdk.md
  - docs/protocol.md
  - docs/quickstart.md
  - docs/operators.md
  - docs/index.md
  - docs/installation.md
  - docs/contributing.md
  - docs/comparison.md
  - docs/benchmarks.md
  - docs/data-scientist-demo.md
  - docs/http-api-examples.sh
  - docs/blog/
  - docs/sdk-api/.keep
  - docs/pipeline-dsl/.keep
  - docs/operators/core/.keep
  - docs/operators/point-ordinal/.keep
  - docs/operators/recency/.keep
  - docs/operators/decay/.keep
  - docs/operators/velocity/.keep
  - docs/operators/sketch/.keep
  - docs/operators/buffer-geo/.keep
  - docs/concepts/.keep
  - docs/architecture/.keep
  - examples/wire/schemas/.keep
  - examples/python/.keep
  - examples/typescript/.keep
  - examples/go/.keep
  - .planning/decisions/ADR-001-bv-table-partial-overturn.md
  - .planning/decisions/ADR-002-polars-op-rename.md
autonomous: true
requirements: []
addresses_decisions:
  - "Claude's Discretion: NUKE-and-rewrite of stale docs/*.md (default per CONTEXT)"
  - "Q3 locked: docs/blog/ NUKE in Plan 13.0-01"
  - "Q4 locked: docs/contributing.md NUKE; Phase 13.8 ships repo-root CONTRIBUTING.md"
  - "Q6 locked: docs/index.md NUKE; Plan 13.0-12 ships canonical landing index"
  - "ADR-001 documents @bv.table aggregation-output revival (partial overturn of project_v0_events_only_scope) per RESEARCH §2 starter"
  - "ADR-002 documents 5 op renames (avg→mean / variance→var / stddev→std / count_distinct→n_unique / percentile→quantile) per RESEARCH §2 starter"
  - "ADR-001 explicitly defers the architectural-test allowlist code change to Phase 13.4 (per CONTEXT.md Claude's Discretion + planner constraints)"
  - "Memory file project_v0_events_only_scope updated with ADR-001 partial-overturn pointer (per RESEARCH §2 ADR-001 follow-on actions)"
must_haves:
  truths:
    - "13 stale docs/*.md files plus docs/blog/ deleted via git rm; git history preserved for archaeology"
    - "docs/operators/cost-class.md preserved (alive Phase 19.2 metadata)"
    - "docs/github-repo-surface-runbook.md preserved as-reference"
    - "New directory tree exists under docs/ and examples/ (15 dirs total) — empty subdirs sit on .keep stub files"
    - "ADR-001 exists at .planning/decisions/ADR-001-bv-table-partial-overturn.md with all 5 Nygard sections (Title H1 + Status + Context + Decision + Consequences)"
    - "ADR-002 exists at .planning/decisions/ADR-002-polars-op-rename.md with all 5 Nygard sections"
    - "ADR-001 explicitly defers Phase 13.4 architectural-test allowlist update for OpNode::Table* permitted on derivation output_kind=table"
    - "ADR-002 declares the 5 op renames with old→new mapping table"
    - "project_v0_events_only_scope memory file updated with ADR-001 partial-overturn pointer"
  artifacts:
    - path: ".planning/decisions/ADR-001-bv-table-partial-overturn.md"
      provides: "Nygard ADR documenting @bv.table aggregation-output revival"
      contains: "ADR-001"
      min_lines: 60
    - path: ".planning/decisions/ADR-002-polars-op-rename.md"
      provides: "Nygard ADR documenting Polars-convention op renames"
      contains: "ADR-002"
      min_lines: 60
    - path: "docs/sdk-api/.keep"
      provides: "Wave 1 plans 02-04 populate this dir"
    - path: "docs/operators/core/.keep"
      provides: "Plan 13.0-05 scaffold + Plan 13.0-06 polish populate"
    - path: "docs/concepts/.keep"
      provides: "Plan 13.0-13 populates"
    - path: "examples/wire/schemas/.keep"
      provides: "Plan 13.0-02 populates"
  key_links:
    - from: "ADR-001 follow-on-actions"
      to: "Phase 13.4 architectural-test allowlist update"
      via: "Deferred-for-13.4 subsection"
      pattern: "Deferred|Phase 13.4"
    - from: "ADR-001 Status"
      to: "project_v0_events_only_scope memory file"
      via: "Partial-overturn pointer added by Task 5"
      pattern: "ADR-001|partial overturn"
---

<objective>
Setup plan for Phase 13.0. NUKES 13 stale `docs/*.md` files (verified pre-12.6 / pre-rename / pre-no-event-time-pivot per RESEARCH §6) plus `docs/blog/`. CREATES the new directory tree under `docs/` and `examples/`. CREATES the project's first two ADRs (ADR-001 partial-overturn for `@bv.table` aggregation-output; ADR-002 Polars-convention op renames) using the Nygard 5-section template. UPDATES the `project_v0_events_only_scope` memory file with the partial-overturn pointer.

This plan unblocks every Wave 1/2/3 doc-authoring plan: they all need either the directory tree or the ADRs.

Output: 14 deletions + 15 new directory `.keep` stubs + 2 ADR files + 1 memory file edit. Single `docs(13.0-01):` commit per CLAUDE.md TDD §Note 4 doc-only-plan exemption.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.0-design-contract-spec-docs/13.0-CONTEXT.md
@.planning/phases/13.0-design-contract-spec-docs/13.0-RESEARCH.md
@CLAUDE.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NUKE 13 stale docs files plus docs/blog/ via git rm</name>
  <read_first>
    - docs/architecture.md
    - docs/http-api.md
    - docs/python-sdk.md
    - docs/protocol.md
    - docs/quickstart.md
    - docs/operators.md
    - docs/index.md
    - docs/installation.md
    - docs/contributing.md
    - docs/comparison.md
    - docs/benchmarks.md
    - docs/data-scientist-demo.md
    - docs/http-api-examples.sh
    - docs/blog/streaming-shouldnt-require-a-platform-team.md
    - docs/operators/cost-class.md
    - docs/github-repo-surface-runbook.md
  </read_first>
  <action>
Step 1 — Sanity-check the keep list before any deletion:

```bash
[ -f docs/operators/cost-class.md ] || { echo "PRECONDITION FAIL: cost-class.md missing"; exit 1; }
[ -f docs/github-repo-surface-runbook.md ] || { echo "PRECONDITION FAIL: runbook missing"; exit 1; }
```

Step 2 — git rm the 13 stale files plus the entire blog dir:

```bash
git rm docs/architecture.md
git rm docs/http-api.md
git rm docs/python-sdk.md
git rm docs/protocol.md
git rm docs/quickstart.md
git rm docs/operators.md
git rm docs/index.md
git rm docs/installation.md
git rm docs/contributing.md
git rm docs/comparison.md
git rm docs/benchmarks.md
git rm docs/data-scientist-demo.md
git rm docs/http-api-examples.sh
git rm -r docs/blog/
```

Step 3 — Verify cost-class.md and github-repo-surface-runbook.md are NOT in the git rm staging:

```bash
git status --porcelain | grep -E '^D ' | sort > /tmp/13_0_01_deleted.txt
grep -E 'docs/(operators/cost-class|github-repo-surface-runbook)\.md' /tmp/13_0_01_deleted.txt && {
  echo "ERROR: KEEP file accidentally staged for deletion"; exit 1
}
```

Step 4 — Verify the docs/ tree state before this task ends:

```bash
test ! -e docs/architecture.md
test ! -e docs/blog
test -f docs/operators/cost-class.md
test -f docs/github-repo-surface-runbook.md
```

NO commit yet — Tasks 2 + 3 + 4 + 5 stage their content; one commit at end of Task 5.
  </action>
  <verify>
    <automated>git status --porcelain | grep -E '^D ' | wc -l | xargs -I {} test {} -ge 13 && test ! -e docs/architecture.md && test ! -e docs/blog && test -f docs/operators/cost-class.md && test -f docs/github-repo-surface-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - 13+ files staged for deletion: `[ "$(git status --porcelain | grep -cE '^D ')" -ge 13 ]`
    - All 13 NUKE files no longer exist on disk: `for f in docs/architecture.md docs/http-api.md docs/python-sdk.md docs/protocol.md docs/quickstart.md docs/operators.md docs/index.md docs/installation.md docs/contributing.md docs/comparison.md docs/benchmarks.md docs/data-scientist-demo.md docs/http-api-examples.sh; do [ ! -e "$f" ] || exit 1; done`
    - docs/blog/ deleted: `[ ! -e docs/blog ]`
    - docs/operators/cost-class.md PRESERVED: `[ -f docs/operators/cost-class.md ]`
    - docs/github-repo-surface-runbook.md PRESERVED: `[ -f docs/github-repo-surface-runbook.md ]`
    - cost-class.md NOT in git-rm staging: `! git status --porcelain | grep -qE '^D  docs/operators/cost-class\.md'`
  </acceptance_criteria>
  <done>14 stale doc paths removed via git rm. KEEP files (cost-class.md + github-repo-surface-runbook.md) untouched. No commit yet.</done>
</task>

<task type="auto">
  <name>Task 2: CREATE new directory tree under docs/ + examples/ via .keep stubs</name>
  <read_first>
    - .planning/phases/13.0-design-contract-spec-docs/13.0-RESEARCH.md
    - .planning/phases/13.0-design-contract-spec-docs/13.0-CONTEXT.md
  </read_first>
  <action>
Use the Write tool (NEVER mkdir+touch) to create 15 `.keep` stub files. Each file is a single-line placeholder so git tracks the empty directory:

File body for ALL 15 .keep files (identical content):

```
# Phase 13.0 placeholder — Wave 2/3 plans populate this directory.
```

Files to create:

1. `docs/sdk-api/.keep`
2. `docs/pipeline-dsl/.keep`
3. `docs/operators/core/.keep`
4. `docs/operators/point-ordinal/.keep`
5. `docs/operators/recency/.keep`
6. `docs/operators/decay/.keep`
7. `docs/operators/velocity/.keep`
8. `docs/operators/sketch/.keep`
9. `docs/operators/buffer-geo/.keep`
10. `docs/concepts/.keep`
11. `docs/architecture/.keep`
12. `examples/wire/schemas/.keep`
13. `examples/python/.keep`
14. `examples/typescript/.keep`
15. `examples/go/.keep`

Note: `.planning/decisions/` is populated by Tasks 3-4 directly (ADR-001 + ADR-002), so no .keep stub there.

After all stubs land, stage them via git add:

```bash
git add docs/sdk-api/.keep docs/pipeline-dsl/.keep \
        docs/operators/core/.keep docs/operators/point-ordinal/.keep \
        docs/operators/recency/.keep docs/operators/decay/.keep \
        docs/operators/velocity/.keep docs/operators/sketch/.keep \
        docs/operators/buffer-geo/.keep \
        docs/concepts/.keep docs/architecture/.keep \
        examples/wire/schemas/.keep \
        examples/python/.keep examples/typescript/.keep examples/go/.keep
```
  </action>
  <verify>
    <automated>for d in docs/sdk-api docs/pipeline-dsl docs/operators/core docs/operators/point-ordinal docs/operators/recency docs/operators/decay docs/operators/velocity docs/operators/sketch docs/operators/buffer-geo docs/concepts docs/architecture examples/wire/schemas examples/python examples/typescript examples/go; do [ -f "$d/.keep" ] || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - 15 .keep files exist: `[ "$(find docs/sdk-api docs/pipeline-dsl docs/operators/core docs/operators/point-ordinal docs/operators/recency docs/operators/decay docs/operators/velocity docs/operators/sketch docs/operators/buffer-geo docs/concepts docs/architecture examples/wire/schemas examples/python examples/typescript examples/go -name .keep -type f 2>/dev/null | wc -l)" -eq 15 ]`
    - Each .keep has placeholder line: `for f in $(find docs examples -name .keep -type f); do grep -q '^# Phase 13.0 placeholder' "$f" || exit 1; done`
    - All 15 .keep files staged: `[ "$(git status --porcelain | grep -cE '^A  .*\.keep$')" -eq 15 ]`
  </acceptance_criteria>
  <done>15 directory placeholders staged. Wave 2/3 plans populate them. NO commit yet.</done>
</task>

<task type="auto">
  <name>Task 3: CREATE .planning/decisions/ADR-001-bv-table-partial-overturn.md (Nygard 5-section)</name>
  <read_first>
    - .planning/phases/13.0-design-contract-spec-docs/13.0-RESEARCH.md
    - .planning/phases/13.0-design-contract-spec-docs/13.0-CONTEXT.md
    - .planning/phases/12.7-table-strip/12.7-SUMMARY.md
    - CLAUDE.md
    - $HOME/.claude/projects/-Users-petrpan26-work-tally/memory/project_v0_events_only_scope.md
    - crates/beava-server/tests/phase12_7_no_table_surface.rs
  </read_first>
  <action>
Use the Write tool to create `.planning/decisions/ADR-001-bv-table-partial-overturn.md`. Use the verbatim starter from RESEARCH §2 (lines 168-265) and append the Deferred-for-13.4 subsection.

Required structure (cite-exact section names):

- H1 (line 1): `# ADR-001: \`@bv.table\` aggregation-output revival (partial overturn of v0 events-only scope)`
- H2: `## Status` — body block: `Accepted` newline `Date: 2026-05-03`
- H2: `## Context` — describes:
  - Phase 12.7 strip (closed 2026-05-01, PASS) removed entire @bv.table surface (~5,500 LOC), citing project_v0_events_only_scope and the architectural test crates/beava-server/tests/phase12_7_no_table_surface.rs
  - 2026-05-03 design session locked Polars-style chained syntax as canonical Python API
  - Code block showing the @bv.table(key="user_id") def UserFeatures(txn) -> bv.Table example with 8 LOC
  - Justification for @bv.table as natural attachment point for group_by().agg() output
  - Explicit narrowness: joins / table mutation / retraction propagation / session windows REMAIN killed
- H2: `## Decision` — declares:
  - `@bv.table(key=...)` decorator on a function returning `events.group_by(...).agg(...)` is REVIVED in public `bv` namespace; the decorator captures the chain, names the result, and emits a derivation node with `output_kind=table`
  - `app.upsert(...)`, `app.delete(...)`, `app.retract(...)` REMAIN absent
  - `OpNode::TableUpsert`, `OpNode::TableDelete`, `OpNode::TableRetract` REMAIN absent
  - `OpNode::Table*` is permitted ONLY for derivation `output_kind=table`
  - `TemporalStore`, `MvccVersion`, `temporal_http`, `RecordType::Table*` WAL variants REMAIN absent
  - The architectural test `phase12_7_no_table_surface.rs` allowlist is amended to permit `OpNode::Table*` on derivation `output_kind=table` only — the amendment lands in Phase 13.4 (Phase 13.0 documents the intent here)
  - `project_v0_events_only_scope` memory is updated with a partial-overturn pointer to this ADR (Task 5 of this plan)
- H2: `## Consequences` — has 3 sub-blocks bolded as **Easier:**, **Harder:**, **Follow-on actions:**
  - Easier: Polars syntax canonical; named/keyed entities; wire spec carries kind=table; SDK porters in 13.6 implement aggregation output naturally
  - Harder: architectural-test allowlist needs careful per-AST-context check (not global symbol grep); contributors might assume full Phase 11.5 surface revival — counter-discipline ADR is normative + test enforces
  - Follow-on actions (bulleted):
    - Phase 13.4 Plan: implement engine-side output_kind=table for derivations + amend phase12_7_no_table_surface.rs allowlist
    - Phase 13.5 Plan: implement Python @bv.table function-form decorator wrapping aggregation chain into derivation node
    - Phase 13.6 Plans: TypeScript + Go SDKs each support bv.table() aggregation-output equivalent (TS: builder-pattern wrapper; Go: function returning a table-derivation struct)
    - CLAUDE.md `§ Events-Only Invariant` block gets a footnote pointing at this ADR (Plan 13.0-15 closure adds it)
    - REQUIREMENTS.md `V0-EVENTS-ONLY-01` gets a sub-bullet documenting the permitted aggregation-output exception (Plan 13.0-15 closure may add it)
- H3 tail subsection: `### Deferred for Phase 13.4 implementation` — explicit list of code changes ADR-001 implies but does NOT land:
  - Update `crates/beava-server/tests/phase12_7_no_table_surface.rs` allowlist to permit `OpNode::Table*` on derivations whose `output_kind` is `table`, while keeping rejection for top-level register table-nodes and all other call sites
  - Implement `OpNode::Table*` enum variants in the engine's op-node tree (or revive prior variants conditionally)
  - Implement `output_kind: "table"` field plumbing through register payload validation
  - Add a register payload that round-trips through `phase12_7_no_table_surface.rs` GREEN to confirm the per-AST-context check works

Length target: 80–110 lines including code blocks.

DO NOT modify any code or test file in this task.
  </action>
  <verify>
    <automated>[ -f .planning/decisions/ADR-001-bv-table-partial-overturn.md ] && [ "$(wc -l < .planning/decisions/ADR-001-bv-table-partial-overturn.md)" -ge 60 ] && grep -q '^# ADR-001:' .planning/decisions/ADR-001-bv-table-partial-overturn.md && for s in Status Context Decision Consequences; do grep -q "^## $s" .planning/decisions/ADR-001-bv-table-partial-overturn.md || exit 1; done && grep -q 'phase12_7_no_table_surface' .planning/decisions/ADR-001-bv-table-partial-overturn.md && grep -q 'output_kind.*table' .planning/decisions/ADR-001-bv-table-partial-overturn.md</automated>
  </verify>
  <acceptance_criteria>
    - ADR-001 file exists, ≥ 60 lines: `[ -f .planning/decisions/ADR-001-bv-table-partial-overturn.md ] && [ "$(wc -l < .planning/decisions/ADR-001-bv-table-partial-overturn.md)" -ge 60 ]`
    - H1 ADR-001 prefix: `grep -q '^# ADR-001:' .planning/decisions/ADR-001-bv-table-partial-overturn.md`
    - All 4 H2 sections present: `for s in Status Context Decision Consequences; do grep -q "^## $s" .planning/decisions/ADR-001-bv-table-partial-overturn.md || exit 1; done`
    - Status has Accepted + 2026-05-03: `grep -A 3 '^## Status' .planning/decisions/ADR-001-bv-table-partial-overturn.md | grep -q 'Accepted' && grep -A 3 '^## Status' .planning/decisions/ADR-001-bv-table-partial-overturn.md | grep -q '2026-05-03'`
    - References Phase 12.7, project_v0_events_only_scope, phase12_7_no_table_surface: `grep -q 'Phase 12\.7' .planning/decisions/ADR-001-bv-table-partial-overturn.md && grep -q 'project_v0_events_only_scope' .planning/decisions/ADR-001-bv-table-partial-overturn.md && grep -q 'phase12_7_no_table_surface' .planning/decisions/ADR-001-bv-table-partial-overturn.md`
    - Decision: upsert/delete/retract absent: `grep -E 'app\.(upsert|delete|retract)' .planning/decisions/ADR-001-bv-table-partial-overturn.md | grep -qiE 'remain absent|absent|stay killed'`
    - Decision: output_kind=table: `grep -q 'output_kind.*table' .planning/decisions/ADR-001-bv-table-partial-overturn.md`
    - Has Deferred subsection: `grep -qE '^### Deferred' .planning/decisions/ADR-001-bv-table-partial-overturn.md`
    - References Phase 13.4 / 13.5 / 13.6 follow-ons: `for ph in 13\.4 13\.5 13\.6; do grep -q "Phase ${ph}" .planning/decisions/ADR-001-bv-table-partial-overturn.md || exit 1; done`
    - Has @bv.table example code block: `grep -q '@bv\.table(key=' .planning/decisions/ADR-001-bv-table-partial-overturn.md`
  </acceptance_criteria>
  <done>ADR-001 landed with all 5 Nygard sections + Deferred-for-13.4 subsection. NO commit yet.</done>
</task>

<task type="auto">
  <name>Task 4: CREATE .planning/decisions/ADR-002-polars-op-rename.md (Nygard 5-section)</name>
  <read_first>
    - .planning/phases/13.0-design-contract-spec-docs/13.0-RESEARCH.md
    - .planning/phases/13.0-design-contract-spec-docs/13.0-CONTEXT.md
    - python/beava/_agg.py
    - crates/beava-core/src/agg_op.rs
    - crates/beava-core/src/register_validate.rs
    - crates/beava-bench/configs/fraud-team.json
    - docs/operators/cost-class.md
  </read_first>
  <action>
Use the Write tool to create `.planning/decisions/ADR-002-polars-op-rename.md`. Use the verbatim starter from RESEARCH §2 (lines 268-348).

Required structure:

- H1 (line 1): `# ADR-002: Rename core aggregation ops to Polars conventions`
- H2: `## Status` — `Accepted` + `Date: 2026-05-03`
- H2: `## Context` — describes:
  - Original beava names chose SQL-prose readability: `avg`, `variance`, `stddev`, `count_distinct`, `percentile`
  - 2026-05-03 design session locked Polars-style chained syntax; Polars uses `mean`, `var`, `std`, `n_unique`, `quantile`
  - Polars convention is documented + well-known to data-engineering audiences + consistent with pandas / dplyr / sqlglot
  - Mixing two name families creates persistent friction at language boundary
  - Names baked into:
    - `crates/beava-core/src/agg_compile.rs::parse_agg_kind` (op-string → AggKind)
    - `crates/beava-core/src/register_validate.rs::lifetime_bound_for_op_str`
    - `python/beava/_agg.py` (helper function names)
    - `crates/beava-bench/configs/{small,medium,large,fraud-team}.json`
    - `docs/operators/cost-class.md`
    - approximately 60 test fixtures across the workspace
  - `FORMAT_VERSION` for WAL + snapshot stays at 1 (rename does not change binary record format — only string constants in JSON-shaped op fields)
- H2: `## Decision` — must include this Markdown table verbatim:

  ```
  | Old name | New name | Rationale |
  |----------|----------|-----------|
  | `avg` | `mean` | Polars / NumPy / pandas convention |
  | `variance` | `var` | Polars convention |
  | `stddev` | `std` | Polars convention |
  | `count_distinct` | `n_unique` | Polars convention |
  | `percentile` | `quantile` | Polars convention |
  ```

  Plus this implementation roadmap (numbered list):
  - Phase 13.4 lands server-side rename: `parse_agg_kind` accepts new names; `AggKind` Rust enum variants stay (`AggKind::Avg`, `AggKind::Variance`, etc.) but public string mapping changes
  - Phase 13.4 also updates the four `crates/beava-bench/configs/*.json` files and `docs/operators/cost-class.md`
  - Phase 13.5 lands Python SDK rename in `_agg.py`; old names remain as deprecation aliases (log warning + dispatch to new helper); `bv.ema` stays as alias for `bv.ewma`
  - Phase 13.6 ships TypeScript + Go SDKs with new names from day one (no legacy aliases; v0 unreleased)
  - Phase 13.0 (this phase) per-op pages cite both names: H1 + Signature use NEW name, "Previously called `<old>`" note in Edge Cases section (Plans 13.0-05/06/07/08/09/10/11)
- H2: `## Consequences` — has **Easier:** / **Harder:** / **Follow-on actions:** sub-blocks:
  - Easier: Polars-experienced users transition immediately; `bv.col(...).mean()` reads naturally; cross-language SDK consistency (TS `pipeline.col("x").mean().over("1h")` matches Python `bv.col("x").mean().over("1h")`)
  - Harder: one-time refactor across server + Python + 4 bench configs + ~60 test fixtures (mechanical but multi-touchpoint); pre-13.0 dev users (small group) need to rename local pipelines (mitigation: deprecation aliases for one v0 minor, removed in v0.1)
  - Follow-on actions:
    - Phase 13.4 Plan: server-side rename + bench-config update
    - Phase 13.5 Plan: Python SDK rename + deprecation aliases
    - Phase 13.6 Plans: TS + Go SDKs use new names from day one
    - Phase 13.0 (this phase) per-op pages document both names
    - ADR-002 referenced in CLAUDE.md as Polars-naming locked decision (Plan 13.0-15 closure may add CLAUDE.md footnote)

Length target: 70-90 lines including the rename table.

DO NOT modify any code or rename anything in this task. Phase 13.4 + 13.5 do the actual renames.
  </action>
  <verify>
    <automated>[ -f .planning/decisions/ADR-002-polars-op-rename.md ] && [ "$(wc -l < .planning/decisions/ADR-002-polars-op-rename.md)" -ge 60 ] && grep -q '^# ADR-002:' .planning/decisions/ADR-002-polars-op-rename.md && for s in Status Context Decision Consequences; do grep -q "^## $s" .planning/decisions/ADR-002-polars-op-rename.md || exit 1; done && grep -q 'avg.*mean' .planning/decisions/ADR-002-polars-op-rename.md && grep -q 'count_distinct.*n_unique' .planning/decisions/ADR-002-polars-op-rename.md && grep -q 'percentile.*quantile' .planning/decisions/ADR-002-polars-op-rename.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists, ≥ 60 lines: `[ -f .planning/decisions/ADR-002-polars-op-rename.md ] && [ "$(wc -l < .planning/decisions/ADR-002-polars-op-rename.md)" -ge 60 ]`
    - H1 ADR-002 prefix: `grep -q '^# ADR-002:' .planning/decisions/ADR-002-polars-op-rename.md`
    - All 4 H2 sections present: `for s in Status Context Decision Consequences; do grep -q "^## $s" .planning/decisions/ADR-002-polars-op-rename.md || exit 1; done`
    - Has rename mapping table — all 5 renames present: `for r in "avg.*mean" "variance.*var" "stddev.*std" "count_distinct.*n_unique" "percentile.*quantile"; do grep -qE "$r" .planning/decisions/ADR-002-polars-op-rename.md || exit 1; done`
    - Mentions Phase 13.4 / 13.5 / 13.6 implementation plans: `for ph in 13\.4 13\.5 13\.6; do grep -q "Phase ${ph}" .planning/decisions/ADR-002-polars-op-rename.md || exit 1; done`
    - Notes `bv.ema` stays alias for `bv.ewma`: `grep -q 'ema.*ewma\|ewma.*ema' .planning/decisions/ADR-002-polars-op-rename.md`
    - Notes FORMAT_VERSION stays at 1: `grep -q 'FORMAT_VERSION.*1\|version.*1' .planning/decisions/ADR-002-polars-op-rename.md`
  </acceptance_criteria>
  <done>ADR-002 landed with all 5 Nygard sections + rename mapping table. NO commit yet.</done>
</task>

<task type="auto">
  <name>Task 5: UPDATE memory file project_v0_events_only_scope.md with ADR-001 partial-overturn pointer + commit everything</name>
  <read_first>
    - $HOME/.claude/projects/-Users-petrpan26-work-tally/memory/project_v0_events_only_scope.md
    - .planning/decisions/ADR-001-bv-table-partial-overturn.md (just-created file)
    - .planning/decisions/ADR-002-polars-op-rename.md (just-created file)
  </read_first>
  <action>
Step 1 — Read the current memory file body in full so you understand its structure (Edit tool requires reading first):

```bash
cat $HOME/.claude/projects/-Users-petrpan26-work-tally/memory/project_v0_events_only_scope.md
```

Step 2 — Use the Edit tool to add a partial-overturn pointer block. The exact insertion point depends on the current memory file structure; the pointer block should be inserted at the END of the file body (before any trailing metadata) and worded as:

```markdown
## Partial overturn 2026-05-03 — ADR-001

Phase 13.0 lands `.planning/decisions/ADR-001-bv-table-partial-overturn.md`,
which partially overturns this commitment. The `@bv.table` decorator is
REVIVED in v0 as an aggregation-output decorator only. The decorator captures
a `group_by().agg()` chain and emits a derivation node with `output_kind=table`.
`app.upsert/delete/retract`, `OpNode::TableUpsert/TableDelete/TableRetract`,
`TemporalStore`, `MvccVersion`, `temporal_http`, and `RecordType::Table*` WAL
variants REMAIN ABSENT. The architectural test
`crates/beava-server/tests/phase12_7_no_table_surface.rs` allowlist is amended
in Phase 13.4 to permit `OpNode::Table*` on derivation `output_kind=table` only.

See: `.planning/decisions/ADR-001-bv-table-partial-overturn.md`
```

If the memory file does NOT exist yet (first read fails), create it via the Write tool with header `# project_v0_events_only_scope` plus the partial-overturn pointer block. If the memory file exists but the pointer block is already present (rare), Edit-replace the existing block to ensure date and ADR file path are correct.

Step 3 — Run the full verification suite before commit:

```bash
[ -f .planning/decisions/ADR-001-bv-table-partial-overturn.md ] && \
  [ -f .planning/decisions/ADR-002-polars-op-rename.md ] && \
  [ "$(find docs/sdk-api docs/pipeline-dsl docs/operators/core docs/operators/point-ordinal docs/operators/recency docs/operators/decay docs/operators/velocity docs/operators/sketch docs/operators/buffer-geo docs/concepts docs/architecture examples/wire/schemas examples/python examples/typescript examples/go -name .keep -type f | wc -l)" -eq 15 ] && \
  [ ! -e docs/architecture.md ] && \
  [ -f docs/operators/cost-class.md ]
```

All checks must pass. If any fails, fix before commit.

Step 4 — Stage final pieces + commit. Memory files live OUTSIDE the repo (`$HOME/.claude/projects/...`) and are not tracked by git. Only stage the in-repo artifacts:

```bash
git add .planning/decisions/ADR-001-bv-table-partial-overturn.md \
        .planning/decisions/ADR-002-polars-op-rename.md
```

(Tasks 1 and 2 already staged the deletions and .keep stubs respectively.)

Final commit message (use HEREDOC):

```
docs(13.0-01): NUKE 13 stale docs/*.md + create new dir tree + ADR-001 + ADR-002 + memory pointer

- NUKE: docs/{architecture,http-api,python-sdk,protocol,quickstart,operators,
  index,installation,contributing,comparison,benchmarks,data-scientist-demo}.md,
  docs/http-api-examples.sh, docs/blog/ — all pre-12.6/pre-rename/pre-no-event-time-pivot
- KEEP: docs/operators/cost-class.md (alive Phase 19.2 metadata),
  docs/github-repo-surface-runbook.md (KEEP-AS-REFERENCE)
- CREATE: 15 new dirs under docs/ + examples/ via .keep stubs
  (Wave 2/3 plans populate)
- CREATE: .planning/decisions/ADR-001-bv-table-partial-overturn.md
  (Nygard 5-section; @bv.table aggregation-output revival;
   architectural-test allowlist update DEFERRED to Phase 13.4)
- CREATE: .planning/decisions/ADR-002-polars-op-rename.md
  (Nygard 5-section; avg→mean / variance→var / stddev→std /
   count_distinct→n_unique / percentile→quantile;
   server rename DEFERRED to Phase 13.4; Python SDK rename DEFERRED to Phase 13.5)
- UPDATE: ~/.claude/.../project_v0_events_only_scope memory with ADR-001
  partial-overturn pointer

Per CLAUDE.md TDD §Note 4 doc-only-plan exemption: single docs() commit.
Phase 13.0 spec-docs phase has no microbench/throughput-run requirement.
```
  </action>
  <verify>
    <automated>[ -f .planning/decisions/ADR-001-bv-table-partial-overturn.md ] && [ -f .planning/decisions/ADR-002-polars-op-rename.md ] && [ ! -e docs/architecture.md ] && [ ! -e docs/blog ] && [ -f docs/operators/cost-class.md ] && [ "$(find docs/sdk-api docs/pipeline-dsl docs/operators docs/concepts docs/architecture examples/wire/schemas examples/python examples/typescript examples/go -name .keep -type f 2>/dev/null | wc -l)" -ge 15 ] && git log -1 --format=%s | grep -qE '^docs\(13\.0-01\)'</automated>
  </verify>
  <acceptance_criteria>
    - All ADR + dir + nuke artifacts in place: `[ -f .planning/decisions/ADR-001-bv-table-partial-overturn.md ] && [ -f .planning/decisions/ADR-002-polars-op-rename.md ] && [ ! -e docs/architecture.md ] && [ ! -e docs/blog ] && [ -f docs/operators/cost-class.md ]`
    - 15 .keep files staged: `[ "$(find docs/sdk-api docs/pipeline-dsl docs/operators docs/concepts docs/architecture examples/wire/schemas examples/python examples/typescript examples/go -name .keep -type f 2>/dev/null | wc -l)" -ge 15 ]`
    - Single commit with docs(13.0-01) prefix: `git log -1 --format=%s | grep -qE '^docs\(13\.0-01\)'`
    - Commit references all major artifacts: `git log -1 --format=%B | grep -qE 'NUKE|ADR-001|ADR-002|partial-overturn'`
    - Memory file (if it exists or just-created) has partial-overturn pointer pointing to ADR-001 path: `grep -q 'ADR-001' "$HOME/.claude/projects/-Users-petrpan26-work-tally/memory/project_v0_events_only_scope.md"`
  </acceptance_criteria>
  <done>Single docs(13.0-01) commit lands. All 15 dirs created, 14 stale paths deleted, 2 ADRs landed, memory file updated. Plan 13.0-01 closed.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.0-01-01 | Information Disclosure | ADR-001 + ADR-002 (public design records) | accept | Intentional disclosure to future contributors and SDK porters; ADRs are normative project records. |
| T-13.0-01-02 | Tampering | docs/operators/cost-class.md (KEEP file) | mitigate | Verification step Task 1 explicitly checks cost-class.md is NOT in git rm staging; acceptance criteria asserts file still exists post-commit. |
| T-13.0-01-03 | Repudiation | Memory file outside repo (not version-controlled) | accept | Memory edits are user-machine-local; rollback is via git revert of the partial-overturn block in the memory file (manual) — acceptable risk for solo-developer workflow per CLAUDE.md. |
</threat_model>

<verification>
- 14 stale paths deleted, 15 new dirs created (tracked via .keep stubs), 2 ADRs landed
- ADR-001 has all 5 Nygard sections + Deferred-for-13.4 subsection
- ADR-002 has all 5 Nygard sections + 5-row rename mapping table
- docs/operators/cost-class.md and docs/github-repo-surface-runbook.md preserved
- Memory file project_v0_events_only_scope updated with ADR-001 pointer
- Single docs(13.0-01) commit per CLAUDE.md TDD §Note 4 doc-only-plan exemption
</verification>

<success_criteria>
- All 15 directories under docs/ + examples/ exist (verified via find)
- ADR-001 + ADR-002 exist with proper Nygard structure
- KEEP files (cost-class.md + runbook.md) untouched
- Memory file updated with partial-overturn pointer
- Commit message references NUKE + ADR-001 + ADR-002 + partial-overturn
- Workspace remains clean (no code edits in this plan)
</success_criteria>

<output>
After this plan lands, every Wave 1/2/3 doc-authoring plan can populate the directory tree without scaffolding overhead. Wave 1 plans 02/03/04 unblocked.

</output>
