---
phase: 13.0-design-contract-spec-docs
plan: 10
subsystem: docs
tags: [operators, catalog, polish, bounded-buffer, histogram, hour-of-day, dow-hour, seasonal-deviation, event-type-mix, most-recent-n, reservoir-sample, v0-mem-gov-02, phase-12.9-boxing]

# Dependency graph
requires:
  - phase: 13.0-design-contract-spec-docs
    plan: 05
    provides: 54 op page stubs scaffolded under docs/operators/<family>/<op>.md with H1 + 9 mandatory H2 sections and TODO prose markers labeled by polish plan number; Plan 10 fills the 7 stubs labeled "Plan 13.0-10" — the bounded-buffer subset of the buffer-geo/ family directory (the 4 geo ops in the same buffer-geo/ subdirectory are owned by Plan 13.0-11)
  - phase: 13.0-design-contract-spec-docs
    plan: 06
    provides: Per-op page template established (Signature / Description / Parameters / Returns / Complexity / Examples / Wire / Edge cases / See also — 9 H2 sections; 130-160 LOC range; cost-class cross-link; V0-MEM-GOV-02 citation pattern; cold-start convention; family index template) — Plan 10 inherits verbatim
  - phase: 13.0-design-contract-spec-docs
    plan: 07
    provides: Processing-time citation pattern (cite project_redis_shaped_no_event_time_ever in Edge cases for every op whose dispatch is time-keyed) + cold-entity eviction cross-link convention (V0-MEM-GOV-01 cold_after= TTL pattern) — Plan 10 inherits verbatim and applies to all 7 bounded-buffer ops (4 of 7 are time-keyed: hour_of_day_histogram + dow_hour_histogram + seasonal_deviation use server now_ms() for the bin index; histogram + event_type_mix + most_recent_n + reservoir_sample are arrival-order-only)
  - phase: 13.0-design-contract-spec-docs
    plan: 08
    provides: Family-internal cross-link pattern (See also section forwards to sibling ops in the same family + complementary ops in adjacent families) + cold-entity eviction reciprocal pattern — Plan 10 inherits verbatim; bounded-buffer pages cross-link to sketch/ (quantile, top_k, entropy, bloom_member), point-ordinal/ (last_n, first_n, lag), and decay/ (ew_zscore, ewma) for adjacent-family complementary ops
  - phase: 13.0-design-contract-spec-docs
    plan: 09
    provides: Cross-family note pattern (reciprocal cross-links between adjacent families with Note blocks for ambiguous-classification ops) — Plan 10 inherits the cross-link convention but does NOT need a Note block (no ambiguous-classification ops in the bounded-buffer subset; all 7 ops are unambiguously bounded-buffer family members per RESEARCH §3 + §5)
  - phase: 12.7-table-strip
    provides: V0-EVENTS-ONLY-01 events-only invariant — bounded-buffer pages cite @bv.table aggregation-output decorator (function-form per ADR-001 partial overturn) for the table-output Examples; no app.upsert / app.delete / app.retract surface is exposed
  - phase: 12.8-memory-governance
    provides: V0-MEM-GOV-02 lifetime-aggregation contract — 4 of 7 bounded-buffer ops are unbounded-by-default and require register-time kwarg declaration per the contract: histogram (BoundedByRequiredKwarg("buckets")), most_recent_n (BoundedByRequiredKwarg("n")), reservoir_sample (BoundedByRequiredKwarg("samples")), event_type_mix (BoundedByConfig("max_categories", 256) — soft-defaulted but cap always declared). Structured-error code unbounded_op_in_lifetime_mode cited for all 3 BoundedByRequiredKwarg ops (event_type_mix uses default, never trips the error path). The remaining 3 ops (hour_of_day_histogram, dow_hour_histogram, seasonal_deviation) are O(1) by structural cap (24/168/24-bucket fixed sizes) and don't need the contract treatment but cite V0-MEM-GOV-02 for ceiling documentation.
  - phase: 12.8-memory-governance
    provides: max_categories=256 cap-hit metric (per Phase 12.8 Plan 06 — Prometheus lifetime_op_cap_hit_total counter incremented per categorical-op cap-and-drop firing) — event_type_mix.md cites this metric explicitly in Edge cases as the production-side surfacing for the cap-and-drop ambiguity (proportions sum to (1 − dropped/total), not to 1.0)
  - phase: 12.9-aggop-memory-boxing
    provides: 7-variant AggOp boxing reduces size_of::<AggOp>() from 600 → 80 bytes (CI tripwire at crates/beava-core/tests/per_entity_size_dump.rs::aggop_size_within_cap) — 3 of 7 bounded-buffer ops are among the boxed variants: HourOfDayHistogram (state 192 B on heap), SeasonalDeviation (state ~600 B on heap), EventTypeMix (state on heap). Cited per-page in the Description/Complexity sections with cross-link to crates/beava-core/src/agg_op.rs line numbers + 12.9-SUMMARY.md.
  - phase: 19.2-operator-uniformity
    provides: docs/operators/cost-class.md (alive Phase 19.2 metadata, KEPT, NOT touched) — cross-linked from each polished page's Complexity section for CPU tier (4 of 7 bounded-buffer ops are Tier 1: histogram, hour_of_day_histogram, dow_hour_histogram, seasonal_deviation; 3 of 7 are Tier 3: event_type_mix, most_recent_n, reservoir_sample due to BTreeMap insert / Value::clone / xorshift PRNG overhead). histogram.md flags an apply-vs-query asymmetry: apply is Tier 1 (linear bucket scan + saturating add), query is Tier 3 (BTreeMap allocation per get).
provides:
  - 7 bounded-buffer/ op pages polished from Plan 05 stubs (histogram, hour_of_day_histogram, dow_hour_histogram, seasonal_deviation, event_type_mix, most_recent_n, reservoir_sample) — each 148-174 LOC; TODO markers replaced with full Description / Parameters / Returns / Complexity / Examples / Edge cases / See also prose
  - 4 of 7 BoundedBy* ops cite V0-MEM-GOV-02 register-time enforcement pattern: histogram (BoundedByRequiredKwarg("buckets")), most_recent_n (BoundedByRequiredKwarg("n")), reservoir_sample (BoundedByRequiredKwarg("samples")), event_type_mix (BoundedByConfig("max_categories", 256)). The 3 BoundedByRequiredKwarg ops cite the structured-error code unbounded_op_in_lifetime_mode (3 occurrences each in their pages); event_type_mix uses the BoundedByConfig default (never trips the error path) and instead cites the Phase 12.8 cap-hit metric as the surfacing channel.
  - event_type_mix.md cites Phase 12.8 max_categories cap-hit metric (lifetime_op_cap_hit_total counter) explicitly in Description + Edge cases for the cap-and-drop ambiguity (proportions sum to (1 − dropped/total), not to 1.0). Allowlist mode (categories=[...]) documented as the recommended pattern when the category set is known in advance — backed by AHashSet for O(1) membership.
  - Phase 12.9 boxing context cited in 3 pages: hour_of_day_histogram.md (HourOfDayHistogramState 192 B on heap), seasonal_deviation.md (SeasonalDeviationState ~600 B on heap), event_type_mix.md (EventTypeMixState on heap behind Box). Each cites crates/beava-core/src/agg_op.rs line number + 12.9-SUMMARY.md cross-link. 4 of 7 ops are NOT Phase 12.9 boxed (histogram, dow_hour_histogram, most_recent_n, reservoir_sample) — their state is small enough to live inline in the AggOp variant.
  - Processing-time citation on all 7 ops (project_redis_shaped_no_event_time_ever) — 4 ops are time-keyed via server now_ms() (hour_of_day_histogram, dow_hour_histogram, seasonal_deviation use rem_euclid for negative-now_ms safety per the established Phase 12.6+ pattern; histogram is value-keyed not time-keyed but cites it for arrival-order discipline); 3 ops (event_type_mix, most_recent_n, reservoir_sample) cite arrival-order discipline only (no time-keying)
  - Cold-start return convention: histogram returns the zero-filled dict (full label set, all 0s); hour_of_day_histogram returns 24 keys at 0; dow_hour_histogram returns 168 keys at 0; seasonal_deviation returns null (no latest observation to score); event_type_mix returns {} (empty dict); most_recent_n returns [] (empty list); reservoir_sample returns [] (empty list). All 7 ops cite their cold-start convention explicitly in Edge cases.
  - Cold-entity eviction (V0-MEM-GOV-01) cross-linked on all 7 ops as the windowing pattern (since 0 of 7 bounded-buffer ops support a window= kwarg in v0 — they are all lifetime-only). The cold_after= kwarg on @bv.event drops the per-entity state per Redis-TTL pattern; bounded-buffer state rebuilds fresh from the next post-eviction matching event.
  - Single docs(13.0-10) commit (60bbc8d) per CLAUDE.md TDD §Note 4 doc-only-plan exemption — 7 polished pages, +797/-145 LOC across the 7 bounded-buffer pages
affects:
  - 13.0-11 (buffer-geo polish — 4 geo ops + buffer-geo/index.md): inherits the per-page template, processing-time citation, cold-start convention; OWNS the buffer-geo/index.md combined family overview covering all 11 ops (7 bounded-buffer + 4 geo)
  - 13.0-12 (pipeline-dsl/compilation-rules.md): forward-referenced from every polished bounded-buffer page's See also section; the link target page does not yet exist (Plan 12 creates it)
  - 13.0-15 (closure plan): runs scripts/check_op_page_coverage.py + verifies remaining TODO markers across the catalogue; this plan's contribution: 0 TODOs in 7 bounded-buffer/ pages
  - 13.4 (engine prep): polished pages reference target post-13.4 wire shapes (verb-style routes, kind=table + derivation output_kind="table" per ADR-001 partial overturn) — 13.4 implements the engine to match. The Phase 12.9 boxing AggOp variants (HourOfDayHistogram / SeasonalDeviation / EventTypeMix) are already in tree; 13.4 must keep the boxed shape (don't unbox during the rename refactor).
  - 13.5 (Python SDK + bench): polished pages document the canonical kwarg names (buckets= for histogram, n= for most_recent_n, samples= for reservoir_sample, max_categories=256 + categories=[...] for event_type_mix) — 13.5 must keep these signature shapes. The histogram bucket-label format ("<10", "10-50", ">=50") is part of the contract per the Edge cases note ("Stable across versions — Python SDKs can dict-key on the labels").
  - 13.6 (TS + Go SDKs): porters consume the polished pages for SDK-surface parity. The kwarg names + structured-error code unbounded_op_in_lifetime_mode + cap-and-drop ambiguity for event_type_mix are wire-form contracts they must respect. The histogram bucket-label format is a wire-form contract.

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-op page template inherited from Plans 06/07/08/09 verbatim: H1 + one-line tagline + 9 H2 sections (Signature / Description / Parameters / Returns / Complexity / Examples / Wire / Edge cases / See also). Length 148-174 LOC across all 7 bounded-buffer ops; 4 of 7 are Tier 1, 3 of 7 are Tier 3 per cost-class.md. All 7 are lifetime-only (no window= kwarg); 4 of 7 are O(1) memory bound by structural cap, 3 of 7 are BoundedByRequiredKwarg, 1 of 7 is BoundedByConfig (event_type_mix)."
    - "BoundedByRequiredKwarg pattern (3 of 7 ops): histogram / most_recent_n / reservoir_sample cite the required kwarg in (1) Signature (REQUIRED comment), (2) Description (explicit 'kwarg is REQUIRED per V0-MEM-GOV-02' paragraph), (3) Parameters table ('Yes' in Required column with V0-MEM-GOV-02 cross-link), (4) Edge cases (rejected with structured error code unbounded_op_in_lifetime_mode). Establishes the BoundedByRequiredKwarg surfacing pattern across the catalog — same shape as point-ordinal/last_n.md and point-ordinal/first_n.md from Plan 07."
    - "BoundedByConfig soft-default pattern (1 of 7 ops): event_type_mix cites max_categories=256 as a BoundedByConfig soft default — the kwarg is OPTIONAL but the per-entity ceiling is always declared. Cap-and-drop semantics documented (proportions sum to (1 − dropped/total) when cap hit, not to 1.0); cap-hit metric (Phase 12.8 lifetime_op_cap_hit_total) cited as the production surfacing channel; allowlist mode (categories=[...]) documented as the recommended workaround when the category set is known in advance. Mirrors sketch/entropy.md (BoundedByConfig sibling)."
    - "Structural-cap O(1) pattern (3 of 7 ops): hour_of_day_histogram (24 bins) / dow_hour_histogram (168 bins) / seasonal_deviation (24 buckets + latest-obs memo) cite their fixed structural caps in (1) Description prose, (2) Parameters note ('No field= kwarg' or 'No required kwargs'), (3) Complexity table ('O(1) — fixed [u64; 24] = 192 bytes' or similar). Establishes that not all bounded-buffer ops need the BoundedBy* contract treatment — structural-cap ops cite V0-MEM-GOV-02 for ceiling documentation but don't need the register-time enforcement path."
    - "Lifetime-only family pattern: ALL 7 bounded-buffer ops are lifetime-only (no window= kwarg in v0). Each page cites this in (1) Description ('There is no window= kwarg in v0 — bv.<op> is lifetime-only'), (2) Complexity ('Lifetime mode — Required'), (3) Edge cases ('window= kwarg attempted: raises TypeError ... For a windowed analogue use @bv.event(cold_after=...) to bound the lifetime via per-entity TTL'). Establishes the lifetime-only-with-cold-entity-TTL composition pattern as the windowing workaround for the entire bounded-buffer family. Distinct from velocity/ where 7 of 9 ops cite window= as REQUIRED."
    - "Phase 12.9 boxing citation pattern (3 of 7 ops): hour_of_day_histogram.md / seasonal_deviation.md / event_type_mix.md cite the Phase 12.9 boxing in Parameters or Description prose with explicit cross-link to (1) crates/beava-core/src/agg_op.rs line number, (2) .planning/phases/12.9-aggop-memory-boxing/12.9-SUMMARY.md, (3) Complexity table ('Boxed inside AggOp per Phase 12.9 to fit the 80-byte enum cap'). Establishes the per-op pointer-to-architecture-decision pattern for variants that participate in the size_of::<AggOp>() = 80 B CI tripwire."
    - "Apply-vs-query asymmetry pattern: histogram.md is the only bounded-buffer op with materially different apply-time vs query-time CPU tiers. Documented in Complexity table as a two-row entry ('CPU per event — Tier 1 (linear bucket scan)' + 'Query — Tier 3 allocates a BTreeMap of len(buckets)+1 entries on each app.get(...)'). Establishes the per-tier asymmetry surfacing pattern for any future op with this profile (e.g. dict-returning aggregations whose apply path is cheap but query path materializes per-call)."
    - "Processing-time citation pattern (project_redis_shaped_no_event_time_ever): all 7 bounded-buffer ops state arrival-order discipline explicitly in Edge cases. 3 ops (hour_of_day_histogram / dow_hour_histogram / seasonal_deviation) are time-keyed via server now_ms() rem_euclid (handles negative now_ms safely without panic or wraparound — established Phase 12.6+ pattern documented per-op). 4 ops (histogram / event_type_mix / most_recent_n / reservoir_sample) cite arrival-order discipline only (no time-keying — buckets / categories / ring slots / reservoir samples are populated in arrival order)."
    - "Cold-start return convention documented per-op consistently: histogram returns the zero-filled dict (full label set), hour_of_day_histogram returns 24 keys at 0, dow_hour_histogram returns 168 keys at 0 (all 'never null'), seasonal_deviation returns null (no latest observation to score), event_type_mix returns {} (empty dict, never null), most_recent_n returns [] (empty list, never null), reservoir_sample returns [] (empty list, never null). The split: count-based ops return the zero-element of their return type (full-zeros dict / empty dict / empty list); deviation/score ops return null. Family index (Plan 11 owns) will enumerate this split for SDK porters."
    - "Cold-entity eviction (V0-MEM-GOV-01) cross-linked on every bounded-buffer op: @bv.event(cold_after=...) drops the underlying state per Redis-TTL pattern; bounded-buffer state rebuilds fresh from the next post-eviction matching event. The lifetime-only nature of the bounded-buffer family makes the cold-entity TTL the only windowing mechanism available — established the composition pattern explicitly in every page."
    - "reservoir_sample determinism guarantee documented: the xorshift64 PRNG is seeded from items_seen XOR'd with the 0x9E37_79B9_7F4A_7C15 golden-ratio constant — no calls to rand:: or wall-clock — so a snapshot + WAL replay reconstructs the SAME reservoir contents. This makes reservoir_sample safe for replay-determinism contracts. Documented in Description prose + Edge cases. Cites Vitter (1985) Algorithm R for sampling-quality guarantee."
    - "Allowlist serde-stability pattern (event_type_mix): the allowed_set: AHashSet field is #[serde(skip)] for snapshot stability; it is rebuilt lazily from allowed: Vec<String> on the first apply post-deserialization. Documented in Edge cases as the one-time post-snapshot-load cost. Establishes the AHashSet-skipped + Vec-stable serde pattern for SDK porters writing snapshot-replay tests."
    - "Cross-family See also pattern: bounded-buffer pages cross-link to sketch/ (quantile, top_k, entropy, bloom_member), point-ordinal/ (last_n, first_n, lag), decay/ (ew_zscore, ewma) for adjacent-family complementary ops. histogram → sketch/quantile (quantile-estimate companion); event_type_mix → sketch/entropy + sketch/top_k (distribution-shape companions); most_recent_n → point-ordinal/last_n + point-ordinal/first_n + point-ordinal/lag + ./reservoir_sample (recency family); reservoir_sample → ./most_recent_n + sketch/top_k (frequency-weighted-sample companion); seasonal_deviation → velocity/z_score + decay/ew_zscore (z-score family). Establishes the cross-family complementary-op cross-link pattern; SDK porters can navigate the catalog laterally without going up to the master index."

# Files
key-files:
  created:
    - .planning/phases/13.0-design-contract-spec-docs/13.0-10-SUMMARY.md (this file)
  modified:
    - docs/operators/buffer-geo/histogram.md (~26 → 158 LOC; +132 LOC; full polish — Tier 1 apply / Tier 3 query, BoundedByRequiredKwarg("buckets"), lifetime-only; fixed-bucket count histogram of a numeric field; query path materializes BTreeMap per get)
    - docs/operators/buffer-geo/hour_of_day_histogram.md (~28 → 148 LOC; +120 LOC; full polish — Tier 1, O(1) by structural cap (24 bins = 192 B), lifetime-only; UTC hour-of-day count histogram; field-less by design; Phase 12.9 boxed variant)
    - docs/operators/buffer-geo/dow_hour_histogram.md (~28 → 154 LOC; +126 LOC; full polish — Tier 1, O(1) by structural cap (168 bins = 1344 B), lifetime-only; UTC day-of-week × hour count histogram; field-less by design; unboxed in AggOp because Vec<u64> header is 24 B inline)
    - docs/operators/buffer-geo/seasonal_deviation.md (~26 → 166 LOC; +140 LOC; full polish — Tier 1, O(1) by structural cap (24 HourBucket + latest-obs memo ≈ 600 B), lifetime-only; z-score against per-hour baseline; null until n ≥ 2 in current bucket; Phase 12.9 boxed variant)
    - docs/operators/buffer-geo/event_type_mix.md (~26 → 169 LOC; +143 LOC; full polish — Tier 3, BoundedByConfig("max_categories", 256) soft default, lifetime-only; per-category proportion over the entity's lifetime; cap-and-drop semantics with Phase 12.8 lifetime_op_cap_hit_total metric; allowlist mode (categories=[...]) with AHashSet for O(1) membership; Phase 12.9 boxed variant)
    - docs/operators/buffer-geo/most_recent_n.md (~26 → 159 LOC; +133 LOC; full polish — Tier 3, BoundedByRequiredKwarg("n"), lifetime-only; circular buffer of N most recent values in arrival order; clone-path variance dominated by Value::Bytes large-payload cost)
    - docs/operators/buffer-geo/reservoir_sample.md (~26 → 174 LOC; +148 LOC; full polish — Tier 3, BoundedByRequiredKwarg("samples"), lifetime-only; uniform K-sample over full history via Vitter Algorithm R (1985); xorshift64 PRNG seeded from items_seen for replay-determinism guarantee; no rand:: dependency)

# Decisions
decisions:
  - "Per-op page template inherited verbatim from Plans 06/07/08/09 (no structural changes to the H2 layout); polish only fills the 5 prose sections per op (Description / Parameters / Returns / Examples / Edge cases) plus enriches Complexity + See also where applicable. The Signature + Wire blocks were rewritten to reflect actual Python signatures (e.g. event_type_mix has both categories= and max_categories= kwargs; reservoir_sample uses samples= not k=) rather than the Plan 05 placeholder `(...)`."
  - "BoundedByRequiredKwarg surfacing pattern (3 of 7 ops, plus 1 BoundedByConfig variant): histogram / most_recent_n / reservoir_sample each cite their required kwarg in 4 places: Signature (REQUIRED comment), Description (explicit dedicated paragraph), Parameters table (Yes in Required column), Edge cases (structured-error code unbounded_op_in_lifetime_mode rejected at register-time). event_type_mix uses BoundedByConfig with max_categories=256 default — kwarg is optional, but cap-and-drop semantics documented + Phase 12.8 cap-hit metric cited as production surfacing. Mirrors point-ordinal/last_n.md + point-ordinal/first_n.md + sketch/entropy.md from Plans 07 + 06."
  - "Structural-cap O(1) treatment (3 of 7 ops): hour_of_day_histogram / dow_hour_histogram / seasonal_deviation cite V0-MEM-GOV-02 in Complexity table for ceiling documentation but do NOT cite the register-time enforcement path (no required kwarg to check). The structural caps (24/168/24-bucket fixed sizes) are documented in (1) Description ('the operator carries no user-supplied size kwarg'), (2) Complexity ('O(1) — fixed [u64; 24] = 192 bytes per V0-MEM-GOV-02'). Establishes that not all V0-MEM-GOV-02-cited ops need the register-time enforcement treatment — structural-cap ops just need ceiling documentation."
  - "Lifetime-only family rule (7 of 7 ops): ALL 7 bounded-buffer ops are lifetime-only — no window= kwarg in v0. Each page cites this consistently in Description + Complexity + Edge cases (window= attempted → TypeError + cold_after= compose recommendation). Distinct from velocity/ (Plan 09) where 7 of 9 ops require window=. Establishes the lifetime-only-with-cold-entity-TTL composition pattern as the windowing workaround for the entire bounded-buffer family."
  - "Phase 12.9 boxing citation rule (3 of 7 ops): hour_of_day_histogram (HourOfDayHistogramState 192 B on heap) + seasonal_deviation (SeasonalDeviationState ~600 B on heap) + event_type_mix (EventTypeMixState BTreeMap on heap) — these 3 are the bounded-buffer subset of the 7 fat AggOp variants boxed in Phase 12.9 to fit the 80-byte enum cap. Each cites crates/beava-core/src/agg_op.rs line number + 12.9-SUMMARY.md cross-link in Parameters or Description. The remaining 4 ops (histogram / dow_hour_histogram / most_recent_n / reservoir_sample) are NOT Phase 12.9 boxed — their state is small enough to live inline (histogram has BTreeMap + Vec headers; dow_hour_histogram has Vec<u64> 24 B header; most_recent_n + reservoir_sample have Vec<Value> headers). Documented per-op for SDK porters who need to know which variants participate in the 80 B CI tripwire."
  - "Apply-vs-query asymmetry surfacing rule (1 of 7 ops): histogram.md is the only bounded-buffer op with materially different apply-time vs query-time CPU tiers — Tier 1 apply (linear bucket scan + saturating add) vs Tier 3 query (BTreeMap allocation per app.get(...)). Documented in Complexity table as a two-row entry to flag the asymmetry. Establishes the per-tier asymmetry surfacing pattern for any future op with this profile (apply cheap, query materializes)."
  - "Processing-time citation rule (project_redis_shaped_no_event_time_ever): all 7 bounded-buffer ops cite arrival-order discipline in Edge cases. 3 of 7 are time-keyed via server now_ms() rem_euclid (hour_of_day_histogram / dow_hour_histogram / seasonal_deviation handle negative now_ms safely — established Phase 12.6+ pattern). 4 of 7 are arrival-order-only (histogram / event_type_mix / most_recent_n / reservoir_sample have no time-keying — buckets / categories / ring slots / reservoir samples are populated in arrival order). All 7 cite the architectural commitment explicitly so SDK porters and benchmark authors don't expect event-time semantics."
  - "Cold-start return convention documented per-op consistently: histogram returns the zero-filled dict (full label set, all 0s); hour_of_day_histogram returns 24 keys at 0; dow_hour_histogram returns 168 keys at 0; seasonal_deviation returns null (no latest observation to score); event_type_mix returns {} (empty dict); most_recent_n returns [] (empty list); reservoir_sample returns [] (empty list). The split: count-based ops return the zero-element of their return type (full-zeros dict / empty dict / empty list); deviation/score ops return null. Pattern documented per-op in Edge cases; family index (Plan 11 owns) will enumerate the split."
  - "Cold-entity eviction (V0-MEM-GOV-01) cross-link rule: every bounded-buffer page cross-links to V0-MEM-GOV-01 in See also AND cites the @bv.event(cold_after=...) compose pattern in Edge cases as the windowing workaround. The lifetime-only nature of the family makes cold-entity TTL the only windowing mechanism — establishes the composition pattern explicitly across the family for SDK porters in 13.5/13.6."
  - "reservoir_sample determinism documentation rule: the xorshift64 PRNG seeded from items_seen XOR'd with the 0x9E37_79B9_7F4A_7C15 golden-ratio constant is documented in Description (no rand:: dependency) + Edge cases (snapshot + WAL replay reconstructs the same reservoir). Cites Vitter (1985) Algorithm R for the sampling-quality guarantee. Establishes the per-op replay-determinism documentation pattern for any future op that uses a deterministic PRNG."
  - "event_type_mix dual-mode documentation rule: the page documents BOTH the soft-cap mode (max_categories=256 default with cap-and-drop semantics) AND the allowlist mode (categories=[...] with AHashSet O(1) membership), plus the interaction (allowlist takes precedence over the cap when both are set, making the cap-and-drop path unreachable). Documented in Description (3 paragraphs — one per mode + interaction) + Parameters + Edge cases. Establishes the dual-mode-with-precedence documentation pattern for SDK porters."
  - "buffer-geo/index.md OWNERSHIP rule: the combined buffer-geo/index.md (covering all 11 ops in the buffer-geo/ subdirectory — 7 bounded-buffer + 4 geo) is OWNED by Plan 13.0-11, NOT by Plan 13.0-10. Plan 10 polishes the 7 bounded-buffer pages only; Plan 11 polishes the 4 geo pages AND writes the combined index. Documented in (1) Plan 13.0-10-PLAN.md objective, (2) the docs(13.0-10) commit message ('buffer-geo/index.md is owned by Plan 13.0-11 (covers full 11-op family)'), (3) this SUMMARY.md affects: section. Verified: docs/operators/buffer-geo/index.md does NOT exist post-Plan-10; docs/operators/buffer-geo/.keep marker still present from Plan 05 scaffold."
  - "Per CLAUDE.md TDD §Note 4 doc-only-plan exemption: single docs(13.0-10) commit covering 7 polished pages (commit 60bbc8d, +797/-145 LOC) — no red-test commit required since the 'test' (acceptance grep loops + per-op LOC checks + 9-section count) is mechanical and passes by construction post-polish. SUMMARY.md committed separately (this file) per Plan 09's pattern (commit 89cdd1b added 13.0-09-SUMMARY.md as a separate docs() commit)."

# Metrics
metrics:
  duration_min: 25
  completed: 2026-05-04T00:15:00Z
  tasks_completed: 1
  loc_added: 797
  loc_removed: 145
  files_added: 1
  files_modified: 7
  commits: 2
---

# Phase 13.0 Plan 10: Operator catalog polish — bounded-buffer (7) Summary

## One-liner

Polished 7 bounded-buffer/ op pages (`histogram`, `hour_of_day_histogram`,
`dow_hour_histogram`, `seasonal_deviation`, `event_type_mix`, `most_recent_n`,
`reservoir_sample`) from Plan 05 stubs into full-prose spec documents —
Description / Parameters / Returns / Complexity / Examples / Edge cases / See
also — with 4 of 7 BoundedBy* ops citing V0-MEM-GOV-02 register-time
enforcement (3 BoundedByRequiredKwarg + 1 BoundedByConfig); 3 of 7 ops citing
Phase 12.9 boxing for the `size_of::<AggOp>() = 80 B` enum cap; 4 of 7 ops
time-keyed via server `now_ms()` rem_euclid; ALL 7 ops lifetime-only with
`@bv.event(cold_after=...)` documented as the windowing workaround per
V0-MEM-GOV-01. The 4 geo/ ops in the same `buffer-geo/` subdirectory
(geo_velocity, geo_distance, geo_spread, distance_from_home) AND the combined
`buffer-geo/index.md` family overview are owned by Plan 13.0-11 — verified
that `docs/operators/buffer-geo/index.md` does NOT exist post-Plan-10.

## What landed

**7 bounded-buffer op pages polished** — each 148-174 LOC, all 9 mandatory H2
sections, 0 TODO markers remaining:

- `docs/operators/buffer-geo/histogram.md` (158 LOC) — Fixed-bucket count
  histogram of a numeric field. **BoundedByRequiredKwarg("buckets")** —
  `unbounded_op_in_lifetime_mode` rejected at register-time when missing.
  **Tier 1 apply / Tier 3 query asymmetry** documented (linear bucket scan +
  saturating add on apply; BTreeMap allocation per `app.get(...)`). Per-entity
  memory: `(len(buckets) + 1) × 8` bytes.

- `docs/operators/buffer-geo/hour_of_day_histogram.md` (148 LOC) — UTC
  hour-of-day 24-bin count histogram. **O(1) by structural cap** (24 bins =
  192 bytes). Field-less by design; bin index = `(now_ms / 3_600_000) mod 24`.
  **Phase 12.9 boxed variant** — `HourOfDayHistogramState` lives on heap
  behind a `Box` to fit the 80 B `AggOp` enum cap. Tier 1.

- `docs/operators/buffer-geo/dow_hour_histogram.md` (154 LOC) — UTC
  day-of-week × hour 168-bin count histogram. **O(1) by structural cap**
  (168 bins = 1344 bytes). Field-less by design. **NOT Phase 12.9 boxed**
  (state is `Vec<u64>` whose stack footprint is just the 24-byte Vec header,
  inlined in the AggOp variant). Tier 1.

- `docs/operators/buffer-geo/seasonal_deviation.md` (166 LOC) — Z-score
  against per-hour baseline of `field`. **O(1) by structural cap** (24
  `HourBucket` triples + latest-obs memo ≈ 600 bytes). **Phase 12.9 boxed
  variant** — `SeasonalDeviationState` on heap behind a `Box`. Returns null
  until `n ≥ 2` in the current bucket. Single-pass `(n, sum, sum_sq)` Welford-
  incompatible textbook variance — catastrophic-cancellation tradeoff
  documented for v0; Welford rewrite deferred to v0.1+. Tier 1.

- `docs/operators/buffer-geo/event_type_mix.md` (169 LOC) — Per-category
  proportion over entity lifetime. **BoundedByConfig("max_categories", 256)**
  — soft-defaulted (kwarg optional). Cap-and-drop semantics documented
  (proportions sum to `(1 − dropped/total)` when cap hit, not 1.0); Phase
  12.8 `lifetime_op_cap_hit_total` metric cited as production surfacing.
  **Allowlist mode** (`categories=[...]`) documented as the recommended
  workaround when category set known in advance — backed by `AHashSet` for
  O(1) membership; allowlist takes precedence over cap. **Phase 12.9 boxed
  variant** — `EventTypeMixState` BTreeMap on heap. Tier 3.

- `docs/operators/buffer-geo/most_recent_n.md` (159 LOC) — Circular buffer
  of last `n` non-null values in arrival order. **BoundedByRequiredKwarg("n")**
  — `unbounded_op_in_lifetime_mode` rejected at register-time when missing.
  Per-entity memory: `n × sizeof(Value)` bytes. **Clone-path variance**
  dominated by `Value::Bytes` large-payload cost; documented per-op as a
  high-throughput-with-large-payload caveat. Tier 3.

- `docs/operators/buffer-geo/reservoir_sample.md` (174 LOC) — Uniform
  K-sample over full history via **Vitter Algorithm R (1985)**.
  **BoundedByRequiredKwarg("samples")** — `unbounded_op_in_lifetime_mode`
  rejected at register-time. Per-entity memory: `samples × sizeof(Value)` +
  `u64` for `items_seen`. **Determinism guarantee documented**: xorshift64
  PRNG seeded from `items_seen` XOR'd with the `0x9E37_79B9_7F4A_7C15`
  golden-ratio constant — no `rand::` or wall-clock dependency, snapshot +
  WAL replay reconstructs the same reservoir contents. Tier 3.

**Single docs(13.0-10) commit:** `60bbc8d` (+797/-145 LOC across the 7 pages)
— per CLAUDE.md TDD §Note 4 doc-only-plan exemption.

**SUMMARY commit (this file):** separate `docs(13.0-10): SUMMARY for
bounded-buffer-family operator catalog polish` per Plan 09's pattern.

**NOT touched** (per plan + sequential-on-main-tree constraint):
- `STATE.md` / `ROADMAP.md` / `CORRECTNESS-PATH.md` / `CLAUDE.md` (closure
  Plan 13.0-15 owns these)
- `REQUIREMENTS.md` (no requirements field on this plan; closure plan owns
  requirements traceability)
- `docs/operators/buffer-geo/index.md` (Plan 13.0-11 owns the combined
  family overview covering all 11 ops)
- The 4 geo/ ops in the same buffer-geo/ subdirectory (Plan 13.0-11 owns
  geo_velocity, geo_distance, geo_spread, distance_from_home)
- `docs/operators/cost-class.md` (alive Phase 19.2 metadata, KEPT)
- All Rust source under `crates/` and Python source under `python/` (this is
  a doc-only plan)

## Verification (acceptance criteria)

All 6 acceptance criteria from `13.0-10-PLAN.md` PASS:

1. **All 7 bounded-buffer ops polished — no TODO (Plan 13.0-) markers:**
   ```bash
   for op in histogram hour_of_day_histogram dow_hour_histogram \
             seasonal_deviation event_type_mix most_recent_n \
             reservoir_sample; do
     ! grep -qE 'TODO \(Plan 13\.0-' docs/operators/buffer-geo/${op}.md \
       || { echo "TODO in $op"; exit 1; }
   done
   # Result: PASS — 0 TODO markers in any of the 7 pages
   ```

2. **All 7 ops ≥ 80 lines:**
   ```bash
   for op in histogram hour_of_day_histogram dow_hour_histogram \
             seasonal_deviation event_type_mix most_recent_n \
             reservoir_sample; do
     [ "$(wc -l < docs/operators/buffer-geo/${op}.md)" -ge 80 ] || exit 1
   done
   # Result: PASS — line counts: 158, 148, 154, 166, 169, 159, 174
   ```

3. **histogram cites buckets + V0-MEM-GOV / BoundedByRequiredKwarg:**
   ```bash
   grep -q 'buckets' docs/operators/buffer-geo/histogram.md
   grep -qE 'V0-MEM-GOV|BoundedByRequiredKwarg' docs/operators/buffer-geo/histogram.md
   # Result: PASS — V0-MEM-GOV cited 7 times, BoundedBy* cited 4 times
   ```

4. **event_type_mix cites max_categories:**
   ```bash
   grep -q 'max_categories' docs/operators/buffer-geo/event_type_mix.md
   # Result: PASS — max_categories cited (8 times in the page; signature,
   # description, parameters, complexity, edge cases)
   ```

5. **most_recent_n + reservoir_sample cite required kwarg:**
   ```bash
   grep -qE 'V0-MEM-GOV|BoundedByRequiredKwarg' \
     docs/operators/buffer-geo/most_recent_n.md
   grep -qE 'V0-MEM-GOV|BoundedByRequiredKwarg' \
     docs/operators/buffer-geo/reservoir_sample.md
   # Result: PASS — both cite V0-MEM-GOV-02 + BoundedByRequiredKwarg
   ```

6. **Single docs(13.0-10) commit + commit notes geo deferred to Plan 11:**
   ```bash
   git log -1 --format=%s | grep -qE '^docs\(13\.0-10\)'
   git log -1 --format=%B | grep -qE 'geo|13.0-11|Plan 11'
   # Result: PASS — commit 60bbc8d "docs(13.0-10): polish 7 bounded-buffer
   # op pages — buffer-geo/ subset (excluding 4 geo ops)" with body note
   # "Note: 4 geo/ ops in same buffer-geo/ subdirectory polished by Plan
   # 13.0-11. buffer-geo/index.md is owned by Plan 13.0-11 (covers full
   # 11-op family)."
   ```

**Additional verification (per success_criteria from prompt):**

- **All 9 mandatory H2 sections per page:** PASS — `grep -c "^## "` returns
  9 for each of the 7 pages (Signature, Description, Parameters, Returns,
  Complexity, Examples, Wire, Edge cases, See also).

- **No modifications to STATE/ROADMAP/CORRECTNESS-PATH/CLAUDE.md:** PASS —
  the docs(13.0-10) commit `60bbc8d` modifies only the 7 bounded-buffer op
  pages; this SUMMARY commit modifies only `.planning/phases/13.0-design-
  contract-spec-docs/13.0-10-SUMMARY.md`.

## Threat Flags

None. The polish work introduces no new security-relevant surface — public
operator catalog pages are intentional disclosure per the threat model
(`T-13.0-10-01: Information Disclosure / accept`).

## Known Stubs

None. All 7 bounded-buffer op pages are fully polished (148-174 LOC per
page, 9/9 mandatory H2 sections per page, 0 TODO markers per page). No
placeholder text, no "coming soon" notes, no unwired components. The 4 geo/
ops in the same `buffer-geo/` subdirectory remain as Plan 05 stubs but are
explicitly OUT OF Plan 10's scope (Plan 13.0-11 owns them) — they are not
known stubs of THIS plan, they are pre-existing stubs assigned to a
sibling plan.

## Deviations from Plan

None — plan executed exactly as written.

The `13.0-10-PLAN.md` `<action>` block guidance was followed verbatim for all
7 ops:

- histogram: BoundedByRequiredKwarg("buckets") cited; transaction-amount
  histogram example matches plan suggestion (buckets=[0, 10, 50, 100, 500,
  inf] interpreted as [10.0, 50.0, 100.0, 500.0] for the user-amount
  example since the underlying `+inf` cell is automatic; explicit fractional
  numbers used to avoid the implicit-`.0` rendering confusion in the
  bucket-label format).
- hour_of_day_histogram: O(24) memory cited; user activity by hour example
  matches.
- dow_hour_histogram: O(168) memory cited; user activity Mon-9am vs Fri-7pm
  example matches.
- seasonal_deviation: z-score against hour-of-day baseline; uses
  `hour_of_day_histogram` state-shape (HourBucket triples) per plan
  guidance; null on cold-start documented.
- event_type_mix: BoundedByConfig("max_categories", 256) cited; cap-hit
  metric (Phase 12.8 lifetime_op_cap_hit_total) cited; cap-and-drop +
  allowlist-drop semantics documented; plan guidance about "most-frequent +
  'other' bucket" reframed to match the actual implementation, which is
  silent drop (not 'other'-bucket collapse) — the plan's "OTHER bucket"
  phrasing was approximate; actual behavior surfaces via the cap-hit metric
  with proportions summing to (1 − dropped/total). Documented explicitly so
  SDK porters in 13.6 implement the actual behavior, not the approximate
  description.
- most_recent_n: BoundedByRequiredKwarg("n") cited; "last 10 IPs" example
  matches.
- reservoir_sample: BoundedByRequiredKwarg("samples") cited; Vitter
  Algorithm R cited; "uniform sample of 100 transactions" example matches;
  determinism guarantee (xorshift PRNG, no wall-clock) documented as
  additional value-add beyond plan guidance.

The minor reframe of event_type_mix's cap-and-drop semantics (silent drop
+ cap-hit metric, not "OTHER bucket collapse") is a documentation-accuracy
fix rather than a deviation from plan intent — the plan was written from a
high-level capacity-planning perspective, and the actual implementation
surfaces the dropped category count via the metric channel (Phase 12.8
introduced this; 12.8-SUMMARY documents the design). Documented per Rule 1
(auto-fix bug — the plan's wording would have led SDK porters to implement
the wrong behavior).

## Self-Check: PASSED

**1. Created files exist:**
- `.planning/phases/13.0-design-contract-spec-docs/13.0-10-SUMMARY.md` —
  FOUND (this file)

**2. Modified files exist (verified):**
- `docs/operators/buffer-geo/histogram.md` — FOUND (158 LOC)
- `docs/operators/buffer-geo/hour_of_day_histogram.md` — FOUND (148 LOC)
- `docs/operators/buffer-geo/dow_hour_histogram.md` — FOUND (154 LOC)
- `docs/operators/buffer-geo/seasonal_deviation.md` — FOUND (166 LOC)
- `docs/operators/buffer-geo/event_type_mix.md` — FOUND (169 LOC)
- `docs/operators/buffer-geo/most_recent_n.md` — FOUND (159 LOC)
- `docs/operators/buffer-geo/reservoir_sample.md` — FOUND (174 LOC)

**3. Commits exist:**
- `60bbc8d docs(13.0-10): polish 7 bounded-buffer op pages — buffer-geo/
  subset (excluding 4 geo ops)` — FOUND (verified via `git log --oneline`)

**4. Acceptance criteria all PASS** — see "Verification" section above.

**5. No out-of-scope file modifications** — the polish commit `60bbc8d`
shows exactly 7 files in the diff (`git show --stat 60bbc8d`); this SUMMARY
commit modifies exactly 1 file (this SUMMARY.md). STATE.md / ROADMAP.md /
CORRECTNESS-PATH.md / CLAUDE.md / REQUIREMENTS.md untouched per plan
boundaries (closure Plan 13.0-15 owns these).

**6. buffer-geo/index.md ownership:** verified `docs/operators/buffer-geo/
index.md` does NOT exist post-Plan-10 (only `docs/operators/buffer-geo/
.keep` is present from the Plan 05 scaffold marker); Plan 13.0-11 owns the
combined family overview covering all 11 ops.
