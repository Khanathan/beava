---
phase: 13.0-design-contract-spec-docs
plan: 06
type: execute
wave: 2
depends_on: [01, 05]
files_modified:
  - docs/operators/core/count.md
  - docs/operators/core/sum.md
  - docs/operators/core/mean.md
  - docs/operators/core/min.md
  - docs/operators/core/max.md
  - docs/operators/core/var.md
  - docs/operators/core/std.md
  - docs/operators/core/ratio.md
  - docs/operators/sketch/n_unique.md
  - docs/operators/sketch/quantile.md
  - docs/operators/sketch/top_k.md
  - docs/operators/sketch/bloom_member.md
  - docs/operators/sketch/entropy.md
  - docs/operators/core/index.md
  - docs/operators/sketch/index.md
autonomous: true
requirements: []
addresses_decisions:
  - "D-02 polish: fill in Description / Parameters / Returns / Complexity / Examples / Edge cases / See also for 13 ops in core (8) + sketch (5)"
  - "ADR-002 NEW names + 'Previously called' notes verified per page (mean/var/std/n_unique/quantile)"
  - "Cite cost-class.md tier per op"
must_haves:
  truths:
    - "13 op stub pages polished: 8 in core/ + 5 in sketch/"
    - "Each polished page replaces TODO markers with prose: Description (2-3 paragraphs), Parameters (full table), Returns (typed shape), Complexity (CPU tier from cost-class.md + memory bound from register_validate.rs), 1-2 Examples (Python + JSON wire), Edge cases (empty stream / NaN / lifetime mode / structured-error code), See also (3-5 cross-links)"
    - "core/index.md and sketch/index.md family overview pages written"
    - "Polish does NOT add new sections beyond Plan 05 scaffold structure"
  artifacts:
    - path: "docs/operators/core/mean.md"
      provides: "Mean op spec (renamed from avg per ADR-002); arithmetic mean over window or lifetime"
      contains: "Previously called"
      min_lines: 80
    - path: "docs/operators/sketch/quantile.md"
      provides: "Quantile op spec (renamed from percentile per ADR-002); DDSketch-backed"
      contains: "DDSketch"
      min_lines: 100
    - path: "docs/operators/core/index.md"
      provides: "Core family overview"
      contains: "Core"
      min_lines: 20
    - path: "docs/operators/sketch/index.md"
      provides: "Sketch family overview"
      contains: "Sketch"
      min_lines: 20
  key_links:
    - from: "core/mean.md, core/var.md, core/std.md, sketch/n_unique.md, sketch/quantile.md"
      to: ".planning/decisions/ADR-002-polars-op-rename.md"
      via: "Previously-called note"
      pattern: "ADR-002|Previously called"
    - from: "Each polished op page Complexity table"
      to: "docs/operators/cost-class.md"
      via: "Cross-link with explicit Tier reference"
      pattern: "cost-class|Tier"
---

<objective>
Polish 13 op stub pages — 8 in core/ + 5 in sketch/ — replacing Plan 05 TODO markers with prose. Add 2 family overview index pages.

Per the 13.0 doc-only TDD §Note 4 exemption, single docs(13.0-06) commit covers all 15 file edits.

Output: 13 polished op pages + 2 family index pages. Single docs(13.0-06) commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13.0-design-contract-spec-docs/13.0-CONTEXT.md
@.planning/phases/13.0-design-contract-spec-docs/13.0-RESEARCH.md
@.planning/decisions/ADR-002-polars-op-rename.md
@docs/wire-spec.md
@docs/sdk-api/python.md
@docs/operators/cost-class.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: POLISH 8 core/ op pages (count, sum, mean, min, max, var, std, ratio)</name>
  <read_first>
    - docs/operators/core/count.md (Plan 05 stub)
    - docs/operators/core/sum.md
    - docs/operators/core/mean.md
    - docs/operators/core/min.md
    - docs/operators/core/max.md
    - docs/operators/core/var.md
    - docs/operators/core/std.md
    - docs/operators/core/ratio.md
    - python/beava/_agg.py
    - crates/beava-core/src/agg_op.rs
    - crates/beava-core/src/register_validate.rs
    - docs/operators/cost-class.md
    - .planning/decisions/ADR-002-polars-op-rename.md
    - .planning/phases/13.0-design-contract-spec-docs/13.0-RESEARCH.md
  </read_first>
  <action>
For each of the 8 core/ op pages, use the Edit tool to REPLACE the TODO markers with concrete prose. The H2 sections (Signature, Description, Parameters, Returns, Complexity, Examples, Wire, Edge cases, See also) and H1 + brief tagline are already in place from Plan 05; this task fills in:

For each op:
1. **Description** (2-3 paragraphs): mathematical/informal definition; when to use; what category
2. **Parameters** (table): Name | Type | Required | Default | Description — extract from `python/beava/_agg.py` signature
3. **Returns**: type + shape; cold-start behavior (empty if no events match)
4. **Complexity**: fill in CPU tier (from `docs/operators/cost-class.md`) + memory bound (from `register_validate.rs::lifetime_bound_for_op_str`) + lifetime mode (Allowed for all 8 core ops since they're O1)
5. **Examples**: 1-2 Python snippets with `@bv.event` + `@bv.table(key=...)` + `agg(name=bv.<op>(...))` + `app.push(...)` + `app.get(...)` showing read-back
6. **Edge cases**: empty stream returns null; NaN inputs (skipped or error per op); lifetime mode (allowed; O1 footprint); per-op structured-error codes if applicable
7. **See also**: cross-links to cost-class.md, related family ops, pipeline-dsl/compilation-rules.md

Specific per-op content guidance:

- **count.md**: window optional → lifetime mode allowed; no field arg; Tier 1 CPU; O1 memory; Example uses bv.count(window="1h") for "last hour event count per user"
- **sum.md**: field is REQUIRED; window REQUIRED at signature level (`window: str` non-optional); rejects non-numeric field at register-time with `schema_mismatch` code; example uses purchase amount
- **mean.md** (renamed from avg): same as sum but returns float average; Welford running mean; cite ADR-002 in "Previously called bv.avg" prose; example uses transaction amount average
- **min.md / max.md**: preserve field type (int stays int; float stays float); special handling for None/null inputs (skipped); example uses temperature min/max
- **var.md** (renamed from variance): Welford variance algorithm; numerically stable; cite ADR-002 in "Previously called"; floats only; lifetime mode allowed
- **std.md** (renamed from stddev): sqrt of var; cite ADR-002; same constraints as var
- **ratio.md**: count(where=match) / count(total); float in [0, 1]; cold-start returns null; lifetime mode default

For Examples in each page, include this canonical shape (adapt to op):

```python
import beava as bv

@bv.event
class Txn:
    user_id: str
    amount: float

@bv.table(key="user_id")
def UserStats(txn) -> bv.Table:
    return (
        txn.group_by("user_id")
           .agg(<feature_name>=bv.<op>(...))
    )

# Push events
app.push("Txn", {"user_id": "alice", "amount": 42.50})
app.push("Txn", {"user_id": "alice", "amount": 17.00})

# Query
result = app.get("UserStats", "alice")
# result == {"<feature_name>": <expected_value>}
```

For ALL Examples, include the JSON wire form below the Python — pulled from the Wire section template (the scaffold has the boilerplate; refine the params dict).

Length per polished page: aim for 70-130 LOC depending on op complexity (count = 70 LOC; quantile = 130 LOC; var = 90 LOC).

NO commit yet.
  </action>
  <verify>
    <automated>for op in count sum mean min max var std ratio; do [ "$(wc -l < docs/operators/core/${op}.md)" -ge 70 ] || exit 1; grep -q '## Description' docs/operators/core/${op}.md || exit 1; ! grep -q 'TODO (Plan 13.0-' docs/operators/core/${op}.md || exit 1; done && grep -q 'Previously called' docs/operators/core/mean.md && grep -q 'Previously called' docs/operators/core/var.md && grep -q 'Previously called' docs/operators/core/std.md</automated>
  </verify>
  <acceptance_criteria>
    - All 8 core ops polished (no TODO markers): `for op in count sum mean min max var std ratio; do ! grep -qE 'TODO \(Plan 13\.0-' docs/operators/core/${op}.md || { echo "TODO remains in core/${op}.md"; exit 1; }; done`
    - All 8 core ops ≥ 70 lines: `for op in count sum mean min max var std ratio; do [ "$(wc -l < docs/operators/core/${op}.md)" -ge 70 ] || exit 1; done`
    - mean / var / std all cite ADR-002 + "Previously called": `for op in mean var std; do grep -q 'ADR-002' docs/operators/core/${op}.md && grep -q 'Previously called' docs/operators/core/${op}.md || exit 1; done`
    - All 8 reference cost-class.md: `for op in count sum mean min max var std ratio; do grep -q 'cost-class' docs/operators/core/${op}.md || exit 1; done`
    - All 8 have Python + JSON wire example: `for op in count sum mean min max var std ratio; do grep -q '@bv.event\|@bv.table' docs/operators/core/${op}.md && grep -q '"op"' docs/operators/core/${op}.md || exit 1; done`
  </acceptance_criteria>
  <done>8 core/ pages polished. NO commit yet.</done>
</task>

<task type="auto">
  <name>Task 2: POLISH 5 sketch/ op pages (n_unique, quantile, top_k, bloom_member, entropy)</name>
  <read_first>
    - docs/operators/sketch/n_unique.md (Plan 05 stub)
    - docs/operators/sketch/quantile.md
    - docs/operators/sketch/top_k.md
    - docs/operators/sketch/bloom_member.md
    - docs/operators/sketch/entropy.md
    - python/beava/_agg.py (note: sketch helpers ARE missing from current _agg.py — Phase 13.5 ships them; spec describes v0-target)
    - crates/beava-core/src/agg_op.rs (CountDistinct, Percentile, TopK, BloomMember, Entropy variants)
    - crates/beava-core/src/register_validate.rs (sketch ops are BoundedSketch / BoundedByConfig)
    - docs/operators/cost-class.md (sketch ops = Tier 2 / Tier 3)
    - .planning/decisions/ADR-002-polars-op-rename.md
    - .planning/phases/13.0-design-contract-spec-docs/13.0-RESEARCH.md (lines 538-688 — full quantile.md worked example)
  </read_first>
  <action>
Polish all 5 sketch/ pages. Use the worked `quantile.md` from RESEARCH §3 (lines 538-688) verbatim for `quantile.md` since it's the canonical RESEARCH example.

For each op, fill in the Plan 05 stub TODO markers:

- **n_unique.md** (renamed from count_distinct): HLL cardinality with `exact_threshold` hybrid; cite ADR-002; params: `field: str`, `window: str | None = None`, `where: bv.Col | None = None`, `exact_threshold: int = 1024`, `hybrid_precision: int = 14`; returns int; CPU Tier 2 (exact) / Tier 3 (HLL); BoundedSketch memory; lifetime mode allowed
- **quantile.md** (renamed from percentile): DDSketch-backed; copy RESEARCH §3 worked example verbatim; params: `field: str`, `q: float (0-1)`, optional `window`/`where`/`exact_threshold=256`/`hybrid_alpha=0.01`; returns float; CPU Tier 2/3; BoundedSketch
- **top_k.md**: SpaceSaving + bounded heap hybrid; params: `field: str`, `k: int`, optional window/where/`exact_threshold=1024`/`hybrid_width=2048`/`hybrid_depth=4`; returns list; BoundedByConfig("k", 10); lifetime mode allowed
- **bloom_member.md**: Bloom-filter ever-seen test; params: `field: str`, `capacity: int = 1024`, `fpr: float = 0.01`; returns bool; BoundedSketch; lifetime mode allowed (no window kwarg — bloom is always lifetime); per-event check is the seen-vs-not-seen test
- **entropy.md**: Shannon entropy over categorical distribution; params: `field: str`, optional window, `max_categories: int = 256` (BoundedByConfig); returns float (log2 base typical); cite V0-MEM-GOV-02 max_categories cap from Phase 12.8

For each page, ensure:
- ADR-002 + "Previously called" note on n_unique + quantile (others not renamed)
- Cite Phase 12.8 V0-MEM-GOV-02 if op is BoundedByConfig (entropy + top_k)
- Cross-link to cost-class.md
- 1-2 worked Python examples
- JSON wire form
- Edge cases: empty stream, lifetime mode behavior, register-time error codes

Length: 80-140 LOC per page (quantile = 130; bloom_member = 80).

NO commit yet.
  </action>
  <verify>
    <automated>for op in n_unique quantile top_k bloom_member entropy; do [ "$(wc -l < docs/operators/sketch/${op}.md)" -ge 80 ] || exit 1; ! grep -qE 'TODO \(Plan 13\.0-' docs/operators/sketch/${op}.md || exit 1; done && grep -q 'Previously called' docs/operators/sketch/n_unique.md && grep -q 'Previously called' docs/operators/sketch/quantile.md</automated>
  </verify>
  <acceptance_criteria>
    - All 5 sketch ops polished: `for op in n_unique quantile top_k bloom_member entropy; do ! grep -qE 'TODO \(Plan 13\.0-' docs/operators/sketch/${op}.md; done`
    - All ≥ 80 lines: `for op in n_unique quantile top_k bloom_member entropy; do [ "$(wc -l < docs/operators/sketch/${op}.md)" -ge 80 ] || exit 1; done`
    - n_unique + quantile cite ADR-002: `grep -q 'ADR-002' docs/operators/sketch/n_unique.md && grep -q 'ADR-002' docs/operators/sketch/quantile.md`
    - quantile mentions DDSketch: `grep -q 'DDSketch\|hybrid' docs/operators/sketch/quantile.md`
    - n_unique mentions HLL: `grep -q 'HLL\|hybrid_precision' docs/operators/sketch/n_unique.md`
    - top_k + entropy cite V0-MEM-GOV: `grep -q 'V0-MEM-GOV\|BoundedByConfig\|max_categories' docs/operators/sketch/top_k.md && grep -q 'V0-MEM-GOV\|max_categories' docs/operators/sketch/entropy.md`
  </acceptance_criteria>
  <done>5 sketch/ pages polished. NO commit yet.</done>
</task>

<task type="auto">
  <name>Task 3: WRITE family index pages docs/operators/{core,sketch}/index.md + commit</name>
  <read_first>
    - docs/operators/core/*.md
    - docs/operators/sketch/*.md
    - docs/operators/index.md
    - .planning/phases/13.0-design-contract-spec-docs/13.0-RESEARCH.md
  </read_first>
  <action>
Step 1 — Use the Write tool to create `docs/operators/core/index.md`:

```markdown
# Core Aggregation Operators

The 8 core ops cover the basic statistical primitives: counts, sums, central tendency, dispersion, and ratios.

All core ops are **O(1)** per-event memory and **Tier 1** CPU. Lifetime mode is permitted for all 8.

## Operators

| Op | Description |
|----|-------------|
| [`bv.count`](./count.md) | Event count over a window or lifetime. |
| [`bv.sum`](./sum.md) | Numeric sum of a field. |
| [`bv.mean`](./mean.md) | Arithmetic mean of a field. *(Renamed from `avg` per [ADR-002](../../../.planning/decisions/ADR-002-polars-op-rename.md).)* |
| [`bv.min`](./min.md) | Minimum value of a field. |
| [`bv.max`](./max.md) | Maximum value of a field. |
| [`bv.var`](./var.md) | Sample variance (Welford). *(Renamed from `variance`.)* |
| [`bv.std`](./std.md) | Standard deviation. *(Renamed from `stddev`.)* |
| [`bv.ratio`](./ratio.md) | Count matching predicate / total count. |

## See also

- [Operator catalog index](../index.md) — full 53-op catalogue
- [cost-class.md](../cost-class.md) — performance tier metadata
- [Pipeline DSL compilation rules](../../pipeline-dsl/compilation-rules.md)
```

Step 2 — Use the Write tool to create `docs/operators/sketch/index.md`:

```markdown
# Sketch Aggregation Operators

The 5 sketch ops cover approximate-cardinality, quantile, frequency, set-membership, and entropy estimation. All use bounded data structures regardless of stream length.

| Op | Memory class | CPU tier |
|----|--------------|----------|
| [`bv.n_unique`](./n_unique.md) | BoundedSketch (HLL) | Tier 2/3 |
| [`bv.quantile`](./quantile.md) | BoundedSketch (DDSketch) | Tier 2/3 |
| [`bv.top_k`](./top_k.md) | BoundedByConfig("k") | Tier 2 |
| [`bv.bloom_member`](./bloom_member.md) | BoundedSketch (Bloom) | Tier 1 |
| [`bv.entropy`](./entropy.md) | BoundedByConfig("max_categories") | Tier 2 |

Two of the five (`n_unique`, `quantile`) were renamed per [ADR-002](../../../.planning/decisions/ADR-002-polars-op-rename.md) for Polars-convention consistency.

## See also

- [Operator catalog index](../index.md)
- [cost-class.md](../cost-class.md)
- Per-operator memory governance: [V0-MEM-GOV-02](../../../.planning/REQUIREMENTS.md) — every lifetime aggregation operator declares a finite per-entity memory ceiling at register-time
```

Step 3 — Stage and commit everything from this plan:

```bash
git add docs/operators/core/ docs/operators/sketch/
```

Commit message:

```
docs(13.0-06): polish 13 op pages — core (8) + sketch (5) + 2 family index pages

- 8 core ops polished from Plan 05 stubs:
  - count, sum, mean (renamed from avg), min, max,
    var (renamed from variance), std (renamed from stddev), ratio
- 5 sketch ops polished:
  - n_unique (renamed from count_distinct), quantile (renamed from percentile),
    top_k, bloom_member, entropy
- Each polished page replaces Plan 05 TODO markers with prose:
  Description (2-3 paragraphs), Parameters table, Returns shape,
  Complexity (CPU tier + memory bound), 1-2 Examples (Python + JSON wire),
  Edge cases, See also cross-links
- 5 renamed ops cite ADR-002 + "Previously called bv.<old>" deprecation note
- 2 sketch ops with BoundedByConfig (top_k, entropy) cite Phase 12.8 V0-MEM-GOV-02
- core/index.md + sketch/index.md family overview pages

Per CLAUDE.md TDD §Note 4 doc-only-plan exemption: single docs() commit.
```
  </action>
  <verify>
    <automated>[ -f docs/operators/core/index.md ] && [ -f docs/operators/sketch/index.md ] && git log -1 --format=%s | grep -qE '^docs\(13\.0-06\)' && for op in count sum mean min max var std ratio n_unique quantile top_k bloom_member entropy; do family=core; [ -f docs/operators/sketch/${op}.md ] && family=sketch; ! grep -qE 'TODO \(Plan 13\.0-' docs/operators/${family}/${op}.md || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - 2 family index pages exist: `[ -f docs/operators/core/index.md ] && [ -f docs/operators/sketch/index.md ]`
    - All 13 ops polished + 2 family pages staged: `git diff --cached --name-only | grep -E 'docs/operators/(core|sketch)/' | wc -l | xargs -I {} test {} -ge 13`
    - Single docs(13.0-06) commit: `git log -1 --format=%s | grep -qE '^docs\(13\.0-06\)'`
    - Commit references core + sketch + family index: `git log -1 --format=%B | grep -qE 'core|sketch'`
    - core/index.md links to all 8 core ops: `for op in count sum mean min max var std ratio; do grep -q "${op}.md" docs/operators/core/index.md || exit 1; done`
    - sketch/index.md links to all 5 sketch ops: `for op in n_unique quantile top_k bloom_member entropy; do grep -q "${op}.md" docs/operators/sketch/index.md || exit 1; done`
  </acceptance_criteria>
  <done>13 op pages polished + 2 family index pages. Single docs(13.0-06) commit.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.0-06-01 | Information Disclosure | Operator catalog (public API surface) | accept | Intentional — operator pages are SEO/docs targets per CONTEXT D-02. |
</threat_model>

<verification>
- 13 op pages polished: 8 core + 5 sketch
- 2 family index pages
- 5 renamed ops cite ADR-002
- 2 BoundedByConfig sketch ops cite V0-MEM-GOV-02
- Single docs(13.0-06) commit
</verification>

<success_criteria>
- Plans 13.0-07/08/09/10/11 polish remaining 41 ops in parallel
- Plan 13.0-15 closure runs check_op_page_coverage.py to verify completeness
</success_criteria>

<output>
13 of 54 op pages polished. Remaining 41 polished by Plans 07-11.
</output>
