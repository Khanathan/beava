---
phase: 13.0-design-contract-spec-docs
plan: 07
subsystem: docs
tags: [operators, catalog, polish, point-ordinal, recency, v0-mem-gov-02, processing-time]

# Dependency graph
requires:
  - phase: 13.0-design-contract-spec-docs
    plan: 05
    provides: 54 op page stubs scaffolded under docs/operators/<family>/<op>.md with H1 + 9 mandatory H2 sections and TODO prose markers labeled by polish plan number; Plan 07 fills the 15 stubs labeled "Plan 13.0-07" (5 point-ordinal + 10 recency)
  - phase: 13.0-design-contract-spec-docs
    plan: 06
    provides: Per-op page template established (134-157 LOC range, 9 H2 sections, cost-class cross-link, V0-MEM-GOV-02 citation pattern, cold-start convention, family index template) — Plan 07 inherits verbatim
  - phase: 12.8-memory-governance
    provides: V0-MEM-GOV-02 lifetime-aggregation contract (BoundedByRequiredKwarg("n") for first_n / last_n / lag / time_since_last_n) — cited in all 4 BoundedByRequiredKwarg op Complexity tables + Edge cases sections
  - phase: 19.2-operator-uniformity
    provides: docs/operators/cost-class.md (alive Phase 19.2 metadata, KEPT, NOT touched) — cross-linked from each polished page's Complexity section for CPU tier (all 15 ops are Tier 1)
provides:
  - 5 point-ordinal/ op pages polished from Plan 05 stubs (first, last, first_n, last_n, lag) — each 137-152 LOC; TODO markers replaced with full Description / Parameters / Returns / Complexity / Examples / Edge cases / See also prose
  - 10 recency/ op pages polished (first_seen, last_seen, age, has_seen, time_since, time_since_last_n, streak, max_streak, negative_streak, first_seen_in_window) — each 132-155 LOC; processing-time + read-time-computation semantics explicitly documented
  - docs/operators/point-ordinal/index.md (23 LOC) — Point/ordinal family overview + 5-op summary table with memory class + Tier 1 CPU
  - docs/operators/recency/index.md (34 LOC) — Recency family overview + 10-op summary table with returns + time-source columns; key invariants section locks processing-time-only contract
  - 4 BoundedByRequiredKwarg("n") citations: first_n / last_n / lag (point-ordinal) + time_since_last_n (recency) cite V0-MEM-GOV-02 in Complexity table + Edge cases + See also
  - 5 processing-time citations: first_seen / last_seen / age / time_since / time_since_last_n explicitly cite project_redis_shaped_no_event_time_ever for "server now_ms() at apply, not event-time" semantics
  - "Read-time computation" pattern documented on age / time_since / time_since_last_n / first_seen_in_window: result changes between reads without new events because the right-hand side of `now_ms() - last_ms` is captured at query time
  - Cold-start return convention extended: first/last/lag return null; first_n/last_n return "[]" (empty list); first_seen/last_seen/age/time_since/time_since_last_n return null; has_seen/first_seen_in_window return false (never null); streak/max_streak/negative_streak return integer 0
affects:
  - 13.0-08 / 09 / 10 / 11 (decay / velocity / buffer / geo polish): inherit the per-page template established by Plans 06 + 07 (132-157 LOC, 9 H2 sections, cost-class cross-link, V0-MEM-GOV-02 citation pattern, cold-start convention)
  - 13.0-12 (pipeline-dsl/compilation-rules.md): forward-referenced from every polished page's See also section; the link target page does not yet exist (Plan 12 creates it)
  - 13.0-15 (closure plan): runs scripts/check_op_page_coverage.py + verifies remaining TODO markers across the catalogue; this plan's contribution: 0 TODOs in point-ordinal/ + recency/
  - 13.4 (engine prep): polished pages reference target post-13.4 wire shapes (verb-style routes, "kind=table" + derivation output_kind) — 13.4 implements the engine to match

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-op page template (RESEARCH §3): H1 + one-line tagline + 9 H2 sections (Signature / Description / Parameters / Returns / Complexity / Examples / Wire / Edge cases / See also). Length 132-155 LOC across all 15 ops; all Tier 1 / O(1) or BoundedByRequiredKwarg(\"n\")."
    - "BoundedByRequiredKwarg(\"n\") citation pattern: first_n / last_n / lag / time_since_last_n explicitly cite V0-MEM-GOV-02 in (1) Complexity row Memory column, (2) Parameters row for n=Yes (REQUIRED), (3) Edge cases section ('n missing' / 'n=0' rejection paths with structured error code unbounded_op_in_lifetime_mode), (4) See also section pointer to V0-MEM-GOV-02. Establishes the citation pattern for the other Phase 12.8 BoundedByRequiredKwarg ops (most_recent_n / reservoir_sample / histogram) when their pages get polished in Plans 10/11."
    - "Processing-time citation pattern (project_redis_shaped_no_event_time_ever): all 5 read-time recency ops (first_seen, last_seen, age, time_since, time_since_last_n) explicitly state 'server now_ms() at apply, NOT event-time' in Description, Edge cases, AND family index. Producers cannot influence captured timestamps via payload fields. Out-of-order event-time payloads do not affect arrival-order semantics. Locked 2026-04-30 commitment."
    - "Read-time computation pattern: age / time_since / time_since_last_n / first_seen_in_window explicitly document that the result changes between reads without any new events arriving — the subtraction `now_ms() - first_ms/last_ms/oldest_ms` happens server-side when app.get(...) reaches the entity, not at apply time. This is the core 'staleness/recency feature' insight; previously implicit, now explicit per page."
    - "Shared-state pattern documented: SeenState (5 ops: first_seen / last_seen / age / has_seen / time_since), StreakState (2 ops: streak / max_streak), NegativeStreakState (1 op), FirstSeenInWindowState (1 op), TimeSinceLastNState (1 op). Per-page Description notes the shared struct so readers understand that registering several recency siblings on the same where= predicate costs roughly the same as registering one (struct is shared; siblings just read different methods)."
    - "Cold-entity eviction (V0-MEM-GOV-01) cross-linked on every recency op: @bv.event(cold_after=...) drops the underlying state per Redis-TTL pattern; recency state rebuilds from the next post-eviction event. Establishes how recency interacts with the cold-after TTL — important for SDK porters in 13.6 who need to wire eviction edge cases through their language equivalents."
    - "Family index template (~25-35 LOC): H1 + one-line family overview + summary table (per-op link + memory + CPU + notes columns) + 'Key invariants' section (recency only — locks processing-time-only contract + read-time-computation pattern + per-op cold-start return values) + See also section linking to operator catalog index + cost-class.md + sibling family + V0-MEM-GOV-02 + pipeline-dsl/compilation-rules.md. Point-ordinal index uses simpler 4-column table (op | memory | CPU | notes); recency index uses 4-column 'Op | Returns | Time source | Notes' to surface the read-time-computation distinction."

# Files
key-files:
  created:
    - docs/operators/point-ordinal/index.md (23 LOC) — Point/ordinal family overview + 5-op table
    - docs/operators/recency/index.md (34 LOC) — Recency family overview + 10-op table + key invariants
    - .planning/phases/13.0-design-contract-spec-docs/13.0-07-SUMMARY.md (this file)
  modified:
    - docs/operators/point-ordinal/first.md (69 → 137 LOC; +68 LOC; full polish — Tier 1, O(1), lifetime-only by definition)
    - docs/operators/point-ordinal/last.md (69 → 137 LOC; +68 LOC; full polish — Tier 1, O(1), processing-time arrival order)
    - docs/operators/point-ordinal/first_n.md (69 → 150 LOC; +81 LOC; full polish — V0-MEM-GOV-02 cited; n required at register time)
    - docs/operators/point-ordinal/last_n.md (69 → 152 LOC; +83 LOC; full polish — V0-MEM-GOV-02 cited; VecDeque ring of capacity n)
    - docs/operators/point-ordinal/lag.md (69 → 150 LOC; +81 LOC; full polish — V0-MEM-GOV-02 cited; ring of capacity n+1; null until n+1 events)
    - docs/operators/recency/first_seen.md (69 → 134 LOC; +65 LOC; full polish — processing-time cited; SeenState shared)
    - docs/operators/recency/last_seen.md (69 → 136 LOC; +67 LOC; full polish — processing-time cited; SeenState shared)
    - docs/operators/recency/age.md (69 → 142 LOC; +73 LOC; full polish — processing-time + read-time-computation pattern documented; clamped to >= 0)
    - docs/operators/recency/has_seen.md (69 → 132 LOC; +63 LOC; full polish — fastest recency op; ~4 ns floor; sticky bool; cold-start returns false not null)
    - docs/operators/recency/time_since.md (69 → 147 LOC; +78 LOC; full polish — processing-time + read-time-computation pattern; null cold-start; clamped to >= 0)
    - docs/operators/recency/time_since_last_n.md (69 → 155 LOC; +86 LOC; full polish — V0-MEM-GOV-02 + processing-time cited; deque of capacity n; null until n matches)
    - docs/operators/recency/streak.md (69 → 139 LOC; +70 LOC; full polish — Tier 1, O(1); event-order driven; reset on non-match; cold-start returns 0)
    - docs/operators/recency/max_streak.md (69 → 140 LOC; +71 LOC; full polish — shares StreakState; sticky once set; only rises)
    - docs/operators/recency/negative_streak.md (69 → 138 LOC; +69 LOC; full polish — mirror of streak; no max_negative_streak op in v0)
    - docs/operators/recency/first_seen_in_window.md (69 → 153 LOC; +84 LOC; full polish — windowed semantics, lifetime O(1) state; window= required at register time; window=\"forever\" rejected)

# Decisions
decisions:
  - "Per-op page template inherited verbatim from Plan 06 (no structural changes to the H2 layout); polish only fills the 5 prose sections per op (Description / Parameters / Returns / Examples / Edge cases) plus enriches Complexity + See also where applicable. The Signature + Wire blocks remain unchanged from Plan 05's scaffold."
  - "All 15 ops are Tier 1 per cost-class.md — no rare-tier polishing required. Memory bounds split: 11 ops are O(1), 4 ops are BoundedByRequiredKwarg(\"n\") (first_n / last_n / lag / time_since_last_n). One op (first_seen_in_window) is special: windowed semantics with lifetime O(1) state — documented explicitly as 'special' in the Complexity row."
  - "Cold-start return convention documented per-op consistently: first/last/lag return null (no value yet); first_n/last_n return \"[]\" (empty list); first_seen/last_seen/age/time_since/time_since_last_n return null; has_seen/first_seen_in_window return false (always bool, never null); streak/max_streak/negative_streak return integer 0 (always int, never null). This per-op cold-start contract is uniformly documented in each Edge cases section — important for SDK porters in 13.6."
  - "Processing-time citation pattern locked: all 5 read-time recency ops (first_seen / last_seen / age / time_since / time_since_last_n) cite project_redis_shaped_no_event_time_ever in Description AND Edge cases AND family index. The phrase 'server now_ms() at apply, NOT event-time' appears verbatim across all 5 — establishes the cross-language wording for SDK porters."
  - "Read-time computation pattern made explicit on age / time_since / time_since_last_n / first_seen_in_window: 'reads grow without new events' / 'reads shrink on a new match' bullets in Edge cases. This was previously implicit in the implementation; now load-bearing in the docs because it's the core insight that distinguishes recency from streak ops."
  - "Cold-entity eviction (V0-MEM-GOV-01) cross-linked on every recency op + 4 of 5 point-ordinal ops: @bv.event(cold_after=...) drops the SeenState/StreakState/etc. per Redis-TTL pattern; recency state rebuilds from the next post-eviction event. Establishes how recency interacts with cold-after TTL for SDK porters."
  - "negative_streak documented as 'no max_negative_streak op in v0' — explicit note in Edge cases acknowledging the asymmetry with streak / max_streak. If demand justifies, the v0.1+ revival path is open; for now users compose two ops manually."
  - "first_seen_in_window window=\"forever\" rejected explicitly in Edge cases — for that semantic, point users at bv.has_seen (the lifetime-boolean variant). Otherwise the windowed-recency operator's read-time horizon is meaningless. The window= kwarg is required by the function signature; missing window= raises ValueError at SDK-helper-call time, with the wire-level register validator as fallback."
  - "Per CLAUDE.md TDD §Note 4 doc-only-plan exemption: single docs(13.0-07) commit covering 15 polished pages + 2 family index pages — no red-test commit required since the 'test' (acceptance grep loops + per-op LOC checks) is mechanical and passes by construction post-polish."

# Metrics
metrics:
  duration_min: 30
  completed: 2026-05-04T00:35:00Z
  tasks_completed: 3
  loc_added: 1491
  loc_removed: 312
  files_added: 2
  files_modified: 15
  commits: 1
---

# Phase 13.0 Plan 07: Operator catalog polish — point-ordinal (5) + recency (10) Summary

## One-liner

Polished 15 op pages (5 point-ordinal + 10 recency) from Plan 05 stubs into
full-prose spec documents — Description / Parameters / Returns / Complexity /
Examples / Edge cases / See also — and added 2 family overview index pages
(`point-ordinal/index.md`, `recency/index.md`). The 4 `BoundedByRequiredKwarg`
ops (`first_n`, `last_n`, `lag`, `time_since_last_n`) cite Phase 12.8
V0-MEM-GOV-02 register-time enforcement; the 5 read-time recency ops
(`first_seen`, `last_seen`, `age`, `time_since`, `time_since_last_n`) cite
`project_redis_shaped_no_event_time_ever` for "server `now_ms()`, NOT event-time"
semantics. Single `docs(13.0-07)` commit per CLAUDE.md TDD §Note 4
doc-only-plan exemption. Inherited the per-op template established by Plan 06
verbatim.

## What landed

### 5 point-ordinal/ op pages polished (137-152 LOC each)

| Op | LOC | Memory | Notes |
|----|----:|--------|-------|
| `first.md` | 137 | `O(1)` | Tier 1 ~25 ns; sticky first non-null value; lifetime-only |
| `last.md` | 137 | `O(1)` | Tier 1 ~30 ns; processing-time arrival order; lifetime-only |
| `first_n.md` | 150 | `BoundedByRequiredKwarg("n")` | Tier 1 ~30 ns; V0-MEM-GOV-02 cited; n required at register time |
| `last_n.md` | 152 | `BoundedByRequiredKwarg("n")` | Tier 1 ~32 ns; VecDeque ring of capacity n |
| `lag.md` | 150 | `BoundedByRequiredKwarg("n")` | Tier 1 ~32 ns; ring of capacity n+1; null until n+1 events |

### 10 recency/ op pages polished (132-155 LOC each)

| Op | LOC | Memory | Returns / Notes |
|----|----:|--------|-----------------|
| `first_seen.md` | 134 | `O(1)` (SeenState) | `Datetime`; processing-time cited; sticky |
| `last_seen.md` | 136 | `O(1)` (SeenState) | `Datetime`; processing-time cited; overwritten each match |
| `age.md` | 142 | `O(1)` (SeenState) | `i64` ms; **read-time computation** — reads grow without events |
| `has_seen.md` | 132 | `O(1)` (SeenState) | `bool`; ~4 ns floor (fastest recency op); cold-start returns false |
| `time_since.md` | 147 | `O(1)` (SeenState) | `i64` ms or `null`; read-time computation; processing-time cited |
| `time_since_last_n.md` | 155 | `BoundedByRequiredKwarg("n")` | `i64` ms or `null`; V0-MEM-GOV-02 + processing-time cited |
| `streak.md` | 139 | `O(1)` (StreakState) | `i64`; consecutive matches; reset on non-match; cold-start 0 |
| `max_streak.md` | 140 | `O(1)` (StreakState) | `i64`; sticky max-of-streak; only rises; shares state with streak |
| `negative_streak.md` | 138 | `O(1)` | `i64`; mirror of streak; no max_negative_streak op in v0 |
| `first_seen_in_window.md` | 153 | `O(1)` (FirstSeenInWindowState) | `bool`; windowed semantics, lifetime state; window= required |

### 2 family overview index pages

- `docs/operators/point-ordinal/index.md` (23 LOC) — Point/ordinal family overview + 5-op summary table (op | memory | CPU | notes columns) + See also pointers. Calls out the 3 `BoundedByRequiredKwarg("n")` ops + the lifetime-only invariant.
- `docs/operators/recency/index.md` (34 LOC) — Recency family overview + 10-op summary table (op | returns | time source | notes columns) + **Key invariants** section locking the processing-time-only contract, read-time-computation pattern, per-op cold-start return values, V0-MEM-GOV-02 ceiling for `time_since_last_n`, and shared-state pattern (5 ops on `SeenState`).

### TODO markers eliminated

Per Plan 05's split, the 15 op stubs in point-ordinal/ + recency/ collectively
carried ~75 TODO markers (5 prose sections × 15 ops). After Plan 07 polish:
**0 TODO markers remain** in point-ordinal/ + recency/. The remaining TODO
markers are distributed across decay/, velocity/, and buffer-geo/ — owned by
Plans 08 / 09 / 10 / 11.

```bash
$ grep -rcE 'TODO \(Plan 13\.0-' docs/operators/point-ordinal/ docs/operators/recency/ | grep -v ':0$'
(empty — 0 TODOs across all 17 files)
```

### Cross-link discipline

Every polished page cross-links to:

- `docs/operators/cost-class.md` (Phase 19.2 alive metadata) — for CPU tier (all 15 ops are Tier 1)
- `docs/operators/index.md` — master 53-op catalog index (via family index pages)
- `docs/pipeline-dsl/compilation-rules.md` — forward-reference (Plan 13.0-12 creates the file)
- `examples/wire/register-fraud-team.request.json` — full payload example placeholder (Plan 06 also forward-referenced this fixture; created by a later plan)
- `.planning/REQUIREMENTS.md` (V0-MEM-GOV-02 + V0-MEM-GOV-01) — for the BoundedByRequiredKwarg ops + cold-after eviction edge cases
- `.planning/PROJECT.md` (project_redis_shaped_no_event_time_ever) — for processing-time-only semantics on the 5 read-time recency ops

## Verification (acceptance criteria)

All Task 1 / Task 2 / Task 3 acceptance criteria verified post-commit:

### Task 1 — 5 point-ordinal ops

- All 5 point-ordinal ops polished, no TODO markers (verified)
- All 5 ≥ 70 lines (got 137-152 LOC)
- first_n / last_n / lag cite V0-MEM-GOV-02 + BoundedByRequiredKwarg (3/3)
- All 5 reference cost-class.md
- All 5 have @bv.event/@bv.table Python example + JSON wire form

### Task 2 — 10 recency ops

- All 10 recency ops polished, no TODO markers (verified)
- All 10 ≥ 70 lines (got 132-155 LOC)
- first_seen / last_seen / age / time_since cite processing-time semantics (4/4)
- time_since_last_n cites V0-MEM-GOV-02 + BoundedByRequiredKwarg (verified)

### Task 3 — family index pages + commit

- docs/operators/point-ordinal/index.md exists (23 LOC ≥ 20)
- docs/operators/recency/index.md exists (34 LOC ≥ 20)
- point-ordinal/index.md links all 5 point-ordinal ops
- recency/index.md links all 10 recency ops
- Single docs(13.0-07) commit at 67a55ac
- 17 files staged in commit (5 point-ordinal + 1 po-index + 10 recency + 1 rec-index)

### Plan-level invariants

- STATE.md / ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md NOT modified (none in commit)
- docs/operators/cost-class.md NOT modified
- 0 TODOs remain in docs/operators/point-ordinal/ and docs/operators/recency/
- Single docs() commit per CLAUDE.md TDD §Note 4 doc-only-plan exemption

## Threat Flags

None. Plan 07 is doc-only polish — no new network surface, auth paths, file
access patterns, or schema changes at trust boundaries are introduced. The
single threat from the plan's `<threat_model>` (T-13.0-07-01: operator-catalog
information disclosure) is `accept` per CONTEXT D-02 (operator pages are
SEO/docs targets — intentional public surface for OSS launch).

## Known Stubs

None in scope. The ~75 TODO markers Plan 05 sowed in the 15 point-ordinal +
recency op stubs are all eliminated by this plan. The remaining TODO markers
(decay/ + velocity/ + buffer-geo/) are owned by Plans 08-11 and are NOT in
this plan's goal scope.

The forward-referenced `docs/pipeline-dsl/compilation-rules.md` link target
does not yet exist (Plan 13.0-12 creates it). This is intentional — the
cross-link is a contract for the page that's coming, not a broken link in
the steady-state docs site.

The forward-referenced `examples/wire/register-fraud-team.request.json` link
target also does not yet exist (Plan 06 also forward-referenced this fixture;
a later plan in the 13.0 wave creates it). Inherited from Plan 06's pattern;
not a contract bug.

## Deviations from Plan

None — plan executed exactly as written. The plan's `<action>` blocks
specified per-op content guidance (Description / Parameters / Returns /
Complexity / Examples / Edge cases / See also); this executor wrote the
prose directly into each Plan 05 stub via the Write tool, replacing the
TODO markers with concrete content. All acceptance criteria verified
pre-commit + post-commit.

The plan's per-op content guidance for the 4 `BoundedByRequiredKwarg("n")`
ops (`first_n`, `last_n`, `lag`, `time_since_last_n`) was followed
verbatim — V0-MEM-GOV-02 register-time enforcement explicitly cited,
structured error code `unbounded_op_in_lifetime_mode` explicitly named,
register-time JSON-prelude shim (`pre_check_unbounded_op_in_lifetime_mode`
in `crates/beava-core/src/register_validate.rs`) explicitly named.

The plan's per-op content guidance for the 5 read-time recency ops
(`first_seen`, `last_seen`, `age`, `time_since`, `time_since_last_n`)
was followed verbatim — `project_redis_shaped_no_event_time_ever` (locked
2026-04-30) explicitly cited, "server now_ms() at apply, NOT event-time"
phrasing used uniformly across all 5 pages, "producers cannot influence
captured timestamps via payload fields" stated explicitly per op.

## Self-Check: PASSED

- FOUND: docs/operators/point-ordinal/first.md (137 LOC, 0 TODOs)
- FOUND: docs/operators/point-ordinal/last.md (137 LOC, 0 TODOs)
- FOUND: docs/operators/point-ordinal/first_n.md (150 LOC, V0-MEM-GOV cited)
- FOUND: docs/operators/point-ordinal/last_n.md (152 LOC, V0-MEM-GOV cited)
- FOUND: docs/operators/point-ordinal/lag.md (150 LOC, V0-MEM-GOV cited)
- FOUND: docs/operators/point-ordinal/index.md (23 LOC, 5 op links)
- FOUND: docs/operators/recency/first_seen.md (134 LOC, processing-time cited)
- FOUND: docs/operators/recency/last_seen.md (136 LOC, processing-time cited)
- FOUND: docs/operators/recency/age.md (142 LOC, processing-time cited)
- FOUND: docs/operators/recency/has_seen.md (132 LOC, 0 TODOs)
- FOUND: docs/operators/recency/time_since.md (147 LOC, processing-time cited)
- FOUND: docs/operators/recency/time_since_last_n.md (155 LOC, V0-MEM-GOV + processing-time cited)
- FOUND: docs/operators/recency/streak.md (139 LOC, 0 TODOs)
- FOUND: docs/operators/recency/max_streak.md (140 LOC, shares StreakState with streak)
- FOUND: docs/operators/recency/negative_streak.md (138 LOC, mirror of streak)
- FOUND: docs/operators/recency/first_seen_in_window.md (153 LOC, window= required)
- FOUND: docs/operators/recency/index.md (34 LOC, 10 op links + key invariants)
- FOUND: commit 67a55ac (`docs(13.0-07): polish 15 op pages — point-ordinal (5) + recency (10) + 2 family index pages`)
- FOUND-NOT-MODIFIED: docs/operators/cost-class.md (NOT in commit; last touched 27 Apr by Phase 19.2)
- FOUND-NOT-MODIFIED: STATE.md / ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md (NOT in commit)
- FOUND-PASSING: 0 TODOs remain in docs/operators/point-ordinal/ and docs/operators/recency/
