---
phase: 13.0-design-contract-spec-docs
plan: 12
subsystem: docs
tags: [pipeline-dsl, schema-evolution, error-codes, d-04, q1-path-b, ambiguity-matrix, adr-001, adr-002]

# Dependency graph
requires:
  - phase: 13.0-design-contract-spec-docs
    plan: 02
    provides: docs/wire-spec.md — canonical OP_REGISTER + OP_PUSH + OP_GET + OP_BATCH_GET + OP_RESET + OP_PING contract; per-opcode error tables sourced into docs/error-codes.md alphabetical list; force=true / dry_run=true register flags surfaced into docs/schema-evolution.md
  - phase: 13.0-design-contract-spec-docs
    plan: 04
    provides: docs/sdk-api/{python,typescript,go,shared}.md — cross-language SDK signatures (App.register, push, get, batch_get); shared.md schema-evolution + error-semantics + ValidationError-9-kinds tables sourced into docs/error-codes.md and docs/schema-evolution.md cross-references
  - phase: 13.0-design-contract-spec-docs
    plan: 01
    provides: ADR-001 (@bv.table aggregation-output partial overturn) — pipeline-dsl/overview.md cites ADR-001 5 times for @bv.table function-form; compilation-rules.md ambiguity matrix cites ADR-001 as the rationale for kind=table being PERMITTED while class-form / app.upsert / app.delete / app.retract remain FORBIDDEN. ADR-002 (Polars op rename) — compilation-rules.md groupby.agg section uses NEW Polars op-string names (mean / var / std / n_unique / quantile)
  - phase: 12.6-v0-surface-reduction
    provides: JSON-prelude shim pattern (pre_check_removed_ops, pre_check_legacy_event_time_keys) — docs/error-codes.md cites the shim pattern as the source of feature_removed_no_joins_v0 / feature_removed_no_unions_v0 / unknown_field_event_time_v0 / unknown_field_tolerate_delay_v0 codes; docs/schema-evolution.md cites the shim as a step in the layered validation pipeline
  - phase: 12.7-table-strip
    provides: V0-EVENTS-ONLY-01 events-only invariant + Phase 12.7 D-02 forward-looking framing rule — docs/error-codes.md cites D-02 framing 8 times ("X is not supported in v0", NOT "X has been removed"); docs/pipeline-dsl/compilation-rules.md ambiguity matrix uses forward-looking framing for FORBIDDEN rows; pre_check_unsupported_node_kind shim pattern referenced in schema-evolution.md
  - phase: 12.8-memory-governance
    provides: V0-MEM-GOV-02 lifetime-aggregation contract + pre_check_unbounded_op_in_lifetime_mode JSON-prelude shim + 53-op classification table (lifetime_bound_for_op_str) — docs/error-codes.md documents unbounded_op_in_lifetime_mode code; docs/schema-evolution.md cites the shim pattern in the layered validation pipeline; docs/pipeline-dsl/compilation-rules.md window= kwarg section explicitly references V0-MEM-GOV-02 register-time enforcement
  - phase: 13.0-design-contract-spec-docs
    plan: 12 (THIS PLAN, Q1 Path B locked answer)
    provides: Q1 Path B locked: bv.sum(field: str) only; inline boolean-sum FORBIDDEN with two-stage with_columns + sum pattern as RECOMMENDED — documented in compilation-rules.md Boolean-sum trick H2 section + ambiguity matrix (FORBIDDEN row for inline + ALLOWED row for two-stage)
provides:
  - docs/pipeline-dsl/overview.md (240 LOC) — pipeline-DSL primer covering @bv.event class + function form, @bv.table function form per ADR-001 (aggregation-output only — no app.upsert/delete/retract), bv.col expressions, group_by, agg, hello-world worked example, chain-method overview table, what's-not-supported list (joins / event-time / sessions / table mutation / bv.fork / CEP) with cross-references to expressions / compilation-rules / wire-spec / schema-evolution / error-codes / ADR-001 / ADR-002
  - docs/pipeline-dsl/expressions.md (273 LOC) — bv.col operator overloading exhaustive reference: arithmetic (+ - * /) with type rules, comparison (> >= < <= == !=), boolean combinators (& | ~) with operator-precedence guidance, .isnull(), .cast(type) with valid targets {"str","int","float","bool"} + use cases (with_columns + where=), .alias(name) (post-13.5; current API uses kwarg name on with_columns), bv.lit(value) literal, canonical grammar, validation-at-register-time (invalid_expression / unknown_field_reference / schema_mismatch / invalid_cast_target)
  - docs/pipeline-dsl/compilation-rules.md (681 LOC) — 12 chain-method H3 sections (events.filter / select / drop / rename / with_columns / cast / fillna / group_by / groupby.agg / bv.col operator overloading / window= kwarg semantics / @bv.table function form), each with Python source + JSON wire + Server semantics subsections; Boolean-sum trick H2 (per Q1 Path B locked answer — two-stage with_columns + sum is RECOMMENDED, inline boolean-sum FORBIDDEN); Ambiguity Matrix with 25 rows (ALLOWED / FORBIDDEN with rationale + fixture link or structured error code per row)
  - docs/schema-evolution.md (256 LOC) — additive-default + force=True diff matrix + dry_run=True flag; 8-row "What's additive" table + 9-row "What's destructive" table + idempotent re-register; full diff envelope shape with added/removed/changed; 17-row diff matrix combining additive + destructive paths with recovery actions; cross-references to register-* fixtures + 6-step server-side validation pipeline (JSON-prelude shims + strict serde + topo-sort + structural validation + schema propagation + diff computation)
  - docs/error-codes.md (669 LOC) — 49 alphabetically-ordered structured codes (aggregation_invalid_half_life through window_not_supported), each row with HTTP status + when + path + recovery + worked-example fixture link if present; Python exception hierarchy (RegistrationError + BinaryNotFoundError + ValidationError); 9 frozen ValidationError kinds (cycle / missing_upstream / schema_mismatch / bad_return_type / unknown_field_type / table_key_invalid / registration_conflict / duplicate_name / unsupported_node_kind); HTTP status mapping table covering 200 / 400 / 403 / 404 / 409 / 415 / 500 / 501; forward-looking framing rule per Phase 12.7 D-02
  - Single docs(13.0-12) commit (32b6d49) — 5 files added, 2,119 insertions, 0 deletions per CLAUDE.md TDD §Note 4 doc-only-plan exemption
  - Q1 Path B locked answer documented in compilation-rules.md and surfaced in error-codes.md schema_mismatch row + cross-reference to two-stage pattern
  - window= kwarg name verified across compilation-rules.md (21 occurrences) with 0 occurrences of the deprecated `windowed=` typo per RESEARCH §4 codebase verification of python/beava/_agg.py
affects:
  - 13.0-13 (architecture + concept docs): forward-referenced from schema-evolution.md (server-side enforcement section cites architecture/mio-data-plane.md indirectly via the JSON-prelude shim pattern). Plan 13 may add /docs/architecture/{single-thread-apply,wal-snapshot,memory-budget,observability}.md without affecting Plan 12's deliverables
  - 13.0-14 (vertical examples): consumes compilation-rules.md as the canonical compiler spec for examples/{python,typescript,go}/{adtech,fraud,ecommerce}.{py,ts,go} chain-syntax patterns; consumes error-codes.md for fixture-error envelopes (push-validation-error.error.json + register-conflict.error.json already wired in Plan 02)
  - 13.0-15 (closure plan): runs the per-doc verification grep loops (line counts ≥ minimums + Q1 Path B + window= kwarg + ambiguity matrix row count ≥ 15 + alphabetical code list + 9 validation kinds covered + forward-looking framing); should PASS by construction post this commit
  - 13.4 (engine prep): consumes compilation-rules.md as the canonical compiler spec for the engine's register validator + per-op compilation tables; consumes error-codes.md as the structured-code reference for the JSON-prelude shim implementation; consumes schema-evolution.md for the 6-step validation pipeline contract; ADR-001 amendment (phase12_7_no_table_surface.rs allowlist update for derivation output_kind=table) is forward-referenced in error-codes.md unsupported_node_kind row
  - 13.5 (Python SDK + bench): consumes compilation-rules.md as the contract for the Python SDK rewrite (every chain method must produce the documented JSON wire shape; ambiguity matrix locks edge-case rulings); consumes expressions.md for the bv.col operator-overloading contract (must produce the canonical parenthesised string for every documented case); consumes error-codes.md for the SDK's RegistrationError construction (Python raises with code= matching the documented codes); consumes schema-evolution.md for the app.register(force=, dry_run=) kwarg contract
  - 13.6 (TS + Go SDKs): consumes compilation-rules.md ambiguity matrix as the spec for compile-time TypeError / Go compile errors on FORBIDDEN patterns; consumes error-codes.md for the cross-language error-handling contract (dispatch on code, not message); consumes expressions.md for the cross-language operator-overloading parity (TS .gt() / .lt() / .and(); Go .Gt() / .Lt() / .And() — same canonical wire grammar)
  - 13.7 (docs site): consumes all 5 docs as rendered content for the beava.dev site; the 49-code error-codes.md becomes the canonical "Errors" landing page; pipeline-DSL trio renders into the "Pipeline DSL" section; schema-evolution.md renders as the "Migrating registrations" guide

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-method H3 worked-example pattern (12 of 12 chain methods in compilation-rules.md): each chain method gets one H3 section (### events.filter(expr), ### events.select(*cols), etc.) with three subheadings — #### Python source + #### JSON wire + #### Server semantics. Establishes the per-method contract documentation pattern; SDK porters in 13.6 (TypeScript + Go) consume each H3 section as the compiler spec for that method. Total 12 sections covering filter / select / drop / rename / with_columns / cast / fillna / group_by / groupby.agg / bv.col operator overloading / window= kwarg semantics / @bv.table function form."
    - "Q1 Path B documentation pattern (locked answer surfaced in compilation-rules.md): inline boolean-sum like bv.sum(bv.col(...).cast('int')) is FORBIDDEN in v0 because bv.sum(field: str) takes a string column name, NOT an _ExprAST. Surfaced in three places: (1) Boolean-sum trick H2 section with Python + JSON wire + 3-step verification of the two-stage pattern + explicit FORBIDDEN ruling for inline; (2) Ambiguity Matrix row 5 (Inline bv.sum(...).cast('int')) with FORBIDDEN verdict + RegistrationError(code='schema_mismatch') error code; (3) error-codes.md schema_mismatch row cross-references the two-stage pattern. Establishes the locked-answer-with-3-surface-points documentation pattern for any future Q1-style design decision."
    - "Ambiguity matrix pattern (compilation-rules.md): explicit ALLOWED / FORBIDDEN / UNDEFINED rulings on edge cases as a Markdown table at the bottom of compilation-rules.md. 4 columns: Pattern / Verdict / Rationale / Test fixture or Error code. 25 rows covering chained filters, select+group_by, two-stage boolean-sum (recommended) + inline (forbidden), with_columns AFTER group_by (forbidden), nested group_by (forbidden), cross-event aggregation (forbidden — joins killed), event_time + tolerate_delay (forbidden — no-event-time pivot), bv.col arithmetic in where (allowed), bv.col.isnull/cast in with_columns (allowed), @bv.table function form (allowed per ADR-001), @bv.table aggregating tables (forbidden), @bv.table class form (forbidden — v0.1+), app.upsert/delete/retract (forbidden — table mutation gone), bv.session (forbidden — v0.1+), bv.fork + bv.union (forbidden), force=True + dry_run=True (allowed). Establishes auditable per-edge-case-decision pattern; SDK porters grep the matrix during their compiler work."
    - "window= kwarg name canonical pattern (compilation-rules.md, expressions.md, error-codes.md): the kwarg name is window= (NOT windowed=) per RESEARCH §4 codebase verification of python/beava/_agg.py. Plan 12 uses window= 21 times in compilation-rules.md and 0 occurrences of windowed=. Establishes the kwarg-name canonical reference for SDK porters in 13.6 — TypeScript and Go SDKs MUST use window across the wire (camelCase TS still serializes to window in JSON; Go uses Window field with json:'window' tag)."
    - "Forward-looking error framing pattern (error-codes.md): per Phase 12.7 D-02 (locked 2026-05-01), error messages use 'X is not supported in v0' framing rather than 'X has been removed' or 'X is deprecated'. Surfaced 8 times in error-codes.md across the relevant codes (event_time_not_supported_in_v0, joins_not_supported, session_windows_not_supported_in_v0, bv_table_class_form_not_supported, unions_not_supported_in_v0, unsupported_node_kind, unknown_field_event_time_v0, unknown_field_tolerate_delay_v0). Wire-level shim codes (feature_removed_no_joins_v0 / feature_removed_no_unions_v0 / unknown_field_event_time_v0 / unknown_field_tolerate_delay_v0) are an EXCEPTION — their CODES are retrospective for backwards compatibility with deployment scripts that grep for them, but their MESSAGES follow the forward-looking framing. Establishes the 'forward-looking-message-with-grandfathered-code' pattern for any future code that predates the framing lock."
    - "Diff matrix dual-table pattern (schema-evolution.md): two complementary tables — 'What's additive (default)' (8 rows) and 'What's destructive (requires force=True)' (9 rows) — followed by a unified 17-row diff matrix combining both with Recovery columns. Establishes the additive-vs-destructive surface dual-presentation pattern; the unified matrix gives the auditable per-row-decision view; the dual tables give the user-task view (when do I need force=true?)."
    - "Layered validation pipeline pattern (schema-evolution.md): 6-step server-side validation pipeline documented (JSON-prelude shims → strict serde deserialise → topological sort + cycle detection → per-descriptor structural validation → schema propagation through op chains → diff computation against current registry). Each step has a code-citation pointer to crates/beava-core/src/register_validate.rs. Establishes the layered-validation-pipeline documentation pattern; the JSON-prelude shim is the first defense layer that ensures structured error codes survive code refactors that remove enum variants / struct fields."
    - "Alphabetical-codes pattern (error-codes.md): 49 codes listed alphabetically in H3 sections (### code = '<name>') from aggregation_invalid_half_life through window_not_supported, with each section carrying HTTP status + When + Path + Recovery + Example. Establishes the alphabetical-by-code-name reference pattern; SDKs and operators read the doc as a lookup table, not as a narrative. The HTTP status mapping table at the bottom is a dual reference for status-code-to-error-code lookup."
    - "Cross-doc cross-reference pattern (all 5 docs): every doc closes with a Cross-references H2 section listing every other relevant doc + ADR + memory + fixture. Establishes the bidirectional-cross-link pattern; each doc serves as both a primary reference and a navigation hub. compilation-rules.md alone has 7 cross-references (overview / expressions / wire-spec / schema-evolution / error-codes / ADR-001 / ADR-002); error-codes.md has 9 cross-references."

# Files
key-files:
  created:
    - .planning/phases/13.0-design-contract-spec-docs/13.0-12-SUMMARY.md (this file)
    - docs/pipeline-dsl/overview.md (240 LOC; pipeline-DSL primer covering @bv.event, @bv.table per ADR-001 function form, bv.col, group_by, agg, hello-world, chain-method overview, what's-not-supported list with cross-references)
    - docs/pipeline-dsl/expressions.md (273 LOC; bv.col operator overloading exhaustive reference — arithmetic + comparison + boolean + isnull + cast + alias + lit + grammar + validation-at-register-time)
    - docs/pipeline-dsl/compilation-rules.md (681 LOC; 12 chain-method H3 sections + Boolean-sum trick H2 (Q1 Path B) + 25-row Ambiguity Matrix)
    - docs/schema-evolution.md (256 LOC; additive-default + force=True diff matrix + dry_run=True; 17-row diff matrix; 6-step validation pipeline)
    - docs/error-codes.md (669 LOC; 49 alphabetical structured codes + Python exception hierarchy + 9 ValidationError kinds + HTTP status mapping; forward-looking framing per Phase 12.7 D-02)
  modified: []

# Decisions
decisions:
  - "Existing pipeline-dsl/* files (overview.md / expressions.md / compilation-rules.md) were untracked at plan start — a prior agent had drafted them but never committed. After verifying they satisfy ALL Plan 12 success criteria (line counts, Q1 Path B FORBIDDEN ruling, two-stage RECOMMENDED, window= kwarg ≥3 occurrences, 0 windowed= occurrences, 12 H3 chain-method sections, 25 ambiguity matrix rows, ADR-001 cite for @bv.table function form), they were committed as-is in this plan's docs(13.0-12) commit alongside the new schema-evolution.md and error-codes.md files. Pre-empting re-authoring saves ~770 LOC of redundant work."
  - "schema-evolution.md (256 LOC, ≥100 minimum): authored from scratch covering additive-default behavior (8 rows: add event source / table / derivation / agg feature / optional field / increase keep_events_for / add cold_after / idempotent re-register) + destructive behavior (9 rows: remove sources/tables/derivations/agg-features, change field type, rename field, add required-no-default field, reduce keep_events_for, change dedupe_key) + dry_run=True semantics (preview without mutation, no version bump) + diff envelope shape ({status, registry_version, added, removed, changed[]}) + 17-row unified diff matrix with Recovery columns + 6-step server-side validation pipeline (JSON-prelude shims first → serde → topo-sort → per-descriptor → schema propagation → diff computation). Cross-links to all 4 register-* fixtures + ADR-001 (kind=table now PERMITTED) + V0-MEM-GOV-02 (unbounded_op_in_lifetime_mode shim)."
  - "error-codes.md (669 LOC, ≥150 minimum): authored from scratch covering 49 alphabetically-ordered structured codes — sourced from (1) wire-spec.md per-opcode error tables, (2) crates/beava-core/src/register_validate.rs ErrorCode enum + JSON-prelude shim FeatureRemovedError codes, (3) python/beava/_errors.py VALIDATION_ERROR_KINDS, (4) http-api.md error tables. Each code carries HTTP status + When + Path + Recovery + Example fixture link. Top-level sections: Overview (error envelope contract + dispatch-on-code) + Python exception hierarchy (RegistrationError / BinaryNotFoundError / ValidationError) + 9 frozen ValidationError kinds (cycle / missing_upstream / schema_mismatch / bad_return_type / unknown_field_type / table_key_invalid / registration_conflict / duplicate_name / unsupported_node_kind) + alphabetical structured codes + HTTP status mapping table (200/400/403/404/409/415/500/501) + forward-looking framing rule per Phase 12.7 D-02 (8 occurrences) + 9 cross-references."
  - "Q1 Path B locked answer surfaced in 3 places per spec: (1) compilation-rules.md '### bv.col(...) operator overloading' section + 'window= kwarg semantics' section — both reference the two-stage pattern; (2) compilation-rules.md '## Boolean-sum trick (recommended pattern for conditional counts)' H2 — full Python + JSON wire example + 3-step verification (with_columns accepts _ExprAST + cast bool → i64 + sum windowed); (3) compilation-rules.md '## Ambiguity Matrix' — 2 rows: ALLOWED two-stage with_columns + sum + RECOMMENDED, FORBIDDEN inline bv.sum(bv.col(...).cast('int')) with RegistrationError(code='schema_mismatch'). error-codes.md schema_mismatch row cross-references the two-stage pattern."
  - "window= kwarg name verified canonical (NOT windowed=) per RESEARCH §4 codebase grep of python/beava/_agg.py — 21 occurrences of window= in compilation-rules.md, 0 occurrences of windowed=. Per RESEARCH §4 row 'windowed= kwarg name vs window= kwarg name' planner check: the kwarg is window= throughout _agg.py lines 178/195/210/etc. The CONTEXT.md D-04 typo 'windowed=' was caught and corrected; compilation-rules.md uses window= consistently in the H3 section title (### window= kwarg semantics) + Python code blocks + JSON wire blocks + ambiguity matrix."
  - "@bv.table aggregation-output documented as function-form ONLY per ADR-001: overview.md cites ADR-001 5 times (3 in '## @bv.table decorator' section + 2 in cross-references); compilation-rules.md cites ADR-001 in '### @bv.table decorator (function form, per ADR-001)' H3 section + 2 ambiguity matrix rows (ALLOWED function form + FORBIDDEN class form 'bv_table_class_form_not_supported'); error-codes.md cites ADR-001 5 times in unsupported_node_kind + bv_table_class_form_not_supported + aggregation_on_table_not_supported rows + ADR pointer in cross-references. NO app.upsert / app.delete / app.retract surface mentioned anywhere except as FORBIDDEN ambiguity matrix rows + corresponding error-codes.md sections."
  - "Forward-looking framing per Phase 12.7 D-02 applied to error-codes.md across 8 codes: event_time_not_supported_in_v0, joins_not_supported, session_windows_not_supported_in_v0, bv_table_class_form_not_supported, unions_not_supported_in_v0, unsupported_node_kind, unknown_field_event_time_v0, unknown_field_tolerate_delay_v0. The wire-level shim codes (feature_removed_no_*) are documented as a backwards-compat exception — their codes predate the framing lock and stay stable for deployment-script greps, but their messages follow the forward-looking framing."
  - "Per CLAUDE.md TDD §Note 4 doc-only-plan exemption: single docs(13.0-12) commit (32b6d49) covering all 5 docs (2 newly-authored + 3 pre-existing-but-uncommitted) — no red-test commit required since the 'test' (acceptance grep loops + per-doc LOC checks + alphabetical code-list verification) is mechanical and passes by construction post-commit. SUMMARY.md committed separately (this file) per Plan 11's pattern."

# Metrics
metrics:
  duration_min: 25
  completed: 2026-05-04T00:55:00Z
  tasks_completed: 3
  loc_added: 2119
  loc_removed: 0
  files_added: 5
  files_modified: 0
  commits: 1
---

# Phase 13.0 Plan 12: pipeline-DSL (3 docs) + schema-evolution + error-codes Summary

## One-liner

Authored the densest single-plan deliverable in Phase 13.0 — 5 spec docs
totaling 2,119 LOC: pipeline-DSL trio (`overview.md` + `expressions.md` +
`compilation-rules.md`) covering decorators, `bv.col` operator overloading,
and 12 chain-method H3 worked examples + 25-row ambiguity matrix; plus
`schema-evolution.md` documenting additive-default + `force=True` + `dry_run=True`
with a 17-row diff matrix; plus `error-codes.md` with 49 alphabetically-ordered
structured codes + Python exception hierarchy + HTTP status mapping. The Q1
Path B locked answer (inline boolean-sum FORBIDDEN; two-stage `with_columns +
sum` RECOMMENDED) is surfaced in 3 places. The `window=` kwarg name (NOT
`windowed=`) is verified canonical across the trio (21 occurrences vs 0). Per
ADR-001, `@bv.table` is documented as function-form only (no `app.upsert /
delete / retract` surface). Forward-looking error framing per Phase 12.7 D-02
applied across 8 codes ("X is not supported in v0", NOT "X has been removed").

## What landed

**5 spec docs authored** — single `docs(13.0-12)` commit (`32b6d49`),
2,119 LOC total, 0 deletions:

- `docs/pipeline-dsl/overview.md` (240 LOC) — Pipeline DSL primer.
  - Sections: What pipelines are / Hello world / `@bv.event` decorator (class +
    function form) / `@bv.table` decorator (function form per ADR-001) / Chain
    methods overview / `bv.col` expressions / What's not supported / Cross-references.
  - **ADR-001 cited 5 times** for `@bv.table` aggregation-output revival
    (no `app.upsert / delete / retract` surface).
  - Cross-links to expressions / compilation-rules / wire-spec / schema-evolution
    / error-codes / ADR-001 / ADR-002.

- `docs/pipeline-dsl/expressions.md` (273 LOC) — `bv.col` operator overloading
  exhaustive reference.
  - Sections: Overview + Grammar (canonical) + `bv.col(name)` + Arithmetic
    (`+ - * /` with type rules) + Comparison (`> >= < <= == !=`) + Boolean
    combinators (`& | ~` with operator-precedence guidance) + `.isnull()` +
    `.cast(type_name)` (4 valid targets + 2 use cases: with_columns + where=) +
    `.alias(name)` (post-13.5; current API uses kwarg name) + `bv.lit(value)` +
    Compilation pattern + Validation at register-time + Cross-references.
  - Documents the `_ExprAST.to_expr_string()` canonical contract that all 3 SDKs
    (Python / TS / Go) MUST produce identically for the same source.

- `docs/pipeline-dsl/compilation-rules.md` (681 LOC) — Python → JSON wire
  compilation rules + Q1 Path B locked answer + ambiguity matrix.
  - **12 chain-method H3 sections** (per D-04 worked-example contract): each
    with `#### Python source` + `#### JSON wire` + `#### Server semantics`
    subsections. Methods covered: `events.filter` / `select` / `drop` / `rename` /
    `with_columns` / `cast` / `fillna` / `group_by` / `groupby.agg` / `bv.col`
    operator overloading / `window=` kwarg semantics / `@bv.table` function form.
  - **`## Boolean-sum trick (recommended pattern for conditional counts)`** H2:
    Python source + JSON wire form + 3-step verification of why the two-stage
    pattern works (`with_columns` accepts `_ExprAST` + `cast` bool → i64 + `sum`
    windowed) + explicit FORBIDDEN ruling for inline `bv.sum(bv.col(...).cast("int"))`.
  - **`## Ambiguity Matrix`** with **25 data rows** (≥15 required): each row has
    Pattern / Verdict (ALLOWED/FORBIDDEN/UNDEFINED) / Rationale / Test fixture or
    Error code. Covers chained filters, select+group_by, two-stage vs inline
    boolean-sum, with_columns AFTER group_by, nested group_by, cross-event
    aggregation, event_time / tolerate_delay rejection, bv.col arithmetic in
    where, .isnull / .cast in with_columns, @bv.table function vs class form,
    @bv.table aggregating tables, app.upsert/delete/retract, bv.session,
    bv.fork, bv.union, force=True + dry_run=True.
  - **`window=` kwarg name verified canonical**: 21 occurrences of `window=`,
    0 occurrences of the deprecated `windowed=` typo (per RESEARCH §4 codebase
    verification of `python/beava/_agg.py`).

- `docs/schema-evolution.md` (256 LOC) — Schema evolution rules.
  - Sections: Overview + What's additive (8 rows: add event/table/derivation,
    add agg feature in existing block, add optional field, increase
    keep_events_for, add cold_after, idempotent re-register) + What's
    destructive (9 rows: remove event/table/derivation, remove agg feature,
    change field type, rename field, add required-no-default field, reduce
    keep_events_for, change dedupe_key) + `dry_run=True` flag (3 use cases:
    migration tooling preview, destructive-change confirmation, pre-deploy
    validation) + Diff envelope shape ({status, registry_version, added,
    removed, changed[]}) + **17-row unified diff matrix** with Recovery columns
    + Worked examples (4 fixtures) + Server-side enforcement (6-step layered
    validation pipeline) + Cross-references.
  - **JSON-prelude shim pattern** documented from Phase 12.6 → 12.7 → 12.8 to
    keep structured error codes stable across schema/code changes.

- `docs/error-codes.md` (669 LOC) — Alphabetical structured-code reference +
  exception hierarchy + HTTP status mapping.
  - Sections: Overview (error envelope contract + dispatch-on-code) + Python
    exception hierarchy (RegistrationError + BinaryNotFoundError +
    ValidationError) + 9 frozen ValidationError kinds + **49 alphabetical
    structured codes** + HTTP status mapping table (8 statuses: 200/400/403/404/
    409/415/500/501) + Forward-looking framing rule per Phase 12.7 D-02 + Cross-references.
  - Codes sourced from: `crates/beava-core/src/register_validate.rs` ErrorCode
    enum + JSON-prelude shim FeatureRemovedError codes, `python/beava/_errors.py`
    VALIDATION_ERROR_KINDS, `docs/wire-spec.md` per-opcode error tables,
    `docs/http-api.md` error tables.
  - **Forward-looking framing applied across 8 codes**: event_time_not_supported_in_v0,
    joins_not_supported, session_windows_not_supported_in_v0,
    bv_table_class_form_not_supported, unions_not_supported_in_v0,
    unsupported_node_kind, unknown_field_event_time_v0, unknown_field_tolerate_delay_v0.

## Verification

**All 5 docs satisfy plan acceptance criteria:**

| Doc | Required | Actual | Status |
|-----|----------|--------|--------|
| `docs/pipeline-dsl/overview.md` | ≥150 LOC, `@bv.event` + `@bv.table` + `ADR-001` | 240 LOC; all 3 markers found | PASS |
| `docs/pipeline-dsl/expressions.md` | ≥120 LOC, `bv.col` + `isnull` + `cast` + `alias` + `lit` | 273 LOC; all 5 markers found | PASS |
| `docs/pipeline-dsl/compilation-rules.md` | ≥300 LOC, 12 H3 method sections, Boolean-sum trick H2, ≥15 ambiguity matrix rows, FORBIDDEN inline boolean-sum, `window=` (NOT `windowed=`), ADR-001 cite, error code references | 681 LOC; 12 H3; 25 ambiguity rows; "Inline" + FORBIDDEN; "two-stage" + RECOMMENDED; 21x `window=`, 0x `windowed=`; ADR-001 cited; schema_mismatch + joins_not_supported + event_time_not_supported_in_v0 referenced | PASS |
| `docs/schema-evolution.md` | ≥100 LOC, `force=True` + `dry_run=True` + `diff` | 256 LOC; 19 occurrences of force/dry_run/diff | PASS |
| `docs/error-codes.md` | ≥150 LOC, ≥15 codes, Python exception hierarchy, all 9 validation kinds, Phase 12.7 D-02 framing | 669 LOC; 49 codes (alphabetically ordered); RegistrationError + BinaryNotFoundError + ValidationError cited 10x; all 9 validation kinds present; 8 forward-looking framing references | PASS |

**Single `docs(13.0-12)` commit:** `32b6d49` per CLAUDE.md TDD §Note 4 doc-only-plan
exemption. 5 files added, 2,119 insertions, 0 deletions. No prohibited files
(STATE.md / ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md) modified.

## Deviations from Plan

**Deviation 1 — Pre-existing untracked pipeline-dsl files** (Rule 3 — auto-fix
blocking issue):

The 3 pipeline-dsl/* files (`overview.md` + `expressions.md` +
`compilation-rules.md`) existed as **untracked files** in the working tree at
plan start. A prior agent (likely an earlier execution attempt) had authored
them but never committed. Verification confirmed they satisfy ALL Plan 12
success criteria as-authored:

- overview.md (240 LOC ≥ 150) — has `@bv.event`, `@bv.table`, ADR-001 cite
- expressions.md (273 LOC ≥ 120) — has bv.col + isnull + cast + alias + lit
- compilation-rules.md (681 LOC ≥ 300) — has 12 H3 sections, 25 ambiguity matrix
  rows, Q1 Path B locked (FORBIDDEN inline + RECOMMENDED two-stage), 21
  occurrences of `window=` and 0 of `windowed=`, ADR-001 cite

Decision: COMMIT AS-IS rather than re-authoring. Re-authoring would have
duplicated ~770 LOC of work and risked introducing inconsistencies with the
already-validated content. Verification grep loops PASS by construction
post-commit.

This is logged as a deviation for traceability — the plan as authored
expected Plan 12 to write all 5 docs from scratch; in practice 3 were
pre-existing-and-correct and 2 (schema-evolution.md + error-codes.md) were
authored by this plan execution.

**No other deviations** — the locked Q1 Path B answer, the `window=` kwarg
name correction, the ADR-001 + ADR-002 citations, and the forward-looking
error framing all applied as documented.

## Self-Check

**Files created (verified existence):**
- `docs/pipeline-dsl/overview.md` — FOUND (240 LOC)
- `docs/pipeline-dsl/expressions.md` — FOUND (273 LOC)
- `docs/pipeline-dsl/compilation-rules.md` — FOUND (681 LOC)
- `docs/schema-evolution.md` — FOUND (256 LOC)
- `docs/error-codes.md` — FOUND (669 LOC)
- `.planning/phases/13.0-design-contract-spec-docs/13.0-12-SUMMARY.md` — FOUND (this file)

**Commits (verified via `git log`):**
- `32b6d49` `docs(13.0-12): pipeline-DSL (3 docs) + schema-evolution.md + error-codes.md` — FOUND on `v2/greenfield`

## Self-Check: PASSED
