---
phase: 13.0-design-contract-spec-docs
plan: 06
subsystem: docs
tags: [operators, catalog, polish, polars-rename, adr-002, core-family, sketch-family]

# Dependency graph
requires:
  - phase: 13.0-design-contract-spec-docs
    plan: 01
    provides: ADR-002 Polars op renames (mean/var/std/n_unique/quantile) — cited in 5 renamed-op pages via "Previously called bv.<old>" deprecation note + ADR link
  - phase: 13.0-design-contract-spec-docs
    plan: 05
    provides: 54 op page stubs scaffolded under docs/operators/<family>/<op>.md with H1 + 9 mandatory H2 sections and TODO prose markers labeled by polish plan number; Plan 06 fills the 13 stubs labeled "Plan 13.0-06" (8 core + 5 sketch)
  - phase: 19.2-operator-uniformity
    provides: docs/operators/cost-class.md (alive Phase 19.2 metadata, KEPT, NOT touched by Plan 06) — cross-linked from each polished page's Complexity table for CPU tier (Tier 1/2/3) lookup
  - phase: 12.8-memory-governance
    provides: V0-MEM-GOV-02 lifetime-aggregation contract (BoundedSketch / BoundedByConfig per-entity ceilings) — cited in all 5 sketch op Complexity sections + Edge cases
provides:
  - 8 core/ op pages polished from Plan 05 stubs (count, sum, mean, min, max, var, std, ratio) — each 134-139 LOC; TODO markers replaced with full Description / Parameters / Returns / Complexity / Examples / Edge cases / See also prose
  - 5 sketch/ op pages polished (n_unique, quantile, top_k, bloom_member, entropy) — each 146-157 LOC; full hybrid mode discussion + BoundedSketch / BoundedByConfig memory governance citations
  - docs/operators/core/index.md (24 LOC) — Core family overview + 8-op summary table with rename annotations
  - docs/operators/sketch/index.md (20 LOC) — Sketch family overview + 5-op summary table with memory class + CPU tier
  - 5 ADR-002 deprecation notes (mean/var/std/n_unique/quantile) — each carries the canonical "Previously called bv.<old>. Renamed to <new> per ADR-002 for Polars-convention consistency. The old name remains as a deprecation alias in v0.0.x and is removed in v0.1." note below the Signature
  - bv.sum page documents Q1 Path B FORBIDDEN ruling: inline boolean-sum (`bv.sum(bv.col("is_fraud").cast(int))`) is NOT supported in v0; cross-link to forthcoming pipeline-dsl/compilation-rules.md for the two-stage pattern
  - bv.bloom_member page documents the no-`window=`-kwarg lifetime-only invariant explicitly — Bloom semantics require lifetime retention; attempting to add `window=` raises TypeError at SDK-helper-call time
  - Quadkey-for-geo recipes documented on bv.n_unique (replacement for deleted bv.unique_cells) + bv.entropy (replacement for deleted bv.geo_entropy) per Phase 19.2-06 removal note
affects:
  - 13.0-07 (point-ordinal + recency polish): inherits the per-page template established here (134-157 LOC range, 9 H2 sections, ADR-002 note pattern, cost-class cross-link, Edge cases cold-start convention)
  - 13.0-08/09/10/11 (decay/velocity/buffer/geo polish): same template inheritance; the polish plans 06-11 collectively eliminate the 270 TODO markers Plan 05 sowed
  - 13.0-12 (pipeline-dsl/compilation-rules.md): forward-referenced from bv.sum Edge cases for the boolean-sum two-stage pattern; from bv.count/sum/mean See also for chain compilation rules (the link target page does not yet exist — Plan 12 creates it)
  - 13.0-15 (closure plan): runs scripts/check_op_page_coverage.py + verifies no TODO markers remain in core/+sketch/ (this plan's contribution: 0 TODOs left in 13 op pages)
  - 13.4 (engine prep): the polished pages reference target post-13.4 wire shapes (verb-style routes, "kind=table"+derivation output_kind) — Plan 13.4 implements the engine to match

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-op page template (RESEARCH §3): H1 + one-line tagline + 9 H2 sections (Signature / Description / Parameters / Returns / Complexity / Examples / Wire / Edge cases / See also). Length 70-160 LOC depending on op complexity (count = 136 / quantile = 152). Polished consistently across all 13 ops."
    - "ADR-002 deprecation-note pattern: every renamed op (mean / var / std / n_unique / quantile) carries the canonical 'Previously called bv.<old>. Renamed to <new> per ADR-002 for Polars-convention consistency. The old name remains as a deprecation alias in v0.0.x and is removed in v0.1.' note BELOW the Signature block, BEFORE the Description block — preserves searchability for users migrating from beava-v1 docs / pre-v0 dev pipelines."
    - "Cold-start convention documented per-op: count returns 0 (integer); ratio returns null when no events; sum/mean/min/max/var/std return null on cold-start; var/std return null when n<2 (Bessel correction undefined); n_unique returns 0 (cardinality of empty set is 0); top_k returns [] (empty list); bloom_member returns null until first matching event; entropy returns null on cold-start. This per-op cold-start contract is now uniformly documented in each Edge cases section."
    - "Q1 Path B FORBIDDEN ruling documented on bv.sum: inline boolean-sum trick (bv.sum(bv.col('is_fraud').cast(int))) is NOT supported in v0 — bv.sum takes a literal field name (str), not an expression. Compile the boolean → integer cast as a stateless with_columns step first, then sum the resulting field. Cross-link to forthcoming docs/pipeline-dsl/compilation-rules.md for the two-stage pattern."
    - "BoundedByConfig memory-governance citation pattern: entropy and top_k pages cite Phase 12.8 V0-MEM-GOV-02 + the BoundedByConfig kwarg name (max_categories=256 for entropy, k for top_k) in both the Complexity table and Edge cases section. Establishes the citation pattern for the other Phase 12.8 BoundedByConfig ops (event_type_mix, distance_from_home) when their pages get polished in Plans 10/11."
    - "Hybrid sketch mode documented as transparent at the API: quantile (exact-then-DDSketch promotion at exact_threshold=256) and n_unique (exact-then-HLL promotion at exact_threshold=1024) callers always read a single value (float / int); promotion is observable only via cost-class shift (Tier 2 → Tier 3) — no API surface for the mode switch. Documented in both Description and Edge cases."
    - "Family index template: short H1 + one-line family overview + summary table (per-op link + one-line description) + See also section linking to operator catalog index + cost-class.md + pipeline-dsl/compilation-rules.md. Core uses simpler 'Description' column (all O(1)/Tier 1); Sketch uses 2-column 'Memory class | CPU tier' to surface the BoundedSketch vs BoundedByConfig distinction."

# Files
key-files:
  created:
    - docs/operators/core/index.md (24 LOC) — Core family overview + 8-op summary table
    - docs/operators/sketch/index.md (20 LOC) — Sketch family overview + 5-op summary table with memory + CPU columns
    - .planning/phases/13.0-design-contract-spec-docs/13.0-06-SUMMARY.md (this file)
  modified:
    - docs/operators/core/count.md (40 → 136 LOC; +96 LOC; full polish — Tier 1, O(1), lifetime allowed)
    - docs/operators/core/sum.md (40 → 134 LOC; +94 LOC; full polish — documents Q1 Path B FORBIDDEN inline boolean-sum)
    - docs/operators/core/mean.md (41 → 137 LOC; +96 LOC; full polish — ADR-002 cited; Welford-friendly accumulator)
    - docs/operators/core/min.md (40 → 136 LOC; +96 LOC; full polish — preserves field type)
    - docs/operators/core/max.md (40 → 134 LOC; +94 LOC; full polish — preserves field type)
    - docs/operators/core/var.md (41 → 139 LOC; +98 LOC; full polish — ADR-002 cited; Welford 5-FP-op step)
    - docs/operators/core/std.md (41 → 139 LOC; +98 LOC; full polish — ADR-002 cited; sqrt deferred to query)
    - docs/operators/core/ratio.md (40 → 134 LOC; +94 LOC; full polish — count(where=p)/count(total))
    - docs/operators/sketch/n_unique.md (41 → 150 LOC; +109 LOC; full polish — ADR-002 cited; HLL hybrid_precision=14)
    - docs/operators/sketch/quantile.md (41 → 152 LOC; +111 LOC; full polish — ADR-002 cited; DDSketch hybrid_alpha=0.01; RESEARCH §3 worked example adapted)
    - docs/operators/sketch/top_k.md (40 → 157 LOC; +117 LOC; full polish — V0-MEM-GOV-02 cited; SpaceSaving + bounded heap hybrid)
    - docs/operators/sketch/bloom_member.md (40 → 148 LOC; +108 LOC; full polish — lifetime-only invariant documented; capacity=1024 + fpr=0.01 defaults)
    - docs/operators/sketch/entropy.md (40 → 146 LOC; +106 LOC; full polish — V0-MEM-GOV-02 cited; max_categories=256 BoundedByConfig)

# Decisions
decisions:
  - "Per-op page template inherited verbatim from Plan 05 scaffold (no structural changes to the H2 layout); polish only fills the 5 prose sections per op (Description / Parameters / Returns / Examples / Edge cases) plus enriches Complexity + See also where applicable. The Signature + Wire + ADR-002 note remain unchanged from Plan 05."
  - "Cold-start return convention documented per-op consistently: count returns integer 0; ratio/sum/mean/min/max/var/std return null; var+std return null when n<2; n_unique returns integer 0; top_k returns empty list; bloom_member returns null until first matching event; entropy returns null. This makes the cold-start contract auditable from the docs without reading source — important for SDK porters in 13.6."
  - "Q1 Path B FORBIDDEN ruling cross-linked from bv.sum: inline boolean-sum (bv.sum(bv.col('is_fraud').cast(int))) is NOT supported in v0 because bv.sum takes a str field name, not an expression. The two-stage pattern (with_columns to cast, then sum the new field) is the documented workaround. Forward-references docs/pipeline-dsl/compilation-rules.md (Plan 13.0-12) for the canonical two-stage example."
  - "Hybrid mode parameter naming: documented quantile.params.exact_threshold=256 + hybrid_alpha=0.01; n_unique.params.exact_threshold=1024 + hybrid_precision=14; top_k.params.exact_threshold=1024 + hybrid_width=2048 + hybrid_depth=4; bloom_member.params.capacity=1024 + fpr=0.01; entropy.params.max_categories=256. These are the v0-target parameter names that 13.4 (engine) and 13.5 (Python SDK) will implement; Plan 06 documents the contract that downstream phases must match. Current crates/beava-core uses camelCase-equivalent (percentile_q, top_k_k, bloom_capacity, bloom_fpr, max_categories) — the rename to snake_case + the exact_threshold/hybrid_* split lands in 13.4."
  - "Quadkey-for-geo recipes documented on n_unique + entropy: replaces deleted bv.unique_cells (Phase 19.2-06: use bv.n_unique(quadkey(lat, lon, zoom))) + deleted bv.geo_entropy (use bv.entropy(quadkey(lat, lon, zoom), max_categories=1024)). This preserves the deleted ops' use cases via the recipe pattern without re-introducing the dedicated AggKind variants."
  - "Per CLAUDE.md TDD §Note 4 doc-only-plan exemption: single docs(13.0-06) commit covering 13 polished pages + 2 family index pages — no red-test commit required since the 'test' (acceptance grep loops + scripts/check_op_page_coverage.py) is mechanical and passes by construction post-polish."

# Metrics
metrics:
  duration_min: 25
  completed: 2026-05-03T23:40:00Z
  tasks_completed: 3
  loc_added: 1265
  loc_removed: 268
  files_added: 2
  files_modified: 13
  commits: 1
---

# Phase 13.0 Plan 06: Operator catalog polish — core (8) + sketch (5) Summary

## One-liner

Polished 13 op pages (8 core + 5 sketch) from Plan 05 stubs into full-prose
spec documents — Description / Parameters / Returns / Complexity / Examples /
Edge cases / See also — and added 2 family overview index pages
(`core/index.md`, `sketch/index.md`). Renamed ops (mean/var/std/n_unique/
quantile) carry ADR-002 deprecation notes; sketch ops with `BoundedByConfig`
ceilings (top_k, entropy) cite Phase 12.8 V0-MEM-GOV-02. The bv.sum page
documents the Q1 Path B FORBIDDEN inline boolean-sum ruling with a forward
cross-link to the two-stage pattern in pipeline-dsl/compilation-rules.md.
Single `docs(13.0-06)` commit per CLAUDE.md TDD §Note 4 doc-only-plan
exemption.

## What landed

### 8 core/ op pages polished (134-139 LOC each)

| Op | LOC | Notes |
|----|----:|-------|
| `count.md` | 136 | Tier 1 ~25 ns; O(1); lifetime allowed; cold-start returns integer 0 |
| `sum.md` | 134 | Tier 1 ~25 ns; documents Q1 Path B FORBIDDEN inline boolean-sum |
| `mean.md` | 137 | Tier 1 ~25 ns; ADR-002 cited (renamed from `avg`); Welford-friendly |
| `min.md` | 136 | Tier 1 ~30 ns; preserves field type; null on cold-start |
| `max.md` | 134 | Tier 1 ~30 ns; preserves field type; null on cold-start |
| `var.md` | 139 | Tier 1 ~32 ns; ADR-002 cited (renamed from `variance`); Welford 5-FP-op |
| `std.md` | 139 | Tier 1 ~32 ns; ADR-002 cited (renamed from `stddev`); sqrt at query |
| `ratio.md` | 134 | Tier 1 ~25 ns; window optional; null on cold-start |

### 5 sketch/ op pages polished (146-157 LOC each)

| Op | LOC | Notes |
|----|----:|-------|
| `n_unique.md` | 150 | Tier 2/3; ADR-002 cited (renamed from `count_distinct`); HLL hybrid `hybrid_precision=14` |
| `quantile.md` | 152 | Tier 2/3; ADR-002 cited (renamed from `percentile`); DDSketch hybrid `hybrid_alpha=0.01`; adapted from RESEARCH §3 worked example |
| `top_k.md` | 157 | Tier 2/3; V0-MEM-GOV-02 cited; SpaceSaving + bounded heap; `BoundedByConfig("k", 10)` |
| `bloom_member.md` | 148 | Tier 2; lifetime-only invariant documented (no `window=` kwarg); `capacity=1024` + `fpr=0.01` |
| `entropy.md` | 146 | Tier 3; V0-MEM-GOV-02 cited; `max_categories=256` BoundedByConfig |

### 2 family overview index pages

- `docs/operators/core/index.md` (24 LOC) — Core family overview + 8-op summary table with rename annotations + See also pointers
- `docs/operators/sketch/index.md` (20 LOC) — Sketch family overview + 5-op summary table with two-column "Memory class | CPU tier" surface to highlight the BoundedSketch vs BoundedByConfig distinction

### TODO markers eliminated

Per Plan 05's split, the 13 op stubs in core/+sketch/ collectively carried
65 TODO markers (5 sections × 13 ops). After Plan 06 polish: **0 TODO
markers remain** in core/ + sketch/. The remaining 205 TODO markers are
distributed across point-ordinal/, recency/, decay/, velocity/, and
buffer-geo/ — owned by Plans 07-11.

```bash
$ grep -rcE 'TODO \(Plan 13\.0-' docs/operators/core/ docs/operators/sketch/ | grep -v ':0$' | wc -l
0
```

### Cross-link discipline

Every polished page cross-links to:

- `docs/operators/cost-class.md` (Phase 19.2 alive metadata) — for CPU tier lookup
- `docs/operators/index.md` — master 53-op catalog index (via family index pages)
- `docs/pipeline-dsl/compilation-rules.md` — forward-reference (Plan 13.0-12 creates the file)
- `examples/wire/register-fraud-team.request.json` — full payload example (the one PUSH/REGISTER fixture that demonstrates the 5 most-used ops)
- ADR-002 (renamed ops only) — `.planning/decisions/ADR-002-polars-op-rename.md`

Sketch ops additionally cross-link `.planning/REQUIREMENTS.md` (V0-MEM-GOV-02
contract) for the lifetime memory-governance contract.

## Verification (acceptance criteria)

All Task 1 / Task 2 / Task 3 acceptance criteria verified post-commit:

### Task 1 — 8 core ops

- ✅ All 8 core ops polished, no TODO markers: `for op in count sum mean min max var std ratio; do ! grep -qE 'TODO \(Plan 13\.0-' docs/operators/core/${op}.md; done` (silent OK)
- ✅ All 8 core ops ≥ 70 lines (got 134-139)
- ✅ mean / var / std cite ADR-002 + "Previously called" (3/3)
- ✅ All 8 reference cost-class.md
- ✅ All 8 have @bv.event/@bv.table Python example + JSON wire form

### Task 2 — 5 sketch ops

- ✅ All 5 sketch ops polished, no TODO markers
- ✅ All 5 ≥ 80 lines (got 146-157)
- ✅ n_unique + quantile cite ADR-002 (2/2)
- ✅ quantile mentions DDSketch (12 occurrences)
- ✅ n_unique mentions HLL/hybrid_precision (13 occurrences)
- ✅ top_k cites V0-MEM-GOV/BoundedByConfig/max_categories (7 occurrences)
- ✅ entropy cites V0-MEM-GOV/max_categories (14 occurrences)

### Task 3 — family index pages + commit

- ✅ docs/operators/core/index.md exists (24 LOC ≥ 20)
- ✅ docs/operators/sketch/index.md exists (20 LOC ≥ 20)
- ✅ core/index.md links all 8 core ops
- ✅ sketch/index.md links all 5 sketch ops
- ✅ Single docs(13.0-06) commit: `git log -1 --format=%s` → `docs(13.0-06): polish 13 op pages — core (8) + sketch (5) + 2 family index pages`
- ✅ Commit references core + sketch in body
- ✅ 15 files staged (8 core + 5 sketch + 2 family index)

### Plan-level invariants

- ✅ STATE.md / ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md NOT modified (none in commit)
- ✅ docs/operators/cost-class.md NOT modified (last touched 27 Apr by Phase 19.2)
- ✅ Plan 05's coverage tripwire still passes: `python3 scripts/check_op_page_coverage.py` → `OK — 54 op pages match catalogue`
- ✅ Single docs() commit per CLAUDE.md TDD §Note 4 doc-only-plan exemption

## Threat Flags

None. Plan 06 is doc-only polish — no new network surface, auth paths, file
access patterns, or schema changes at trust boundaries are introduced. The
single threat from the plan's `<threat_model>` (T-13.0-06-01: operator-catalog
information disclosure) is `accept` per CONTEXT D-02 (operator pages are
SEO/docs targets — intentional public surface).

## Known Stubs

None in scope. The 65 TODO markers Plan 05 sowed in the 13 core+sketch op
stubs are all eliminated by this plan. The remaining 205 TODO markers
(point-ordinal/ + recency/ + decay/ + velocity/ + buffer-geo/) are owned
by Plans 07-11 and are NOT in this plan's goal scope.

The forward-referenced `docs/pipeline-dsl/compilation-rules.md` link target
does not yet exist (Plan 13.0-12 creates it). This is intentional — the
cross-link is a contract for the page that's coming, not a broken link in
the steady-state docs site.

## Deviations from Plan

None — plan executed exactly as written. The plan's `<action>` blocks
specified per-op content guidance (Description / Parameters / Returns /
Complexity / Examples / Edge cases / See also); this executor wrote the
prose directly into each Plan 05 stub via the Write tool, replacing the
TODO markers with concrete content. All acceptance criteria verified
pre-commit + post-commit.

The plan's per-op content guidance for `bv.sum` mentioned the boolean-sum
trick FORBIDDEN ruling explicitly — documented in Edge cases on bv.sum.md
with the cross-link to the forthcoming pipeline-dsl/compilation-rules.md
two-stage pattern, exactly as specified.

The RESEARCH §3 worked `quantile.md` example (lines 538-688) was used
verbatim as the structural template for sketch/quantile.md, adapted to:
(1) preserve the Plan 05 stub's existing ADR-002 deprecation note format,
(2) align the "Wire" section to point at the existing
`examples/wire/register-fraud-team.request.json` fixture (which uses
`quantile` op-name post-13.0-04), and (3) remove the forward-reference to
`examples/wire/register-quantile-pipeline.request.json` (that fixture file
does not exist; the fraud-team fixture covers the same shape).

## Self-Check: PASSED

- ✅ FOUND: docs/operators/core/count.md (136 LOC, 0 TODOs)
- ✅ FOUND: docs/operators/core/sum.md (134 LOC, 0 TODOs, FORBIDDEN inline boolean-sum documented)
- ✅ FOUND: docs/operators/core/mean.md (137 LOC, ADR-002 cited)
- ✅ FOUND: docs/operators/core/min.md (136 LOC, 0 TODOs)
- ✅ FOUND: docs/operators/core/max.md (134 LOC, 0 TODOs)
- ✅ FOUND: docs/operators/core/var.md (139 LOC, ADR-002 cited)
- ✅ FOUND: docs/operators/core/std.md (139 LOC, ADR-002 cited)
- ✅ FOUND: docs/operators/core/ratio.md (134 LOC, 0 TODOs)
- ✅ FOUND: docs/operators/sketch/n_unique.md (150 LOC, ADR-002 cited, HLL documented)
- ✅ FOUND: docs/operators/sketch/quantile.md (152 LOC, ADR-002 cited, DDSketch documented)
- ✅ FOUND: docs/operators/sketch/top_k.md (157 LOC, V0-MEM-GOV-02 cited)
- ✅ FOUND: docs/operators/sketch/bloom_member.md (148 LOC, lifetime-only invariant documented)
- ✅ FOUND: docs/operators/sketch/entropy.md (146 LOC, V0-MEM-GOV-02 cited)
- ✅ FOUND: docs/operators/core/index.md (24 LOC, 8 op links)
- ✅ FOUND: docs/operators/sketch/index.md (20 LOC, 5 op links)
- ✅ FOUND: commit 60be0a9 (`docs(13.0-06): polish 13 op pages — core (8) + sketch (5) + 2 family index pages`)
- ✅ FOUND-NOT-MODIFIED: docs/operators/cost-class.md (NOT in commit; last touched 27 Apr by Phase 19.2)
- ✅ FOUND-NOT-MODIFIED: STATE.md / ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md (NOT in commit)
- ✅ FOUND-PASSING: scripts/check_op_page_coverage.py exits 0 post-commit (54-page invariant preserved)
