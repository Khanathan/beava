---
phase: 13.0-design-contract-spec-docs
plan: 13
subsystem: docs
tags: [concepts, architecture, v0, mio, wal, snapshot, memory-budget, observability, embed, lifetime-aggregation, processing-time-only, events-vs-tables, single-thread-apply, adr-001, v0-mem-gov]

# Dependency graph
requires:
  - phase: 13.0-design-contract-spec-docs
    plan: 02
    provides: docs/wire-spec.md — wire-level OP_PUSH ack semantics + ack_lsn cited in architecture/wal-snapshot.md SyncMode section + architecture/single-thread-apply.md apply-loop step 6
  - phase: 13.0-design-contract-spec-docs
    plan: 04
    provides: docs/sdk-api/{python,shared}.md — App.__init__ + transport selection (URL scheme tcp/http/embed) cited in concepts/embed-mode.md cross-references
  - phase: 13.0-design-contract-spec-docs
    plan: 12
    provides: docs/pipeline-dsl/compilation-rules.md (681 LOC) — window= kwarg semantics + @bv.table aggregation-output function form per ADR-001 + ambiguity matrix; cited in concepts/{events-vs-tables,lifetime-aggregation,processing-time-only}.md cross-references
  - phase: 13.0-design-contract-spec-docs
    plan: 12
    provides: docs/error-codes.md (49 alphabetical codes) — unbounded_op_in_lifetime_mode + bv_table_class_form_not_supported + aggregation_on_table_not_supported + event_time_not_supported_in_v0 + unknown_field_event_time_v0 + unknown_field_tolerate_delay_v0 + joins_not_supported + feature_removed_no_joins_v0 + feature_removed_no_unions_v0 + BinaryNotFoundError; cited in 8 of 9 docs
  - phase: 13.0-design-contract-spec-docs
    plan: 12
    provides: docs/schema-evolution.md — JSON-prelude shim layered validation pipeline cited in concepts/lifetime-aggregation.md (V0-MEM-GOV-02 enforcement section)
  - phase: 13.0-design-contract-spec-docs
    plan: 01
    provides: ADR-001 (@bv.table aggregation-output partial overturn) — cited 4 times in concepts/events-vs-tables.md ("What @bv.table is NOT in v0" section + "function-form" callouts + cross-references)
  - phase: 12.6-v0-surface-reduction
    provides: phase12_6_mio_only_dataplane.rs CI architectural test + phase12_6_legacy_axum_killed.rs companion + JSON-prelude shim pattern; cited in architecture/mio-data-plane.md (3 occurrences) + architecture/single-thread-apply.md
  - phase: 12.7-table-strip
    provides: FORMAT_VERSION = 1 reset (Plan 12.7-05 D-01 hard rip) + RecordType variants 0x01/0x02 only; SNAPSHOT_FORMAT_VERSION + SNAPSHOT_BODY_FORMAT_VERSION = 1; cited in architecture/wal-snapshot.md
  - phase: 12.8-memory-governance
    provides: V0-MEM-GOV-01 (cold_after= TTL) + V0-MEM-GOV-02 (lifetime aggregation contract + pre_check_unbounded_op_in_lifetime_mode shim + lifetime_bound_for_op_str classification) + V0-MEM-GOV-03 (per-event bucket reclaim) + 5 Prometheus metric families (beava_cold_entity_evictions_total / beava_lifetime_op_cap_hit_total / beava_entity_count_resident / beava_bucket_reclaim_total / beava_bytes_per_entity_p99); cited in architecture/memory-budget.md + architecture/observability.md + concepts/lifetime-aggregation.md
  - phase: 12.9-aggop-memory-boxing
    provides: size_of::<AggOp>() = 80 B post-boxing of 7 fat variants + crates/beava-core/tests/per_entity_size_dump.rs::aggop_size_within_cap CI tripwire + ~6 KB/entity weighted-avg fraud-team verified number; cited in architecture/memory-budget.md (12 occurrences of aggop_size_within_cap or Phase 12.9)
provides:
  - 9 concept + architecture docs totaling 1,583 LOC under docs/concepts/ + docs/architecture/
  - docs/concepts/events-vs-tables.md (177 LOC) — @bv.event vs @bv.table conceptual model per ADR-001 partial overturn (function-form aggregation-output decorator only; no app.upsert/delete/retract; no MVCC; no class-form); push vs read semantics; lifecycle; memory implications cross-link to memory-budget.md; ADR-001 cited 4 times
  - docs/concepts/embed-mode.md (144 LOC) — bv.App() no-URL embed mode; 4-step binary discovery (BEAVA_BINARY env → PATH → walk-upward target/debug/beava → BinaryNotFoundError) per python/beava/_embed.py::discover_binary; subprocess lifecycle (spawn / ready signal / SIGTERM teardown); when to use vs not use; worked example
  - docs/concepts/lifetime-aggregation.md (168 LOC) — window= omitted = lifetime mode; V0-MEM-GOV-02 contract (every lifetime op declares finite per-entity ceiling at register-time); 4-class memory bound classification table (O1 / BoundedSketch / BoundedByRequiredKwarg / BoundedByConfig with example ops); JSON-prelude shim pre_check_unbounded_op_in_lifetime_mode + structured error code unbounded_op_in_lifetime_mode; BEAVA_MEMORY_GOV_ENFORCE env-gate default-ON
  - docs/concepts/processing-time-only.md (131 LOC) — server now_ms() only; no event-time / no watermarks / no joins / no PIT per project_redis_shaped_no_event_time_ever (locked 2026-04-30); 6 wire-level/decorator rejections (event_time_ms field / event_time_field= kwarg / tolerate_delay_ms / joins / unions); 3 reasons (operational simplicity / Redis mental model parity / eliminates correctness bugs); user implications (push when events happen; replay reflects current now_ms; v0.1+ historical extraction engine for event-time semantics)
  - docs/architecture/single-thread-apply.md (151 LOC) — single OS thread for apply per project_no_sharded_apply (locked 2026-04-26; Phase 13.3 lockless-apply REJECTED); apply loop responsibilities (mio poll / parse / validate / apply / WAL append / reply); 6 explicit non-responsibilities (no async, no locks on hot path, no cross-entity coordination, no fsync on apply thread, no snapshot serialization on apply thread); auxiliary threads table (Apply / IoPool / WAL writer / Snapshot / Admin sidecar / Recovery); horizontal scale = Redis-cluster pattern; per-instance ceiling 109,895 EPS post-12.9 fraud-team M4
  - docs/architecture/mio-data-plane.md (168 LOC) — mio sole data-plane runtime per project_phase18_no_dual_runtime (locked 2026-04-23); 3 reasons (hand-rolled control / no async lifetime gymnastics / predictable performance); 6-step per-tick lifecycle (poll / distribute_reads / join / apply / distribute_writes / join); single dispatch entry point apply_shard.rs::dispatch_one (the only data-plane caller of apply_event_to_aggregations besides recovery.rs); admin sidecar tokio+axum on cfg.admin_addr serves /health + /ready + /metrics + /registry only; CI enforced by phase12_6_mio_only_dataplane.rs + phase12_6_legacy_axum_killed.rs (both run on every PR via cargo test --workspace)
  - docs/architecture/wal-snapshot.md (210 LOC) — Redis RDB+AOF pattern; FORMAT_VERSION = 1 (post-12.7 RESET; pre-12.7 dev WALs fail at version-byte check); WAL frame [u32 length][u32 crc32c][u64 lsn][u8 record_type][payload] + MAGIC b"BEAVAWAL"; RecordType variants table (0x01 Event + 0x02 RegistryBump + deleted 0x03/0x04/0x05 Table*); SNAPSHOT_FORMAT_VERSION + SNAPSHOT_BODY_FORMAT_VERSION = 1; recovery flow (snapshot → WAL replay from LSN N+1 → mark ready); SyncMode::Periodic acks=1 default group-commit fsync 1-5ms or 1MB ~15× EPS lift; SyncMode::PerEvent acks=all (OP_PUSH_SYNC v0.1+); 4-watermark LSN discipline (committed/written/synced/acked) per Phase 18 design; 3-buffer state machine 16 MiB × 3 buffers
  - docs/architecture/memory-budget.md (230 LOC) — VERIFIED Phase 12.9 numbers (size_of::<AggOp>() = 80 B post-boxing of 7 fat variants; -87% / 7.5× shrink; user_id 78-feature entity 46.8 KB → 6.2 KB; fraud-team weighted-avg ~22 KB → ~6 KB); CI tripwire crates/beava-core/tests/per_entity_size_dump.rs::aggop_size_within_cap with const AGGOP_SIZE_CAP_BYTES = 80; per-entity memory math table (30 × 80 B AggOp slots = 2.4 KB + heap + overhead = 3-7 KB total); capacity calc (7 KB × 100M entities = ~700 GB fits 1 TB NVMe); 3 ways to blow the budget (unbounded lifetime / cold accumulation / high-cardinality sketches); V0-MEM-GOV-01/02/03 invariants in full (cold_after= TTL + lifetime register-time contract + per-event bucket reclaim) with CI tests + metrics; what's NOT in budget (registry / WAL ring / listener buffers / snapshot working memory)
  - docs/architecture/observability.md (204 LOC) — admin sidecar 4 endpoints (/health liveness + /ready readiness 503-during-recovery + /metrics Prometheus + /registry); X-Runtime: tokio (admin) vs hand-rolled (data plane) middleware tag; Phase 13-01 metrics (beava_register_total + beava_push_total + beava_push_latency_seconds + beava_get_latency_seconds); 5 Phase 12.8 memory-governance metric families (beava_cold_entity_evictions_total + beava_lifetime_op_cap_hit_total + beava_entity_count_resident + beava_bucket_reclaim_total + beava_bytes_per_entity_p99) with V0-MEM-GOV invariant cross-links; WAL/snapshot metrics + four-watermark LSN gauges; structured JSON logs at INFO/WARN/ERROR; X-Trace-Id propagation
  - Single docs(13.0-13) commit (2dce656) — 9 files added, 1,583 insertions, 0 deletions per CLAUDE.md TDD §Note 4 doc-only-plan exemption
  - Cross-link integrity verified: 0 broken markdown links across all 9 docs (post-fix; initial Python-resolved cross-link script flagged 2 forward-references which were rewritten to plain-text "(forthcoming, owned by Plan 13.0-NN)" callouts)
affects:
  - 13.0-14 (vertical examples + quickstart): Plan 14 owns docs/quickstart.md; Plan 13 forward-references it as "(forthcoming, Plan 13.0-14)" plain-text callout in concepts/embed-mode.md cross-references
  - 13.0-15 (closure plan): closure plan owns concepts/global-aggregation.md per ADR-003 scope amendment 2026-05-03; Plan 13 forward-references it as "(forthcoming, owned by Plan 13.0-15 closure per ADR-003)" plain-text callout in concepts/events-vs-tables.md cross-references; closure plan also runs link-check + per-doc verification grep loops which should PASS by construction post-this-commit
  - 13.4 (engine prep): consumes architecture/mio-data-plane.md as the contract for keeping the apply_shard.rs::dispatch_one entry point sole on the data plane (architectural test phase12_6_mio_only_dataplane.rs already enforces); consumes architecture/wal-snapshot.md as the contract for FORMAT_VERSION/SNAPSHOT_FORMAT_VERSION staying = 1 (no schema bump in 13.4 unless explicitly justified); consumes architecture/single-thread-apply.md as the contract for not introducing apply sharding; consumes architecture/memory-budget.md as the contract for size_of::<AggOp>() ≤ 80 B (already enforced by aggop_size_within_cap CI tripwire)
  - 13.5 (Python SDK + bench): consumes concepts/embed-mode.md as the contract for bv.App() no-URL embed mode binary discovery; consumes concepts/events-vs-tables.md + concepts/lifetime-aggregation.md + concepts/processing-time-only.md as user-facing tone reference for Python SDK docstrings + error messages
  - 13.6 (TS + Go SDKs): consumes concepts/{events-vs-tables,lifetime-aggregation,processing-time-only}.md for SDK-surface parity (cross-language semantics); SDK porters reference these docs when writing TS/Go-side validation that mirrors Python's
  - 13.7 (docs site): consumes all 9 docs as rendered content for beava.dev "Concepts" + "Architecture" sections; per project_beava_website_ia (locked 2026-04-23) target user Priya (YC fintech CTO) reads these alongside operator pages to evaluate beava for production fraud workloads

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Locked-architectural-commitment citation pattern (all 9 docs): every doc cites the corresponding CLAUDE.md § Invariant block + the underlying memory file (project_phase18_no_dual_runtime / project_redis_shaped_no_event_time_ever / project_no_sharded_apply / project_v0_events_only_scope / project_phase18_wal_architecture). Establishes the doc-as-rationale-trail pattern; future contributors who want to overturn an invariant follow the citation chain to the memory file + see the locked-with-explicit-user-override-and-new-ADR escape hatch."
    - "VERIFIED-numbers citation pattern (architecture/memory-budget.md): every load-bearing memory number cites a CI test + a Phase SUMMARY + the source file. size_of::<AggOp>() = 80 B cites crates/beava-core/tests/per_entity_size_dump.rs::aggop_size_within_cap + .planning/phases/12.9-aggop-memory-boxing/12.9-SUMMARY.md + crates/beava-core/src/agg_op.rs. Establishes the verifiable-claims pattern; reviewers can independently verify every number rather than trusting prose."
    - "Cross-doc cross-reference pattern (all 9 docs): every doc closes with a Cross-references H2 section listing every other relevant doc + ADR + memory + CI test + REQUIREMENTS.md anchor. Establishes the bidirectional-cross-link pattern with explicit citations to ~/.claude/projects/-Users-petrpan26-work-tally/memory/*.md files for the 4 architecturally-locked memories. memory-budget.md alone has 11 cross-references; observability.md has 6."
    - "Forward-reference plain-text callout pattern (concepts/{embed-mode,events-vs-tables}.md): forward-references to docs not yet authored (docs/quickstart.md owned by Plan 13.0-14; docs/concepts/global-aggregation.md owned by Plan 13.0-15 closure per ADR-003 scope amendment) use plain-text 'docs/<path> (forthcoming, owned by Plan NN)' callouts instead of broken markdown links. Cross-link verification script returns 0 broken links. Establishes the no-broken-forward-link pattern; CI link checkers in Phase 13.7 (docs site) won't flag intentional forward references."
    - "Wire-level rejection envelope pattern (concepts/processing-time-only.md): the doc enumerates the 6 wire-level + decorator-level rejections (event_time_ms field / event_time_field= kwarg / tolerate_delay_ms / joins / unions / @bv.table class form) with their structured error codes (event_time_not_supported_in_v0 / unknown_field_event_time_v0 / unknown_field_tolerate_delay_v0 / joins_not_supported / feature_removed_no_joins_v0 / feature_removed_no_unions_v0 / bv_table_class_form_not_supported). Establishes the locked-architectural-commitment-surface-via-error-codes pattern; users discover the lock via the error code + can grep docs/error-codes.md for the recovery action."
    - "Auxiliary-threads table pattern (architecture/single-thread-apply.md): the apply-thread-vs-everything-else separation is documented as a 6-row table (Apply / IoPool worker / WAL writer / Snapshot / Admin sidecar / Recovery) with Role + Lives-in columns. Establishes the explicit-thread-boundary-documentation pattern; future contributors adding a new thread know where it goes (and that it's NOT another data-plane apply caller per the architectural test)."
    - "Per-entity memory math walkthrough pattern (architecture/memory-budget.md): the 30-feature-pack memory math is documented as a 5-row table (30 × 80 B AggOp slots / heap state for boxed variants / heap state for unboxed bounded ops / entity-key overhead / HashMap bucket overhead) with Bytes column + Total row. Establishes the per-component-memory-attribution pattern; reviewers can independently audit the 7 KB / entity claim by summing the rows."

# Files
key-files:
  created:
    - .planning/phases/13.0-design-contract-spec-docs/13.0-13-SUMMARY.md (this file)
    - docs/concepts/events-vs-tables.md (177 LOC) — @bv.event vs @bv.table conceptual model per ADR-001
    - docs/concepts/embed-mode.md (144 LOC) — bv.App() no-URL embed mode + 4-step binary discovery
    - docs/concepts/lifetime-aggregation.md (168 LOC) — V0-MEM-GOV-02 lifetime aggregation register-time contract
    - docs/concepts/processing-time-only.md (131 LOC) — project_redis_shaped_no_event_time_ever
    - docs/architecture/single-thread-apply.md (151 LOC) — project_no_sharded_apply single OS thread for apply
    - docs/architecture/mio-data-plane.md (168 LOC) — project_phase18_no_dual_runtime mio sole data plane
    - docs/architecture/wal-snapshot.md (210 LOC) — post-12.7 FORMAT_VERSION=1 + recovery flow + SyncMode
    - docs/architecture/memory-budget.md (230 LOC) — VERIFIED Phase 12.9 numbers + V0-MEM-GOV-01/02/03
    - docs/architecture/observability.md (204 LOC) — admin sidecar 4 endpoints + 5 Phase 12.8 metric families
  modified: []

# Decisions
decisions:
  - "All 9 docs authored per plan spec without scope expansion. Section structure follows the plan's H1/H2 outline verbatim; no extra sections added; no sections dropped. Each doc weighs in at the upper end of the plan's LOC target (concepts: 131-177 vs 80-150 minimum; architecture: 151-230 vs 80-150 minimum) — extra LOC came from worked examples, full table fills, and exhaustive cross-reference sections."
  - "memory-budget.md cites VERIFIED Phase 12.9 numbers as the load-bearing claim of the v0 memory story. Cited 12 times across the doc (size_of, aggop_size_within_cap, Phase 12.9 SUMMARY cross-link). Per-entity math walkthrough table sums to 3-7 KB explicitly so reviewers can audit the ~7 KB / entity claim. Cited as the canonical evidence trail for CLAUDE.md § Constraints memory line."
  - "events-vs-tables.md scoped @bv.table strictly to ADR-001 (function-form aggregation-output decorator ONLY). 'What @bv.table is NOT in v0' enumerates the 6 absent surfaces: NOT mutable upserted store / NOT tombstoned delete store / NOT temporal MVCC table / NOT retraction-aware aggregation / NOT class-form decorator / NOT aggregation source. Cross-links to error-codes.md for the structured error codes (bv_table_class_form_not_supported / aggregation_on_table_not_supported / unsupported_node_kind)."
  - "processing-time-only.md cites project_redis_shaped_no_event_time_ever as the locked architectural commitment (locked 2026-04-30). The doc is forward-looking — explicitly names v0.1+ historical extraction engine as the path for users who need event-time semantics, and points to .planning/ideas/v0.1-historical-extraction-engine.md. Establishes the 'forever-rejected-in-v0-but-here-is-the-v0.1+-path' pattern for documenting permanent architectural commitments without leaving users orphaned."
  - "lifetime-aggregation.md surfaces V0-MEM-GOV-02 in full — the 4-class memory bound classification (O1 / BoundedSketch / BoundedByRequiredKwarg / BoundedByConfig) is documented as a 7-row table covering every operator family. The JSON-prelude shim pattern (pre_check_unbounded_op_in_lifetime_mode) is explained inline so readers understand WHY the rejection survives code refactors. Cited from concepts/events-vs-tables.md memory implications section + architecture/memory-budget.md V0-MEM-GOV-02 invariant section."
  - "single-thread-apply.md + mio-data-plane.md split per the plan's separate-file structure. Both reference each other heavily (mio-data-plane.md cites single-thread-apply.md for 'the apply thread that owns dispatch'; single-thread-apply.md cites mio-data-plane.md for 'the architectural test that enforces the single dispatch entry point'). The split keeps single-thread-apply.md focused on the threading model + horizontal-scale story while mio-data-plane.md focuses on the runtime + admin-sidecar separation."
  - "Forward references to docs/quickstart.md (Plan 13.0-14) + docs/concepts/global-aggregation.md (Plan 13.0-15 closure scope amendment per ADR-003) rewritten as plain-text callouts after initial cross-link verification flagged them as broken markdown links. Format: 'docs/<path> (forthcoming, owned by Plan 13.0-NN per <reason>)'. CI link checkers in Phase 13.7 (docs site) won't flag these as broken because they're plain text, not markdown links. Decision rationale: keep the forward-reference info (so users discover the upcoming docs) without breaking the link-check gate."
  - "Per CLAUDE.md TDD §Note 4 doc-only-plan exemption: single docs(13.0-13) commit (2dce656) covering all 9 docs — no red-test commit required since the 'test' (acceptance grep loops + per-doc LOC checks + cross-link verification) is mechanical and passes by construction post-commit. SUMMARY.md committed separately per Plan 12's pattern."
  - "Did NOT touch STATE.md / ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md per the plan's sequential_execution constraint. Closure plan (13.0-15) owns those updates."
  - "Did NOT create docs/concepts/global-aggregation.md per the plan's critical_constraints (Plan 13.0-15 closure owns it per ADR-003 scope amendment 2026-05-03). Forward-referenced as plain-text callout in concepts/events-vs-tables.md only."

# Metrics
metrics:
  duration_min: 22
  completed: 2026-05-03T22:45:00Z
  tasks_completed: 2
  loc_added: 1583
  loc_removed: 0
  files_added: 9
  files_modified: 0
  commits: 1
---

# Phase 13.0 Plan 13: 9 concept + architecture docs Summary

## One-liner

Authored 9 concept and architecture docs (1,583 LOC across docs/concepts/
and docs/architecture/) covering the v0 conceptual model + every locked
architectural commitment with verified Phase 12.9 memory numbers (size_of
AggOp = 80 B + ~6 KB/entity fraud-team weighted-avg + 7 KB × 100M entities
~700 GB capacity math) cross-linked to CLAUDE.md § Invariant blocks +
ADR-001 + V0-MEM-GOV-01/02/03 + 5 Phase 12.8 Prometheus metric families
+ phase12_6_mio_only_dataplane.rs CI gate. Single docs(13.0-13) commit
(2dce656) per CLAUDE.md TDD §Note 4 doc-only-plan exemption.

## Performance

- **Duration:** ~22 min
- **Started:** 2026-05-03T22:23:00Z
- **Completed:** 2026-05-03T22:45:00Z
- **Tasks:** 2 / 2 completed
- **Files added:** 9
- **Files modified:** 0
- **LOC added:** 1,583

## Accomplishments

- 4 concept docs in docs/concepts/ covering v0 user-facing conceptual model
- 5 architecture docs in docs/architecture/ covering v0 internals + locked invariants
- All 9 docs cross-link to relevant CLAUDE.md § Invariant blocks + ADRs + memories + REQUIREMENTS.md anchors
- memory-budget.md cites VERIFIED Phase 12.9 numbers (80 B size_of AggOp + ~6 KB/entity weighted-avg + aggop_size_within_cap CI tripwire) 12 times
- 0 broken markdown links across all 9 docs (verified via Python-based cross-link resolver)
- Single docs(13.0-13) commit (2dce656) per CLAUDE.md TDD §Note 4 doc-only-plan exemption

## What landed

**9 spec docs authored** — single `docs(13.0-13)` commit (`2dce656`),
1,583 LOC total, 0 deletions:

### Concept docs (4)

- **`docs/concepts/events-vs-tables.md`** (177 LOC) — Conceptual model
  of `@bv.event` vs `@bv.table` per ADR-001 partial overturn.
  - Sections: Overview / `@bv.event` declares an event source / `@bv.table`
    declares an aggregation output / What `@bv.table` is NOT in v0 (6
    absent surfaces) / When to use which / Push vs read semantics /
    Memory implications / Cross-references.
  - **ADR-001 cited 4 times** (function-form callouts + the
    "What @bv.table is NOT in v0" rationale section + cross-references).
  - Cross-links to CLAUDE.md § Events-Only Invariant + pipeline-dsl/
    overview.md + pipeline-dsl/compilation-rules.md + error-codes.md +
    .planning/ideas/v0.1-deferrals.md.

- **`docs/concepts/embed-mode.md`** (144 LOC) — `bv.App()` no-URL embed
  mode + binary discovery.
  - Sections: How it works / Binary discovery (4-step) / Lifecycle /
    When to use embed mode / When NOT to use embed mode / Worked example
    / Cross-references.
  - **4-step binary discovery cited explicitly** (BEAVA_BINARY env →
    PATH → walk-upward target/debug/beava → BinaryNotFoundError) per
    `python/beava/_embed.py::discover_binary`.
  - Cross-links to sdk-api/python.md + sdk-api/shared.md + error-codes.md
    + forthcoming docs/quickstart.md (Plan 13.0-14 plain-text callout).

- **`docs/concepts/lifetime-aggregation.md`** (168 LOC) — Lifetime mode +
  V0-MEM-GOV-02 register-time enforcement.
  - Sections: What lifetime mode means / When to use lifetime mode /
    V0-MEM-GOV-02 contract / Memory bound classifications (4-class table)
    / Register-time enforcement / What this catches / Memory budget
    connection (V0-MEM-GOV-01/02/03 trio) / Cross-references.
  - **V0-MEM-GOV cited 8 times** (V0-MEM-GOV-02 contract + invariant
    cross-links to architecture/memory-budget.md).
  - **JSON-prelude shim pattern** documented in full (the same pattern
    used by Phases 12.6 / 12.7 / 12.8 to keep structured error codes
    durable across code refactors).
  - **`BEAVA_MEMORY_GOV_ENFORCE` env-gate** documented as default-ON;
    escape hatch documented as `BEAVA_MEMORY_GOV_ENFORCE=0` for ops
    emergencies (NOT for production).

- **`docs/concepts/processing-time-only.md`** (131 LOC) — Server `now_ms()`
  only; no event-time ever per `project_redis_shaped_no_event_time_ever`.
  - Sections: What "no event-time ever" means / Why no event-time (3
    reasons) / Implications for users / What you still get /
    Cross-references.
  - **`project_redis_shaped_no_event_time_ever` cited explicitly** as
    the locked architectural commitment (locked 2026-04-30).
  - **6 wire-level / decorator-level rejections enumerated** with their
    structured error codes (event_time_ms field / event_time_field= kwarg
    / tolerate_delay_ms / joins / unions).
  - Forward-looking — explicitly names v0.1+ historical extraction
    engine path for users who need event-time semantics.

### Architecture docs (5)

- **`docs/architecture/single-thread-apply.md`** (151 LOC) — Single OS
  thread for apply per `project_no_sharded_apply`.
  - Sections: Why single-thread / Apply loop responsibilities (6-step) /
    What the apply loop does NOT do / Auxiliary threads (6-row table) /
    Implications for users / Cross-references.
  - **`project_no_sharded_apply` cited explicitly** (locked 2026-04-26;
    Phase 13.3 lockless-apply REJECTED).
  - **Per-instance ceiling cited** as 109,895 EPS post-12.9 fraud-team
    M4 (median of 3 runs).
  - Horizontal scale = Redis-cluster pattern explicitly documented as
    the v0 scaling story.

- **`docs/architecture/mio-data-plane.md`** (168 LOC) — mio sole
  data-plane runtime per `project_phase18_no_dual_runtime`.
  - Sections: Why mio (not tokio) / What mio does (6-step per-tick
    lifecycle) / The single dispatch entry point / Admin sidecar (tokio
    + axum) / How enforced (CI tests) / Implications for contributors /
    Cross-references.
  - **`phase12_6_mio_only_dataplane.rs` cited 3 times** as the CI
    architectural test enforcing the invariant.
  - **`project_phase18_no_dual_runtime` cited explicitly** as the
    locked-2026-04-23 commitment.
  - **Single dispatch entry point** (`apply_shard.rs::dispatch_one`)
    documented as the only data-plane caller of
    `apply_event_to_aggregations` besides `recovery.rs`.

- **`docs/architecture/wal-snapshot.md`** (210 LOC) — Redis RDB+AOF
  pattern + post-12.7 FORMAT_VERSION=1 reset.
  - Sections: WAL format (post-12.7 RESET) / Snapshot format (post-12.7
    RESET) / Recovery on boot / Sync modes (Periodic acks=1 default +
    PerEvent acks=all) / Four-watermark LSN discipline / Snapshot
    truncation / Implications for users / Cross-references.
  - **`FORMAT_VERSION = 1`** cited 5 times with Phase 12.7-05 D-01 hard
    rip RESET context (no migration shim; operators clear
    `.beava/wal` + `.beava/snapshots` before booting).
  - **Pre-12.7 dev WALs/snapshots fail at version-byte check** — explicit
    operator guidance.
  - **3-buffer state machine** + **four-watermark LSN discipline** per
    `project_phase18_wal_architecture` cited.

- **`docs/architecture/memory-budget.md`** (230 LOC) — VERIFIED Phase
  12.9 numbers + V0-MEM-GOV-01/02/03 invariants.
  - Sections: Verified Phase 12.9 numbers / CI tripwire
    `aggop_size_within_cap` / Per-entity memory math (5-row table) /
    Capacity calculation / What CAN blow the budget (3 failure modes) /
    V0-MEM-GOV-01 cold-entity TTL / V0-MEM-GOV-02 lifetime contract /
    V0-MEM-GOV-03 per-event bucket reclaim / `/metrics` observable (5
    Phase 12.8 families) / What's NOT in budget / Cross-references.
  - **`size_of::<AggOp>() = 80 B` cited 3 times** with the cap context
    + the CI tripwire test code excerpt + the underlying constant.
  - **`aggop_size_within_cap` CI tripwire cited 12 times** across the
    doc (Phase 12.9 SUMMARY cross-link + per-entity math + V0-MEM-GOV-02
    invariant).
  - **7 KB / entity capacity math cited 8 times** (post-Phase-12.9
    weighted-avg ~6 KB headroom + 7 KB × 100M entities = ~700 GB +
    fits 1 TB NVMe).
  - **V0-MEM-GOV-01 / -02 / -03 each documented in full** with CI test
    + Prometheus metric + recovery action.

- **`docs/architecture/observability.md`** (204 LOC) — Admin sidecar 4
  endpoints + 5 Phase 12.8 metric families + WAL/snapshot metrics.
  - Sections: Overview / `/health` / `/ready` / `/metrics` (Prometheus
    exposition + 4 metric family categories) / `/registry` / Logs /
    `X-Trace-Id` propagation / Implementation notes / Cross-references.
  - **All 5 Phase 12.8 metric families cited**:
    `beava_cold_entity_evictions_total`, `beava_lifetime_op_cap_hit_total`,
    `beava_entity_count_resident`, `beava_bucket_reclaim_total`,
    `beava_bytes_per_entity_p99` (with the placeholder=7000 caveat per
    Phase 12.8 PLANNER-SURFACED CONCERN).
  - **`X-Runtime: tokio` (admin) vs `X-Runtime: hand-rolled` (data
    plane)** middleware tag documented as the runtime-identification
    contract.
  - Phase 13-01 metrics + WAL/snapshot metrics + runtime identity +
    op-specific metric families all enumerated.

## Verification

**All 9 docs satisfy plan acceptance criteria:**

| Doc | Required | Actual | Status |
|-----|----------|--------|--------|
| `docs/concepts/events-vs-tables.md` | ≥80 LOC, ADR-001 cite | 177 LOC; ADR-001 cited 4 times | PASS |
| `docs/concepts/embed-mode.md` | ≥80 LOC, BEAVA_BINARY/PATH/target/debug discovery markers | 144 LOC; all 4 markers found | PASS |
| `docs/concepts/lifetime-aggregation.md` | ≥80 LOC, V0-MEM-GOV cite | 168 LOC; V0-MEM-GOV cited 8 times | PASS |
| `docs/concepts/processing-time-only.md` | ≥80 LOC, project_redis_shaped_no_event_time_ever cite | 131 LOC; cited explicitly + "no event-time" framing | PASS |
| `docs/architecture/single-thread-apply.md` | ≥80 LOC, project_no_sharded_apply cite | 151 LOC; cited explicitly + Phase 13.3 REJECTED context | PASS |
| `docs/architecture/mio-data-plane.md` | ≥80 LOC, mio + phase12_6_mio_only_dataplane | 168 LOC; mio mentioned throughout; arch test cited 3 times | PASS |
| `docs/architecture/wal-snapshot.md` | ≥80 LOC, FORMAT_VERSION = 1 / Phase 12.7 cite | 210 LOC; cited 5 times | PASS |
| `docs/architecture/memory-budget.md` | ≥150 LOC, 80B size_of, aggop_size_within_cap, 7 KB capacity math, V0-MEM-GOV-01/02/03 | 230 LOC; 80 B/size_of cited 3 times; aggop_size_within_cap cited 12 times; 7 KB cited 8 times; all 3 V0-MEM-GOV anchors present | PASS |
| `docs/architecture/observability.md` | ≥80 LOC, all 5 Phase 12.8 metrics | 204 LOC; all 5 metric families cited | PASS |

**Cross-link verification:** 0 broken markdown links across all 9 docs
(via Python-based cross-link resolver). Two initial broken-link
candidates (`../quickstart.md` from embed-mode.md + `./global-aggregation.md`
from events-vs-tables.md) were rewritten as plain-text "(forthcoming,
owned by Plan 13.0-NN)" callouts to preserve the forward-reference info
without breaking the link gate.

**Single `docs(13.0-13)` commit:** `2dce656` per CLAUDE.md TDD §Note 4
doc-only-plan exemption. 9 files added, 1,583 insertions, 0 deletions.
No prohibited files (STATE.md / ROADMAP.md / CORRECTNESS-PATH.md /
CLAUDE.md) modified.

## Files Created/Modified

**Created (9 docs + this SUMMARY):**

- `docs/concepts/events-vs-tables.md` (177 LOC)
- `docs/concepts/embed-mode.md` (144 LOC)
- `docs/concepts/lifetime-aggregation.md` (168 LOC)
- `docs/concepts/processing-time-only.md` (131 LOC)
- `docs/architecture/single-thread-apply.md` (151 LOC)
- `docs/architecture/mio-data-plane.md` (168 LOC)
- `docs/architecture/wal-snapshot.md` (210 LOC)
- `docs/architecture/memory-budget.md` (230 LOC)
- `docs/architecture/observability.md` (204 LOC)
- `.planning/phases/13.0-design-contract-spec-docs/13.0-13-SUMMARY.md` (this file)

**Modified:** none.

## Decisions Made

See `decisions:` field in frontmatter for the 10 plan-execution
decisions in full. Highlights:

- All 9 docs authored per plan spec without scope expansion (verbatim
  H1/H2 outline).
- memory-budget.md cites VERIFIED Phase 12.9 numbers as the load-bearing
  claim; per-entity math walkthrough table sums to 3-7 KB explicitly
  for reviewer audit.
- events-vs-tables.md scoped @bv.table strictly to ADR-001 (function-
  form aggregation-output decorator ONLY); 6 absent surfaces enumerated.
- processing-time-only.md cites project_redis_shaped_no_event_time_ever
  + explicitly names v0.1+ historical extraction engine path for users
  who need event-time semantics.
- Forward references (docs/quickstart.md + docs/concepts/global-aggregation.md)
  rewritten as plain-text callouts to preserve link-check integrity.
- Did NOT touch STATE.md / ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md
  per plan's sequential_execution constraint.
- Did NOT create docs/concepts/global-aggregation.md (Plan 13.0-15
  closure owns it per ADR-003 scope amendment 2026-05-03).

## Deviations from Plan

**None.**

The plan as authored expected 2 tasks producing 9 docs + a single
docs(13.0-13) commit. That's exactly what landed. No Rule 1 / Rule 2 /
Rule 3 auto-fixes triggered.

The only minor adjustment was rewriting 2 forward-reference markdown
links as plain-text callouts after the Python-based cross-link
verification flagged them as broken — but this is well within the plan's
explicit "(forthcoming, owned by Plan 13.0-NN)" guidance for the
global-aggregation forward reference. Both forward references preserve
their information content + their target plan attribution.

## Self-Check

**Files created (verified existence):**

- `docs/concepts/events-vs-tables.md` — FOUND (177 LOC)
- `docs/concepts/embed-mode.md` — FOUND (144 LOC)
- `docs/concepts/lifetime-aggregation.md` — FOUND (168 LOC)
- `docs/concepts/processing-time-only.md` — FOUND (131 LOC)
- `docs/architecture/single-thread-apply.md` — FOUND (151 LOC)
- `docs/architecture/mio-data-plane.md` — FOUND (168 LOC)
- `docs/architecture/wal-snapshot.md` — FOUND (210 LOC)
- `docs/architecture/memory-budget.md` — FOUND (230 LOC)
- `docs/architecture/observability.md` — FOUND (204 LOC)
- `.planning/phases/13.0-design-contract-spec-docs/13.0-13-SUMMARY.md` — FOUND (this file)

**Commits (verified via `git log`):**

- `2dce656` `docs(13.0-13): 9 concept + architecture docs covering v0 conceptual model + locked architectural commitments` — FOUND on `v2/greenfield`

## Self-Check: PASSED
