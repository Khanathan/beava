---
phase: 13.0-design-contract-spec-docs
plan: 09
subsystem: docs
tags: [operators, catalog, polish, velocity, trend, rate-of-change, inter-arrival, burst, outlier, z-score, processing-time]

# Dependency graph
requires:
  - phase: 13.0-design-contract-spec-docs
    plan: 05
    provides: 54 op page stubs scaffolded under docs/operators/<family>/<op>.md with H1 + 9 mandatory H2 sections and TODO prose markers labeled by polish plan number; Plan 09 fills the 9 stubs labeled "Plan 13.0-09" (velocity family). The 9 ops in scope are rate_of_change / inter_arrival_stats / burst_count / delta_from_prev / trend / trend_residual / outlier_count / value_change_count / z_score — note that rate_of_change lives in velocity/ (NOT decay/) per RESEARCH §3 directory layout + §5 op classification, and z_score (REQUIREMENTS family AGG-Z-*) lives here per RESEARCH §3 grouping.
  - phase: 13.0-design-contract-spec-docs
    plan: 06
    provides: Per-op page template established (Signature / Description / Parameters / Returns / Complexity / Examples / Wire / Edge cases / See also — 9 H2 sections; 130-160 LOC range; cost-class cross-link; V0-MEM-GOV-02 citation pattern; cold-start convention; family index template) — Plan 09 inherits verbatim
  - phase: 13.0-design-contract-spec-docs
    plan: 07
    provides: Family index template (~25-50 LOC, summary table + key invariants + when-to-use-which guidance + see also) + processing-time citation pattern + cold-entity eviction cross-link convention — Plan 09 inherits verbatim and applies the processing-time citation to all 9 velocity ops (windowed dispatch bins by server now_ms() per project_redis_shaped_no_event_time_ever)
  - phase: 13.0-design-contract-spec-docs
    plan: 08
    provides: Decay-family index page (docs/operators/decay/index.md) cross-references Plan 09's velocity/rate_of_change.md page + the cross-family-note pattern (decay/index.md includes a "Note" block forwarding to velocity/rate_of_change.md). Plan 09 reciprocates by linking back to decay/ for ew_zscore (z_score sibling) and ewma (smoothing the underlying signal before differencing).
  - phase: 12.8-memory-governance
    provides: V0-MEM-GOV-02 lifetime-aggregation contract (velocity ops classified as O(1) lifetime-bound at register_validate.rs ~line 439-449) — cited in Complexity table for all 9 ops
  - phase: 19.2-operator-uniformity
    provides: docs/operators/cost-class.md (alive Phase 19.2 metadata, KEPT, NOT touched) — cross-linked from each polished page's Complexity section for CPU tier (8 of 9 velocity ops are Tier 1; outlier_count is the only Tier 2 due to per-event sqrt() in the sigma-threshold test)
provides:
  - 9 velocity/ op pages polished from Plan 05 stubs (rate_of_change, inter_arrival_stats, burst_count, delta_from_prev, trend, trend_residual, outlier_count, value_change_count, z_score) — each 146-163 LOC; TODO markers replaced with full Description / Parameters / Returns / Complexity / Examples / Edge cases / See also prose
  - docs/operators/velocity/index.md (49 LOC) — Velocity family overview + 9-op summary table + key invariants section + when-to-use-which guidance + cross-link to decay/ for ew_zscore companion + Note block clarifying that rate_of_change lives here (NOT decay/) per RESEARCH §3 + §5 and that z_score (REQUIREMENTS family AGG-Z-*) lives here per RESEARCH §3 grouping
  - 7 of 9 ops cite `window=` as REQUIRED (rate_of_change / inter_arrival_stats / burst_count / trend / trend_residual / outlier_count / value_change_count); delta_from_prev is **lifetime-only** (no window — diffs the most recent two values); z_score uses the SDK ergonomic alias `baseline_window=` for the same wire-form `params.window` field
  - burst_count cites BOTH `window=` AND `sub_window=` as REQUIRED; sub_window must be smaller than window; "forever" rejected for sub_window; structured error aggregation_invalid_sub_window if it reaches register_validate.rs
  - outlier_count cites `sigma=3.0` default + `MIN_BASELINE_N=5` warm-up convention; only Tier 2 op in the family due to per-event sqrt() for the sigma-threshold test
  - inter_arrival_stats documents v0 emits `mean_ms` only (Welford state already tracks m2 for stddev_ms / cv but wire shape stays scalar in v0; v0.1+ widens to {mean_ms, stddev_ms, cv} dict)
  - z_score cites `baseline_window=` as the SDK ergonomic alias making "the baseline against which the current event is scored" intent explicit; the wire-form params field is still named `"window"` for opcode consistency
  - Processing-time citations on all 9 ops: windowed dispatch bins by server now_ms() per project_redis_shaped_no_event_time_ever; late events (Δt ≤ 0) clamped to 0 (in inter_arrival_stats) or skipped (in rate_of_change) per established pattern
  - Cold-start return convention: 6 ops return null on empty stream (rate_of_change / inter_arrival_stats / delta_from_prev / trend / trend_residual / z_score) — typically null until n >= 2; 3 ops return 0 and only ever increment (burst_count / outlier_count / value_change_count)
  - Cold-entity eviction (V0-MEM-GOV-01) cross-linked on every velocity op (and the family index)
affects:
  - 13.0-10 / 11 (buffer-geo polish): inherits the per-page template, processing-time citation, cold-start convention, and family index template established by Plans 06 + 07 + 08 + 09
  - 13.0-12 (pipeline-dsl/compilation-rules.md): forward-referenced from every polished velocity page's See also section; the link target page does not yet exist (Plan 12 creates it)
  - 13.0-15 (closure plan): runs scripts/check_op_page_coverage.py + verifies remaining TODO markers across the catalogue; this plan's contribution: 0 TODOs in velocity/
  - 13.4 (engine prep): polished pages reference target post-13.4 wire shapes (verb-style routes, `kind=table` + derivation `output_kind="table"`) — 13.4 implements the engine to match. v0.1+ widening of inter_arrival_stats return shape to `{mean_ms, stddev_ms, cv}` dict is documented but not implemented; 13.4 keeps the scalar contract.
  - 13.5 (Python SDK + bench): polished pages document baseline_window= as the z_score SDK ergonomic alias for params.window — 13.5 must keep this alias rather than renaming. 13.5 also keeps inter_arrival_stats field-less (no positional argument permitted at SDK-helper-call time).
  - 13.6 (TS + Go SDKs): porters consume the polished pages for SDK-surface parity. The baseline_window= alias and field-less helpers (inter_arrival_stats, burst_count, value_change_count) are wire-form contracts they must respect.

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-op page template inherited from Plans 06/07/08 verbatim: H1 + one-line tagline + 9 H2 sections (Signature / Description / Parameters / Returns / Complexity / Examples / Wire / Edge cases / See also). Length 146-163 LOC across all 9 velocity ops; 8 are Tier 1 / 1 is Tier 2 (outlier_count). All 9 are O(1) memory bound."
    - "window-required pattern (7 of 9 ops): rate_of_change / inter_arrival_stats / burst_count / trend / trend_residual / outlier_count / value_change_count cite window= as REQUIRED in (1) Parameters row, (2) Description prose, (3) Edge cases (missing → ValueError; malformed → server-side fallback aggregation_invalid_window). Establishes the window-mandatory convention before downstream SDK porters in Plans 13.5/13.6."
    - "burst_count dual-window-required pattern: burst_count is the only velocity op requiring TWO duration kwargs (window= AND sub_window=). sub_window must be smaller than window; sub_window='forever' is rejected by _validate_sub_window at SDK call time. Documented per-op in Edge cases + Description (the 64-slot bucket ring). Structured error aggregation_invalid_sub_window if it reaches register_validate.rs."
    - "outlier_count Tier 2 isolation: outlier_count is the ONLY Tier 2 op in the velocity family — per-event sqrt() for the sigma-threshold test. Documented in Complexity (Tier 2 ~32 ns vs Tier 1 ~25 ns peers) + Description. Establishes that velocity-family CPU isn't uniformly Tier 1; SDK porters and benchmarks should not assume otherwise."
    - "outlier_count sigma=3.0 default + MIN_BASELINE_N=5 warm-up convention: the classic three-sigma rule is the default (tighten to 2.0 for ~5% tail; loosen to 4.0 for ~0.006% tail); the warm-up of 5 matching events before firing the outlier check avoids spurious early-stream increments. Both documented in Parameters + Edge cases per outlier_count.md so SDK porters in 13.5/13.6 keep the same defaults."
    - "z_score baseline_window= ergonomic-alias pattern: the SDK helper exposes baseline_window= (not window=) to make the 'baseline-against-which-the-current-event-is-scored' intent explicit; the wire-form params field is still named 'window' for opcode consistency. Documented in (1) Parameters table (named baseline_window), (2) Wire block (params.window — explicit Note block), (3) Edge cases (missing baseline_window= → ValueError). SDK porters in 13.6 (TS, Go) must keep this alias even though the wire is window=."
    - "inter_arrival_stats field-less + scalar return (v0): the helper is field-less by design (no field= kwarg — operates on arrival timestamps, not payload values); v0 emits mean_ms only despite the Welford state tracking m2 to derive stddev_ms / cv. Documented in (1) Parameters (no field row + 'No field= kwarg' note), (2) Returns (v0 scalar; v0.1+ widens to dict), (3) Edge cases (positional field argument → TypeError). Establishes both the field-less contract AND the v0/v0.1 widening boundary."
    - "delta_from_prev lifetime-only pattern: delta_from_prev is the ONLY velocity op WITHOUT a window= kwarg — it just diffs the two most recent matching values regardless of elapsed time. Documented in (1) Signature (no window= row), (2) Description (lifetime-only by design — pair with rate_of_change for the per-Δt rate), (3) Complexity (no Lifetime mode row — there is no other mode), (4) the family index summary table 'Required kwarg(s)' column shows '—'. Establishes that not every velocity op is windowed; the family is unified by behavioral / value-trajectory semantics, not by windowed-dispatch."
    - "Processing-time citation pattern (project_redis_shaped_no_event_time_ever): all 9 velocity ops state 'windowed dispatch bins by server now_ms() at apply' explicitly in Description. Late-event handling (Δt ≤ 0 → either clamped to 0 or update skipped depending on op) documented per op in Edge cases — this is the load-bearing edge case for replay determinism."
    - "Family index template (~50 LOC for velocity, slightly larger than decay's 43): H1 + one-line family overview + 5-column summary table (op | required kwarg(s) | returns | CPU tier | notes) + 'Key invariants' section (velocity-specific: processing-time-only, window= canonical kwarg name with delta_from_prev exception + z_score baseline_window= alias, burst_count dual-window required, outlier_count sigma=3.0 default + MIN_BASELINE_N=5 warm-up, lifetime mode allowed for all 9, cold-start returns null OR 0 per op, cold-entity eviction reference) + 'When to use which' guidance bullets + 'Note' block clarifying that rate_of_change lives here (NOT decay/) AND z_score lives here (REQUIREMENTS family AGG-Z-*) + See also section. Two cross-family note blocks (vs decay/index.md's one) because the velocity family has two ambiguous-classification ops worth disambiguating."
    - "Cross-family note pattern (reciprocal): velocity/index.md cross-references decay/ for ew_zscore as the drift-aware counterpart to velocity/z_score, and for ewma as the smoothed counterpart to the underlying signals fed into trend / rate_of_change. Mirrors decay/index.md's reciprocal cross-link to velocity/rate_of_change.md. Establishes the bidirectional cross-family-link pattern for any future related-but-separate-family op pair."

# Files
key-files:
  created:
    - docs/operators/velocity/index.md (49 LOC) — Velocity family overview + 9-op summary table + key invariants + when-to-use-which + dual rate_of_change / z_score classification notes
    - .planning/phases/13.0-design-contract-spec-docs/13.0-09-SUMMARY.md (this file)
  modified:
    - docs/operators/velocity/rate_of_change.md (~26 → 153 LOC; +127 LOC; full polish — Tier 1, O(1), window REQUIRED; the canonical Phase 9 velocity-family op per RESEARCH §5; lives here NOT decay/)
    - docs/operators/velocity/inter_arrival_stats.md (~26 → 150 LOC; +124 LOC; full polish — Tier 1, O(1), window REQUIRED; field-less by design; v0 emits mean_ms only — Welford state has m2 for v0.1+ stddev_ms / cv widening)
    - docs/operators/velocity/burst_count.md (~26 → 153 LOC; +127 LOC; full polish — Tier 1, O(1), window AND sub_window REQUIRED; field-less; 64-slot bucket ring; max-event-count-in-any-sub-window primitive)
    - docs/operators/velocity/delta_from_prev.md (~26 → 146 LOC; +120 LOC; full polish — Tier 1, O(1), lifetime-only — NO window= kwarg; absolute jump (no Δt denominator); pair with rate_of_change for the per-ms rate)
    - docs/operators/velocity/trend.md (~26 → 153 LOC; +127 LOC; full polish — Tier 1, O(1), window REQUIRED; online OLS slope of (now_ms, field); shares regression state with trend_residual)
    - docs/operators/velocity/trend_residual.md (~26 → 158 LOC; +132 LOC; full polish — Tier 1, O(1), window REQUIRED; latest value minus its trend-line prediction; shares state with trend)
    - docs/operators/velocity/outlier_count.md (~26 → 163 LOC; +137 LOC; full polish — Tier 2 (only one in family — per-event sqrt()), O(1), window REQUIRED; sigma=3.0 default + MIN_BASELINE_N=5 warm-up; counts events beyond sigma * stddev from rolling mean)
    - docs/operators/velocity/value_change_count.md (~26 → 158 LOC; +132 LOC; full polish — Tier 1, O(1), window REQUIRED; counts adjacent flips, NOT net distinct values; cheaper than count_distinct + 1 since no sketch state)
    - docs/operators/velocity/z_score.md (~26 → 159 LOC; +133 LOC; full polish — Tier 1, O(1), baseline_window REQUIRED (SDK ergonomic alias for wire-form params.window); current event's deviation against entity's running (mean, stddev) per Welford; null when stddev=0 from single observation)

# Decisions
decisions:
  - "Per-op page template inherited verbatim from Plans 06/07/08 (no structural changes to the H2 layout); polish only fills the 5 prose sections per op (Description / Parameters / Returns / Examples / Edge cases) plus enriches Complexity + See also where applicable. The Signature + Wire blocks were rewritten to reflect actual Python signatures (e.g. inter_arrival_stats is field-less keyword-only; z_score uses baseline_window=) rather than the Plan 05 placeholder `(...)`."
  - "8 of 9 ops are Tier 1 per cost-class.md; outlier_count is the ONLY Tier 2 op in the velocity family due to per-event sqrt() in the sigma-threshold test. Documented prominently in (1) the family index summary table 'CPU tier' column, (2) the family index 'Key invariants' / 'When to use which' guidance, (3) outlier_count.md Description + Complexity. Establishes that velocity-family CPU isn't uniformly Tier 1; SDK porters and benchmarks should not assume otherwise."
  - "rate_of_change INCLUSION rule (Plan 09 acceptance gate): rate_of_change is a Phase 9 velocity-family op per RESEARCH §3 (directory layout) + §5 (op classification), NOT a decay-family op (it computes a slope across two adjacent events, not an exponentially-weighted statistic). Plan 09 verifies `[ -f docs/operators/velocity/rate_of_change.md ]` and includes a Note block in velocity/index.md cross-linking BACK to decay/ for ewma (smoothing) and ew_zscore (drift-aware z-score sibling). Reciprocates Plan 08's note block in decay/index.md."
  - "z_score INCLUSION rule: z_score is REQUIREMENTS family AGG-Z-* but lives in velocity/ per RESEARCH §3 directory layout — it pairs naturally with the velocity / trend / outlier ops (entity-level statistics that answer 'is this entity moving / drifting / spiking / breaking from history?'). Documented explicitly in velocity/index.md Note block. Establishes that REQUIREMENTS family-buckets are NOT the same as docs/operators/<family>/ directory layout — directory grouping is documentation/SEO concern, family-bucket is requirements-traceability concern."
  - "window-required pattern (7 of 9 ops): rate_of_change / inter_arrival_stats / burst_count / trend / trend_residual / outlier_count / value_change_count cite window= as REQUIRED. Documented in Parameters + Description + Edge cases for each. delta_from_prev EXCLUDED (lifetime-only by design — diffs the most recent two values regardless of elapsed time); z_score uses baseline_window= alias (still REQUIRED but renamed at SDK layer for ergonomics)."
  - "burst_count dual-window-required pattern: burst_count is the only velocity op requiring TWO duration kwargs (window= AND sub_window=). sub_window must be smaller than window; sub_window='forever' is rejected at the SDK helper layer. Documented per-op in Parameters + Description + Edge cases. The 64-slot bucket ring is the structural memory cap; bucket count is fixed by ring-size, not by sub_window-count, so memory is genuinely O(1)."
  - "outlier_count sigma=3.0 default + MIN_BASELINE_N=5 warm-up: classic three-sigma rule (tighten to 2.0 for ~5% tail; loosen to 4.0 for ~0.006% tail); 5-event warm-up before firing the outlier check avoids spurious early-stream increments. Both documented in Parameters + Edge cases. SDK porters in 13.6 (TS, Go) must keep both defaults so cross-language behavior is identical."
  - "z_score baseline_window= ergonomic-alias decision: the SDK helper exposes baseline_window= (not window=) to make the 'baseline-against-which-the-current-event-is-scored' intent explicit; the wire-form params field is still named 'window' for opcode consistency (the engine doesn't care what the Python helper calls it). Documented in z_score.md Parameters table + Wire block + Edge cases. SDK porters in 13.5/13.6 must keep this alias — renaming back to window= at the SDK layer would break the Python signature contract."
  - "inter_arrival_stats v0 scalar return + v0.1+ dict widening: v0 emits the running mean_ms only (single f64), even though the Welford state tracks m2 for stddev_ms / cv. The wire return shape stays scalar in v0; v0.1+ widens to a {mean_ms, stddev_ms, cv} dict once the wire-level return shape supports nested values. Documented in inter_arrival_stats.md Returns + Edge cases (with explicit forward-pointer to v0.1+). Sets the v0/v0.1 boundary expectation for SDK porters in 13.5/13.6."
  - "Field-less helper pattern (3 ops): inter_arrival_stats / burst_count / value_change_count are field-less by design. inter_arrival_stats operates on arrival timestamps (not payload values); burst_count counts events (not values); value_change_count counts adjacent value flips but takes the field via positional argument. Passing a positional field= to inter_arrival_stats / burst_count raises TypeError at SDK-helper-call time. Documented in each op's Parameters table with an explicit 'No field= kwarg' note. SDK porters in 13.6 must respect the field-less constraint."
  - "delta_from_prev lifetime-only EXCLUSION rule: delta_from_prev is the ONLY velocity op WITHOUT a window= kwarg — it diffs the most recent two matching values regardless of elapsed time. Documented in (1) Signature (no window= row), (2) Description ('lifetime-only by design — pair with rate_of_change for the per-Δt rate'), (3) Complexity (no Lifetime mode row — there is no other mode), (4) family index summary table 'Required kwarg(s)' column shows '—'. Establishes that not every velocity op is windowed; the family is unified by behavioral / value-trajectory semantics, not by windowed-dispatch."
  - "Processing-time citation pattern (project_redis_shaped_no_event_time_ever): all 9 velocity ops state 'windowed dispatch bins by server now_ms() at apply' explicitly in Description. Late-event handling (Δt ≤ 0) varies per op: rate_of_change skips the rate update (no division by zero); inter_arrival_stats clamps Δt to 0; burst_count uses the current bucket regardless. All variations documented per op in Edge cases. The architecture commitment is 'time never moves backward'; the per-op late-event behavior preserves replay determinism without advancing the time cursor."
  - "Cold-start return convention documented per-op consistently: 6 ops return null on empty stream / single-event start (rate_of_change / inter_arrival_stats / delta_from_prev / trend / trend_residual / z_score) — typically null until n >= 2; 3 ops return 0 and only ever increment (burst_count / outlier_count / value_change_count). Family index 'Key invariants' section enumerates this split; each op's Edge cases section restates the per-op rule. SDK porters need to know: null-on-cold for derived statistics, 0-on-cold for counts."
  - "Cold-entity eviction (V0-MEM-GOV-01) cross-linked on every velocity op: @bv.event(cold_after=...) drops the underlying state per Redis-TTL pattern; velocity ops rebuild fresh from the next post-eviction matching event. Establishes how velocity ops interact with cold-after TTL for SDK porters."
  - "Per CLAUDE.md TDD §Note 4 doc-only-plan exemption: single docs(13.0-09) commit covering 9 polished pages + 1 family index page (commit da9f1d5) — no red-test commit required since the 'test' (acceptance grep loops + per-op LOC checks) is mechanical and passes by construction post-polish. SUMMARY.md committed separately (this file) per Plan 08's pattern (commit 371ce07 added 13.0-08-SUMMARY.md as a separate docs() commit)."

# Metrics
metrics:
  duration_min: 30
  completed: 2026-05-03T23:45:00Z
  tasks_completed: 2
  loc_added: 1020
  loc_removed: 190
  files_added: 1
  files_modified: 9
  commits: 1
---

# Phase 13.0 Plan 09: Operator catalog polish — velocity (9) Summary

## One-liner

Polished 9 velocity-family op pages (`rate_of_change`, `inter_arrival_stats`,
`burst_count`, `delta_from_prev`, `trend`, `trend_residual`, `outlier_count`,
`value_change_count`, `z_score`) from Plan 05 stubs into full-prose spec
documents — Description / Parameters / Returns / Complexity / Examples / Edge
cases / See also — and added 1 family overview index page
(`docs/operators/velocity/index.md`) including two Note blocks that disambiguate
the two cross-family ops (`rate_of_change` lives here NOT in `decay/` per
RESEARCH §3 + §5; `z_score` is REQUIREMENTS family `AGG-Z-*` but lives here
per RESEARCH §3 directory layout). 7 of 9 ops cite `window=` as REQUIRED;
`burst_count` cites BOTH `window=` AND `sub_window=`; `delta_from_prev` is
**lifetime-only** (no window — diffs the most recent two values regardless of
elapsed time); `z_score` uses the SDK ergonomic alias `baseline_window=`
mapping to wire-form `params.window`. `outlier_count` is the only Tier 2 op
in the family (per-event `sqrt()` for the sigma-threshold test); the other 8
are Tier 1. All 9 are O(1) memory bound and cite
`project_redis_shaped_no_event_time_ever` for processing-time `Δt` semantics.
Single `docs(13.0-09)` commit for the polish (da9f1d5) + this SUMMARY commit
per CLAUDE.md TDD §Note 4 doc-only-plan exemption.

## What landed

### 9 velocity/ op pages polished (146-163 LOC each)

| Op | LOC | Required kwarg(s) | Returns | CPU tier | Notes |
|----|----:|-------------------|---------|----------|-------|
| `rate_of_change.md` | 153 | `window` | `f64` or `null` | Tier 1 | Two-event delta divided by `Δt_ms`; lives here NOT `decay/` |
| `inter_arrival_stats.md` | 150 | `window` | `f64` or `null` | Tier 1 | v0 emits `mean_ms` only; v0.1+ widens to `{mean_ms, stddev_ms, cv}` dict; field-less |
| `burst_count.md` | 153 | `window`, `sub_window` | `i64` | Tier 1 | Max events in any 1-of-64 sliding sub-window slots; field-less |
| `delta_from_prev.md` | 146 | — (lifetime-only) | `f64` or `null` | Tier 1 | Absolute jump; pair with `rate_of_change` for per-Δt rate |
| `trend.md` | 153 | `window` | `f64` or `null` | Tier 1 | Online OLS slope of `(now_ms, field)`; smoother than `rate_of_change` |
| `trend_residual.md` | 158 | `window` | `f64` or `null` | Tier 1 | Latest value minus trend-line prediction; shares state with `trend` |
| `outlier_count.md` | 163 | `window` (`sigma=3.0` default) | `i64` | **Tier 2** | Welford-baseline + sigma-threshold count; only Tier 2 op in family (per-event `sqrt()`); MIN_BASELINE_N=5 warm-up |
| `value_change_count.md` | 158 | `window` | `i64` | Tier 1 | Counts adjacent flips, not net distinct values; cheaper than `count_distinct + 1` (no sketch state) |
| `z_score.md` | 159 | `baseline_window` | `f64` or `null` | Tier 1 | Current event's deviation against running `(mean, stddev)` per Welford; SDK ergonomic alias for wire-form `params.window`; null when stddev=0 |

### 1 family overview index page

- `docs/operators/velocity/index.md` (49 LOC) — Velocity family overview + 9-op
  summary table (op | required kwarg(s) | returns | CPU tier | notes columns)
  + **Key invariants** section locking the processing-time-only contract,
  window-canonical-kwarg-name rule (with `delta_from_prev` lifetime-only
  exception and `z_score` baseline_window= alias), burst_count
  dual-window-required rule, outlier_count `sigma=3.0` + `MIN_BASELINE_N=5`
  defaults, lifetime-mode-allowed-for-all-9, cold-start returns null/0 split,
  cold-entity eviction reference + **When to use which** guidance bullets +
  **Note** block clarifying (1) `bv.rate_of_change` lives here NOT
  `decay/` per RESEARCH §3 + §5, and (2) `bv.z_score` is REQUIREMENTS
  family `AGG-Z-*` but lives here per RESEARCH §3 directory layout +
  See also pointers (operator catalog index, cost-class.md, decay family,
  sketch family, recency family, shared.md window grammar, V0-MEM-GOV-02,
  pipeline-dsl/compilation-rules.md).

### Cross-family note pattern (bidirectional)

- `velocity/index.md` cross-references `decay/` for two reciprocal pairings:
  - `bv.ew_zscore` (decay) is the drift-aware counterpart to `bv.z_score`
    (velocity)
  - `bv.ewma` (decay) is the smoothed counterpart to the underlying signals
    fed into `bv.trend` / `bv.rate_of_change` (velocity)
- This reciprocates Plan 08's `decay/index.md` Note block forwarding to
  `velocity/rate_of_change.md`. Establishes the bidirectional
  cross-family-link pattern for any future related-but-separate-family op
  pair.

### TODO markers eliminated

Per Plan 05's split, the 9 op stubs in velocity/ collectively carried ~45
TODO markers (5 prose sections × 9 ops). After Plan 09 polish:
**0 TODO markers remain** in velocity/. The remaining TODO markers are
distributed across buffer-geo/ — owned by Plans 10 / 11 (already polished
by Plans 06/07/08: core, sketch, point-ordinal, recency, decay).

```bash
$ grep -rcE 'TODO \(Plan 13\.0-' docs/operators/velocity/ | grep -v ':0$'
(empty — 0 TODOs across all 10 files)
```

### Cross-link discipline

Every polished page cross-links to:

- `docs/operators/cost-class.md` (Phase 19.2 alive metadata) — for CPU
  tier (8 of 9 velocity ops are Tier 1; outlier_count is the only Tier 2)
- `docs/operators/velocity/index.md` — family overview (added by this plan)
- `docs/operators/index.md` — master 53-op catalog index (via family index)
- `docs/operators/decay/` — sibling family (ewma for smoothing,
  ew_zscore for drift-aware z-score)
- `docs/operators/sketch/` — sibling family (entropy / n_unique for
  non-numeric anomaly detection)
- `docs/operators/recency/` — sibling family (streak pairs naturally with
  value_change_count: consecutive matches vs. flips)
- `docs/pipeline-dsl/compilation-rules.md` — forward-reference (Plan
  13.0-12 creates the file)
- `examples/wire/register-fraud-team.request.json` — full payload example
  placeholder (Plan 06/07/08 also forward-referenced this fixture; created
  by a later plan)
- `.planning/PROJECT.md` (project_redis_shaped_no_event_time_ever) — for
  processing-time-only semantics on the windowed-dispatch `Δt`
- `.planning/REQUIREMENTS.md` (V0-MEM-GOV-02 — lifetime mode allowed for
  all 9 velocity ops; V0-MEM-GOV-01 — cold-after eviction) — for the
  lifetime-bound contract

## Verification (acceptance criteria)

All Task 1 / Task 2 acceptance criteria verified post-commit:

### Task 1 — 9 velocity ops

- All 9 velocity ops polished, no TODO markers (verified via `for op in
  rate_of_change inter_arrival_stats burst_count delta_from_prev trend
  trend_residual outlier_count value_change_count z_score; do ! grep -qE
  'TODO \(Plan 13\.0-' docs/operators/velocity/${op}.md; done` — PASS)
- All 9 ≥ 80 lines (got 146-163 LOC)
- rate_of_change lives in velocity/ NOT decay/ (`[ -f
  docs/operators/velocity/rate_of_change.md ] && [ ! -f
  docs/operators/decay/rate_of_change.md ]` — PASS)
- rate_of_change cites window kwarg (`grep -q 'window'
  docs/operators/velocity/rate_of_change.md` — PASS)
- burst_count documents sub_window (`grep -q 'sub_window'
  docs/operators/velocity/burst_count.md` — PASS)
- outlier_count documents sigma (`grep -q 'sigma'
  docs/operators/velocity/outlier_count.md` — PASS)
- z_score documents baseline_window (`grep -q 'baseline_window'
  docs/operators/velocity/z_score.md` — PASS)
- inter_arrival_stats documents struct return (`grep -qE
  'mean_ms|stddev_ms|cv' docs/operators/velocity/inter_arrival_stats.md` —
  PASS, `mean_ms` cited even though v0 emits the scalar form)
- velocity/ family directory has exactly 9 ops + index (`find
  docs/operators/velocity -name '*.md' -not -name 'index.md' | wc -l` →
  9 — PASS)

### Task 2 — family index page + commit

- velocity/index.md exists ≥ 25 lines (got 49 LOC)
- Index links all 9 ops (`for op in rate_of_change inter_arrival_stats
  burst_count delta_from_prev trend trend_residual outlier_count
  value_change_count z_score; do grep -q "${op}.md"
  docs/operators/velocity/index.md; done` — all PASS)
- Single docs(13.0-09) commit at da9f1d5 (`git log --format=%s --all |
  grep -qE '^docs\(13\.0-09\)'` — PASS)
- Commit body refs velocity (`git show da9f1d5 --format=%B | grep -qE
  'velocity|trend|rate_of_change'` — PASS)
- 10 files staged in commit (9 velocity op pages + 1 velocity-index)

### Plan-level invariants

- STATE.md / ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md NOT modified
  (none in commit per `git show da9f1d5 --stat`)
- docs/operators/cost-class.md NOT modified
- 0 TODOs remain in docs/operators/velocity/
- No new `rate_of_change.md` created in `decay/` (correctly absent —
  Plan 08's `decay/index.md` cross-links to the `velocity/` location)
- Single docs() commit per CLAUDE.md TDD §Note 4 doc-only-plan exemption

## Threat Flags

None. Plan 09 is doc-only polish — no new network surface, auth paths,
file access patterns, or schema changes at trust boundaries are
introduced. The single threat from the plan's `<threat_model>`
(T-13.0-09-01: operator-catalog information disclosure) is `accept` per
CONTEXT D-02 (operator pages are SEO/docs targets — intentional public
surface for OSS launch).

## Known Stubs

None in scope. The ~45 TODO markers Plan 05 sowed in the 9 velocity op
stubs are all eliminated by this plan. The remaining TODO markers
(buffer-geo/) are owned by Plans 10-11 and are NOT in this plan's goal
scope.

The forward-referenced `docs/pipeline-dsl/compilation-rules.md` link
target does not yet exist (Plan 13.0-12 creates it). This is intentional —
the cross-link is a contract for the page that's coming, not a broken
link in the steady-state docs site.

The forward-referenced `examples/wire/register-fraud-team.request.json`
link target also does not yet exist (a later plan in the 13.0 wave creates
it). Inherited from Plans 06/07/08's pattern; not a contract bug.

The forward-referenced `docs/operators/decay/ewma.md` and
`docs/operators/decay/ew_zscore.md` link targets exist as Plan 08-polished
pages — both polished and live. The cross-links resolve to fully-polished
pages.

The `inter_arrival_stats.md` Returns block documents the v0 scalar `mean_ms`
emission AND the v0.1+ widening to `{mean_ms, stddev_ms, cv}` dict. This
is a documented forward-deferral, not a stub — v0 emits the scalar; the
underlying Welford state already supports the dict form; v0.1+ widens
the wire return shape. SDK porters in 13.5/13.6 must keep the scalar
contract for v0.

## Deviations from Plan

None — plan executed exactly as written. The plan's `<action>` blocks
specified per-op content guidance (mathematical primitives for trend
slope / Welford for inter_arrival_stats and z_score / 64-slot bucket ring
for burst_count / sigma-threshold + warm-up for outlier_count / lifetime-only
for delta_from_prev / baseline_window= alias for z_score / window+sub_window
for burst_count / processing-time Δt / cold-start behavior / structured error
codes); this executor wrote the prose directly into each Plan 05 stub via the
Write tool, replacing the TODO markers with concrete content. All acceptance
criteria verified pre-commit + post-commit (see Verification section above).
The Wire JSON blocks were rewritten with concrete field/window/sub_window/
sigma/baseline_window values matching the Examples (e.g. burst_count's wire
block now shows `field: "ip", window: "1h", sub_window: "1m"` matching
Example 1) rather than the Plan 05 placeholder `params: {}`.

The plan's `<action>` block for `inter_arrival_stats.md` specified the
return as a "struct/dict `{mean_ms, stddev_ms, cv}`" but investigation
against `crates/beava-core/src/agg_op.rs` + `python/beava/_agg.py`
confirmed v0 emits the scalar `mean_ms` only — the Welford state tracks
`m2` so the dict-form is computable, but the wire-level return shape is
still scalar in v0. Resolved by documenting v0 emits scalar `mean_ms`
explicitly + a forward-pointer to v0.1+ widening (see "Known Stubs"
above). This is a v0/v0.1 boundary call, not a contract bug.

## Self-Check: PASSED

- FOUND: docs/operators/velocity/rate_of_change.md (153 LOC, 0 TODOs, window cited)
- FOUND: docs/operators/velocity/inter_arrival_stats.md (150 LOC, 0 TODOs, window cited, mean_ms cited, field-less documented)
- FOUND: docs/operators/velocity/burst_count.md (153 LOC, 0 TODOs, window + sub_window cited)
- FOUND: docs/operators/velocity/delta_from_prev.md (146 LOC, 0 TODOs, lifetime-only documented)
- FOUND: docs/operators/velocity/trend.md (153 LOC, 0 TODOs, window cited)
- FOUND: docs/operators/velocity/trend_residual.md (158 LOC, 0 TODOs, window cited)
- FOUND: docs/operators/velocity/outlier_count.md (163 LOC, 0 TODOs, sigma + window cited, Tier 2)
- FOUND: docs/operators/velocity/value_change_count.md (158 LOC, 0 TODOs, window cited)
- FOUND: docs/operators/velocity/z_score.md (159 LOC, 0 TODOs, baseline_window cited)
- FOUND: docs/operators/velocity/index.md (49 LOC, 9 op links + cross-family note for rate_of_change + cross-family note for z_score)
- FOUND: commit da9f1d5 (`docs(13.0-09): polish 9 velocity-family op pages + family index`)
- FOUND-NOT-MODIFIED: docs/operators/cost-class.md (NOT in commit; last touched 27 Apr by Phase 19.2)
- FOUND-NOT-MODIFIED: STATE.md / ROADMAP.md / CORRECTNESS-PATH.md / CLAUDE.md (NOT in commit per `git show da9f1d5 --stat`)
- FOUND-NOT-CREATED: docs/operators/decay/rate_of_change.md (correctly absent — lives in velocity/)
- FOUND-PASSING: 0 TODOs remain in docs/operators/velocity/
