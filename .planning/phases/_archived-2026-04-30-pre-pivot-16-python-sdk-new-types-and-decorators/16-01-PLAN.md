---
phase: 16-python-sdk-new-types-and-decorators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/tally/_schema.py
  - python/tally/_source.py
  - python/tally/_dataset.py
  - python/tests/test_new_api.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05

must_haves:
  truths:
    - "EventSet subclass collects Field descriptors from annotations and supports instantiation"
    - "FeatureSet subclass collects Field descriptors from annotations and supports instantiation"
    - "@tl.source decorator creates a source object whose _compile() returns keyless stream JSON"
    - "@tl.dataset(depends_on=[...]) decorator creates a dataset with .group_by().agg() that compiles to keyed stream JSON"
    - "tl.union(a, b) returns a UnionSource whose children flatten into depends_on list"
  artifacts:
    - path: "python/tally/_schema.py"
      provides: "EventSet, FeatureSet, Field classes with dataclass_transform"
      contains: "class EventSet"
    - path: "python/tally/_source.py"
      provides: "@tl.source decorator and SourceDef class"
      contains: "def source"
    - path: "python/tally/_dataset.py"
      provides: "@tl.dataset decorator, DatasetDef, GroupedDataset, UnionSource, group_by free function"
      contains: "def dataset"
    - path: "python/tests/test_new_api.py"
      provides: "Unit tests for all new API types"
      contains: "class TestSource"
  key_links:
    - from: "python/tally/_source.py"
      to: "python/tally/_schema.py"
      via: "import EventSet for schema reference"
      pattern: "from tally._schema import"
    - from: "python/tally/_dataset.py"
      to: "python/tally/_operators.py"
      via: "reuse existing operator classes in agg()"
      pattern: "from tally._operators import"
    - from: "python/tally/_dataset.py"
      to: "python/tally/_source.py"
      via: "depends_on accepts SourceDef and UnionSource"
      pattern: "from tally._source import"
---

<objective>
Create the core new API types: EventSet/FeatureSet schema classes, @tl.source decorator, @tl.dataset decorator with .group_by().agg(), and tl.union() -- all compiling to the existing RegisterRequest JSON format.

Purpose: Establish the new function-based API surface that replaces @st.stream, with typed schemas and explicit aggregation.
Output: Three new modules (_schema.py, _source.py, _dataset.py) and comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-python-sdk-new-types-and-decorators/16-CONTEXT.md
@.planning/phases/16-python-sdk-new-types-and-decorators/16-RESEARCH.md

@python/tally/__init__.py
@python/tally/_operators.py
@python/tally/_stream.py
@python/tally/_dataframe.py
@python/tally/_app.py

<interfaces>
<!-- Existing operator protocol the new API must reuse -->
From python/tally/_operators.py:
```python
class OperatorBase:
    def to_json(self, name: str) -> dict: ...

class Count(OperatorBase):
    def __init__(self, *, window: str, where: str | None = None, bucket: str | None = None, backfill: bool = False): ...

class Derive(OperatorBase):
    def __init__(self, expr: str): ...

class Lookup(OperatorBase):
    def __init__(self, target: str, *, on: str): ...
```

From python/tally/_stream.py (compilation target format):
```python
def _to_register_json(cls) -> dict:
    # Returns: {"name": str, "key_field": str|None, "features": list[dict], ...}
    # Optional: "depends_on": list[str], "entity_ttl": str, "history_ttl": str, "filter": str, "type": "view"
```

From python/tally/_app.py (registration protocol):
```python
def register(self, *stream_classes) -> None:
    # Accepts anything with _collect_registrations() -> list[dict]
    # OR _to_register_json() -> dict
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create _schema.py with EventSet, FeatureSet, and Field</name>
  <files>python/tally/_schema.py, python/tests/test_new_api.py</files>
  <read_first>
    - python/tally/_operators.py (operator classes to understand type system)
    - python/tally/_stream.py (existing pattern for __init_subclass__ vs metaclass)
    - python/tally/__init__.py (current exports, avoid name collisions)
  </read_first>
  <behavior>
    - Test: EventSet subclass with `user_id: str = Field()` and `amount: float = Field()` has `_fields` dict with those two entries
    - Test: FeatureSet subclass with `tx_count: int = Field()` has `_fields` dict with that entry
    - Test: Field(dtype=float, description="amount in USD", default=0.0) stores all three attributes
    - Test: EventSet subclass can be instantiated: `TxnEvent(user_id="u1", amount=50.0)` and attributes are accessible
    - Test: EventSet subclass with no Field() default raises TypeError on missing required arg
    - Test: Field() with no dtype infers from annotation type hint
  </behavior>
  <action>
Create `python/tally/_schema.py` with:

1. `Field` class with `__init__(self, *, dtype=None, description="", default=...)` where `...` (Ellipsis) is the sentinel for "required". Store all three as instance attributes.

2. `@dataclass_transform(field_specifiers=(Field,))` on `EventSet` base class:
   - `__init_subclass__` collects fields from `cls.__annotations__` (iterate annotation names, check if class attribute is a `Field` instance; if annotation exists but no Field, create `Field()` automatically)
   - Store as `cls._fields: dict[str, Field]`
   - Generate `__init__` dynamically: for each field in `_fields`, if `field.default is ...` then required param, else optional with default. Use `exec()` to build `__init__` with correct signature for IDE support.

3. `@dataclass_transform(field_specifiers=(Field,))` on `FeatureSet` base class:
   - Same `__init_subclass__` pattern as EventSet (can share implementation via a helper function `_collect_fields(cls)`)
   - Same `__init__` generation

4. Use `from __future__ import annotations` at top (consistent with all other SDK files). Use `typing.get_type_hints(cls)` when resolving actual types for dtype inference (pitfall 1 from RESEARCH.md).

Write tests first in `python/tests/test_new_api.py` in class `TestSchema` with the behaviors listed above, then implement to pass.
  </action>
  <verify>
    <automated>cd /data/home/tally/python && python -m pytest tests/test_new_api.py::TestSchema -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - python/tally/_schema.py contains `class Field:`, `class EventSet:`, `class FeatureSet:`
    - python/tally/_schema.py contains `@dataclass_transform(field_specifiers=(Field,))`
    - python/tally/_schema.py contains `def _collect_fields(cls)`
    - python/tests/test_new_api.py contains `class TestSchema`
    - TestSchema has at least 5 test methods covering EventSet, FeatureSet, Field, instantiation, and required-field validation
    - `cd /data/home/tally/python && python -m pytest tests/test_new_api.py::TestSchema -x` exits 0
  </acceptance_criteria>
  <done>EventSet and FeatureSet support Field descriptors, dataclass_transform, instantiation, and field collection. All TestSchema tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create _source.py with @tl.source decorator</name>
  <files>python/tally/_source.py, python/tests/test_new_api.py</files>
  <read_first>
    - python/tally/_schema.py (just created in Task 1)
    - python/tally/_stream.py (reference for _to_register_json pattern, lines 97-133)
    - python/tally/_operators.py (Derive for derive-only sources)
    - python/tests/test_new_api.py (append tests to existing file)
  </read_first>
  <behavior>
    - Test: @tl.source decorated class produces _compile() returning {"name": "Transactions", "key_field": None, "features": []}
    - Test: @tl.source class with EventSet `event` attribute stores schema reference
    - Test: @tl.source class has _tally_stream_name equal to class name (App.register compat)
    - Test: @tl.source class has _to_register_json() that returns same dict as _compile() (App.register compat)
    - Test: @tl.source class has _collect_registrations() returning [_compile()] (App.register_all compat)
    - Test: source with entity_ttl="5m" includes it in compiled JSON
  </behavior>
  <action>
Create `python/tally/_source.py` with:

1. `class SourceDef`:
   - `__init__(self, name: str, event_schema: type | None, entity_ttl: str | None, history_ttl: str | None)` storing all fields
   - `_name: str` attribute (the class name)
   - `_tally_stream_name` property returning `self._name` (compatibility with `App.register()` which checks `hasattr(dep, '_tally_stream_name')`)
   - `_compile(self) -> dict`: returns `{"name": self._name, "key_field": None, "features": []}` plus optional `entity_ttl`, `history_ttl` if set
   - `_to_register_json(self) -> dict`: delegates to `_compile()` (App.register compat)
   - `_collect_registrations(self) -> list[dict]`: returns `[self._compile()]` (App.register_all compat)

2. `def source(cls=None, *, entity_ttl=None, history_ttl=None)`:
   - If `cls` is None, return a decorator (supports both `@tl.source` and `@tl.source(entity_ttl="5m")`)
   - Extract `event` attribute from class if it exists and is an EventSet subclass
   - Return `SourceDef(cls.__name__, event_schema, entity_ttl, history_ttl)`

Write tests first in `TestSource` class, then implement.
  </action>
  <verify>
    <automated>cd /data/home/tally/python && python -m pytest tests/test_new_api.py::TestSource -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - python/tally/_source.py contains `class SourceDef:`, `def source(`
    - python/tally/_source.py contains `def _compile(self)`, `def _to_register_json(self)`, `def _collect_registrations(self)`
    - python/tally/_source.py contains `_tally_stream_name`
    - python/tests/test_new_api.py contains `class TestSource` with at least 5 test methods
    - `cd /data/home/tally/python && python -m pytest tests/test_new_api.py::TestSource -x` exits 0
  </acceptance_criteria>
  <done>@tl.source decorator creates SourceDef objects that compile to keyless stream JSON and are compatible with App.register(). All TestSource tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create _dataset.py with @tl.dataset, GroupedDataset, group_by, union</name>
  <files>python/tally/_dataset.py, python/tests/test_new_api.py</files>
  <read_first>
    - python/tally/_source.py (just created, SourceDef interface)
    - python/tally/_schema.py (EventSet/FeatureSet for schema refs)
    - python/tally/_operators.py (OperatorBase, all operator classes for agg())
    - python/tally/_dataframe.py (reference GroupBy pattern, lines 162-190; Table._to_register_json, lines 366-381)
    - python/tally/_stream.py (depends_on resolution pattern, lines 127-130)
    - python/tests/test_new_api.py (append tests)
  </read_first>
  <behavior>
    - Test: group_by("user_id").agg(tx_count=tl.count(window="1h")) returns a GroupedDataset with key="user_id" and features dict
    - Test: @tl.dataset(depends_on=[source]) with features = group_by(...).agg(...) compiles to {"name": "X", "key_field": "user_id", "depends_on": ["SourceName"], "features": [...]}
    - Test: compiled features list matches operator.to_json(name) output exactly
    - Test: depends_on resolves SourceDef class references to string names via ._name
    - Test: tl.union(source_a, source_b) returns UnionSource; dataset depending on union has depends_on: ["source_a", "source_b"]
    - Test: @tl.dataset with entity_ttl="5m" and history_ttl="72h" includes them in compiled JSON
    - Test: DatasetDef has _tally_stream_name, _to_register_json(), _collect_registrations() for App compat
    - Test: _collect_registrations() includes upstream source registrations before self
    - Test: derive features work via features assignment in dataset body
  </behavior>
  <action>
Create `python/tally/_dataset.py` with:

1. `def group_by(key: str) -> GroupedDataset`:
   - Free function (top-level) returning `GroupedDataset(key=key, features={})`
   - This is used inside class body: `features = tl.group_by("user_id").agg(...)`

2. `class GroupedDataset`:
   - `__init__(self, key: str, features: dict[str, OperatorBase])` 
   - `.agg(self, **kwargs: OperatorBase) -> GroupedDataset`: validates all values are OperatorBase instances, returns new GroupedDataset with key and features dict updated
   - Stores `self._key` and `self._features`

3. `class UnionSource`:
   - `__init__(self, *sources)`: stores list of source references
   - `_name` property: not used (union is transparent)
   - Method to flatten source names: `_get_depends_on_names() -> list[str]` resolving each source to its `._name`

4. `def union(*sources) -> UnionSource`:
   - Free function returning `UnionSource(*sources)`

5. `class DatasetDef`:
   - `__init__(self, name, depends_on, grouped_dataset, event_schema, entity_ttl, history_ttl)`
   - `_name: str`
   - `_tally_stream_name` property returning `self._name`
   - `_compile(self) -> dict`:
     - Build `{"name": self._name, "key_field": grouped.key, "features": [op.to_json(name) for name, op in grouped.features.items()]}`
     - Resolve `depends_on`: for each dep, if it's a `UnionSource`, call `_get_depends_on_names()` and flatten; if it's a `SourceDef` or `DatasetDef`, use `dep._name`. Store as `"depends_on": [list of string names]`
     - Add optional `entity_ttl`, `history_ttl` if set
   - `_to_register_json(self) -> dict`: delegates to `_compile()`
   - `_collect_registrations(self) -> list[dict]`:
     - Walk `depends_on` list, for each dep (SourceDef/DatasetDef) call `dep._collect_registrations()`, deduplicate by name, append self last
     - For UnionSource, iterate its sources and collect from each

6. `def dataset(*, depends_on: list, entity_ttl=None, history_ttl=None)`:
   - Returns a decorator function
   - Decorator extracts `event` attribute (EventSet subclass if present), `features` attribute (must be a GroupedDataset)
   - Also scan class body for OperatorBase instances (derive features set as class attributes, like `failure_rate = tl.derive("...")`)
   - Returns `DatasetDef(cls.__name__, depends_on, grouped_dataset, event_schema, entity_ttl, history_ttl)`

Write tests first in `TestDataset`, `TestGroupByAgg`, and `TestUnion` classes, then implement.
  </action>
  <verify>
    <automated>cd /data/home/tally/python && python -m pytest tests/test_new_api.py::TestDataset tests/test_new_api.py::TestGroupByAgg tests/test_new_api.py::TestUnion -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - python/tally/_dataset.py contains `class DatasetDef:`, `class GroupedDataset:`, `class UnionSource:`, `def dataset(`, `def group_by(`, `def union(`
    - python/tally/_dataset.py contains `def _compile(self)`, `def _to_register_json(self)`, `def _collect_registrations(self)`
    - python/tally/_dataset.py contains `_tally_stream_name`
    - python/tests/test_new_api.py contains `class TestDataset`, `class TestGroupByAgg`, `class TestUnion`
    - TestDataset has at least 4 test methods; TestGroupByAgg has at least 2; TestUnion has at least 2
    - `cd /data/home/tally/python && python -m pytest tests/test_new_api.py::TestDataset tests/test_new_api.py::TestGroupByAgg tests/test_new_api.py::TestUnion -x` exits 0
  </acceptance_criteria>
  <done>@tl.dataset decorator, group_by().agg(), and union() all work correctly, compile to the same RegisterRequest JSON format as the old API, and are App.register() compatible. All TestDataset, TestGroupByAgg, and TestUnion tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK -> Server | Pipeline definitions compiled to JSON cross from Python to Rust via TCP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | T (Tampering) | _compile() output | accept | JSON is validated server-side by Rust RegisterRequest deserialization; SDK is trusted client code |
| T-16-02 | I (Info Disclosure) | Field descriptors | accept | Schema types are local Python objects; no secrets involved |
</threat_model>

<verification>
```bash
cd /data/home/tally/python && python -m pytest tests/test_new_api.py -x -v
```
All test classes (TestSchema, TestSource, TestDataset, TestGroupByAgg, TestUnion) pass.
</verification>

<success_criteria>
- _schema.py, _source.py, _dataset.py exist with all required classes
- EventSet/FeatureSet support Field descriptors and instantiation
- @tl.source compiles to keyless stream JSON
- @tl.dataset with .group_by().agg() compiles to keyed stream JSON
- tl.union() flattens multi-parent depends_on
- All new classes have _to_register_json() and _collect_registrations() for App compat
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-python-sdk-new-types-and-decorators/16-01-SUMMARY.md`
</output>
