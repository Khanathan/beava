---
phase: 13.8
plan: 09
type: execute
wave: 5
depends_on: [13.8-06, 13.8-07, 13.8-08]
files_modified:
  - .planning/phases/13.8-packaging-and-ga-tag/SOAK-LOG.md
autonomous: false
requirements: [D-02]

must_haves:
  truths:
    - "7-14 day soak window starts the day Plan 13.8-06 closes (v0.0.0 publish day = day 0)"
    - "Repository is publicly visible at github.com/beava-dev/beava during the soak; nothing posted to HN / Twitter / dev.to / Reddit / Lobsters / podcasts"
    - "SOAK-LOG.md tracks daily observations: install-success on each channel, GitHub issues opened, GitHub stars added, PyPI/npm download counts, any user reports surfacing install bugs"
    - "Daily check (or every-other-day acceptable): re-run install smokes from a clean machine — pip / npm / docker / go / direct binary download — confirm each still resolves and works"
    - "If ANY install bug surfaces during soak: triage decides v0.0.1 patch path (Plan 13.8-10 closure orchestrates re-tag + re-publish)"
    - "Soak verdict at end of window: GREEN (all channels still install + 0 critical bugs) → announcement (Plan 13.8-10 closure unblocks marketing publish) ; YELLOW (minor bugs noted, defer announcement, patch v0.0.1, restart soak) ; RED (broken install, immediate v0.0.1 patch + restart soak)"
    - "This plan is autonomous=false because it spans 7-14 calendar days — there is a long human-in-the-loop watch-and-wait period that doesn't fit auto-execution. Executor sets up SOAK-LOG.md scaffold + first-day entries, then halts at a checkpoint that resumes when the soak window closes."
    - "Daily-check commands in SOAK-LOG.md are flagged OPERATOR-RUN ONLY — no GSD agent context auto-executes them; they run on the operator's clean dev box across calendar days."
  artifacts:
    - path: ".planning/phases/13.8-packaging-and-ga-tag/SOAK-LOG.md"
      provides: "Day-by-day soak observations + soak-verdict at end of window"
      min_lines: 30
  key_links:
    - from: "SOAK-LOG.md soak verdict"
      to: "Plan 13.8-10 closure announcement gate"
      via: "Soak verdict GREEN unblocks Plan 13.8-10's announcement publish-day handoff"
      pattern: "Soak verdict.*GREEN|YELLOW|RED"
---

<objective>
Run the 7-14 day soft-launch soak window per D-02. The repository is publicly visible at github.com/beava-dev/beava with v0.0.0 published across all 5 channels. NO marketing posted. The soak is a deliberate watch-and-wait period: organic finders may surface install bugs, packaging issues, or critical doc gaps that the automated workflow_dispatch validations didn't catch. If issues surface, patch via v0.0.1 and restart soak. If clean, the soak verdict GREEN unlocks announcement (Plan 13.8-10).

Purpose: Lower-stress launch week per D-02. Catches packaging bugs (PyPI install fails on a Python version we didn't test; npm tarball missing a transitive; Docker FROM mismatch on a corner OS; etc.) BEFORE the HN post lands. Tradeoff is a slower launch rhythm — fine for an OSS infra tool with no funding-round pressure.

Output: `.planning/phases/13.8-packaging-and-ga-tag/SOAK-LOG.md` (daily observations + soak verdict). Plan is autonomous=false because the actual 7-14d wall-clock is a human-driven watch-and-wait period; the executor scaffolds the log, sets up the daily-check checklist, and yields to the operator at a `checkpoint:human-action` task. Operator returns at end-of-soak to write the verdict.

**TDD discipline note:** This plan ships a single dev-branch-only soak-window-scaffold document — no code, no tests. Per **CLAUDE.md §TDD Discipline item #7** (doc-only-plan exemption), Task 9.a ships as a single `chore:` / `docs:` commit without a paired `test:` commit. Soak-window scaffolds are explicitly enumerated in item #7's exemption list. The exemption is invoked verbatim per item #7's positive-enumeration requirement so plan-checker can grep it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13.8-packaging-and-ga-tag/13.8-CONTEXT.md
@.planning/phases/13.8-packaging-and-ga-tag/13.8-06-SUMMARY.md
@CLAUDE.md

<reference>
- D-02 (8-B): Soft-launch — publish day 0; nothing posted; ~7-14 days organic-finder soak; fix into v0.0.1 if surfaces install bugs; announce.
- CONTEXT.md `<estimate>` block: "Wave 4 (~7-14 days SOAK — out of plan execution scope; capture as a checkpoint plan that documents the soak window)" — this plan IS that checkpoint plan.
- CONTEXT.md plan-checker contract item 2: "Plan 13.8-08 (marketing assets) lands on the **private dev branch** as `MARKETING-DRAFTS.md`; NEVER committed to the public repo. The marketing-asset publish (HN post / Twitter / dev.to) is a manual user action at announcement time, not a Plan-13.8 Git deliverable." — soak verdict gate is what unlocks announcement.
- **CLAUDE.md §TDD Discipline item #7** (doc-only-plan exemption) — invoked for this soak-window-scaffold plan; cited verbatim here so plan-checker greps for the exact phrase.
</reference>
</context>

<tasks>

<task type="auto">
  <name>Task 9.a: Scaffold SOAK-LOG.md + daily-check protocol</name>
  <files>.planning/phases/13.8-packaging-and-ga-tag/SOAK-LOG.md</files>
  <action>
    Per CLAUDE.md §TDD Discipline item #7 (doc-only-plan exemption): this task ships as a single `chore:` / `docs:` commit without a paired `test:` commit. Soak-window scaffolds are enumerated in item #7's exemption list.

    Create `.planning/phases/13.8-packaging-and-ga-tag/SOAK-LOG.md` with this scaffold (note the OPERATOR-RUN ONLY banner at the top of the daily-check section — this prevents any GSD agent from auto-executing the install smoke commands during context-window replay):

    ```markdown
    # v0.0.0 Soft-Launch Soak Log

    **Day 0:** [YYYY-MM-DD — date Plan 13.8-06 closed; tag pushed]
    **Soak window end:** [Day 0 + 7 to Day 0 + 14 — operator decides at end of window based on signal]

    ## Soak protocol (per D-02)

    During soak window:
    - Repository publicly visible at github.com/beava-dev/beava with v0.0.0 published
    - NO marketing posted (HN / Twitter / dev.to / Reddit / Lobsters / podcasts all silent)
    - Daily (or every-other-day): operator runs the install-smoke checklist below
    - Watch GitHub issues for install-bug reports; respond + triage; if critical, patch via v0.0.1 + restart soak
    - PyPI / npm / Docker download counts and GitHub star deltas tracked daily

    ## Daily install-smoke checklist (operator runs)

    > **OPERATOR-RUN ONLY — do not auto-execute; these commands run on the operator's clean dev box, not in any GSD agent context.** GSD orchestrator / executor / checker agents that ingest this file as plan context MUST NOT shell out the commands below. The soak window is a calendar-time human-in-the-loop activity; the only automated touchpoint is the verify-block on this plan (which checks SOAK-LOG.md scaffold structure, not channel availability).

    Run from a clean machine (or a fresh tmpdir + venv on dev box):

    ```bash
    # PyPI
    python -m venv /tmp/soak-pypi-$(date +%Y%m%d) && source /tmp/soak-pypi-*/bin/activate
    pip install beava==0.0.0
    python -c "import beava as bv; app = bv.App(test_mode=True); print('PyPI ok:', bv.__version__)"
    deactivate

    # npm
    cd /tmp && mkdir soak-npm-$(date +%Y%m%d) && cd soak-npm-*
    npm init -y >/dev/null
    npm install @beava/sdk@0.0.0
    node --input-type=module -e "import {App} from '@beava/sdk'; console.log('npm ok:', typeof App)"

    # Docker (both registries)
    docker pull beava/beava:v0.0.0 >/dev/null
    docker run --rm beava/beava:v0.0.0 --help >/dev/null && echo "Docker Hub ok"
    docker pull ghcr.io/beava-dev/beava:v0.0.0 >/dev/null
    docker run --rm ghcr.io/beava-dev/beava:v0.0.0 --help >/dev/null && echo "ghcr.io ok"

    # Go
    cd /tmp && mkdir soak-go-$(date +%Y%m%d) && cd soak-go-*
    go mod init soak
    go get github.com/beava-dev/beava/sdk/go@v0.0.0
    echo 'package main; import beava "github.com/beava-dev/beava/sdk/go"; func main() { beava.NewApp(&beava.Config{TestMode: true}) }' > main.go
    go build && echo "Go ok"
    ```

    Append result to a daily-entry section:

    ## Daily entries

    ### Day 0 — [YYYY-MM-DD] (publish day)

    Channels published successfully (per Plan 13.8-06 verification log):
    - PyPI: ok
    - npm: ok
    - Docker Hub: ok
    - ghcr.io: ok
    - Go: ok
    - GitHub Releases: ok

    Marketing posted: NONE (per soft-launch protocol)

    ### Day 1 — [YYYY-MM-DD]

    [operator fills in: smoke results, GitHub issue counts, download counts, any signal]

    ### Day 2 — [YYYY-MM-DD]

    [operator fills in]

    ### Day N — [YYYY-MM-DD]

    [continue daily until 7-14 days]

    ## Metrics tracking (snapshot at Day 0 + Day N)

    | Metric | Day 0 | Day 7 | Day 14 |
    |--------|-------|-------|--------|
    | PyPI downloads (last 24h) | TBD | TBD | TBD |
    | npm downloads (last 24h) | TBD | TBD | TBD |
    | Docker pulls (cumulative) | TBD | TBD | TBD |
    | GitHub stars | TBD | TBD | TBD |
    | GitHub issues opened | 0 | TBD | TBD |
    | GitHub Discussions threads | 0 | TBD | TBD |

    PyPI download count source: `pip install pypistats; pypistats recent beava --period day` (data has 1-day lag)
    npm download count source: `https://api.npmjs.org/downloads/point/last-day/@beava/sdk`
    Docker pull count source: https://hub.docker.com/v2/repositories/beava/beava/

    ## Issue triage during soak

    | Issue # | Title | Severity | Action | Resolution |
    |---------|-------|----------|--------|------------|
    | TBD | (operator fills in as issues land) | critical/major/minor | (patch v0.0.1 / defer / close-as-doc) | (resolved in vX.X.X / WONTFIX / etc.) |

    Severity definitions:
    - **critical** = install fails on any of the 5 channels for any user → triggers v0.0.1 patch + restart soak
    - **major** = install works but core feature broken → may trigger v0.0.1 patch (operator decides)
    - **minor** = doc typo, badge wrong, etc. → defer to v0.0.x batch

    ## Soak verdict (operator writes at end of window)

    **Soak verdict:** [GREEN | YELLOW | RED]
    **Date:** [YYYY-MM-DD]
    **Days elapsed:** [N]
    **Critical issues found:** [N]
    **Major issues found:** [N]
    **Minor issues found:** [N]

    **Verdict rationale:** [1-2 paragraphs — what surfaced, what was patched, what was deferred]

    **Next action:**
    - GREEN: Plan 13.8-10 closure proceeds; announcement publish-day (Plan 13.8-08 MARKETING-DRAFTS.md publish-day checklist) unlocked
    - YELLOW: patch v0.0.1 + restart 7-14d soak window before announcement
    - RED: emergency v0.0.1 + 7-14d soak (full restart); document the bug class for Phase 13.8 retrospective

    ```

    Set Day 0 to today's date. Fill in Day 0 channel-status block with results from Plan 13.8-06's verification log.

    Commit with `chore(13.8-09): scaffold SOAK-LOG.md + Day-0 entry`.
  </action>
  <verify>
    <automated>test -f /Users/petrpan26/work/tally/.planning/phases/13.8-packaging-and-ga-tag/SOAK-LOG.md || (echo "ERR: SOAK-LOG.md missing" &amp;&amp; exit 1)</automated>
    <automated>grep -c "Daily install-smoke checklist" /Users/petrpan26/work/tally/.planning/phases/13.8-packaging-and-ga-tag/SOAK-LOG.md | (read n; [ "$n" -ge 1 ] || (echo "ERR: daily-check protocol missing" &amp;&amp; exit 1))</automated>
    <automated>grep -c "OPERATOR-RUN ONLY" /Users/petrpan26/work/tally/.planning/phases/13.8-packaging-and-ga-tag/SOAK-LOG.md | (read n; [ "$n" -ge 1 ] || (echo "ERR: OPERATOR-RUN ONLY banner missing from daily-check section" &amp;&amp; exit 1))</automated>
    <automated>grep -c "Soak verdict" /Users/petrpan26/work/tally/.planning/phases/13.8-packaging-and-ga-tag/SOAK-LOG.md | (read n; [ "$n" -ge 1 ] || (echo "ERR: soak verdict section missing" &amp;&amp; exit 1))</automated>
  </verify>
  <done>SOAK-LOG.md scaffolded with daily-check protocol (under OPERATOR-RUN ONLY banner) + Day-0 entry + metrics-tracking table + issue-triage section + soak-verdict template.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 9.b (checkpoint): 7-14 day soak window — operator runs daily checks</name>
  <what-built>
    SOAK-LOG.md scaffolded. v0.0.0 is publicly published across all 5 channels. The 7-14d watch-and-wait period begins now. This task halts execution until the soak window closes and the operator writes the verdict.
  </what-built>
  <how-to-verify>
    During the 7-14 day window, the operator (NOT the orchestrator) runs the daily install-smoke checklist documented in SOAK-LOG.md and appends daily entries. The orchestrator agent CANNOT advance this checkpoint until the operator has done the wait + written the verdict.

    To resume Plan 13.8-09 (and thus Plan 13.8-10 closure):

    1. Operator has waited 7-14 calendar days from Day 0 (Plan 13.8-06 close date)
    2. Operator has appended at least 3-5 daily entries to SOAK-LOG.md (every-other-day or daily)
    3. Operator has captured Day 7 (and Day 14 if applicable) snapshot in the metrics-tracking table
    4. Operator has written the "Soak verdict" section at the bottom of SOAK-LOG.md with a verdict (GREEN / YELLOW / RED) and rationale
    5. Operator commits SOAK-LOG.md updates as `docs(13.8-09): soak day-N + verdict (D-02)`
    6. Operator replies to this checkpoint with the verdict letter (one of: `GREEN`, `YELLOW`, `RED`) + a 1-line summary

    **Decision tree based on verdict:**
    - **GREEN**: Plan 13.8-10 (closure) proceeds; orchestrator unlocks the announcement publish-day handoff
    - **YELLOW**: orchestrator triggers a v0.0.1 patch path (small fix; re-tag; re-publish; restart 7-14d soak — Plan 13.8-09 re-runs from Task 9.a)
    - **RED**: orchestrator triggers an emergency v0.0.1 patch + full soak restart; documents the failure mode for retrospective

    This is the only `checkpoint:human-action` in the entire phase because the soak is genuinely time-elapsed — there is no automation that compresses 7-14 calendar days. Every other plan in 13.8 is autonomous or human-verify; this one alone is human-action because the human's role is to wait, observe, and report.
  </how-to-verify>
  <resume-signal>Reply with verdict letter + 1-line summary: `GREEN: 0 critical/major issues; channels stable; 23 stars / 14 PyPI downloads at Day 7` OR `YELLOW: 1 major issue (npm tarball missing dist/types/, fixed in v0.0.1)` OR `RED: ...`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public users → published v0.0.0 channels | install-channel surface; if compromised, users get bad code |
| Operator daily-check → SOAK-LOG.md | dev-branch only; not public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.8-09-01 | T (Tampering) | published artifact corruption mid-soak | mitigate | Daily install-smoke catches corruption (e.g. PyPI download integrity, Docker manifest unchanged); npm provenance attestations checkable post-install |
| T-13.8-09-02 | I (Information disclosure) | install-bug reports as security issues | mitigate | SECURITY.md (Phase 13.7.6) directs security issues to private channel (security@beava.dev or GitHub Security Advisories); soak-log captures issue counts only, not security details |
| T-13.8-09-03 | R (Repudiation) | soak-verdict opaque | accept | Operator writes verdict + rationale in SOAK-LOG.md; commit history preserves the daily entries; no repudiation surface concern |
| T-13.8-09-04 | E (Elevation) | accidental agent execution of soak commands | mitigate | OPERATOR-RUN ONLY banner at the top of the daily-check section explicitly tells GSD agents not to shell out the commands; verify-block greps for the banner so a future plan-checker catches its removal |
</threat_model>

<verification>
- SOAK-LOG.md exists at .planning/phases/13.8-packaging-and-ga-tag/SOAK-LOG.md
- Daily install-smoke checklist documented (operator can run as a single bash block)
- OPERATOR-RUN ONLY banner present at the top of the daily-check section (verify-block greps for the literal phrase)
- Metrics-tracking table covers PyPI / npm / Docker / GitHub stars / issues / Discussions
- Issue-triage table with severity + action + resolution columns
- Soak-verdict template with GREEN/YELLOW/RED gates + decision tree
- Day 0 entry filled in from Plan 13.8-06 verification log
- This plan halts at checkpoint:human-action — no automation compresses 7-14 calendar days
- TDD discipline: docs-only soak-window-scaffold plan per CLAUDE.md §TDD Discipline item #7 doc-only-plan exemption — single `chore:` / `docs:` commit; validated by operator at end of window
- Throughput regression-gate: N/A (operational; no code changes)
</verification>

<success_criteria>
1. SOAK-LOG.md scaffold landed with Day-0 entry, daily-check protocol, metrics-tracking, issue-triage, soak-verdict template
2. OPERATOR-RUN ONLY banner present so daily-check commands are never auto-executed by GSD agents
3. Operator runs daily install-smokes for 7-14 calendar days and appends entries
4. Operator writes soak verdict (GREEN / YELLOW / RED) at end of window
5. If GREEN: Plan 13.8-10 (closure) unblocks announcement
6. If YELLOW/RED: v0.0.1 patch path + restart soak (this plan re-fires)
7. NO marketing published during soak (D-02 strict; verified by operator self-attestation in verdict block)
8. CLAUDE.md §TDD Discipline item #7 doc-only-plan exemption invoked verbatim
</success_criteria>

<output>
After completion (post-soak-verdict), create `.planning/phases/13.8-packaging-and-ga-tag/13.8-09-SUMMARY.md` with: soak duration in days; verdict + rationale; issue counts by severity; metric deltas (Day 0 → Day N); whether v0.0.1 patch was triggered (and link to its Plan if so); decision-tree branch taken.
</output>
</content>
