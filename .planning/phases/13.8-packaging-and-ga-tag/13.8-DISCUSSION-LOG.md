# Phase 13.8 — Discussion Log (batch-discuss 2026-05-04)

Captured during a 4-phase batch-discuss session. This log captures the 6 decisions affecting Phase 13.8.

## 8-A — First public tag

**Question:** First public release tag?

**Options presented:**
- (a) `v0.0.0` (Recommended).
- (b) `v0.0.1` (skip the .0 hotfix slot).
- (c) `v0.1.0`.

**User selection:** (a) `v0.0.0`.

**Notes / follow-ups:** Standard semver-zero pattern (DuckDB, bun). Tag space: `v0.0.0` → `v0.0.x` patches → `v0.1.0` next milestone → `v1.0.0` years out.

---

## 8-B — Release strategy

**Question:** Release strategy for v0.0.0?

**Options presented:**
- (a) `v0.0.0-rc.1` → 3-7d soak → `v0.0.0` (Recommended).
- (b) Ship-and-post directly — fastest; HN top comment risk.
- (c) Soft-launch v0.0.0 — publish + WAIT 1-2wk before HN post.

**User selection:** (c) Soft-launch v0.0.0.

**Notes / follow-ups:** No `rc.N` tags. Tag `v0.0.0` final on day 0; publish all 4 channels; post nothing; allow ~7-14 days organic-finder soak; fix into `v0.0.1` if any channel install fails; announce when install paths are battle-tested. Lower-stress launch week.

---

## 8-C — Wheel build matrix

**Question:** Which platforms does the v0.0.0 PyPI wheel matrix cover?

**Options presented:**
- (a) ROADMAP-3 + macOS Intel x86_64 = 4 wheels (Recommended).
- (b) ROADMAP exact — 3 wheels only.
- (c) Add Windows x86_64 too — 5 wheels.
- (d) Linux-only — 2 wheels.

**User selection:** (a) 4 wheels (Linux x86_64 + Linux ARM64 + macOS ARM64 + macOS Intel x86_64).

**Notes / follow-ups:** Windows + Linux musl deferred to v0.1+.

---

## 8-D — Container registry

**Question:** Where does the multi-arch beava Docker image publish for v0.0.0?

**Options presented:**
- (a) Docker Hub primary + ghcr.io mirror (Recommended).
- (b) Docker Hub only (per ROADMAP).
- (c) ghcr.io only.

**User selection:** (a) Docker Hub + ghcr.io mirror.

**Notes / follow-ups:** Pre-launch user-action: claim Docker Hub `beava` namespace. Same multi-arch manifest pushed to both registries.

---

## 8-E — npm package scope

**Question:** npm package name for the TypeScript SDK?

**Options presented:**
- (a) `@beava/sdk` (claim `beava` npm org) (Recommended).
- (b) `@beava-dev/sdk` (matches GitHub org).
- (c) `beava-sdk` (unscoped fallback).

**User selection:** (a) `@beava/sdk`.

**Notes / follow-ups:** Pre-launch user-action: claim npm org `beava` + verify `hoang@beava.dev` for it. If `beava` org is taken on npm, fall back to `@beava-dev/sdk` (matches GitHub org); user's first preference is `@beava/sdk`.

---

## 8-F — Marketing-asset scope

**Question:** What marketing assets ship with the v0.0.0 announcement (~2wk after soft-launch)?

**Options presented:**
- (a) README hero + HN Show post + Twitter announcement + dev.to crosspost (Recommended).
- (b) ROADMAP-minimum: README + HN + Twitter (3 assets).
- (c) Add Reddit + Lobsters too (6 assets).
- (d) Just the README hero + HN — 2 assets, surgical.

**User selection:** (a) 4 assets — README + HN + Twitter + dev.to.

**Notes / follow-ups:** Reddit / Lobsters / podcasts excluded; can add v0.0.x or v0.1+ if HN signal is positive. Marketing asset drafts (HN post / Twitter thread / dev.to writeup) are committed to PRIVATE DEV BRANCH as `MARKETING-DRAFTS.md` for review-then-publish; NEVER committed to the public repo (would be weird "we're posting this in 2 weeks" leak otherwise).

---

## Deferred ideas surfaced during discuss

- Windows x86_64 wheel — v0.1+ if demand.
- Linux musl wheel — v0.1+ if Alpine users surface.
- Reddit / Lobsters / podcast outreach — v0.0.x or v0.1+ if HN signal is positive.
- v0.0.1 patch tag — only triggered if soak surfaces install bugs.
- Migration guides from competitor systems — v0.1+ marketing content.
- Cross-SDK conformance harness publication — DEFER per Phase 13.7.5 D-03.
- Premium / commercial-tier — explicitly out of v0 scope.

## Claude's Discretion (planner picks)

- Plan numbering and wave shape (~6-8 plans across 3 waves; planner picks).
- Wheel-build tooling — `cibuildwheel` vs maturin vs hand-rolled (planner picks per pyproject.toml current state).
- Trusted Publishers vs token for npm — modern OSS prefers Trusted Publishers; planner picks per existing setup.
- Exact HN / Twitter / dev.to wording — drafted in `MARKETING-DRAFTS.md`; user reviews + finalizes at announcement time.
- Smoke-test recipe per channel — planner picks (likely `pip install`, `npm install`, `go get`, `docker pull` from a fresh tmpdir + run a 3-line "register → push → get" script per platform).
- README hero exact code example — planner picks (probably a 10-line curl-only flow lifted from `docs/quickstart.md`).

---

*Discussion conducted via AskUserQuestion (single-question turns) on 2026-05-04. 6 decisions locked + 2 logistics-autonomous. 1 user override on 7.6-E flowed into D-05 here as constraint (CLAUDE.md not in repo → invariants not in CONTRIBUTING.md → Phase 13.8 README hero stays product-focused, not invariant-focused).*
