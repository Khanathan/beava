---
phase: 13.8
plan: 06
type: execute
wave: 3
depends_on: [13.8-05]
files_modified:
  - Cargo.toml
  - python/pyproject.toml
  - sdk/typescript/package.json
  - CHANGELOG.md
autonomous: false
requirements: [D-01, D-02]

must_haves:
  truths:
    - "All version-bearing manifests bumped to v0.0.0: Cargo.toml workspace.package.version, python/pyproject.toml [project] version, sdk/typescript/package.json version (already 0.0.0 from Phase 13.6 — verify, no change), sdk/go/go.mod (Go has no version field; covered by tag form)"
    - "CHANGELOG.md has a v0.0.0 section summarizing v0 OSS launch contents — section header `## v0.0.0` is awk-parseable in the format `release.yml` consumes (tested in Task 6.a item #10)"
    - "Pre-launch checklist verified: beava-dev GitHub org exists; hoang@beava.dev verified; Docker Hub `beava` namespace claimed; npm org `beava` claimed (or fallback to `@beava-dev/sdk` documented); Cloudflare Pages dashboard auth + DNS for beava.dev (deferred from Phase 13.7); 13.5.1 + 13.7.5 + 13.7.6 all CLOSED; PyPI `beava` package shows ZERO published versions (no monotonicity collision); npm `@beava/sdk` shows zero published versions"
    - "BOTH tags pushed: `v0.0.0` (main) AND `sdk/go/v0.0.0` (Go submodule, per Plan 13.8-03 contract)"
    - "release.yml execution observed GREEN across all 8 jobs; per-channel install verified on a clean machine (the executor's dev box is acceptable as the 'clean machine' for v0 — full 4-platform fresh-VM smoke is the soak (Plan 13.8-09) responsibility)"
    - "After tag push, `pip install beava` from a fresh venv produces a working `bv.App(test_mode=True)`; `npm install @beava/sdk` resolves; `docker pull beavadev/beava:v0.0.0` pulls the multi-arch image; `go get github.com/beava-dev/beava/sdk/go@v0.0.0` resolves"
  artifacts:
    - path: "Cargo.toml"
      provides: "workspace.package.version = \"0.0.0\""
      contains: "version = \"0.0.0\""
    - path: "python/pyproject.toml"
      provides: "[project] version = \"0.0.0\""
      contains: "version = \"0.0.0\""
    - path: "CHANGELOG.md"
      provides: "v0.0.0 release notes section"
      min_lines: 30
  key_links:
    - from: "git tag v0.0.0 + git tag sdk/go/v0.0.0"
      to: ".github/workflows/release.yml"
      via: "tag push fires release.yml; orchestrator dispatches all 5 publish/verify jobs"
      pattern: "git push --tags"
    - from: "release.yml execution"
      to: "PyPI + npm + Docker Hub + ghcr.io + GitHub Releases + pkg.go.dev"
      via: "5 parallel publish jobs with secrets passthrough"
      pattern: "release-summary"
---

<objective>
Bump all version manifests to v0.0.0, verify the pre-launch checklist (incl. PyPI/npm version-monotonicity defensive check + CHANGELOG awk-parseability), push the v0.0.0 + sdk/go/v0.0.0 tags, observe release.yml run GREEN, and verify each channel installs cleanly. This is the day-0 publish step of D-02 soft-launch — no announcement is posted yet.

Purpose: This plan is the actual publish-button-press for v0.0.0. Plans 13.8-01..05 built the publish machinery; this plan turns the key. CONTAINS 2 USER CHECKPOINTS for irreversible-action verification (pre-launch checklist sign-off + post-tag-push channel-verification sign-off).

Output: 4 manifest version bumps committed; CHANGELOG.md v0.0.0 entry; v0.0.0 + sdk/go/v0.0.0 tags pushed; release.yml run GREEN; all 5 channels verifiably installable.

**TDD discipline note:** This plan is a closure / publish-orchestration plan whose code-touching tasks are limited to YAML/version-string edits (no logic changes). Per **CLAUDE.md §TDD Discipline item #7** (doc-only-plan exemption), Tasks 6.b, 6.c, 6.e ship as `chore:` / `docs:` commits without paired `test:` commits — version bumps + tag pushes are validated end-to-end by Plan 13.8-05 release.yml's per-channel smoke jobs (an upstream plan that already shipped per-task red→green pairs per item #1) and by the post-publish channel-verification checkpoint Task 6.d. The exemption is invoked verbatim per item #7's positive-enumeration requirement so plan-checker can grep it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13.8-packaging-and-ga-tag/13.8-CONTEXT.md
@CLAUDE.md
@Cargo.toml
@python/pyproject.toml
@sdk/typescript/package.json
@sdk/go/go.mod
@.github/workflows/release.yml

<interfaces>
<!-- Current versions (to be bumped). -->

- Cargo.toml: `version = "0.1.0"` → bump to `"0.0.0"`
- python/pyproject.toml: `version = "0.3.0"` → bump to `"0.0.0"` (the historical 0.3.0 was an internal/test build; v0.0.0 is the FIRST PUBLIC release per D-01)
- sdk/typescript/package.json: `version = "0.0.0"` (already correct from Phase 13.6 — verify, no change)
- sdk/go/go.mod: no version field (Go modules version via tag only; covered by `sdk/go/v0.0.0` tag push)

The semver-zero pattern (D-01): walking `python/pyproject.toml` BACKWARD from 0.3.0 → 0.0.0 is unusual but consistent with the user-locked decision that v0.0.0 is the first PUBLIC tag and prior versions were never published to PyPI. **PyPI/npm version-monotonicity defensive check** (Task 6.a item #9) verifies the package name shows ZERO published versions on both registries — if any version was ever published, PyPI permanently blocks re-uploading versions ≤ max(published), so v0.0.0 publish would fail with `400 File already exists` and the operator would need to either revise the tag to a version > max(published) or change the package name. Pre-validation of the `beava` PyPI name (curl returned 404 at plan-write time) and `@beava/sdk` npm name (npm view returned 404 at plan-write time) suggests both names are clean, but the checklist re-verifies at execute time.
</interfaces>

<reference>
- D-01: First public tag = v0.0.0 (USER-LOCKED).
- D-02: Soft-launch — publish day 0, announce ~7-14 days later. NO marketing posted in this plan.
- Pre-launch checklist (CONTEXT.md `<estimate>` block): 8 items required GREEN before tag push; this plan extends to 10 items adding (9) PyPI/npm version-monotonicity check and (10) CHANGELOG awk-parseability.
- CONTEXT carrying_forward: Phases 13.5.1 + 13.7.5 + 13.7.6 + 13.5.1 (acceptance tests + transport impl) MUST be CLOSED before this plan executes.
- Plan 13.8-03: dual-tag form (`v0.0.0` for main; `sdk/go/v0.0.0` for Go submodule).
- **CLAUDE.md §TDD Discipline item #7** (doc-only-plan exemption) — invoked for code-light tasks in this plan; cited verbatim here so plan-checker greps for the exact phrase.
</reference>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6.a (checkpoint): Pre-launch checklist sign-off (BEFORE any version bump)</name>
  <what-built>
    Plans 13.8-01 through 13.8-05 are CLOSED with workflow_dispatch validations GREEN. The publish machinery is ready to fire on tag push. This checkpoint verifies the human-side prerequisites (10 items) are in place before triggering an irreversible publish.
  </what-built>
  <how-to-verify>
    Verify each item by visiting the linked URL and confirming. Reply `approved` to proceed; reply with the specific blocker(s) to abort.

    1. **GitHub org**: https://github.com/beava-dev — exists; hoang@beava.dev is owner/admin.
    2. **GitHub email**: https://github.com/settings/emails — `hoang@beava.dev` listed and verified.
    3. **Docker Hub namespace**: https://hub.docker.com/u/beava (or hub.docker.com/orgs) — `beava` namespace claimed; hoang@beava.dev associated. (Fallback: `beavadev` namespace if `beava` is taken.)
    4. **npm org**: https://www.npmjs.com/org/beava — `beava` org claimed; hoang@beava.dev verified. (Fallback per CONTEXT D-05: `@beava-dev/sdk` if `beava` org is taken — operator MUST update sdk/typescript/package.json `name` field at this checkpoint AND in this plan's commit if fallback is needed.)
    5. **Cloudflare Pages**: dashboard auth completed for beava.dev; DNS A/CNAME pointing at Cloudflare Pages app. (Deferred from Phase 13.7; not strictly needed for v0.0.0 publish but listed in checklist.)
    6. **GitHub repo secrets**: https://github.com/beava-dev/beava/settings/secrets/actions — confirms presence of: `PYPI_API_TOKEN`, `NPM_TOKEN` (or Trusted Publishers configured per Plan 13.8-02 fallback path), `DOCKERHUB_USERNAME`, `DOCKERHUB_TOKEN`. (`GITHUB_TOKEN` is auto-injected; not in this list.)
    7. **Phases 13.5.1 / 13.7.5 / 13.7.6 closed**: confirm by `cat .planning/STATE.md | head -50` showing all three phases marked CLOSED with PASS verdicts. (If any are still in flight, abort this plan — Phase 13.8 cannot tag v0.0.0 with a blocking dependency open.)
    8. **Working tree clean + on `main`**: `git status` clean; `git branch --show-current` returns `main` (Phase 13.7.6 D-03 repo rename + branch rename `v2/greenfield → main` must have completed).
    9. **PyPI + npm version-monotonicity check (defensive)**: Verify https://pypi.org/project/beava/ shows ZERO published versions (the package name has never been uploaded). If any version exists on PyPI, escalate to user — the v0.0.0 tag must be revised to a version > max(published) or the PyPI package name must change. Same check for `@beava/sdk` on npmjs.com (`npm view @beava/sdk` → expected 404 / E404).

       Run these defensive checks at execute time:
       ```bash
       # PyPI
       curl -fsS -o /dev/null -w "%{http_code}\n" https://pypi.org/pypi/beava/json
       # Expected: 404 (package name has never been published).
       # If 200 — STOP. The first JSON field `info.version` is max(published).
       # PyPI blocks re-uploading any version ≤ max(published). v0.0.0 publish will fail.
       # Resolution: bump tag to next-greater version OR change package name to something untaken.

       # npm
       npm view @beava/sdk versions 2>&1 | head -5
       # Expected: "npm error code E404" (package name has never been published).
       # If output lists versions — STOP. npm allows publishing any version not already
       # in the list, so v0.0.0 is OK only if it's not in the list. If it IS, bump.
       ```
       At plan-write time both checks returned 404 (clean names). At execute-time, re-verify.

    10. **CHANGELOG.md awk-parseability**: After Task 6.b lands the v0.0.0 entry, but BEFORE proceeding to tag push (Task 6.c), verify that `release.yml` can extract the v0.0.0 section by awk:
        ```bash
        awk '/^## v0.0.0/,/^## /' /Users/petrpan26/work/tally/CHANGELOG.md | head -50
        ```
        Output MUST start with `## v0.0.0` and end either at EOF or at the next `## ` (an older version section). If the awk extraction returns empty or malformed output, fix the CHANGELOG header format (the `^## v0.0.0` literal pattern is what release.yml's release-notes-extraction step keys on).

        Note: this item is verified AFTER Task 6.b commits the CHANGELOG entry; checkpoint approval covers items 1-9 pre-bump and 10 post-bump (operator re-runs the awk gate as the final pre-tag check).

    If items 1-9 pass, reply `approved` — executor proceeds to Task 6.b (version bump + CHANGELOG entry). After Task 6.b lands, operator runs the item #10 awk gate locally and confirms (reply `changelog ok`) before Task 6.c pushes the tag. If ANY item fails, reply with details — executor aborts and blocks until the operator resolves.
  </how-to-verify>
  <resume-signal>Reply `approved` (items 1-9 GREEN; item #10 verified post-bump) OR describe specific blockers.</resume-signal>
</task>

<task type="auto">
  <name>Task 6.b: Bump version manifests + CHANGELOG.md v0.0.0 entry</name>
  <files>Cargo.toml, python/pyproject.toml, sdk/typescript/package.json, CHANGELOG.md</files>
  <action>
    1. **Cargo.toml**: change `[workspace.package]` `version = "0.1.0"` → `version = "0.0.0"`.
    2. **python/pyproject.toml**: change `[project]` `version = "0.3.0"` → `version = "0.0.0"`. Update classifier `Development Status :: 3 - Alpha` → `Development Status :: 4 - Beta` (matches v0 OSS-cut framing per D-01).
    3. **sdk/typescript/package.json**: verify `version = "0.0.0"` (already correct from Phase 13.6); no change. If fallback to `@beava-dev/sdk` was triggered at the checkpoint, update `name` field here.
    4. **CHANGELOG.md**: prepend a v0.0.0 section (~30-50 lines) summarizing:
       ```markdown
       ## v0.0.0 — 2026-MM-DD (first public OSS release)

       ### Highlights
       - Single-binary real-time feature server. Apache-2.0.
       - HTTP API (axum) + custom-framed TCP fast path on the runtime defaults extracted in Plan 13.8-04 Task 4.0.
       - Python SDK (`pip install beava`), TypeScript SDK (`npm install @beava/sdk`), Go SDK (`go get github.com/beava-dev/beava/sdk/go`), Docker image (`docker pull beavadev/beava:v0.0.0`).
       - 53 aggregation operators across 7 families (core / sketch / point-ordinal / recency / decay / velocity / buffer-geo).
       - Events-only surface (no joins, no event-time, no tables). Tables for aggregation-output return in v0.1+.
       - 3M+ events/sec/core sustained throughput; P99 batch-get < 10ms.

       ### Wire spec
       - JSON Schema 2020-12 contracts for all opcodes + endpoints.
       - Verb-style HTTP routes (POST /push/:event_name, /get, /batch_get, /reset, /ping).
       - 5 binary opcodes (OP_PUSH, OP_GET, OP_BATCH_GET, OP_REGISTER, OP_RESET).

       ### Compatibility
       - Python ≥ 3.10. Node ≥ 18. Go ≥ 1.21. Docker linux/amd64 + linux/arm64.
       - Wheels: manylinux2014_x86_64, manylinux2014_aarch64, macosx_11_0_arm64, macosx_10_9_x86_64.

       ### Deferred to v0.1+
       - Windows + Alpine wheels; tables (modification + retraction); joins; session windows; async API; Java SDK; cross-SDK conformance harness; migration guides from competitor systems.

       ### Acknowledgments
       - Thank you to early testers and the OSS streaming community.

       ### Documentation
       - Quickstart: https://beava.dev/docs/quickstart
       - Operators catalogue: https://beava.dev/docs/operators
       - Wire spec: https://beava.dev/docs/wire-spec
       - GitHub: https://github.com/beava-dev/beava
       - Issues: https://github.com/beava-dev/beava/issues
       ```
       If CHANGELOG.md doesn't exist, create it with this v0.0.0 section + standard "Keep a Changelog" preamble. Header MUST be exactly `## v0.0.0 — ...` so the Task 6.a item #10 awk gate (`awk '/^## v0.0.0/,/^## /'`) extracts cleanly.

    5. Run `cargo check --workspace --locked` to verify the version bump didn't break Cargo.lock — if Cargo.lock needs updating, `cargo update --workspace` and commit the lock changes.
    6. Run `cargo test --workspace -p beava-server --tests` (smoke; full test suite is verified by release.yml) — confirm no version-specific tests fail.

    Per CLAUDE.md §TDD Discipline item #7 (doc-only-plan exemption): commit as `chore(13.8-06): bump versions to v0.0.0 + CHANGELOG.md (D-01)` without a paired `test:` commit. Validation flows from Plan 13.8-05's release.yml smoke jobs (already shipped with red→green pairs per item #1) + Task 6.a item #10 CHANGELOG awk gate.
  </action>
  <verify>
    <automated>grep -E '^version = "0\\.0\\.0"' /Users/petrpan26/work/tally/Cargo.toml | head -1 | grep -q "0.0.0" || (echo "ERR: Cargo.toml not bumped to 0.0.0" &amp;&amp; exit 1)</automated>
    <automated>grep -E '^version = "0\\.0\\.0"' /Users/petrpan26/work/tally/python/pyproject.toml | head -1 | grep -q "0.0.0" || (echo "ERR: pyproject.toml not bumped to 0.0.0" &amp;&amp; exit 1)</automated>
    <automated>grep -c "## v0.0.0" /Users/petrpan26/work/tally/CHANGELOG.md | (read n; [ "$n" -ge 1 ] || (echo "ERR: CHANGELOG.md missing v0.0.0 section" &amp;&amp; exit 1))</automated>
    <automated>awk '/^## v0.0.0/,/^## /' /Users/petrpan26/work/tally/CHANGELOG.md | wc -l | awk '{ if ($1 &lt; 10) { print "ERR: awk extraction of v0.0.0 section returned &lt;10 lines; check header format"; exit 1 } }'</automated>
    <automated>cd /Users/petrpan26/work/tally &amp;&amp; cargo check --workspace --locked 2>&amp;1 | tail -1 | grep -E "Finished|warning" || (echo "ERR: cargo check failed after version bump" &amp;&amp; exit 1)</automated>
  </verify>
  <done>4 manifests bumped or verified at v0.0.0; CHANGELOG.md v0.0.0 section ≥ 30 LOC; awk-extraction of `## v0.0.0` block returns ≥ 10 lines (release.yml release-notes parser will succeed); cargo check GREEN; commit landed.</done>
</task>

<task type="auto">
  <name>Task 6.c: Push v0.0.0 + sdk/go/v0.0.0 tags + observe release.yml run</name>
  <files>(no files modified — tag push is the action)</files>
  <action>
    Per CLAUDE.md §TDD Discipline item #7 (doc-only-plan exemption): tag push is an irreversible operational action with no testable code surface; the validation is per-channel install smokes captured in Task 6.c step 5 + Task 6.d post-publish channel-verification. Commit-message-only step (no test pairing required).

    1. Verify clean tree + on main + Task 6.b committed:
       ```bash
       git status
       git branch --show-current
       git log -1 --pretty=format:"%h %s"  # MUST show "chore(13.8-06): bump versions to v0.0.0..."
       ```
    2. Create + push the main release tag:
       ```bash
       git tag -a v0.0.0 -m "Beava v0.0.0 — first public OSS release"
       git push origin v0.0.0
       ```
    3. Create + push the Go submodule tag (per Plan 13.8-03 contract — same commit, different ref name):
       ```bash
       git tag -a sdk/go/v0.0.0 -m "Beava Go SDK v0.0.0 (submodule tag)"
       git push origin sdk/go/v0.0.0
       ```
    4. Watch release.yml:
       ```bash
       sleep 10  # let GHA pick up the tag push
       gh run list --workflow=release.yml --limit 2  # MUST show 2 in-progress runs (one per tag)
       gh run watch <run-id-for-v0.0.0>
       ```
    5. Per-channel verification (when release.yml run completes GREEN):
       ```bash
       # PyPI
       python -m venv /tmp/smoke-pypi && source /tmp/smoke-pypi/bin/activate
       pip install beava==0.0.0
       python -c "import beava as bv; app = bv.App(test_mode=True); print('PyPI smoke ok:', bv.__version__)"
       deactivate

       # npm
       cd /tmp && mkdir smoke-npm && cd smoke-npm
       npm init -y &gt;/dev/null
       npm install @beava/sdk@0.0.0
       node --input-type=module -e "import {App} from '@beava/sdk'; console.log('npm smoke ok:', typeof App)"

       # Docker
       docker pull beavadev/beava:v0.0.0
       docker run --rm beavadev/beava:v0.0.0 --help &gt;/dev/null  # binary exec sanity
       docker pull ghcr.io/beava-dev/beava:v0.0.0  # mirror reachable
       docker run --rm ghcr.io/beava-dev/beava:v0.0.0 --help &gt;/dev/null

       # Go
       cd /tmp && mkdir smoke-go && cd smoke-go
       go mod init smoke
       go get github.com/beava-dev/beava/sdk/go@v0.0.0
       cat &gt; main.go &lt;&lt;'EOF'
       package main
       import (
           "fmt"
           beava "github.com/beava-dev/beava/sdk/go"
       )
       func main() {
           app, err := beava.NewApp(&amp;beava.Config{TestMode: true})
           if err != nil { panic(err) }
           fmt.Println("Go smoke ok:", app != nil)
       }
       EOF
       go build &amp;&amp; ./smoke

       # GitHub Release
       gh release view v0.0.0  # MUST show 3 binary attachments + sha256 sidecars + CHANGELOG body
       ```
    6. Capture per-channel exit-codes + outputs in a log saved to `.planning/phases/13.8-packaging-and-ga-tag/13.8-06-VERIFICATION.log` (append, don't overwrite).
    7. **If ANY channel fails the smoke**: tag is already public — DO NOT delete (PyPI does not allow deletion of yanked versions; npm allows `npm unpublish` only within 72h with caveats; Docker tags can be re-pushed but `latest` is sticky). The fix path per CONTEXT D-02: Plan 13.8-09 soak surfaces install bugs → patch in v0.0.1. This task captures the failure mode and proceeds; Plan 13.8-08 (closure) decides whether to immediately re-tag v0.0.1 or proceed to soak as v0.0.0-with-known-issue.
    8. **If ALL channels GREEN**: commit a verification log entry with `chore(13.8-06): v0.0.0 tag published + per-channel smoke verified`.
  </action>
  <verify>
    <automated>git -C /Users/petrpan26/work/tally tag --list v0.0.0 | grep -q v0.0.0 || (echo "ERR: v0.0.0 tag not created" &amp;&amp; exit 1)</automated>
    <automated>git -C /Users/petrpan26/work/tally tag --list sdk/go/v0.0.0 | grep -q sdk/go/v0.0.0 || (echo "ERR: sdk/go/v0.0.0 tag not created" &amp;&amp; exit 1)</automated>
    <automated>cd /Users/petrpan26/work/tally &amp;&amp; gh run list --workflow=release.yml --limit 1 --json status,conclusion --jq '.[0].conclusion' | grep -E "^success$" || (echo "ERR: release.yml v0.0.0 run did not succeed; check logs and consider v0.0.1 patch path" &amp;&amp; exit 1)</automated>
    <automated>test -f /Users/petrpan26/work/tally/.planning/phases/13.8-packaging-and-ga-tag/13.8-06-VERIFICATION.log || (echo "ERR: per-channel verification log missing" &amp;&amp; exit 1)</automated>
  </verify>
  <done>Both tags pushed; release.yml run GREEN end-to-end; all 5 channels (PyPI, npm, Docker Hub, ghcr.io, Go, GitHub Releases) verifiably installable; verification log written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6.d (checkpoint): Post-publish channel verification sign-off</name>
  <what-built>
    Task 6.c pushed v0.0.0 + sdk/go/v0.0.0 tags; release.yml ran GREEN; per-channel smokes captured to 13.8-06-VERIFICATION.log. Before declaring v0.0.0 published and entering the soak window (Plan 13.8-09), the human verifies the dev-box smoke matches expected behavior.
  </what-built>
  <how-to-verify>
    Reply `approved` to enter the 7-14 day soak window (Plan 13.8-09). Reply with specific failures to trigger the v0.0.1 hotfix path (Plan 13.8-08 closure decides).

    1. **Visit https://pypi.org/project/beava/0.0.0/** — package page renders; description shows the v0 pitch; "Project links" point at github.com/beava-dev/beava.
    2. **Visit https://www.npmjs.com/package/@beava/sdk/v/0.0.0** — package page renders; provenance attestation badge visible; tarball size ≤ 100 KB.
    3. **Visit https://hub.docker.com/r/beavadev/beava** — both `v0.0.0` and `latest` tags visible; "Architecture" column shows linux/amd64 + linux/arm64 for v0.0.0.
    4. **Visit https://github.com/beava-dev/beava/pkgs/container/beava** — ghcr.io mirror visible; `v0.0.0` + `latest` tags both present.
    5. **Visit https://pkg.go.dev/github.com/beava-dev/beava/sdk/go@v0.0.0** — pkg.go.dev page renders the doc.go banner; the version selector shows `v0.0.0` (proxy may take 1-30 minutes to crawl after tag push; if not yet visible, wait then refresh).
    6. **Visit https://github.com/beava-dev/beava/releases/tag/v0.0.0** — GitHub Release page renders; CHANGELOG body visible; 3 platform binaries + sha256 attached.

    If ALL 6 pages render correctly, reply `approved` — Plan 13.8-08 (marketing drafts) + Plan 13.8-09 (soak) proceed.

    If ANY fail, reply with the specific failure — orchestrator decides:
    - PyPI / npm / Docker Hub publish-failed (no version on registry) → re-trigger release.yml or open `gh issue` in beava-dev/beava and patch in v0.0.1
    - Page renders but content wrong → cosmetic; defer to v0.0.x
    - pkg.go.dev not yet showing → wait up to 30 min; if still missing, manually `goproxy` cache: `GOPROXY=proxy.golang.org go list -m github.com/beava-dev/beava/sdk/go@v0.0.0` to force-fetch
  </how-to-verify>
  <resume-signal>Reply `approved` (all 6 channel pages render correctly) OR describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator local machine → GitHub | tag push is operator-credentialed (SSH key / GitHub CLI auth) |
| Tag push → release.yml → 5 channels | secrets passthrough handled by Plan 13.8-05; this plan is the trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.8-06-01 | T (Tampering) | irreversible publish | mitigate | Pre-launch checklist (Task 6.a, 10 items incl. version-monotonicity + CHANGELOG awk-parseability) gates the publish; soft-launch (D-02) gives a 7-14d soak before any announcement; PyPI / npm / Docker tags survive a bad publish (recovery = v0.0.1 patch, not delete) |
| T-13.8-06-02 | E (Elevation) | tag-push privilege | accept | Operator (hoang@beava.dev) is the only org-admin with tag-push privilege at this stage; future tag pushes can be locked behind tag-protection rules |
| T-13.8-06-03 | R (Repudiation) | release.yml audit trail | mitigate | release-summary job (Plan 13.8-05) emits per-channel status to GHA step summary; verification log (this plan) captures dev-box smoke; publish events are tied to specific GHA run IDs |
| T-13.8-06-04 | I (Information disclosure) | secrets exposure | mitigate | Per-channel secrets pass through workflow_call secrets: blocks (Plan 13.8-05); operator never directly handles tokens during this plan |
</threat_model>

<verification>
- 4 version manifests at v0.0.0 (Cargo.toml, pyproject.toml, package.json verified, go.mod NA)
- CHANGELOG.md v0.0.0 section ≥ 30 LOC; awk-extraction (`awk '/^## v0.0.0/,/^## /'`) returns ≥ 10 lines (release.yml release-notes parser succeeds)
- Pre-launch checklist (Task 6.a, 10 items) verified by human
- PyPI `beava` shows zero published versions (404 on JSON endpoint); npm `@beava/sdk` shows zero published versions (E404)
- v0.0.0 + sdk/go/v0.0.0 tags pushed to origin
- release.yml run GREEN across all 8 jobs
- Per-channel smoke verified on dev-box: pip install / npm install / docker pull / go get / gh release view all succeed
- Post-publish channel pages (Task 6.d) verified by human
- Verification log captured at .planning/phases/13.8-packaging-and-ga-tag/13.8-06-VERIFICATION.log
- TDD discipline: Tasks 6.b / 6.c / (post-publish chore commit) follow CLAUDE.md §TDD Discipline item #7 doc-only-plan exemption — `chore:` / `docs:` commits without paired `test:` commits; validation flows from Plan 13.8-05 smoke jobs + Task 6.d
- Throughput regression-gate: deferred to Plan 13.8-10 closure (1 row append; packaging shouldn't change perf, but the regression-gate cell verifies)
</verification>

<success_criteria>
1. All version manifests at v0.0.0 (Cargo.toml, pyproject.toml; package.json verified; go.mod tag-driven)
2. CHANGELOG.md has a v0.0.0 entry summarizing v0 OSS launch contents (awk-parseable in release.yml format)
3. Pre-launch checklist (10 items) signed off by human
4. v0.0.0 + sdk/go/v0.0.0 tags exist on origin
5. release.yml run GREEN across all 8 jobs
6. All 5 channels (PyPI / npm / Docker Hub / ghcr.io / Go / GitHub Releases) installable on dev box
7. Post-publish channel pages signed off by human
8. NO marketing posted, NO announcement made (D-02 soft-launch — Plans 13.8-08 / 13.8-09 own announcement timing)
9. CLAUDE.md §TDD Discipline item #7 doc-only-plan exemption invoked verbatim for code-light tasks
</success_criteria>

<output>
After completion, create `.planning/phases/13.8-packaging-and-ga-tag/13.8-06-SUMMARY.md` with: tag-push timestamps; release.yml run-ids (one per tag form); per-channel smoke results table (channel × dev-box pass/fail); URLs to the 6 channel pages (Task 6.d); any v0.0.0-known-issue items captured for Plan 13.8-09 soak window; PyPI/npm version-monotonicity check results from Task 6.a item #9.
</output>
</content>
