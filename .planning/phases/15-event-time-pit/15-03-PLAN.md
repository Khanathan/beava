---
phase: 15-event-time-pit
plan: 03
type: tdd
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - crates/beava-server/src/http.rs
  - crates/beava-server/src/temporal_http.rs
  - crates/beava-server/src/register.rs
  - crates/beava-persistence/src/snapshot.rs
  - crates/beava-core/benches/temporal_mvcc.rs
  - .planning/phases/15-event-time-pit/15-SUMMARY.md
  - .planning/phases/15-event-time-pit/15-VERIFICATION.md
  - .planning/perf-baselines.md
  - .planning/throughput-baselines.md
autonomous: true
requirements: ["PIT-07", "PIT-08", "PIT-09", "PIT-10"]
must_haves:
  truths:
    - "GET /table/{name}?as_of=<event_time_ms> is mounted ONLY when BEAVA_DEV_ENDPOINTS=1; returns 404 otherwise"
    - "POST /push-table and POST /retract remain production-mounted"
    - "Register-time 400 temporal_table_requires_event_time_field when a temporal table's feeding stream lacks event_time_field"
    - "Snapshot v1 (pre-Phase-15) rejected at load with SnapshotLoadError::IncompatibleSchema; v2 round-trips cleanly"
    - "Temporal criterion microbench records a new Phase 15 baseline row in perf-baselines.md"
    - "Throughput harness records a new Phase 15 row (temporal write + PIT lookup pipeline) in throughput-baselines.md"
  artifacts:
    - path: "crates/beava-server/src/http.rs"
      provides: "temporal_read_router gated inside dev_endpoints_enabled block"
      contains: "if dev_endpoints_enabled"
    - path: "crates/beava-persistence/src/snapshot.rs"
      provides: "snapshot_schema_version: u32 header field + v1-rejection load path"
      contains: "IncompatibleSchema"
    - path: "crates/beava-core/benches/temporal_mvcc.rs"
      provides: "event-time chain bench rows for lookup_at_event_time + upsert + late-upsert self-heal"
      contains: "lookup_at_event_time"
    - path: ".planning/phases/15-event-time-pit/15-SUMMARY.md"
      provides: "Phase 15 summary + dev-gate operator note"
    - path: ".planning/phases/15-event-time-pit/15-VERIFICATION.md"
      provides: "SC1-SC7 verification checklist with command transcripts"
  key_links:
    - from: "http.rs router assembly"
      to: "temporal_read_router"
      via: "only mounted inside the if dev_endpoints_enabled block"
      pattern: "temporal_read_router"
    - from: "snapshot load path"
      to: "IncompatibleSchema error"
      via: "header.snapshot_schema_version != 2 → hard error"
      pattern: "IncompatibleSchema"
---

<objective>
Lock down the public HTTP surface: move `GET /table` behind the dev gate; keep write primitives production-mounted; enforce the `event_time_field` requirement on streams feeding temporal tables at register time; bump the snapshot format to v2 with an explicit migration-reject test; add a Phase 15 criterion bench + throughput-harness row; write SUMMARY + VERIFICATION.

Purpose: Close the Phase 15 contract. After this plan lands, Phase 12 Plan 04 can call `lookup_at_event_time(key, event.event_time_ms)` and the public API is v0-stable.

Output: Full Phase 15 verification green; baselines updated; migration-reject test proving snapshot v1 no longer loads.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-event-time-pit/15-CONTEXT.md
@.planning/phases/15-event-time-pit/15-01-PLAN.md
@.planning/phases/15-event-time-pit/15-02-PLAN.md
@crates/beava-server/src/http.rs
@crates/beava-server/src/temporal_http.rs
@crates/beava-server/src/register.rs
@crates/beava-persistence/src/snapshot.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.a (red): Test GET /table is 404 without BEAVA_DEV_ENDPOINTS=1 + event_time_field enforcement</name>
  <files>crates/beava-server/src/temporal_http.rs, crates/beava-server/src/register.rs</files>
  <behavior>
    - Test `get_table_returns_404_when_dev_endpoints_disabled` — spin up server with `dev_endpoints_enabled=false`, register a temporal table + upsert a row, call `GET /table/T?key=k&as_of=123` → 404.
    - Test `get_table_returns_row_when_dev_endpoints_enabled` — same scenario with `dev_endpoints_enabled=true` → 200 + row.
    - Test `push_table_works_without_dev_endpoints` — `POST /push-table/T` with `dev_endpoints_enabled=false` → 200 (write primitive remains production-mounted).
    - Test `retract_works_without_dev_endpoints` — `POST /retract` with `dev_endpoints_enabled=false` → 200 (retract primitive remains production-mounted).
    - Test `register_rejects_temporal_table_with_feeder_stream_missing_event_time_field` — register stream S (no `event_time_field`) that feeds temporal table T → 400 `temporal_table_requires_event_time_field` naming S.
    - Test `as_of_query_param_is_event_time_ms_not_lsn` — temporal table, upsert at event_time=500; `GET /table/T?key=k&as_of=499` → 404; `as_of=500` → 200.
  </behavior>
  <action>
    Add the 6 tests. The first two will fail because the current router mounts `temporal_router` unconditionally (line 90 of http.rs). The next two should already pass (push_table + retract will stay mounted). The register test will fail (no such check). The as_of semantic test will fail because the handler currently treats `as_of` as LSN.

    Commit: `test(15-03): add failing tests for dev-gate, event_time_field enforcement, event-time as_of`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && cargo test -p beava-server --lib 2>&1 | tail -30</automated>
  </verify>
  <done>6 red tests committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.b (green): Split temporal router + event_time_field enforcement + as_of=event_time_ms</name>
  <files>crates/beava-server/src/http.rs, crates/beava-server/src/temporal_http.rs, crates/beava-server/src/register.rs</files>
  <behavior>
    - In `temporal_http.rs`: split `temporal_router()` into `temporal_write_router()` (mounts `POST /push-table/:name`, `POST /delete-table/:name`, `POST /retract`) and `temporal_read_router()` (mounts `GET /table/:name`).
    - In `http.rs`: mount `temporal_write_router()` wherever `temporal_router()` was mounted (production); move `temporal_read_router()` inside the `if dev_endpoints_enabled` block.
    - In `table_get_handler`: parse `as_of` as `i64` event_time_ms, call `store.lookup_at_event_time(&entity_key, as_of_ms)`, remove the LSN-based `u64::MAX` fallback logic.
    - In `push_table_handler`: extract `event_time_ms` from the body using the feeder stream's `event_time_field`. If the table is temporal and no `event_time_field` was resolved, return 400 (should be register-time prevented, but handler is belt-and-suspenders).
    - In `register.rs`: after schema commit, for each newly-registered or updated temporal table T, walk the feeder streams (same DAG walk Plan 15-02 built). If any feeder stream has `event_time_field == None`, reject the register with 400 `temporal_table_requires_event_time_field` naming the stream; the entire register transaction rolls back.
  </behavior>
  <action>
    Implement per behavior block. Wire `store.upsert(key, lsn, event_time_ms, row)` (the new Plan 15-01 signature) in `push_table_handler`. Green all 6 red tests.

    Commit: `feat(15-03): dev-gate GET /table + event_time_field enforcement + event-time as_of semantics`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && cargo test -p beava-server --lib 2>&1 | tail -30</automated>
  </verify>
  <done>All 6 tests green; `cargo test --workspace` green; `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.a (red): Tests for snapshot v1 rejection + v2 round-trip</name>
  <files>crates/beava-persistence/src/snapshot.rs</files>
  <behavior>
    - Test `snapshot_v2_roundtrip_preserves_event_time_chain` — build a TemporalStore with upserts at various `(event_time, lsn)` positions + one retraction, snapshot it, load it, verify bit-identical chain state.
    - Test `snapshot_v1_load_rejects_with_incompatible_schema` — construct a byte sequence with `snapshot_schema_version = 1` in the header (or no version field, simulating Phase 11.5's format); load → `Err(SnapshotLoadError::IncompatibleSchema { found: 1, required: 2 })`.
    - Test `snapshot_v2_header_field_present` — serialize a snapshot, inspect the first bytes of the header, confirm the version field is `2`.
  </behavior>
  <action>
    Construct a minimal v1 snapshot byte sequence manually (hard-coded buffer mimicking the Phase 11.5 positional bincode layout). Add tests. All three fail because the version field + rejection branch don't exist.

    Commit: `test(15-03): add failing tests for snapshot v2 round-trip + v1 rejection`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && cargo test -p beava-persistence --lib snapshot:: 2>&1 | tail -30</automated>
  </verify>
  <done>3 red tests committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.b (green): Bump snapshot format to v2 with schema version header</name>
  <files>crates/beava-persistence/src/snapshot.rs</files>
  <behavior>
    - Add `snapshot_schema_version: u32` as the first field of the snapshot header; current constant is `SNAPSHOT_SCHEMA_VERSION: u32 = 2`.
    - Change the serialized temporal-store chain payload from `BTreeMap<Lsn, MvccVersion>` with `wall_ms` to the new `BTreeMap<(i64, u64), MvccVersion>` with `event_time_ms` (Plan 15-01's new shape).
    - On load: read version first; if `!= 2`, return `SnapshotLoadError::IncompatibleSchema { found: <version>, required: 2 }` with message `"snapshot schema v{found} incompatible with beava-core v2; discard the snapshot and restart from WAL (pre-v0 breaking change; see Phase 15 CONTEXT D-07)"`.
    - Green all 3 red tests.
  </behavior>
  <action>
    Implement the header version byte + load-time reject. Update any snapshot-producer callers elsewhere in the codebase to emit v2. Grep for usages; typical call sites: snapshot writer in beava-persistence, recovery path in beava-server startup.

    Commit: `feat(15-03): snapshot v2 with (event_time, lsn) chain + v1 rejection at load`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && cargo test -p beava-persistence --lib 2>&1 | tail -30 && cargo test --workspace 2>&1 | tail -30</automated>
  </verify>
  <done>All 3 tests green; workspace tests green; snapshot header starts with version=2; v1 fixture byte sequence produces IncompatibleSchema error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.a (red): Criterion bench for lookup_at_event_time + late-upsert self-heal</name>
  <files>crates/beava-core/benches/temporal_mvcc.rs</files>
  <behavior>
    - Bench group `temporal_event_time`:
      - `upsert_in_order_1k_entities` — 1000 entities, 100 upserts each, strictly increasing event_time.
      - `upsert_out_of_order_1k_entities` — same volume, shuffled event_time arrival.
      - `lookup_at_event_time_shallow_chain` — 10-version chains, random `as_of`.
      - `lookup_at_event_time_deep_chain` — 1000-version chains, random `as_of`.
      - `late_upsert_self_heal` — 1000 entities, 99 in-order upserts + 1 late upsert at a random older event_time; measure the late-upsert insert path only.
  </behavior>
  <action>
    Extend `crates/beava-core/benches/temporal_mvcc.rs` with the new bench group. These benches need the Plan 15-01 API (`lookup_at_event_time`, new `upsert` signature) — they won't compile against pre-Plan-15-01 code, so they're effectively "red" until 15-01 landed. Since 15-01 and 15-02 are already merged by this point, this task lands the bench.

    Commit: `test(15-03): add event-time criterion bench group temporal_event_time`.
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && cargo bench -p beava-core --bench temporal_mvcc -- --test 2>&1 | tail -30</automated>
    Compiles and runs in test mode (single-iteration sanity).
  </verify>
  <done>Bench compiles and runs end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.b (green): Run bench + throughput + update baselines + write SUMMARY + VERIFICATION</name>
  <files>.planning/perf-baselines.md, .planning/throughput-baselines.md, .planning/phases/15-event-time-pit/15-SUMMARY.md, .planning/phases/15-event-time-pit/15-VERIFICATION.md, .planning/phases/15-event-time-pit/15-perf-row.md, .planning/phases/15-event-time-pit/15-throughput-row.md</files>
  <behavior>
    - Run `cargo bench -p beava-core --bench temporal_mvcc` full iterations; capture the 5 new `temporal_event_time` rows + re-measure the pre-existing Phase 11.5 rows (LSN-keyed variants have been removed, so their baselines retire).
    - Regression check: for the overlap benches (e.g., in-order upsert 1k), compare new event-time-keyed numbers to the 11.5 LSN-keyed baselines in the same hw-class. 10% slower = WARNING; 25% slower = BLOCKER (per Phase 6+ perf discipline). Composite key is modestly more expensive than u64 key; expect some cost here — document it.
    - Run `beava-bench` on the simple/medium/large pipeline shapes over HTTP + TCP (BATCH_MS per repo default). Append one row per (shape, transport) to `.planning/throughput-baselines.md` keyed to Phase 15.
    - Write `15-SUMMARY.md` covering what shipped, design decisions realized, test counts, perf deltas, operator notes (especially: `GET /table` is dev-only, set `BEAVA_DEV_ENDPOINTS=1` to exercise), known gaps.
    - Write `15-VERIFICATION.md` listing SC1-SC7 with check-marks, command transcripts, and any deviations.
    - Stage `15-perf-row.md` and `15-throughput-row.md` as phase-local artifacts (Phase 11.5 convention).
  </behavior>
  <action>
    Execute the benches. If WARNING-level regressions appear in any overlap bench, document them in `15-SUMMARY.md`'s perf section with a note that "composite (i64, u64) key vs u64 key is the expected cause; accepted per CONTEXT D-01". If BLOCKER-level appears, stop and flag — do not proceed with baseline commit.

    Commit separately:
    - `bench(15-03): record Phase 15 temporal_event_time baselines (perf + throughput)`
    - `docs(15-03): SUMMARY + VERIFICATION for Phase 15 event-time PIT`
  </action>
  <verify>
    <automated>cd /Users/petrpan26/work/tally && cargo test --workspace 2>&1 | tail -5 && test -s .planning/phases/15-event-time-pit/15-SUMMARY.md && test -s .planning/phases/15-event-time-pit/15-VERIFICATION.md && grep -q "Phase 15" .planning/perf-baselines.md && grep -q "Phase 15" .planning/throughput-baselines.md && echo OK</automated>
  </verify>
  <done>Baselines committed; SUMMARY + VERIFICATION committed; all SC1-SC7 checked off with evidence; any WARNING-level regression explicitly justified.</done>
</task>

</tasks>

<verification>
- `cargo test --workspace` green
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean
- `cargo fmt --all --check` clean
- `curl -s http://localhost:<port>/table/T?key=k&as_of=123` returns 404 when `BEAVA_DEV_ENDPOINTS` unset; returns row (or 404 for missing key) when set
- `.planning/perf-baselines.md` contains a Phase 15 section with 5+ rows
- `.planning/throughput-baselines.md` contains Phase 15 rows for small/medium/large × HTTP/TCP
- Snapshot v1 fixture loaded at server startup produces IncompatibleSchema error (manual recipe documented in SUMMARY)
</verification>

<success_criteria>
SC1 — `(event_time_ms, lsn)` chain verified via serde round-trip (Task 2.a/2.b) ✅
SC2 — Late upsert self-heal verified in bench + Plan 15-01 unit test ✅
SC3 — `GET /table?as_of=<event_time>` is 404 when `BEAVA_DEV_ENDPOINTS` unset (Task 1.a/1.b) ✅
SC4 — `derived_retention_ms` auto-computes (Plan 15-02) ✅
SC5 — Retention sweep uses event-time (Plan 15-02) ✅
SC6 — Snapshot v2 round-trip + v1 rejection (Task 2.a/2.b) ✅
SC7 — All Phase 11.5 retraction tests pass under new chain shape (Plan 15-01 + 15-03 integration tests) ✅
</success_criteria>

<output>
After completion:
- `.planning/phases/15-event-time-pit/15-SUMMARY.md` written
- `.planning/phases/15-event-time-pit/15-VERIFICATION.md` written
- `.planning/phases/15-event-time-pit/15-perf-row.md` written
- `.planning/phases/15-event-time-pit/15-throughput-row.md` written
- `.planning/perf-baselines.md` + `.planning/throughput-baselines.md` updated
- `.planning/ROADMAP.md` Phase 15 row flagged ✅ shipped (caller/orchestrator handles the state transition)
</output>
