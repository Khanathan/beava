---
context: phase
phase: 18-redis-hand-roll
plan: 18-06
status: paused
last_updated: 2026-04-26T21:39:53.851Z
---

<current_state>
Phase 18 (Redis-shaped hand-rolled hot path) is in the wrap-up window. The
hot-path perf work for Plan 18-05 + 18-06 + 18-16 landed and shipped 393k
to 522k EPS (+33%) at saturated load on M4 (msgpack TCP, p=16/pd=256, small
pipeline). What remains is bookkeeping: 18-06 wire polish (zero-copy argv,
hand-rolled response formatters, static byte responses), Phase 18 SUMMARY +
verification, and the gsd-code-review chain. Pause is taken because session
context hit 96% during a Vec[agg_id] / encode-off-apply / busy-spin / waker
push that landed cleanly but consumed the budget for follow-on wrap.
</current_state>

<completed_work>
This session (in commit order):
- 24ac587 test(18-16): RED — agg_id assigned monotonically
- adcbf64 feat(18-16): GREEN — agg_id field + monotonic assignment
- 6a22216 (user) cleanup commit — git history scrub of large files
- c419b2a chore(18-16): drop now-unused AggStateTable + BTreeMap imports
- 1d2ca82 feat(18-06): wire start_worker<MioBackend> into serve_with_dirs
- 7e05863 perf(18-06): apply busy-spin + worker→apply waker
- ad782ad perf(18-06): move response encoding off apply — +36% EPS  ← biggest win
- 4841cef perf(18-06): event_id_index hashbrown+FxBuildHasher; bookkeeping sub-stage trace
- 10be7e2 perf(18-16): Vec[agg_id] state_tables — Plan 18-16 Task 16.2
- f939404 docs(roadmap): add Phase 19 (1M-EPS bench Python+Rust) + Phase 20 (operator/streaming audit)
- c1f2079 docs(roadmap): expand Phase 20 to include push/get API audit
- bbc1062 chore(cleanup): mark Phase 13.3 REJECTED + prune stale worktrees and stashes

Headline perf delta (M4, msgpack TCP, small, p=16/pd=256):
| Metric            | Before | After  | Δ     |
|-------------------|--------|--------|-------|
| EPS sustained     | 393k   | 522k   | +33%  |
| p50 push latency  | 9.4ms  | 5.8ms  | -38%  |
| p99 push latency  | ~22ms  | 17ms   | -23%  |
| Pickup gap (idle) | 8170ns | 278ns  | -97%  |
| bk_evid           | 388ns  | 266ns  | -31%  |
| table_lookup      | 54ns   | 27ns   | -50%  |

Cleanup performed:
- 12 agent-* worktrees pruned (locked, dead from old async sessions)
- phase-13.3-lockless-apply worktree deleted (REJECTED architectural decision)
- phase-13.2-coalesce + phase-13.2-followup worktrees deleted (ABANDONED)
- 3 stale stashes dropped (arch/tpc-full-shard branch leftovers)
- Worktree count: 22 → 12
</completed_work>

<remaining_work>
Phase 18 wrap (deferred):
1. Plan 18-06 wire polish (zero-copy argv, hand-rolled response formatters, static responses)
2. Plan 18-06 perf gate 6.1 — Linux Xeon HARD ≥3M EPS/core (out of scope on M4)
3. Phase 18 SUMMARY.md + verification
4. Surviving worktrees decision (12 remaining)

Cross-cutting:
5. gsd-code-review chain on diff since c4c3f7a
6. Stash debug — `stash@{0}` is WIP --total-events bench (entry to Phase 19)

New phases (planned, not started):
7. Phase 19: 1M-EPS bench harness Python + Rust × multiple workload sizes
8. Phase 20: operator catalogue + streaming-semantics + push/get API audit
</remaining_work>

<decisions_made>
1. **Phase 13.3 REJECTED** — Beava commits to single-threaded data plane forever (Redis-cluster pattern). Phase 18 hand-rolled hot path achieved the apply-loop perf the rejected refactor targeted; further in-process apply sharding gives diminishing returns vs. multi-instance scale-out.

2. **Encode-off-apply via Box<FnOnce>** — apply ships closure to write_rx capturing GlueResponse by move; worker invokes on its own thread, encoding directly into client.write_buf. +36% EPS at saturated load.

3. **Apply busy-spin + worker→apply mio::Waker** — apply does try_recv in tight loop; listener tick(0ms) every 1024 iters or tick(50ms) after 1024 idle iters. Worker fires apply_waker.wake() after read_tx.send. Pickup gap 8170 → 278 ns.

4. **Vec[agg_id] state_tables** (Plan 18-16 Task 16.2) — replaces hashbrown::HashMap<String> with Vec<AggStateTable> indexed by desc.agg_id. Pure array indexing, no hash, no String clone. Vec grows on registration via ensure_capacity_for + lazy resize fallback in apply_event_to_aggregations.

5. **event_id_index hashbrown+FxBuildHasher** — std HashMap with SipHash on u64 was 388 ns/insert; FxBuildHasher dropped to 266 ns.

6. **Roadmap additions**:
   - Phase 19: 1M-EPS bench harness Python + Rust × small/medium/large/large_phase9
   - Phase 20: operator + streaming-semantics + push/get API audit
</decisions_made>

<blockers>
None. All gates green at end of session: cargo build/test/clippy/fmt all clean. The stashed --total-events bench has a runtime stall to debug but doesn't block — it's intentionally stashed for Phase 19 entry.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured machine-readable state for `/gsd-resume-work`
2. `.planning/ROADMAP.md` §Phase 18 + §Phase 13.3 + §Phase 19 + §Phase 20 — read new entries to understand Phase 19/20 scope and Phase 13.3 rejection rationale
3. `.planning/phases/18-redis-hand-roll/18-CONTEXT.md` — D-01..D-16 locked decisions for Phase 18
4. `git log c4c3f7a..HEAD --oneline` — recent commits since Phase 18-05 SUMMARY commit
5. **Memory: project_no_sharded_apply** — single-threaded data plane forever decision

## Critical Anti-Patterns (do NOT repeat these)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| Trace-mode EPS as ground truth | Trace mode dropped EPS from 522k to 5.6k due to per-event eprintln; per-stage trace numbers include 50%+ overhead. | advisory | Use trace ONLY for relative breakdown; always re-run no-trace for EPS comparison. |
| Bench tool blast loop without isolating bench-side ceiling | Current bench-v18 has continuous-pipeline semaphore + per-event encoding capping at ~580k EPS regardless of server speed. | blocking | Phase 19 MUST split timing into "last byte sent" (bench-bound) vs "last ack received" (server-bound). |
| Test fixtures bypassing register handler | apply_event_to_aggregations indexes by desc.agg_id; tests bypassing the register handler need ensure_capacity_for or rely on the lazy-resize fallback. | advisory | Use helpers in agg_state_table.rs: new_state_tables_for, lookup_table_by_name, has_entries_for_name, ensure_capacity_for. |
| `git add -A` with accumulated untracked planning/website files | Bundled 504 unrelated files into a perf commit. Recovered via reset --soft + selective re-add. | advisory | Always stage explicit paths for source/perf commits. |

## Infrastructure State

- Rust toolchain stable; cargo build --workspace --all-targets green
- Disk 82% used (was 100% earlier; 80GB freed via target/ wipe + 14 worktree prune)
- Bench binary `target/release/beava-bench-v18` built post Vec[agg_id] (10be7e2)
- Stashed bench WIP: `stash@{0}` --total-events + pre-encoded-frame; stalls after pre-warm; debug as Phase 19 entry
- 12 surviving worktrees: phase-6.1, 10, 11.5, 12-followup, 12-joins, 13-followup, 13-ship, 13.1, 15, 16, sdk-syntax-audit. Each needs an explicit decision.

## Where time goes per push (post all session changes, real ns)

```
parse 67 → lookup 35 → validate 29 → wal_build 39 → wal_append 38 →
agg ~600  (entity_row_init 350 / features 70 / registry 40 / entity_key 30 / table_lookup 15) →
bk_counters 10 → bk_evid 200 → bk_dedupe 5
─────────────────────────────────
TOTAL ~1040 ns per push
```

Highest-leverage remaining post-Phase-18:
1. EntityKey single-u64 fast path (entity_row_init 350 → ~150 ns)
2. Skip event_id_index insert for non-retract descriptors (bk_evid 200 → ~5 ns)
3. Inline AggOp::update Count/Sum hot paths (features 70 → ~40 ns)

Estimated achievable: ~600 ns/push → ~700-800k EPS ceiling on M4 with current architecture.

<context>
Session goal was Plan 18-05 → 18-06 wrap. Landed more than planned:
encode-off-apply, busy-spin + waker, event_id_index hashbrown, Vec[agg_id]
(Task 16.2). Each driven by a trace-led "where's the cost" question, with
user steering away from premature optimization (EntityKey hash caching was
correctly rejected when I realized hashing happens once per descriptor).

Roadmap extended: Phase 19 (1M-EPS bench Python + Rust) and Phase 20
(operators + streaming-semantics + push/get API audit) queued as
post-Phase-18 work. Phase 19 is higher-priority — "can Beava process
1M events in 1s" is the headline question users ask. Stashed --total-events
bench is Phase 19 entry point.

Phase 13.3 officially REJECTED, archived from worktree list, marked in
ROADMAP. Single-threaded-forever decision locked in Claude memory.

Mental model: per-event apply work is at architectural floor for current
EntityKey shape. Pushing further requires single-u64 fast path OR
multi-instance scale-out. Neither on v0 critical path — priority is
Phase 18 wrap (SUMMARY + verification) and 1M-EPS bench harness so
public ship-readiness number can be defended.
</context>

<next_action>
Run `/gsd-resume-work` to load this state, then choose:

(A) Wrap Phase 18 first — write `.planning/phases/18-redis-hand-roll/SUMMARY.md`,
    run gsd-verifier, decide surviving worktrees, then gsd-code-review.
    ~1 medium-context window.

(B) Jump to Phase 19 (RECOMMENDED) — debug `stash@{0}`, finalize Rust
    bench, scaffold Python harness, run matrix against
    small/medium/large/large_phase9. ~1-2 medium-context windows.
    Phase 18 SUMMARY can borrow the 1M EPS data point from this work.

If unspecified, default to (B). The 1M EPS measurement is the public
ship-readiness number and Phase 18 SUMMARY drafts cleanly from this handoff
once Phase 19 produces the headline data.
</next_action>
