---
phase: 18-redis-hand-roll
plan: 18-00
type: research
wave: 0
depends_on: []
files_modified:
  - .planning/phases/18-redis-hand-roll/18-redis-research.md
  - .planning/phases/18-redis-hand-roll/18-rust-translation.md
  - .planning/phases/18-redis-hand-roll/18-CONTEXT.md
autonomous: true
requirements: []
must_haves:
  - Redis 7.x architecture summary committed
  - Rust translation spec committed
  - Locked decisions captured in CONTEXT.md
---

# Plan 18-00 — Research + design

**TDD exemption:** This plan produces architectural markdown documents only — no source code. Per CLAUDE.md §Conventions, TDD red-green does NOT apply.

## Goal

Distill Redis 7.x's hot-path architecture into a Rust-targeted design spec. Produce reference docs that subsequent stages cite without re-deriving.

## Tasks

### Task 0.1 — Redis 7.x source-code research → `18-redis-research.md` ✅ COMPLETE

**Status:** committed `a5edf82`. ~235 lines.

**Scope:**
- Read `src/ae.c` (event loop), `src/networking.c` (clients + I/O threads), `src/aof.c` (AOF write path), `src/server.c` (main, command dispatch)
- Summarize: event loop structure, client state machine, I/O threads protocol (atomic counters + spin-wait), AOF inline-append + scheduled-fsync, RESP zero-copy parse

### Task 0.2 — Rust translation spec → `18-rust-translation.md` ✅ COMPLETE

**Status:** committed `4798bd3`. ~103 lines.

**Scope:**
- Map each Redis pattern (C) to Rust equivalent: `pthread` → `std::thread`, `_Atomic` → `AtomicU64`, `aeMain` → custom `mio`-based loop, AOF → `Rc<RefCell<WalWriter>>`, RESP argv → `(*const u8, usize)` slices into `BytesMut`, etc.
- Note pitfalls (`!Send` requirements, ownership, Rust lint friction)

### Task 0.3 — CONTEXT.md (D-01..D-16) ✅ COMPLETE

**Status:** committed by Phase 18 planner continuation. ~120 lines.

**Scope:**
- Lock decisions D-01..D-16 per design session 2026-04-24
- Document SC1-SC8, file inventory, plan structure, grey areas

## Verification

Plan complete when all three artifacts are present in `.planning/phases/18-redis-hand-roll/` and committed to `v2/greenfield`. No code, no perf gates.

## Outputs

- `18-redis-research.md` — architecture summary
- `18-rust-translation.md` — translation table
- `18-CONTEXT.md` — locked decisions + plan structure

## Next plan

`18-01-PLAN.md` — Hand-rolled event loop with HTTP + TCP listeners (Stage 18.1).
