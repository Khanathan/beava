# Phase 11.5 — Throughput row (scratch; orchestrator merges to canonical .planning/throughput-baselines.md)

**Hardware:** macOS aarch64 (Apple Silicon, 10 cores), HTTP transport, fsync ~7.4ms expected (macOS F_FULLSYNC hw-class limit per Phase 6 baseline)
**Date:** 2026-04-23
**Workload:** temporal-fraud (1 temporal table, 70% upsert / 25% read / 5% retract)
**Harness:** `cargo run -p beava-bench --release --bin temporal_throughput -- --duration-secs 10 --parallel 8`
**Plan:** 11.5-01 Task 7

| Pipeline shape | Transport | Op       | EPS  | P99 (ms) | P50 (ms) | Notes |
|----------------|-----------|----------|-----:|---------:|---------:|-------|
| temporal-fraud | http      | upsert   | 840  | 18.96    | 8.04     | first table-write baseline |
| temporal-fraud | http      | read     | 299  | 3.50     | 0.16     | first temporal-read baseline |
| temporal-fraud | http      | retract  | 59   | 17.95    | 8.05     | first retract baseline |

**Measured run:** `./target/release/temporal_throughput --duration-secs 10 --parallel 8` → elapsed=10.02s, upserts=8410, reads=2999, retracts=592 (target mix 70/25/5; 5% retract × 8410 effective writes ≈ 590 retracts ✓).

## Notes

- **TCP push not yet wired** for table-writes (Phase 8 sibling work). HTTP-only.
- **fsync ~7.4 ms on macOS** dominates upsert latency — expect ~1000 EPS upper bound on `/push-table` per single-threaded fsync flush, mirroring the Phase 7.5 stream-push baseline (small=990, medium=1031, large=1007 EPS).
- **Reads** are pure in-memory MVCC `lookup_at_lsn` — no fsync, expect µs-range P99.
- **Retract** appends a WAL `Retract` record then mutates the chain — same fsync-bound EPS as upsert.
- This is the **first table-write throughput baseline**; no prior comparison. Phase 12 (joins consuming `as_of=`) will compare against this.

**Per CLAUDE.md §End-to-end throughput regression contract: orchestrator merges to canonical `.planning/throughput-baselines.md`.**
