---
phase: 14.1-streaming-modifiability
plan: 06
type: execute
wave: 6
depends_on: [14.1-05]
files_modified:
  - crates/beava-core/benches/modifiability_cost.rs
  - crates/beava-server/src/metrics.rs
  - crates/beava-core/src/sketches/count_distinct.rs
  - .planning/perf-baselines.md
  - .planning/throughput-baselines.md
  - .planning/phases/14.1-streaming-modifiability/14.1-SUMMARY.md
  - .planning/phases/14.1-streaming-modifiability/14.1-VERIFICATION.md
autonomous: true
requirements:
  - SC3 (fast-path zero-cost for modifiable=false)
  - SC6 (adaptive promotion metric)
  - SC8 (throughput regression ≤ 5%)

must_haves:
  truths:
    - "Criterion microbench proves mod=false overhead ≤ 5 ns/event vs Phase 14 baseline on Apple-M4"
    - "Criterion microbench captures mod=true slow-path cost (≤ 5 µs per Tier 3 feature per slow-path event)"
    - "`beava-bench` small-simple-fraud-HTTP-BATCH_MS=0 regression ≤ 5% vs Phase 14 baseline"
    - "`beava-bench` row captured for modifiable=True Tier-3-heavy pipeline (new baseline; no gate)"
    - "`beava_feature_promoted_to_approx_total{feature}` counter increments on CountDistinct ExactArray→HLL promotion under modifiable stream"
    - "`beava_modification_log_evicted_total{feature}` counter increments on RejectedOlderThanOldest"
    - "SUMMARY.md + VERIFICATION.md land evidence per SC1-SC8"
  artifacts:
    - path: "crates/beava-core/benches/modifiability_cost.rs"
      provides: "criterion microbench: fast-path overhead + slow-path replay cost"
      contains: "criterion_group"
    - path: "crates/beava-server/src/metrics.rs"
      provides: "beava_feature_promoted_to_approx_total + beava_modification_log_evicted_total"
      contains: "feature_promoted_to_approx"
    - path: ".planning/perf-baselines.md"
      provides: "modifiability_cost row"
      contains: "modifiability_cost"
    - path: ".planning/throughput-baselines.md"
      provides: "small × modifiable=false (regression row) + small × modifiable=true (new baseline)"
      contains: "modifiable"
  key_links:
    - from: "CountDistinct state machine (sketches/count_distinct.rs)"
      to: "beava_feature_promoted_to_approx_total metric"
      via: "increment on ExactArray/HashSet → HLL promotion when source is modifiable"
      pattern: "feature_promoted_to_approx"
---

<objective>
Ship the performance + observability + sign-off layer for Phase 14.1.

Criterion microbench proves:
- **Fast path for `modifiable=False` ≤ 5 ns overhead** vs Phase 14 baseline.
  If this budget blows, the branch-predictor assumption in Plan 05 Task 1 is
  wrong and we revisit.
- **Slow path for `modifiable=True` ≤ 5 µs per Tier 3 feature per slow-path
  event** on Apple-M4 (D-06 budget). Per-op breakdown captured.

`beava-bench` throughput runs:
- Small-simple-fraud × HTTP × BATCH_MS=0 × `modifiable=False` → ≤ 5% regression
  vs Phase 14 baseline. Gate applies.
- Small-Tier-3-heavy × HTTP × BATCH_MS=0 × `modifiable=True` → first baseline
  captured. No regression gate (new measurement point).

Metrics:
- `beava_feature_promoted_to_approx_total{feature}` — increments on
  CountDistinct ExactArray/HashSet → HLL promotion when the source is
  modifiable (D-10).
- `beava_modification_log_evicted_total{feature}` — increments on
  `RejectedOlderThanOldest` from Plan 02.

Close the phase with SUMMARY.md + VERIFICATION.md covering SC1-SC8.

Output: bench file + metrics wire-up + baselines rows + summary + verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14.1-streaming-modifiability/14.1-CONTEXT.md
@.planning/perf-baselines.md
@.planning/throughput-baselines.md
@crates/beava-core/src/sketches/count_distinct.rs
@crates/beava-server/src/metrics.rs

<interfaces>
From Plan 05:
- `ModifiabilityMetrics` struct on `AppState` with counter fields.
- `apply_with_modifiability` is the entry point under test.

From Phase 13 / 13.1:
- `/metrics` Prometheus registry exists at `beava-server/src/metrics.rs`.
- Pattern for registering a counter + incrementing from hot path.

From Phase 10:
- `count_distinct.rs` has a state machine with three modes:
  `ExactArray → HashSet → Hll`. Promotion points are explicit in the
  update path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: modifiability_cost.rs criterion microbench + perf-baselines row</name>
  <files>
    crates/beava-core/benches/modifiability_cost.rs,
    .planning/perf-baselines.md
  </files>
  <behavior>
    Test 1 (red): `cargo bench --bench modifiability_cost` runs four scenarios:
      - `apply/mod_off` — modifiable=False source + 1 Tier C feature.
        Baseline for the Phase 14 comparison row.
      - `apply/mod_off_vs_phase14` — direct comparison against calling
        `apply_event_to_aggregations` on the same payload. Difference must be
        ≤ 5 ns/iter (median, not noise-floor).
      - `apply/mod_on_forward` — modifiable=True + 1 Tier A feature (Ewma)
        + all events forward. Measures the log-push cost.
      - `apply/mod_on_slowpath` — modifiable=True + 1 Tier A feature (Ewma)
        + every other event is out-of-order (forces 50% slow-path). Measures
        replay cost at `modification_log_depth=16`.

    Test 2 (criterion assertion pattern): Each scenario records median +
    P99 in `target/criterion/`. The bench doesn't hard-assert budgets; Task 3
    (VERIFICATION.md) reads the JSON output and checks budgets manually.
    Documented budgets in the bench file comments:
      - mod_off overhead ≤ 5 ns
      - mod_on_forward overhead ≤ 50 ns per Tier 3 feature per push
      - mod_on_slowpath ≤ 5 µs per slow-path event × 1 Tier 3 feature

    Test 3 (red): Runs on at least two `AggKind` variants to exercise
    dispatch: Ewma (Tier A floating-point) and Trend (Tier B accumulator).

    Test 4: Append results to `.planning/perf-baselines.md` under a new
    `## Phase 14.1 — modifiability_cost` section with hw-class tagging per
    the existing convention (e.g. `hw: apple-m4; os: darwin; cpu-count: 12`).
  </behavior>
  <action>
    Create `crates/beava-core/benches/modifiability_cost.rs` using the
    existing criterion setup pattern (see Phase 5.5 or 13.3 bench files).

    Four `criterion_group!` benches. Each constructs a minimal `Registry +
    AggStateTable + AppState` fixture in `bench_function`; the hot path is
    `apply_with_modifiability(...)` called in a tight loop. Use
    `black_box` to prevent dead-code elim.

    Pre-populate state so we measure steady-state cost, not first-push
    allocation cost.

    `modifiability_cost_slowpath`:
    - Fixture: 16-event log pre-populated with in-order events.
    - Each bench iter: insert one event whose `event_time_ms` is between
      two existing entries → triggers InsertOutcome::Inserted → replay.
    - Measures cost of one slow-path replay over a full log.

    Append result rows to `.planning/perf-baselines.md`:
    ```
    ## Phase 14.1 — modifiability_cost
    hw: apple-m4; os: darwin; cpu-count: 12

    | Bench                       | Median (ns) | P99 (ns) | Budget (ns) | Status |
    |-----------------------------|-------------|----------|-------------|--------|
    | apply/mod_off               | <measured>  | ...      | baseline    | –      |
    | apply/mod_off_vs_phase14    | <measured>  | ...      | +5          | PASS   |
    | apply/mod_on_forward        | <measured>  | ...      | +50         | PASS   |
    | apply/mod_on_slowpath       | <measured>  | ...      | 5000        | PASS   |
    ```
  </action>
  <verify>
    <automated>cargo bench --bench modifiability_cost -- --sample-size 20</automated>
  </verify>
  <done>
    Criterion bench file lands; four scenarios measured; baseline row in
    `.planning/perf-baselines.md`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promotion + eviction metrics wire-up + throughput-baselines rows</name>
  <files>
    crates/beava-server/src/metrics.rs,
    crates/beava-core/src/sketches/count_distinct.rs,
    .planning/throughput-baselines.md
  </files>
  <behavior>
    Test 1 (red): Register modifiable event with one `CountDistinct` feature.
    Push 1500 unique values to one entity. Assert
    `beava_feature_promoted_to_approx_total{feature="unique_ips"}` increments
    from 0 to 1 at the HashSet→HLL boundary (currently ~1024 uniques).

    Test 2 (red): Same feature on a non-modifiable source — promotion still
    happens internally (state machine unchanged) but the metric does NOT
    increment (promotion-metric is only emitted under modifiable streams per
    D-10 — rationale: for non-modifiable streams, promotion is an internal
    optimization, not a correctness concern the user needs to know about).

    Test 3 (red): Register modifiable event with one Tier A feature +
    `modification_log_depth=4`. Push 10 events out-of-order such that 5
    insert attempts land older than the log's oldest-retained. Assert
    `beava_modification_log_evicted_total{feature=...}` increments by 5.

    Test 4 (throughput run): `beava-bench --pipeline=small-simple-fraud
    --transport=http --batch-ms=0` with all sources `modifiable=False` →
    append row to `.planning/throughput-baselines.md`; regression gate is
    ≤ 5% vs the most recent Phase 14 row in the same hw-class. If regression
    > 5% blocks the plan.

    Test 5 (throughput run): Construct a second bench pipeline
    `small-simple-fraud-modifiable` (same shape but one Tier A Ewma feature
    added, all sources `modifiable=True`, 10% of events out-of-order within
    a 5s tolerate window). Run once and append as new baseline.
  </behavior>
  <action>
    In `metrics.rs`, declare two new counters following the Phase 13 pattern:
    ```rust
    pub static FEATURE_PROMOTED_TO_APPROX: Lazy<IntCounterVec> = Lazy::new(|| {
        register_int_counter_vec!(
            "beava_feature_promoted_to_approx_total",
            "Entity cardinality crossed from exact (HashSet) to approximate (HLL) under a modifiable stream.",
            &["feature"]
        ).unwrap()
    });

    pub static MODIFICATION_LOG_EVICTED: Lazy<IntCounterVec> = Lazy::new(|| {
        register_int_counter_vec!(
            "beava_modification_log_evicted_total",
            "Modification log insert rejected because the event predates the oldest-retained entry.",
            &["feature"]
        ).unwrap()
    });
    ```

    Wire promotion increment in `count_distinct.rs`:
    - Find the promotion point (ExactArray→HashSet or HashSet→HLL).
    - Thread a `bool source_modifiable` flag through the state update entry
      point (the cleanest way: extend the state machine's update signature
      with a `promotion_cb: Option<&dyn Fn()>` callback, invoked on
      promotion. The apply path passes `Some(&|| metric.inc())` only when
      source is modifiable.)
    - Alternative lighter wire: CountDistinct's state struct gains a
      `promoted_recently: Cell<bool>` flag; after `update`, the caller checks
      and increments the metric. Pick the cheaper option during execution.

    Wire eviction increment in `apply_with_modifiability` (Plan 05 placeholder
    already has `metrics.events_rejected_log_underflow.inc()`; rename to
    match the final metric name here).

    Extend `beava-bench` with the new `small-simple-fraud-modifiable`
    pipeline in its config (crates/beava-bench/src/...). Re-run both small-
    simple-fraud baselines.

    Append rows to `.planning/throughput-baselines.md` under a new
    `## Phase 14.1 — modifiability` section.
  </action>
  <verify>
    <automated>cargo test -p beava-server metrics_modifiability &amp;&amp; cargo run -p beava-bench -- --pipeline small-simple-fraud --transport http --batch-ms 0 --modifiable-pipeline small-simple-fraud-modifiable</automated>
  </verify>
  <done>
    Two new counters exported on `/metrics`; CountDistinct promotion fires
    increment under modifiable streams only; log eviction fires increment;
    two throughput-baselines rows landed; regression gate holds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SUMMARY.md + VERIFICATION.md — phase close</name>
  <files>
    .planning/phases/14.1-streaming-modifiability/14.1-SUMMARY.md,
    .planning/phases/14.1-streaming-modifiability/14.1-VERIFICATION.md
  </files>
  <behavior>
    Test 1: Files exist with required frontmatter + sections (matching
    template in `.claude/get-shit-done/templates/summary.md`).

    Test 2: VERIFICATION.md has explicit evidence per SC1-SC8:
    - SC1: ref to Plan 01 test + command line to reproduce.
    - SC2: ref to Plan 02 memory-instrumented test + evidence snippet.
    - SC3: ref to Plan 06 Task 1 bench row, actual numbers.
    - SC4: ref to Plan 04 proptest + Plan 05 integration smoke.
    - SC5: ref to Plan 03 wire test (three tier paths).
    - SC6: ref to Plan 06 Task 2 Test 1 (CountDistinct promotion).
    - SC7: ref to Plan 05 Task 2 kill-and-restart scenario.
    - SC8: ref to Plan 06 Task 2 Tests 4+5 (beava-bench rows).

    Test 3: SUMMARY.md includes the tier catalogue table (copy from CONTEXT
    D-05) + the documented memory budget note from D-12 + grey-areas
    resolutions from CONTEXT.

    Test 4: Both files pass `gsd-tools frontmatter validate` for the
    phase-summary / phase-verification schemas (confirm schema names during
    execution).
  </behavior>
  <action>
    Write SUMMARY.md capturing:
    - What shipped (6 plans, ~19 tasks).
    - Tier catalogue table (from D-05 verbatim).
    - Fast-path cost measurement (headline number from Plan 06 Task 1).
    - Slow-path cost measurement (headline number).
    - Throughput regression results.
    - Memory budget caveat (D-12 docs note).
    - Known follow-ups: subtractive-Cormode EWMA upgrade (v0.1); stream
      retraction (v1); Phase 17 table modifiability extension.

    Write VERIFICATION.md with per-SC evidence + command-line reproducers.
    Use the Phase 14 VERIFICATION.md (after Phase 14 ships) as the stylistic
    template.

    Final commit: `docs(14.1-06): phase complete — SUMMARY + VERIFICATION +
    baselines landed`.
  </action>
  <verify>
    <automated>node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate .planning/phases/14.1-streaming-modifiability/14.1-SUMMARY.md --schema summary &amp;&amp; node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate .planning/phases/14.1-streaming-modifiability/14.1-VERIFICATION.md --schema verification</automated>
  </verify>
  <done>
    SUMMARY + VERIFICATION land; all SC1-SC8 have reproducible evidence;
    phase directory is self-contained (future readers can replay every
    measurement from these two files + the PLAN files + the test commits).
  </done>
</task>

</tasks>

<verification>
All three tasks pass. `cargo bench --bench modifiability_cost` runs. Metrics
wired. Throughput rows landed with ≤ 5% regression on the mod=false pipeline.
SUMMARY + VERIFICATION frontmatter validated. Phase ready to ship.
</verification>

<success_criteria>
- Criterion bench proves mod=false ≤ 5 ns overhead (SC3) and slow-path
  ≤ 5 µs per replay
- `beava_feature_promoted_to_approx_total` + `beava_modification_log_evicted_
  total` counters exported + tested
- `beava-bench` regression gate ≤ 5% on mod=false pipeline (SC8)
- New baseline row for mod=true Tier-3-heavy pipeline
- SUMMARY + VERIFICATION close the phase with per-SC evidence
</success_criteria>

<output>
This is the final plan. After completion, the phase is ready for the
`/gsd-verify-phase 14.1` workflow or direct merge.
</output>
