---
context: default
phase: null
status: paused-post-architectural-pivot
last_updated: 2026-04-30T13:35:11.840Z
---

# Beava v2 — paused post no-event-time architectural pivot

> **TL;DR:** Single doc-only commit `2d58d80` on `v2/greenfield`. Architectural pivot locked: no event-time / no watermarks / no joins / no PIT — ever. Five phase dirs archived, Phase 12.6 (v0 surface reduction) inserted as new biggest blocker, Phase 25 (session windows) inserted at v0.1+. Next session: `/gsd-discuss-phase 12.6`.

## Required Reading (in order, before resuming)

1. **`.planning/HANDOFF.json`** — full structured state (machine-readable, the canonical source for `/gsd-resume-work`)
2. **`~/.claude/projects/-Users-petrpan26-work-tally/memory/project_redis_shaped_no_event_time_ever.md`** — the permanent architectural commitment that frames everything below
3. **`.planning/CORRECTNESS-PATH.md`** — revised P0 list with Phase 12.6 as biggest v0 blocker
4. **`.planning/ROADMAP.md`** — Phase 12.6 entry (the new v0 surface reduction phase) + Phase 25 entry (session windows)
5. **`.planning/PROJECT.md`** — updated What This Is + Out of Scope + Key Decisions reflecting pivot

## Critical Anti-Patterns (do NOT repeat next session)

| Pattern | Severity | Description / Mitigation |
|---|---|---|
| Re-introducing event-time / watermarks / joins | blocking | Locked OUT permanently per `project_redis_shaped_no_event_time_ever`. Reviving requires explicit user override + new ADR. Reject any plan/discuss that reintroduces these. |
| Adding a second hot-path entry alongside mio | blocking | mio data plane (`apply_shard.rs::dispatch_*_sync`) is the only data-plane entry. Tokio sidecar is admin-only on a separate port. Phase 12.6 enforces this structurally. |
| Trusting alphabetic phase resolver under collisions | advisory | `gsd-tools.cjs init phase-op N` resolves alphabetically. When multiple `N-*` dirs exist (e.g. earlier session had two Phase 14 dirs), the resolver may pick the wrong one. Always check `ls .planning/phases/ | grep "^N"` for collisions before trusting init output. |

## What landed this session (summary)

Single doc-only commit `2d58d80`. **No code changes** — entirely a strategic pivot captured in docs + memory + dir archives.

- 5 phase dirs archived to `_archived-*`: 14-streaming-correctness, 14.1-streaming-modifiability, 15-event-time-pit, 15-snapshot-io-off-main-thread, 14-per-stream-locks-dashmap-concurrency
- 1 NEW memory: `project_redis_shaped_no_event_time_ever`
- 1 MODIFIED memory: `project_stateful_architecture` (Decision 2 PIT marked SUPERSEDED)
- 5 .planning docs swept: PROJECT.md, ROADMAP.md, REQUIREMENTS.md, STATE.md, CORRECTNESS-PATH.md
- ROADMAP changes:
  - Phase 12 retitled "push/get API completion (joins/unions REMOVED)"
  - Phase 12.5 archived (superseded by Plan 12-10)
  - Phase 14, 14.1, 15 ARCHIVED
  - Phase 17 reworked (Tier B/C only; no PIT-driven retention)
  - **NEW Phase 12.6** inserted — v0 surface reduction (legacy axum kill + event-time strip + dead-code sweep + windowed-op time-source swap + join/union removal + REQUIREMENTS sweep + mio-only enforcement)
  - **NEW Phase 25** inserted — Session window operator family (v0.1+)

## Five locked answers from this session

1. `event_time_ms` REMOVED from wire entirely (push payload + WAL schema bump)
2. 14+ windowed ops switch to `now_ms()` server clock (Path X — preserves 55-op catalogue)
3. Session windows close on **both** lazy-on-query AND flip-on-next-event-after-gap
4. Latest closed session retained per (entity, feature); fixed memory
5. `bv.union` deferred to v0.1+ alongside joins

## Why this matters for v0 polish

User explicitly framed: **v0 polish is the most important thing.** This pivot eliminates a huge chunk of dead surface that would have confused OSS reviewers — joins methods that don't work, event_time fields that are ignored, two parallel hot paths, ~3500 LOC of legacy axum. Phase 12.6 is the single biggest polish move because it makes "what's in the docs = what's in the code."

After 12.6, the path to ship is Phase 13 minimum subset (packaging + quickstart + readme + website final) → launch.

## Pre-Execution Critique Required

None — this was a strategic pivot, not an execution session. Phase 12.6 will need its own discuss → plan → execute flow next session.

## Infrastructure State

- **Branch:** `v2/greenfield` @ `2d58d80`
- **Test status:** `phase11_smoke::all_eleven_ops_round_trip_through_http` still deterministic-fail (`tests/phase11_smoke.rs:235`); folded into Phase 12.6 TestServerV18 migration. NOT a separate workstream.
- **Binaries:** `target/release/{beava, beava-bench-v18}` are pre-pivot builds; Phase 12.6 will rebuild after surface changes.
- **Worktrees:** `phase-12-joins` (joins plumbing is now dead architecture — careful audit during merge), `phase-12-followup` (descoped — skip join/union plans), `phase-13-ship`, `phase-13-followup`, `phase-13.2-coalesce` (ABANDONED), `phase-13.3-lockless-apply` (REJECTED, archival decision pending — folded into 12.6 sweep)
- **gsd-tools.cjs:** `node /Users/petrpan26/.claude/get-shit-done/bin/gsd-tools.cjs init phase-op 14` now correctly resolves to `_archived-14-streaming-correctness-killed-no-event-time` (after archiving the per-stream-locks dir mid-session)
- **Model profile:** quality (all 17 GSD agents on opus)

## Next Action

```
/gsd-resume-work
```

Then:

```
/gsd-discuss-phase 12.6
```

The discuss should surface gray areas:
- (a) **TestServerV18 harness shape** — drop-in replacement (`Server::serve` API parity) vs new test pattern (e.g. dispatch_wire_request_sync directly + helper assertions)
- (b) **Wire schema bump strategy** — new schema version handles old+new for one release cycle vs hard cutover at 12.6 ship
- (c) **Dead-code threshold for "sweep complete"** — cargo-deadcode % target (e.g. ≤ 5%); manual audit thoroughness
- (d) **Smoke-test fate per file** — migrate to V18 vs delete (coverage replicated elsewhere?) vs replace with new test
- (e) **REQUIREMENTS sweep depth** — only the SDK-JOIN section + obvious event_time refs vs every reference inc. comments and docs
- (f) **Docs touch scope** — beava.dev guide pages, recipes; some require website work alongside; does 12.6 own the website sweep or just the code-side docs?
- (g) **`phase-12-joins` branch handling** during merge — the joins plumbing is now dead architecture; need to audit what merges vs what's deleted
- (h) **Plan ordering inside 12.6** — axum kill first, or event-time strip first, or interleave by area? (each ordering has different test-failure exposure during the work)

## Context

User has been driving rapid scope reduction over the past 2-3 sessions:
- 2026-04-29: Park read-path Layers 1+2 post-v0
- 2026-04-30 (am): Defer Phase 14 streaming correctness
- 2026-04-30 (this session): Cascade pivot — joins removed → watermarks permanent → event-time gone → mio-only enforcement

Each step simplified v0 surface. The result: Beava is now committed to being **Redis with stateful operators, processing-time only**. This is *the* architectural identity. Future sessions should treat any proposal that contradicts this as requiring explicit user override.

User signal: "polish for v0 release [is] the most important thing." Phase 12.6 surface reduction is the polish backbone. Phase 13 minimum subset (packaging + quickstart + readme + website final) is the ship-readiness layer. Plan 12-10 push-and-get is DEFERRED.

**Pause is intentional.** User wants to open Phase 12.6 discuss fresh next session with full attention. Do not auto-advance.
