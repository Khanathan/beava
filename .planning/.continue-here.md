---
context: phase-transition
phase: post-19.4-cross-platform-closed; pre-12-07-or-14
status: phase-19.4-shipped-pass; cross-platform-validated; read-path-blocked-by-12-07
last_updated: 2026-04-29T04:40:04Z
---

# Phase 19.4 cross-platform validation closed; Plan 12-07 scoped; ready for v0 critical path

> **Headline:** 8 commits beyond prior pause. Phase 19.4 fraud-team verified at 102.8k EPS Apple-M4 / 51-58k EPS Hetzner EPYC vServer. IoPool default formula bug discovered (M4 wins at iot=1, +14.4% avg; Hetzner default iot=4 is the WORST config). `--io-threads N` CLI flag shipped. Read-bench harness shipped but blocked by Phase 12 PARTIAL leftover (`/get` not wired into production mio HTTP path) — scoped as Plan 12-07.

## Critical Anti-Patterns (do NOT repeat — discovered this session)

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Background agents bailing at 65% context without doing work | 2 consecutive Python/Rust read-bench agents bailed at startup citing context concerns after reading 1-2 files. Both could have finished with tighter scope. | advisory | Re-dispatch with explicitly tightened scope (single platform, single pipeline, single mode). Don't accept full-scope bail. |
| IoPool default formula `max(2, n_cpus/4)` is structurally wrong | On M4 gives iot=2 (iot=1 wins +14.4% avg). On Hetzner 16-vCPU gives iot=4 (the WORST tested config; iot=1/2/8/12 all give +10%). | advisory | Plan 12-07 or Phase 19.5 followup re-derives formula from wider hw-class sweep. `--io-threads N` CLI flag (commit 3e4d62d) is the user-facing escape hatch. |
| `/get` route 404 on production binary — Phase 12 PARTIAL leftover | feature_query_router built and mounted in axum router_with_push but production mio-based beava binary doesn't include /get in hand-rolled HTTP parser. | blocking | Plan 12-07 scope doc captures full diagnosis + 3 fix paths. Path B1 (wire /get into mio fast path) recommended per memory project_phase18_no_dual_runtime. |

<current_state>
Branch v2/greenfield at HEAD `6fb83a9`. Phase 19.4 fully shipped at PASS. All 10 pipeline configs have post-19.4 throughput baselines on BOTH Apple-M4 AND Hetzner Linux EPYC. IoPool sensitivity mapped on both platforms. `--io-threads N` CLI flag committed and tested. Python read-bench harness shipped but currently 404s on every request — root-caused to Phase 12 PARTIAL leftover scoped as Plan 12-07.

Cross-platform numbers (filtered medians):
- Apple-M4 fraud-team: 102.8k EPS (Rust continuous bench) / 107.9k EPS (Python multi-conn) / **104.7k EPS at iot=1**
- Hetzner EPYC vServer fraud-team: 51.4k EPS (Rust) / 70.0k EPS (Python multi-conn at iot=1)
- Linux:M4 ratio: 0.50× sketch-heavy / 0.96-1.08× simple Tier-1 — gap is virtualization + microarch, NOT Beava code

Beava-vs-Redis story (corrected):
- Per-state-mutation: Beava 4.5-9.2M state-mutations/sec/core vs Redis ~1M HINCRBY/sec/core pipelined
- Per-event-fraud-team: Beava 50k EPS/instance × 88 features = ~4.5M state mutations/sec; Redis pipelined would do ~11k EPS/instance for same workload
- **Beava is 4-7× faster than Redis per instance on multi-feature-per-event workloads** (was incorrectly framed earlier as 'competitive with Redis')
</current_state>

<completed_work>

### Commits since last pause (8 in chronological order)

| Commit | Subject |
|---|---|
| `7233bfd` | bench(19.4): Linux EPYC-Genoa rebaseline + Python SDK validation |
| `de4eee6` | bench(19.4): Python multi-process bench (Apple-M4 + Linux EPYC-Genoa) |
| `3f72985` | bench(19.4): Linux EPYC-Genoa production-mode perf tuning experiments |
| `81bfb6c` | test(19.4): add red test for --io-threads CLI flag (Phase 19.4 followup) |
| `3e4d62d` | bench(19.4): M4 IoPool=1 sweep + Hetzner sweep + GREEN impl --io-threads |
| `a116d87` | bench(19.4): Python read benchmark harness — fraud-team /get driver |
| `6fb83a9` | docs(phase-12-07): scope doc — wire /get into mio HTTP fast path |

### Key findings

- **M4 IoPool=1 sweep:** +14.4% avg lift across all 10 pipelines, ZERO regressions. large-with-sketches +33%, medium +21.9%, fraud-team +1.8%.
- **Hetzner IoPool sweep:** Default iot=4 on 16-vCPU is the WORST config; iot=1/2/8/12 all give +10% on fraud-team. Tier-1 prefers iot≥4 (parse-bound).
- **Python multi-conn bench:** Reveals Rust bench-v18 continuous-mode is back-pressure-limited on sketch-heavy pipelines. Python multi-process gives higher server-ceiling reading on fraud-team (107.9k M4 vs 102.8k Rust; 70k Hetzner vs 51k Rust).
- **Hetzner is KVM-virtualized vServer** (not bare-metal). The 0.50× ratio decomposes ~50/50 between Apple Silicon advantage on sketch-heavy + KVM virtualization overhead.

</completed_work>

<remaining_work>

### v0 Ship Critical Path Queue (in execution order)

| # | Phase | What | Effort |
|---:|---|---|---|
| **1** | **12-07** | wire `/get` into mio HTTP fast path (RECOMMENDED next) | 1-2 days |
| 2 | 14 | streaming correctness Chunk A (watermark + bucket-reset bug fix) | 1 week |
| 3 | 15 | event-time PIT temporal store (BLOCKS Phase 12 Plan 04) | 1 week |
| 4 | 14.1 | streaming modifiability Chunk B | 2 weeks |
| 5 | 12-followup | joins (5/6 plans pending on phase-12-followup worktree) | 2-3 weeks |
| 6 | 12.5 | push_and_get atomic endpoint | 1 week |
| 7 | 16 | SDK source annotation | 1 week |
| 8 | 13-followup | cold-entity GC + perf gate + metric counters | 1 week |
| 9 | v0 | ship tag | 1 day |

**Total ~9-10 weeks v0 critical path.**

### Read-bench measurement runs (blocked by 12-07)

Once `/get` is wired into production binary, run:
```bash
export BEAVA_IO_THREADS=1
mkdir -p /tmp/py-read-bench
echo "run,wall_clock_ms,req_per_sec,kf_per_sec,p50_us,p95_us,p99_us" > /tmp/py-read-bench/progress.csv
for i in 1 2 3; do
  python3 python/benches/read_bench.py \
    --pipeline crates/beava-bench/configs/fraud-team.json \
    --total-reads 50000 --warmup-events 100000 \
    --parallel 8 --server-port 28080 --tcp-port 28081 --io-threads 1 \
    > /tmp/py-read-bench/run-$i.log 2>&1
  csv=$(grep '^READBENCH_CSV,' /tmp/py-read-bench/run-$i.log | tail -1)
  echo "${i},${csv#READBENCH_CSV,}" >> /tmp/py-read-bench/progress.csv
done
cat /tmp/py-read-bench/progress.csv
```
(Note: port 28080 not 18080 — Cursor IDE holds 18080 on local M4.)

### Out of v0 critical path (deferred to v0.1+)

- Phase 19.5 sharding deployment story
- Phase 17 table aggregation tiered modifiability
- Phase 20 operator catalogue + push/get API audit
- Rust read-bench binary
- Mixed (push+read) bench
- Hetzner read measurements
- Hetzner AX bare-metal validation
- IoPool default formula fix (needs wider hw-class sweep)
</remaining_work>

<decisions_made>
- **Phase 19.4 PASS verdict accepted** — fraud-team 102.8k EPS Apple-M4 / 107.9k Python multi-conn / 104.7k iot=1 — clears 100k v0 ship gate
- **Phase 19.5 sharding deployment moved OUT of v0 critical path** — only relevant if customer needs >100k single-instance
- **Cost-model methodology change** — flamegraph-derived predictions only (memory feedback_cost_model_from_flamegraph)
- **Read-bench harness shipped despite 404 bug** — bug is server-side; harness is correct; runs unmodified once 12-07 lands
- **IoPool=1 confirmed BEST on Apple-M4** (+14.4% avg, zero regressions); 4-IoPool default formula confirmed BROKEN on Hetzner 16-vCPU
- **Plan 12-07 Path B1 recommended** (wire /get into mio fast path) over B2 axum sidecar — per memory project_phase18_no_dual_runtime
- **Beava-vs-Redis claim corrected** to "5-7× faster per instance on multi-feature-per-event workloads" — was 'competitive with Redis'
</decisions_made>

<blockers>
1. **Read-bench measurements blocked by Plan 12-07** — `POST /get` returns 404 on production beava binary. Plan 12-07 scope doc captures full diagnosis + fix paths. Path B1 implementation: 1-2 days.
</blockers>

## Required Reading (in order, before resuming)

1. **`.planning/HANDOFF.json`** — full structured state (this session's deltas)
2. **`.planning/phases/12-server-side-async-push-coalescing/12-07-SCOPE.md`** — Plan 12-07 diagnosis + fix paths (THE thing to plan/execute next)
3. **`.planning/phases/19.4-final-100k-push/19.4-LINUX-PERF-TUNING.md`** — full perf-tuning experiments + IoPool sweeps (M4 + Hetzner)
4. **`.planning/phases/19.4-final-100k-push/19.4-LINUX-VALIDATION.md`** — Hetzner Linux EPYC baseline
5. **`.planning/throughput-baselines.md` § rebaseline 19.4 + Linux EPYC + Python bench sections**
6. **`crates/beava-server/src/feature_query.rs:81-86`** — the unmounted `feature_query_router` (Plan 12-07 target)
7. **`crates/beava-server/src/server.rs:949+`** — mio EventLoop where /get needs to land (Plan 12-07 target)

## Critical Anti-Patterns (do NOT repeat these)

- **Cost-model conjecture from arithmetic** → memory `feedback_cost_model_from_flamegraph`
- **Sub-step attribution error** → decompose flamegraph self-time before predicting lift
- **Stale cost model in multi-plan phases** → re-baseline cost model after each plan lands
- **System-load contamination** → quiet-system protocol; load-filter (drop highest-2-of-7)
- **Verifier conjecture without measurement** → live trace + criterion BOTH required
- **Wrapper-bypass in dispatch refactor** → memory `feedback_dispatch_refactor_enumerate_wrappers`
- **Background agents bailing at 65% context** (NEW) → re-dispatch with tightened scope
- **IoPool formula assumes n_cpus/4 helps** (NEW) → 16-vCPU Linux is the destructive sweet spot

## Infrastructure State

- **Branch:** v2/greenfield at HEAD `6fb83a9`
- **Local M4:** binaries built post-19.4-04 production-mode (`target/release/beava` + `beava-bench-v18`)
- **Hetzner:** full repo at /root/tally/, full build, Python SDK installed, bench artifacts under /tmp/bench-* and /tmp/exp-*
- **Background processes M4:** Cursor IDE on port 18080 (use 28080+ for benches)
- **Background processes Hetzner:** Playwright Chromium killed earlier; Arc + Cursor + Antigravity may still consume CPU
- **Worktrees pending future work:**
  - `.claude/worktrees/phase-12-followup` — 5 of 6 join plans pending
  - `.claude/worktrees/phase-13-followup` — cold-entity GC + perf gate + metric counter wiring
  - Plus accumulated agent worktrees from this session — needs `/gsd-cleanup`
- **Model profile:** quality (all 18 GSD agents on opus per system-table customization)

<context>
Phase 19.4 is now richer than at original pause:
- Cross-platform validated (M4 + Linux EPYC vServer)
- IoPool sensitivity mapped on both platforms (formula bug discovered)
- `--io-threads N` user-facing knob shipped
- Python multi-conn bench reveals Rust bench harness back-pressure
- Beava-vs-Redis comparison rigorous (5-7× per instance on multi-feature events)
- Read story scoped (Plan 12-07) with 3 fix paths and clear recommendation

Next session: pick Plan 12-07 (wire /get into mio) OR Phase 14 (streaming correctness). Both v0-blocking. Plan 12-07 unblocks read benchmarks AND completes Phase 12 PARTIAL — recommended first for marketing-completeness reasons.
</context>

<next_action>
Resume with `/gsd-resume-work` to verify state restoration. Then:

**Recommended path (Plan 12-07):**
1. Read `.planning/phases/12-server-side-async-push-coalescing/12-07-SCOPE.md` (full diagnosis + fix paths)
2. `/gsd-discuss-phase 12-07` (or scoped `/gsd-quick`) to break Path B1 into red-green tasks — ~30 min discuss + plan
3. Execute Plan 12-07 — wire /get into mio HTTP fast path; ~1-2 days work
4. Re-run `python/benches/read_bench.py` unmodified once /get is live — produces Phase 19.4 read baseline
5. Then Phase 14 → 15 → 14.1 → 12-followup → 12.5 → 16 → 13-followup → ship

**Alternative path (Phase 14 first):**
- Skip 12-07; do Phase 14 (silent-data-loss bug fix in agg_windowed) first because it's correctness-critical, not just feature-complete. Defer reads to later. Faster to v0 ship if read benchmarks aren't a marketing priority.

**Cleanup-first option:**
- `/gsd-cleanup` to archive accumulated worktrees from Phase 19.x; gitignore Launch_beava-bench_*.trace/; commit or discard 19.4-02-REMEASUREMENT.md. ~30 min hygiene.
</next_action>
