---
context: multi-phase-discuss-complete
phase: 13.4-13.7 (4 parallel sub-phases)
status: paused-after-discuss
last_updated: 2026-05-04T03:44:12.000Z
session_arc: "Phase 13.0 closure → 4 interactive discuss-phases for 13.4/13.5/13.6/13.7 → pause"
---

# Resume Beava v0 launch — 4 phases ready for plan-phase

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Over-eager --auto delegation | First-pass auto-decided 4 CONTEXTs without user input; user rejected mid-stream and required interactive re-do | advisory | When user requests "fully autonomous" on 4 phases × 4 gray areas = 16+ product decisions, surface honest "this is significant decision delegation; want to confirm before I proceed?" check first. Don't mistake "do it all" for "decide it all." |

## Current State

Phase 13.0 design contract is OFFICIALLY CLOSED (commit `e4ac958`). Phase 13.7.5 pre-OSS polish captured and slotted in ROADMAP (commit `ad552ca`).

**4 phase CONTEXTs ready for plan-phase** (all interactively discussed with user):

- **13.4 engine prep** — `c47c768` + amend `352f049`
- **13.5 Python SDK + bench CLI** — `5312380` + amend `2150105`
- **13.6 TS+Go communicate-only SDKs** — `fe595d1` (RESCOPED)
- **13.7 docs site (integrate into beava-website)** — `bbaa3b3` (RESCOPED)

Next step: run `/gsd-plan-phase 13.4` first (engine prereq), then 13.5/6/7 sequentially or in parallel.

## Completed Work (this session arc)

1. Phase 13.0 closure landed (commit `e4ac958`) — 16 plans across 3 waves; ADR-001/002/003; 158 artifacts; 68 Python tests; 9 vertical demos; wire fixtures
2. Phase 13.7.5 captured + slotted (commits `3f94309`, `ad552ca`) — pre-OSS polish (comment audit + test coverage)
3. Tried --auto CONTEXTs for 13.4-13.7 (commit `726598e`); user rejected; reverted (`42d431f`)
4. Interactive discuss-phase × 4:
   - 13.4: 4 user-locked decisions (strict diff matrix; pure in-RAM persistence; OP_RESET full-clear gated on test_mode 3-ways; surgical allowlist permit)
   - 13.5: 4 user-locked decisions (mypy --strict + Any escapes; in-package demos ~3MB; subcommand+long-form CLI; flat module structure) + fsync bench mode amendment + cross-amendment from 13.4 D-03 (bv.App test_mode kwarg)
   - 13.6: 4 user-locked decisions + **MAJOR SCOPE CHANGE: communicate-only** (~600 LOC each, down from ~1800; pipeline DSL Python-only) + GitHub org = beava-dev (NOT beava-io); monorepo with sdk/go subdir
   - 13.7: 4 user-locked decisions + **MAJOR SCOPE REDUCTIONS: integrate into existing beava-website + Pagefind** (NOT MkDocs spin-up); vertical guides DEFERRED to user-authored interactive follow-up

## Remaining Work

1. **`/gsd-plan-phase 13.4`** — RUN FIRST (engine prereq; 13.5/6/7 integration tests gate on this). Estimated ~8-10 plans across 3 waves.
2. `/gsd-plan-phase 13.5` — Python SDK rewrite + beava bench CLI (~10-12 plans across 3 waves)
3. `/gsd-plan-phase 13.6` — TS+Go communicate-only SDKs (~6-8 plans, smaller post-rescope)
4. `/gsd-plan-phase 13.7` — docs site integration (~3-4 plans, smaller post-rescope)
5. After all 4 plans: drive execute-phases. 13.4 first (prereq), then 13.5/6/7 in parallel via `/gsd-manager` terminals. Each is 2-5 hours wall-clock; total ~10+ hours of agent execution work.
6. After 13.4-13.7 ALL CLOSED: Phase 13.7.5 (pre-OSS polish — comment audit + test coverage)
7. After 13.7.5: Phase 13.8 (packaging + GA tag = v0.0.0 ship)

## Decisions Made (this session)

### Phase 13.4 (engine)
- D-01 force=True diff matrix = STRICT (rename, type-change, op removal, agg removal, window-change, key-cols change all = destructive; categorized lists payload)
- D-02 In-memory persistence = PURE in-RAM (no WAL); state lost on restart; embed mode + tests use case
- D-03 OP_RESET = full clear (state + registry); GATED on test_mode (3 ways: BEAVA_TEST_MODE env var, Server::new(Config{test_mode:true}), bv.App(test_mode=True) SDK kwarg)
- D-04 Architectural-test allowlist = surgical permit (edit FORBIDDEN_PATTERNS in place; add new test phase13_4_table_derivation_allowed.rs)

### Phase 13.5 (Python SDK + bench)
- D-01 mypy = `--strict` clean overall + documented Any escapes for AST-style helpers (`bv.col(...)` chains, `bv.lit(...)`)
- D-02 Demo datasets = in-package (`python/beava/demos/{adtech,fraud,ecommerce}/`; ~3 MB pip install bump)
- D-03 `beava bench` CLI = subcommand + long-form flags (`beava bench throughput --workload=fraud`); **4 modes: throughput/mixed/memory/fsync** (fsync added per user directive — measures acks=all per-push p50/p99/p999 latency)
- D-04 Module structure = keep current flat (`bv.App`, `bv.event`, etc.; `beava.test`, `beava.cli` submodules)
- D-05 (cross-amendment from 13.4): `bv.App(test_mode=True)` SDK kwarg + `beava.test.fixture()` defaults

### Phase 13.6 (TS+Go communicate-only)
- **MAJOR SCOPE CHANGE per user directive: pipeline DSL is Python-only; TS+Go are wire-thin communicate-only SDKs** (~600 LOC each, down from ~1800). API surface = App + register(jsonDescriptors) + push/pushSync/get/batchGet/reset/ping/close. NO @bv.event/@bv.table/bv.col/bv.lit/53 op helpers/chained DSL in TS/Go.
- D-01 TS bundle = ESM-only (Node 18+ LTS baseline; ~200 LOC less plumbing than dual)
- D-02 Go module + repo layout = **`github.com/beava-dev/beava` monorepo** with `sdk/go/` subdir + TS at `sdk/typescript/` (npm `@beava/sdk`); GitHub org = **`beava-dev`** (NOT `beava-io` as ROADMAP/docs say — needs patching in 13.6 closure)
- D-03 Cross-SDK conformance = single Python orchestrator at `python/tests/conformance/test_cross_sdk.py` (drives all 3 SDKs against same scenario via subprocess; ~200 LOC)
- D-04 Doc patches in 13.6 closure (rewrite `docs/sdk-api/typescript.md` + `go.md` + `shared.md` to scope down; patch ROADMAP §13.6 entry)

### Phase 13.7 (docs site)
- **MAJOR SCOPE REDUCTION per user directive: integrate into existing `beava-website/` + Pagefind** (NOT spin up MkDocs Material from scratch). Markdown→HTML conversion glue (~200-400 LOC) renders `docs/*.md` into `beava-website/project/docs/<...>/index.html`; extends `build-search.mjs` for Pagefind index.
- D-01 SSG = integrate-into-existing (above)
- D-02 Visual identity = use existing `beava-design-system/` tokens (colors_and_type.css, ui_kits) — zero new design work
- D-03 **Vertical guides DEFERRED** to user-authored follow-up (will be interactive — likely v0.1+ browser-WASM via `@beava/browser`); Phase 13.7 ships `/docs/` only; `/guide/` pages stay as existing or get "coming soon" stubs
- D-04 Deploy = Cloudflare Pages

## Blockers

None active. Two human-action items NOT blocking 13.4-13.7 plan-phase:
- Decide if `tally` repo should be renamed to `beava` for v0 launch (Phase 13.8 territory)
- Verify `beava-dev` GitHub org exists or create before Phase 13.8 GA tag

## Required Reading (in order)

1. `.planning/STATE.md` — current critical path
2. `.planning/ROADMAP.md` lines 615-907 — Phase 13 umbrella + sub-phases (NOTE: §13.6 entry says `beava-io` org; reality is `beava-dev` per user — patch in 13.6 closure)
3. `.planning/phases/13.4-engine-prep-wire-spec/13.4-CONTEXT.md` — engine decisions
4. `.planning/phases/13.5-python-sdk-and-bench-cli/13.5-CONTEXT.md` — Python SDK + bench decisions
5. `.planning/phases/13.6-typescript-go-sdks/13.6-CONTEXT.md` — TS+Go communicate-only decisions
6. `.planning/phases/13.7-docs-site-beava-dev/13.7-CONTEXT.md` — docs site integration decisions
7. `.planning/decisions/ADR-001-bv-table-partial-overturn.md`
8. `.planning/decisions/ADR-002-polars-op-rename.md`
9. `.planning/decisions/ADR-003-global-aggregation-and-bv-lit.md`
10. CLAUDE.md — TDD discipline + mio-only invariant + events-only invariant + memory governance invariant

## Infrastructure State

- Branch: `v2/greenfield`
- Last commit: `bbaa3b3 docs(13.7): capture phase context — 4 decisions + 2 scope reductions`
- Working tree: clean (all 4 CONTEXTs + 13.7.5 idea doc + ROADMAP entry committed)
- Worktrees: ~30 stale `agent-aXXX` worktrees from prior agent runs (locked; not actively in use; not blocking but could be cleaned with `git worktree prune` + manual unlock if cleanup desired)
- Existing dirs to be aware of:
  - `beava-website/` — hand-rolled static HTML site with Pagefind (target for 13.7)
  - `beava-design-system/` — shared design tokens (colors_and_type.css, ui_kits) — used by 13.7
  - `python/beava/` — current Python SDK (DELETE/KEEP target per 13.5)
  - `python/tests/v0/` — 68 high-volume integration tests from Plan 13.0-16; gate for 13.4 + 13.5 acceptance
  - `examples/{python,typescript,go}/` — 9 vertical demos from Plan 13.0-14; gate for 13.4 + 13.5 + 13.6 acceptance
  - `crates/beava-bench/src/bin/{beava-bench-v18,beava-bench-v2}.rs` — promote to `beava bench` subcommand per 13.5 D-03

## Context Notes (for fresh agent)

- Session was VERY long (Phase 13.0 closure → 4 CONTEXTs). Context warning at 67% when paused.
- User chose "Option B" (fully autonomous chain) early in this session arc but corrected mid-stream when --auto delegation went too deep on product-shaping decisions. Reverted at `42d431f`.
- All 4 CONTEXTs are interactively discussed; every product-shaping decision was user-locked. Don't second-guess them in plan-phase.
- The 16+ original autonomous decisions are still in git history at commit `726598e` (reverted but preserved) for reference if needed.
- Two MAJOR scope reductions vs original ROADMAP entries:
  - 13.6 went from ~3600 LOC (TS+Go full DSL) → ~1200 LOC (TS+Go communicate-only)
  - 13.7 went from "spin up MkDocs Material from scratch" → "integrate into existing beava-website/ + Pagefind" + vertical guides DEFERRED
- The `beava-dev` GitHub org name + monorepo layout is locked but ROADMAP §13.6 still says `beava-io` and `separate repo` — Phase 13.6 closure patches this.

## Next Action

```
/clear
/gsd-plan-phase 13.4
```

Run `/gsd-plan-phase 13.4` first (engine prereq; 13.5/6/7 integration tests gate on this).

After 13.4 plan lands, run `/gsd-plan-phase 13.5`, `/gsd-plan-phase 13.6`, `/gsd-plan-phase 13.7` sequentially (each fast in plan-phase mode).

After all 4 plans land, drive execute-phases. 13.4 first (prereq), then 13.5/6/7 in parallel via `/gsd-manager` terminals.

To resume cleanly: `/gsd-resume-work` (reads `.planning/HANDOFF.json` + this file).
