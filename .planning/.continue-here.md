---
context: between-phases
phase: v0-OSS-launch
task: discuss+plan-handoff
total_tasks: 4-phases-remaining
status: paused-between-phases
last_updated: 2026-05-04T14:48:16.342Z
---

# Pause handoff — v0 OSS launch, between executed and remaining phases

User taking over the discuss + plan steps for the 4 remaining v0 OSS phases. Resume by running `/gsd-discuss-phase 13.5.1` (or whichever phase you choose to discuss first).

## What just shipped (this session)

| Phase | Verdict | Notes |
|---|---|---|
| **13.4** Engine prep | ✅ PASS-WITH-WARN | 10 plans / 41 commits. Microbench cold-key −32.7%. small/tcp throughput −0.4% PASS. Open WARN: HTTP-transport regression −24% to −32% on small/medium/large/http (deferred v0.0.x). |
| **13.5** Python SDK + bench CLI | 🟡 DONE-WITH-DEFICIT | 12 plans / 32 commits across base + 2 continuations. Workspace GREEN (clippy/fmt/cargo test/mypy --strict/145 internal tests). **Open deficit: 0/68 v0 acceptance tests** — Http/Tcp/EmbedTransport.send_* stubbed to NotImplementedError; mypy passed via MagicMock. Plus @bv.table empty-annotation AttributeError. Phase 13.5.1 fix-up captured. |
| **13.6** TS + Go SDKs | ✅ PASS | 8 plans / 35 commits. TS 19/19 + 1 skip + Go all + 1 skip GREEN. Open: conformance harness payload bug (sends top-level `kind:"table"` instead of derivation). |
| **13.7** Docs site | ✅ PASS | 4 plans / 4 commits. 86 docs pages, 93 Pagefind fragments, 0 broken links. Open: Cloudflare Pages dashboard auth + DNS for beava.dev (one-time manual user action). |

HEAD on `v2/greenfield`: `b8fd998a` (post-handoff WIP commit). Session committed ~129 commits across the four phases plus orchestration housekeeping.

## What's next — 4 phases awaiting discuss + plan

| # | Phase | Scope | Est. | Open decisions |
|---|---|---|---|---|
| 1 | **13.5.1** Transport fix-up | Wire `Http/Tcp/EmbedTransport.send_*` against Phase 13.4 engine; harden `@bv.table` empty-annotation. Greens up the 68 v0 acceptance tests. | ~2-3 days | HTTP client choice (`urllib.request` vs `requests` vs `httpx`); TCP framing alignment; embed-spawn protocol; decorator empty-annotation behavior |
| 2 | **13.7.5** Pre-OSS code polish | Comment audit (per-component AI-slop removal) + test coverage matrix + gap fill. Idea doc at `.planning/ideas/phase-13.7.5-pre-oss-polish.md`. 13 plans across 2 workstreams. | ~1-2 weeks | Confirm KEEP/DELETE checklist + per-component plan ordering |
| 3 | **13.7.6** Pre-OSS repo polish | Security audit + lint sweep (clippy, audit, deny, OWASP via /cso) + commit-path sanitization (strip ~30+ Co-Authored-By Claude trailers + 🤖 markers + decide history shape + branch rename + optional repo rename) + public-facing files (LICENSE/README/CONTRIBUTING/SECURITY/CODE_OF_CONDUCT/CHANGELOG/.github templates). Idea doc at `.planning/ideas/phase-13.7.6-pre-oss-security-and-commit-path.md`. 24 plans. | ~3-5 days | **D-01** history shape (4 options; default rec: keep history + strip `.planning/` via filter-repo); **D-04** subject rewrite scope (trailer-only vs also rewrite Claude refs in subjects — note: legitimate `CLAUDE.md` filename references should stay); repo rename `tally → beava` y/n/defer; author email on rewritten history; `CLAUDE.md` filename keep / `AGENTS.md` / `CONVENTIONS.md` |
| 4 | **13.8** Packaging + GA tag | PyPI multi-arch wheels × 3 + npm `@beava/sdk` + Go module + Docker Hub multi-arch + GitHub Releases × 3 + CI green + v0.0.0 tag + marketing assets. | ~5-7 days | (locks during 13.8 discuss) |

**Sequencing:** 13.5.1 → 13.7.5 → 13.7.6 → 13.8. Total ~3-5 weeks one-engineer; ~30% reducible with worktrees on 13.7.5 comment-audit plans.

## Manual user actions (parallel-able with discuss/plan)

- Cloudflare Pages dashboard auth + DNS A/CNAME for beava.dev / www.beava.dev (steps in `beava-website/README.md § Deploy`)
- Verify or create `beava-dev` GitHub org for Phase 13.6 npm publish + Go module + 13.8 release homes

## v0.0.x follow-ups (post-GA, non-blocking)

- HTTP-transport throughput regression (13.4)
- Conformance harness payload bug (13.6)
- SDK error-pass-through e2e tests (parse_error column messages, schema_mismatch through SDK layer, cast() defensive null behavior, where=null-excludes-event)
- `phase5_smoke::sc6_aggregation_on_table_rejected` 409→400 (13.4 D-04 surfaced)
- `phase2_5_smoke::criterion_6_pipelined_registers_return_in_order` parallel flake (pre-existing)
- OP_BATCH_GET wire-spec doc cohesion (`{table, entity_id}` vs `{table, key}`)
- OP_BATCH_GET DoS cap on `requests[]`
- Duplicate `/docs/quickstart` paths in beava-website
- Hero-page combined latency/throughput/memory headlines

## Required reading (in order) for resuming agent

1. `.planning/HANDOFF.json` — structured machine-readable state (this same info)
2. `.planning/STATE.md` § frontmatter + § session-continuity comments — full execution narrative
3. `.planning/ROADMAP.md` lines 55-65 — 13.x table with current verdicts
4. `.planning/phases/13.5-python-sdk-and-bench-cli/STATE-IN-WORKTREE.md` + `13.5-11-SUMMARY.md` — transport-impl deficit detail (input for 13.5.1 discuss)
5. `.planning/ideas/phase-13.7.5-pre-oss-polish.md` — 13.7.5 scope + KEEP/DELETE heuristic + 13-plan structure
6. `.planning/ideas/phase-13.7.6-pre-oss-security-and-commit-path.md` — 13.7.6 scope + 4 workstreams + decision matrix
7. `CLAUDE.md` — project conventions; especially TDD discipline (red→green for code), perf gates, mio-only / events-only / memory-governance invariants
8. Memory: `feedback_no_ai_attribution_in_commits` — never add `Co-Authored-By: Claude` or 🤖 markers to commits going forward

## Critical anti-patterns (avoid these)

| Pattern | What it is | Severity | Prevention |
|---|---|---|---|
| Worktree-init on stale branch | Runtime bug — Agent tool's `isolation="worktree"` sometimes initializes the new branch from `e9ace7c` (Phase 44 fork-replica history) instead of `v2/greenfield` HEAD. Hit 3× this session. | advisory | Every worktree-isolated agent prompt should include the ancestry-check + `git reset --hard v2/greenfield` snippet at the top (template in earlier dispatched prompts). |
| MagicMock silently passing transport tests | Phase 13.5 internal tests used MagicMock as the Transport, so `send_*` stubs returning NotImplementedError never surfaced — until Plan 11 ran the v0 acceptance tests against a real engine and 0/68 passed. | blocking | For 13.5.1 Plan tasks: integration tests MUST use a real Beava engine (embed mode is fine — Persistence::Memory + ServerV18Config { test_mode: true }) — never `MagicMock`. Add this to the 13.5.1 plan-checker contract. |
| Subagent nesting | Subagents (general-purpose, gsd-executor) cannot dispatch their own Task subagents — Claude Code blocks subagent nesting. | blocking | Phase-driver agents must execute plans inline (read execute-plan.md playbook directly), not spawn gsd-executor sub-sub-agents. The gsd-execute-phase Skill's Task-dispatching pattern only works at the TOP-LEVEL orchestrator (the main session). |
| Plan-vs-reality file mismatch | Phase 13.7 PLAN.md referenced React infra files (DocsSidebar.jsx, build-search.mjs, package.json) that didn't exist on `v2/greenfield`. Agent built static-HTML + Pagefind from scratch instead. | advisory | discuss-phase agents should grep that referenced file paths actually exist on the target branch before drafting plans. |
| AI attribution in commits | System-prompt git template adds `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` and 🤖 markers; user wants these stripped. ~30+ historical instances exist. | advisory | Memory `feedback_no_ai_attribution_in_commits` saved 2026-05-04. Going forward: never add. Retroactive: 13.7.6 D-04 strips via `git filter-repo --message-callback`. |

## Infrastructure state

- Branch: `v2/greenfield` at `b8fd998a` (post-handoff WIP commit). ~129+ commits ahead of `f229755` (session start).
- Worktrees: 30+ stale `worktree-agent-*` worktrees in `.claude/worktrees/`. Will be cleaned up in 13.7.6 Plan 12.
- Workspace gates: `cargo test --workspace --features testing` GREEN except 1 pre-existing flake (`phase2_5_smoke::criterion_6_pipelined_registers_return_in_order`). cargo clippy + cargo fmt clean. mypy --strict GREEN. 145 internal Python tests GREEN. **0/68 v0 acceptance tests** (Phase 13.5.1 fix needed).
- Pre-existing untracked WIP in `beava-design-system/` and `beava-website/` (visible in initial session git status — user's WIP from before; not touched).
- 13.7-merge backups in `beava-website/`: 4 `.pre-13.7-merge-backup` files + `_pagefind.pre-13.7-merge-backup/` directory — preserved for user diff review post-merge.

## Decisions made this session (locked into ROADMAP/STATE)

1. Renumber transport-fix bracket from 13.7.5 → **13.5.1** (it's a 13.5 fix-up, not pre-OSS work). Original 13.7.5 (captured 2026-05-03) keeps its number for comment audit + test coverage.
2. Capture **13.7.6** as new pre-OSS repo-polish phase (security audit + commit-path sanitization + public-facing files). Sits between 13.7.5 and 13.8.
3. v0 OSS critical path: **13.5.1 → 13.7.5 → 13.7.6 → 13.8**. Sequential.
4. Total wall-clock estimate: **~3-5 weeks** one-engineer.
5. **No AI attribution in commits** — going forward + retroactive scrub in 13.7.6 D-04. Memory saved.

## Next action

Resume with `/gsd-resume-work` to load this context, then `/gsd-discuss-phase 13.5.1` (recommended first — smallest, unblocks the 68 v0 acceptance tests).

Alternative: discuss any of the other 3 phases first if you want to batch decisions before any execution.
