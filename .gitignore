# Rust
target/
*.rs.bk

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
*.whl

# Plan 30-01: maturin develop drops the built extension into the source tree
# so pytest against the symlinked python/ layout can find it. The compiled
# artefact is regenerated on every `maturin build` / `maturin develop`.
python-native/python_src/tally/_native*.so

# Tally-specific
.planning/
.thoughts/
deck/
*.snapshot*
events/
tally.snapshot*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.*

# Benchmark results
benchmark/**/results/

# OS
.DS_Store
Thumbs.db

# Claude Code: ignore everything except the shipped /tally skill
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/tally/

# MCP server config (personal, points to local gbrain instance)
.mcp.json
