{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://beava.dev/schemas/ping.response.schema.json",
  "title": "OP_PING response body",
  "description": "server_version is the beava server's semantic version (e.g., '0.0.0' for v0). registry_version is a monotonic counter that increments on every successful register; clients use it as a cache key for feature schemas.",
  "type": "object",
  "properties": {
    "server_version": { "type": "string" },
    "registry_version": {
      "type": "integer",
      "minimum": 0
    }
  },
  "required": ["server_version", "registry_version"],
  "unevaluatedProperties": false
}
